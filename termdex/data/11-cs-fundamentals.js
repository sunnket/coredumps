TD.add("cs-fundamentals", [

{t:"Algorithm", d:"A finite, unambiguous sequence of steps that transforms an input into a desired output.", l:"core", g:["fundamentals"],
 b:["An algorithm must terminate, be unambiguous at every step, and produce the correct result for every valid input. It is independent of any programming language — the same algorithm can be written in Python, C or on paper.",
    "Algorithms are judged on correctness first and efficiency second. Efficiency is expressed asymptotically, in terms of how time and memory grow with input size, because that is what determines whether something that works on a thousand records still works on a hundred million."],
 k:["Must terminate, be unambiguous, and be correct for all valid inputs.",
    "Language-independent — the idea, not the implementation.",
    "Judged by asymptotic time and space growth.",
    "Correctness first; a fast wrong answer is worthless."],
 dg:"bigo",
 r:["Data Structure","Big O Notation","Time Complexity","Sorting Algorithm"]},

{t:"Data Structure", d:"A way of organising data in memory so that particular operations become efficient.", l:"core", g:["fundamentals"],
 b:["Every data structure trades one operation's speed for another's. An array gives O(1) random access and O(n) insertion in the middle; a linked list reverses that. A hash table gives O(1) lookup by key but no ordering; a balanced tree gives O(log n) lookup and keeps everything sorted.",
    "Choosing the right structure is usually a larger performance win than optimising code, because it changes the complexity class rather than the constant factor. The question to ask is always: which operations does this code perform most often?"],
 k:["Every structure trades one operation's cost for another's.",
    "Choose based on which operations dominate your workload.",
    "The right structure changes complexity class, not just constants.",
    "Arrays, hash tables, trees and graphs cover the vast majority of cases."],
 r:["Array","Hash Table","Linked List","Tree","Big O Notation"]},

{t:"Big O Notation", d:"A notation describing how an algorithm's cost grows as its input grows, ignoring constants and lower-order terms.", l:"core", g:["fundamentals","complexity"],
 b:["Big O gives an upper bound on growth. O(1) is constant, O(log n) logarithmic, O(n) linear, O(n log n) linearithmic, O(n²) quadratic, O(2ⁿ) exponential. Constants and lower-order terms are dropped because they stop mattering as n grows.",
    "The practical value is predicting behaviour at scale. At a million elements, an O(log n) algorithm takes about 20 steps and an O(n²) one takes a trillion. That difference is the reason to care about which structure you chose, and it is invisible in a test with fifty rows."],
 k:["Describes growth rate, not actual runtime.",
    "Constants dropped: O(2n) is O(n), O(n² + n) is O(n²).",
    "At n = 1,000,000: log n ≈ 20 steps, n² ≈ 10¹² steps.",
    "Big O is worst case; Θ is tight and Ω is a lower bound."],
 x:{lang:"python", code:'# O(n^2) — nested loop over the same collection\nfor a in items:\n    for b in items:\n        if a + b == target: return (a, b)\n\n# O(n) — one pass with a hash set\nseen = set()\nfor a in items:\n    if target - a in seen: return (target - a, a)\n    seen.add(a)'},
 dg:"bigo",
 r:["Time Complexity","Space Complexity","Algorithm","Amortised Analysis"]},

{t:"Time Complexity", d:"How an algorithm's running time grows with the size of its input.", l:"core", g:["fundamentals","complexity"],
 b:["Count the operations that dominate as input grows. A single loop over n items is O(n); a nested loop is O(n²); halving the search space each step is O(log n). Sequential blocks add (and the largest dominates), nested blocks multiply.",
    "Distinguish best, average and worst case. Quicksort is O(n log n) on average and O(n²) in the worst case, which is why implementations randomise the pivot. For a service, the worst case is what determines your tail latency."],
 k:["Nested loops multiply; sequential blocks add and the largest dominates.",
    "Halving the problem each step gives O(log n).",
    "Best, average and worst case can differ dramatically.",
    "Worst case is what sets your p99 latency."],
 dg:"bigo",
 r:["Big O Notation","Space Complexity","Algorithm","Amortised Analysis"]},

{t:"Space Complexity", d:"How much additional memory an algorithm needs as its input grows.", l:"core", g:["fundamentals","complexity"],
 b:["Space complexity counts auxiliary memory — what the algorithm allocates beyond the input itself. An in-place sort is O(1) auxiliary space; merge sort needs O(n) for its temporary arrays; a recursive function uses O(depth) stack space even if it allocates nothing explicitly.",
    "Time and space usually trade off. Memoisation buys speed with memory; recomputing values buys memory with time. Which is right depends on which resource is scarce."],
 k:["Counts auxiliary memory, not the input.",
    "Recursion consumes stack proportional to its depth.",
    "In-place means O(1) auxiliary space.",
    "Memoisation is the classic time-for-space trade."],
 r:["Time Complexity","Big O Notation","Memoisation","Recursion"]},

{t:"Amortised Analysis", d:"Averaging the cost of an operation over a sequence, when occasional operations are expensive but rare.", l:"advanced", g:["complexity","analysis"],
 b:["A dynamic array appends in O(1) most of the time, but occasionally must allocate a larger buffer and copy everything, which is O(n). Because the array doubles in size, those expensive copies happen rarely enough that the average cost per append is O(1) — the amortised cost.",
    "This is different from average-case analysis: amortised is a guarantee over any sequence of operations, not a probabilistic statement. Hash table insertion and union-find operations are also analysed this way."],
 k:["Averages cost across a sequence, guaranteed rather than probabilistic.",
    "Dynamic array append is amortised O(1) because of doubling.",
    "Different from average case, which assumes an input distribution.",
    "A single operation can still be slow — matters for latency-sensitive code."],
 r:["Big O Notation","Array","Hash Table","Time Complexity"]},

{t:"Array", d:"A fixed-size block of contiguous memory holding elements of the same type, accessed by index.", l:"core", g:["data-structure"],
 b:["Because elements sit next to each other and are the same size, the address of element i is a simple calculation — base + i × size — giving O(1) random access. Contiguity also means arrays are cache-friendly: reading one element pulls its neighbours into cache, so iteration is very fast.",
    "The costs are that size is fixed at creation and that inserting or deleting in the middle requires shifting everything after it, which is O(n). Dynamic arrays (Python lists, Java ArrayList, C++ vector) hide the resizing but keep the shifting cost."],
 k:["O(1) access by index; O(n) insert or delete in the middle.",
    "Contiguous memory makes iteration cache-friendly and fast.",
    "Fixed size — dynamic arrays reallocate and copy to grow.",
    "The default choice unless you specifically need something else."],
 dg:"array-list",
 r:["Linked List","Data Structure","Hash Table","Amortised Analysis"]},

{t:"Linked List", d:"A sequence of nodes where each node holds a value and a pointer to the next.", l:"core", g:["data-structure"],
 b:["Nodes live anywhere in memory and are joined by pointers, so inserting or deleting is O(1) once you hold a reference to the position — just rewire two pointers, with no shifting. Growth requires no reallocation.",
    "The price is that finding position i requires walking from the head, which is O(n), and that scattered nodes defeat the CPU cache. In practice arrays outperform linked lists for most workloads despite better asymptotic insertion, because cache behaviour dominates at realistic sizes."],
 k:["O(1) insert and delete given a node reference; O(n) to find one.",
    "No contiguous memory needed, so no reallocation on growth.",
    "Poor cache locality — usually slower than arrays in practice.",
    "Underlies stacks, queues and hash table collision chains."],
 dg:"array-list",
 r:["Array","Stack","Queue","Data Structure","Pointer"]},

{t:"Stack", d:"A last-in-first-out collection where you can only add to and remove from the top.", l:"core", g:["data-structure"],
 b:["Push adds to the top, pop removes from the top, peek reads it. Both mutations are O(1). The restriction is the point: it exactly models nested structure, which is why it appears in function call management, expression evaluation, bracket matching, undo history and depth-first traversal.",
    "The call stack is a literal stack: each function call pushes a frame with its local variables, and returning pops it. Infinite recursion exhausts it, producing a stack overflow."],
 k:["LIFO: push, pop and peek all operate on the top, all O(1).",
    "The call stack is exactly this structure.",
    "Natural fit for nesting: brackets, undo, expression parsing.",
    "DFS uses a stack, either explicitly or via recursion."],
 dg:"stack-queue",
 r:["Queue","Depth-First Search","Recursion","Stack and Heap Memory"]},

{t:"Queue", d:"A first-in-first-out collection where items are added at one end and removed from the other.", l:"core", g:["data-structure"],
 b:["Enqueue adds at the back, dequeue removes from the front, both O(1) with the right implementation. It models fairness and ordering: request handling, print jobs, task scheduling, and breadth-first traversal all need the earliest item served first.",
    "A deque (double-ended queue) allows adding and removing at both ends and covers both stack and queue use cases. A circular buffer implements a fixed-capacity queue without any allocation."],
 k:["FIFO: enqueue at the back, dequeue at the front, both O(1).",
    "BFS uses a queue; DFS uses a stack.",
    "A deque supports both ends and subsumes stack and queue.",
    "Message queues apply the same semantics across a network."],
 dg:"stack-queue",
 r:["Stack","Breadth-First Search","Message Queue","Data Structure"]},

{t:"Hash Table", a:"Hash Map / Dictionary", d:"A structure that maps keys to values using a hash function, giving average O(1) lookup.", l:"core", g:["data-structure"],
 b:["A hash function converts a key into an array index. Storing and retrieving therefore takes constant time on average, regardless of how many entries exist — which is why dictionaries, sets, caches, database indexes and symbol tables are all built on them.",
    "Two keys can hash to the same slot, a collision, handled by chaining (a list per bucket) or open addressing (probe for the next free slot). Worst case degrades to O(n) if everything collides, and the table must resize when it fills, which is an O(n) rehash amortised across insertions."],
 k:["Average O(1) insert, lookup and delete; worst case O(n).",
    "Collisions resolved by chaining or open addressing.",
    "Keys must be hashable and, in most languages, immutable.",
    "No ordering — use a tree structure if you need sorted keys."],
 x:{lang:"python", code:'counts = {}\nfor word in text.split():\n    counts[word] = counts.get(word, 0) + 1     # O(1) average per word\n\n# membership testing: O(1) in a set, O(n) in a list\nseen = set(large_list)\nif candidate in seen: ...'},
 dg:"hash-table",
 r:["Hash Collision","Array","Set","Index","B-Tree"]},

{t:"Hash Collision", d:"When two different keys hash to the same slot in a hash table.", l:"intermediate", g:["data-structure","concept"],
 b:["Collisions are unavoidable — you are mapping an unbounded key space into a finite array. Separate chaining stores a list at each bucket; open addressing probes for the next free slot. Both are O(1) on average while the load factor stays low.",
    "Adversarial collisions are a real attack: if an attacker can predict your hash function, they can craft inputs that all hash to one bucket, turning O(1) lookups into O(n) and taking the service down. Languages now randomise their hash seeds per process for this reason."],
 k:["Unavoidable — the pigeonhole principle guarantees them.",
    "Chaining and open addressing are the two resolution strategies.",
    "Load factor above ~0.75 triggers a resize in most implementations.",
    "Hash flooding is a genuine DoS vector; seeds are randomised to prevent it."],
 dg:"hash-table",
 r:["Hash Table","Hashing","Denial of Service","Amortised Analysis"]},

{t:"Set", d:"A collection of unique elements with fast membership testing.", l:"core", g:["data-structure"],
 b:["A set stores each value at most once and answers is this present in O(1) average time, usually because it is a hash table without values. Set operations — union, intersection, difference — map directly onto common data problems.",
    "The single most common performance fix in beginner code is replacing `if x in some_list` (O(n)) with `if x in some_set` (O(1)). Inside a loop that turns quadratic behaviour into linear."],
 k:["Unique elements, O(1) average membership testing.",
    "`x in set` is O(1); `x in list` is O(n).",
    "Union, intersection and difference are built-in operations.",
    "Ordered set variants use a tree and give O(log n) with sorting."],
 r:["Hash Table","Array","Data Structure"]},

{t:"Tree", d:"A hierarchical structure of nodes where each node has one parent and any number of children, with no cycles.", l:"core", g:["data-structure"],
 b:["Trees model hierarchy: file systems, organisation charts, the DOM, JSON documents, parse trees, category taxonomies. A tree with n nodes has exactly n−1 edges and exactly one path between any two nodes.",
    "The key property is height. A balanced tree has height O(log n), so operations that descend from the root are logarithmic. A degenerate tree — every node with one child — has height O(n) and behaves like a linked list."],
 k:["One root, one parent per node, no cycles; n nodes have n−1 edges.",
    "Balanced height is O(log n); degenerate height is O(n).",
    "Models any hierarchy: filesystems, DOM, JSON, parse trees.",
    "Traversals: pre-order, in-order, post-order and level-order."],
 dg:"binary-tree",
 r:["Binary Tree","Binary Search Tree","Graph","Heap","Trie"]},

{t:"Binary Tree", d:"A tree in which every node has at most two children.", l:"core", g:["data-structure"],
 b:["The two-child restriction makes traversal and reasoning simple and is the basis for search trees and heaps. Three depth-first traversals matter: in-order (left, node, right), pre-order (node, left, right) and post-order (left, right, node), plus breadth-first level-order traversal.",
    "A complete binary tree with n nodes has height ⌊log₂ n⌋, which is why operations on balanced binary trees are logarithmic. In-order traversal of a binary search tree yields sorted output."],
 k:["At most two children per node: left and right.",
    "In-order, pre-order and post-order are the three DFS traversals.",
    "Level-order traversal uses a queue, not recursion.",
    "Complete tree height is ⌊log₂ n⌋."],
 dg:"binary-tree",
 r:["Binary Search Tree","Tree","Heap","Depth-First Search"]},

{t:"Binary Search Tree", a:"BST", d:"A binary tree where every left descendant is smaller than its node and every right descendant is larger.", l:"core", g:["data-structure"],
 b:["The ordering invariant means searching is a sequence of comparisons that halves the remaining space, giving O(log n) search, insert and delete on a balanced tree. In-order traversal produces sorted output for free.",
    "The catch is that inserting already-sorted data produces a degenerate tree of height n, and every operation becomes O(n). Self-balancing variants — AVL, red-black — restructure on insertion to guarantee logarithmic height, and are what standard library ordered maps actually use."],
 k:["Left subtree smaller, right subtree larger — the invariant enables the search.",
    "O(log n) when balanced, O(n) when degenerate.",
    "Sorted insertion order produces the worst case.",
    "AVL and red-black trees self-balance to guarantee O(log n)."],
 dg:"binary-tree",
 r:["Binary Tree","Balanced Binary Tree","Binary Search","B-Tree"]},

{t:"Balanced Binary Tree", d:"A binary search tree that restructures itself on modification to keep its height logarithmic.", l:"advanced", g:["data-structure"],
 b:["AVL trees keep the height difference between any node's subtrees at most one, giving very strict balance and fast lookups at the cost of more rotations on insertion. Red-black trees allow looser balance with fewer rotations, which makes them better for insert-heavy workloads.",
    "This is what standard library ordered containers use: C++ `std::map`, Java `TreeMap`. B-trees generalise the idea to wide nodes matched to disk pages, which is why databases use them for indexes."],
 k:["Rotations restore balance after insertion and deletion.",
    "AVL is more strictly balanced — faster reads, slower writes.",
    "Red-black trees are the usual standard library choice.",
    "B-trees extend the concept to disk-based storage."],
 r:["Binary Search Tree","B-Tree","Tree","Index"]},

{t:"Heap", d:"A tree where every parent compares favourably to its children, giving constant-time access to the minimum or maximum.", l:"core", g:["data-structure"],
 b:["In a min-heap every parent is smaller than its children, so the smallest element is always at the root. Insertion and removal are O(log n) as elements sift up or down; reading the extreme is O(1). It is almost always stored as an array, with a node at index i having children at 2i+1 and 2i+2.",
    "This makes it the natural implementation of a priority queue, and it powers heapsort, Dijkstra's algorithm, task schedulers and top-k selection."],
 k:["Root holds the extreme value; O(1) to read, O(log n) to remove.",
    "Stored as an array — no pointers needed.",
    "Only the root ordering is guaranteed; siblings are unordered.",
    "Top-k in O(n log k) using a heap of size k."],
 x:{lang:"python", code:'import heapq\n\nheap = []\nfor score, item in stream:\n    heapq.heappush(heap, (score, item))\n    if len(heap) > 10:\n        heapq.heappop(heap)      # keeps the 10 largest, O(n log k)\n\ntop10 = sorted(heap, reverse=True)'},
 r:["Priority Queue","Tree","Sorting Algorithm","Dijkstra's Algorithm"]},

{t:"Priority Queue", d:"A queue where the highest-priority item is served first, regardless of arrival order.", l:"core", g:["data-structure"],
 b:["Unlike a plain FIFO queue, a priority queue orders by a key. It is normally implemented with a heap, giving O(log n) insertion and O(log n) removal of the highest-priority element.",
    "It appears wherever ordering by importance matters: OS process scheduling, Dijkstra's shortest path, A* search, event simulation, and job queues with urgency levels. Watch for starvation — a low-priority item may never be served if high-priority work keeps arriving."],
 k:["Ordered by priority, not by arrival time.",
    "Heap-backed: O(log n) push and pop.",
    "Used by schedulers, Dijkstra, A* and event simulation.",
    "Starvation is a real risk — consider ageing priorities upward."],
 dg:"stack-queue",
 r:["Heap","Queue","Dijkstra's Algorithm"]},

{t:"Trie", a:"Prefix Tree", d:"A tree where each path from the root spells out a string, sharing common prefixes between entries.", l:"advanced", g:["data-structure"],
 b:["Each edge represents a character, so all words with the same prefix share the same path from the root. Lookup, insertion and prefix search all take O(k) where k is the key length — independent of how many keys are stored.",
    "That prefix property is what makes it right for autocomplete, spell checking, IP routing tables and dictionary lookups. The cost is memory: a naive trie stores a child array per node, which is wasteful. Compressed variants (radix trees) merge single-child chains."],
 k:["O(k) operations where k is key length, independent of the number of keys.",
    "Shared prefixes are stored once.",
    "Ideal for autocomplete, prefix search and IP routing.",
    "Memory-hungry unless compressed into a radix tree."],
 r:["Tree","Hash Table","Full-Text Search"]},

{t:"Graph", d:"A set of nodes connected by edges, able to represent any network of relationships.", l:"core", g:["data-structure"],
 b:["Graphs model social networks, road maps, dependencies, web links, state machines and molecules. Edges may be directed or undirected, weighted or unweighted, and unlike trees, graphs may contain cycles and disconnected components.",
    "Representation matters. An adjacency list stores each node's neighbours and uses O(V + E) space — right for sparse graphs, which most real ones are. An adjacency matrix uses O(V²) but gives O(1) edge lookup, which suits dense graphs."],
 k:["Directed or undirected, weighted or unweighted, cyclic or acyclic.",
    "Adjacency list for sparse graphs; adjacency matrix for dense ones.",
    "A tree is a connected acyclic graph.",
    "BFS and DFS are the two fundamental traversals."],
 dg:"graph-traversal",
 r:["Breadth-First Search","Depth-First Search","Tree","Graph Database","Dijkstra's Algorithm"]},

{t:"Adjacency List", d:"A graph representation storing, for each node, the list of nodes it connects to.", l:"intermediate", g:["data-structure"],
 b:["An adjacency list uses O(V + E) space, which for a sparse graph — most real networks — is far less than a matrix. Iterating a node's neighbours is proportional to its degree, exactly what traversal algorithms need.",
    "Checking whether a specific edge exists requires scanning the list, which is O(degree) rather than O(1). If that operation dominates, use a matrix or store neighbours in a hash set."],
 k:["O(V + E) space — the right choice for sparse graphs.",
    "Neighbour iteration costs O(degree), ideal for BFS and DFS.",
    "Edge existence check is O(degree), not O(1).",
    "Storing neighbours as sets restores O(1) edge lookup."],
 r:["Graph","Breadth-First Search","Depth-First Search","Data Structure"]},

{t:"Binary Search", d:"Finding a value in sorted data by repeatedly halving the search range.", l:"core", g:["algorithm","search"],
 b:["Compare with the middle element; if the target is smaller, discard the upper half, otherwise discard the lower. Each comparison halves the remaining candidates, so the cost is O(log n) — twenty steps for a million elements.",
    "It requires sorted input, which is the real constraint: sorting costs O(n log n), so binary search pays off when you search many times over stable data. Off-by-one errors in the loop bounds are notoriously easy to introduce; use the standard library implementation."],
 k:["O(log n), but requires sorted input.",
    "20 steps for a million elements, 30 for a billion.",
    "Worth it when you search repeatedly; not for a single lookup.",
    "Also applies to searching an answer space, not just an array."],
 x:{lang:"python", code:'import bisect\n\n# find insertion point in sorted data — O(log n)\ni = bisect.bisect_left(sorted_scores, target)\n\n# binary search over an answer space, not an array\nlo, hi = 1, max_capacity\nwhile lo < hi:\n    mid = (lo + hi) // 2\n    if feasible(mid): hi = mid\n    else:             lo = mid + 1'},
 r:["Sorting Algorithm","Binary Search Tree","Time Complexity","Algorithm"]},

{t:"Sorting Algorithm", d:"A procedure for arranging elements into order — one of the most studied problems in computing.", l:"core", g:["algorithm","sorting"],
 b:["Comparison-based sorts cannot beat O(n log n) in the worst case, and merge sort, heapsort and quicksort all achieve it (quicksort on average). Bubble, insertion and selection sort are O(n²) and only competitive on very small or nearly sorted inputs.",
    "Non-comparison sorts break the bound by exploiting structure: counting sort and radix sort achieve O(n) for integers in a bounded range. In practice you call the standard library, which uses a hybrid — Timsort in Python and Java, introsort in C++."],
 k:["O(n log n) is the comparison-based lower bound.",
    "Stability preserves the relative order of equal elements — often required.",
    "Counting and radix sort achieve O(n) for bounded integer keys.",
    "Use the standard library; it is a well-tuned hybrid."],
 r:["Merge Sort","Quick Sort","Binary Search","Time Complexity","Heap"]},

{t:"Merge Sort", d:"A divide-and-conquer sort that splits the input in half, sorts each half, and merges the sorted results.", l:"intermediate", g:["algorithm","sorting"],
 b:["Recursively halve until you reach single elements, then merge pairs of sorted runs back together. Every level does O(n) merging work and there are log n levels, so it is O(n log n) in every case — best, average and worst.",
    "It is stable, which matters when sorting by multiple keys, and it parallelises and works well on external data too large for memory. The cost is O(n) extra space for the merge buffers."],
 k:["O(n log n) guaranteed — no bad-input worst case.",
    "Stable, preserving the order of equal elements.",
    "Requires O(n) auxiliary space.",
    "Natural fit for external sorting and parallel execution."],
 r:["Quick Sort","Sorting Algorithm","Divide and Conquer","Space Complexity"]},

{t:"Quick Sort", d:"A divide-and-conquer sort that partitions around a pivot and recurses on each side.", l:"intermediate", g:["algorithm","sorting"],
 b:["Choose a pivot, move smaller elements left and larger right, then recurse. Average performance is O(n log n) with very small constants and excellent cache behaviour, which makes it the fastest general comparison sort in practice.",
    "Its weakness is pivot choice: consistently picking the smallest or largest element gives O(n²), which already-sorted input triggers with a naive first-element pivot. Randomised or median-of-three pivots avoid it, and introsort switches to heapsort if recursion goes too deep."],
 k:["O(n log n) average, O(n²) worst case with poor pivots.",
    "In-place — only O(log n) stack space.",
    "Not stable in its standard form.",
    "Randomise the pivot; introsort adds a heapsort fallback."],
 r:["Merge Sort","Sorting Algorithm","Divide and Conquer","Recursion"]},

{t:"Recursion", d:"A function that solves a problem by calling itself on a smaller version of it.", l:"core", g:["technique","fundamentals"],
 b:["Every recursive function needs a base case that returns without recursing, and a recursive case that moves measurably toward it. Missing or unreachable base cases produce infinite recursion and a stack overflow.",
    "Recursion expresses naturally recursive structures — trees, nested data, divide and conquer — far more clearly than loops. The costs are stack space proportional to depth and function call overhead; converting to iteration or adding memoisation addresses both."],
 k:["Base case plus a recursive case that shrinks the problem.",
    "Stack depth is a real limit — Python defaults to about 1,000 frames.",
    "Natural for trees, graphs and divide-and-conquer.",
    "Memoisation turns exponential recursion into linear."],
 x:{lang:"python", code:'def walk(node, depth=0):\n    if node is None:            # base case\n        return\n    print("  " * depth, node.value)\n    for child in node.children: # recursive case\n        walk(child, depth + 1)'},
 dg:"recursion",
 r:["Stack","Memoisation","Dynamic Programming","Divide and Conquer","Tree"]},

{t:"Memoisation", d:"Caching a function's results so repeated calls with the same arguments return instantly.", l:"core", g:["technique","optimisation"],
 b:["Naive recursive Fibonacci recomputes the same values exponentially many times. Storing each computed result in a dictionary and checking it first turns O(2ⁿ) into O(n). This is the top-down form of dynamic programming.",
    "It only works for pure functions — the result must depend solely on the arguments. Watch memory growth on unbounded input spaces, and use an LRU cache with a size limit rather than an unbounded dictionary in long-running processes."],
 k:["Cache keyed by arguments; only valid for pure functions.",
    "Converts exponential recursion into linear time.",
    "Top-down dynamic programming.",
    "Bound the cache size in long-lived processes."],
 x:{lang:"python", code:'from functools import lru_cache\n\n@lru_cache(maxsize=None)\ndef fib(n):\n    return n if n < 2 else fib(n - 1) + fib(n - 2)\n\nfib(200)   # instant; without the cache this never finishes'},
 r:["Dynamic Programming","Recursion","Caching","Pure Function"]},

{t:"Dynamic Programming", a:"DP", d:"Solving a problem by breaking it into overlapping subproblems and reusing their solutions.", l:"intermediate", g:["technique","algorithm"],
 b:["Dynamic programming applies when a problem has optimal substructure (the best solution is built from best solutions to subproblems) and overlapping subproblems (the same subproblems recur). Top-down memoisation caches recursive results; bottom-up tabulation fills a table iteratively.",
    "Knapsack, edit distance, longest common subsequence, coin change and shortest paths are the canonical examples. The hard part is almost always defining the state — what exactly does dp[i][j] represent — after which the recurrence usually follows."],
 k:["Requires optimal substructure and overlapping subproblems.",
    "Top-down memoisation versus bottom-up tabulation — same complexity.",
    "Defining the state correctly is the whole difficulty.",
    "Often reducible to O(1) space by keeping only the previous row."],
 r:["Memoisation","Recursion","Greedy Algorithm","Time Complexity"]},

{t:"Greedy Algorithm", d:"Making the locally best choice at each step and never reconsidering.", l:"intermediate", g:["technique","algorithm"],
 b:["Greedy algorithms are simple and fast, and sometimes provably optimal — Dijkstra, Huffman coding, Kruskal's minimum spanning tree and interval scheduling all work greedily. The requirement is the greedy choice property: a locally optimal choice must be part of some globally optimal solution.",
    "When that property does not hold, greedy fails, sometimes badly. Coin change with denominations 1, 3 and 4 needs two coins for 6 (3+3) but greedy picks 4+1+1. Proving greedy correctness matters; assuming it is a common source of subtly wrong solutions."],
 k:["Locally optimal choice, never revisited.",
    "Correct only when the greedy choice property provably holds.",
    "Dijkstra, Huffman and Kruskal are proven greedy algorithms.",
    "When it fails, dynamic programming is usually the fallback."],
 r:["Dynamic Programming","Dijkstra's Algorithm","Algorithm"]},

{t:"Divide and Conquer", d:"Splitting a problem into independent subproblems, solving them recursively, and combining the results.", l:"intermediate", g:["technique","algorithm"],
 b:["Three steps: divide the problem, conquer the pieces recursively, combine the answers. Merge sort, quicksort, binary search, fast Fourier transform and Strassen's matrix multiplication all follow this shape.",
    "It differs from dynamic programming in that the subproblems are independent and do not overlap, so there is nothing to memoise. Independence is also what makes it naturally parallelisable — each branch can run on a different core."],
 k:["Divide, conquer recursively, combine.",
    "Subproblems are independent — unlike dynamic programming.",
    "Naturally parallel: branches are independent.",
    "The Master Theorem gives the complexity of the standard recurrences."],
 r:["Merge Sort","Recursion","Dynamic Programming","Binary Search"]},

{t:"Backtracking", d:"Exploring candidate solutions incrementally and abandoning a branch as soon as it cannot succeed.", l:"advanced", g:["technique","algorithm"],
 b:["Backtracking builds a solution step by step. At each step it tries an option; if the partial solution can still lead somewhere, it recurses; if not, it undoes the choice and tries the next. N-queens, sudoku, permutation generation and constraint satisfaction all use it.",
    "The critical optimisation is pruning: detecting failure as early as possible avoids exploring an exponentially large subtree. Without pruning, backtracking degenerates into brute-force enumeration."],
 k:["Try, recurse, undo — depth-first search over a solution space.",
    "Early pruning is what makes it tractable.",
    "Worst case is exponential; pruning determines practical performance.",
    "N-queens, sudoku and constraint satisfaction are canonical uses."],
 r:["Recursion","Depth-First Search","Dynamic Programming","Greedy Algorithm"]},

{t:"Two Pointers", d:"Using two indices moving through a sequence to solve in one pass what would otherwise need nested loops.", l:"intermediate", g:["technique","algorithm"],
 b:["Two pointers can converge from both ends (finding a pair summing to a target in a sorted array), move at different speeds (detecting a cycle in a linked list), or trail one another (removing duplicates in place). Each variant replaces an O(n²) nested scan with a single O(n) pass.",
    "The precondition is usually that the data is sorted or has some monotonic property that lets you decide which pointer to move without checking every combination."],
 k:["Turns O(n²) nested scanning into O(n) with O(1) extra space.",
    "Usually requires sorted or monotonic data.",
    "Fast and slow pointers detect cycles in linked structures.",
    "Read and write pointers do in-place filtering."],
 r:["Sliding Window","Array","Binary Search","Time Complexity"]},

{t:"Sliding Window", d:"Maintaining a moving range over a sequence and updating its aggregate incrementally instead of recomputing.", l:"intermediate", g:["technique","algorithm"],
 b:["To find the maximum sum of any k consecutive elements, do not sum each window from scratch. Add the entering element and subtract the leaving one — O(1) per step, O(n) overall instead of O(n·k).",
    "Fixed-size windows suit rolling averages and k-element problems; variable-size windows expand and contract to satisfy a constraint, which covers longest-substring and minimum-window problems. The same idea appears in streaming aggregations and rate limiting."],
 k:["Update the aggregate incrementally rather than recomputing.",
    "Fixed window for k-element problems; variable window for constraint problems.",
    "Turns O(n·k) into O(n).",
    "The same pattern underlies streaming windows and rate limiters."],
 r:["Two Pointers","Array","Windowing","Rate Limiting"]},

{t:"Breadth-First Search", a:"BFS", d:"Exploring a graph level by level, visiting all neighbours before going deeper.", l:"core", g:["algorithm","graph"],
 b:["BFS uses a queue: dequeue a node, enqueue its unvisited neighbours, repeat. Because it expands outward in rings, the first time it reaches a node it has found the shortest path in edge count — which is why it solves unweighted shortest path.",
    "Its memory cost is the frontier, which can be very wide in a broad graph. Applications include shortest path in unweighted graphs, level-order tree traversal, web crawling by depth, and finding connected components."],
 k:["Queue-based; explores level by level.",
    "Finds the shortest path in an unweighted graph.",
    "Memory scales with the width of the frontier.",
    "O(V + E) time with an adjacency list."],
 x:{lang:"python", code:'from collections import deque\n\ndef shortest_path(graph, start, goal):\n    q, seen = deque([(start, [start])]), {start}\n    while q:\n        node, path = q.popleft()\n        if node == goal: return path\n        for nb in graph[node]:\n            if nb not in seen:\n                seen.add(nb)\n                q.append((nb, path + [nb]))\n    return None'},
 dg:"graph-traversal",
 r:["Depth-First Search","Graph","Queue","Dijkstra's Algorithm"]},

{t:"Depth-First Search", a:"DFS", d:"Exploring a graph by going as deep as possible along each branch before backtracking.", l:"core", g:["algorithm","graph"],
 b:["DFS uses a stack, either explicitly or via recursion: visit a node, then fully explore its first unvisited neighbour before moving to the next. Memory is proportional to the depth of the path, not the width of the graph.",
    "It is the basis for cycle detection, topological sorting, finding connected components, maze solving and backtracking search. It does not find shortest paths — that is BFS's job."],
 k:["Stack-based or recursive; explores deeply before broadly.",
    "Memory scales with depth, not width — often much cheaper than BFS.",
    "Does not find shortest paths.",
    "Underlies cycle detection, topological sort and backtracking."],
 dg:"graph-traversal",
 r:["Breadth-First Search","Graph","Stack","Recursion","Topological Sort"]},

{t:"Dijkstra's Algorithm", d:"Finding the shortest path from one node to all others in a graph with non-negative edge weights.", l:"advanced", g:["algorithm","graph"],
 b:["Dijkstra greedily expands the unvisited node with the smallest known distance, using a priority queue, and relaxes the distances of its neighbours. With a binary heap it runs in O((V + E) log V).",
    "It requires non-negative weights — a negative edge could make an already-finalised distance wrong. Bellman-Ford handles negative weights at higher cost, and A* speeds Dijkstra up with a heuristic estimate of remaining distance, which is what mapping applications use."],
 k:["Requires non-negative edge weights; use Bellman-Ford otherwise.",
    "O((V + E) log V) with a binary heap.",
    "Greedy: always expand the closest unfinalised node.",
    "A* adds a heuristic and is what routing engines actually run."],
 r:["Graph","Priority Queue","Greedy Algorithm","Breadth-First Search"]},

{t:"Topological Sort", d:"Ordering the nodes of a directed acyclic graph so every edge points forward.", l:"intermediate", g:["algorithm","graph"],
 b:["If task A must happen before task B, a topological sort produces an order in which A precedes B. It exists only if the graph has no cycles — a cycle means a circular dependency with no valid order.",
    "This is what build systems, package managers, task schedulers and course prerequisite planners compute. Kahn's algorithm repeatedly removes nodes with no incoming edges; DFS-based topological sort reverses the post-order."],
 k:["Only exists for a DAG — a cycle means no valid ordering.",
    "Kahn's algorithm removes zero in-degree nodes repeatedly.",
    "Multiple valid orderings usually exist.",
    "Build tools, package managers and schedulers all rely on it."],
 r:["Directed Acyclic Graph","Graph","Depth-First Search","Apache Airflow"]},

{t:"Union-Find", a:"Disjoint Set Union", d:"A structure that tracks which elements belong to the same group, with near-constant-time merging and querying.", l:"advanced", g:["data-structure","algorithm"],
 b:["Union-find supports two operations: find, which returns the representative of an element's set, and union, which merges two sets. With path compression (flattening the tree during find) and union by rank, both run in effectively constant amortised time.",
    "It is the natural structure for connected components, Kruskal's minimum spanning tree, cycle detection in undirected graphs, and any incremental grouping problem such as network connectivity or image segmentation."],
 k:["Two operations: find the representative, union two sets.",
    "Path compression plus union by rank gives near-O(1) amortised cost.",
    "Cannot split a set once merged — merging is one-way.",
    "Powers Kruskal's algorithm and connected-component tracking."],
 r:["Graph","Tree","Amortised Analysis","Greedy Algorithm"]},

{t:"Bit Manipulation", d:"Operating directly on the individual bits of a number using bitwise operators.", l:"advanced", g:["technique","low-level"],
 b:["AND, OR, XOR, NOT and shifts let you pack flags into a single integer, test membership in a small set, multiply or divide by powers of two, and perform certain arithmetic without branches. XOR has useful properties: x^x is 0 and x^0 is x, which is why it finds the single unpaired element in a list.",
    "Bit tricks are compact and fast but easy to get wrong and hard to read. Use them where they are idiomatic — permission flags, bitsets, low-level protocols, embedded code — and prefer clarity elsewhere."],
 k:["`x & (x-1)` clears the lowest set bit; `x & -x` isolates it.",
    "`x << n` multiplies by 2ⁿ; `x >> n` divides.",
    "XOR of all elements finds the unpaired one in O(1) space.",
    "Bitsets pack boolean arrays 8× smaller than byte arrays."],
 r:["Integer Overflow","Floating Point","Hash Table"]},

{t:"P vs NP", d:"The open question of whether every problem whose solution can be verified quickly can also be solved quickly.", l:"advanced", g:["theory","complexity"],
 b:["P is the class of problems solvable in polynomial time. NP is the class whose solutions can be verified in polynomial time. Every P problem is in NP; whether NP problems are all in P is the most famous open question in computer science, with a million-dollar prize attached.",
    "The practical consequence is that for NP-complete problems — travelling salesman, boolean satisfiability, graph colouring — no known algorithm scales, so real systems use approximations, heuristics or solvers that are fast on typical instances rather than in the worst case."],
 k:["P: solvable quickly. NP: verifiable quickly.",
    "P = NP is unresolved; almost everyone expects P ≠ NP.",
    "If P = NP, most modern cryptography would break.",
    "NP-hard problems in practice are handled with heuristics and approximation."],
 r:["NP-Complete","Time Complexity","Algorithm","Encryption"]},

{t:"NP-Complete", d:"The hardest problems in NP — if any one could be solved quickly, all of them could.", l:"advanced", g:["theory","complexity"],
 b:["A problem is NP-complete if it is in NP and every other NP problem reduces to it in polynomial time. Boolean satisfiability was the first proven example; travelling salesman, graph colouring, knapsack and thousands of others followed.",
    "Recognising that your problem is NP-complete is genuinely useful: it tells you to stop searching for an efficient exact algorithm and instead use approximation, heuristics, constraint solvers, or exploit structure specific to your instances."],
 k:["In NP, and every NP problem reduces to it.",
    "SAT, TSP, knapsack and graph colouring are the classic examples.",
    "Recognising NP-completeness redirects effort toward approximation.",
    "Modern SAT solvers handle large real instances despite the worst case."],
 r:["P vs NP","Time Complexity","Greedy Algorithm","Algorithm"]},

{t:"Object-Oriented Programming", a:"OOP", d:"Organising code around objects that bundle data with the behaviour that operates on it.", l:"core", g:["paradigm"],
 b:["OOP models a system as interacting objects, each holding state and exposing methods. Its four pillars are encapsulation (hide internal state), abstraction (expose a simple interface), inheritance (share behaviour through a type hierarchy) and polymorphism (one interface, many implementations).",
    "It suits domains with clear entities and behaviours, and large teams benefit from the boundaries it enforces. Its overuse — deep inheritance hierarchies, anaemic classes that hold data and nothing else, patterns applied ceremonially — is a real and common failure mode."],
 k:["Four pillars: encapsulation, abstraction, inheritance, polymorphism.",
    "Objects bundle state with the behaviour that manipulates it.",
    "Favour composition over deep inheritance hierarchies.",
    "Not universally appropriate — data pipelines often read better functionally."],
 dg:"oop",
 r:["Encapsulation","Inheritance","Polymorphism","Abstraction","Class"]},

{t:"Class", d:"A blueprint defining the data and behaviour that its instances will have.", l:"core", g:["oop"],
 b:["A class specifies fields (state) and methods (behaviour). Creating an object from it is instantiation, and the object gets its own copy of the instance data while sharing the method definitions. Class-level (static) members belong to the class itself rather than to any instance.",
    "Not everything needs to be a class. A function is often clearer than a class with one method, and a dataclass, record or struct is better than a class full of getters and setters."],
 k:["Blueprint; the object is the instance.",
    "Instance members are per-object; static members belong to the class.",
    "Constructors initialise state and should leave the object valid.",
    "Prefer a function or a record when there is no behaviour to encapsulate."],
 r:["Object","Object-Oriented Programming","Inheritance","Interface"]},

{t:"Object", d:"A specific instance of a class, with its own state and access to the class's behaviour.", l:"core", g:["oop"],
 b:["If Dog is the class, then a particular dog with a name and an age is the object. Each object holds its own field values while sharing the method definitions. Objects have identity, so two objects with identical field values are still distinct unless equality is explicitly defined otherwise.",
    "The distinction between identity (is this the same object) and equality (do these have the same value) is a recurring source of bugs, especially when objects are used as dictionary keys or compared with the wrong operator."],
 k:["An instance with its own state and its own identity.",
    "Identity (`is`) and equality (`==`) are different questions.",
    "Override equality and hashing together, or hash-based collections break.",
    "Mutable objects used as dictionary keys are a classic bug."],
 r:["Class","Object-Oriented Programming","Encapsulation","Immutability"]},

{t:"Encapsulation", d:"Hiding an object's internal state and exposing only a controlled interface to it.", l:"core", g:["oop","principle"],
 b:["Encapsulation means callers interact through methods rather than reaching into fields directly. That lets the object enforce its own invariants — a bank account can guarantee its balance never goes negative — and lets the internal representation change without breaking every caller.",
    "The mechanism varies: `private` in Java and C++, a leading underscore convention in Python, closures in JavaScript. What matters is the discipline, not the keyword."],
 k:["Callers use the interface; the internals stay changeable.",
    "Lets an object enforce its own invariants.",
    "Getters and setters for every field are not encapsulation — they are a data bag.",
    "Python relies on convention rather than enforcement."],
 dg:"oop",
 r:["Abstraction","Object-Oriented Programming","Class","Interface"]},

{t:"Inheritance", d:"Defining a class in terms of another, acquiring its fields and methods.", l:"core", g:["oop"],
 b:["A subclass inherits from a superclass and can add or override behaviour. It expresses an is-a relationship: a Dog is an Animal. Done well it removes duplication and enables polymorphic treatment of related types.",
    "Done badly it produces fragile hierarchies where a change to a base class breaks distant subclasses, and where the hierarchy is used to share code rather than to model a genuine is-a relationship. Composition — holding a reference to a collaborator — is usually more flexible and is the modern default advice."],
 k:["Expresses is-a; use composition for has-a.",
    "Deep hierarchies are fragile — changes propagate unpredictably.",
    "Liskov substitution: a subclass must be usable wherever its parent is.",
    "Prefer composition unless the is-a relationship is genuine and stable."],
 dg:"oop",
 r:["Polymorphism","Composition over Inheritance","Class","Object-Oriented Programming"]},

{t:"Polymorphism", d:"One interface with many implementations, so calling code works with any of them.", l:"core", g:["oop"],
 b:["Call `shape.area()` and the correct implementation runs depending on whether the shape is a circle or a rectangle. The caller needs to know nothing about which. This is what lets you add a new shape type without changing any calling code — the open/closed principle in action.",
    "Subtype polymorphism uses inheritance or interfaces; parametric polymorphism uses generics; duck typing achieves it structurally in dynamic languages. All three serve the same purpose: decoupling callers from concrete types."],
 k:["One call site, many implementations, resolved at runtime.",
    "Enables adding new types without modifying existing code.",
    "Subtype, parametric (generics) and duck typing are three forms.",
    "Replaces long if/else chains on type with dispatch."],
 dg:"oop",
 r:["Inheritance","Interface","Object-Oriented Programming","Duck Typing"]},

{t:"Abstraction", d:"Exposing what something does while hiding how it does it.", l:"core", g:["principle","fundamentals"],
 b:["Every layer of computing is an abstraction over the one below: a file hides disk sectors, TCP hides packet loss, a function hides its implementation. Good abstractions let you reason at one level without holding the levels below in your head.",
    "Abstractions leak — TCP hides packet loss until the network is bad enough that latency spikes reveal it. Knowing when to look through an abstraction is as important as building one, and premature abstraction over a single use case is a common source of unnecessary complexity."],
 k:["Expose the what, hide the how.",
    "All non-trivial abstractions leak under stress.",
    "Abstract after the second or third real use case, not the first.",
    "The wrong abstraction costs more than duplication."],
 dg:"oop",
 r:["Encapsulation","Interface","Object-Oriented Programming","Design Pattern"]},

{t:"Interface", d:"A contract specifying which methods a type must provide, without saying how.", l:"core", g:["oop","design"],
 b:["An interface names a capability. Code written against `Repository` works with a Postgres implementation, an in-memory fake for tests, or a future replacement — none of which the calling code needs to know about. This is what makes dependency injection and testing straightforward.",
    "Keep interfaces small. An interface with fifteen methods forces every implementer to provide fifteen methods, most of which they do not need — the interface segregation principle exists precisely to prevent that."],
 k:["Declares capability; contains no implementation.",
    "Enables substitution: real implementation, test double, future replacement.",
    "Small interfaces are easier to implement and to mock.",
    "Go's implicit interfaces and Python protocols are structural rather than declared."],
 r:["Abstraction","Polymorphism","Dependency Injection","Duck Typing"]},

{t:"Composition over Inheritance", d:"Building behaviour by combining objects rather than by extending a class hierarchy.", l:"intermediate", g:["principle","design"],
 b:["Instead of `class ElectricCar extends Car`, hold an `Engine` that happens to be electric. Composition assembles behaviour from parts that can be swapped at runtime, tested in isolation and reused across unrelated types — none of which inheritance offers.",
    "Inheritance couples a subclass to its parent's implementation, so changes ripple downward unpredictably. The advice is not that inheritance is always wrong, but that composition should be the default and inheritance the deliberate exception."],
 k:["Has-a beats is-a as a default.",
    "Behaviour can be swapped at runtime, unlike a fixed hierarchy.",
    "Avoids fragile base class problems.",
    "Strategy, decorator and adapter patterns are all composition."],
 r:["Inheritance","Design Pattern","Interface","Dependency Injection"]},

{t:"Functional Programming", a:"FP", d:"Building programs from pure functions and immutable data, avoiding shared mutable state.", l:"core", g:["paradigm"],
 b:["Functional programming treats computation as the evaluation of functions without side effects. Data is immutable, functions are values that can be passed and returned, and behaviour is composed rather than sequenced.",
    "The payoff is reasoning: a pure function's behaviour depends only on its arguments, so it can be tested in isolation, cached, reordered and parallelised safely. Most mainstream languages have absorbed the useful parts — map, filter, reduce, lambdas, immutable collections — without requiring full purity."],
 k:["Pure functions plus immutable data.",
    "First-class functions: pass, return and store them like any value.",
    "Easier to test, cache and parallelise than stateful code.",
    "You can adopt the useful 80% in any modern language."],
 r:["Pure Function","Immutability","Higher-Order Function","Closure","Side Effect"]},

{t:"Pure Function", d:"A function whose output depends only on its inputs and which changes nothing outside itself.", l:"core", g:["functional","principle"],
 b:["Given the same arguments, a pure function always returns the same result and has no side effects — no I/O, no mutation of arguments or globals, no randomness, no reading the clock. That makes it trivially testable and safe to call from anywhere.",
    "Real programs must have effects, so the practical pattern is to push effects to the edges: a thin impure shell that reads input and writes output, wrapping a large pure core that does the actual logic."],
 k:["Same input, same output, always.",
    "No I/O, no mutation, no randomness, no clock reads.",
    "Safely cacheable, testable and parallelisable.",
    "Functional core, imperative shell is the practical architecture."],
 r:["Side Effect","Functional Programming","Immutability","Memoisation"]},

{t:"Side Effect", d:"Any observable change a function makes beyond returning a value.", l:"core", g:["functional","concept"],
 b:["Writing a file, mutating an argument, updating a global, printing, sending a request, changing the DOM — all side effects. They are what makes a program do anything useful, and also what makes it hard to reason about, because a function's behaviour now depends on context you cannot see at the call site.",
    "The discipline is not eliminating effects but confining them: keep them out of the deep logic, make them explicit in the function's name and signature, and concentrate them at the system boundary."],
 k:["Any observable change beyond the return value.",
    "Makes behaviour depend on invisible context.",
    "Confine to the boundary; keep the core pure.",
    "A function named `get_user` that also writes a log is lying about what it does."],
 r:["Pure Function","Functional Programming","Immutability","Idempotency"]},

{t:"Immutability", d:"Data that cannot be changed after creation — modifications produce a new value instead.", l:"core", g:["functional","principle"],
 b:["An immutable value can be shared freely: no caller can change it under another's feet, no defensive copying is needed, and concurrent readers need no locks. That last point is the reason functional languages handle concurrency so cleanly.",
    "The cost is allocation, which persistent data structures mitigate by sharing unchanged parts of a structure between versions. Python's default mutable arguments and JavaScript's mutable arrays are both frequent sources of aliasing bugs that immutability prevents."],
 k:["Shared without risk — no defensive copies, no lock needed for readers.",
    "Persistent structures share unchanged parts between versions.",
    "Eliminates an entire class of aliasing and concurrency bugs.",
    "Python's mutable default arguments are the classic counterexample."],
 r:["Pure Function","Functional Programming","Concurrency","Side Effect"]},

{t:"Higher-Order Function", d:"A function that takes another function as an argument or returns one.", l:"core", g:["functional"],
 b:["`map`, `filter`, `reduce`, `sort(key=...)` and decorators are all higher-order. They let you separate what to do from how to iterate: `map` owns the traversal, and you supply the transformation.",
    "Treating functions as values is what makes callbacks, middleware, event handlers, dependency injection and function composition possible. Nearly every modern language supports it, whatever paradigm it nominally belongs to."],
 k:["Takes or returns a function — functions are values.",
    "map, filter, reduce and sort keys are the everyday examples.",
    "Enables middleware, decorators and callback patterns.",
    "Separates iteration mechanics from the operation applied."],
 x:{lang:"python", code:'def retry(times):\n    def decorator(fn):\n        def wrapper(*args, **kw):\n            for attempt in range(times):\n                try:    return fn(*args, **kw)\n                except Exception:\n                    if attempt == times - 1: raise\n        return wrapper\n    return decorator\n\n@retry(3)\ndef fetch(url): ...'},
 r:["Closure","Functional Programming","Pure Function","Callback"]},

{t:"Closure", d:"A function that captures and retains access to variables from the scope where it was defined.", l:"intermediate", g:["functional","concept"],
 b:["When an inner function references a variable from its enclosing function, it keeps that variable alive even after the outer function returns. The function and its captured environment together form a closure.",
    "Closures enable private state without classes, function factories, decorators and callbacks that remember context. The classic trap is capturing a loop variable by reference — all closures then see the final value, not the value at creation time."],
 k:["Captures the enclosing scope's variables, not just their values at call time.",
    "Enables private state and function factories without classes.",
    "Loop variable capture is the classic bug — bind explicitly.",
    "Captured variables keep their objects alive, which can leak memory."],
 r:["Higher-Order Function","Functional Programming","Scope","JavaScript"]},

{t:"Declarative Programming", d:"Describing what result you want, leaving the how to the system.", l:"intermediate", g:["paradigm"],
 b:["SQL, HTML, CSS, Terraform, React's JSX and Kubernetes manifests are declarative: you state the desired outcome and an engine works out the steps. That frees the engine to optimise, and frees you from sequencing details.",
    "The trade is control and debuggability. When a declarative system does something unexpected, you must understand its execution model anyway — which is why reading query plans and reconciliation logs is a real skill."],
 k:["State the goal; the engine determines the steps.",
    "SQL, HTML, CSS, Terraform and Kubernetes manifests are declarative.",
    "Lets the engine optimise in ways you did not specify.",
    "Debugging requires understanding the hidden execution model."],
 r:["Imperative Programming","SQL","Infrastructure as Code","Kubernetes"]},

{t:"Imperative Programming", d:"Specifying the exact sequence of steps that change program state to reach a result.", l:"intermediate", g:["paradigm"],
 b:["Loops, assignments, conditionals and explicit control flow — do this, then that, and if this then the other. It maps closely to how hardware executes, which makes performance reasoning direct.",
    "Most code is imperative, and most codebases mix paradigms freely: imperative control flow with functional data transformations and object-oriented structure. Paradigms are tools, not identities."],
 k:["Explicit sequence of state-changing steps.",
    "Maps closely to hardware execution, so performance is predictable.",
    "Procedural and object-oriented programming are both imperative.",
    "Real codebases mix paradigms deliberately."],
 r:["Declarative Programming","Object-Oriented Programming","Functional Programming"]},

{t:"Compiler", d:"A program that translates source code into another form — usually machine code — before execution.", l:"core", g:["tooling","fundamentals"],
 b:["A compiler runs several stages: lexical analysis produces tokens, parsing builds an abstract syntax tree, semantic analysis checks types and scoping, optimisation rewrites the intermediate representation, and code generation emits target instructions.",
    "Modern optimising compilers restructure code aggressively — inlining functions, unrolling loops, vectorising, eliminating dead code — so the machine code often bears little resemblance to what you wrote. That is also why undefined behaviour is dangerous: the compiler may assume it never happens."],
 k:["Stages: lex, parse, analyse, optimise, generate.",
    "Optimisation can transform code beyond recognition.",
    "Errors are caught before the program ever runs.",
    "LLVM is a shared backend for many modern language front-ends."],
 r:["Interpreter","Parser","Abstract Syntax Tree","Machine Code","Compiled Language"]},

{t:"Interpreter", d:"A program that executes source code directly, translating as it goes.", l:"core", g:["tooling","fundamentals"],
 b:["An interpreter reads and executes statements one at a time, usually via an internal bytecode step. There is no separate build, so the feedback loop is immediate and the same source runs on any platform with the interpreter.",
    "The cost is speed — translation happens repeatedly at runtime rather than once ahead of time. The line between compiler and interpreter is blurry: CPython compiles to bytecode then interprets it, and V8 interprets then JIT-compiles hot code."],
 k:["Executes directly, with no separate build step.",
    "Slower than compiled code, but immediate to run.",
    "Most interpreters compile to bytecode internally first.",
    "Modern runtimes blend interpretation with JIT compilation."],
 r:["Compiler","Bytecode","JIT Compilation","Interpreted Language"]},

{t:"Parser", d:"A component that reads text according to a grammar and builds a structured representation of it.", l:"intermediate", g:["tooling","fundamentals"],
 b:["Parsing turns a flat sequence of tokens into a tree that reflects the grammar's structure. Compilers parse source code, browsers parse HTML and CSS, and every JSON, YAML or configuration file you read went through a parser.",
    "The important practical rule: use a real parser for structured formats. Regular expressions cannot correctly parse nested structures such as HTML or JSON, and attempting it produces code that works on your examples and fails on real input."],
 k:["Tokens in, syntax tree out, according to a grammar.",
    "Regex cannot parse nested structures — use a real parser.",
    "Recursive descent is the most readable hand-written approach.",
    "Parser generators build parsers from a grammar specification."],
 r:["Abstract Syntax Tree","Compiler","Regular Expression","JSON"]},

{t:"Abstract Syntax Tree", a:"AST", d:"A tree representing the structure of source code, stripped of syntactic noise.", l:"intermediate", g:["tooling","fundamentals"],
 b:["An AST captures what the code means structurally — this is a function call with these arguments — while discarding punctuation, whitespace and comments. Compilers, linters, formatters, refactoring tools and codemods all operate on it rather than on text.",
    "Working at the AST level is what makes a formatter like Prettier or Black reliable: it reprints from structure rather than manipulating characters, so it cannot produce syntactically invalid output."],
 k:["Structure without syntactic noise.",
    "Linters, formatters and refactoring tools all work at this level.",
    "Reprinting from an AST cannot produce invalid syntax.",
    "Python exposes its own AST through the `ast` module."],
 r:["Parser","Compiler","Directed Acyclic Graph","Linter"]},

{t:"Machine Code", d:"The binary instructions a CPU executes directly.", l:"intermediate", g:["low-level","fundamentals"],
 b:["Machine code is the actual bit patterns the processor decodes — load, add, compare, jump. Assembly language is a one-to-one human-readable form of it. Every higher-level language ultimately produces machine code, whether ahead of time, just in time, or through an interpreter's own compiled loop.",
    "It is architecture-specific: x86-64 machine code will not run on ARM, which is why software must be built per target and why Apple's transition to its own silicon required either recompilation or translation."],
 k:["Binary instructions decoded directly by the CPU.",
    "Architecture-specific — x86-64 and ARM64 are incompatible.",
    "Assembly is a readable one-to-one mapping of it.",
    "Instruction set architecture defines what the CPU understands."],
 r:["Assembly Language","Compiler","CPU","Bytecode"]},

{t:"Memory Management", d:"Allocating and releasing memory during a program's execution.", l:"core", g:["fundamentals","systems"],
 b:["Manual management (C, C++) gives precise control and full responsibility: forget to free and you leak, free twice and you corrupt the heap, use after freeing and you have a security vulnerability. Automatic management via garbage collection removes those bugs at the cost of pauses and overhead.",
    "Rust takes a third path: ownership rules checked at compile time give deterministic deallocation with no garbage collector and no manual frees. Reference counting, used by Swift and Python, sits in between and needs cycle detection."],
 k:["Manual, garbage collected, reference counted or ownership based.",
    "Leaks, double frees and use-after-free are the manual failure modes.",
    "GC trades predictable latency for safety.",
    "Rust's ownership gives safety without runtime cost."],
 r:["Garbage Collection","Stack and Heap Memory","Pointer","Memory Safety","Rust"]},

{t:"Stack and Heap Memory", d:"Two regions of a program's memory: a fast automatic stack and a flexible manually managed heap.", l:"core", g:["fundamentals","systems"],
 b:["The stack holds function frames — parameters, local variables, return addresses — and grows and shrinks automatically as functions are called and return. Allocation is a pointer bump, so it is extremely fast, but the size must be known at compile time and the space is limited.",
    "The heap holds dynamically sized, longer-lived data. Allocation is slower and requires explicit release or garbage collection, but objects can outlive the function that created them. Returning a pointer to a stack variable is a classic and dangerous bug."],
 k:["Stack: fast, automatic, fixed-size, function-scoped.",
    "Heap: flexible, slower, requires management, outlives the function.",
    "Stack overflow comes from deep or infinite recursion.",
    "Never return a pointer to a stack variable."],
 r:["Memory Management","Pointer","Recursion","Garbage Collection"]},

{t:"Pointer", d:"A variable that holds a memory address rather than a value.", l:"intermediate", g:["fundamentals","low-level"],
 b:["Pointers allow indirection: passing large structures without copying, building linked structures, and manipulating memory directly. Dereferencing a pointer reads the value at the address it holds.",
    "They are also the source of most memory-safety failures: null dereference, dangling pointers to freed memory, and arithmetic that walks off the end of a buffer. Languages with references but no pointer arithmetic (Java, Python, Go) keep the indirection and remove most of the danger."],
 k:["Holds an address; dereferencing reads the value there.",
    "Enables linked structures and copy-free parameter passing.",
    "Null, dangling and out-of-bounds pointers cause most memory bugs.",
    "References give the same indirection without arithmetic."],
 r:["Memory Management","Memory Safety","Linked List","C","Stack and Heap Memory"]},

{t:"Garbage Collection", a:"GC", d:"Automatically reclaiming memory that a program can no longer reach.", l:"core", g:["fundamentals","runtime"],
 b:["A garbage collector periodically determines which objects are still reachable from the program's roots and frees the rest. Mark-and-sweep traces reachability; generational collectors exploit the observation that most objects die young; reference counting frees immediately when the count hits zero but cannot collect cycles.",
    "The trade is pause time. Modern collectors — G1, ZGC, Shenandoah — keep pauses in the single-digit milliseconds even on large heaps, but garbage collection remains a poor fit for hard real-time systems."],
 k:["Frees memory that is no longer reachable from program roots.",
    "Generational collection exploits the fact that most objects die young.",
    "Reference counting cannot collect reference cycles on its own.",
    "Pause times matter for latency-sensitive services."],
 r:["Memory Management","Java Virtual Machine","Memory Safety","Stack and Heap Memory"]},

{t:"Concurrency", d:"Structuring a program so multiple tasks can be in progress at once, whether or not they run simultaneously.", l:"core", g:["fundamentals","concurrency"],
 b:["Concurrency is about dealing with many things at once; parallelism is about doing many things at once. A single-threaded event loop is concurrent but not parallel — it interleaves tasks, switching whenever one waits on I/O.",
    "The hard part is shared mutable state. Two tasks touching the same data without coordination produce race conditions, and the coordination mechanisms — locks, channels, atomics — bring their own hazards in deadlock and contention."],
 k:["Concurrency is structure; parallelism is simultaneous execution.",
    "Shared mutable state is the source of nearly every concurrency bug.",
    "Message passing avoids shared state entirely.",
    "Concurrency bugs are non-deterministic and therefore hard to reproduce."],
 r:["Parallelism","Thread","Race Condition","Deadlock","Mutex"]},

{t:"Parallelism", d:"Executing multiple computations at literally the same time on separate processing units.", l:"core", g:["fundamentals","concurrency"],
 b:["Parallelism requires multiple cores or machines. It speeds up CPU-bound work by dividing it, but Amdahl's law bounds the gain: if 10% of a program is inherently sequential, no amount of hardware makes it more than ten times faster.",
    "Data parallelism applies the same operation to different data — the model GPUs are built for. Task parallelism runs different operations concurrently. Coordination overhead means more cores do not always mean faster."],
 k:["Requires multiple cores; concurrency does not.",
    "Amdahl's law: the sequential fraction caps the achievable speed-up.",
    "Data parallelism suits GPUs; task parallelism suits multi-core CPUs.",
    "Coordination overhead can exceed the gain on small workloads."],
 r:["Concurrency","Thread","GPU","Distributed Training"]},

{t:"Thread", d:"An independent sequence of execution within a process, sharing that process's memory.", l:"core", g:["fundamentals","concurrency"],
 b:["Threads within a process share the heap and open file handles but have their own stack and registers. Sharing memory makes communication cheap and makes data races possible — two threads writing the same variable without synchronisation produce undefined results.",
    "OS threads cost a megabyte or more of stack each, so thousands are expensive. Green threads, goroutines, virtual threads and coroutines are lightweight alternatives scheduled in user space, which is how modern runtimes handle high concurrency."],
 k:["Shares heap memory with its process; has its own stack.",
    "Shared memory makes communication cheap and races possible.",
    "OS threads are expensive; green threads and coroutines are not.",
    "Python's GIL prevents true CPU parallelism across threads."],
 r:["Process","Concurrency","Race Condition","Mutex","Parallelism"]},

{t:"Process", d:"An independent running program with its own isolated memory space.", l:"core", g:["fundamentals","os"],
 b:["Each process has its own address space, so one process cannot corrupt another's memory. That isolation is what keeps a crashing application from taking down the system, and it is why browsers now run each tab in its own process.",
    "The cost is that processes are heavier than threads to create and that communication requires explicit inter-process mechanisms — pipes, sockets, shared memory segments — rather than simply sharing variables."],
 k:["Isolated address space — a crash cannot corrupt other processes.",
    "Heavier to create than a thread; communication needs explicit IPC.",
    "Python's multiprocessing sidesteps the GIL by using processes.",
    "Containers isolate processes further with namespaces and cgroups."],
 r:["Thread","Concurrency","Container","Operating System"]},

{t:"Race Condition", d:"A bug where the result depends on the unpredictable timing of concurrent operations.", l:"core", g:["failure-mode","concurrency"],
 b:["Two threads read a counter as 5, both increment to 6, both write 6 — one increment vanished. The read-modify-write sequence was not atomic. Race conditions are non-deterministic, so they pass tests, survive code review, and appear in production under load.",
    "Fixes are to make the operation atomic, to protect it with a lock, or to remove the sharing entirely by giving each thread its own state and combining afterwards. Time-of-check-to-time-of-use races are the security-relevant variant."],
 k:["Result depends on timing — non-deterministic and hard to reproduce.",
    "Read-modify-write is the classic unsafe sequence.",
    "Fix with atomics, locks, or by eliminating the sharing.",
    "TOCTOU races are a recognised security vulnerability class."],
 r:["Concurrency","Mutex","Atomic Operation","Deadlock","Thread"]},

{t:"Mutex", a:"Mutual Exclusion Lock", d:"A lock that allows only one thread at a time into a critical section.", l:"core", g:["concurrency","primitive"],
 b:["A thread acquires the mutex before touching shared data and releases it afterwards; any other thread attempting to acquire it blocks until it is free. That serialisation is what prevents race conditions.",
    "The costs are contention (threads waiting reduce parallelism), deadlock (two threads each holding what the other needs), and forgotten releases. Hold locks for the shortest possible time, always acquire multiple locks in a consistent order, and use scope-based release (`with`, RAII, `defer`) so an exception cannot skip the unlock."],
 k:["Only one holder at a time; others block.",
    "Hold for the shortest possible critical section.",
    "Acquire multiple locks in a globally consistent order to avoid deadlock.",
    "Use scope-based release so exceptions cannot leak a held lock."],
 r:["Race Condition","Deadlock","Semaphore","Concurrency","Atomic Operation"]},

{t:"Semaphore", d:"A counter-based synchronisation primitive that limits how many threads may proceed at once.", l:"intermediate", g:["concurrency","primitive"],
 b:["A semaphore holds a count of available permits. Acquiring decrements it and blocks at zero; releasing increments it. A semaphore initialised to one behaves like a mutex; initialised to n it limits concurrency to n.",
    "That makes it the natural tool for bounding resource use: at most ten concurrent database connections, at most five parallel downloads, at most three GPU jobs. Unlike a mutex, a semaphore need not be released by the thread that acquired it."],
 k:["Counts permits; blocks when none remain.",
    "Binary semaphore ≈ mutex; counting semaphore bounds concurrency.",
    "Ideal for connection pools and rate limiting parallel work.",
    "Can be released by a different thread than acquired it."],
 r:["Mutex","Concurrency","Rate Limiting","Connection Pool"]},

{t:"Deadlock", d:"A standstill where each of several tasks waits for a resource held by another, so none can proceed.", l:"core", g:["failure-mode","concurrency"],
 b:["Four conditions must hold simultaneously: mutual exclusion, hold-and-wait, no preemption, and circular wait. Breaking any one prevents deadlock, and breaking circular wait — by always acquiring locks in the same global order — is the most practical.",
    "Timeouts convert a permanent hang into a recoverable error, which is why database and distributed systems detect deadlocks and abort a victim. The symptom in production is a service that stops responding while consuming no CPU."],
 k:["Four conditions: mutual exclusion, hold-and-wait, no preemption, circular wait.",
    "Consistent lock ordering breaks the circular wait condition.",
    "Symptom: hung service using no CPU.",
    "Timeouts turn a hang into a recoverable failure."],
 r:["Mutex","Race Condition","Concurrency","Transaction"]},

{t:"Atomic Operation", d:"An operation that completes entirely or not at all, with no observable intermediate state.", l:"intermediate", g:["concurrency","primitive"],
 b:["Atomic increments, compare-and-swap and atomic loads and stores are implemented with CPU instructions that cannot be interrupted mid-operation. They let you build correct concurrent code without locks, which avoids contention and deadlock entirely.",
    "Compare-and-swap is the foundation of lock-free data structures: read a value, compute a new one, and swap only if the old value is unchanged — retrying if another thread got there first."],
 k:["Indivisible at the hardware level — no interleaving possible.",
    "Compare-and-swap underpins lock-free algorithms.",
    "Avoids lock contention and deadlock.",
    "Lock-free code is genuinely difficult to write correctly — prefer proven libraries."],
 r:["Race Condition","Mutex","Concurrency","Transaction"]},

{t:"Asynchronous Programming", d:"Starting an operation and continuing other work instead of blocking until it finishes.", l:"core", g:["concurrency","paradigm"],
 b:["When a thread waits on a network request, it does nothing for tens of milliseconds — an eternity in CPU terms. Asynchronous code releases the thread to run other tasks and resumes when the result arrives, which is how a single thread can handle thousands of concurrent connections.",
    "It suits I/O-bound work: network calls, disk reads, database queries. It does nothing for CPU-bound work, where the thread is busy rather than waiting. Mixing blocking calls into async code silently destroys the benefit by holding the event loop."],
 k:["Excellent for I/O-bound work; useless for CPU-bound work.",
    "One thread can serve thousands of concurrent I/O operations.",
    "A single blocking call inside async code stalls everything.",
    "async/await is syntax over callbacks and promises."],
 x:{lang:"python", code:'import asyncio, aiohttp\n\nasync def fetch(session, url):\n    async with session.get(url) as r:\n        return await r.json()\n\nasync def main(urls):\n    async with aiohttp.ClientSession() as s:\n        return await asyncio.gather(*(fetch(s, u) for u in urls))\n\n# 100 requests concurrently on one thread'},
 r:["Event Loop","Promise","Callback","Concurrency","Node.js"]},

{t:"Callback", d:"A function passed to another function to be called when an operation finishes.", l:"core", g:["concurrency","pattern"],
 b:["Callbacks were the original asynchronous mechanism: start the operation, hand over a function, and it runs on completion. They work, but nesting several sequential asynchronous steps produces deeply indented code — callback hell — with awkward error handling at every level.",
    "Promises flattened the nesting and unified error handling; async/await made asynchronous code read sequentially. Callbacks remain everywhere in event handlers, where the pattern fits naturally."],
 k:["Function passed in, invoked on completion.",
    "Nesting sequential async steps produces callback hell.",
    "Error handling must be repeated at every level.",
    "Still the right pattern for event handlers."],
 r:["Promise","Asynchronous Programming","Higher-Order Function","Event Loop"]},

{t:"Promise", a:"Future", d:"An object representing a value that is not available yet but will be, or will fail.", l:"core", g:["concurrency","pattern"],
 b:["A promise is in one of three states: pending, fulfilled or rejected, and it settles exactly once. Chaining with `.then()` flattens what would have been nested callbacks, and a single `.catch()` handles errors from anywhere in the chain.",
    "`async`/`await` is syntax over promises that lets asynchronous code read sequentially. The most common mistake is forgetting to await, which produces a promise object where a value was expected and swallows any error it contains."],
 k:["Three states, settles once, cannot be re-settled.",
    "Chaining flattens nesting; one catch covers the whole chain.",
    "async/await is syntactic sugar over the same objects.",
    "An unawaited promise loses its errors silently."],
 r:["Asynchronous Programming","Callback","Event Loop","JavaScript"]},

{t:"Serialisation", d:"Converting an in-memory object into a byte or text format that can be stored or transmitted.", l:"core", g:["fundamentals","data"],
 b:["Objects live in memory as pointers and structures that mean nothing outside the process. Serialisation flattens them into JSON, Protobuf, MessagePack or a binary format so they can cross a network or be written to disk; deserialisation reverses it.",
    "Text formats are human-readable and debuggable; binary formats are smaller and faster. The security warning is absolute: never deserialise untrusted data with a format that can instantiate arbitrary objects — Python's pickle, Java's native serialisation and PHP's unserialize have all produced remote code execution vulnerabilities."],
 k:["JSON and YAML are readable; Protobuf and Avro are compact and fast.",
    "Schema-based formats give versioning and compatibility rules.",
    "Never deserialise untrusted input with pickle or Java serialisation.",
    "Round-trip test it — floats, dates and encodings lose fidelity easily."],
 r:["JSON","Avro","API","Deserialisation Attack"]},

{t:"Unicode", d:"A universal character encoding standard assigning a unique code point to every character in every writing system.", l:"core", g:["fundamentals","text"],
 b:["Unicode maps characters to code points; UTF-8 is the dominant way to encode those code points as bytes, using one byte for ASCII and up to four for others. That backward compatibility with ASCII is why UTF-8 won.",
    "The complications are real: a single visible character (a grapheme) may be several code points — an emoji with a skin-tone modifier, or an accented letter composed of a base plus a combining mark. String length, reversal and truncation are therefore all subtler than they look. Normalise to NFC before comparing strings."],
 k:["Code points are characters; UTF-8 is the byte encoding.",
    "UTF-8 is ASCII-compatible for the first 128 characters.",
    "One grapheme can be multiple code points — length is ambiguous.",
    "Normalise (NFC) before comparing or hashing user-supplied strings."],
 r:["Serialisation","Tokenisation","Text Normalisation"]},

{t:"Floating Point", d:"A binary representation of real numbers that trades exactness for range.", l:"core", g:["fundamentals","numeric"],
 b:["IEEE 754 stores a sign, exponent and mantissa, so numbers span an enormous range with limited precision. Because it is binary, decimals such as 0.1 have no exact representation, which is why `0.1 + 0.2 == 0.3` is false in nearly every language.",
    "Never use floating point for money — use integers of the smallest unit or a decimal type. Compare floats with a tolerance rather than equality, and be aware that adding a small number to a very large one can silently do nothing."],
 k:["0.1 + 0.2 ≠ 0.3 — binary cannot represent decimal fractions exactly.",
    "Never represent money as a float; use integer minor units or decimal.",
    "Compare with a tolerance, not with `==`.",
    "fp32, fp16 and bf16 trade precision for speed in machine learning."],
 r:["Integer Overflow","Quantisation","Mixed Precision Training"]},

{t:"Integer Overflow", d:"What happens when an arithmetic result exceeds the range its integer type can hold.", l:"intermediate", g:["failure-mode","numeric"],
 b:["A 32-bit signed integer holds up to 2,147,483,647. Add one and it wraps to −2,147,483,648 in languages that use two's complement wrapping. That silent wraparound has caused real failures, from the Ariane 5 explosion to countless buffer size miscalculations that became security vulnerabilities.",
    "Languages differ: C wraps (and signed overflow is undefined behaviour), Java wraps, Rust panics in debug builds, Python promotes to arbitrary precision automatically. Know which your language does before trusting arithmetic on user input."],
 k:["32-bit signed maximum is 2,147,483,647; the next increment wraps negative.",
    "A frequent root cause of buffer-size security bugs.",
    "Python has arbitrary precision integers; most languages do not.",
    "Signed overflow is undefined behaviour in C and C++."],
 r:["Floating Point","Bit Manipulation","Buffer Overflow","Memory Safety"]},

{t:"State Machine", d:"A model of computation with a finite set of states and defined transitions between them.", l:"intermediate", g:["pattern","design"],
 b:["A state machine has states, events that trigger transitions, and rules about which transitions are legal. An order can be pending, paid, shipped or cancelled, and only certain moves between those are valid. Making that explicit prevents an entire class of invalid-state bugs.",
    "State machines appear in protocol implementations, UI flows, workflow engines, regular expression engines and lexers. Modelling explicitly beats scattering boolean flags across a codebase, where invalid combinations become possible by accident."],
 k:["Finite states, defined events, explicit legal transitions.",
    "Makes invalid states unrepresentable rather than merely unlikely.",
    "Replaces sprawling boolean flag combinations.",
    "Underpins protocols, workflows, parsers and UI flows."],
 r:["Algorithm","Design Pattern","Parser"]},

{t:"Turing Complete", d:"Capable of computing anything a Turing machine can, given enough time and memory.", l:"advanced", g:["theory","fundamentals"],
 b:["A system is Turing complete if it supports conditional branching and unbounded loops or recursion. Every general-purpose programming language qualifies, which means they are all equivalent in what they can compute — differences are about convenience, safety and performance, not capability.",
    "Accidental Turing completeness is a recurring source of surprise and of security problems: TypeScript's type system, Excel formulas, Magic: The Gathering and various configuration languages have all been shown Turing complete, which also means their behaviour is undecidable in general."],
 k:["Requires conditional branching plus unbounded iteration.",
    "All general-purpose languages are computationally equivalent.",
    "Accidental Turing completeness makes analysis undecidable.",
    "Deliberately non-Turing-complete languages (SQL core, Bitcoin Script) guarantee termination."],
 r:["Halting Problem","Algorithm","Domain-Specific Language"]},

{t:"Halting Problem", d:"The proven impossibility of writing a program that decides whether any given program will finish.", l:"advanced", g:["theory","fundamentals"],
 b:["Turing proved in 1936 that no general algorithm can determine, for every program and input, whether it halts. The proof is a diagonalisation argument: assume such a checker exists, build a program that does the opposite of what the checker predicts, and derive a contradiction.",
    "The practical consequences are everywhere: no static analyser can catch all infinite loops, no compiler can perfectly optimise every program, and no antivirus can decide with certainty whether arbitrary code is malicious. Real tools use conservative approximation — they may say maybe."],
 k:["No general algorithm can decide halting for all programs.",
    "Implies most interesting properties of programs are undecidable.",
    "Static analysers must be conservative and accept false positives.",
    "This is why perfect malware detection is impossible in principle."],
 r:["Turing Complete","Algorithm","Static Analysis","Formal Verification"]},

{t:"Scope", d:"The region of a program in which a name refers to a particular variable.", l:"core", g:["fundamentals","language"],
 b:["Scope determines visibility and lifetime. Block scope confines a variable to its braces; function scope to the whole function; module and global scope widen it further. Lexical scoping — the norm — resolves names by where the code is written, not by where it is called from.",
    "Shadowing, where an inner scope declares a name already used outside, is legal and frequently confusing. Broad scope is a maintenance liability: the narrower a variable's scope, the less code you must read to understand it."],
 k:["Lexical scoping resolves names by code position, not call site.",
    "`var` is function-scoped in JavaScript; `let` and `const` are block-scoped.",
    "Shadowing is legal and a common source of confusion.",
    "Declare variables in the narrowest scope that works."],
 r:["Closure","Functional Programming","JavaScript"]},

{t:"Idempotency", d:"The property that performing an operation multiple times has the same effect as performing it once.", l:"core", g:["fundamentals","reliability"],
 b:["Setting a value to 5 is idempotent; incrementing by 1 is not. This matters enormously in distributed systems, where a network timeout leaves you unsure whether the request arrived, and the safe response is to retry — which is only safe if the operation is idempotent.",
    "HTTP GET, PUT and DELETE are defined as idempotent; POST is not. The standard technique for making an unsafe operation safe is an idempotency key: the client sends a unique id, and the server records it so a repeat request returns the original result rather than acting twice."],
 k:["Doing it twice equals doing it once.",
    "Makes retries safe, which is essential when networks are unreliable.",
    "GET, PUT and DELETE are idempotent by specification; POST is not.",
    "Idempotency keys make payment and order creation safely retryable."],
 x:{lang:"sql", code:'-- not idempotent: two retries create two rows\nINSERT INTO orders (customer_id, total) VALUES (42, 99.50);\n\n-- idempotent: a repeat with the same key changes nothing\nINSERT INTO orders (idempotency_key, customer_id, total)\nVALUES ($1, 42, 99.50)\nON CONFLICT (idempotency_key) DO NOTHING\nRETURNING id;'},
 r:["REST","Retry","Data Pipeline","Exactly-Once Semantics","Side Effect"]}

]);
