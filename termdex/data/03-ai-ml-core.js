TD.add("ai-ml-core", [

{t:"Artificial Intelligence", a:"AI", d:"The broad field of building systems that perform tasks normally requiring human intelligence — the umbrella that machine learning sits inside.", l:"core", g:["fundamentals","ai"],
 b:["AI is the outer circle. Inside it sits machine learning (systems that learn from data), and inside that sits deep learning (machine learning with many-layered neural networks). Rule-based expert systems, search algorithms, planners and constraint solvers are all AI too, and none of them learn anything.",
    "The definition is famously unstable — the AI effect describes how each solved problem stops counting as AI. Chess, OCR, speech recognition and translation were all AI until they worked, at which point they became just software."],
 k:["AI ⊃ machine learning ⊃ deep learning — three nested circles, not synonyms.",
    "Symbolic AI (rules, search, logic) involves no learning at all.",
    "Narrow AI does one task well; general AI does not exist.",
    "The AI effect: once it works reliably, people stop calling it AI."],
 dg:"ml-paradigms",
 r:["Machine Learning","Deep Learning","Expert System","Large Language Model"]},

{t:"Machine Learning", a:"ML", d:"Building systems that improve at a task by learning patterns from data, instead of being explicitly programmed with rules.", l:"core", g:["fundamentals","ai"],
 b:["The traditional approach is: write rules, get answers. Machine learning inverts it: supply examples with answers, and the algorithm derives the rules. That matters enormously for problems where nobody can articulate the rules — recognising a cat, judging whether a transaction is fraudulent, predicting next month's demand.",
    "A model is just a function with parameters fitted to data. Training adjusts those parameters to minimise a loss; inference applies the fitted function to new inputs. Everything else — architectures, optimisers, regularisation — is machinery around that single idea."],
 k:["Learns rules from examples rather than having them coded by hand.",
    "Three ingredients: data, a model with parameters, a loss to minimise.",
    "Generalising to unseen data is the goal; memorising training data is the failure.",
    "Data quality dominates model choice more often than people expect."],
 dg:"ml-paradigms",
 r:["Artificial Intelligence","Supervised Learning","Deep Learning","Overfitting","Model"]},

{t:"Supervised Learning", d:"Learning from labelled examples, where each input comes paired with the correct output.", l:"core", g:["paradigm","training"],
 b:["You give the algorithm thousands of emails already marked spam or not spam, and it learns a mapping from email to label. This is by far the most common form of machine learning in production, splitting into classification (predict a category) and regression (predict a number).",
    "The bottleneck is labels. They are expensive, often need domain experts, and are frequently inconsistent — two radiologists may disagree on the same scan. A large share of applied ML work is really label acquisition and quality control."],
 k:["Requires labelled data: every training example has a known correct answer.",
    "Classification predicts categories; regression predicts continuous values.",
    "Label quality caps model quality — noisy labels mean a noisy ceiling.",
    "Labelling cost is the usual practical constraint, not compute."],
 dg:"ml-paradigms",
 r:["Unsupervised Learning","Classification","Regression","Training Data","Semi-Supervised Learning"]},

{t:"Unsupervised Learning", d:"Finding structure in data that has no labels — grouping, compressing, or spotting what does not fit.", l:"core", g:["paradigm","training"],
 b:["With no answers to learn from, the algorithm looks for structure that is inherently there: clusters of similar customers, the few dimensions that carry most of the variance, points that sit far from everything else.",
    "Evaluation is the hard part. Without ground truth there is no accuracy to compute, so you rely on internal metrics like silhouette score, on downstream task performance, or on a domain expert saying yes, those groups are real."],
 k:["No labels — the algorithm finds structure on its own.",
    "Clustering, dimensionality reduction and anomaly detection are the main tasks.",
    "Evaluation is genuinely difficult without ground truth.",
    "Often used for exploration before a supervised model is built."],
 dg:"kmeans",
 r:["Supervised Learning","K-Means Clustering","Principal Component Analysis","Anomaly Detection"]},

{t:"Semi-Supervised Learning", d:"Training on a small labelled set plus a large unlabelled set, using the unlabelled data to sharpen the decision boundary.", l:"intermediate", g:["paradigm","training"],
 b:["Labels are expensive; raw data usually is not. Semi-supervised methods exploit that asymmetry. Pseudo-labelling trains on the labelled data, predicts on the unlabelled data, and adds the confident predictions back as training examples. Consistency regularisation instead requires that a model give the same answer for two augmented views of the same unlabelled input.",
    "It works when the unlabelled data comes from the same distribution and the cluster assumption holds — that points in the same dense region share a label. When it does not, pseudo-labelling amplifies the model's own mistakes."],
 k:["Uses a small labelled set plus abundant unlabelled data.",
    "Pseudo-labelling and consistency regularisation are the two main families.",
    "Assumes the unlabelled data shares the labelled data's distribution.",
    "Confidence thresholds matter — low ones feed the model its own errors."],
 r:["Supervised Learning","Self-Supervised Learning","Active Learning","Data Augmentation"]},

{t:"Self-Supervised Learning", d:"Creating labels automatically from the structure of the data itself, so no human annotation is needed.", l:"intermediate", g:["paradigm","training"],
 b:["Hide part of the input and ask the model to predict it. Mask a word and predict it (BERT), predict the next token (GPT), rotate an image and predict the angle, or push two augmented views of the same image together in embedding space (SimCLR, DINO). The supervision comes free from the data.",
    "This is what made foundation models possible. The internet is an effectively infinite labelled dataset if the label is the next token, which is why pre-training scaled the way it did while supervised datasets plateaued."],
 k:["The label is derived from the data — no annotators required.",
    "Next-token prediction and masked-token prediction power modern LLMs.",
    "Contrastive methods learn by pulling similar pairs together in embedding space.",
    "The reason foundation models could scale to trillions of tokens."],
 r:["Large Language Model","Pre-training","Embedding","Transformer"]},

{t:"Reinforcement Learning", a:"RL", d:"Learning by acting in an environment and adjusting behaviour based on rewards and penalties.", l:"intermediate", g:["paradigm","training"],
 b:["An agent observes a state, takes an action, receives a reward and lands in a new state. Over many episodes it learns a policy that maximises cumulative reward. Nobody tells it the right action; it discovers which actions pay off.",
    "The hard parts are credit assignment (which of the last two hundred moves caused the loss?) and the exploration-exploitation trade-off. RL powers game-playing systems, robotics control, recommendation and — through RLHF — the alignment of large language models to human preferences."],
 k:["Reward signal, not labels: the agent learns from consequences.",
    "Exploration versus exploitation is the central dilemma.",
    "Sparse and delayed rewards make credit assignment hard.",
    "RLHF applies it to align LLMs with human preferences."],
 dg:"ml-paradigms",
 r:["RLHF","Markov Decision Process"]},

{t:"Classification", d:"A supervised task where the model predicts which discrete category an input belongs to.", l:"core", g:["task","supervised"],
 b:["Binary classification chooses between two classes (spam or not); multi-class picks one of many (which digit); multi-label assigns any number simultaneously (which tags apply to this article). Most classifiers output a probability per class, and a threshold converts that into a decision.",
    "That threshold is a business decision, not a modelling one. For cancer screening you tolerate false alarms to avoid missing a case; for an automated bank transfer you do the opposite. Moving the threshold trades precision against recall without retraining anything."],
 k:["Binary, multi-class and multi-label are three different problems.",
    "Models output probabilities; the threshold turns them into decisions.",
    "Threshold choice encodes the relative cost of each error type.",
    "Accuracy is misleading whenever classes are imbalanced."],
 dg:"confusion-matrix",
 r:["Regression","Confusion Matrix","Precision","Recall","Logistic Regression"]},

{t:"Regression", d:"A supervised task where the model predicts a continuous numeric value.", l:"core", g:["task","supervised"],
 b:["House price, tomorrow's demand, expected time to delivery — anything on a continuous scale. The model minimises a distance-based loss such as mean squared error, and is evaluated with MAE, RMSE or R².",
    "The choice of loss shapes behaviour more than people expect. MSE squares errors, so it chases outliers aggressively; MAE treats all errors linearly and is more robust; Huber loss switches between the two. Pick the one that matches how the business feels about a large miss."],
 k:["Predicts a number on a continuous scale, not a category.",
    "MSE punishes large errors quadratically; MAE treats them linearly.",
    "R² reports the share of variance explained — and can be negative.",
    "Always compare against a naive baseline such as the mean or last value."],
 r:["Classification","Linear Regression","Mean Squared Error","Loss Function"]},

{t:"Clustering", d:"Grouping unlabelled data points so that points in the same group are more similar to each other than to points elsewhere.", l:"core", g:["task","unsupervised"],
 b:["K-means partitions data into k spherical clusters by minimising distance to centroids. DBSCAN finds arbitrarily shaped dense regions and labels sparse points as noise. Hierarchical clustering builds a tree you can cut at any level to get any number of clusters.",
    "Distance metric and feature scaling dominate the outcome: an unscaled feature measured in thousands will swamp one measured in fractions. Choosing k is a judgement call supported by the elbow method or silhouette score, never determined by them."],
 k:["Scale your features first — distance metrics are not scale-invariant.",
    "K-means assumes roughly spherical, similarly sized clusters.",
    "DBSCAN handles arbitrary shapes and identifies noise; k-means cannot.",
    "The elbow and silhouette methods suggest k; domain knowledge decides it."],
 dg:"kmeans",
 r:["K-Means Clustering","DBSCAN","Unsupervised Learning","Feature Scaling"]},

{t:"Dimensionality Reduction", d:"Reducing the number of input features while keeping as much useful information as possible.", l:"intermediate", g:["task","unsupervised"],
 b:["High-dimensional data is sparse, slow and hard to visualise. Reduction techniques project it into fewer dimensions: PCA finds the linear directions of greatest variance, t-SNE and UMAP produce non-linear 2-D maps for visualisation, and autoencoders learn a compressed representation with a neural network.",
    "Use PCA when you need a reversible linear transform to feed downstream models. Use t-SNE or UMAP when you want to look at the data — but never read distances between clusters on those plots as meaningful, because they are not."],
 k:["Fights the curse of dimensionality and speeds up downstream models.",
    "PCA is linear and reversible; t-SNE and UMAP are neither.",
    "Distances between clusters on a t-SNE plot are not meaningful.",
    "Fit the reduction on training data only, then apply it to validation and test."],
 dg:"pca",
 r:["Principal Component Analysis","t-SNE","UMAP","Curse of Dimensionality","Autoencoder"]},

{t:"Anomaly Detection", d:"Identifying data points that differ enough from the norm to be worth investigating.", l:"intermediate", g:["task","unsupervised"],
 b:["Fraud, equipment failure, network intrusion and data quality problems all present as rare, unusual points. Because anomalies are rare and often unlabelled, this is usually framed as unsupervised: learn what normal looks like, then flag distance from it. Isolation Forest, one-class SVM, autoencoder reconstruction error and statistical z-scores are the common tools.",
    "The operational challenge is the base rate. At 0.1% fraud, even a 99% accurate detector produces mostly false positives, and alert fatigue kills the system faster than poor accuracy does."],
 k:["Anomalies are rare, so precision matters far more than accuracy.",
    "Isolation Forest, one-class SVM and autoencoder reconstruction error are standard.",
    "Normal behaviour drifts — the definition of anomalous must be updated.",
    "Alert fatigue is the usual failure mode in production."],
 r:["Unsupervised Learning","Class Imbalance","Precision","Model Drift"]},

{t:"Feature", d:"An individual measurable property of the data, used as an input to a model.", l:"core", g:["data","fundamentals"],
 b:["A feature is a column: age, price, number of previous purchases, whether the email contains a link. Models see nothing except features, so what you give them defines the ceiling of what they can learn. Classical ML depends heavily on features being hand-designed; deep learning learns them from raw input instead.",
    "Features come in types that need different handling: numeric (scale them), categorical (encode them), ordinal (preserve the order), temporal (extract components and be careful about leakage), and text or image (embed them)."],
 k:["Models see only features — everything else is invisible to them.",
    "Numeric, categorical, ordinal, temporal and unstructured need different treatment.",
    "Deep learning learns features from raw inputs; classical ML needs them designed.",
    "Any feature unavailable at prediction time is useless, however predictive."],
 r:["Feature Engineering","Feature Selection","One-Hot Encoding","Feature Scaling"]},

{t:"Feature Engineering", d:"Creating, transforming and selecting input variables to make patterns easier for a model to learn.", l:"core", g:["data","workflow"],
 b:["Good feature engineering routinely beats a fancier algorithm. Extracting day-of-week and is-holiday from a timestamp, computing a ratio instead of two raw numbers, aggregating a user's last 30 days of behaviour, binning a skewed variable — each encodes domain knowledge the model cannot discover from a few thousand rows.",
    "The cardinal rule is that every feature must be computable at prediction time using only information available then. Aggregating over a window that includes the future is the most common form of data leakage, and it produces spectacular validation scores followed by a useless production model."],
 k:["Domain knowledge encoded as features usually beats a bigger model.",
    "Ratios, differences, aggregates over time windows and interaction terms are the workhorses.",
    "Any feature that peeks at the future is leakage.",
    "A feature store keeps training and serving computations identical."],
 r:["Feature","Data Leakage","Feature Selection","Feature Store","Feature Scaling"]},

{t:"Feature Selection", d:"Choosing the subset of features that actually helps, and discarding the rest.", l:"intermediate", g:["data","workflow"],
 b:["More features is not better. Irrelevant ones add noise, correlated ones destabilise coefficients, and every extra dimension makes the space sparser. Selection methods fall into three groups: filters (rank by correlation or mutual information), wrappers (train repeatedly with different subsets), and embedded methods (L1 regularisation and tree importances, which select while fitting).",
    "Selection must happen inside the cross-validation loop. Choosing features using the whole dataset and then cross-validating leaks test information into the selection and inflates your score."],
 k:["Filter, wrapper and embedded are the three families.",
    "L1 regularisation performs selection by driving coefficients to exactly zero.",
    "Do selection inside the CV fold, never before splitting.",
    "Fewer features also means faster inference and simpler monitoring."],
 r:["Feature Engineering","L1 and L2 Regularisation","Cross-Validation","Curse of Dimensionality"]},

{t:"Feature Scaling", a:"Normalisation / Standardisation", d:"Putting numeric features onto a comparable scale so no single one dominates by virtue of its units.", l:"core", g:["preprocessing","data"],
 b:["Min-max normalisation squeezes values into [0, 1]. Standardisation subtracts the mean and divides by the standard deviation, giving mean 0 and variance 1. Robust scaling uses the median and interquartile range, which resists outliers.",
    "Any algorithm that measures distance or uses gradient descent needs this: KNN, SVM, k-means, PCA and every neural network. Tree-based models do not care, because they split on thresholds within a single feature. Critically, fit the scaler on training data only and reuse it — fitting on everything leaks test statistics."],
 k:["Required for distance-based and gradient-based models; irrelevant for trees.",
    "Standardisation for roughly normal data, min-max for bounded ranges, robust when outliers exist.",
    "Fit on train only; transform validation and test with those parameters.",
    "Save the scaler with the model — serving must apply the identical transform."],
 x:{lang:"python", code:'from sklearn.preprocessing import StandardScaler\n\nscaler = StandardScaler()\nX_train_s = scaler.fit_transform(X_train)   # fit ONLY on train\nX_test_s  = scaler.transform(X_test)        # reuse the same statistics'},
 r:["Feature Engineering","Data Leakage","K-Nearest Neighbours","Gradient Descent"]},

{t:"One-Hot Encoding", d:"Turning a categorical variable into a set of binary columns, one per category.", l:"core", g:["preprocessing","data"],
 b:["A colour column with values red, green and blue becomes three columns holding 0 or 1. This avoids implying an order that does not exist — encoding them as 1, 2, 3 would tell a linear model that blue is three times red, which is meaningless.",
    "The problem is high cardinality: a column of 50,000 product ids becomes 50,000 columns. Alternatives are target encoding (replace with the mean target per category, carefully, inside CV folds), hashing, or learned embeddings — which is what neural networks do."],
 k:["Prevents a model from inferring a false ordering between categories.",
    "Explodes on high-cardinality columns — thousands of sparse features.",
    "Drop one column to avoid perfect collinearity in linear models.",
    "Handle unseen categories at inference time explicitly, or serving will crash."],
 x:{lang:"python", code:'import pandas as pd\n\ndf = pd.DataFrame({"colour": ["red", "green", "blue", "red"]})\npd.get_dummies(df, columns=["colour"], drop_first=True)\n#    colour_green  colour_red\n# 0             0           1\n# 1             1           0'},
 r:["Feature Engineering","Label Encoding","Embedding"]},

{t:"Label Encoding", d:"Replacing each category with an integer — appropriate for ordinal data, misleading for nominal data.", l:"core", g:["preprocessing","data"],
 b:["Mapping small, medium, large to 0, 1, 2 preserves genuine order and is correct. Mapping London, Tokyo, Cairo to 0, 1, 2 invents an order that does not exist, and a linear model will happily conclude that Cairo is twice Tokyo.",
    "Tree-based models are more forgiving because they split on thresholds rather than assuming linearity, which is why label encoding often works fine with gradient boosting and badly with logistic regression."],
 k:["Correct for ordinal categories where the order is real.",
    "Wrong for nominal categories in linear and distance-based models.",
    "Trees tolerate it; linear models and KNN do not.",
    "Persist the encoder mapping — serving must use the identical assignment."],
 r:["One-Hot Encoding","Feature Engineering"]},

{t:"Training Data", d:"The labelled examples a model learns its parameters from.", l:"core", g:["data","workflow"],
 b:["Training data determines what the model can possibly know. If a demographic, a failure mode or a season is absent, the model has no basis for handling it. Every bias in the data becomes a bias in the model, and no amount of architecture fixes that.",
    "Quantity matters less than people assume once you pass a threshold; representativeness and label accuracy matter more. A thousand carefully labelled diverse examples often beat a hundred thousand noisy ones scraped from one source."],
 k:["The model cannot learn what the data does not contain.",
    "Bias in, bias out — and it is hard to detect after the fact.",
    "Label quality usually beats raw quantity.",
    "Training data must resemble the data you will see in production."],
 dg:"train-split",
 r:["Training, Validation and Test Split","Data Leakage","Bias","Data Augmentation"]},

{t:"Training, Validation and Test Split", d:"Dividing data into three parts so model selection and final evaluation stay honest.", l:"core", g:["evaluation","workflow"],
 b:["The training set fits parameters. The validation set guides your choices — which architecture, which hyperparameters, when to stop. The test set is opened once, at the end, to estimate real-world performance. A typical split is 70/15/15.",
    "The test set stops being a test set the moment you use it to make a decision. Every time you look at test performance and change something, you leak a little information into the model, and your final number becomes optimistic. For time-ordered data, split by time — a random split lets the model learn from the future."],
 k:["Train fits, validation selects, test estimates — once.",
    "Repeatedly tuning against the test set is a slow form of overfitting.",
    "Time series must be split chronologically, never randomly.",
    "Stratify the split when classes are imbalanced."],
 dg:"train-split",
 r:["Cross-Validation","Overfitting","Data Leakage","Training Data"]},

{t:"Cross-Validation", d:"Rotating which slice of data serves as validation, so the estimate of performance does not depend on one lucky split.", l:"core", g:["evaluation","workflow"],
 b:["K-fold cross-validation splits data into k parts, trains k times each holding out a different part, and averages the scores. With k=5 every point is used for validation exactly once, giving a more stable estimate plus a standard deviation that tells you how sensitive the model is to the split.",
    "Variants matter: stratified k-fold preserves class ratios, group k-fold keeps all rows from one entity in the same fold (essential when the same user appears many times), and time-series split only ever validates on data later than the training window."],
 k:["Averaging over k folds gives a less noisy estimate than a single split.",
    "Use stratified folds for imbalanced classification.",
    "Group folds when rows are not independent — same user, same patient, same device.",
    "Time-series CV must never train on data from after the validation window."],
 x:{lang:"python", code:'from sklearn.model_selection import cross_val_score, StratifiedKFold\n\ncv = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)\nscores = cross_val_score(model, X, y, cv=cv, scoring="f1")\nprint(f"{scores.mean():.3f} +/- {scores.std():.3f}")'},
 r:["Training, Validation and Test Split","Overfitting","Hyperparameter Tuning","Data Leakage"]},

{t:"Overfitting", d:"When a model learns the noise and quirks of the training data instead of the underlying pattern, and fails on new data.", l:"core", g:["failure-mode","training"],
 b:["The signature is a large gap between training and validation performance: 99% on training, 71% on validation. The model has effectively memorised. This happens when the model has more capacity than the data supports, when training runs too long, or when there is simply not enough data.",
    "The remedies are all forms of constraint: more data, simpler models, regularisation, dropout, early stopping, data augmentation, or ensembling. Cross-validation is how you detect it before production does."],
 k:["Symptom: training score far above validation score.",
    "Causes: too much model capacity, too little data, too many epochs.",
    "Fixes: more data, regularisation, dropout, early stopping, augmentation.",
    "A model that scores perfectly on training data should worry you, not please you."],
 dg:"overfitting",
 r:["Underfitting","Regularisation","Bias-Variance Trade-off","Cross-Validation","Early Stopping"]},

{t:"Underfitting", d:"When a model is too simple to capture the real pattern, and performs poorly even on its training data.", l:"core", g:["failure-mode","training"],
 b:["Underfitting shows up as poor performance everywhere — training and validation alike. A straight line cannot fit a curve no matter how much data you give it. Causes are insufficient model capacity, over-aggressive regularisation, missing informative features, or stopping training too early.",
    "It is much easier to fix than overfitting: add capacity, add features, reduce regularisation, train longer. The standard workflow is to deliberately overfit a small sample first, proving the model can learn at all, then add constraints."],
 k:["Symptom: poor performance on training data itself.",
    "Causes: too little capacity, too much regularisation, weak features.",
    "Easier to fix than overfitting.",
    "Sanity check: a healthy model should be able to overfit a tiny sample perfectly."],
 dg:"overfitting",
 r:["Overfitting","Bias-Variance Trade-off","Model Capacity","Feature Engineering"]},

{t:"Bias-Variance Trade-off", d:"The tension between a model too rigid to capture the pattern and one so flexible it captures noise.", l:"core", g:["theory","training"],
 b:["Bias is error from wrong assumptions — a linear model applied to a curved relationship. Variance is error from sensitivity to the particular training sample — a deep tree that would look completely different on slightly different data. Total error decomposes into bias², variance and irreducible noise.",
    "Increasing complexity lowers bias and raises variance; the total error curve is a U, and you want its minimum. Modern deep networks complicate the classic picture with double descent, where error falls again past the interpolation threshold — but the intuition remains the right first tool."],
 k:["High bias = underfitting; high variance = overfitting.",
    "Total error = bias² + variance + irreducible noise.",
    "Bagging mainly cuts variance; boosting mainly cuts bias.",
    "Very large networks can escape the classic U through double descent."],
 dg:"bias-variance",
 r:["Overfitting","Underfitting","Regularisation","Ensemble Learning"]},

{t:"Regularisation", d:"Any technique that constrains a model to discourage it from fitting noise.", l:"core", g:["training","technique"],
 b:["Regularisation adds a penalty for complexity. L2 (ridge) penalises the sum of squared weights, shrinking them all toward zero. L1 (lasso) penalises absolute values and drives some to exactly zero, performing feature selection. Elastic net blends both.",
    "Beyond weight penalties, dropout randomly disables neurons during training, early stopping halts before memorisation begins, data augmentation manufactures variety, and weight decay is L2 applied in the optimiser. All of them say the same thing: prefer the simpler explanation."],
 k:["Penalises complexity so the model prefers simpler fits.",
    "L2 shrinks weights; L1 zeroes some out and thus selects features.",
    "Dropout, early stopping and augmentation regularise without touching the loss.",
    "The regularisation strength is a hyperparameter to tune, not a constant."],
 dg:"bias-variance",
 r:["L1 and L2 Regularisation","Overfitting","Dropout","Early Stopping","Weight Decay"]},

{t:"L1 and L2 Regularisation", a:"Lasso and Ridge", d:"Two ways of penalising large model weights: L1 by absolute value, L2 by square.", l:"intermediate", g:["training","technique"],
 b:["L2 adds λ·Σw² to the loss. Its gradient is proportional to the weight, so weights shrink smoothly toward zero without ever reaching it — every feature keeps a small say. This is the default and it handles correlated features gracefully by spreading weight among them.",
    "L1 adds λ·Σ|w|. Its gradient is constant, so weights are pushed to exactly zero and stay there, producing a sparse model that has effectively selected a feature subset. Use L1 when you want interpretability or genuine selection; use elastic net when features are correlated and you still want sparsity."],
 k:["L2 (ridge): smooth shrinkage, keeps all features, handles correlation well.",
    "L1 (lasso): produces exact zeros, giving automatic feature selection.",
    "Elastic net combines both and is often the safe default.",
    "λ controls strength — too high underfits, too low does nothing."],
 x:{lang:"python", code:'from sklearn.linear_model import Ridge, Lasso, ElasticNet\n\nridge = Ridge(alpha=1.0).fit(X, y)          # shrinks all coefficients\nlasso = Lasso(alpha=0.1).fit(X, y)          # many coefficients become 0\nnet   = ElasticNet(alpha=0.1, l1_ratio=0.5).fit(X, y)'},
 r:["Regularisation","Feature Selection","Linear Regression","Overfitting"]},

{t:"Hyperparameter", d:"A configuration value you set before training, as opposed to a parameter the model learns during it.", l:"core", g:["training","tuning"],
 b:["Learning rate, tree depth, number of layers, regularisation strength, k in k-means, batch size — none of these are learned from data. You choose them, train, evaluate on validation, and choose again.",
    "The distinction matters because hyperparameters must be selected using validation data, never test data. Model parameters — weights, coefficients, split thresholds — are what gradient descent or the fitting algorithm produces."],
 k:["Set before training; parameters are learned during it.",
    "Tuned against the validation set, never the test set.",
    "Learning rate is usually the single most impactful one for neural networks.",
    "Always record the hyperparameters that produced a given result."],
 r:["Hyperparameter Tuning","Grid Search","Learning Rate","Model"]},

{t:"Hyperparameter Tuning", d:"Systematically searching for the hyperparameter values that give the best validation performance.", l:"core", g:["training","tuning"],
 b:["Grid search tries every combination on a predefined grid — thorough, exponentially expensive. Random search samples combinations at random and, counter-intuitively, usually finds better configurations for the same budget, because only a few hyperparameters actually matter and random sampling explores those more finely.",
    "Bayesian optimisation (Optuna, Hyperopt) models the relationship between settings and score and picks the next trial intelligently. Whatever the method, tune against validation folds and report the final number on the untouched test set."],
 k:["Random search beats grid search at equal budget in most real problems.",
    "Bayesian optimisation is more sample-efficient again — use it when trials are expensive.",
    "Tune on validation, report on test, or your number is inflated.",
    "Log every trial; the search itself is an experiment worth reproducing."],
 x:{lang:"python", code:'import optuna\n\ndef objective(trial):\n    params = {\n        "max_depth": trial.suggest_int("max_depth", 3, 12),\n        "learning_rate": trial.suggest_float("learning_rate", 1e-3, 0.3, log=True),\n    }\n    return cross_val_score(make_model(params), X, y, cv=5).mean()\n\nstudy = optuna.create_study(direction="maximize")\nstudy.optimize(objective, n_trials=50)'},
 r:["Hyperparameter","Grid Search","Cross-Validation","AutoML"]},

{t:"Grid Search", d:"Exhaustively trying every combination of a specified set of hyperparameter values.", l:"intermediate", g:["training","tuning"],
 b:["You list candidate values per hyperparameter and the search evaluates the full Cartesian product, usually with cross-validation at each point. It is simple, parallelisable and reproducible.",
    "Its weakness is combinatorial: three hyperparameters with five values each is 125 fits, and adding a fourth makes it 625. Since typically only one or two hyperparameters matter much, most of that compute is spent distinguishing between configurations that are effectively identical."],
 k:["Evaluates every combination — cost grows exponentially with the number of knobs.",
    "Fully parallelisable across machines.",
    "Wastes budget on hyperparameters that barely affect the result.",
    "Fine for two or three parameters; use random or Bayesian search beyond that."],
 r:["Hyperparameter Tuning","Cross-Validation"]},

{t:"Loss Function", d:"The function that measures how wrong a model's prediction is — the quantity training tries to minimise.", l:"core", g:["training","theory"],
 b:["The loss defines what better means. Mean squared error for regression, cross-entropy for classification, hinge loss for SVMs, and countless task-specific ones. Training is nothing more than adjusting parameters to push this number down.",
    "Choosing a loss is choosing an objective. If false negatives cost ten times more than false positives, a weighted loss encodes that directly, and the model will behave accordingly. A mismatch between the loss and the actual business goal is one of the most common quiet failures in applied ML."],
 k:["Defines the objective — the model optimises exactly this and nothing else.",
    "MSE for regression, cross-entropy for classification, custom for everything else.",
    "Must be differentiable for gradient-based training.",
    "Class weights inside the loss are how you encode asymmetric error costs."],
 r:["Cost Function","Gradient Descent","Cross-Entropy","Mean Squared Error"]},

{t:"Cost Function", d:"The loss averaged over the whole training set — the single number that gradient descent minimises.", l:"intermediate", g:["training","theory"],
 b:["The terms are used loosely, but the conventional distinction is that a loss function measures error on one example while a cost function aggregates it across the dataset, often adding regularisation terms.",
    "The shape of the cost surface determines how easy training is. Convex surfaces (linear and logistic regression) have one global minimum and are reliably solvable. Neural network surfaces are non-convex with many local minima and saddle points — which turns out to matter far less in high dimensions than early theory feared."],
 k:["Loss is per-example; cost is the aggregate plus regularisation.",
    "Convex costs have a single global minimum and reliable convergence.",
    "Neural network costs are non-convex but optimise well in practice.",
    "Plotting the cost curve is the first diagnostic when training goes wrong."],
 r:["Loss Function","Gradient Descent","Convex Optimisation","Regularisation"]},

{t:"Gradient Descent", d:"The optimisation algorithm that repeatedly steps parameters in the direction that most reduces the loss.", l:"core", g:["training","optimisation"],
 b:["Compute the gradient of the loss with respect to every parameter — the direction of steepest increase — then step the opposite way, scaled by the learning rate. Repeat until the loss stops improving. This is how essentially every neural network is trained.",
    "Batch gradient descent uses the whole dataset per step (accurate, slow). Stochastic uses one example (noisy, fast). Mini-batch uses 32 to 512 examples and is what everyone actually does — enough signal to be stable, small enough to fit on a GPU and to step often."],
 k:["Step downhill along the negative gradient, scaled by the learning rate.",
    "Mini-batch is the practical default: stable enough, fast enough.",
    "Too large a learning rate diverges; too small crawls.",
    "Momentum and Adam adapt the step size and direction to accelerate convergence."],
 x:{lang:"python", code:'for epoch in range(epochs):\n    for xb, yb in loader:              # mini-batches\n        preds = model(xb)\n        loss  = criterion(preds, yb)\n        optimizer.zero_grad()\n        loss.backward()                # compute gradients\n        optimizer.step()               # step downhill'},
 dg:"gradient-descent",
 r:["Stochastic Gradient Descent","Learning Rate","Loss Function","Adam Optimiser","Backpropagation"]},

{t:"Stochastic Gradient Descent", a:"SGD", d:"Gradient descent that estimates the gradient from a small random sample rather than the whole dataset.", l:"intermediate", g:["training","optimisation"],
 b:["Computing the exact gradient over ten million examples for every step is unaffordable. SGD approximates it from one example, or in practice from a mini-batch, making each step cheap and letting the model take thousands of steps per epoch instead of one.",
    "The noise is not purely a cost. It helps the optimiser escape sharp local minima and appears to bias training toward flatter minima that generalise better. Momentum smooths the trajectory; Adam adapts a per-parameter step size on top."],
 k:["Estimates the gradient from a mini-batch — cheap steps, many of them.",
    "Gradient noise helps escape poor minima and may improve generalisation.",
    "Momentum accumulates a velocity term to damp oscillation.",
    "Learning rate schedules (decay, warmup, cosine) are near-universal."],
 dg:"gradient-descent",
 r:["Gradient Descent","Adam Optimiser","Learning Rate","Batch Size"]},

{t:"Learning Rate", d:"The step size that controls how far parameters move on each gradient descent update.", l:"core", g:["training","hyperparameter"],
 b:["Too high and the loss oscillates or explodes to NaN; too low and training takes forever or stalls in a poor region. It is usually the single most important hyperparameter in deep learning, and the first thing to check when training misbehaves.",
    "Schedules do the heavy lifting: warmup ramps up from near zero to avoid destabilising early steps, then cosine or step decay reduces it as training progresses so the model can settle. A learning-rate range test — increasing it until the loss diverges — quickly finds a sensible starting point."],
 k:["The most impactful hyperparameter in neural network training.",
    "Loss going to NaN almost always means it is too high.",
    "Warmup then decay is the standard schedule for transformers.",
    "Adam adapts per-parameter rates but still needs a sane global value."],
 r:["Gradient Descent","Adam Optimiser","Hyperparameter","Learning Rate Schedule"]},

{t:"Model", d:"A function with learned parameters that maps inputs to predictions.", l:"core", g:["fundamentals"],
 b:["Strip away the mystique and a model is a parameterised function. Linear regression has one coefficient per feature; a decision tree has split thresholds; GPT-4 class models have hundreds of billions of weights. Training chooses the parameter values; inference evaluates the function.",
    "In production a model is also an artefact: a file with weights, plus the preprocessing pipeline, plus the exact library versions needed to load it. Shipping the weights alone and forgetting the preprocessing is a classic and very confusing deployment failure."],
 k:["A parameterised function — training fits the parameters, inference applies them.",
    "The deployable artefact includes preprocessing, not just weights.",
    "Version models like code: same input, same version, same output.",
    "Architecture is the shape; parameters are what was learned."],
 r:["Machine Learning","Inference","Model Deployment","Hyperparameter"]},

{t:"Inference", d:"Using a trained model to make predictions on new data.", l:"core", g:["deployment","serving"],
 b:["Training happens once (or periodically); inference happens on every request forever. Its constraints are therefore completely different — latency budgets, throughput, cost per prediction, and hardware that may be a CPU rather than a cluster of GPUs.",
    "Batch inference scores a large set offline on a schedule. Online inference answers one request at a time under a latency SLA. Streaming inference processes events as they arrive. Optimisations include quantisation, distillation, batching, caching and compiling to ONNX or TensorRT."],
 k:["Training cost is one-off; inference cost recurs on every prediction.",
    "Batch, online and streaming have very different engineering requirements.",
    "Quantisation, distillation and compilation cut latency and cost significantly.",
    "The preprocessing at inference must match training exactly, or predictions silently degrade."],
 r:["Model Deployment","Quantisation","Model Serving","Latency"]},

{t:"Generalisation", d:"A model's ability to perform well on data it has never seen — the entire point of machine learning.", l:"core", g:["theory","evaluation"],
 b:["Any model can memorise its training set. Generalisation is whether it learned a pattern that transfers. The gap between training and held-out performance is the direct measure, and shrinking it is what regularisation, more data and better validation practice are all for.",
    "Generalisation assumes the deployment distribution resembles the training distribution. When it does not — a new user segment, a pandemic, a changed upstream pipeline — performance drops regardless of how well the model validated. That is distribution shift, and it is a data problem, not a model problem."],
 k:["Measured by the gap between training and held-out performance.",
    "Assumes deployment data resembles training data — often untrue over time.",
    "More diverse data generalises better than more data from one source.",
    "Distribution shift breaks generalisation without breaking the model."],
 r:["Overfitting","Model Drift","Cross-Validation","Regularisation"]},

{t:"Data Leakage", d:"Information from outside the training set leaking into it, producing validation scores that cannot survive production.", l:"core", g:["failure-mode","evaluation"],
 b:["Leakage takes several forms. Target leakage: a feature that is a consequence of the label, like `refund_issued` when predicting fraud. Train-test contamination: scaling or imputing using statistics from the whole dataset. Temporal leakage: using future information in a time-series split. Duplicate leakage: the same record in both train and test.",
    "The symptom is a suspiciously good score — 99% accuracy on a hard problem. The discipline is to ask, for every feature, whether it would genuinely be available at prediction time and whether it could be a downstream effect of the thing you are predicting."],
 k:["Symptom: results that are too good to be true. They usually are.",
    "Fit every transformer on training data only, inside the CV fold.",
    "Ask of each feature: would this exist at prediction time?",
    "Deduplicate before splitting, and split time series chronologically."],
 r:["Cross-Validation","Feature Engineering","Training, Validation and Test Split","Overfitting"]},

{t:"Class Imbalance", d:"When one class massively outnumbers another, so a model can score well by ignoring the rare class entirely.", l:"core", g:["failure-mode","data"],
 b:["With 0.5% fraud, a model that always predicts not fraud is 99.5% accurate and completely worthless. Accuracy is the wrong metric here; precision, recall, F1 and precision-recall AUC describe what is actually happening.",
    "Remedies come in three layers: resample (oversample the minority with SMOTE, or undersample the majority), reweight (class weights in the loss so minority errors cost more), and rethreshold (move the decision boundary after training). Threshold adjustment is often the cheapest and most effective."],
 k:["Accuracy is meaningless under imbalance — use precision, recall, PR-AUC.",
    "Class weights in the loss are usually simpler and safer than resampling.",
    "SMOTE synthesises minority examples but must be applied inside the CV fold only.",
    "Moving the decision threshold requires no retraining at all."],
 dg:"confusion-matrix",
 r:["SMOTE","Precision","Recall","Confusion Matrix","Anomaly Detection"]},

{t:"SMOTE", a:"Synthetic Minority Over-sampling Technique", d:"Generating synthetic minority-class examples by interpolating between existing ones.", l:"intermediate", g:["technique","data"],
 b:["Rather than duplicating minority rows — which just makes the model more confident about the same points — SMOTE picks a minority example, finds its k nearest minority neighbours, and creates a new point somewhere on the line between them. The minority region gets filled in rather than merely reweighted.",
    "It must be applied only to the training fold. Running SMOTE before splitting means synthetic points derived from test examples end up in training, which is textbook leakage and produces an impressive, meaningless score."],
 k:["Interpolates new minority points between real neighbours.",
    "Apply inside the training fold only — never before the split.",
    "Can blur class boundaries when classes genuinely overlap.",
    "Class weighting is often just as effective and much simpler."],
 r:["Class Imbalance","Data Leakage","Cross-Validation","Data Augmentation"]},

{t:"Confusion Matrix", d:"A table of true positives, false positives, true negatives and false negatives — the source of every classification metric.", l:"core", g:["evaluation","metrics"],
 b:["Rows are actual classes, columns are predicted. TP and TN are correct; FP is a false alarm (type I error); FN is a miss (type II error). Every classification metric is a ratio drawn from these four cells.",
    "Looking at the matrix rather than a single number tells you what kind of mistakes the model makes, which is usually the actionable information. A fraud model with 300 false positives and 2 false negatives needs a different fix from one with the reverse."],
 k:["Accuracy = (TP+TN)/total; Precision = TP/(TP+FP); Recall = TP/(TP+FN).",
    "False positive = type I error; false negative = type II error.",
    "Always inspect the matrix, not just the headline metric.",
    "For multi-class it becomes n×n and reveals which classes get confused."],
 dg:"confusion-matrix",
 r:["Precision","Recall","F1 Score","Accuracy","Classification"]},

{t:"Accuracy", d:"The fraction of predictions that were correct — the most intuitive metric and the most frequently misleading.", l:"core", g:["evaluation","metrics"],
 b:["Accuracy treats every error as equally costly and every class as equally important. When classes are balanced and errors are symmetric, it is a fine summary. When 99% of examples are one class, it is actively deceptive.",
    "Balanced accuracy averages recall across classes and is a better default under imbalance. In practice, report accuracy alongside precision, recall and the confusion matrix rather than in place of them."],
 k:["Assumes balanced classes and symmetric error costs.",
    "Useless when the positive class is rare.",
    "Balanced accuracy averages per-class recall and is safer.",
    "Always compare against the majority-class baseline."],
 dg:"confusion-matrix",
 r:["Precision","Recall","Confusion Matrix","Class Imbalance"]},

{t:"Precision", d:"Of everything the model flagged as positive, the fraction that really was positive.", l:"core", g:["evaluation","metrics"],
 b:["Precision = TP / (TP + FP). It answers: when this model raises an alarm, how often should I believe it? High precision means few false alarms.",
    "It matters most when acting on a positive prediction is expensive or harmful — blocking a legitimate transaction, sending a patient for invasive tests, deleting a real email as spam. You can always raise precision by raising the decision threshold, at the cost of recall."],
 k:["TP / (TP + FP) — the trustworthiness of a positive prediction.",
    "Optimise it when false positives are costly.",
    "Raising the threshold raises precision and lowers recall.",
    "Meaningless in isolation — always report it with recall."],
 dg:"confusion-matrix",
 r:["Recall","F1 Score","Confusion Matrix","Precision-Recall Curve"]},

{t:"Recall", a:"Sensitivity, True Positive Rate", d:"Of all the actual positives, the fraction the model successfully found.", l:"core", g:["evaluation","metrics"],
 b:["Recall = TP / (TP + FN). It answers: how much of what I was looking for did I catch? High recall means few misses.",
    "It matters most when missing a positive is dangerous — an undetected tumour, an unflagged security breach, a missed safety fault. Lowering the decision threshold raises recall and lowers precision; the right point depends entirely on the relative cost of the two errors."],
 k:["TP / (TP + FN) — the share of real positives that were caught.",
    "Optimise it when false negatives are dangerous.",
    "Trivially maximised by predicting everything positive — hence always pair with precision.",
    "Called sensitivity in medicine and true positive rate in signal detection."],
 dg:"confusion-matrix",
 r:["Precision","F1 Score","Confusion Matrix"]},

{t:"F1 Score", d:"The harmonic mean of precision and recall, giving one number that punishes imbalance between them.", l:"core", g:["evaluation","metrics"],
 b:["F1 = 2·(P·R)/(P+R). The harmonic mean is used deliberately: unlike the arithmetic mean, it collapses toward the smaller value. A model with precision 1.0 and recall 0.0 has an arithmetic mean of 0.5 but an F1 of 0 — which is the honest assessment.",
    "Fβ generalises it: β>1 weights recall more, β<1 weights precision more. F2 is common in medical screening, where missing a case is far worse than a false alarm."],
 k:["Harmonic mean — a single bad component drags the score down.",
    "Good default single metric for imbalanced binary classification.",
    "F2 favours recall; F0.5 favours precision.",
    "Macro-F1 averages per class; micro-F1 pools all predictions."],
 r:["Precision","Recall","Confusion Matrix","Class Imbalance"]},

{t:"ROC Curve", a:"Receiver Operating Characteristic", d:"A plot of true positive rate against false positive rate across every possible decision threshold.", l:"intermediate", g:["evaluation","metrics"],
 b:["Each point on the curve is one threshold. The diagonal is random guessing; the top-left corner is perfect. The curve shows the whole trade-off space rather than a single operating point, which makes it useful for comparing models before you have chosen a threshold.",
    "Its blind spot is class imbalance. Because the false positive rate has the large negative class in its denominator, ROC curves look flattering when positives are rare. Precision-recall curves are the honest choice there."],
 k:["Plots TPR against FPR at every threshold.",
    "The diagonal is a coin flip; up and to the left is better.",
    "Threshold-independent, so good for comparing models.",
    "Over-optimistic under heavy class imbalance — use a PR curve instead."],
 dg:"roc",
 r:["AUC","Precision-Recall Curve","Classification","Recall"]},

{t:"AUC", a:"Area Under the Curve", d:"The area beneath the ROC curve — the probability that a random positive is scored above a random negative.", l:"intermediate", g:["evaluation","metrics"],
 b:["AUC of 0.5 is random, 1.0 is perfect, and below 0.5 means your model is anti-correlated (invert it). Its interpretation as a ranking probability is what makes it useful: it measures whether the model orders examples correctly, independent of any threshold.",
    "It says nothing about calibration — a model can rank perfectly while outputting probabilities that are systematically too high. And under strong imbalance, PR-AUC is far more informative."],
 k:["Interpretation: P(score of a random positive > score of a random negative).",
    "0.5 is random; below 0.5 means the ranking is inverted.",
    "Measures ranking quality, not probability calibration.",
    "Prefer PR-AUC when the positive class is rare."],
 dg:"roc",
 r:["ROC Curve","Precision-Recall Curve","Calibration","Classification"]},

{t:"Precision-Recall Curve", d:"A plot of precision against recall across thresholds — the right curve when positives are rare.", l:"intermediate", g:["evaluation","metrics"],
 b:["Because both axes concern the positive class, a PR curve is not flattered by a huge negative class. For a fraud detector with 0.1% positives, the ROC curve may look excellent while the PR curve shows precision collapsing as soon as you ask for meaningful recall.",
    "The baseline is the positive class prevalence, not 0.5 — so with 1% positives, average precision of 0.3 is a thirty-fold improvement over random, even though the number looks poor."],
 k:["Both axes focus on the positive class, so imbalance cannot hide.",
    "The random baseline equals the positive class rate, not 0.5.",
    "Average precision (PR-AUC) summarises it as one number.",
    "The standard choice for fraud, anomaly and information retrieval."],
 r:["ROC Curve","AUC","Precision","Recall","Class Imbalance"]},

{t:"Mean Squared Error", a:"MSE", d:"The average of squared differences between predicted and actual values — the default regression loss.", l:"core", g:["evaluation","metrics"],
 b:["Squaring makes all errors positive and penalises large ones disproportionately: being wrong by 10 is a hundred times worse than being wrong by 1. That is useful when big misses are genuinely much worse, and harmful when your data has outliers you cannot remove.",
    "MSE is in squared units, which is uninterpretable, so RMSE (its square root) is usually reported instead because it shares units with the target. MSE remains the training loss because it is smooth and differentiable everywhere."],
 k:["Penalises large errors quadratically — very sensitive to outliers.",
    "RMSE puts the number back into the units of the target.",
    "Smooth and differentiable, which is why it is the default training loss.",
    "Use MAE or Huber loss when outliers are legitimate data."],
 r:["Loss Function","Regression","Mean Absolute Error","R-Squared"]},

{t:"Mean Absolute Error", a:"MAE", d:"The average absolute difference between predictions and actuals — a robust, directly interpretable regression metric.", l:"core", g:["evaluation","metrics"],
 b:["MAE treats an error of 10 as ten times an error of 1, not a hundred times. That makes it far less sensitive to outliers than MSE, and it is expressed in the target's own units, so an MAE of 4.2 minutes means exactly what it sounds like.",
    "Its gradient is constant, which makes optimisation slightly less well-behaved near the minimum. Huber loss is the common compromise: quadratic for small errors, linear for large ones."],
 k:["Linear penalty — robust to outliers.",
    "Same units as the target, so it is directly interpretable.",
    "Optimising MAE targets the median; optimising MSE targets the mean.",
    "Huber loss blends MAE robustness with MSE smoothness."],
 r:["Mean Squared Error","Regression","Loss Function"]},

{t:"R-Squared", a:"Coefficient of Determination", d:"The proportion of variance in the target that the model explains, relative to simply predicting the mean.", l:"intermediate", g:["evaluation","metrics"],
 b:["R² = 1 means perfect prediction, 0 means no better than predicting the average, and negative values mean worse than the average — which happens more often than beginners expect on held-out data.",
    "It always increases when you add features, even useless ones, so adjusted R² penalises the parameter count. And a high R² does not imply a useful model: it can come entirely from an obvious trend, or from leakage."],
 k:["1 is perfect, 0 equals predicting the mean, negative is worse than that.",
    "Never decreases when features are added — use adjusted R² to compare models.",
    "High R² can still mean a useless model if the baseline was already strong.",
    "Not defined meaningfully for classification."],
 r:["Regression","Mean Squared Error","Baseline Model"]},

{t:"Cross-Entropy", a:"Log Loss", d:"The standard classification loss, measuring how far predicted probabilities are from the true labels.", l:"intermediate", g:["evaluation","metrics","training"],
 b:["Cross-entropy penalises confident wrong answers brutally: predicting 0.99 for the wrong class produces a very large loss, while predicting 0.6 for the wrong class produces a modest one. That is the desired behaviour — it forces the model to be calibrated, not merely correct.",
    "Binary cross-entropy handles two classes; categorical cross-entropy handles many via softmax. Because it operates on probabilities rather than hard decisions, it is threshold-independent and differentiable, making it the natural training objective."],
 k:["Penalises confident mistakes far more than uncertain ones.",
    "Encourages calibrated probabilities, not just correct rankings.",
    "Paired with softmax for multi-class, sigmoid for binary and multi-label.",
    "Class weights inside it are the standard remedy for imbalance."],
 r:["Loss Function","Softmax","Classification","Calibration","Entropy"]},

{t:"Baseline Model", d:"The simplest reasonable predictor, used as the bar every real model must clear.", l:"core", g:["evaluation","workflow"],
 b:["Predict the majority class. Predict the mean. Predict yesterday's value. Use last month's business rule. Each takes minutes and gives you a number that makes every subsequent result interpretable — an 87% accuracy means nothing until you know the baseline was 85%.",
    "Skipping the baseline is one of the most common mistakes in applied ML, and it leads teams to ship complex models that a three-line heuristic would have matched."],
 k:["Majority class, mean, or last observed value are the standard baselines.",
    "Without one, no accuracy number can be judged.",
    "Include the existing production rule as a baseline if there is one.",
    "If the complex model barely beats it, ship the simple one."],
 r:["Accuracy","Model","Evaluation Metric"]},

{t:"Linear Regression", d:"Fitting a straight line (or hyperplane) through the data by minimising squared error.", l:"core", g:["algorithm","supervised"],
 b:["The model is y = w₁x₁ + w₂x₂ + ... + b. Coefficients are directly interpretable — a one-unit increase in x₁ changes the prediction by w₁, holding everything else fixed — which is why it remains the default in economics, epidemiology and any setting where explaining the model matters as much as its accuracy.",
    "It assumes a linear relationship, independent errors with constant variance, and little multicollinearity. Violations are usually fixable: transform the target, add interaction or polynomial terms, or move to a regularised variant."],
 k:["Coefficients are directly interpretable, which is often the whole point.",
    "Assumes linearity, independent errors, constant variance, low multicollinearity.",
    "Ridge and lasso are the regularised versions.",
    "Always an excellent baseline — sometimes the right final answer."],
 x:{lang:"python", code:'from sklearn.linear_model import LinearRegression\n\nmodel = LinearRegression().fit(X_train, y_train)\nprint(dict(zip(feature_names, model.coef_.round(3))))\nprint("intercept:", round(model.intercept_, 3))'},
 r:["Logistic Regression","Regression","L1 and L2 Regularisation","Mean Squared Error"]},

{t:"Logistic Regression", d:"A linear model for classification that squashes its output through a sigmoid to produce a probability.", l:"core", g:["algorithm","supervised"],
 b:["Despite the name it is a classifier. It computes a linear combination of features and passes it through a sigmoid, giving a value in (0, 1) interpreted as P(class = 1). Training minimises cross-entropy.",
    "Its coefficients are log-odds, so exponentiating one gives an odds ratio — a form of interpretability that regulated industries rely on. It is fast, calibrated, hard to overfit with regularisation, and an outstanding baseline for any binary problem."],
 k:["Linear in log-odds; the sigmoid maps that to a probability.",
    "exp(coefficient) is an odds ratio — genuinely interpretable.",
    "Naturally well-calibrated, unlike many tree ensembles.",
    "The standard first model for binary classification."],
 x:{lang:"python", code:'from sklearn.linear_model import LogisticRegression\n\nclf = LogisticRegression(C=1.0, class_weight="balanced").fit(X, y)\nproba = clf.predict_proba(X_new)[:, 1]      # probability of class 1\npreds = (proba > 0.3).astype(int)           # tune this threshold'},
 r:["Linear Regression","Classification","Sigmoid","Cross-Entropy"]},

{t:"Decision Tree", d:"A model that repeatedly splits the data on feature thresholds, forming a tree of yes/no questions ending in predictions.", l:"core", g:["algorithm","supervised"],
 b:["At each node the algorithm picks the feature and threshold that best separates the classes, measured by Gini impurity or information gain, and recurses. The result is a flowchart a non-technical stakeholder can read directly.",
    "A tree grown without limits will perfectly memorise the training data. Depth limits, minimum samples per leaf and pruning control that. Trees also need no feature scaling and handle non-linear interactions naturally, which is why they are the building block of the strongest tabular models."],
 k:["Highly interpretable — the path to a prediction is a readable rule.",
    "No feature scaling required; handles mixed types well.",
    "Unconstrained trees overfit badly; limit depth or prune.",
    "Unstable — a slightly different sample can produce a very different tree."],
 dg:"decision-tree",
 r:["Random Forest","Gradient Boosting","Gini Impurity","Ensemble Learning"]},

{t:"Random Forest", d:"An ensemble of decorrelated decision trees whose votes are averaged.", l:"core", g:["algorithm","ensemble"],
 b:["Each tree is trained on a bootstrap sample of the rows and, at each split, considers only a random subset of the features. That double randomisation makes the trees make different mistakes, and averaging cancels much of the individual variance while preserving the low bias.",
    "It is a superb default for tabular data: minimal tuning, robust to outliers and irrelevant features, provides feature importances, and rarely overfits badly as you add trees. Gradient boosting usually edges it on accuracy but demands more tuning."],
 k:["Bagging plus random feature subsets keeps the trees decorrelated.",
    "More trees never hurts accuracy — only training and inference time.",
    "Out-of-bag error gives a free validation estimate.",
    "Feature importances are biased toward high-cardinality features — prefer permutation importance."],
 dg:"ensemble",
 r:["Decision Tree","Bagging","Gradient Boosting","Ensemble Learning"]},

{t:"Gradient Boosting", d:"Building an ensemble sequentially, where each new tree is fitted to the errors the previous ones made.", l:"core", g:["algorithm","ensemble"],
 b:["Start with a weak prediction. Compute the residuals. Fit a small tree to those residuals. Add it, scaled by a learning rate. Repeat hundreds of times. Each tree corrects what remains wrong, which drives bias down aggressively.",
    "This family — XGBoost, LightGBM, CatBoost — dominates tabular machine learning and wins the majority of Kaggle competitions on structured data. The price is tuning: learning rate, tree depth, subsample ratio and regularisation all interact, and without early stopping it will happily overfit."],
 k:["Sequential: each tree fits the residual error of the ensemble so far.",
    "Lower learning rate plus more trees generally generalises better.",
    "Requires early stopping on a validation set — it will overfit otherwise.",
    "The strongest general approach for tabular data by a clear margin."],
 x:{lang:"python", code:'import lightgbm as lgb\n\nmodel = lgb.LGBMClassifier(\n    n_estimators=2000, learning_rate=0.03,\n    num_leaves=31, subsample=0.8, colsample_bytree=0.8)\n\nmodel.fit(X_tr, y_tr,\n          eval_set=[(X_val, y_val)],\n          callbacks=[lgb.early_stopping(100)])'},
 dg:"ensemble",
 r:["XGBoost","LightGBM","CatBoost","Random Forest","Boosting"]},

{t:"XGBoost", a:"Extreme Gradient Boosting", d:"The gradient boosting implementation that made the technique fast, regularised and ubiquitous.", l:"intermediate", g:["library","ensemble"],
 b:["XGBoost added second-order gradient information, built-in L1 and L2 regularisation on leaf weights, clever handling of missing values (it learns a default direction per split), and heavily optimised parallel tree construction. It won so many competitions between 2015 and 2018 that it became the default tabular model.",
    "LightGBM is generally faster on large datasets and CatBoost handles categorical features better, but XGBoost remains an extremely strong, well-documented, well-supported choice."],
 k:["Uses second-order gradients (Newton boosting) for better steps.",
    "Regularisation on leaf weights is built into the objective.",
    "Learns a default direction for missing values automatically.",
    "GPU training is supported and dramatically faster on large data."],
 r:["Gradient Boosting","LightGBM","CatBoost","Random Forest"]},

{t:"LightGBM", d:"Microsoft's gradient boosting library, built for speed on large datasets through leaf-wise growth and histogram binning.", l:"intermediate", g:["library","ensemble"],
 b:["LightGBM grows trees leaf-wise — always splitting the leaf with the highest loss reduction — rather than level by level. That produces deeper, more accurate trees for the same number of leaves, and also overfits more easily on small datasets. Histogram-based binning of continuous features cuts split-finding cost dramatically.",
    "It handles categorical features natively without one-hot encoding, and is typically several times faster than XGBoost on datasets with millions of rows."],
 k:["Leaf-wise growth: more accurate, more prone to overfit small data.",
    "Histogram binning makes split finding very fast.",
    "Native categorical support — no one-hot explosion.",
    "Control complexity with `num_leaves` rather than `max_depth`."],
 r:["Gradient Boosting","XGBoost","CatBoost"]},

{t:"CatBoost", d:"Yandex's gradient boosting library, designed around principled handling of categorical features.", l:"intermediate", g:["library","ensemble"],
 b:["CatBoost uses ordered target statistics to encode categorical variables, computing each row's encoding from only the rows before it in a random permutation. That removes the target leakage that naive target encoding introduces, which is otherwise a subtle and common bug.",
    "It also uses oblivious trees — the same split condition across an entire level — which makes inference extremely fast and acts as regularisation. It often performs best out of the box with minimal tuning."],
 k:["Ordered target encoding avoids the leakage of naive target statistics.",
    "Oblivious (symmetric) trees give very fast inference.",
    "Strong default hyperparameters — often competitive with no tuning.",
    "Best of the three when the data is dominated by categorical features."],
 r:["Gradient Boosting","XGBoost","LightGBM","One-Hot Encoding"]},

{t:"Support Vector Machine", a:"SVM", d:"A classifier that finds the boundary with the widest possible margin between classes.", l:"intermediate", g:["algorithm","supervised"],
 b:["Rather than any separating line, an SVM seeks the one furthest from the nearest points of either class — those points are the support vectors, and they alone define the boundary. Maximising the margin is a principled way to favour boundaries that generalise.",
    "The kernel trick lets it draw non-linear boundaries by implicitly mapping data into a higher-dimensional space without ever computing the coordinates. RBF is the usual kernel. SVMs are strong on small-to-medium, high-dimensional datasets and scale poorly past tens of thousands of rows."],
 k:["The margin is maximised; only the support vectors matter.",
    "The kernel trick gives non-linear boundaries without explicit feature expansion.",
    "Requires feature scaling — it is entirely distance-based.",
    "Training is roughly quadratic in samples, so it does not scale to millions of rows."],
 r:["Classification","Feature Scaling","Logistic Regression"]},

{t:"K-Nearest Neighbours", a:"KNN", d:"Classify or predict by looking at the k closest training examples and taking their majority or average.", l:"core", g:["algorithm","supervised"],
 b:["KNN does no training at all — it stores the dataset and defers all work to prediction time, which is why it is called a lazy learner. To predict, it finds the k nearest points by some distance metric and votes.",
    "Small k gives a jagged, high-variance boundary; large k smooths it toward the global majority. It needs scaled features, suffers badly from the curse of dimensionality, and gets slower as the dataset grows — but it is conceptually simple and surprisingly effective on low-dimensional problems."],
 k:["No training phase; all computation happens at prediction time.",
    "k controls the bias-variance balance — small k overfits.",
    "Feature scaling is mandatory; distance is the whole algorithm.",
    "Degrades sharply in high dimensions as distances become uniform."],
 r:["Curse of Dimensionality","Feature Scaling","Clustering","Approximate Nearest Neighbour"]},

{t:"Naive Bayes", d:"A probabilistic classifier applying Bayes' theorem while assuming all features are independent.", l:"intermediate", g:["algorithm","supervised"],
 b:["The independence assumption — that seeing the word free tells you nothing about whether money also appears — is obviously false, which is the naive part. Yet the classifier works remarkably well, because it only needs to rank classes correctly, not to estimate probabilities accurately.",
    "It trains in a single pass, needs very little data, and handles thousands of features comfortably. That made it the classic spam filter, and it remains an excellent fast baseline for text classification."],
 k:["Assumes features are conditionally independent given the class.",
    "Trains in one pass; extremely fast and memory-light.",
    "Multinomial for word counts, Gaussian for continuous features, Bernoulli for binary.",
    "Probabilities are poorly calibrated even when the ranking is good."],
 dg:"bayes",
 r:["Bayes Theorem","Classification","Text Classification","Logistic Regression"]},

{t:"K-Means Clustering", d:"Partitioning data into k groups by iteratively assigning points to the nearest centroid and recomputing centroids.", l:"core", g:["algorithm","unsupervised"],
 b:["Initialise k centroids, assign every point to its nearest one, move each centroid to the mean of its members, and repeat until nothing moves. It converges quickly and scales to large datasets.",
    "The assumptions are strong: clusters are spherical, similarly sized and similarly dense. Elongated or nested clusters break it. k must be chosen in advance, k-means++ initialisation avoids poor local optima, and features must be scaled or the largest-magnitude feature dictates the result."],
 k:["Assumes spherical, comparably sized clusters — often violated.",
    "Sensitive to initialisation; use k-means++ and multiple restarts.",
    "Choose k with the elbow method or silhouette score, plus domain sense.",
    "Scale features first, always."],
 x:{lang:"python", code:'from sklearn.cluster import KMeans\nfrom sklearn.preprocessing import StandardScaler\n\nX_s = StandardScaler().fit_transform(X)\nkm  = KMeans(n_clusters=4, n_init=10, random_state=42).fit(X_s)\nprint(km.labels_[:10], km.inertia_)'},
 dg:"kmeans",
 r:["Clustering","DBSCAN","Unsupervised Learning","Feature Scaling"]},

{t:"DBSCAN", a:"Density-Based Spatial Clustering", d:"A clustering algorithm that finds dense regions of arbitrary shape and marks sparse points as noise.", l:"intermediate", g:["algorithm","unsupervised"],
 b:["DBSCAN takes two parameters: eps, a distance radius, and minPts, the number of neighbours needed to count as dense. Points with enough neighbours become core points; connected core points form a cluster; everything else is noise.",
    "Unlike k-means you do not choose the number of clusters — it emerges — and clusters can be any shape, including nested crescents. Its weaknesses are sensitivity to eps and poor handling of clusters with very different densities. HDBSCAN addresses both."],
 k:["No need to specify the cluster count in advance.",
    "Finds arbitrarily shaped clusters and explicitly labels outliers.",
    "Struggles when clusters have very different densities.",
    "Use a k-distance plot to pick eps; HDBSCAN removes the need entirely."],
 r:["K-Means Clustering","Clustering","Anomaly Detection","Unsupervised Learning"]},

{t:"Hierarchical Clustering", d:"Building a tree of nested clusters by repeatedly merging the closest pairs (or splitting the largest group).", l:"intermediate", g:["algorithm","unsupervised"],
 b:["Agglomerative clustering starts with every point as its own cluster and merges the nearest pair repeatedly, producing a dendrogram. Cutting that tree at any height gives any number of clusters, so you can decide k after seeing the structure rather than before.",
    "The linkage criterion matters: single linkage chains clusters together, complete linkage produces compact ones, and Ward's method minimises within-cluster variance and is the usual default. It is O(n²) or worse in memory, so it does not scale to very large datasets."],
 k:["Produces a dendrogram — choose the cluster count after seeing the structure.",
    "Linkage (single, complete, average, Ward) changes results substantially.",
    "O(n²) memory limits it to tens of thousands of points.",
    "Excellent for exploratory analysis and taxonomy building."],
 r:["Clustering","K-Means Clustering","Unsupervised Learning"]},

{t:"Principal Component Analysis", a:"PCA", d:"A linear transformation that rotates data onto the axes of greatest variance, so fewer dimensions carry most of the information.", l:"core", g:["algorithm","unsupervised"],
 b:["PCA finds orthogonal directions — principal components — ordered by how much variance each explains. Keeping the first few gives a lower-dimensional representation that preserves most of the structure. It is computed from the eigenvectors of the covariance matrix, or equivalently by SVD.",
    "Data must be standardised first or the largest-scale feature dominates every component. The components are linear combinations of original features, which makes them hard to interpret, and PCA cannot capture non-linear structure — that is what kernel PCA, UMAP and autoencoders are for."],
 k:["Components are orthogonal and ordered by explained variance.",
    "Standardise before applying, or scale alone decides the components.",
    "Use the explained-variance curve to choose how many to keep.",
    "Linear only — non-linear structure needs UMAP, t-SNE or an autoencoder."],
 x:{lang:"python", code:'from sklearn.decomposition import PCA\n\npca = PCA(n_components=0.95)      # keep 95% of variance\nX_reduced = pca.fit_transform(X_scaled)\nprint(X.shape, "->", X_reduced.shape)\nprint(pca.explained_variance_ratio_[:5].round(3))'},
 dg:"pca",
 r:["Dimensionality Reduction","t-SNE","UMAP","Eigenvalue","Singular Value Decomposition"]},

{t:"t-SNE", a:"t-Distributed Stochastic Neighbour Embedding", d:"A non-linear technique that projects high-dimensional data into two dimensions for visualisation, preserving local neighbourhoods.", l:"intermediate", g:["algorithm","visualisation"],
 b:["t-SNE converts distances into probabilities of being neighbours and finds a low-dimensional layout with a similar neighbour structure. The result is often a striking picture of well-separated clusters, which is exactly why it became so popular for inspecting embeddings.",
    "It is a visualisation tool, not a general transform. Distances between clusters carry no meaning, cluster sizes carry no meaning, results change with the random seed, and the perplexity parameter substantially alters the picture. Never draw quantitative conclusions from a t-SNE plot."],
 k:["Preserves local structure; global distances are not meaningful.",
    "Cluster sizes and inter-cluster gaps on the plot mean nothing.",
    "Perplexity (typically 5–50) changes the output significantly — try several.",
    "Non-parametric: it cannot transform new points without refitting."],
 r:["UMAP","Dimensionality Reduction","Principal Component Analysis","Embedding"]},

{t:"UMAP", a:"Uniform Manifold Approximation and Projection", d:"A dimensionality reduction technique that is faster than t-SNE and preserves more global structure.", l:"intermediate", g:["algorithm","visualisation"],
 b:["UMAP builds a fuzzy topological graph of the high-dimensional data and optimises a low-dimensional layout to match it. It is considerably faster than t-SNE, scales to millions of points, and retains more of the global arrangement — though still not enough to read distances literally.",
    "Unlike t-SNE it is parametric enough to transform new points with the fitted model, which makes it usable as a preprocessing step and not only as a plot."],
 k:["Faster than t-SNE and scales to millions of points.",
    "Preserves more global structure, though distances remain approximate.",
    "Can transform unseen data with the fitted model.",
    "`n_neighbors` trades local detail against global structure."],
 r:["t-SNE","Dimensionality Reduction","Principal Component Analysis","Embedding"]},

{t:"Ensemble Learning", d:"Combining several models so their collective prediction beats any individual one.", l:"core", g:["technique","ensemble"],
 b:["Ensembles work when the members make different mistakes. Averaging then cancels uncorrelated errors while preserving the shared signal. The three classic strategies are bagging (train in parallel on bootstrapped samples, average), boosting (train sequentially, each correcting the last) and stacking (train a meta-model on the base models' outputs).",
    "Diversity is the requirement. Ten identical models average to one model. Different algorithms, different features, different random seeds and different data subsets all create the necessary disagreement."],
 k:["Only helps if the members err differently — diversity is the whole mechanism.",
    "Bagging cuts variance; boosting cuts bias; stacking learns how to combine.",
    "Nearly always improves accuracy at the cost of latency and interpretability.",
    "Wins almost every tabular competition — and complicates every deployment."],
 dg:"ensemble",
 r:["Bagging","Boosting","Stacking","Random Forest","Gradient Boosting"]},

{t:"Bagging", a:"Bootstrap Aggregating", d:"Training many models on random resamples of the data and averaging their predictions to reduce variance.", l:"intermediate", g:["technique","ensemble"],
 b:["Each model trains on a bootstrap sample — drawn with replacement, so roughly 63% of unique rows appear and some appear multiple times. The models therefore see different data and make different errors, and averaging cancels much of that variance.",
    "The rows left out of each sample form the out-of-bag set, which provides a validation estimate for free. Bagging helps most with high-variance, low-bias learners: deep decision trees are the canonical case, and random forest is bagging plus feature randomisation."],
 k:["Bootstrap samples create model diversity through data variation.",
    "Reduces variance, leaves bias roughly unchanged.",
    "Out-of-bag error is a free validation estimate.",
    "Trains in parallel — trivially distributable."],
 dg:"ensemble",
 r:["Random Forest","Boosting","Ensemble Learning","Bias-Variance Trade-off"]},

{t:"Boosting", d:"Training models in sequence, each one focused on the examples its predecessors got wrong.", l:"intermediate", g:["technique","ensemble"],
 b:["AdaBoost reweights misclassified examples so the next weak learner concentrates on them. Gradient boosting generalises this: each new model fits the gradient of the loss with respect to the current predictions — the residuals, in the squared-error case.",
    "Because each stage attacks remaining error directly, boosting drives bias down aggressively and can reach very high accuracy with shallow trees. It is also sequential (harder to parallelise) and will overfit without a low learning rate and early stopping."],
 k:["Sequential and adaptive — each model targets the current errors.",
    "Reduces bias primarily; strong models from weak learners.",
    "Overfits without early stopping and a small learning rate.",
    "Harder to parallelise than bagging by construction."],
 dg:"ensemble",
 r:["Gradient Boosting","Bagging","Ensemble Learning"]},

{t:"Stacking", d:"Training a meta-model to learn the best way to combine the predictions of several base models.", l:"advanced", g:["technique","ensemble"],
 b:["Base models make predictions; those predictions become the features for a second-level model that learns when to trust each one. A simple meta-model — logistic or ridge regression — is usually best, because complex meta-models overfit the small meta-dataset.",
    "The base predictions must come from out-of-fold predictions, not from predictions on data the base models trained on. Skipping that is the standard stacking mistake and produces a meta-model trained on unrealistically good inputs."],
 k:["The meta-model learns which base model to trust in which region.",
    "Use out-of-fold predictions to build the meta-features, always.",
    "Keep the meta-model simple; the meta-dataset is small.",
    "Highest accuracy of the ensemble family, highest complexity too."],
 r:["Ensemble Learning","Bagging","Boosting","Cross-Validation"]},

{t:"Curse of Dimensionality", d:"The phenomenon whereby data becomes hopelessly sparse and distances lose meaning as the number of features grows.", l:"intermediate", g:["theory","data"],
 b:["Covering a 1-D range with 10 samples per unit takes 10 points; 2-D takes 100; 10-D takes ten billion. Real datasets never keep up, so high-dimensional space is almost entirely empty. Worse, distances converge: the nearest and farthest neighbours become nearly equidistant, which quietly breaks every distance-based method.",
    "The practical responses are dimensionality reduction, feature selection, regularisation, and choosing models that cope well with many features — linear models with L1, and tree ensembles."],
 k:["Data requirements grow exponentially with dimensions.",
    "Distance ratios flatten, undermining KNN, k-means and RBF kernels.",
    "Fight it with PCA, feature selection and regularisation.",
    "Real data usually lies on a much lower-dimensional manifold — which is why anything works at all."],
 r:["Dimensionality Reduction","K-Nearest Neighbours","Feature Selection","Principal Component Analysis"]},

{t:"No Free Lunch Theorem", d:"Averaged over all possible problems, no learning algorithm is better than any other.", l:"advanced", g:["theory"],
 b:["Wolpert's result says that any algorithm's superiority on one class of problems is exactly paid for by inferiority on another. There is no universally best model, and any claim that one algorithm dominates is a claim about the problems being considered, not about the algorithm.",
    "The practical reading: algorithm choice is an assumption about the structure of your data. That is also why the empirical habit — try several, evaluate honestly — beats theoretical arguments about which family is best."],
 k:["No algorithm is best across all possible problems.",
    "Choosing a model is choosing an inductive bias about the data.",
    "Justifies empirical benchmarking over first-principles argument.",
    "Real-world data is highly structured, which is why some models do dominate in practice."],
 r:["Inductive Bias","Model","Machine Learning"]},

{t:"Inductive Bias", d:"The assumptions a learning algorithm makes in order to generalise beyond the data it has seen.", l:"advanced", g:["theory"],
 b:["Infinitely many functions fit any finite dataset. To pick one, an algorithm must assume something: linear regression assumes linearity, CNNs assume local spatial structure and translation invariance, RNNs assume sequential dependence, transformers assume that relevance can be learned via attention.",
    "Matching inductive bias to the problem is most of what model selection is. It also explains data efficiency: a CNN needs far fewer images than a fully connected network because its assumptions about images are already correct."],
 k:["Without assumptions, generalisation from finite data is impossible.",
    "CNNs assume locality; RNNs assume sequence; GNNs assume graph structure.",
    "Stronger bias means better data efficiency when the bias is right.",
    "Very large models trade inductive bias for data scale."],
 r:["No Free Lunch Theorem","Convolutional Neural Network","Generalisation","Transformer"]},

{t:"Interpretability", d:"The degree to which a human can understand why a model produced a particular output.", l:"intermediate", g:["theory","governance"],
 b:["Some models are intrinsically interpretable: linear regression coefficients, decision tree paths, rule lists. Others are opaque, and require post-hoc explanation — SHAP values, LIME, partial dependence plots, attention maps.",
    "It is not a nice-to-have in regulated settings. Credit decisions in many jurisdictions require an explanation, medical devices require justification, and the EU AI Act imposes transparency obligations on high-risk systems. There is a real accuracy-interpretability trade-off, though it is often smaller than assumed."],
 k:["Intrinsically interpretable models beat post-hoc explanations of opaque ones.",
    "SHAP and LIME approximate local behaviour; they are not ground truth.",
    "Regulated domains often require explanation by law.",
    "Explanations can be plausible and wrong — validate them."],
 r:["SHAP","Feature Importance","Model"]},

{t:"SHAP", a:"SHapley Additive exPlanations", d:"A method that assigns each feature a contribution to a specific prediction, based on cooperative game theory.", l:"advanced", g:["technique","interpretability"],
 b:["SHAP treats features as players in a game and the prediction as the payout, distributing credit using Shapley values — the unique attribution satisfying fairness axioms including local accuracy and consistency. The contributions for one prediction sum exactly to the difference from the base rate.",
    "TreeSHAP computes it exactly and quickly for tree ensembles; KernelSHAP approximates it for any model at much higher cost. Beware interpreting SHAP values as causal — they explain the model, not the world."],
 k:["Contributions for one prediction sum to prediction minus base value.",
    "TreeSHAP is exact and fast for tree models; KernelSHAP is slow and approximate.",
    "Explains the model's behaviour, not causal effects in reality.",
    "Correlated features split credit in ways that can mislead."],
 r:["Interpretability","Feature Importance","Gradient Boosting"]},

{t:"Feature Importance", d:"A ranking of how much each input contributes to a model's predictions.", l:"intermediate", g:["technique","interpretability"],
 b:["Tree-based impurity importance counts how much each feature reduced impurity across splits — fast, but biased toward high-cardinality and continuous features. Permutation importance shuffles one column and measures the drop in performance, which is model-agnostic and generally more trustworthy.",
    "Both measure association with the model's output, not causation. A feature can be important because it proxies for something else, and dropping it may change nothing if a correlated feature absorbs its role."],
 k:["Impurity importance is biased toward high-cardinality features.",
    "Permutation importance is model-agnostic and more reliable.",
    "Correlated features share and dilute importance between them.",
    "Importance is not causality — never read it as an intervention effect."],
 r:["SHAP","Interpretability","Feature Selection","Random Forest"]},

{t:"Active Learning", d:"Letting the model choose which unlabelled examples a human should label next, to spend the labelling budget where it helps most.", l:"advanced", g:["technique","training"],
 b:["Train on a small labelled seed, score the unlabelled pool, and send the most informative examples to annotators. Informative usually means most uncertain (near the decision boundary), or most disagreed-upon by an ensemble, or most representative of an unlabelled region.",
    "When labelling is expensive — medical imaging, legal review, specialist annotation — active learning can reach a target accuracy with a fraction of the labels. Its risk is a feedback loop where the model's blind spots never get sampled."],
 k:["Labels the examples that will teach the model the most.",
    "Uncertainty sampling and query-by-committee are the common strategies.",
    "Can cut labelling cost several-fold when annotation is expensive.",
    "Risks reinforcing existing blind spots — mix in random sampling."],
 r:["Semi-Supervised Learning","Training Data","Human-in-the-Loop"]},

{t:"Online Learning", d:"Updating a model incrementally as each new example arrives, rather than retraining from scratch on a full dataset.", l:"advanced", g:["technique","training"],
 b:["Online learning processes data one example or mini-batch at a time and updates immediately. It suits streaming data, environments that drift constantly, and datasets too large to hold in memory. Stochastic gradient descent is naturally online.",
    "The danger is catastrophic forgetting and vulnerability to poisoning: a burst of anomalous or adversarial data immediately shifts the model with no review step. Production systems usually pair online updates with periodic full retraining and strong monitoring."],
 k:["Adapts continuously — no full retraining cycle needed.",
    "Constant memory footprint regardless of total data seen.",
    "Vulnerable to data poisoning and sudden distribution shifts.",
    "Usually combined with scheduled batch retraining as a safety net."],
 r:["Model Drift","Stochastic Gradient Descent","Stream Processing"]},

{t:"AutoML", d:"Automating model selection, feature preprocessing and hyperparameter tuning.", l:"intermediate", g:["tooling","workflow"],
 b:["AutoML tools — AutoGluon, H2O AutoML, Vertex AI, Auto-sklearn — search over pipelines and hyperparameters, then often ensemble the best candidates. On tabular problems they routinely produce results competitive with an experienced practitioner's first week of work.",
    "They do not automate the parts that matter most: framing the problem, sourcing and cleaning the data, defining the metric, detecting leakage, and deciding whether the model should be deployed at all. Treat AutoML as a strong baseline generator, not a replacement for judgement."],
 k:["Excellent at producing a strong tabular baseline quickly.",
    "Does not touch problem framing, data quality or leakage detection.",
    "Resulting pipelines can be large ensembles that are slow to serve.",
    "Best used to set the bar your bespoke model must clear."],
 r:["Hyperparameter Tuning","Baseline Model","Feature Engineering","Model"]},

{t:"Recommendation System", d:"A system that predicts which items a user is likely to want, from their behaviour and from similar users' behaviour.", l:"intermediate", g:["application","system"],
 b:["Collaborative filtering finds patterns in the user-item interaction matrix — people who liked what you liked also liked this. Content-based filtering matches item attributes to a user profile. Hybrid systems combine both, and modern production systems use a two-stage design: fast candidate retrieval (often via embeddings and ANN search) followed by a heavy ranking model.",
    "The recurring problems are cold start (no history for a new user or item), popularity bias (recommending only what is already popular) and filter bubbles. Diversity and exploration terms are added deliberately to counteract them."],
 k:["Two stages in practice: cheap candidate retrieval, then expensive ranking.",
    "Cold start affects new users and new items differently and needs different fixes.",
    "Offline metrics correlate poorly with online engagement — A/B test.",
    "Explicit diversity and exploration prevent feedback loops."],
 r:["Collaborative Filtering","Embedding","A/B Testing","Approximate Nearest Neighbour"]},

{t:"Collaborative Filtering", d:"Recommending items based on the behaviour of users with similar tastes, without needing to know anything about the items.", l:"intermediate", g:["technique","recommendation"],
 b:["User-based collaborative filtering finds similar users and recommends what they liked. Item-based finds items frequently liked by the same people. Matrix factorisation decomposes the sparse user-item matrix into low-rank user and item embeddings whose dot product predicts a rating — the approach that won the Netflix Prize.",
    "It needs no item metadata, which is its great strength and also its limitation: a brand-new item with no interactions cannot be recommended at all."],
 k:["Requires no item features — only interaction history.",
    "Matrix factorisation learns latent user and item embeddings.",
    "Cold start is fatal for new items; content-based methods fill the gap.",
    "The interaction matrix is typically over 99% empty."],
 r:["Recommendation System","Embedding","Cold Start"]},

{t:"Time Series Forecasting", d:"Predicting future values of a variable that is measured in time order.", l:"intermediate", g:["task","supervised"],
 b:["Time series data has structure other data does not: trend, seasonality, autocorrelation and a strict ordering that cannot be shuffled. Classical methods (ARIMA, exponential smoothing, Prophet) model these components explicitly; ML approaches turn the problem into supervised learning with lag features; deep approaches use temporal convolutions or transformers.",
    "The most common error is careless validation. A random train-test split lets the model see the future, producing brilliant offline results and useless forecasts. Always split chronologically and use rolling-origin evaluation."],
 k:["Never shuffle — split chronologically and validate with rolling origins.",
    "Decompose into trend, seasonality and residual before modelling.",
    "Lag and rolling-window features turn it into a supervised problem.",
    "Compare against naive baselines: last value, and seasonal naive."],
 r:["Data Leakage","Cross-Validation","Time-Series Database","Regression"]},

{t:"Model Capacity", d:"How complex a relationship a model is able to represent.", l:"intermediate", g:["theory","training"],
 b:["Capacity is set by the number of parameters and the family of functions the architecture can express. Too little and the model underfits — a linear model cannot represent an XOR. Too much and it can memorise the training set exactly.",
    "The usual workflow is to give the model enough capacity to overfit a small sample (proving it can learn), then add regularisation and data until the validation gap closes. Capacity without regularisation is a memorisation machine."],
 k:["Determined by parameter count and architecture family.",
    "Too little causes underfitting; too much enables memorisation.",
    "Sanity check: can the model overfit 100 examples perfectly? If not, something is broken.",
    "Modern practice favours high capacity plus strong regularisation."],
 r:["Overfitting","Underfitting","Regularisation","Bias-Variance Trade-off"]},

{t:"Calibration", d:"Whether a model's predicted probabilities match observed frequencies — when it says 70%, does it happen 70% of the time?", l:"advanced", g:["evaluation","metrics"],
 b:["A model can rank perfectly and still be badly calibrated. Neural networks and boosted trees are typically overconfident, pushing probabilities toward 0 and 1. Logistic regression is naturally well calibrated because cross-entropy on a linear model produces it.",
    "Calibration matters whenever the probability itself is used — expected-value decisions, risk pricing, triage thresholds. Platt scaling and isotonic regression fit a correction on a held-out set; reliability diagrams and expected calibration error measure it."],
 k:["Good ranking (AUC) does not imply good calibration.",
    "Deep networks and boosted trees are usually overconfident.",
    "Fix with Platt scaling or isotonic regression on held-out data.",
    "Essential whenever the probability feeds a downstream decision rule."],
 r:["AUC","Cross-Entropy","Logistic Regression","Evaluation Metric"]},

{t:"Evaluation Metric", d:"The number chosen to decide whether one model is better than another.", l:"core", g:["evaluation","workflow"],
 b:["Choosing the metric is a design decision with consequences. Optimise accuracy on imbalanced data and you get a model that ignores the rare class. Optimise clicks and you get clickbait. The metric becomes the target, and the target becomes the behaviour.",
    "Good practice: pick one primary metric aligned with the business outcome, track a small set of guardrail metrics that must not degrade, and verify that the offline metric actually correlates with the online result before trusting it."],
 k:["Goodhart's law: when a measure becomes a target, it stops measuring well.",
    "One primary metric, plus guardrails that must not regress.",
    "Offline metrics must be validated against online outcomes.",
    "Metric choice encodes the relative cost of each kind of error."],
 r:["Accuracy","F1 Score","Baseline Model","A/B Testing"]}

]);
