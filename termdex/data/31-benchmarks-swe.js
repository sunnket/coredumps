/* Benchmarks, the remaining ML and Python/SWE vocabulary, system-design terms
   and the interview-format words the roadmap uses throughout. */

TD.add("mlops", [

{t:"MMLU", a:"Massive Multitask Language Understanding", d:"A 57-subject multiple-choice benchmark, long the default headline number for model knowledge.", l:"intermediate", g:["evaluation"],
 b:["Roughly 16,000 multiple-choice questions spanning mathematics, law, medicine, history and more, at difficulty from school to professional level. It became the standard single figure quoted for a model's breadth of knowledge.",
    "Both of its limitations are worth stating whenever you quote it. It is effectively saturated — frontier models cluster near 90, so the differences sit inside the noise — and being years old and widely published, contamination is very likely. MMLU-Pro and GPQA exist as harder successors."],
 k:["57 subjects, ~16k multiple-choice questions, school to professional level.",
    "Saturated: leading models cluster near 90, so it no longer discriminates.",
    "High contamination risk — it has been on the public web for years.",
    "Multiple choice is not how anyone actually uses a model."],
 x:{lang:"text", code:"Q. A 45-year-old presents with...\n   (A) ...  (B) ...  (C) ...  (D) ...\n\nmeasures recall over 57 subjects\ndoes NOT measure: reasoning depth, tool use, long context,\n                  or anything resembling a real task"},
 r:["Benchmark Saturation","Benchmark Contamination","GSM8K","Evaluation"]},

{t:"GSM8K", d:"A benchmark of 8,500 grade-school maths word problems, long used to measure step-by-step reasoning.", l:"intermediate", g:["evaluation"],
 b:["Each problem needs two to eight arithmetic steps stated in plain language, so it tests whether a model can carry a chain of reasoning rather than recall a fact. It was the benchmark that made chain-of-thought prompting famous, because prompting for intermediate steps improved scores dramatically.",
    "It is now largely saturated — frontier models score in the high nineties — so it no longer separates them. Its other weakness is that the problems are short and self-contained, which is nothing like a real multi-step task. Quote it as history rather than as evidence."],
 k:["8.5k grade-school word problems needing 2-8 arithmetic steps.",
    "The benchmark that demonstrated chain-of-thought prompting.",
    "Effectively saturated; frontier models score in the high nineties.",
    "Short, self-contained problems -- unlike any real task."],
 x:{lang:"text", code:"Natalia sold clips to 48 friends in April, and half as\nmany in May. How many did she sell altogether?\n\n  April 48, May 24, total 72\n  ^ scoring jumps when the model is asked to show its steps"},
 r:["MMLU","Benchmark Saturation","Chain-of-Thought Prompting","Evaluation"]},

{t:"HumanEval", d:"A benchmark of 164 Python functions to be written from their docstring, scored by running unit tests.", l:"intermediate", g:["evaluation"],
 b:["Each item gives a function signature and a docstring; the model writes the body and the result is executed against hidden tests. Because it is checked by running code rather than by matching text, it is far more objective than most benchmarks, and pass@k reports the chance that at least one of k samples passes.",
    "Its limitation is scale: 164 tiny self-contained functions resemble nothing about working in a real codebase with existing conventions, cross-file dependencies and ambiguous requirements. SWE-bench exists precisely to close that gap."],
 k:["164 Python functions written from a docstring, verified by running tests.",
    "Objective -- graded by execution, not by text similarity.",
    "pass@k: the probability at least one of k samples passes.",
    "Tiny isolated functions; nothing like working in a real repository."],
 x:{lang:"python", code:"def has_close_elements(numbers: list[float], threshold: float) -> bool:\n    \"\"\"Return True if any two numbers are closer than threshold.\"\"\"\n    # the model writes the body; hidden tests then run it"},
 r:["SWE-bench","MMLU","Benchmark Saturation","Evaluation"]},

{t:"SWE-bench", d:"A benchmark that asks a model to resolve real GitHub issues in real repositories, verified by the projects' own tests.", l:"advanced", g:["evaluation"],
 b:["Each task is a genuine issue from a large open-source Python project. The model must locate the relevant code across many files, make a change, and have the repository's existing test suite pass. That requires navigating an unfamiliar codebase rather than writing an isolated function.",
    "It is the current headline coding benchmark precisely because it is so much more realistic than HumanEval, and scores are correspondingly far lower. SWE-bench Verified is the human-filtered subset that removes underspecified or impossible tasks, and is the variant usually quoted."],
 k:["Real GitHub issues in real repositories, graded by the project's own tests.",
    "Requires multi-file navigation, not writing one isolated function.",
    "Far harder and far more realistic than HumanEval.",
    "SWE-bench Verified is the human-checked subset that is normally quoted."],
 x:{lang:"text", code:"input   a repository at a given commit + an issue description\ntask    produce a patch\ngrade   run the repo's own test suite -- pass or fail\n\nno partial credit, and nothing short enough to memorise"},
 r:["HumanEval","Benchmark","Evaluation","Benchmark Contamination"]},

{t:"AI Governance", d:"The policies, records and review processes that make an organisation's AI use accountable.", l:"intermediate", g:["governance"],
 b:["It covers who may deploy a model, what must be documented, how risk is classified, how decisions are logged and appealed, and who is answerable when something goes wrong. Model cards, data provenance records, eval reports and incident procedures are its artefacts.",
    "For an engineer the practical implication is that regulated deployments need a paper trail as well as working code: what the model was trained on, how it was evaluated, its known limitations, and what a person can do about a bad decision. Building that in from the start is far cheaper than reconstructing it later."],
 k:["Policy, documentation and review around how AI is built and deployed.",
    "Artefacts: model cards, data provenance, eval reports, incident procedures.",
    "Regulated uses need an audit trail, not only working code.",
    "Risk classification drives the obligations — see the EU AI Act tiers."],
 x:{lang:"text", code:"before deploying a decision-affecting model:\n  what data trained it, and may we use it?\n  what is its risk tier?\n  evaluated across demographic slices?\n  can a human review and reverse a decision?\n  who is accountable, and who is on call?"},
 r:["EU AI Act","Model Card","Responsible AI","Evaluation"]},

{t:"Two-Tower Model", d:"A retrieval architecture with separate encoders for query and item, so item vectors can be precomputed.", l:"advanced", g:["recsys","retrieval"],
 b:["One tower encodes the user or query, the other encodes the item; both produce vectors in a shared space where a dot product means relevance. Because the towers are independent, every item can be embedded offline and indexed for approximate nearest-neighbour search.",
    "That is what makes candidate generation possible over millions of items in milliseconds. It is the same structural bargain as a bi-encoder in retrieval, and it carries the same limitation — no interaction between the two sides — which is why a heavier ranking model re-scores the shortlist afterwards."],
 k:["Separate query and item encoders producing vectors in one shared space.",
    "Item vectors precomputed and indexed, so retrieval is ANN search.",
    "No query-item interaction, so it is coarse — a ranker refines the shortlist.",
    "The retrieval half of the candidate-generation-then-ranking pattern."],
 x:{lang:"text", code:"user tower  -> user_vec  ]\n                          } dot product = relevance\nitem tower  -> item_vec  ]\n\nitems embedded offline -> ANN index -> top 500 candidates\nthen a heavy ranker scores those 500"},
 r:["Candidate Generation","Bi-Encoder","Embedding","Approximate Nearest Neighbour"]},

{t:"Candidate Generation", d:"The cheap first stage of a recommender that narrows millions of items down to a few hundred.", l:"advanced", g:["recsys"],
 b:["Scoring every item with an expensive model is impossible at catalogue scale. Recommenders therefore run in stages: candidate generation retrieves a few hundred plausible items very cheaply, and a heavier ranking model scores only those.",
    "Recall matters far more than precision at this stage — anything not retrieved can never be recommended, however good the ranker is. Multiple sources are usually blended: embedding similarity, recent popularity, co-visitation, and the user's own history."],
 k:["Millions of items down to a few hundred, cheaply. Then a heavy ranker scores those.",
    "Optimise for **recall** here; a missed candidate is unrecoverable downstream.",
    "Usually blends several sources: embeddings, popularity, co-visitation, history.",
    "The same retrieve-then-rerank shape as production RAG."],
 x:{lang:"text", code:"10,000,000 items\n  -> candidate generation (ANN + popularity + history)   ~500\n     -> ranking model (rich features, expensive)          ~50\n        -> re-rank for diversity and freshness            ~10\n\nrecall matters at stage 1; precision at stage 2"},
 r:["Two-Tower Model","Reranking","Recommendation System","Cold Start"]},

{t:"Train-Serve Skew", d:"The gap between how features are computed in training and how they are computed at serving time.", l:"advanced", g:["mlops"],
 b:["A feature engineered in a training notebook with pandas and recomputed in production Java will eventually disagree — different null handling, different rounding, a different time window. The model then sees inputs at serving time that differ subtly from anything it was trained on, and quality degrades with no error anywhere.",
    "It is the central problem a feature store exists to solve: define each feature once and serve the same computation to both paths. The related trap is point-in-time correctness — training on a feature value that was not yet knowable at prediction time, which is leakage wearing a different hat."],
 k:["The same feature computed two ways eventually diverges, silently.",
    "The model sees serving inputs unlike anything it trained on. Quality drops, nothing errors.",
    "A feature store fixes it by defining each feature once for both paths.",
    "Point-in-time correctness is the sibling problem — do not train on future values."],
 x:{lang:"text", code:"training   pandas, nulls -> 0, 30-day window\nserving    Java,   nulls -> -1, 28-day window\n\nsame feature name, different values, no error raised\n-> define it once in a feature store and serve both from it"},
 r:["Feature Store","Data Leakage","Data Drift","Model Serving"]}

]);

TD.add("programming-languages", [

{t:"Type Hints", d:"Optional annotations declaring what types a Python function expects and returns.", l:"core", g:["python","typing"],
 b:["Python does not enforce them at runtime — they are metadata. Their value comes from tooling: `mypy` and `pyright` check them statically, editors use them for completion and inline errors, and libraries such as Pydantic and FastAPI use them to generate validation and API schemas automatically.",
    "For anyone hiring AI engineers, typed code is also the fastest visual signal that a candidate is not a notebook-only developer. The modern syntax is builtin generics (`list[str]`, `dict[str, int]`) and `X | None` rather than the older `typing` imports."],
 k:["Not enforced at runtime; their value is static checking and tooling.",
    "`mypy` or `pyright` in CI is what turns them from decoration into a check.",
    "Pydantic and FastAPI generate validation and schemas from them.",
    "Modern style: `list[str]`, `dict[str, int]`, `X | None`."],
 x:{lang:"python", code:"from typing import Literal\n\ndef rank(docs: list[str],\n         k: int = 5,\n         mode: Literal[\"dense\", \"hybrid\"] = \"hybrid\") -> list[tuple[str, float]]:\n    ...\n\n# mypy --strict catches the caller passing k=\"5\""},
 r:["Pydantic","Static Typing","Pytest","Python"]},

{t:"Pydantic", d:"The Python library that turns type hints into runtime validation, and the backbone of structured LLM output.", l:"intermediate", g:["python","tools"],
 b:["Declare a model as a class with typed fields, and Pydantic validates and coerces incoming data against it, raising a precise error when something does not fit. It is what FastAPI uses for request and response schemas, and what most structured-output libraries use to constrain what a model may return.",
    "Its role in LLM work is specific and important: a Pydantic model gives you a JSON schema to send to the model, and validation of what comes back. That converts *hope the model returns valid JSON* into *guaranteed shape or a clear exception*, which is the difference between a demo and a service."],
 k:["Type hints become runtime validation, with precise error messages.",
    "Generates the JSON schema you hand to the model for structured output.",
    "Validates the response, so bad shapes fail loudly instead of silently.",
    "The foundation of FastAPI request and response handling. v2 is the current line."],
 x:{lang:"python", code:"from pydantic import BaseModel, Field\n\nclass Ticket(BaseModel):\n    category: str\n    urgency: int = Field(ge=1, le=5)\n    summary: str\n\nTicket.model_json_schema()          # give this to the model\nTicket.model_validate_json(reply)   # raises if the reply is wrong"},
 r:["Type Hints","FastAPI","Structured Output","JSON"]},

{t:"Pytest", d:"Python's standard testing framework, built around plain assert statements and fixtures.", l:"core", g:["python","testing"],
 b:["Tests are ordinary functions whose names start with `test_`, using plain `assert`. Fixtures supply reusable setup through function arguments, and `parametrize` runs one test body across many inputs without duplication.",
    "For LLM work the pieces that matter are mocking external APIs (`responses`, `respx`) so tests do not call a paid provider, and `pytest-asyncio` for async code. A repository with tests reads as professional; one without reads as homework — and that is a hiring judgement made in seconds."],
 k:["Plain `assert`; test functions named `test_*`; no boilerplate classes.",
    "Fixtures for reusable setup, `parametrize` for table-driven cases.",
    "Mock provider calls with `responses` or `respx` — never call a paid API in CI.",
    "`pytest-asyncio` for async tests; `-k` and markers to select subsets."],
 x:{lang:"python", code:"import pytest\n\n@pytest.fixture\ndef client(): return make_test_client()\n\n@pytest.mark.parametrize(\"text,expected\", [(\"\", 0), (\"hi\", 1)])\ndef test_chunk_count(client, text, expected):\n    assert len(client.chunk(text)) == expected"},
 r:["Unit Test","Test-Driven Development","Regression Testing","Python"]},

{t:"Async/Await", d:"Python's syntax for concurrent I/O, letting one thread handle many waits at once.", l:"intermediate", g:["python","concurrency"],
 b:["An `async def` function can suspend at an `await` and let the event loop run something else while it waits. It gives concurrency without threads, and it is ideal for I/O-bound work — which is exactly what LLM API calls are.",
    "The practical difference is large: twenty sequential calls at one second each take twenty seconds, while `asyncio.gather` runs them concurrently in about one. The rules that matter are that any blocking call inside a coroutine stalls the whole loop, and that CPU-bound work belongs in processes, not coroutines."],
 k:["Concurrency for I/O-bound work without threads. LLM calls are I/O-bound.",
    "`asyncio.gather` turns twenty sequential seconds into roughly one.",
    "A blocking call inside a coroutine freezes the entire event loop.",
    "CPU-bound work needs processes; async does not help there."],
 x:{lang:"python", code:"import asyncio, httpx\n\nasync def main(prompts):\n    async with httpx.AsyncClient() as c:\n        return await asyncio.gather(*(call(c, p) for p in prompts))\n\n# use httpx, not requests -- requests blocks the loop"},
 r:["Global Interpreter Lock","Concurrency","Generator","Python"]},

{t:"Generator", d:"A function that yields values one at a time, producing them lazily instead of building a whole list.", l:"intermediate", g:["python"],
 b:["A function containing `yield` returns an iterator rather than a value. Execution pauses at each yield and resumes on the next request, so only one item exists in memory at a time. That is what lets you process a file larger than RAM.",
    "In LLM work it is how token streaming is implemented: the server yields tokens as they are produced, and the client renders them immediately, which is why time-to-first-token feels fast even when total generation time is unchanged."],
 k:["`yield` produces values lazily; only one is in memory at a time.",
    "Processes files and streams larger than available RAM.",
    "How token streaming works — yield each token as it arrives.",
    "Single-use: once consumed, a generator is exhausted."],
 x:{lang:"python", code:"def stream_tokens(response):\n    for chunk in response:\n        yield chunk.delta.text        # nothing is buffered\n\nfor token in stream_tokens(r):\n    print(token, end=\"\", flush=True)"},
 r:["Iterator","Async/Await","Streaming Response","Python"]},

{t:"Global Interpreter Lock", a:"GIL", d:"The CPython lock that lets only one thread execute Python bytecode at a time.", l:"intermediate", g:["python","concurrency"],
 b:["Because only one thread runs Python bytecode at once, threads do not give parallel CPU speedup in CPython. They still help for I/O, because the lock is released while waiting on a socket or a file.",
    "The rule to have ready is short: **threads for I/O, processes for CPU, async for many concurrent I/O waits.** NumPy and similar libraries release the lock inside their C code, which is why numeric work often parallelises despite it. Recent CPython versions offer an experimental free-threaded build without the GIL."],
 k:["One thread executes Python bytecode at a time in CPython.",
    "Threads for I/O, processes for CPU, async for many concurrent waits.",
    "Released during I/O and inside C extensions such as NumPy.",
    "An experimental free-threaded CPython build now exists."],
 x:{lang:"python", code:"# CPU-bound -> processes\nfrom multiprocessing import Pool\nwith Pool() as p: p.map(heavy, items)\n\n# I/O-bound -> async (or threads)\nawait asyncio.gather(*(fetch(u) for u in urls))"},
 r:["Async/Await","Process vs Thread","Concurrency","Python"]},

{t:"cProfile", d:"Python's built-in profiler, reporting how much time is spent in each function and how often it is called.", l:"intermediate", g:["python","tools"],
 b:["Run a program under it and get a table of calls, total time and cumulative time per function. That replaces guessing about bottlenecks with measurement, and the result is regularly surprising — the loop you were about to optimise turns out to be a rounding error next to a query inside it.",
    "Read the call counts as carefully as the times: fifty thousand calls to one function usually means an N+1 pattern rather than slow code. `py-spy` is the sampling alternative that can attach to an already-running process, which is what you want in production."],
 k:["`tottime` is the function itself; `cumtime` includes everything it calls.",
    "Call counts often reveal more than timings — high counts mean N+1 patterns.",
    "Profile realistic data; small inputs hide the real problem.",
    "`py-spy` samples a live process without restarting it."],
 x:{lang:"bash", code:"python -m cProfile -s cumtime app.py | head -20\n\n#  ncalls  tottime  cumtime  function\n#   50000    0.021   12.400  api.py:44(fetch_user)   <- 50k calls!\n\npy-spy top --pid 4821        # attach to a running process"},
 r:["Profiler","Bottleneck","Premature Optimisation","Python"]},

{t:"Poetry", d:"A Python dependency manager that resolves versions, writes a lockfile and packages projects from one config file.", l:"intermediate", g:["python","tools"],
 b:["It manages dependencies in `pyproject.toml`, resolves a consistent set of versions, records exact pins in `poetry.lock`, and manages a virtual environment. The lockfile is the important part: it is what makes an install reproducible today and in six months.",
    "`uv` has become the faster alternative and is now many teams' default, but the principle is identical and it is the principle that matters — declared dependencies plus a committed lockfile. Reproducibility is a repeatedly named hiring gap."],
 k:["Dependencies in `pyproject.toml`; exact pins in a committed lockfile.",
    "The lockfile is what makes an install reproducible months later.",
    "`uv` is the faster modern alternative with the same model.",
    "**Commit the lockfile.** Without it the declaration means little."],
 x:{lang:"bash", code:"poetry add httpx pydantic\npoetry install --sync        # exactly the lockfile, nothing else\npoetry run pytest\n\n# uv, same idea, much faster:\n#   uv add httpx && uv sync"},
 r:["Virtual Environment","Lockfile","Dependency","Package"]},

{t:"Tenacity", d:"A Python library for retrying failed operations with configurable backoff and stop conditions.", l:"intermediate", g:["python","tools"],
 b:["LLM and network APIs fail routinely — rate limits, 500s, timeouts — and code that does not handle that is not production code. Tenacity wraps a function in a decorator specifying how many attempts, how long to wait between them, and which exceptions are worth retrying.",
    "The two details that matter are exponential backoff with jitter, so recovering clients do not synchronise into a new spike, and never retrying a 4xx, because the same malformed request will fail identically. Retrying a client error is how a small problem becomes a rate-limit ban."],
 k:["Decorator-based retries with exponential backoff and a stop condition.",
    "Add jitter, or many clients retry in lockstep and recreate the spike.",
    "**Retry 5xx and 429. Never retry 4xx** — it will fail the same way.",
    "Always set a stop condition; unbounded retries are their own outage."],
 x:{lang:"python", code:"from tenacity import retry, stop_after_attempt, wait_random_exponential\n\n@retry(stop=stop_after_attempt(4),\n       wait=wait_random_exponential(min=1, max=20),   # jitter included\n       retry=retry_if_exception_type(RateLimitError))\nasync def call_model(prompt): ..."},
 r:["Rate Limit","Retry","Idempotency","Error Handling"]},

{t:"Structured Logging", d:"Emitting logs as machine-readable key-value records rather than as prose sentences.", l:"intermediate", g:["python","observability"],
 b:["A sentence like *failed to process order 4821* can only be grepped. The same event as `{\"event\": \"order_failed\", \"order_id\": 4821, \"reason\": \"timeout\"}` can be filtered, aggregated, counted and alerted on. That difference is what makes logs usable at scale.",
    "Two rules follow. Include a request or trace ID on every line so one request's journey can be reconstructed across services. And never log secrets or personal data — logs are widely readable, retained for a long time, and are the most common accidental disclosure path."],
 k:["Key-value records, not sentences — filterable, aggregatable, alertable.",
    "Carry a request or trace ID on every line to reconstruct a journey.",
    "**Never log secrets, tokens or PII.** Logs are widely readable and long-lived.",
    "`structlog` in Python; `print()` has no place in a service."],
 x:{lang:"python", code:"import structlog\nlog = structlog.get_logger()\n\nlog.info(\"retrieval_complete\",\n         request_id=rid, chunks=5, latency_ms=143, cache_hit=False)\n\n# {\"event\":\"retrieval_complete\",\"request_id\":\"a91f\",\"chunks\":5,...}"},
 r:["Log Level","Observability","Distributed Tracing","Secret"]},

{t:"FastAPI", d:"A modern Python web framework that derives validation and API documentation from type hints.", l:"intermediate", g:["python","tools"],
 b:["Declare a route as a typed function and FastAPI validates the request against those types via Pydantic, serialises the response, and generates an OpenAPI schema and interactive documentation with no extra work. It is async-first, which suits I/O-bound LLM calls.",
    "For AI services the features that matter are `StreamingResponse` for token-by-token output, dependency injection for shared clients and auth, and background tasks for work that must outlive the response. It is the default choice for putting a model behind an HTTP API."],
 k:["Validation, serialisation and OpenAPI docs derived from type hints.",
    "Async-first, which fits I/O-bound LLM calls naturally.",
    "`StreamingResponse` / SSE is essential for token-by-token output.",
    "Dependency injection for clients and auth; background tasks for post-response work."],
 x:{lang:"python", code:"from fastapi import FastAPI\nfrom fastapi.responses import StreamingResponse\n\napp = FastAPI()\n\n@app.post(\"/chat\")\nasync def chat(req: ChatRequest) -> StreamingResponse:   # validated by Pydantic\n    return StreamingResponse(stream_tokens(req.prompt),\n                             media_type=\"text/event-stream\")"},
 r:["Pydantic","Type Hints","Server-Sent Events","Async/Await"]}

]);

TD.add("data-engineering", [

{t:"pgvector", d:"The Postgres extension that stores embeddings and searches them, alongside your ordinary relational data.", l:"intermediate", g:["vector","tools"],
 b:["It adds a `vector` column type plus distance operators and index types (HNSW and IVFFlat). Because it lives inside Postgres, a vector search can join tables, filter on any column, and participate in a transaction — all things a standalone vector database makes awkward.",
    "That is why it is the right default for most applications. Metadata filtering, permissions and freshness constraints are ordinary SQL `WHERE` clauses rather than a separate filtering feature, and you have one database to back up and operate instead of two."],
 k:["A `vector` column type with distance operators and HNSW/IVFFlat indexes.",
    "Filtering and joins are plain SQL — no separate metadata system needed.",
    "One database to operate and back up rather than two.",
    "`<=>` cosine, `<->` L2, `<#>` inner product. Match the operator to your index."],
 x:{lang:"sql", code:"CREATE EXTENSION vector;\nALTER TABLE docs ADD COLUMN embedding vector(1536);\nCREATE INDEX ON docs USING hnsw (embedding vector_cosine_ops);\n\nSELECT id, text FROM docs\nWHERE tenant_id = $1 AND updated_at > now() - interval '1 year'\nORDER BY embedding <=> $2 LIMIT 10;"},
 r:["Vector Database","HNSW","IVFFlat","Metadata Filtering"]},

{t:"Airflow", d:"The widely used orchestrator that runs data pipelines as scheduled directed acyclic graphs of tasks.", l:"intermediate", g:["pipelines","tools"],
 b:["Pipelines are declared in Python as a DAG of tasks with dependencies, and Airflow schedules them, retries failures, and tracks each run. Its UI showing which task failed on which date is what makes it operationally popular.",
    "The concepts that transfer to any orchestrator are the ones worth knowing: idempotent tasks so a retry is safe, backfilling to reprocess historical dates, and the fact that a DAG is exactly a topological sort of the work. Dagster and Prefect are the modern alternatives with a stronger data-asset focus."],
 k:["Pipelines as DAGs of tasks; scheduling, retries and per-run history.",
    "Tasks must be idempotent — retries and backfills rerun them.",
    "Backfilling reprocesses historical dates through the same pipeline.",
    "Dagster and Prefect are the newer, more asset-oriented alternatives."],
 x:{lang:"python", code:"with DAG(\"embed_docs\", schedule=\"@daily\", catchup=True) as dag:\n    extract = PythonOperator(task_id=\"extract\", python_callable=pull)\n    embed   = PythonOperator(task_id=\"embed\",   python_callable=embed_batch)\n    extract >> embed         # a dependency edge -- the DAG is the schedule"},
 r:["Directed Acyclic Graph","Topological Sort","Idempotency","Data Pipeline"]},

{t:"Spark", a:"Apache Spark", d:"A distributed engine for processing datasets far larger than one machine's memory.", l:"intermediate", g:["pipelines","tools"],
 b:["It partitions data across a cluster and runs transformations in parallel, building a lazy plan and executing it only when a result is actually requested. That laziness lets it optimise the whole chain rather than each step in isolation.",
    "For AI work it appears in large-scale preprocessing: deduplicating a training corpus, computing features over billions of rows, or batch-embedding a large document set. Awareness level is enough for a fresher — knowing that it exists, that it is lazy, and that shuffles between partitions are the expensive operation."],
 k:["Distributes data across a cluster; transformations run in parallel.",
    "Lazy evaluation — nothing runs until an action requests a result.",
    "Shuffles (joins, groupBy) move data between nodes and dominate cost.",
    "Awareness level suffices for a fresher; it appears in design discussions."],
 x:{lang:"python", code:"df = spark.read.parquet(\"s3://corpus/\")\nclean = (df.filter(df.lang == \"en\")\n           .dropDuplicates([\"hash\"])          # a shuffle -- the expensive part\n           .repartition(200))\nclean.write.parquet(\"s3://corpus-clean/\")     # only NOW does anything run"},
 r:["Data Pipeline","Batch Processing","Airflow","Parquet"]}

]);

TD.add("software-engineering", [

{t:"Technical Interview", d:"A live assessment of problem-solving where how you reason is graded alongside what you produce.", l:"core", g:["career","interview"],
 b:["The format is deliberately under-specified: ambiguity is left in so that clarifying questions are part of the assessment. Starting to code immediately is the most common early mistake, because the interviewer is trying to observe how you handle an incomplete problem statement.",
    "A reliable structure is: clarify, work a small example by hand, state the brute force with its complexity, name the bottleneck and the tool that removes it, code cleanly while narrating, then test your own code against edge cases and state both complexities. A silent correct answer scores worse than a narrated one."],
 k:["Ambiguity is deliberate — clarifying questions are part of the assessment.",
    "Clarify → example → brute force → optimise → code → test.",
    "State the brute force and its complexity; it buys goodwill and a fallback.",
    "Narrate. A silent correct solution scores below a narrated one."],
 x:{lang:"text", code:"~2 min  clarify: empty input? duplicates? negatives? size of n?\n~3 min  work one small example by hand\n~1 min  \"brute force is nested loops, O(n^2) time, O(1) space\"\n3-5     name the bottleneck, then the tool that removes it\n15-20   code cleanly, talking but not narrating every keystroke\n3-5     test it yourself; finish with time AND space complexity"},
 r:["System Design Interview","Behavioural Interview","STAR Method","Online Assessment"]},

{t:"Behavioural Interview", d:"A round assessing ownership, collaboration and judgement through stories about your past work.", l:"core", g:["career","interview"],
 b:["It is not small talk, and at senior levels it often carries more of the levelling decision than the coding rounds. The questions probe how you behave under ambiguity, disagreement and failure — a project that went wrong, a technical decision you disagreed with, a time you chose speed over quality.",
    "The upgrade that matters is adding reasoning to the story. Describing what happened is the baseline; explaining why you chose that path, what you would do differently and why, is what demonstrates judgement rather than recall."],
 k:["Assesses ownership, collaboration and judgement — not personality.",
    "Often decides the level, especially at senior grades.",
    "Use STAR, then add *what I would do differently and why*.",
    "Prepare specific stories in advance; generic answers are transparently generic."],
 x:{lang:"text", code:"have a real story ready for each:\n  a technical decision you disagreed with\n  a project that failed, and what you learned\n  ambiguous requirements you had to resolve\n  choosing speed over quality, deliberately\n  an ethical concern you raised"},
 r:["STAR Method","Technical Interview","System Design Interview","Take-Home Assignment"]},

{t:"STAR Method", d:"A structure for answering behavioural questions: Situation, Task, Action, Result.", l:"core", g:["career","interview"],
 b:["Set the context briefly, say what you were responsible for, describe what **you** specifically did, and finish with the measurable outcome. Its purpose is to stop rambling and to keep the focus on your own contribution rather than the team's.",
    "The most common failures are spending too long on Situation and describing what the team did rather than what you did. For engineering roles, add a fifth element after Result — what you would do differently and why — because that is where judgement is actually visible."],
 k:["Situation, Task, Action, Result — keep Situation to a couple of sentences.",
    "Action means *what you did*, not what the team did.",
    "Result should carry a number wherever one exists.",
    "Add *what I would change and why* — that is the part that shows judgement."],
 x:{lang:"text", code:"S  ingestion pipeline failed silently for three days\nT  I owned the ingestion service\nA  added dead-letter queue + alert on zero-row runs;\n   backfilled the missing window\nR  detection went from 3 days to 4 minutes\n+  I would have added the alert when I built it -- I had\n   assumed failures would be loud, and they were not"},
 r:["Behavioural Interview","Technical Interview","Postmortem","Root Cause Analysis"]},

{t:"System Design Interview", d:"An open-ended round where you architect a system under stated constraints and defend the trade-offs.", l:"intermediate", g:["career","interview"],
 b:["There is no correct answer, and that is the point — the round measures how you reason about scale, cost, latency and failure. For AI roles it is usually the decisive round, and it is the one candidates prepare least because it cannot be ground out like algorithm practice.",
    "A workable structure: clarify who uses it and at what scale, choose the approach and justify it against the constraints, draw the components and the data flow, go deep on one part, then cover failure modes and how you would evaluate and monitor it. Most candidates run out of material exactly at those last two steps, which is where the differentiation is."],
 k:["Clarify → approach → architecture → deep dive → failures → evaluation.",
    "The decisive round for AI roles, and the least prepared for.",
    "Say the trade-offs out loud; unprompted trade-off reasoning is what scores.",
    "Failure modes and monitoring are where most candidates run dry — prepare those."],
 x:{lang:"text", code:"say these unprompted:\n  \"bigger chunks give more context but dilute the embedding\"\n  \"a smaller model halves cost -- I would need an eval to confirm quality\"\n  \"caching cuts cost but risks serving stale answers\"\n\nthen: what breaks, and how would I know?"},
 r:["Technical Interview","Behavioural Interview","Evaluation","Load Balancing"]},

{t:"Take-Home Assignment", d:"A coding task done in your own time, scored on professionalism as much as on the solution.", l:"intermediate", g:["career","interview"],
 b:["The algorithmic part is usually the easy part. What is being read is whether you write like a professional: a README saying how to run it and what you assumed, tests that exist at all, sensible file structure, deliberate error handling, and several small commits rather than one called *done*.",
    "The highest-value section is *what I would do with more time*. Three honest bullet points convert every omission into a deliberate decision, pre-empt the obvious criticism, and demonstrate judgement about scope — which is exactly what the exercise is trying to observe."],
 k:["Scored on professionalism: README, tests, structure, commits, error handling.",
    "Finishing the stated scope beats a half-built extra feature.",
    "*What I would do with more time* is the highest-value paragraph — be specific.",
    "Several small commits; one commit called *done* reads badly."],
 x:{lang:"text", code:"## What I would do with more time\n- No pagination on /items; fine at this size, breaks past ~5k rows.\n- Tests cover the happy path and two edge cases; I would add\n  property-based tests for the parser.\n- Config is hard-coded -- would move to environment variables.\n\nnot: \"add more tests and improve performance\""},
 r:["Technical Interview","README","Unit Test","Code Review"]},

{t:"Online Assessment", d:"An unsupervised, timed coding test with hidden test cases, used as an early filter.", l:"core", g:["career","interview"],
 b:["Nobody reads your reasoning, so narrating buys nothing and the brute force earns no credit — only passing tests count. You must invent your own edge cases, because the hidden tests certainly include them and no interviewer will hint.",
    "The checklist that saves most failures: empty input, a single element, all-identical values, already-sorted and reverse-sorted input, the maximum n in the constraints, negatives and zero, integer overflow in C++ or Java, and — the one that fails more submissions than any algorithmic error — re-reading the required return format before you submit."],
 k:["Only passing tests count; explanation and partial progress earn nothing.",
    "Write the real solution — a timeout scores the same as a wrong answer.",
    "Invent your own edge cases: empty, single, identical, sorted, max n, negatives.",
    "**Re-read the return format.** Values instead of indices scores zero everywhere."],
 x:{lang:"text", code:"before submitting, check:\n  [ ] empty input and single element\n  [ ] all values identical\n  [ ] sorted and reverse-sorted\n  [ ] maximum n from the constraints -- does it finish?\n  [ ] negatives and zero, if permitted\n  [ ] overflow (C++/Java)\n  [ ] the RETURN FORMAT -- indices or values? sorted? 0- or 1-indexed?"},
 r:["Technical Interview","Edge Case","Time Complexity","Take-Home Assignment"]}

]);
