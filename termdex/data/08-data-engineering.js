TD.add("data-engineering", [

{t:"Data Pipeline", d:"An automated sequence of steps that moves and transforms data from where it is produced to where it is used.", l:"core", g:["fundamentals","pipeline"],
 b:["A pipeline extracts from sources, applies transformations, and loads into a destination. The engineering challenge is rarely the transformation logic — it is everything around it: what happens when a source is late, when a schema changes silently, when a run fails halfway, when someone reruns yesterday's job.",
    "The properties that separate a real pipeline from a script are idempotency (rerunning produces the same result), observability (you know when it failed and why), and lineage (you can trace any output back to its inputs)."],
 k:["Idempotency is the single most important property — reruns must be safe.",
    "Design for late-arriving and out-of-order data from the start.",
    "Failures are normal; partial-failure recovery is part of the design.",
    "Every output should be traceable back to its source."],
 dg:"etl-elt",
 r:["ETL","ELT","Apache Airflow","Idempotency","Data Quality"]},

{t:"ETL", a:"Extract, Transform, Load", d:"The classical pipeline order: pull data out, clean and reshape it, then load the finished result into the warehouse.", l:"core", g:["pipeline","pattern"],
 b:["Transforming before loading made sense when warehouse storage and compute were expensive — you only paid to store the clean, modelled result. It also means the warehouse only ever contains validated data.",
    "The costs are that the raw data is not retained, so a transformation bug discovered later cannot be fixed by reprocessing, and the transformation layer becomes a separate system to build and scale."],
 k:["Transform happens before loading, so only clean data lands.",
    "Raw data is not preserved — bugs found later may be unfixable.",
    "Made sense when warehouse compute was the scarce resource.",
    "Still correct when data must be masked or filtered before it lands."],
 dg:"etl-elt",
 r:["ELT","Data Pipeline","Data Warehouse","Data Quality"]},

{t:"ELT", a:"Extract, Load, Transform", d:"Load raw data into the warehouse first, then transform it there using the warehouse's own compute.", l:"core", g:["pipeline","pattern"],
 b:["Cheap cloud storage and elastic warehouse compute inverted the old economics. ELT loads everything raw, then transforms with SQL inside the warehouse. Because the raw layer is retained, any transformation can be rebuilt from source when logic changes or a bug is found.",
    "This is the modern default, and dbt exists to manage the transformation layer: SQL models, dependency graphs, tests and documentation, all in version control."],
 k:["Raw data is preserved, so transformations can always be replayed.",
    "Transformation runs as SQL on warehouse compute — one system, not two.",
    "dbt is the standard tool for managing the transformation layer.",
    "Storage cost of the raw layer is the price of that flexibility."],
 dg:"etl-elt",
 r:["ETL","dbt","Data Warehouse","Data Pipeline","Medallion Architecture"]},

{t:"Batch Processing", d:"Processing data in large scheduled chunks rather than continuously.", l:"core", g:["pipeline","pattern"],
 b:["A batch job runs on a schedule — hourly, nightly — reads a bounded set of data, processes it and writes results. Because the input is bounded and the job runs in isolation, batch is far simpler to reason about, test, retry and backfill than streaming.",
    "The cost is latency: results are as fresh as the last run. For reporting, billing, training data preparation and most analytics that is entirely acceptable, and batch remains the right default."],
 k:["Bounded input, scheduled execution, simple retry and backfill semantics.",
    "Latency equals the schedule interval.",
    "Much easier to test and reason about than streaming.",
    "Still the correct default for analytics and reporting."],
 dg:"batch-stream",
 r:["Stream Processing","Data Pipeline","Apache Airflow","Backfill"]},

{t:"Stream Processing", d:"Processing each event as it arrives, producing continuously updated results.", l:"core", g:["pipeline","pattern"],
 b:["Streaming handles an unbounded sequence of events with second- or sub-second latency. It is what fraud detection, live dashboards, real-time personalisation and alerting require. Kafka Streams, Flink and Spark Structured Streaming are the common engines.",
    "It is genuinely harder than batch. Events arrive late and out of order, so you need windowing and watermarks; state must be maintained and checkpointed; and exactly-once semantics require careful coordination. Adopt it when latency genuinely matters, not by default."],
 k:["Unbounded input, continuous output, seconds of latency.",
    "Late and out-of-order events are the norm — windows and watermarks handle them.",
    "Stateful operations need checkpointing to survive restarts.",
    "Meaningfully harder to operate than batch — justify the latency requirement."],
 dg:"batch-stream",
 r:["Batch Processing","Apache Kafka","Apache Flink","Windowing","Exactly-Once Semantics"]},

{t:"Apache Kafka", d:"A distributed append-only log used as the backbone for event streaming between systems.", l:"core", g:["tool","streaming"],
 b:["Kafka stores records in topics, each split into partitions that are ordered, immutable logs. Producers append; consumers read at their own pace, tracking their own offset. Because the log is retained rather than deleted on read, many independent consumers can read the same data and a consumer can rewind and reprocess history.",
    "The partition is the unit of parallelism and of ordering: records are ordered within a partition, not across a topic. Choosing a partition key that both distributes evenly and keeps related events together is the central design decision."],
 k:["Topics are split into partitions; ordering is guaranteed within a partition only.",
    "Consumers track their own offset — replay is a normal operation.",
    "Retention is time or size based, not read based.",
    "Partition key choice determines both ordering and load balance."],
 x:{lang:"python", code:'from kafka import KafkaProducer, KafkaConsumer\nimport json\n\nproducer = KafkaProducer(\n    bootstrap_servers="localhost:9092",\n    value_serializer=lambda v: json.dumps(v).encode(),\n    acks="all")                       # wait for all in-sync replicas\n\nproducer.send("orders", key=b"cust-42", value={"id": 1, "total": 99.5})\n\nconsumer = KafkaConsumer("orders", group_id="billing",\n                         enable_auto_commit=False)   # commit after processing'},
 dg:"kafka",
 r:["Stream Processing","Message Queue","Apache Flink","Change Data Capture","Partitioning"]},

{t:"Apache Spark", d:"A distributed engine for large-scale data processing, in batch and in streaming.", l:"core", g:["tool","processing"],
 b:["Spark distributes work across a cluster and keeps intermediate results in memory, which made it far faster than MapReduce for iterative workloads. You write transformations on DataFrames and Spark builds a lazy execution plan, optimises it with Catalyst, and runs it only when an action requires a result.",
    "The recurring performance issues are shuffles — operations like joins and group-bys that move data across the network — and data skew, where one partition holds far more rows than the others and one task takes ten times as long as the rest."],
 k:["Lazy evaluation: transformations build a plan, actions trigger execution.",
    "Shuffles are the expensive operation — minimise and inspect them.",
    "Data skew makes one task dominate runtime; salt keys to fix it.",
    "PySpark wraps the Scala API; use DataFrames rather than RDDs."],
 x:{lang:"python", code:'from pyspark.sql import functions as F\n\ndaily = (spark.read.parquet("s3://lake/events/")\n         .filter(F.col("event_date") >= "2025-01-01")\n         .groupBy("event_date", "country")\n         .agg(F.countDistinct("user_id").alias("dau"))\n         .repartition("event_date"))\n\ndaily.write.mode("overwrite").partitionBy("event_date").parquet("s3://lake/dau/")'},
 r:["Hadoop","MapReduce","Batch Processing","Parquet","Data Skew"]},

{t:"Hadoop", d:"The original open-source framework for distributed storage and batch processing of very large datasets.", l:"intermediate", g:["tool","processing"],
 b:["Hadoop combined HDFS, a distributed filesystem replicating blocks across commodity machines, with MapReduce for computation and YARN for resource scheduling. Its core insight was to move computation to the data rather than pulling terabytes across the network.",
    "It made big data affordable on commodity hardware and defined an era. Cloud object storage replaced HDFS and Spark replaced MapReduce, so new systems rarely start here — but the concepts and a great deal of running infrastructure remain."],
 k:["HDFS for storage, MapReduce for compute, YARN for scheduling.",
    "Move computation to the data, not data to the computation.",
    "Largely displaced by object storage plus Spark.",
    "Its concepts underpin most of what replaced it."],
 r:["MapReduce","HDFS","Apache Spark","Data Lake"]},

{t:"MapReduce", d:"A programming model that splits computation into a parallel map phase and an aggregating reduce phase.", l:"intermediate", g:["pattern","processing"],
 b:["Map applies a function to each input record independently, producing key-value pairs. The framework groups pairs by key — the shuffle — and reduce aggregates each group. Because map tasks are independent, the model parallelises across thousands of machines and tolerates individual node failures by re-running tasks.",
    "The limitation is that every stage writes to disk, so iterative algorithms are slow. Spark's in-memory model addressed exactly that. The mental model — partition, process independently, combine — remains fundamental to distributed data work."],
 k:["Map in parallel, shuffle by key, reduce per group.",
    "Fault tolerant by re-running failed tasks.",
    "Disk between stages makes iteration slow.",
    "The conceptual foundation of nearly all distributed processing."],
 r:["Hadoop","Apache Spark","Data Skew","Batch Processing"]},

{t:"HDFS", a:"Hadoop Distributed File System", d:"A distributed filesystem that splits large files into blocks replicated across many machines.", l:"advanced", g:["storage","tool"],
 b:["HDFS splits files into large blocks (typically 128 MB) and stores three replicas of each on different nodes, giving fault tolerance and letting computation run on a node that already holds the data. A NameNode holds the metadata; DataNodes hold the blocks.",
    "It is optimised for large sequential reads and append-only writes, and is poor at small files — millions of them exhaust NameNode memory. Cloud object storage (S3, GCS, ADLS) has largely replaced it because it separates storage from compute and scales without cluster management."],
 k:["Large blocks, three replicas, write-once append-only semantics.",
    "Data locality lets computation run where the data already is.",
    "The small-files problem exhausts NameNode memory.",
    "Superseded by cloud object storage in most new systems."],
 r:["Hadoop","Object Storage","Data Lake","MapReduce"]},

{t:"Apache Airflow", d:"An orchestrator that schedules and monitors data workflows defined as directed acyclic graphs in Python.", l:"core", g:["tool","orchestration"],
 b:["Airflow expresses a workflow as a DAG of tasks with dependencies. It handles scheduling, retries, backfills, alerting and a UI showing what ran and what failed. Because DAGs are Python, they can be generated programmatically and kept in version control.",
    "The standard mistakes are putting heavy computation inside Airflow tasks rather than delegating to Spark or the warehouse, and writing tasks that are not idempotent — which makes retries and backfills produce wrong results."],
 k:["Workflows as Python DAGs, with scheduling, retries and backfill built in.",
    "Airflow orchestrates; it should not do the heavy computation itself.",
    "Every task must be idempotent or retries corrupt data.",
    "Dagster and Prefect are the modern alternatives, with better data awareness."],
 x:{lang:"python", code:'from airflow.decorators import dag, task\nimport pendulum\n\n@dag(schedule="0 3 * * *", start_date=pendulum.datetime(2025, 1, 1),\n     catchup=False, max_active_runs=1)\ndef daily_revenue():\n\n    @task\n    def extract(ds=None): return pull_orders(day=ds)\n\n    @task(retries=3)\n    def load(rows):       return warehouse.upsert("orders", rows)\n\n    load(extract())\n\ndaily_revenue()'},
 r:["Directed Acyclic Graph","Data Pipeline","Idempotency","Backfill","dbt"]},

{t:"Directed Acyclic Graph", a:"DAG", d:"A graph of tasks with dependencies and no cycles — the standard way to model a workflow.", l:"core", g:["fundamentals","orchestration"],
 b:["Nodes are tasks, edges are dependencies, and the absence of cycles guarantees a valid execution order exists. The orchestrator topologically sorts the graph, runs independent branches in parallel, and blocks a task until its upstream dependencies succeed.",
    "The same structure appears throughout computing: build systems, Spark execution plans, git commit history, dbt model graphs and neural network computation graphs are all DAGs."],
 k:["No cycles means a valid execution order always exists.",
    "Independent branches run in parallel automatically.",
    "A cycle in a workflow definition is a configuration error, always.",
    "The same structure underlies build tools, git history and Spark plans."],
 r:["Apache Airflow","Data Pipeline","dbt","Computational Graph"]},

{t:"dbt", a:"data build tool", d:"A framework for managing warehouse transformations as version-controlled, tested, documented SQL models.", l:"core", g:["tool","transformation"],
 b:["dbt lets you write each transformation as a `SELECT` statement in a `.sql` file. References between models with the `ref()` function build a dependency graph automatically, so dbt knows the correct build order and can rebuild only what changed.",
    "Its real contribution is bringing software engineering practice to analytics: version control, code review, automated tests on data (uniqueness, not-null, referential integrity, accepted values), generated documentation and lineage graphs."],
 k:["Each model is a SELECT; `ref()` builds the dependency graph automatically.",
    "Built-in data tests: unique, not_null, relationships, accepted_values.",
    "Incremental models rebuild only new or changed rows.",
    "Generates documentation and a lineage graph from the code itself."],
 x:{lang:"sql", code:'-- models/marts/daily_revenue.sql\n{{ config(materialized=\'incremental\', unique_key=\'order_date\') }}\n\nSELECT\n    order_date,\n    country,\n    SUM(total) AS revenue\nFROM {{ ref(\'stg_orders\') }}\n{% if is_incremental() %}\nWHERE order_date > (SELECT MAX(order_date) FROM {{ this }})\n{% endif %}\nGROUP BY 1, 2'},
 r:["ELT","Data Warehouse","Data Quality","Data Lineage","SQL"]},

{t:"Apache Flink", d:"A distributed stream processing engine with strong state management and exactly-once guarantees.", l:"advanced", g:["tool","streaming"],
 b:["Flink is streaming-first: batch is treated as a bounded stream rather than the other way round. It provides event-time processing with watermarks, sophisticated windowing, and large keyed state backed by RocksDB with periodic checkpoints to durable storage.",
    "Those checkpoints are what enable exactly-once semantics end to end: on failure, Flink restores state from the last checkpoint and replays from the corresponding source offsets. It is the engine of choice for genuinely stateful, low-latency stream processing."],
 k:["Streaming-first; batch is a bounded stream.",
    "Event-time processing with watermarks handles out-of-order data correctly.",
    "Checkpointed keyed state gives exactly-once processing.",
    "More operationally demanding than Kafka Streams for simple cases."],
 r:["Stream Processing","Apache Kafka","Windowing","Exactly-Once Semantics"]},

{t:"Data Warehouse", d:"A central analytical database holding cleaned, modelled data from across an organisation.", l:"core", g:["storage","analytics"],
 b:["A warehouse is optimised for reading: columnar storage, compression, massive parallelism and a schema designed around the questions people ask. Data arrives from operational systems, is cleaned and conformed, and is organised into fact and dimension tables.",
    "Its defining property is schema on write — structure is imposed as data lands, which guarantees consistency but requires modelling work up front and makes schema changes expensive. Snowflake, BigQuery, Redshift and Databricks SQL are the common platforms."],
 k:["Schema on write: structure is enforced before data lands.",
    "Columnar storage and compression make analytical scans fast.",
    "Modelled around business questions, usually as a star schema.",
    "Separate from operational databases by design — never query production for analytics."],
 dg:"warehouse-lake",
 r:["Data Lake","Lakehouse","Star Schema","OLAP","Snowflake"]},

{t:"Data Lake", d:"A large repository holding raw data in its original format, structured or not, until someone needs it.", l:"core", g:["storage","analytics"],
 b:["A lake stores files — JSON, CSV, Parquet, images, logs — in cheap object storage with no schema imposed on arrival. Structure is applied at read time by whatever tool queries it, which means you can land data now and decide how to use it later.",
    "That flexibility is also the failure mode. Without cataloguing, governance and quality checks, a lake becomes a data swamp: petabytes nobody can find, trust or interpret. Metadata discipline is what separates the two."],
 k:["Schema on read: land anything now, interpret it later.",
    "Cheap object storage, any file format.",
    "Without a catalogue and governance it degrades into a swamp.",
    "Table formats like Iceberg and Delta add warehouse guarantees on top."],
 dg:"warehouse-lake",
 r:["Data Warehouse","Lakehouse","Object Storage","Data Catalog","Parquet"]},

{t:"Lakehouse", d:"An architecture that adds warehouse guarantees — transactions, schema enforcement, time travel — to files in a data lake.", l:"intermediate", g:["storage","architecture"],
 b:["Open table formats — Delta Lake, Apache Iceberg, Apache Hudi — store metadata alongside Parquet files describing which files constitute the current table version. That gives ACID transactions, schema evolution, time travel and efficient upserts on top of plain object storage.",
    "The result is lake economics with warehouse behaviour, and no vendor lock-in on the storage layer since the files remain open Parquet that any engine can read."],
 k:["Open table formats add ACID and schema control over Parquet files.",
    "Time travel lets you query the table as of any earlier version.",
    "Efficient upserts and deletes — historically the lake's weakest point.",
    "Delta, Iceberg and Hudi are the three implementations."],
 dg:"warehouse-lake",
 r:["Data Lake","Data Warehouse","Apache Iceberg","Delta Lake","Parquet"]},

{t:"Apache Iceberg", d:"An open table format that brings ACID transactions and schema evolution to files in object storage.", l:"advanced", g:["storage","format"],
 b:["Iceberg maintains a metadata tree — snapshots pointing to manifest lists pointing to data files — so a table version is a precise set of files. Readers see a consistent snapshot while writers add new ones, giving isolation without locking.",
    "Hidden partitioning is its standout feature: the partition scheme is recorded in metadata rather than encoded in directory paths, so queries do not need to know about it and the scheme can change without rewriting data. It is engine-neutral, readable by Spark, Trino, Flink, Snowflake and DuckDB."],
 k:["Snapshot-based metadata gives serialisable isolation without locks.",
    "Hidden partitioning: queries need not know the partition scheme.",
    "Partition evolution without rewriting existing data.",
    "Engine-neutral — the strongest argument against lock-in."],
 r:["Lakehouse","Delta Lake","Parquet","Data Lake","Partitioning"]},

{t:"Delta Lake", d:"Databricks' open table format, adding a transaction log over Parquet files.", l:"advanced", g:["storage","format"],
 b:["Delta writes an ordered transaction log (`_delta_log`) recording every change. Readers reconstruct the current table state from that log, giving ACID guarantees, time travel by version or timestamp, and efficient `MERGE` for upserts and change data capture.",
    "It is the native format on Databricks and integrates tightly with Spark. Functionally it overlaps heavily with Iceberg; the choice usually follows the platform you are already on."],
 k:["An ordered JSON transaction log over Parquet data files.",
    "Time travel by version number or timestamp.",
    "`MERGE INTO` makes CDC and upserts straightforward.",
    "`OPTIMIZE` and `VACUUM` compact small files and clear old versions."],
 r:["Lakehouse","Apache Iceberg","Parquet","Apache Spark","Change Data Capture"]},

{t:"Parquet", d:"A columnar file format that compresses well and lets queries read only the columns they need.", l:"core", g:["format","storage"],
 b:["Parquet stores data column by column within row groups, with per-column statistics and encoding. A query touching 3 of 200 columns reads only those three, and predicate pushdown skips entire row groups whose min-max statistics rule them out.",
    "Compression is dramatically better than row formats because a column holds similar values — a column of timestamps or country codes compresses far more than a mixed row. It is the default storage format for analytics, and the physical layer under Delta and Iceberg."],
 k:["Columnar: reads only the columns the query mentions.",
    "Row-group statistics enable predicate pushdown and row-group skipping.",
    "Typically 5–10× smaller than the equivalent CSV.",
    "Self-describing — the schema travels with the file."],
 r:["Columnar Storage","Data Lake","Apache Iceberg","Avro","ORC"]},

{t:"Avro", d:"A row-based binary format with a schema attached, designed for streaming and schema evolution.", l:"intermediate", g:["format","storage"],
 b:["Avro stores whole records together, which suits writing and reading one record at a time — exactly what streaming does. Its schema is defined in JSON and stored with the data (or in a schema registry), and it has well-defined compatibility rules for adding, removing and renaming fields.",
    "The division of labour is conventional: Avro for data in motion through Kafka, Parquet for data at rest in the lake."],
 k:["Row-oriented — good for writes and whole-record reads.",
    "Explicit schema with defined forward and backward compatibility rules.",
    "The standard payload format for Kafka with a schema registry.",
    "Avro in motion, Parquet at rest."],
 r:["Parquet","Apache Kafka","Schema Evolution","Data Contract"]},

{t:"ORC", a:"Optimised Row Columnar", d:"A columnar format from the Hive ecosystem, similar in purpose to Parquet.", l:"advanced", g:["format","storage"],
 b:["ORC stores data in stripes with column-level indexes, statistics and optional bloom filters, giving strong compression and fast predicate pushdown. It also supports ACID operations within Hive.",
    "Parquet and ORC are close in capability; Parquet has broader ecosystem support and is the more common default outside Hive-centric estates."],
 k:["Columnar with stripe-level indexes and optional bloom filters.",
    "Strong compression and predicate pushdown, comparable to Parquet.",
    "Supports ACID transactions inside Hive.",
    "Parquet has wider tool support; choose it unless Hive dictates otherwise."],
 r:["Parquet","Columnar Storage","Hadoop"]},

{t:"Partitioning", d:"Splitting a table into physical segments by a column value, so queries can skip the segments they do not need.", l:"core", g:["performance","storage"],
 b:["Partitioning by date means a query for last week reads seven directories rather than three years of data. This partition pruning is often the difference between a query costing pennies and costing hundreds of pounds on a per-byte-scanned platform.",
    "Choose a column that appears in most `WHERE` clauses and has moderate cardinality. Partitioning by a high-cardinality column such as user id creates millions of tiny files, and the resulting metadata overhead is worse than no partitioning at all."],
 k:["Partition pruning skips whole segments — the biggest scan-cost lever.",
    "Date is the most common and usually the right choice.",
    "High-cardinality partition keys create the small-files problem.",
    "Target partitions of roughly 100 MB to 1 GB."],
 r:["Sharding","Parquet","Data Warehouse","BigQuery","Bucketing"]},

{t:"Bucketing", a:"Clustering", d:"Distributing rows into a fixed number of buckets by hashing a column, so joins and aggregations avoid a shuffle.", l:"advanced", g:["performance","storage"],
 b:["If two tables are bucketed on the same column into the same number of buckets, matching rows are guaranteed to be in corresponding buckets, so a join can proceed bucket by bucket without moving data across the network. On large joins this removes the dominant cost.",
    "Unlike partitioning, bucketing suits high-cardinality columns such as user id. The bucket count is fixed at write time and changing it requires rewriting the table."],
 k:["Hash-based distribution into a fixed number of buckets.",
    "Co-bucketed tables join without a shuffle.",
    "Suits high-cardinality keys where partitioning would fail.",
    "Bucket count is fixed at write time — choose carefully."],
 r:["Partitioning","Apache Spark","Data Skew","Join"]},

{t:"Change Data Capture", a:"CDC", d:"Streaming every insert, update and delete out of a database as it happens, by reading the transaction log.", l:"intermediate", g:["pipeline","integration"],
 b:["Rather than polling a table for changed rows — which misses deletes, misses intermediate states and puts load on the source — CDC reads the database's own write-ahead log. Every committed change becomes an event, in order, with before and after images.",
    "Debezium is the standard open-source implementation, typically publishing to Kafka. CDC is how operational databases feed warehouses, search indexes and caches in near real time without dual writes."],
 k:["Reads the transaction log, so nothing is missed — including deletes.",
    "Negligible additional load on the source database.",
    "Debezium plus Kafka is the common stack.",
    "Handle schema changes explicitly; they appear in the stream too."],
 r:["Apache Kafka","Data Pipeline","Replication","Stream Processing","Delta Lake"]},

{t:"Data Quality", d:"The degree to which data is accurate, complete, consistent, timely and fit for its purpose.", l:"core", g:["governance","reliability"],
 b:["Quality is checked along defined dimensions: completeness (are required fields populated), uniqueness (are there duplicates), validity (do values match the expected domain), consistency (do related tables agree), timeliness (did it arrive on schedule) and accuracy (does it reflect reality).",
    "The engineering practice is to encode these as automated tests that run with every pipeline execution and fail loudly. dbt tests, Great Expectations and Soda all do this. Quality problems found by a business user in a dashboard have already cost you trust."],
 k:["Six dimensions: completeness, uniqueness, validity, consistency, timeliness, accuracy.",
    "Automate the checks and run them on every pipeline execution.",
    "Fail the pipeline on critical violations rather than publishing bad data.",
    "Trust is lost far faster than it is rebuilt."],
 r:["Data Contract","Data Lineage","dbt","Data Governance","Data Observability"]},

{t:"Data Lineage", d:"A traceable record of where each dataset came from and what transformations produced it.", l:"intermediate", g:["governance","observability"],
 b:["Lineage answers two questions that come up constantly: if this source is wrong, what downstream reports are affected, and where did this number in the dashboard actually come from. Column-level lineage answers them precisely rather than at table granularity.",
    "It is generated automatically by tools that parse SQL and orchestration graphs — dbt, OpenLineage, and most data catalogues. Manually maintained lineage documentation is always out of date."],
 k:["Impact analysis downstream, root-cause analysis upstream.",
    "Column-level lineage is far more useful than table-level.",
    "Generate it from code; hand-maintained diagrams go stale immediately.",
    "Increasingly a regulatory requirement, not just an engineering convenience."],
 r:["Data Governance","dbt","Data Catalog","Data Quality"]},

{t:"Data Governance", d:"The policies and controls defining who may access which data, how it is classified, and how long it is kept.", l:"intermediate", g:["governance","compliance"],
 b:["Governance covers ownership (who is accountable for this dataset), classification (is it public, internal, confidential, or personal data), access control, retention and deletion, and audit. GDPR, CCPA, HIPAA and sector rules turn much of this from good practice into legal obligation.",
    "Effective governance is enforced by systems rather than documents: access granted through roles, PII tagged in the catalogue, retention implemented as automated deletion jobs, and every access logged."],
 k:["Ownership, classification, access, retention, audit.",
    "Right to erasure means you must be able to find every copy of a person's data.",
    "Enforce in systems; policy documents alone change nothing.",
    "Classify data at ingestion, not later — later never happens."],
 r:["Data Lineage","Data Catalog","GDPR","Role-Based Access Control","Data Quality"]},

{t:"Data Catalog", d:"A searchable inventory of an organisation's datasets, with schemas, owners, descriptions and lineage.", l:"intermediate", g:["governance","tooling"],
 b:["Once an organisation has thousands of tables, discovery becomes the bottleneck: analysts rebuild datasets that already exist because they could not find them. A catalogue makes datasets searchable and records what each one means, who owns it, how fresh it is and what it depends on.",
    "Catalogues that rely on humans to write documentation decay. The ones that work harvest metadata automatically from warehouses, orchestrators and dbt, and use usage statistics to surface what people actually rely on."],
 k:["Solves discovery — the main bottleneck in a large data estate.",
    "Harvest metadata automatically; manual documentation rots.",
    "Usage statistics reveal which datasets genuinely matter.",
    "DataHub, Amundsen, OpenMetadata and Unity Catalog are common choices."],
 r:["Data Governance","Data Lineage","Data Lake","Data Quality"]},

{t:"Data Contract", d:"An explicit, enforced agreement between a data producer and its consumers about schema, semantics and guarantees.", l:"advanced", g:["governance","reliability"],
 b:["Most pipeline breakages come from upstream changes nobody announced: a column renamed, a type changed, an enum extended. A data contract makes the interface explicit — schema, field meanings, freshness SLA, allowed values — and enforces it in the producer's CI, so a breaking change fails the producer's build rather than the consumer's pipeline.",
    "It applies the API versioning discipline that service teams already use to data, and shifts responsibility for compatibility to where the change is made."],
 k:["Makes the producer-consumer interface explicit and testable.",
    "Enforced in the producer's CI — breaking changes fail their build.",
    "Covers schema, semantics, freshness and quality guarantees.",
    "Versioned like an API, with deprecation periods."],
 r:["Schema Evolution","Data Quality","API","Data Governance","Avro"]},

{t:"Schema Evolution", d:"Changing a dataset's structure over time without breaking existing readers.", l:"intermediate", g:["governance","format"],
 b:["Backward compatible changes let new readers read old data (adding an optional field). Forward compatible changes let old readers read new data (which requires readers to ignore unknown fields). Full compatibility means both.",
    "Adding optional fields is safe. Removing a field, renaming one, or narrowing a type is breaking. Avro, Protobuf and Iceberg all define explicit compatibility rules; a schema registry can enforce them at write time."],
 k:["Adding an optional field is safe; removing or renaming is breaking.",
    "Backward, forward and full compatibility are distinct guarantees.",
    "A schema registry rejects incompatible changes before they ship.",
    "Iceberg tracks columns by id, so renames do not break existing data."],
 r:["Data Contract","Avro","Apache Iceberg","Database Migration"]},

{t:"Star Schema", d:"A dimensional model with one central fact table surrounded by denormalised dimension tables.", l:"core", g:["modelling","warehouse"],
 b:["The fact table holds measurements — one row per sale, click or event — with foreign keys to dimensions and numeric measures. Dimensions hold descriptive attributes: product, customer, store, date. Every analytical query is a shallow join from the fact to a few dimensions.",
    "Dimensions are deliberately denormalised, which violates third normal form on purpose. The redundancy costs storage, which is cheap, and saves joins, which are not. It remains the dominant warehouse modelling pattern."],
 k:["One fact table, many denormalised dimensions, shallow predictable joins.",
    "Deliberately denormalised — storage is cheaper than join complexity.",
    "Every fact table needs a date dimension.",
    "Grain — what one fact row represents — is the first decision to make."],
 dg:"star-schema",
 r:["Snowflake Schema","Fact Table","Dimension Table","Data Warehouse","Denormalisation"]},

{t:"Snowflake Schema", d:"A star schema whose dimensions are normalised into multiple related tables.", l:"intermediate", g:["modelling","warehouse"],
 b:["Instead of one wide product dimension containing category and supplier names, a snowflake schema splits those into their own tables. Storage drops and updates become simpler, but queries now require more joins and are harder to read.",
    "In modern columnar warehouses, where storage is cheap and wide tables compress well, star schemas are usually preferred. Snowflaking is worth it for very large dimensions or where a dimension attribute changes frequently."],
 k:["Dimensions normalised into sub-tables, adding join depth.",
    "Saves storage; costs query simplicity and performance.",
    "Star is the default in modern columnar warehouses.",
    "Named for the shape of its entity diagram, not the vendor."],
 r:["Star Schema","Normalisation","Dimension Table","Data Warehouse"]},

{t:"Fact Table", d:"The central table in a dimensional model, holding measurable events with references to dimensions.", l:"intermediate", g:["modelling","warehouse"],
 b:["Fact tables are narrow and very long: foreign keys plus numeric measures, one row per business event. The grain — precisely what one row represents — must be decided first and stated explicitly, because every measure and every join depends on it.",
    "Three types recur: transaction facts (one row per event), periodic snapshots (one row per entity per period), and accumulating snapshots (one row per process instance, updated as it progresses)."],
 k:["Define the grain first and write it down — everything depends on it.",
    "Narrow and long: keys plus numeric measures.",
    "Additive measures sum across all dimensions; semi-additive ones do not.",
    "Transaction, periodic snapshot and accumulating snapshot are the three shapes."],
 dg:"star-schema",
 r:["Star Schema","Dimension Table","Data Warehouse","OLAP"]},

{t:"Dimension Table", d:"A table of descriptive attributes used to filter, group and label facts.", l:"intermediate", g:["modelling","warehouse"],
 b:["Dimensions answer who, what, where and when: customer, product, store, date. They are wide (many attributes) and comparatively short (thousands to millions of rows), and they supply every label a report displays and every axis a user slices by.",
    "The date dimension deserves special mention: precomputing fiscal periods, holidays, week numbers and day names into a table removes an enormous amount of repeated date arithmetic from queries."],
 k:["Wide and short — many descriptive attributes, relatively few rows.",
    "Supplies the labels and grouping keys every report uses.",
    "A precomputed date dimension pays for itself immediately.",
    "Slowly changing dimension patterns handle attributes that change over time."],
 dg:"star-schema",
 r:["Fact Table","Star Schema","Slowly Changing Dimension","Data Warehouse"]},

{t:"Slowly Changing Dimension", a:"SCD", d:"A set of patterns for handling dimension attributes that change over time.", l:"advanced", g:["modelling","warehouse"],
 b:["A customer moves city. Type 1 overwrites the old value, losing history — simple, and it silently rewrites past reports. Type 2 adds a new row with validity dates and a current flag, preserving history so historical facts still join to the attribute values that were true at the time. Type 3 keeps a previous-value column.",
    "Type 2 is the standard choice when history matters, which in finance, compliance and cohort analysis it almost always does. It also makes the dimension grow, so surrogate keys become essential."],
 k:["Type 1 overwrites; Type 2 versions rows with validity dates; Type 3 keeps one prior value.",
    "Type 2 is the default when historical accuracy matters.",
    "Type 2 requires surrogate keys — natural keys are no longer unique.",
    "Type 1 silently changes what past reports say."],
 r:["Dimension Table","Star Schema","Data Warehouse","Primary Key"]},

{t:"Medallion Architecture", d:"Organising a lakehouse into bronze, silver and gold layers of progressively refined data.", l:"intermediate", g:["architecture","pattern"],
 b:["Bronze holds raw ingested data exactly as it arrived, append-only, so anything can be reprocessed. Silver holds cleaned, deduplicated, conformed data with types and quality checks applied. Gold holds business-level aggregates and marts shaped for consumption.",
    "The value is that each layer has one clear responsibility and reprocessing is always possible from bronze. It is essentially ELT expressed as a directory convention, and it makes lineage and ownership obvious."],
 k:["Bronze raw, silver cleaned, gold aggregated for consumption.",
    "Bronze is append-only and immutable — the replay source of truth.",
    "Quality checks belong at the bronze-to-silver boundary.",
    "Makes lineage and layer ownership explicit."],
 r:["Lakehouse","ELT","Data Quality","Data Lake","Delta Lake"]},

{t:"Data Mesh", d:"An organisational approach that gives domain teams ownership of their own data products instead of centralising everything.", l:"advanced", g:["architecture","organisation"],
 b:["Data mesh rests on four principles: domain ownership (the team that produces the data owns it), data as a product (with an owner, SLA, documentation and consumers), self-serve platform (central infrastructure, not central pipelines), and federated governance (global standards, local implementation).",
    "It addresses a real bottleneck — a central data team that becomes the constraint on every domain. It also demands genuine data engineering capability inside every domain team, which many organisations do not have, and it is frequently adopted as a label rather than a practice."],
 k:["Four principles: domain ownership, data as a product, self-serve platform, federated governance.",
    "Solves the central-team bottleneck in large organisations.",
    "Requires data engineering skill distributed across every domain.",
    "An organisational change first, a technical one second."],
 r:["Data Governance","Data Contract","Data Catalog","Microservices"]},

{t:"Backfill", d:"Reprocessing historical periods after fixing a bug or adding a new field.", l:"core", g:["operations","pipeline"],
 b:["When transformation logic changes, existing outputs are wrong. A backfill reruns the pipeline over past date partitions to regenerate them. This only works if the pipeline is idempotent and partitioned by time — otherwise a rerun duplicates rows or produces different results.",
    "Practical concerns: throttle concurrency so the backfill does not starve production jobs, do it in date order so partial completion is coherent, and validate a sample before committing to a three-year run."],
 k:["Only safe when the pipeline is genuinely idempotent.",
    "Requires time-partitioned outputs so periods can be replaced independently.",
    "Limit concurrency — backfills can starve scheduled production runs.",
    "Validate one partition before running the full range."],
 r:["Idempotency","Apache Airflow","Data Pipeline","Partitioning"]},

{t:"Data Skew", d:"When data distributes unevenly across partitions, so one task does far more work than the rest.", l:"advanced", g:["performance","distributed"],
 b:["Group by customer id where one customer accounts for 40% of rows, and one Spark task processes 40% of the data while the others idle. The job's runtime is set by that single straggler, and adding executors changes nothing.",
    "The signature is a long tail in the task duration distribution. Fixes include salting the key (append a random suffix, aggregate twice), broadcasting the small side of a skewed join, or separating the known hot keys and handling them differently. Spark's adaptive query execution now detects and splits many skewed partitions automatically."],
 k:["Runtime is set by the slowest task, so more executors do not help.",
    "Diagnose from the task duration distribution, not the average.",
    "Salting spreads a hot key across artificial sub-keys.",
    "Broadcast joins avoid shuffling the skewed side entirely."],
 r:["Apache Spark","Partitioning","MapReduce","Bucketing"]},

{t:"Windowing", d:"Grouping unbounded stream events into finite chunks so aggregates can be computed.", l:"advanced", g:["streaming","technique"],
 b:["You cannot compute an average over an infinite stream, so streams are cut into windows. Tumbling windows are fixed and non-overlapping; sliding windows overlap; session windows group activity separated by gaps of inactivity.",
    "The critical distinction is event time versus processing time. Event time — when the event actually happened — is what business logic needs, and it requires watermarks to decide when a window is complete enough to emit, given that some events always arrive late."],
 k:["Tumbling, sliding and session windows suit different questions.",
    "Event time is what matters; processing time is what is convenient.",
    "Watermarks decide when a window can be closed despite late data.",
    "Allowed lateness trades result completeness against latency."],
 r:["Stream Processing","Apache Flink","Apache Kafka","Exactly-Once Semantics"]},

{t:"Exactly-Once Semantics", d:"A guarantee that each event affects the final result exactly once, despite failures and retries.", l:"advanced", g:["streaming","reliability"],
 b:["At-most-once may lose data; at-least-once may duplicate it; exactly-once does neither. It is achieved not by delivering exactly once — networks make that impossible — but by making processing effectively once, through idempotent writes, transactional sinks, or coordinated checkpoints with source offsets.",
    "Kafka provides transactions and idempotent producers; Flink coordinates checkpoints with sources and sinks. The guarantee holds only within systems that participate; an external HTTP call from inside a stream job is not covered."],
 k:["Achieved through idempotency and transactions, not through delivery guarantees.",
    "At-least-once plus idempotent writes is often the simpler equivalent.",
    "Only holds across participating systems — external side effects are not covered.",
    "Costs throughput; confirm you actually need it."],
 r:["Idempotency","Apache Kafka","Apache Flink","Stream Processing"]},

{t:"Data Observability", d:"Monitoring the health of data itself — freshness, volume, schema, distribution and lineage.", l:"intermediate", g:["operations","reliability"],
 b:["Application monitoring tells you the pipeline ran. Data observability tells you whether the output is sensible: did the row count drop 90% overnight, did a column become 40% null, did the average order value triple, did the schema change without warning.",
    "The five pillars are freshness, volume, schema, distribution and lineage. Automated anomaly detection on these catches problems before a business user does — which is the entire point, since the alternative is losing trust in the data."],
 k:["Five pillars: freshness, volume, schema, distribution, lineage.",
    "Pipeline succeeded does not mean the data is correct.",
    "Alert on distribution shifts, not only on hard failures.",
    "Catching it before a stakeholder does is what preserves trust."],
 r:["Data Quality","Data Lineage","Monitoring","Model Drift"]},

{t:"Reverse ETL", d:"Pushing modelled data from the warehouse back into operational tools where teams work.", l:"intermediate", g:["pipeline","pattern"],
 b:["The warehouse ends up holding the best version of customer data — the unified profile, the health score, the churn prediction. Reverse ETL syncs that back into Salesforce, HubSpot, Zendesk or an ad platform so the people who need it see it where they work.",
    "It closes the loop between analytics and operations. The concerns are rate limits on destination APIs, incremental syncing so you do not push everything every time, and making sure the warehouse remains the acknowledged source of truth."],
 k:["Warehouse to operational tools — the opposite direction to ETL.",
    "Puts modelled data where business teams actually work.",
    "Destination API rate limits are the main engineering constraint.",
    "Sync incrementally, and keep the warehouse as the source of truth."],
 r:["ETL","Data Warehouse","Data Pipeline","API"]},

{t:"Object Storage", d:"Storage that holds data as immutable objects in a flat namespace, accessed over HTTP.", l:"core", g:["storage","cloud"],
 b:["S3, Google Cloud Storage and Azure Blob Storage store objects with a key, data and metadata. There are no real directories — prefixes just look like them — and objects are replaced wholesale rather than modified in place. Durability is extremely high and cost per terabyte is very low.",
    "It is the foundation of every modern data lake. Its characteristics shape lakehouse design: listing many objects is slow, small files are inefficient, and eventual consistency on some operations historically required careful handling."],
 k:["Flat key namespace; directory structure is a naming convention only.",
    "Objects are immutable — updates replace the whole object.",
    "Extremely cheap and durable; latency is higher than block storage.",
    "Small files are expensive in listing and request overhead — compact them."],
 r:["Data Lake","HDFS","Parquet","Lakehouse"]},

{t:"Data Versioning", d:"Tracking versions of datasets so an experiment or pipeline run can be reproduced exactly.", l:"intermediate", g:["reproducibility","tooling"],
 b:["Code versioning is universal; data versioning often is not, which makes results unreproducible — the model trained last month cannot be recreated because the training data has since changed. DVC stores lightweight pointers in git while data lives in object storage; lakehouse table formats give time travel by version or timestamp.",
    "The minimum viable practice is to record, with every model or report, the exact dataset version or snapshot identifier that produced it."],
 k:["Without it, results cannot be reproduced once the source data changes.",
    "DVC keeps pointers in git; the data stays in object storage.",
    "Delta and Iceberg give time travel natively.",
    "Record the dataset version alongside every model artefact."],
 r:["Reproducibility","Delta Lake","Apache Iceberg","Experiment Tracking"]}

]);
