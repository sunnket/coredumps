/* Retrieval, agents, evaluation, serving and safety — the vocabulary a GenAI
   system-design round is drawn from. */

TD.add("genai-llm", [

/* ---------- retrieval ---------- */

{t:"HNSW", a:"Hierarchical Navigable Small World", d:"The dominant vector index: a layered graph you traverse greedily to find near neighbours fast.", l:"advanced", g:["rag","retrieval"],
 b:["Vectors become nodes in a multi-layer graph. The sparse top layers act as express lanes for long jumps; the dense bottom layer refines the result. A search starts at the top, greedily moves to whichever neighbour is closer to the query, then drops a layer and repeats.",
    "It is approximate by construction, and `ef_search` is the dial: raising it explores more candidates, improving recall and costing latency. That dial is the reason to measure recall@k on your own data rather than assuming the index is exact — an assumption that quietly loses answers in production RAG."],
 k:["Multi-layer proximity graph; greedy descent from sparse to dense layers.",
    "Fast and accurate, at a higher memory cost than IVFFlat.",
    "`ef_search` trades recall against latency — measure it, do not assume it.",
    "Approximate, not exact: some true neighbours will be missed."],
 x:{lang:"sql", code:"CREATE INDEX ON docs USING hnsw (embedding vector_cosine_ops)\n  WITH (m = 16, ef_construction = 64);\n\nSET hnsw.ef_search = 100;   -- higher: better recall, slower"},
 r:["IVFFlat","Approximate Nearest Neighbour","Vector Database","Embedding"]},

{t:"IVFFlat", a:"Inverted File Index", d:"A vector index that clusters vectors and searches only the nearest few clusters.", l:"advanced", g:["rag","retrieval"],
 b:["Partition the vectors into a set of clusters with k-means, then at query time compare against only the handful of cluster centroids closest to the query and search inside those. The `probes` parameter controls how many clusters are examined.",
    "It uses far less memory than HNSW and builds faster, but it must be trained on representative data first — an index built on an empty or unrepresentative table has meaningless clusters. Recall also degrades badly if the relevant vector happens to sit in an unprobed cluster, which is the trade-off against HNSW's graph traversal."],
 k:["k-means clusters; search only the `probes` nearest ones.",
    "Lower memory and faster to build than HNSW; generally lower recall.",
    "**Must be trained on real data** — building it on an empty table is useless.",
    "More probes means better recall and slower queries."],
 x:{lang:"sql", code:"CREATE INDEX ON docs USING ivfflat (embedding vector_cosine_ops)\n  WITH (lists = 100);          -- build AFTER loading real data\n\nSET ivfflat.probes = 10;        -- more probes: better recall, slower"},
 r:["HNSW","Approximate Nearest Neighbour","Vector Database","K-Means Clustering"]},

{t:"BM25", d:"The classic keyword ranking function, still the sparse half of every good hybrid search.", l:"intermediate", g:["rag","retrieval"],
 b:["It scores a document by how often the query's terms appear in it, discounted by how common those terms are across the whole collection, and normalised so long documents do not win merely by being long. It is decades old and it has not been superseded.",
    "The reason it survives alongside embeddings is that the two fail differently. Vector search understands that *car* and *automobile* are related but is weak on exact tokens — product codes, error numbers, rare names. BM25 is the opposite. Running both and fusing the results is why hybrid search beats either alone."],
 k:["Term frequency, discounted by document frequency, normalised by length.",
    "Excellent on exact tokens: identifiers, error codes, rare names, acronyms.",
    "Blind to synonyms — which is exactly where dense retrieval helps.",
    "Combine with vector search via reciprocal rank fusion."],
 x:{lang:"sql", code:"-- Postgres full-text search is BM25-like\nSELECT id, ts_rank_cd(tsv, query) AS score\nFROM docs, plainto_tsquery('english', $1) query\nWHERE tsv @@ query\nORDER BY score DESC LIMIT 50;"},
 r:["Hybrid Search","Reciprocal Rank Fusion","Full-Text Search","Embedding"]},

{t:"Reciprocal Rank Fusion", a:"RRF", d:"Merging several ranked lists by summing 1/(k + rank), with no need to calibrate their scores.", l:"advanced", g:["rag","retrieval"],
 b:["Dense and sparse retrievers produce scores on completely different scales — a cosine similarity and a BM25 score are not comparable, and normalising them is fiddly and data-dependent. RRF sidesteps the problem entirely by discarding the scores and using only the ranks.",
    "Each document scores the sum over all lists of 1/(k + rank), with k conventionally 60. A document ranked highly by both retrievers rises to the top; the constant k damps the influence of the very top positions so a single list cannot dominate. It is simple, parameter-light and remarkably hard to beat."],
 k:["Uses ranks only, so no score calibration between systems is needed.",
    "`score(d) = Σ 1/(k + rank_i(d))`, with k ≈ 60 by convention.",
    "k damps the top ranks so one list cannot dominate the fusion.",
    "The standard way to combine BM25 with vector search."],
 x:{lang:"python", code:"def rrf(rankings, k=60):\n    scores = {}\n    for ranked in rankings:            # one list per retriever\n        for rank, doc in enumerate(ranked, 1):\n            scores[doc] = scores.get(doc, 0) + 1 / (k + rank)\n    return sorted(scores, key=scores.get, reverse=True)"},
 r:["Hybrid Search","BM25","Reranking","Vector Database"]},

{t:"Bi-Encoder", d:"An embedding model that encodes the query and each document separately, so document vectors can be precomputed.", l:"advanced", g:["rag","retrieval"],
 b:["Because the two texts never meet inside the model, every document can be embedded once, offline, and stored in a vector index. A query is then embedded once and compared against millions of stored vectors with a cheap distance calculation.",
    "That independence is both the strength and the weakness. It is what makes search over millions of documents possible at all, and it means the model can never consider the query and document jointly — so the ranking is coarse. The standard fix is to retrieve widely with a bi-encoder and then re-rank the shortlist with a cross-encoder."],
 k:["Query and document are encoded independently — vectors are precomputable.",
    "Fast enough to search millions of documents; that is the whole point.",
    "Coarser than a cross-encoder because the two texts never interact.",
    "Retrieve wide with this, then rerank narrow with a cross-encoder."],
 x:{lang:"python", code:"# offline, once per document\ndoc_vecs = model.encode(documents)      # stored in the vector DB\n\n# online, per query\nq = model.encode(query)                 # one encode\nhits = index.search(q, k=50)            # then a cheap distance"},
 r:["Cross-Encoder","Embedding","Vector Database","Reranking"]},

{t:"Cross-Encoder", d:"A model that reads the query and document together, giving a far more accurate relevance score — and a much slower one.", l:"advanced", g:["rag","retrieval"],
 b:["Both texts go into the model as one input, so attention runs across the pair and the model can weigh how the query's specific terms relate to the document's. Nothing can be precomputed, because the score exists only for that particular pairing.",
    "That makes it far too slow to run over a whole corpus and ideal for re-ranking a shortlist. Retrieving 50 candidates cheaply and re-ranking them to the best 5 typically produces the single largest quality jump available in a RAG system."],
 k:["Query and document are encoded jointly, so attention spans both.",
    "Much more accurate, and impossible to precompute.",
    "Used to re-rank a shortlist of ~50, never to search the whole corpus.",
    "Usually the biggest single quality gain in a RAG pipeline."],
 x:{lang:"python", code:"from sentence_transformers import CrossEncoder\n\nranker = CrossEncoder(\"cross-encoder/ms-marco-MiniLM-L-6-v2\")\npairs  = [(query, d.text) for d in candidates]     # 50 pairs\nscores = ranker.predict(pairs)\ntop5   = [d for _, d in sorted(zip(scores, candidates), reverse=True)[:5]]"},
 r:["Bi-Encoder","Reranking","Embedding","Hybrid Search"]},

{t:"HyDE", a:"Hypothetical Document Embeddings", d:"Asking the model to invent a plausible answer, then searching with that answer's embedding instead of the question's.", l:"advanced", g:["rag","retrieval"],
 b:["Questions and documents look different in embedding space: a question is short and interrogative, a document is a declarative passage. Searching with the question's vector therefore compares two different kinds of text. HyDE has the model hallucinate an answer first, then embeds that — because a fake answer resembles a real document far more closely than a question does.",
    "The hallucination not being factually correct is fine, and initially counterintuitive. It only needs the right *shape* and vocabulary to land near genuine answers in embedding space. The cost is an extra LLM call before every search, which is real latency."],
 k:["Embed a hypothetical answer rather than the question — shapes match better.",
    "The invented answer need not be true; only its vocabulary and form matter.",
    "Strong gains on short or vague queries, and on zero-shot retrieval.",
    "Costs one extra LLM call of latency per search."],
 x:{lang:"python", code:"hypo = llm(f\"Write a short passage answering: {query}\")\nvec  = embed(hypo)          # embed the ANSWER, not the question\nhits = index.search(vec, k=50)\n# hypo is thrown away -- it was only ever a search key"},
 r:["Query Rewriting","Embedding","Retrieval-Augmented Generation","Bi-Encoder"]},

{t:"Query Rewriting", d:"Using an LLM to turn a user's raw question into a better search query before retrieval.", l:"intermediate", g:["rag","retrieval"],
 b:["*What about the second one?* is meaningless as a standalone search query. In a conversation, resolving pronouns against the history, expanding acronyms and splitting compound questions into separate searches all materially improve what comes back.",
    "It is close to mandatory for conversational RAG, where most turns depend on earlier ones. The trade-off is an extra LLM call before every retrieval; a common optimisation is to rewrite only when the query contains a pronoun or is very short."],
 k:["Resolves pronouns against the conversation, expands acronyms, splits compounds.",
    "Effectively required for multi-turn RAG — follow-ups are not standalone queries.",
    "Costs one extra LLM call before retrieval.",
    "Rewriting only short or pronoun-bearing queries is a cheap optimisation."],
 x:{lang:"text", code:"history: \"Compare the Pro and Max plans.\"\nuser:    \"what about the second one?\"\n\nrewritten: \"What are the features and price of the Max plan?\"\n            ^ now it is actually searchable"},
 r:["HyDE","Retrieval-Augmented Generation","Multi-Hop","Agentic RAG"]},

{t:"Multi-Hop", d:"Answering a question that needs several rounds of retrieval, where each answer supplies the next query.", l:"advanced", g:["rag","retrieval"],
 b:["*Who was the CEO when the merger closed?* cannot be answered by one search: you must first find when the merger closed, then search for who was CEO at that date. No single chunk contains both facts, so single-shot retrieval fails however good the index is.",
    "The system retrieves, reasons about what it now knows and what is still missing, then retrieves again. It needs a hop limit and a check for whether the question is even answerable, because a model that never concludes it lacks the information will keep searching until the budget runs out."],
 k:["Some questions need chained facts that no single chunk holds.",
    "Retrieve → reason → retrieve again, using what you learned as the next query.",
    "Cap the hops, or it retrieves until the budget is exhausted.",
    "The failure to detect is a confident answer built from only the first hop."],
 x:{lang:"text", code:"Q: \"Who was CEO when the merger closed?\"\n\nhop 1: \"when did the merger close?\"     -> March 2019\nhop 2: \"who was CEO in March 2019?\"     -> the answer\n\none-shot retrieval finds neither chunk useful on its own"},
 r:["Agentic RAG","Query Rewriting","Retrieval-Augmented Generation","AI Agent"]},

{t:"Contextual Retrieval", d:"Prepending a short LLM-written description of where each chunk sits in its document, before embedding it.", l:"advanced", g:["rag","retrieval"],
 b:["A chunk reading *the limit was raised to 50* is nearly useless in isolation — which limit, in which product, in which year? Contextual retrieval asks a model to write a sentence of surrounding context for each chunk at indexing time, and embeds the chunk together with that sentence.",
    "Anthropic reported that combining contextual embeddings with contextual BM25 cut top-20 retrieval failures by 49% (from 5.7% to 2.9%), and by 67% when reranking was added on top; contextual embeddings alone gave 35%. It costs one LLM call per chunk at indexing time, which prompt caching makes affordable."],
 k:["An LLM writes a context sentence per chunk before embedding it.",
    "Reported 49% fewer retrieval failures with contextual BM25; 67% with reranking added.",
    "Cost is one indexing-time LLM call per chunk — prefix caching makes it viable.",
    "Directly attacks the *chunk makes no sense alone* failure mode."],
 x:{lang:"text", code:"raw chunk:\n  \"The limit was raised to 50 per minute.\"\n\ncontextualised:\n  \"From the Enterprise API rate-limit policy (2024 revision),\n   describing paid tiers: The limit was raised to 50 per minute.\"\n     ^ prepended before embedding AND before BM25 indexing"},
 r:["Chunking","Chunk Overlap","Embedding","BM25"]},

{t:"Metadata Filtering", d:"Restricting a vector search by structured attributes — date, source, tenant, permissions — alongside the similarity match.", l:"intermediate", g:["rag","retrieval"],
 b:["Semantic similarity alone ignores whether the reader is allowed to see a document, whether it is current, or which customer it belongs to. Filtering on stored metadata during retrieval handles all three.",
    "This is also the correct answer to *how do you enforce access control in RAG?* — filter by the user's permissions at retrieval time, so unauthorised documents never enter the context. Instructing the model not to reveal them is not a control; it is a suggestion, and prompt injection defeats it."],
 k:["Filter on date, source, tenant or permissions during the vector search.",
    "**Access control belongs at retrieval time**, never in the prompt.",
    "A pre-filter can wreck ANN recall; post-filtering can return too few results.",
    "pgvector and Qdrant handle combined filtering well; some stores do it poorly."],
 x:{lang:"sql", code:"SELECT id, text\nFROM docs\nWHERE tenant_id = $1                    -- isolation\n  AND $2 = ANY(allowed_roles)           -- permissions\n  AND updated_at > now() - interval '1 year'\nORDER BY embedding <=> $3\nLIMIT 20;"},
 r:["Vector Database","Retrieval-Augmented Generation","Least Privilege","HNSW"]},

{t:"GraphRAG", d:"Retrieval over a knowledge graph of entities and relationships rather than over isolated text chunks.", l:"advanced", g:["rag","retrieval"],
 b:["Standard RAG retrieves the chunks most similar to a query, which works well for questions whose answer sits in one passage. It fails on global questions — *what are the main themes across all these documents?* — because no individual chunk contains the answer.",
    "GraphRAG extracts entities and relationships during indexing, clusters them into communities, and summarises each community. Retrieval then returns subgraphs or community summaries. Indexing is expensive, since it needs LLM calls over the whole corpus, so it is worth it only when global questions genuinely matter."],
 k:["Indexes entities and relationships, not just text chunks.",
    "Answers global questions that no single chunk can support.",
    "Indexing is expensive — LLM extraction across the entire corpus.",
    "Overkill when the questions are all local and specific."],
 x:{lang:"text", code:"chunk RAG      \"what is the refund window?\"       one chunk answers it\nGraphRAG       \"what themes recur across these\n                200 support tickets?\"             no chunk answers it\n\nindex: entities + relations -> communities -> summaries"},
 r:["Retrieval-Augmented Generation","Knowledge Graph","RAPTOR","Agentic RAG"]},

{t:"Agentic RAG", d:"Letting an agent decide whether to retrieve, from which source, and whether the results were good enough.", l:"advanced", g:["rag","agents"],
 b:["A fixed RAG pipeline always retrieves, always from the same index, and always trusts what comes back. Agentic RAG puts those decisions in the model's hands: it may skip retrieval for a question it can answer directly, choose between a document store and a SQL database, judge the retrieved passages, and search again with a better query if they were poor.",
    "It suits heterogeneous sources and mixed question types, and it costs more latency and money per query while being harder to evaluate — the same input can now take different paths. Stop conditions matter as much as they do for any agent."],
 k:["The model decides whether to retrieve, from where, and whether to retry.",
    "Good for heterogeneous sources and questions that vary in kind.",
    "More latency and cost; harder to evaluate because paths vary per query.",
    "Needs the usual agent stop conditions — cap the retrieval loops."],
 x:{lang:"text", code:"question -> does this need retrieval at all?\n              no  -> answer directly\n              yes -> which source? docs / SQL / web\n                     retrieve -> are these good enough?\n                       no  -> rewrite the query, retry (bounded)\n                       yes -> answer with citations"},
 r:["AI Agent","Retrieval-Augmented Generation","Multi-Hop","Query Rewriting"]},

{t:"RAPTOR", d:"Recursively clustering and summarising chunks into a tree, so retrieval can happen at several levels of abstraction.", l:"advanced", g:["rag","retrieval"],
 b:["Embed the leaf chunks, cluster them, and have an LLM summarise each cluster. Embed those summaries, cluster them again, and repeat — producing a tree whose leaves are detail and whose upper nodes are progressively broader overviews.",
    "Retrieval can then match a specific question against a leaf or a broad question against a high-level summary. It sits between plain chunk RAG and GraphRAG: cheaper to index than a knowledge graph, and better than flat chunks on questions that need an overview of a long document."],
 k:["Recursive clustering plus summarisation builds a tree of abstraction levels.",
    "Detailed questions match leaves; broad questions match upper summaries.",
    "Cheaper to index than GraphRAG, more capable than flat chunking.",
    "Best for long documents needing both detail and overview."],
 x:{lang:"text", code:"level 2        [ whole-document summary ]\nlevel 1    [ section summary ] [ section summary ]\nleaves   [chunk][chunk][chunk] [chunk][chunk][chunk]\n\nsearch across ALL levels; specific queries hit leaves,\nbroad ones hit the summaries"},
 r:["GraphRAG","Chunking","Retrieval-Augmented Generation","Embedding"]},

{t:"RAGAS", d:"The standard library for measuring a RAG system, with metrics that separate retrieval failures from generation failures.", l:"intermediate", g:["rag","evaluation"],
 b:["It computes context recall and context precision to score retrieval, and faithfulness and answer relevance to score generation. Most of the metrics are LLM-judged, so they need a capable judge model and, ideally, validation against human labels.",
    "Its real value is diagnostic rather than the scores themselves. Low context recall points at chunking, the embedding model or the lack of hybrid search; high recall with low faithfulness means retrieval is fine and the model is ignoring what it was given. Separating those two cases is the core skill in debugging RAG."],
 k:["Context recall and precision score retrieval; faithfulness and relevance score generation.",
    "Mostly LLM-judged — validate the judge before trusting the numbers.",
    "Low recall → fix retrieval. High recall, low faithfulness → fix the prompt.",
    "Alternatives: ARES, RAGChecker, DeepEval."],
 x:{lang:"python", code:"from ragas import evaluate\nfrom ragas.metrics import faithfulness, context_recall, context_precision\n\nreport = evaluate(dataset, metrics=[faithfulness, context_recall, context_precision])\n# run it in CI and fail the build on regression"},
 r:["Context Recall","Context Precision","Faithfulness","Evaluation"]},

{t:"Context Recall", d:"Whether retrieval actually returned all the information needed to answer the question.", l:"intermediate", g:["rag","evaluation"],
 b:["Measured against a reference answer or ground-truth passages: of everything required to answer correctly, how much made it into the retrieved context? If a needed fact was never retrieved, no amount of prompt engineering can recover it — the answer will be wrong or incomplete regardless.",
    "It is therefore the first metric to check when a RAG system underperforms. Low context recall points squarely at the retrieval half: chunking strategy, embedding model, hybrid search, or simply passing too few results to the model."],
 k:["Did retrieval return everything the answer needed?",
    "Low recall cannot be fixed downstream — the fact was never in the context.",
    "Diagnose with chunking, embedding choice, hybrid search, and top-k.",
    "Check this before touching the prompt."],
 x:{lang:"text", code:"question needs facts A, B, C\nretrieved context contains A and B\n\ncontext recall = 2/3\n-> the answer CANNOT be complete; fix retrieval, not the prompt"},
 r:["Context Precision","Faithfulness","RAGAS","Recall@k"]},

{t:"Context Precision", d:"How much of what was retrieved was actually relevant, and whether the relevant parts ranked highly.", l:"intermediate", g:["rag","evaluation"],
 b:["Retrieving ten chunks when two are relevant is not free. The eight irrelevant ones dilute attention, cost tokens, and measurably degrade answers — the effect usually called context pollution. Precision measures that waste, and rank-aware variants also reward putting the relevant chunks near the top.",
    "It is the metric that justifies a reranker. Retrieving widely raises recall and lowers precision; re-ranking the shortlist restores precision without giving up the recall, which is exactly why the two-stage retrieve-then-rerank design wins."],
 k:["Of the retrieved chunks, how many were relevant — and how highly ranked?",
    "Low precision dilutes the answer and wastes tokens: context pollution.",
    "The metric that a reranker is supposed to move.",
    "Retrieve wide for recall, rerank for precision — measure both."],
 x:{lang:"text", code:"retrieved 10, relevant 2   -> precision 0.2, noisy context\n\nadd a cross-encoder reranker: top 50 -> top 5\nrecall stays high, precision rises sharply"},
 r:["Context Recall","Reranking","Cross-Encoder","RAGAS"]},

{t:"Faithfulness", d:"Whether every claim in the answer is actually supported by the retrieved context.", l:"intermediate", g:["rag","evaluation"],
 b:["This is the hallucination metric for a RAG system. The answer is broken into individual claims and each is checked against the provided context; a claim the context does not support is an unfaithful one, regardless of whether it happens to be true in the world.",
    "That distinction matters. A model can produce a factually correct sentence from its own parametric memory while ignoring the documents you gave it — which is a failure, because the system's guarantee was that answers are grounded in your data. Requiring inline citations and verifying that the cited IDs exist is the practical control."],
 k:["Every claim in the answer must be supported by the retrieved context.",
    "This is the hallucination metric — measured against context, not the world.",
    "Correct-but-unsupported still counts as unfaithful, and should.",
    "Enforce with required citations, then verify the cited IDs programmatically."],
 x:{lang:"text", code:"context: \"The refund window is 30 days.\"\nanswer:  \"The refund window is 30 days and covers shipping.\"\n                                        ^ unsupported claim\n\nfaithfulness = 1/2 -- even if shipping IS covered in reality"},
 r:["Hallucination","Context Recall","RAGAS","LLM-as-a-Judge"]},

{t:"Recall@k", d:"The fraction of relevant documents that appear in the top k results.", l:"intermediate", g:["rag","evaluation"],
 b:["A classic information-retrieval metric that needs no LLM: given a labelled set of queries and their relevant documents, count how many of the relevant ones the retriever placed in the top k. Cheap, deterministic, and fast enough to run on every commit.",
    "Pair it with the k you actually pass to the model. Recall@50 being excellent is irrelevant if you only forward the top 5 and the correct chunk sits at rank 23 — which is precisely the failure a reranker exists to fix."],
 k:["Of the relevant documents, how many made the top k?",
    "Deterministic and LLM-free — cheap enough for CI on every commit.",
    "Measure at the k you actually pass to the model, not a flattering k.",
    "High recall@50 with low recall@5 is exactly the case for a reranker."],
 x:{lang:"python", code:"def recall_at_k(retrieved, relevant, k):\n    return len(set(retrieved[:k]) & set(relevant)) / len(relevant)\n\n# recall@50 = 0.95 but recall@5 = 0.40\n#   -> retrieval is fine; ranking is not. Add a reranker."},
 r:["Mean Reciprocal Rank","nDCG","Context Recall","Reranking"]},

{t:"Mean Reciprocal Rank", a:"MRR", d:"The average of 1/rank of the first correct result across a set of queries.", l:"intermediate", g:["rag","evaluation"],
 b:["If the first relevant document appears at rank 1 the query scores 1; at rank 2 it scores 0.5; at rank 10, 0.1. Averaging over all queries gives a single number that is dominated by how often the right answer is *at the top*.",
    "That makes it the right metric when only the first useful result matters — a search box, a single-answer lookup. It ignores everything after the first hit, so for questions needing several supporting documents, recall@k or nDCG describes the system better."],
 k:["Average of 1/rank of the *first* relevant result.",
    "Right metric when only the top hit matters.",
    "Ignores every relevant document after the first one.",
    "Use nDCG or recall@k when multiple documents are needed."],
 x:{lang:"python", code:"def mrr(results):\n    total = 0\n    for retrieved, relevant in results:\n        for i, doc in enumerate(retrieved, 1):\n            if doc in relevant:\n                total += 1 / i\n                break                 # first hit only\n    return total / len(results)"},
 r:["Recall@k","nDCG","Reranking","Evaluation"]},

{t:"nDCG", a:"Normalised Discounted Cumulative Gain", d:"A ranking metric that rewards putting highly relevant results near the top, on a graded scale.", l:"advanced", g:["rag","evaluation"],
 b:["It handles what recall@k and MRR cannot: relevance in degrees rather than yes-or-no. Each result contributes its relevance grade discounted by a logarithm of its position, so a highly relevant document at rank 1 counts for much more than the same document at rank 10. Dividing by the ideal ordering's score normalises everything into 0..1.",
    "That makes it the standard metric for comparing ranking systems, and the right choice when relevance is genuinely graded — perfect, partial, tangential — rather than binary."],
 k:["Graded relevance, discounted logarithmically by rank, normalised to 0..1.",
    "Rewards placing the *most* relevant results highest, not merely finding them.",
    "The standard metric for comparing rankers; needs graded labels.",
    "Use recall@k for binary relevance; nDCG when relevance has degrees."],
 x:{lang:"text", code:"DCG  = sum over i of  rel_i / log2(i + 1)\nnDCG = DCG / IDCG          (IDCG = the perfect ordering)\n\ngrades: 3 = perfect, 2 = useful, 1 = tangential, 0 = irrelevant\nrank 1 counts far more than rank 10"},
 r:["Recall@k","Mean Reciprocal Rank","Reranking","Evaluation"]},

{t:"Semantic Gap", d:"The mismatch between the words a user searches with and the words a document actually uses.", l:"intermediate", g:["rag","retrieval"],
 b:["A user types *cancel subscription*; the policy document is headed *account termination procedure*. There is no shared vocabulary, so keyword search finds nothing even though the correct document exists and is unambiguous.",
    "Closing that gap is the reason dense embeddings exist — they place the two phrases near each other because they mean the same thing. The mirror failure is worth remembering too: embeddings are weak on exact tokens like error codes and product identifiers, which is why the practical answer is hybrid search rather than either alone."],
 k:["The user's vocabulary and the document's vocabulary do not overlap.",
    "The core reason dense retrieval beats keyword search on natural questions.",
    "The mirror failure is embeddings missing exact codes and identifiers.",
    "Hybrid search plus query rewriting is the practical answer to both."],
 x:{lang:"text", code:"user:     \"how do I cancel my subscription\"\ndocument: \"Account Termination Policy\"\n\nBM25      no shared terms -> no match\nembedding close in vector space -> found\n\nthe reverse: \"ERR_4021\" -- BM25 nails it, embeddings do not"},
 r:["Embedding","Hybrid Search","BM25","Query Rewriting"]}

]);

/* ---------- agents ---------- */

TD.add("genai-llm", [

{t:"LangGraph", d:"An agent framework built on an explicit graph of nodes and edges, with persistent state and checkpointing.", l:"intermediate", g:["agents","tools"],
 b:["Instead of a loop you cannot inspect, you declare nodes (steps) and edges (transitions), including conditional ones. The state is an explicit object threaded through the graph, which makes the whole run inspectable, resumable and testable.",
    "Checkpointing is the feature that matters in production: state persists to a store, so a run can survive a crash, pause for human approval and resume, or be replayed for debugging. That is the usual reason to choose it over a hand-rolled loop — rebuilding durable state and retries correctly is genuinely hard."],
 k:["Explicit nodes and edges rather than an opaque loop.",
    "Checkpointing persists state, enabling resume, human-in-the-loop and replay.",
    "The production choice when you need control and debuggability.",
    "Persistence is configured, not inherited — you must supply a checkpointer."],
 x:{lang:"python", code:"from langgraph.graph import StateGraph, END\n\ng = StateGraph(AgentState)\ng.add_node(\"retrieve\", retrieve)\ng.add_node(\"answer\", answer)\ng.add_conditional_edges(\"retrieve\", enough_context, {True: \"answer\", False: \"retrieve\"})\ng.add_edge(\"answer\", END)\napp = g.compile(checkpointer=SqliteSaver.from_conn_string(\"state.db\"))"},
 r:["LangChain","AI Agent","ReAct","Stop Condition"]},

{t:"LangChain", d:"A large framework of integrations and abstractions for chaining LLM calls, tools and retrievers together.", l:"intermediate", g:["agents","tools"],
 b:["Its strength is breadth: connectors for hundreds of model providers, vector stores, loaders and tools, so gluing a prototype together is fast. Its abstractions have been criticised for hiding what is actually being sent to the model, which matters when you need to debug a prompt.",
    "The advice most practitioners converge on is to write one agent loop by hand first — a while loop, a tool registry, a message list. It takes an afternoon, demystifies every framework afterwards, and being able to say *underneath, a framework is a loop that appends tool results to a message list* is worth more in an interview than listing frameworks."],
 k:["Very broad integration surface; fastest route to a working prototype.",
    "Abstractions can obscure the actual prompt — a real debugging cost.",
    "Build one loop by hand first; the frameworks become obvious afterwards.",
    "LangGraph is its lower-level, production-oriented sibling."],
 x:{lang:"python", code:"# underneath every framework:\nmessages = [system, user]\nwhile True:\n    reply = model(messages, tools=tools)\n    if not reply.tool_calls:\n        return reply.content\n    for call in reply.tool_calls:\n        messages.append(run_tool(call))       # that is the whole idea"},
 r:["LangGraph","LlamaIndex","AI Agent","Tool Use"]},

{t:"LlamaIndex", d:"A framework designed retrieval-first, with strong document ingestion and indexing abstractions.", l:"intermediate", g:["agents","rag"],
 b:["Where LangChain starts from chains of calls, LlamaIndex starts from data: loaders for many document formats, node parsers for chunking, and index types including vector, tree, keyword and knowledge-graph indices.",
    "That focus makes it the natural fit for RAG-heavy applications, particularly where ingesting messy real-world documents is the hard part. Its query engines also compose multiple indices, which suits systems retrieving from several differently-shaped sources."],
 k:["Retrieval-first design; ingestion and indexing are the strong parts.",
    "Loaders for many formats, plus vector, tree, keyword and graph indices.",
    "The natural choice when document ingestion is the difficult half.",
    "Query engines compose several indices behind one interface."],
 x:{lang:"python", code:"from llama_index.core import VectorStoreIndex, SimpleDirectoryReader\n\ndocs  = SimpleDirectoryReader(\"./data\").load_data()\nindex = VectorStoreIndex.from_documents(docs)\nengine = index.as_query_engine(similarity_top_k=5)\nprint(engine.query(\"What is the refund window?\"))"},
 r:["LangChain","Retrieval-Augmented Generation","Vector Database","Chunking"]},

{t:"DSPy", d:"A framework that optimises prompts programmatically against a metric instead of by hand-tuning wording.", l:"advanced", g:["agents","evaluation"],
 b:["You declare the *signature* of each step — what goes in, what comes out — and supply a metric plus a small training set. DSPy then searches over prompt phrasings and few-shot example selections to maximise that metric, rather than you rewriting instructions and hoping.",
    "It only works if you already have a good eval set, which is the honest precondition and also the reason it is worth mentioning: it makes prompt engineering measurable and reproducible rather than folkloric. Being able to name it signals that you think about prompts as something to optimise, not to tweak."],
 k:["Declare input/output signatures; the framework optimises the prompt.",
    "Requires a metric and an eval set — that is the precondition, not a detail.",
    "Turns prompt engineering from folklore into a measurable search.",
    "Optimises few-shot selection as well as instruction wording."],
 x:{lang:"python", code:"import dspy\n\nclass Extract(dspy.Signature):\n    \"\"\"Pull the ticket's category and urgency.\"\"\"\n    ticket:   str = dspy.InputField()\n    category: str = dspy.OutputField()\n\nprogram   = dspy.ChainOfThought(Extract)\noptimised = dspy.BootstrapFewShot(metric=my_metric).compile(program, trainset=examples)"},
 r:["Evaluation","LLM-as-a-Judge","Prompt Template","Golden Dataset"]},

{t:"Agent Memory", d:"Structured storage that lets an agent recall things beyond its current context window.", l:"intermediate", g:["agents"],
 b:["The context window is working memory and it is finite. Anything an agent should remember for longer needs somewhere to live: semantic memory for facts about the user or domain, episodic memory for what happened in past runs, and procedural memory for learned workflows.",
    "The common mistake is stuffing an ever-growing history into the context. It gets slower, more expensive, and measurably *less* accurate as irrelevant content dilutes attention. Structured memory with explicit retrieval is the alternative — and note that connecting an MCP server does not provide memory, which is configured separately."],
 k:["Working memory is the context window; everything longer needs storage.",
    "Semantic (facts), episodic (past runs), procedural (learned workflows).",
    "Growing the context indefinitely makes quality *worse*, not just slower.",
    "Retrieve relevant memories explicitly rather than appending everything."],
 x:{lang:"python", code:"# retrieve only what is relevant to this turn\nfacts = memory.search(user_query, k=3)\nmessages = [system, *facts, *recent_turns, user_query]\n\n# not: messages = everything_that_ever_happened"},
 r:["Episodic Memory","Context Window","AI Agent","Retrieval-Augmented Generation"]},

{t:"Episodic Memory", d:"An agent's store of past interactions and how they turned out, retrieved when a similar situation recurs.", l:"advanced", g:["agents"],
 b:["Semantic memory holds facts (*this user prefers metric units*); episodic memory holds events (*last Tuesday this query was tried, this tool failed, this is what eventually worked*). Retrieving similar past episodes lets an agent avoid repeating a route that previously failed.",
    "It is what makes an agent appear to improve with use without any retraining. The practical requirement is storing the outcome alongside the episode — an episode with no recorded result teaches nothing when it is retrieved later."],
 k:["Stores past interactions *with their outcomes*, not just what was said.",
    "Retrieved by similarity when a comparable situation arises.",
    "Produces apparent learning with no retraining.",
    "Without recorded outcomes, retrieved episodes are useless."],
 x:{lang:"json", code:"{\n  \"task\": \"refund a duplicate charge\",\n  \"steps\": [\"lookup_order\", \"check_policy\", \"issue_refund\"],\n  \"outcome\": \"success\",\n  \"note\": \"check_policy must run first or issue_refund 403s\"\n}"},
 r:["Agent Memory","AI Agent","Vector Database","Reflection"]},

{t:"Reflection", d:"An agent pattern where a critic reviews the output and sends it back for revision before it is returned.", l:"advanced", g:["agents"],
 b:["The generator produces a draft; a critic — often the same model under a different prompt — evaluates it against explicit criteria and either accepts it or returns specific feedback. The generator revises, and the loop repeats within a bounded number of passes.",
    "It measurably improves quality on tasks with checkable criteria: code that must compile, output that must match a schema, writing that must satisfy a rubric. It costs at least one extra full pass, and it needs an iteration cap — a critic that is never satisfied will loop until the budget runs out."],
 k:["A critic reviews the draft and returns specific feedback for revision.",
    "Works best where criteria are checkable: compiles, validates, matches a rubric.",
    "Costs at least one extra pass; cap the iterations.",
    "Grounding the critic in a real check (a test run) beats a vague rubric."],
 x:{lang:"text", code:"generate -> critique -> revise -> critique -> accept\n                 ^                    |\n                 +--- max 2 rounds ----+\n\nbest critic: an actual test run, not \"is this good?\""},
 r:["AI Agent","Multi-Agent System","LLM-as-a-Judge","Stop Condition"]},

{t:"Planner-Executor", d:"An agent pattern that separates decomposing a task into steps from carrying each step out.", l:"advanced", g:["agents"],
 b:["One model, prompted for planning, breaks the goal into an ordered list of steps. A second — often a cheaper model — executes each step with the available tools. The separation exists because planning and doing reward different prompts and different amounts of context.",
    "It suits long tasks where a single ReAct loop drifts or forgets the objective, since the plan stays visible throughout. The trade-off is rigidity: a plan made before any information was gathered may be wrong, so worthwhile implementations allow replanning when a step fails."],
 k:["A planner decomposes the goal; an executor carries out each step.",
    "Keeps long tasks on track where one ReAct loop tends to drift.",
    "The executor can be a smaller, cheaper model than the planner.",
    "Allow replanning — a plan made before gathering information is often wrong."],
 x:{lang:"text", code:"PLANNER   goal -> 1. find the invoice\n                  2. check the payment status\n                  3. draft the reply\n\nEXECUTOR  runs each step with tools; on failure,\n          returns to the planner rather than improvising"},
 r:["AI Agent","Multi-Agent System","ReAct","Reflection"]},

{t:"Stop Condition", d:"The bounds that stop an agent looping forever — iterations, tokens, time, spend, or repeated actions.", l:"intermediate", g:["agents","safety"],
 b:["An agent that misreads a tool error can retry the same call indefinitely, and because each iteration costs money, an unbounded loop is a financial incident as well as a hung request. This is the single most common production failure for agents.",
    "Several limits are needed rather than one, because they fail differently: a maximum iteration count, a token budget, a wall-clock timeout, a spend cap, and a detector for repeated identical actions. Anything irreversible — sending, deleting, paying — should additionally require explicit human approval."],
 k:["Max iterations, max tokens, wall-clock timeout, spend cap, repeat detector.",
    "The number one production failure mode for agents, and it costs real money.",
    "A repeated-identical-action detector catches the most common loop.",
    "Require human approval for anything irreversible."],
 x:{lang:"python", code:"for step in range(MAX_STEPS):\n    if tokens_used > TOKEN_BUDGET: break\n    if time.time() - start > TIMEOUT: break\n    action = agent.next()\n    if action == last_action:              # looping on itself\n        break\n    last_action = action"},
 r:["AI Agent","ReAct","Excessive Agency","Denial of Wallet"]}

]);

/* ---------- evaluation ---------- */

TD.add("mlops", [

{t:"Evaluation", a:"Evals", d:"A measured, repeatable test of whether an AI system actually works — the most underrated skill in AI engineering.", l:"core", g:["evaluation"],
 b:["Traditional software fails loudly: it crashes and a unit test catches it. An LLM system fails fluently — a confident, well-written, wrong answer — and no assertion catches that. Evals are the substitute: a golden dataset, a set of checks, and a score you can track across changes.",
    "Practitioners and hiring managers converge on this being the biggest gap in entry-level candidates. Almost no fresher portfolio contains an eval harness, which is exactly why building one is the highest-return item available. Start with free deterministic assertions — valid JSON, required fields, latency, cost — before reaching for an LLM judge."],
 k:["LLM systems fail fluently, so ordinary tests catch nothing.",
    "Deterministic assertions first; they are free and catch a surprising amount.",
    "Golden dataset of 50–200 hand-labelled real inputs is the foundation.",
    "Wire it into CI and fail the build on regression — that is the whole point."],
 x:{lang:"python", code:"def test_extraction():\n    for case in golden_set:\n        out = system(case.input)\n        assert valid_json(out)                    # free\n        assert set(out) >= REQUIRED_FIELDS        # free\n        assert out[\"total\"] == case.expected_total\n        assert latency < SLA"},
 r:["Golden Dataset","LLM-as-a-Judge","Error Analysis","Eval Gate"]},

{t:"Error Analysis", d:"Reading actual failures one by one and grouping them by cause, before deciding what to measure.", l:"intermediate", g:["evaluation"],
 b:["The instinct is to pick metrics first and start measuring. The better order is to run the system on fifty real inputs, read every output, and sort the failures into categories. Three to five dominant failure modes almost always emerge, and those tell you what is worth measuring.",
    "Skipping it produces suites full of metrics that move without anything improving. The step is unglamorous and manual, and it is where practitioners consistently say the value is."],
 k:["Read 50 real outputs and categorise the failures before choosing metrics.",
    "Three to five dominant failure modes usually account for most errors.",
    "Metrics invented in the abstract measure things that do not matter.",
    "Repeat it periodically — the failure distribution shifts as you fix things."],
 x:{lang:"text", code:"50 outputs read by hand:\n  18  retrieved the wrong document\n  12  ignored the context, answered from memory\n   7  malformed JSON on long inputs\n   4  refused a legitimate question\n\nnow you know what to measure -- and in what order to fix"},
 r:["Evaluation","Golden Dataset","Faithfulness","Root Cause Analysis"]},

{t:"Position Bias", d:"An LLM judge preferring whichever response it is shown first, regardless of quality.", l:"advanced", g:["evaluation"],
 b:["Present the same pair of responses in the opposite order and a judge model will often change its verdict. The preference is for the position, not the content, and it can be large enough to invalidate a comparison entirely.",
    "The mitigation is straightforward: evaluate each pair twice with the order swapped and average, or count only the cases where both orderings agree. Naming this alongside verbosity bias and self-preference is what signals you have actually run LLM-judged evaluations rather than read about them."],
 k:["Judges favour the first response shown, independent of quality.",
    "Mitigate by swapping the order and averaging both verdicts.",
    "Or count only pairs where both orderings agree — stricter and cleaner.",
    "One of the four biases to name: position, verbosity, self-preference, clustering."],
 x:{lang:"python", code:"a_first = judge(question, response_a, response_b)\nb_first = judge(question, response_b, response_a)\n\nif a_first != flip(b_first):\n    return \"tie\"          # the judge is position-driven here"},
 r:["LLM-as-a-Judge","Verbosity Bias","Self-Preference Bias","Evaluation"]},

{t:"Verbosity Bias", d:"An LLM judge preferring longer answers regardless of whether the extra length adds anything.", l:"advanced", g:["evaluation"],
 b:["Length reads as effort and thoroughness, and judge models pick up that association from their training. A padded answer scores above a crisp correct one, which quietly pushes any system optimised against such a judge towards waffle.",
    "The consequences are practical: your outputs get longer, your token costs rise, and users find the product worse. Mitigations are to state conciseness in the rubric explicitly, to compare answers of similar length, or to include length as a separate reported metric so the drift is visible."],
 k:["Longer answers score higher even when the extra content adds nothing.",
    "Optimising against such a judge makes your product verbose and expensive.",
    "Put conciseness in the rubric explicitly, and track output length as a metric.",
    "Watch for it whenever average response length creeps up release over release."],
 x:{lang:"text", code:"A: \"30 days.\"                                    correct, complete\nB: \"Great question! Our refund policy, which we\n    designed with customers in mind, allows...\"  same fact, 60 words\n\nan unguarded judge prefers B"},
 r:["LLM-as-a-Judge","Position Bias","Self-Preference Bias","Evaluation"]},

{t:"Self-Preference Bias", d:"A judge model rating outputs from its own family more highly than equivalent outputs from others.", l:"advanced", g:["evaluation"],
 b:["Models recognise their own stylistic fingerprints and score them favourably. Using GPT to judge a GPT-versus-Claude comparison therefore tilts the result before any quality difference is considered.",
    "It matters most when choosing between models, which is exactly when people reach for an LLM judge. Use a judge from a third family, or several judges and take the majority, and validate against human labels on a sample — an unvalidated judge is just another unmeasured model."],
 k:["Judges rate their own family's outputs higher.",
    "Most damaging in model-selection comparisons — precisely where judges are used.",
    "Use a third-party judge, or an ensemble with a majority vote.",
    "Always validate the judge against human labels on a sample."],
 x:{lang:"text", code:"comparing GPT vs Claude?\n  do NOT judge with either of them\n  use a third model, or several and take the majority\n\nthen have a human label 30 cases and measure agreement"},
 r:["LLM-as-a-Judge","Position Bias","Verbosity Bias","Evaluation"]},

{t:"MT-Bench", d:"A benchmark scoring open-ended, multi-turn conversation quality via a strong LLM judge.", l:"intermediate", g:["evaluation"],
 b:["Eighty multi-turn questions across writing, reasoning, coding and extraction, scored by a judge model. It measures something the knowledge benchmarks miss — whether a model is actually pleasant and coherent to converse with over more than one turn.",
    "The limitation is the one to state: preference is not correctness. A stylish, confident, wrong answer can beat a plain correct one, and the same judge biases apply. Chatbot Arena is the human-voted equivalent and carries the same caveat."],
 k:["80 multi-turn questions, judged by a strong LLM.",
    "Measures conversational quality, which knowledge benchmarks ignore.",
    "**Preference is not correctness** — style can beat substance.",
    "Chatbot Arena is the human-vote version, with the same caveat."],
 x:{lang:"text", code:"turn 1  \"Write a poem about recursion.\"\nturn 2  \"Now rewrite it for a five-year-old.\"\n          ^ tests whether context carried over\n\njudged 1-10 by a strong model -- subject to every judge bias"},
 r:["LLM-as-a-Judge","Benchmark","MMLU","Benchmark Saturation"]},

{t:"Benchmark Contamination", d:"Test data leaking into a model's training set, making benchmark scores meaningless.", l:"intermediate", g:["evaluation"],
 b:["Public benchmarks live on the open web, and web-scale pretraining corpora scoop up the open web. A model that has memorised the test set answers correctly without any of the ability the benchmark was meant to measure.",
    "It is difficult to rule out and difficult to detect. The practical responses are held-out private sets, benchmarks released after a model's training cutoff, and canary strings embedded in test data to detect memorisation. It is the strongest reason to trust your own eval set over any public leaderboard."],
 k:["Test data appears in the training corpus, so scores measure memorisation.",
    "Hard to detect and effectively impossible to rule out for public benchmarks.",
    "Mitigations: private held-out sets, post-cutoff benchmarks, canary strings.",
    "The best reason to build an eval set from your own data."],
 x:{lang:"text", code:"symptoms of contamination:\n  near-perfect on a public benchmark,\n  ordinary on a paraphrase of the same questions\n\nthe defence: an eval set nobody has ever published"},
 r:["Benchmark Saturation","Evaluation","Golden Dataset","MMLU"]},

{t:"Benchmark Saturation", d:"A benchmark stopping being useful because the leading models all score near the ceiling.", l:"intermediate", g:["evaluation"],
 b:["When every frontier model scores 89, 90 and 91 on the same test, the differences are inside the noise and inside the remaining label errors. The benchmark can no longer separate the models, which is the only thing a benchmark is for.",
    "MMLU and GSM8K both reached this point, which is why harder successors appeared and why the field moved towards agentic benchmarks like SWE-bench that measure whether real work gets done. When quoting any benchmark, say whether it is saturated."],
 k:["Scores cluster at the ceiling, so the benchmark stops discriminating.",
    "MMLU and GSM8K are both effectively saturated.",
    "Remaining headroom is often just label noise in the test set.",
    "The move towards SWE-bench-style agentic benchmarks is a response to this."],
 x:{lang:"text", code:"MMLU  2020: best ~43%      a real signal\n      2025: several at ~89%  differences are noise\n\n-> harder benchmarks, or your own task-specific eval set"},
 r:["Benchmark Contamination","Benchmark","MMLU","Evaluation"]},

{t:"DeepEval", d:"A pytest-style library for asserting on LLM output quality inside an ordinary test suite.", l:"intermediate", g:["evaluation","tools"],
 b:["It exposes LLM evaluation as familiar test assertions, so quality checks live beside unit tests and run under the same command in the same CI job. Metrics include faithfulness, answer relevance, hallucination and custom G-Eval rubrics.",
    "Its value is mostly organisational rather than technical: making evals a normal part of the test suite is what stops them from being run once at project start and quietly abandoned."],
 k:["pytest-style assertions on LLM output; runs in your existing suite.",
    "Built-in faithfulness, relevance, hallucination and custom rubric metrics.",
    "Keeps evals beside unit tests so they actually keep running.",
    "Alternatives: RAGAS for RAG-specific metrics, Promptfoo for prompt comparison."],
 x:{lang:"python", code:"from deepeval import assert_test\nfrom deepeval.metrics import FaithfulnessMetric\nfrom deepeval.test_case import LLMTestCase\n\ndef test_answer_is_grounded():\n    case = LLMTestCase(input=q, actual_output=out, retrieval_context=ctx)\n    assert_test(case, [FaithfulnessMetric(threshold=0.8)])"},
 r:["Evaluation","RAGAS","Promptfoo","Eval Gate"]},

{t:"Promptfoo", d:"A tool for comparing prompts, models and providers side by side on the same test cases, and for red-teaming them.", l:"intermediate", g:["evaluation","tools"],
 b:["Declare your test cases and the variants you want to compare — different prompts, different models, different temperatures — and it runs the matrix and produces a comparison view. That turns *this prompt feels better* into a table of numbers.",
    "It also includes red-teaming: automatically generating adversarial inputs including prompt injections and jailbreaks against your configuration. Running that before shipping is a cheap way to find the obvious holes."],
 k:["Runs a matrix of prompts × models × cases and compares them side by side.",
    "Replaces *this prompt feels better* with a table you can defend.",
    "Built-in red-teaming generates adversarial and injection inputs.",
    "Config-file driven, so the comparison is reproducible and reviewable."],
 x:{lang:"yaml", code:"prompts: [prompts/v1.txt, prompts/v2.txt]\nproviders: [openai:gpt-4o, anthropic:claude-sonnet-4]\ntests:\n  - vars: {question: \"What is the refund window?\"}\n    assert:\n      - type: contains\n        value: \"30 days\""},
 r:["Evaluation","DeepEval","Red Teaming","Prompt Injection"]},

{t:"Eval Gate", d:"A CI check that fails the build when an eval score drops below its threshold.", l:"intermediate", g:["evaluation","mlops"],
 b:["Without it, evals are something someone runs occasionally and interprets generously. As a gate, a prompt change that quietly degrades eight percent of cases cannot merge — the pipeline blocks it exactly as a failing unit test would.",
    "It is the step that converts having evals into being protected by them, and it is rare enough in fresher portfolios to be a genuine differentiator. Set thresholds from the current measured baseline rather than aspirationally, or the gate will be disabled within a week."],
 k:["CI fails the build when an eval metric regresses past its threshold.",
    "Turns evals from an occasional ritual into an actual safety net.",
    "Set thresholds from the measured baseline, not from ambition.",
    "Very rare in fresher portfolios — a real differentiator."],
 x:{lang:"yaml", code:"- name: Evaluation gate\n  run: |\n    python -m evals.run --suite golden --out report.json\n    python -m evals.check --min-faithfulness 0.85 \\\n                          --min-recall-at-5 0.80 \\\n                          --max-p95-latency-ms 2000"},
 r:["Evaluation","Regression Testing","Continuous Integration","Golden Dataset"]},

{t:"Regression Testing", d:"Re-running a fixed suite after every change to prove that what used to work still works.", l:"core", g:["evaluation","mlops"],
 b:["In ordinary software this is a solved habit. In LLM systems it is harder and more necessary: outputs are non-deterministic, so a change can degrade a subset of cases without failing anything loudly. A prompt edit that improves one category while breaking another is invisible without a fixed suite run before and after.",
    "The practical requirements are a stable golden set, a fixed seed and temperature where determinism is possible, and thresholds rather than exact-match assertions for the parts that genuinely vary."],
 k:["Prove the change did not break what already worked.",
    "Essential for LLM systems, where degradation is silent and partial.",
    "Fix the seed and temperature where you can; use thresholds where you cannot.",
    "Every newly discovered production failure should join the suite."],
 x:{lang:"text", code:"before  faithfulness 0.91 | recall@5 0.84 | p95 1.4s\nafter   faithfulness 0.89 | recall@5 0.61 | p95 1.3s\n                                       ^ the prompt change broke retrieval\n\nwithout the fixed suite, nobody would have noticed"},
 r:["Eval Gate","Evaluation","Continuous Integration","Golden Dataset"]}

]);
