/* Crash Course, part 5 — the machine, the shell, and everyday version control.

   House rule, same as the rest of the crash course: explain it the way you
   would to a smart friend who has never written a line. Short sentences. Real
   examples. Nothing hand-waved.

   Scope rule for this file: only words that no other category owns. The deep
   OS material lives in os-networking and the git workflow material lives in
   software-engineering — what is here is the daily, practical layer that every
   engineer is assumed to know and nobody is ever taught. */

TD.add("crash-course", [

{t:"Working Directory", d:"The folder your terminal is currently *in* — the invisible starting point every relative path is measured from.", l:"core", g:["tools","shell","crash-course"],
 b:["Every terminal session sits inside exactly one folder at a time. When you type a command that mentions a file, the shell looks for it starting from there. This is why the same command works in one window and fails in another with *no such file*: the command was fine, you were simply standing somewhere else.",
    "`pwd` prints where you are and `cd` moves you. The habit worth forming is to check `pwd` the moment anything file-related behaves oddly, before you start doubting the command itself. Almost every early terminal frustration is really this."],
 k:["Every terminal is *in* one folder; relative paths start from there.",
    "`pwd` prints it, `cd` changes it, `cd -` jumps back to the previous one.",
    "*No such file* usually means wrong directory, not a missing file.",
    "Each terminal tab has its own working directory, independently."],
 x:{lang:"bash", code:"pwd                 # /home/aryan/projects/api\nls                  # what is here?\ncd ../frontend      # up one, then into frontend\ncd -                # back to where I just was\ncd                  # with no argument: straight home"},
 r:["File Path","Command Line Interface","Shell","Absolute and Relative Path"]},

{t:"Absolute and Relative Path", d:"An absolute path starts from the root of the disk; a relative one starts from wherever you happen to be.", l:"core", g:["tools","shell","crash-course"],
 b:["`/home/aryan/notes.txt` is absolute — it means the same file no matter where you run it from. `notes.txt` or `../notes.txt` is relative, and its meaning depends entirely on the working directory. Absolute paths begin with `/` on macOS and Linux, or a drive letter like `C:\\` on Windows.",
    "The practical rule: relative paths inside a project, absolute paths in scripts that could run from anywhere. A cron job or a CI step has no sensible working directory, so a relative path there is a bug waiting for a bad day."],
 k:["Absolute starts at the root and never changes meaning.",
    "Relative starts at the working directory and moves with you.",
    "`.` is here, `..` is the parent, `~` is your home folder.",
    "In scripts run by cron or CI, prefer absolute — there is no reliable *here*."],
 x:{lang:"bash", code:"/var/log/app.log        # absolute: always the same file\n./config.yml            # relative: config.yml in the current folder\n../shared/utils.py      # relative: up one level, then into shared\n~/Downloads/data.csv    # ~ expands to your home directory"},
 r:["Working Directory","File Path","Shell","Environment Variable"]},

{t:"PATH", d:"The list of folders your shell searches, in order, when you type a command name.", l:"core", g:["tools","shell","crash-course"],
 b:["When you type `python`, the shell does not know where that program is. It walks through the folders listed in the `PATH` environment variable, in order, and runs the first match it finds. That is the entire mechanism, and it explains a whole family of confusing errors.",
    "*Command not found* means nothing in `PATH` matched. Getting the wrong version means an earlier folder matched first — which is why `which python` is the fastest way to answer *which one is actually running?*. Installing a tool and still seeing the old version almost always means the new one landed in a folder further down the list."],
 k:["A colon-separated list of folders, searched left to right.",
    "*Command not found* = the program is not in any of them.",
    "Wrong version = an earlier folder in the list won.",
    "`which cmd` on macOS/Linux, `where cmd` on Windows, tells you the winner."],
 x:{lang:"bash", code:"echo $PATH          # /usr/local/bin:/usr/bin:/bin\nwhich python        # /usr/local/bin/python  <- the one that runs\n\n# put your own folder first, for this session only\nexport PATH=\"$HOME/.local/bin:$PATH\""},
 r:["Environment Variable","Shell","Command Line Interface","Package"]},

{t:"Glob Pattern", d:"The `*` and `?` wildcards the shell expands into a list of matching filenames before your command ever runs.", l:"core", g:["tools","shell","crash-course"],
 b:["When you type `rm *.log`, the shell finds every matching file first and hands `rm` the full list. The command itself never sees the star. This ordering matters: if nothing matches, most shells pass the literal `*.log` through, and the resulting error looks baffling until you know why.",
    "`*` matches any run of characters, `?` matches exactly one, and `**` matches across directories in most modern shells. It is not a regular expression, and confusing the two is common — in a glob, `*` on its own means *anything*, whereas in a regex it means *zero or more of the previous thing*."],
 k:["The shell expands globs before the command runs; the command sees filenames.",
    "`*` = any characters, `?` = exactly one, `[abc]` = one of these.",
    "Globs are not regular expressions — `*` means something different in each.",
    "Quote the pattern when you want the command, not the shell, to expand it."],
 x:{lang:"bash", code:"ls *.py             # every Python file here\nls **/*.py          # and in every subfolder too\nrm report-?.csv     # report-1.csv but not report-10.csv\n\n# quote it so find does its own matching, not the shell\nfind . -name \"*.log\""},
 r:["Shell","Command Line Interface","Regular Expression","File Path"]},

{t:"Permissions", d:"The read, write and execute flags that decide who may do what to a file.", l:"core", g:["tools","shell","crash-course"],
 b:["Every file on a Unix system carries three permissions — read, write, execute — for three audiences: the owner, the group, and everyone else. `ls -l` shows them as a string like `-rwxr-xr--`, which reads as: owner may do everything, group may read and run, others may only read.",
    "The numeric form is the same thing added up: read is 4, write is 2, execute is 1. So `chmod 755` means owner 7 (4+2+1, everything) and the other two 5 (4+1, read and run). *Permission denied* when running a script almost always means the execute bit is missing, and `chmod +x` fixes it."],
 k:["Three permissions (read/write/execute) × three audiences (owner/group/other).",
    "Numbers add up: read 4, write 2, execute 1. So 7 is all three.",
    "`chmod +x script.sh` is the fix for *permission denied* on a script.",
    "`chmod 777` grants everything to everyone — almost never the right answer."],
 x:{lang:"bash", code:"ls -l deploy.sh     # -rw-r--r--  : readable, but not runnable\nchmod +x deploy.sh  # add the execute bit\nls -l deploy.sh     # -rwxr-xr-x  : now it runs\n\nchmod 600 ~/.ssh/id_rsa   # private key: owner only, or SSH refuses it"},
 r:["Shell","File Path","SSH Key","Least Privilege"]},

{t:"Symbolic Link", d:"A file that is really a signpost pointing at another file somewhere else.", l:"core", g:["tools","shell","crash-course"],
 b:["A symlink looks and behaves like the file it points at — you can open it, edit through it, run it — but it stores only a path. Delete the original and the link still exists while pointing at nothing, which is called a broken or dangling symlink.",
    "They are everywhere once you notice them. `/usr/local/bin/python` is usually a symlink to a specific version, which is how switching versions works without moving anything. Node's `node_modules/.bin` is full of them. The practical gotcha: copying a folder may copy the links rather than what they point at, depending on the flags you used."],
 k:["A file containing a path to another file, followed transparently.",
    "Delete the target and the link survives but breaks.",
    "`ln -s target linkname` — the order is target first, and it is easy to reverse.",
    "`ls -l` shows `link -> target`; `readlink -f` resolves the whole chain."],
 x:{lang:"bash", code:"ln -s /usr/bin/python3.11 ~/bin/python   # target first, then the name\nls -l ~/bin/python\n# python -> /usr/bin/python3.11\n\nreadlink -f ~/bin/python    # follow it all the way to the real file"},
 r:["File Path","Shell","Permissions","Working Directory"]},

{t:"Standard Streams and Redirection", d:"Every program is born with an input, an output and an error channel — and you can point each one wherever you like.", l:"core", g:["tools","shell","crash-course"],
 b:["A program writes normal results to standard output and complaints to standard error. They are separate on purpose: you can save the results to a file while still seeing the errors on screen. `>` redirects output to a file, `>>` appends instead of overwriting, and `2>` redirects errors specifically.",
    "The distinction is what makes pipelines usable. `command > out.txt` captures the answer but leaves errors visible, which is what you almost always want. `2>&1` means *send errors to wherever output is currently going*, and the order matters — putting it before the `>` sends errors to the old destination."],
 k:["stdout (1) is results, stderr (2) is complaints — deliberately separate.",
    "`>` overwrites, `>>` appends. Getting this wrong destroys files silently.",
    "`2>&1` merges errors into output; write it *after* the redirect, not before.",
    "`/dev/null` is the bin — `2>/dev/null` discards errors entirely."],
 x:{lang:"bash", code:"python app.py > out.log            # results to file, errors on screen\npython app.py >> out.log           # append instead of overwrite\npython app.py > all.log 2>&1       # both into one file\npython app.py 2>/dev/null          # discard errors, keep results"},
 r:["Standard Input and Output","Pipe","Shell","Exit Code"]},

{t:"Background Process", d:"A command that keeps running while you get your prompt back.", l:"core", g:["tools","shell","crash-course"],
 b:["Adding `&` to a command starts it and returns your prompt immediately. `Ctrl-Z` suspends whatever is running, `bg` resumes it in the background and `fg` brings it forward again. `jobs` lists what you have parked.",
    "The catch that surprises everyone: a background job is still tied to your terminal, so closing the window usually kills it. For anything that must survive a disconnect, use `nohup`, or a terminal multiplexer like `tmux` or `screen` — which is why every server tutorial eventually mentions them."],
 k:["`&` starts in the background; `Ctrl-Z` then `bg` moves a running one there.",
    "`jobs` lists them, `fg` brings one back, `kill %1` stops job number 1.",
    "Closing the terminal usually kills background jobs — they are not detached.",
    "`nohup`, `tmux` or `screen` for anything that must outlive the session."],
 x:{lang:"bash", code:"python train.py &        # runs in the background, prompt returns\njobs                     # [1]+ Running  python train.py &\nfg %1                    # bring it back to the foreground\n\nnohup python train.py &  # survives logout, output goes to nohup.out"},
 r:["Process ID","Shell","Process vs Thread","Signal"]},

{t:"Process ID", d:"The number the operating system uses to refer to one running program.", l:"core", g:["tools","shell","crash-course"],
 b:["Every running program gets a PID when it starts. That number is how you inspect it, pause it or stop it. `ps` and `top` list processes with their PIDs; `kill 1234` asks process 1234 to shut down politely.",
    "The most useful trick in practice is finding what is holding a port. *Address already in use* means an old copy of your server is still running, and `lsof -i :8000` names the culprit so you can stop it. Knowing this turns a five-minute mystery into a ten-second fix."],
 k:["A unique number per running process, assigned at start and reused later.",
    "`kill PID` asks politely; `kill -9 PID` is the forced last resort.",
    "*Address already in use* = an old process still holds the port.",
    "`lsof -i :8000` (or `netstat`) finds which process owns a port."],
 x:{lang:"bash", code:"ps aux | grep python     # find it\nlsof -i :8000            # what is holding port 8000?\nkill 4821                # ask it to stop\nkill -9 4821             # make it stop (no cleanup, last resort)"},
 r:["Background Process","Process vs Thread","Port","Signal"]},

{t:"Exit Code Conventions", d:"The number a program returns when it finishes: zero means success, anything else means a specific failure.", l:"core", g:["tools","shell","crash-course"],
 b:["Every command ends with a numeric status. Zero means it worked. Any other number means it did not, and the particular number often identifies which failure. It is invisible until you look — `echo $?` prints the code from the last command.",
    "This is the mechanism behind every script and CI pipeline. `&&` runs the next command only if the previous one returned zero; `||` runs it only if it failed. A build step *passes* purely because its exit code was zero, which is also why a script that ignores exit codes will happily continue after a failed step and deploy something broken."],
 k:["0 is success; every other value is a failure with a meaning.",
    "`echo $?` shows the last command's code.",
    "`a && b` runs b only on success; `a || b` only on failure.",
    "`set -e` at the top of a script stops it at the first failing command."],
 x:{lang:"bash", code:"grep -q \"ERROR\" app.log\necho $?              # 0 if found, 1 if not\n\nnpm test && npm run deploy    # deploy ONLY if tests passed\n\nset -euo pipefail    # the standard safety header for a bash script"},
 r:["Exit Code","Shell","Continuous Integration","Script"]},

{t:"Shell Alias", d:"A short name you invent for a longer command you type constantly.", l:"core", g:["tools","shell","crash-course"],
 b:["`alias gs='git status'` means typing `gs` from then on runs `git status`. Put it in your shell's startup file — `~/.bashrc` or `~/.zshrc` — and it is there in every new terminal. It is the cheapest productivity gain available and most people set it up years later than they should.",
    "Two things worth knowing. Aliases only expand at the start of a command, so they do not work inside scripts — use a function or a script file for that. And an alias that shadows a real command, like `alias rm='rm -i'`, will surprise you on a machine that lacks it, so avoid making destructive commands *feel* safe."],
 k:["`alias name='command'`, saved in `~/.bashrc` or `~/.zshrc`.",
    "Only expands at the start of a command line, and not inside scripts.",
    "`type name` reveals whether something is an alias, function or real program.",
    "Avoid aliasing destructive commands to safer versions — the habit breaks elsewhere."],
 x:{lang:"bash", code:"alias gs='git status'\nalias ll='ls -lah'\nalias ..='cd ..'\n\n# a function, when you need arguments in the middle\nmkcd() { mkdir -p \"$1\" && cd \"$1\"; }"},
 r:["Shell","Command Line Interface","Configuration File","Script"]},

{t:".gitignore", d:"The file listing what git should pretend does not exist.", l:"core", g:["git","tools","crash-course"],
 b:["Some files should never be committed: secrets, dependency folders, build output, editor settings, anything machine-specific. `.gitignore` lists them by name or pattern, and git then leaves them out of `git status` and every commit.",
    "The rule that catches everyone: **`.gitignore` only affects untracked files.** If a file was already committed, adding it to `.gitignore` changes nothing — git is already tracking it and continues to. You must untrack it explicitly with `git rm --cached`. This is precisely how committed `.env` files persist in a repository long after somebody *fixed* it."],
 k:["Patterns, one per line; `#` for comments, `/` anchors to the repo root.",
    "It only affects files git is not already tracking.",
    "Already committed by mistake? `git rm --cached file` then commit.",
    "A secret in git history stays there — rotate it, do not just delete it."],
 x:{lang:"bash", code:"# .gitignore\n.env                # secrets, never\nnode_modules/       # reinstallable\n__pycache__/\n*.log\n.DS_Store\ndist/               # build output\n\n# already committed by accident:\n# git rm --cached .env && git commit -m \"stop tracking .env\""},
 r:["Repository","API Key","Secrets Management","Environment Variable"]},

{t:"Git Blame", d:"The command that shows who last changed each line, and in which commit.", l:"core", g:["git","tools","crash-course"],
 b:["Despite the name, its real use is archaeology rather than accusation. You find a strange line, run blame, and get the commit that introduced it — then read that commit's message and diff to learn *why*. That context is usually the answer to whether the line can safely be changed.",
    "Most editors show it inline, which is where it becomes a habit rather than a command. The natural follow-up is `git log -S \"some text\"`, which finds the commit where a piece of text first appeared or disappeared — often faster than blame when the line has moved between files."],
 k:["Shows the last commit to touch each line, with author and date.",
    "The point is finding the *reason*, in the commit message — not the person.",
    "`git log -S \"text\"` finds when a string appeared or vanished.",
    "A reformatting commit obscures blame; `-w` ignores whitespace-only changes."],
 x:{lang:"bash", code:"git blame app.py            # who last touched each line\ngit blame -L 40,60 app.py   # just lines 40-60\ngit show abc123             # read the full commit blame pointed at\n\ngit log -S \"retry_count\"    # when did this string enter the codebase?"},
 r:["Commit","Repository","Diff","Code Review"]},

{t:"Squash", d:"Collapsing several commits into one before merging, so history reads as finished work rather than as it was made.", l:"core", g:["git","tools","crash-course"],
 b:["A branch typically accumulates commits like *wip*, *fix typo* and *actually fix it*. Squashing folds them into a single commit with one clear message. Most hosts offer *Squash and merge* as a button, which is where the majority of squashing happens.",
    "The trade-off is real and teams genuinely disagree. Squashing gives a clean, readable main branch where every commit is a complete change — better for `git bisect` and for reading history later. Keeping the commits preserves the detail of how the work developed. The one rule everyone agrees on: never rewrite history that other people have already pulled."],
 k:["Combines several commits into one, with a new message.",
    "*Squash and merge* on a pull request is the common route.",
    "Cleaner main history and easier bisecting; loses intermediate detail.",
    "Never rewrite commits that others have already pulled."],
 x:{lang:"bash", code:"git rebase -i HEAD~3     # mark commits as 'squash' or 'fixup'\n\n# or let the host do it:\n#   Squash and merge  ->  one commit on main\n\ngit merge --squash feature   # stage everything as one change"},
 r:["Commit","Rebase","Pull Request","Branch"]},

{t:"Root Cause Analysis", d:"Continuing to ask *why* past the first plausible answer, until you reach the thing that actually needs fixing.", l:"core", g:["practice","crash-course"],
 b:["The site went down because the disk filled up. Why? Logs were not rotating. Why? The rotation config was never deployed to that host. Why? That host was provisioned by hand, outside the normal process. Fixing the disk buys you a week; fixing the provisioning gap fixes the class of problem.",
    "The common failure is stopping at the first cause that explains the symptom, because it feels like an answer and the pressure to move on is real. The counter-habit is mechanical: ask *why* roughly five times, and notice when the answers stop being technical and start being about process. That is usually where the real fix lives."],
 k:["Ask *why* repeatedly; the first answer is a symptom, not a cause.",
    "Stop when the fix would prevent the whole class of failure, not this instance.",
    "The real cause is often a process gap, not a line of code.",
    "Fix the immediate problem first, then the cause — but do fix the cause."],
 x:{lang:"text", code:"Symptom  API returned 500s for 20 minutes\nWhy      the database ran out of connections\nWhy      a background job opened one per row\nWhy      it was written before the pool existed\nWhy      no review caught it -- the author was the only reviewer\nFIX      connection pooling AND a two-reviewer rule"},
 r:["Postmortem","Debugging","Incident Response","Bug"]},

{t:"Premature Optimisation", d:"Making code faster before you know what is slow — usually paying in clarity for a speed-up nobody notices.", l:"core", g:["practice","crash-course"],
 b:["Knuth's line, almost always quoted in fragments, is that premature optimisation is the root of all evil *in the 97% of cases that do not matter*. The full sentence matters, because the remaining 3% is where real engineering happens. The instruction is not to ignore performance; it is to find the 3% by measuring rather than guessing.",
    "The practical damage is that optimised code is usually harder to read, and hard-to-read code accumulates bugs. Worse, intuitions about what is slow are famously unreliable — the hand-tuned loop is rarely the bottleneck, and the innocent database call inside it usually is. Write the clear version, measure it, then optimise the part the profiler names."],
 k:["Write clearly first, measure second, optimise only what the profiler names.",
    "Intuition about bottlenecks is unreliable; measurement is not.",
    "Optimised code is harder to read, and harder-to-read code grows bugs.",
    "The exception is algorithmic choice — an O(n²) design is worth fixing on sight."],
 x:{lang:"python", code:"# guessed at:  micro-optimising the loop\nfor i in range(len(items)):        # \"range is faster than enumerate!\"\n    process(items[i])\n\n# measured:  the loop was 0.1% of runtime.\n# 94% was one query, called once per item.\nrows = db.fetch_all(ids)          # one query instead of n"},
 r:["Profiler","Bottleneck","Time Complexity","Technical Debt"]},

{t:"Scope Creep", d:"A task quietly growing past what was agreed, one reasonable-sounding addition at a time.", l:"core", g:["process","crash-course"],
 b:["No single addition is unreasonable. *While you are in there, could you also...* is how a two-day task becomes a three-week one, and because each step was small, nobody made a decision to spend three weeks. That is the whole problem: the cost was never weighed.",
    "The defence is not refusal, it is visibility. *Yes, that is worth doing — it adds about three days, so shall we ship the original scope first and do that next?* You have agreed, priced it, and handed the decision to whoever owns the deadline. The same discipline applies to your own ideas mid-task, which is where most creep actually originates."],
 k:["Each addition seems small; the total was never agreed by anyone.",
    "Do not refuse — price it and make the trade-off visible.",
    "Most creep is self-inflicted: your own *while I am here* ideas.",
    "Write down the original scope; you cannot notice creep without a baseline."],
 x:{lang:"text", code:"Agreed:   add CSV export to the reports page      (2 days)\n+ \"and Excel?\"                                    (+1)\n+ \"and email it on a schedule?\"                   (+4)\n+ \"and let them pick columns?\"                    (+3)\n\nShipped nothing for two weeks. The CSV export\nwould have been live on day two."},
 r:["Minimum Viable Product","Estimation","Technical Specification","Bikeshedding"]},

{t:"Bikeshedding", d:"A group spending far more time on a trivial decision than on the difficult one, because everyone has an opinion about the trivial one.", l:"core", g:["process","crash-course"],
 b:["From Parkinson's example: a committee approving a nuclear power station waves the reactor through in minutes, then argues for an hour about the bike shed. The reactor is too complex for most members to have a view on; the bike shed is not, so that is where the discussion goes.",
    "It shows up constantly in engineering — long debates about naming, formatting or which colour the button should be, while the data model that will be painful for years passes without comment. The tell is when discussion time is inversely proportional to consequence. The cure is usually to make the trivial thing automatic: a formatter ends formatting debates permanently, because there is nothing left to have an opinion about."],
 k:["Debate time is inversely proportional to a decision's real consequence.",
    "Trivial topics attract opinions because everyone can hold one.",
    "Automate the trivia — a formatter ends the formatting argument for good.",
    "Naming it in the room, politely, is usually enough to move on."],
 x:{lang:"text", code:"45 min   what to call the endpoint\n 3 min   whether it should exist at all\n 0 min   what happens when it is called twice"},
 r:["Code Review","Code Formatter","Scope Creep","Standup"]},

{t:"Yak Shaving", d:"The chain of unrelated tasks you must finish before you can start the thing you actually sat down to do.", l:"core", g:["process","crash-course"],
 b:["You want to fix a bug. To reproduce it you need the test suite. The test suite needs a newer Python. The installer needs a system library. The library needs a package manager update, which needs disk space, so now you are deleting old Docker images. Four hours later you have not looked at the bug.",
    "Some of it is genuinely necessary — you often cannot proceed until the yak is shaved. The failure is doing it unconsciously. The habit worth building is to notice the third level of the chain, write down what you were originally doing, and decide deliberately whether to continue or find a shortcut around it."],
 k:["A recursive chain of prerequisites between you and the real task.",
    "Often necessary; the danger is doing it without noticing.",
    "Write down the original goal when you notice you are two levels deep.",
    "Ask whether a shortcut exists — a container or a colleague's setup often is one."],
 x:{lang:"text", code:"fix the login bug\n  -> reproduce it locally\n     -> the test suite will not run\n        -> wrong Python version\n           -> pyenv needs a system library\n              -> package manager is out of date\n                 -> the disk is full\n                    -> deleting old Docker images  <- you are here"},
 r:["Scope Creep","Toil","Debugging","Virtual Environment"]},

{t:"Chesterton's Fence", d:"The rule that you should not remove something until you understand why it was put there.", l:"core", g:["practice","crash-course"],
 b:["Chesterton's image is a fence across a field with no obvious purpose. The reformer says *I see no use for this, let us clear it away*. The wiser reply is: go and find out why it was built, and then we can discuss removing it. Ignorance of the reason is not evidence that there is none.",
    "In code it is the strange `if` nobody understands, the sleep before a call, the seemingly redundant null check. They usually encode a real incident that is no longer visible. `git blame` and the commit message are how you find the fence's builder — and sometimes the answer genuinely is *no longer needed*, at which point you remove it with confidence rather than hope."],
 k:["Understand why something exists before deleting it.",
    "Odd code often encodes a real past incident nobody documented.",
    "`git blame` plus the commit message is how you find the reason.",
    "Once you know the reason and it no longer applies, remove it properly."],
 x:{lang:"python", code:"# do not remove -- looks pointless, and is not\ntime.sleep(0.5)      # <- what is this for?\n\n# git blame says:\n#   \"upstream API rate-limits bursts; 500ms avoids 429s\"\n#\n# now you can fix it properly with real backoff,\n# instead of deleting it and rediscovering the outage."},
 r:["Git Blame","Legacy Code","Technical Debt","Code Review"]},

{t:"Cargo Cult Programming", d:"Copying the form of a solution without understanding it, and expecting the result to follow.", l:"core", g:["practice","crash-course"],
 b:["The phrase comes from post-war Pacific islands where airstrips and control towers were built from bamboo in the hope that cargo planes would return. The shape was right and the mechanism was absent.",
    "In code it is the config block pasted from Stack Overflow that nobody can explain, the `try/except: pass` that makes an error message disappear, the five decorators copied from a tutorial because it worked there. It is not the copying that is the problem — everyone copies. It is shipping it without ever finding out what it does, which means you cannot debug it, cannot safely change it, and cannot tell when it stops being needed."],
 k:["Copying the shape of a solution without its reasoning.",
    "Copying is fine; shipping code you cannot explain is not.",
    "The test: could you explain each line to someone who asked?",
    "`try/except: pass` is the most common example — it hides the problem."],
 x:{lang:"python", code:"# pasted from an answer, never understood\n@retry(3)\n@cache\n@validate\n@log_calls\ndef get_user(uid): ...\n\n# @cache above @retry means failures get cached too.\n# Nobody noticed for six months."},
 r:["Rubber Duck Debugging","Code Review","Technical Debt","Chesterton's Fence"]},

{t:"Bus Factor", d:"The number of people who would have to be unavailable before a project stalls — and it is often one.", l:"core", g:["process","crash-course"],
 b:["Named, grimly, for how many team members could be hit by a bus. A bus factor of one means exactly one person understands the deployment, or the billing logic, or why that service exists. It is a risk measure, and it is usually invisible until the person takes leave.",
    "It is rarely anyone's fault. Knowledge concentrates naturally around whoever built something. The fixes are unglamorous and effective: write the runbook, rotate who handles which area, review across boundaries rather than within them, and pair on anything only one person has ever touched. Documentation written by the person who *does not* know is often the most useful, because it records what was actually confusing."],
 k:["How many people must vanish before work stops. One is common and dangerous.",
    "It concentrates naturally — nobody creates it deliberately.",
    "Runbooks, rotation and cross-boundary review are the practical fixes.",
    "Let the newcomer write the documentation; they know what is unclear."],
 x:{lang:"text", code:"Who can deploy to production?        1  <- risk\nWho understands the billing rules?   1  <- risk\nWho can restore from backup?         0  <- has anyone tried?\nWho can review the frontend?         4"},
 r:["Runbook","Documentation","Legacy Code","On-Call"]},

{t:"Toil", d:"Manual, repetitive work that keeps a system running but never makes it better.", l:"core", g:["process","crash-course"],
 b:["The term comes from Google's SRE practice, and its value is that it names something precisely. Toil is manual, repetitive, automatable, has no lasting value, and grows in proportion to the service. Restarting a service every Monday is toil. Writing the fix so it does not need restarting is not.",
    "The reason it needs a name is that toil is invisible in most planning. It looks like being busy and it feels like working hard, so it rarely appears on a roadmap — while quietly consuming the capacity that would have eliminated it. SRE teams cap it explicitly, often at fifty per cent, precisely so the automation work cannot be permanently deferred."],
 k:["Manual, repetitive, automatable, no lasting value, scales with the system.",
    "It feels productive, which is why it goes unchallenged.",
    "Track it. Untracked toil silently consumes the time that would remove it.",
    "The fix is automation or removing the need — not doing it faster."],
 x:{lang:"text", code:"TOIL           restarting the worker every Monday morning\nNOT TOIL       finding the leak that makes it need restarting\n\nTOIL           copying numbers into a weekly spreadsheet\nNOT TOIL       the query that generates it on demand"},
 r:["Runbook","On-Call","Automation","Yak Shaving"]},

{t:"Dogfooding", d:"Using your own product for real work, so you meet its problems before your users do.", l:"core", g:["process","crash-course"],
 b:["From *eating your own dog food*. If the team building a deployment tool deploys with it, the rough edges get found on Tuesday afternoon rather than in a support ticket. It converts empathy from something you have to remember into something unavoidable.",
    "It has a known blind spot worth stating: you are not a typical user. You know the shortcuts, you have the right permissions, and your data is already set up. Dogfooding reliably catches broken workflows and daily friction; it reliably misses onboarding problems, because you never onboard. Both kinds of testing are needed and they find different things."],
 k:["Use your own product for genuine work, not a scripted demo.",
    "Finds daily friction and broken workflows quickly.",
    "Misses onboarding problems — you are never a new user again.",
    "Pair it with watching an actual new user, in silence."],
 x:{lang:"text", code:"Team ships a CLI.\nNobody on the team uses it -- they run the Python directly.\n\nSix months of tickets:\n  \"--help output is wrong\"\n  \"errors print a stack trace\"\n  \"it hangs with no output on a slow network\"\n\nAll three would have been found in a day of real use."},
 r:["Minimum Viable Product","Human-in-the-Loop","Smoke Test","Feature Flag"]}

]);
