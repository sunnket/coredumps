/* Crash Course, part 6 — the web, the file formats and the tooling everyone
   is assumed to already know.

   Same house rule: plain English, real examples, nothing hand-waved. Same
   scope rule: only what no other category owns. The protocol internals live in
   os-networking and the API design material lives in backend-architecture —
   this is the practical layer between them. */

TD.add("crash-course", [

{t:"URL Anatomy", d:"The parts of a web address, each of which does a different job.", l:"core", g:["web","basics","crash-course"],
 b:["`https://api.example.com:443/v1/users?limit=10&sort=name#results` has six parts. The scheme (`https`) is the protocol. The host (`api.example.com`) is the machine. The port (`443`) is the door, usually hidden because 443 is the default for https. The path (`/v1/users`) is what you want. The query string (`?limit=10&sort=name`) is the options. The fragment (`#results`) is a position within the page.",
    "Two details matter in practice. **The fragment is never sent to the server** — it exists only in the browser, which is why single-page apps once used `#/route`. And the query string is a convention, not a rule: keys and values are just text, and anything unusual in them must be percent-encoded or it will break the parse."],
 k:["scheme://host:port/path?query#fragment — six parts, six jobs.",
    "The fragment never leaves the browser; the server never sees it.",
    "Default ports are hidden: 443 for https, 80 for http.",
    "Query keys may repeat — `?tag=a&tag=b` is normal and parsed as a list."],
 x:{lang:"text", code:"https://api.example.com:443/v1/users?limit=10&sort=name#results\n\\___/   \\_____________/\\__/\\_______/\\________________/\\______/\nscheme       host      port  path        query        fragment\n\n  sent to server:  host, port, path, query\n  never sent:      fragment"},
 r:["URL Encoding","Query Parameter","Port","Endpoint"]},

{t:"URL Encoding", d:"Replacing characters that would break a URL with a `%` and their hex code.", l:"core", g:["web","basics","crash-course"],
 b:["A URL cannot contain a raw space, `&`, `?` or `#`, because each already means something structural. Percent-encoding replaces them: a space becomes `%20`, `&` becomes `%26`, `/` becomes `%2F`. This is why a search for *cats & dogs* arrives as `q=cats%20%26%20dogs`.",
    "The bug this causes is specific and common. If you build a URL by pasting strings together and a value happens to contain `&`, everything after it is parsed as a new parameter — silently, with no error. Always use the library function (`encodeURIComponent` in JavaScript, `urllib.parse.quote` in Python) rather than concatenating, and note that the *component* version is the one that escapes `/` and `&`."],
 k:["Structural characters must be escaped: space `%20`, `&` `%26`, `/` `%2F`.",
    "Never build query strings by concatenation — one `&` in a value breaks it.",
    "`encodeURIComponent` / `quote` for values; the plain versions leave `/` alone.",
    "`+` also means space in query strings, a legacy of HTML forms."],
 x:{lang:"python", code:"from urllib.parse import urlencode\n\n# wrong: an & in the value silently creates a new parameter\nurl = f\"/search?q={query}&limit=10\"\n\n# right: the library escapes everything\nurl = \"/search?\" + urlencode({\"q\": query, \"limit\": 10})\n# /search?q=cats+%26+dogs&limit=10"},
 r:["URL Anatomy","Query Parameter","Encoding","Escaping"]},

{t:"HTTP Status Code Families", d:"The first digit tells you who has the problem, before you look anything up.", l:"core", g:["web","basics","crash-course"],
 b:["There are dozens of codes and you need to recognise five groups. **2xx** succeeded. **3xx** go somewhere else. **4xx** you made a mistake. **5xx** the server made a mistake. **1xx** is rare and informational. Knowing only the first digit already tells you whose bug it is, and that is most of the value.",
    "The distinction that matters when debugging: **4xx means stop and fix your request, 5xx means the request was reasonable and might work if retried.** That is why retry logic should retry 5xx and 429, and never retry a 400 — retrying a malformed request just sends the same malformed request again."],
 k:["2xx worked · 3xx moved · 4xx your fault · 5xx their fault.",
    "401 = who are you? 403 = I know you, and no. Different fixes.",
    "404 not found, 409 conflict, 422 understood but invalid, 429 too many requests.",
    "Retry 5xx and 429. Never retry 4xx — the same request will fail identically."],
 x:{lang:"text", code:"200 OK            it worked\n201 Created       it worked, and something new exists\n301 / 302         moved: permanently / temporarily\n400 Bad Request   malformed -- do not retry\n401 / 403         not authenticated / not allowed\n404 Not Found     no such thing\n429 Too Many      slow down, then retry\n500 / 503         server broke / temporarily unavailable -- retry"},
 r:["Status Code","Request","Response","Rate Limit"]},

{t:"CRUD", d:"Create, Read, Update, Delete — the four operations almost every application performs on its data.", l:"core", g:["web","basics","crash-course"],
 b:["Nearly every feature reduces to these four verbs against some resource. Recognising that is genuinely useful: once you see a screen as *CRUD over orders*, you know roughly what the endpoints, the database calls and the permissions will look like before writing anything.",
    "The four map onto HTTP methods and SQL statements almost one to one, which is why REST feels natural once you notice the pattern. The one that gets skipped is delete — teams often implement a *soft delete* (a `deleted_at` column) instead, because real deletion loses audit history and breaks anything referencing the row."],
 k:["Create, Read, Update, Delete — the four verbs behind most features.",
    "HTTP: POST, GET, PUT/PATCH, DELETE. SQL: INSERT, SELECT, UPDATE, DELETE.",
    "PUT replaces the whole thing; PATCH changes part of it.",
    "*Soft delete* — a `deleted_at` flag — is more common than real deletion."],
 x:{lang:"text", code:"           HTTP              SQL\nCreate     POST   /users     INSERT\nRead       GET    /users/1   SELECT\nUpdate     PUT    /users/1   UPDATE   (replace)\n           PATCH  /users/1   UPDATE   (partial)\nDelete     DELETE /users/1   DELETE"},
 r:["REST","Endpoint","Request","Idempotency"]},

{t:"Rate Limit", d:"A cap on how many requests you may make in a window, and what to do when you hit it.", l:"core", g:["web","tools","crash-course"],
 b:["Services limit you to protect themselves: 60 requests a minute, 10,000 a day, or a token budget. Exceeding it returns **429 Too Many Requests**, usually with a `Retry-After` header saying how long to wait. Treating 429 as a normal failure and retrying immediately makes the situation worse and can extend the block.",
    "The correct response is exponential backoff with jitter: wait 1s, then 2s, then 4s, adding a small random amount so that many clients recovering at once do not synchronise into a new spike. Read `Retry-After` when present and obey it. Most rate-limit outages are self-inflicted by retry loops that had no backoff."],
 k:["429 means slow down; `Retry-After` usually says by how much.",
    "Back off exponentially — 1s, 2s, 4s — and add random jitter.",
    "Jitter matters: without it, every client retries in lockstep.",
    "Check the `X-RateLimit-Remaining` header before you are cut off, not after."],
 x:{lang:"python", code:"import time, random\n\nfor attempt in range(5):\n    r = call_api()\n    if r.status_code != 429:\n        break\n    wait = float(r.headers.get(\"Retry-After\", 2 ** attempt))\n    time.sleep(wait + random.uniform(0, 1))   # jitter"},
 r:["Rate Limiting","Status Code","API Key","Idempotency"]},

{t:"MIME Type", d:"The label saying what kind of data a file or response contains, so the receiver knows how to read it.", l:"core", g:["web","basics","crash-course"],
 b:["`application/json`, `text/html`, `image/png`. Sent as the `Content-Type` header, it tells the browser or client how to interpret the bytes that follow. The extension on a filename is a hint for humans; the MIME type is what software actually acts on.",
    "Two practical consequences. Sending JSON with `text/plain` means many clients will not parse it, producing a *why is my response a string* bug. And trusting a MIME type on an upload is a security hole — the client sets it, so a `.php` file can claim to be `image/png`. Validate the actual content, not the label."],
 k:["`Content-Type` tells the receiver how to interpret the body.",
    "`application/json`, `text/html`, `text/csv`, `image/png`, `application/pdf`.",
    "The wrong type means the client will not parse a perfectly valid body.",
    "Never trust an uploaded file's declared type — check the real content."],
 x:{lang:"text", code:"Content-Type: application/json          parsed as JSON\nContent-Type: text/plain                a plain string, unparsed\nContent-Type: multipart/form-data       file uploads\nContent-Type: application/octet-stream  unknown bytes: download it\n\nCharset matters too:\n  Content-Type: text/html; charset=utf-8"},
 r:["HTTP Header","Response","Encoding","UTF-8"]},

{t:"Redirect", d:"A response that says *not here — go there instead*, which the browser usually follows automatically.", l:"core", g:["web","basics","crash-course"],
 b:["A 3xx status with a `Location` header sends the client somewhere else. The distinction that matters is permanence: **301 is permanent** and browsers and search engines cache it aggressively, while **302 is temporary** and is re-checked each time.",
    "Caching is what makes a wrong 301 painful. Once a browser has cached a permanent redirect, it stops asking the server entirely, and visitors keep going to the old destination long after you fixed it. When unsure, use 302 or 307 — you can always promote it later, but you cannot easily recall a 301."],
 k:["3xx plus a `Location` header; the client follows it.",
    "301 permanent (cached hard) · 302/307 temporary (re-checked).",
    "A mistaken 301 is cached in users' browsers and hard to undo.",
    "307/308 preserve the method; 301/302 may turn a POST into a GET."],
 x:{lang:"text", code:"HTTP/1.1 301 Moved Permanently\nLocation: https://example.com/new-path\n\n301  permanent -- search engines transfer ranking, browsers cache\n302  temporary -- for maintenance pages, A/B tests, login bounces\n307  temporary, and the POST stays a POST"},
 r:["Status Code","URL Anatomy","Cache","Response"]},

{t:"Cookie and Session", d:"A small value the browser stores and sends back on every request, usually to remember who you are.", l:"core", g:["web","basics","crash-course"],
 b:["HTTP itself remembers nothing between requests. A cookie is the workaround: the server sends `Set-Cookie`, the browser stores it and returns it automatically on every subsequent request to that domain. A *session* is the usual use — the cookie holds an opaque id, and the real data sits on the server against that id.",
    "The flags are the security-relevant part, and they are easy to get wrong. `HttpOnly` hides the cookie from JavaScript, which blunts XSS theft. `Secure` sends it only over https. `SameSite` controls whether it is attached to cross-site requests, which is the main defence against CSRF. A session cookie without `HttpOnly` and `SameSite` is a well-known vulnerability, not a stylistic choice."],
 k:["Sent automatically by the browser on every matching request.",
    "A session cookie holds an id; the data lives server-side.",
    "`HttpOnly` blocks JavaScript access · `Secure` requires https · `SameSite` limits cross-site sending.",
    "Because they are sent automatically, they enable CSRF — hence `SameSite`."],
 x:{lang:"text", code:"Set-Cookie: session=a91f...; HttpOnly; Secure; SameSite=Lax; Max-Age=3600\n\nHttpOnly   JavaScript cannot read it  -> limits XSS damage\nSecure     https only                 -> not sent in the clear\nSameSite   not sent cross-site        -> limits CSRF\nMax-Age    seconds until it expires"},
 r:["Stateless","Client","Server","Two-Factor Authentication"]},

{t:"CSV", d:"Comma-separated rows of plain text — the simplest data format, and the one with the most sharp edges.", l:"core", g:["data","formats","crash-course"],
 b:["One line per row, values separated by commas, usually with a header row. Every spreadsheet and database imports it, which is why it refuses to disappear despite having no type system, no encoding declaration and no official specification.",
    "The edge cases are where the time goes. A value containing a comma must be quoted; a quote inside a quoted value is doubled. Excel will helpfully turn `01234` into `1234` and a gene name into a date. And there is no way to declare the character encoding, so a file written in UTF-8 and opened as Latin-1 produces the familiar mojibake. **Use the library's CSV reader, never `split(',')`.**"],
 k:["No types, no encoding declaration, no real specification.",
    "Values containing commas or newlines must be quoted; quotes are doubled.",
    "Never parse with `split(',')` — it breaks on the first quoted comma.",
    "Excel silently reformats: leading zeros vanish, text becomes dates."],
 x:{lang:"python", code:"import csv\n\n# right: handles quotes, embedded commas and newlines\nwith open(\"data.csv\", newline=\"\", encoding=\"utf-8\") as f:\n    for row in csv.DictReader(f):\n        print(row[\"name\"])\n\n# wrong: breaks on  Smith, John, \"Bengaluru, KA\"\n# name, city = line.split(\",\")"},
 r:["Encoding","UTF-8","JSON","Serialization"]},

{t:"UTF-8", d:"The character encoding the whole internet settled on, and why text sometimes turns into question marks.", l:"core", g:["data","formats","crash-course"],
 b:["Computers store numbers; an encoding decides which number means which character. UTF-8 can represent every character in every language, uses one byte for plain English so it stays compact, and is backwards-compatible with ASCII. It is the correct default in every situation, without exception.",
    "Mojibake — `café` appearing as `cafÃ©` — always means the same thing: text written with one encoding was read with another. The fix is never to strip the characters; it is to declare UTF-8 explicitly at every boundary. And remember that in UTF-8 one character is not one byte, so counting bytes to truncate a string can slice a character in half."],
 k:["Variable width: 1 byte for ASCII, up to 4 for anything else.",
    "`cafÃ©` means UTF-8 bytes were read as Latin-1. Fix the declaration, not the text.",
    "Declare it everywhere: file open, database column, HTTP header, HTML meta.",
    "One character ≠ one byte — truncating by bytes can corrupt a character."],
 x:{lang:"python", code:"open(\"f.txt\", encoding=\"utf-8\")          # always be explicit\n\nlen(\"café\")                # 4 characters\nlen(\"café\".encode(\"utf-8\"))  # 5 bytes\n\n# HTML:  <meta charset=\"utf-8\">\n# MySQL: utf8mb4, NOT utf8 -- 'utf8' there is 3-byte and breaks emoji"},
 r:["Encoding","CSV","Base64","MIME Type"]},

{t:"Line Endings", d:"The invisible characters that end each line, and differ between Windows and everything else.", l:"core", g:["tools","formats","crash-course"],
 b:["Unix and macOS end a line with `\\n`. Windows uses `\\r\\n`. The difference is invisible in most editors and causes a specific family of baffling problems: a shell script that fails with a strange character in the error, a diff showing every line changed when nothing was, a file that looks identical but does not match.",
    "Git can normalise this with the `core.autocrlf` setting, and a `.gitattributes` file is the durable fix because it applies to everyone who clones rather than depending on each person's local config. The one to remember: a `.sh` file with Windows line endings fails with `bad interpreter: /bin/bash^M`, because the `\\r` became part of the interpreter path."],
 k:["`\\n` on Unix/macOS, `\\r\\n` on Windows. Both invisible.",
    "`bad interpreter: ...^M` always means CRLF in a shell script.",
    "A whole-file diff with no visible change is usually line endings.",
    "Commit a `.gitattributes` — it applies to everyone, unlike local config."],
 x:{lang:"text", code:"# .gitattributes -- the durable fix\n* text=auto\n*.sh text eol=lf        # shell scripts must be LF, always\n*.bat text eol=crlf\n\n# inspect what you have:\nfile script.sh\n#  script.sh: Bourne-Again shell script, ASCII text, with CRLF line terminators"},
 r:["Encoding","UTF-8","Diff","Shell"]},

{t:"Markdown", d:"Plain text with light punctuation that renders as formatted documents — the format READMEs, docs and chat are written in.", l:"core", g:["tools","formats","crash-course"],
 b:["`# heading`, `**bold**`, `` `code` ``, `- list`. It is readable as plain text and renders as rich text, which is why it became the default for READMEs, documentation sites, issue trackers and chat.",
    "The practical caveat is that *Markdown* is not one thing. GitHub Flavored Markdown adds tables, task lists and fenced code blocks with syntax highlighting; other renderers support different subsets. Fenced blocks with a language tag are the single most useful feature — always tag the language, because it turns an unreadable wall into highlighted code."],
 k:["`#` heading · `**bold**` · `` `code` `` · `- list` · `[text](url)`.",
    "Dialects differ; GitHub Flavored Markdown is the de facto standard.",
    "Always tag the language on a fenced code block for highlighting.",
    "A blank line separates paragraphs — a single newline usually does nothing."],
 x:{lang:"text", code:"# Project\n\nInstall with `pip install thing`.\n\n- works on Linux\n- **requires** Python 3.10+\n\n```python\nfrom thing import run     # tag the language\nrun()\n```\n\n| Option | Default |\n|--------|---------|\n| retries| 3       |"},
 r:["README","Documentation","Changelog","Docstring"]},

{t:"Escaping", d:"Marking a character as *literal text* so the surrounding system does not treat it as an instruction.", l:"core", g:["basics","formats","crash-course"],
 b:["Every system has characters that mean something: quotes in code, `<` in HTML, `%` in SQL LIKE, `.` in a regex, `$` in a shell. Escaping tells that system *this one is data, not syntax* — usually with a backslash, sometimes by doubling it.",
    "This is not a formatting nicety; it is the mechanism behind an entire class of security holes. SQL injection, XSS and shell injection are all *unescaped data interpreted as instructions*. The reliable defence is never to escape by hand but to use the tool that does it for you: parameterised queries, a templating engine that escapes by default, and argument lists rather than shell strings."],
 k:["Marks a character as data rather than syntax, usually with `\\`.",
    "Injection bugs are exactly *unescaped data read as instructions*.",
    "Never escape by hand — use parameterised queries and auto-escaping templates.",
    "Different contexts need different escaping; HTML rules are not SQL rules."],
 x:{lang:"python", code:"# shell/SQL/HTML all have their own escaping. Do not hand-roll any of them.\n\n# WRONG -- SQL injection\ncur.execute(f\"SELECT * FROM users WHERE name = '{name}'\")\n\n# RIGHT -- the driver escapes it\ncur.execute(\"SELECT * FROM users WHERE name = %s\", (name,))\n\n# subprocess: a list, never a shell string\nsubprocess.run([\"grep\", pattern, path])"},
 r:["Escape Character","URL Encoding","Regular Expression","Sanitisation"]},

{t:"Debugger and Breakpoints", d:"Pausing a running program at a chosen line to inspect what every variable actually contains.", l:"core", g:["tools","debugging","crash-course"],
 b:["A breakpoint stops execution at a line. While paused you can read every variable, walk up the call stack to see who called this function with what, and step forward one line at a time. It answers *what is actually in this variable right now*, which is nearly always the question.",
    "Most people rely on print statements far longer than they should. Prints are fine for a quick check, but a debugger shows you everything without deciding in advance what to print — and re-running to add one more print is the slow loop a debugger removes. The Python entry point is now trivially `breakpoint()`; every editor has a button that does the same."],
 k:["Breakpoint pauses execution; then inspect variables and the call stack.",
    "Step over, step into, continue — the three controls you need.",
    "A conditional breakpoint fires only when a condition is true — ideal inside a loop.",
    "`breakpoint()` in Python, `debugger` in JavaScript, or your editor's gutter."],
 x:{lang:"python", code:"def process(order):\n    total = compute(order)\n    breakpoint()          # execution stops here\n    return charge(total)\n\n# at the prompt:\n#   total        inspect it\n#   order.items  and anything else in scope\n#   n            next line\n#   c            continue"},
 r:["Debugging","Stack Trace","Log Level","Profiler"]},

{t:"Log Level", d:"The severity tag on each log line that lets you turn detail up when debugging and down in production.", l:"core", g:["tools","debugging","crash-course"],
 b:["DEBUG, INFO, WARNING, ERROR, CRITICAL. You set a threshold and anything below it is discarded. Production usually runs at INFO, so the DEBUG lines cost nothing; when something breaks, you lower the threshold and the detail appears without changing any code.",
    "The discipline is choosing the right level as you write. ERROR should mean *a human must look at this*, so logging every handled exception as ERROR makes the alerting useless — a phenomenon known as alert fatigue. Use WARNING for *recovered, but suspicious*, and INFO for the handful of events that describe normal operation."],
 k:["DEBUG < INFO < WARNING < ERROR < CRITICAL; set a threshold, discard below it.",
    "ERROR means a human must act. Overusing it destroys your alerting.",
    "Log structured key-value pairs, not sentences — they can be searched and aggregated.",
    "Never log secrets, tokens or personal data; logs are widely readable."],
 x:{lang:"python", code:"import logging\nlog = logging.getLogger(__name__)\n\nlog.debug(\"payload=%s\", payload)        # off in production\nlog.info(\"order placed\", extra={\"order_id\": oid})\nlog.warning(\"retrying after timeout\", extra={\"attempt\": n})\nlog.error(\"payment failed\", exc_info=True)   # include the traceback"},
 r:["Debugger and Breakpoints","Stack Trace","Metric","Runbook"]},

{t:"Profiler", d:"A tool that measures where a program actually spends its time, instead of where you assume it does.", l:"core", g:["tools","debugging","crash-course"],
 b:["A profiler runs your code and reports the time spent in each function, along with how often each was called. It replaces guessing, and the result is regularly surprising — the loop you were about to optimise turns out to be 0.3% of runtime while a logging call inside it is 40%.",
    "Two things worth knowing. Profile something representative: optimising against a ten-row test tells you nothing about a million rows. And distinguish total time from time excluding sub-calls — a function that looks expensive is often just the one that calls the expensive thing, and the *cumulative* versus *own* columns are what separate them."],
 k:["Measures real time per function; replaces guessing about bottlenecks.",
    "Profile realistic data — small inputs hide the actual problem.",
    "*Cumulative* includes callees; *own* time is the function itself.",
    "Optimise the top entry, measure again, and stop when it is fast enough."],
 x:{lang:"python", code:"python -m cProfile -s cumtime app.py | head -20\n\n#   ncalls  tottime  cumtime  filename:lineno(function)\n#    50000    0.021   12.400  api.py:44(fetch_user)   <- 50,000 calls!\n#        1    0.001   12.410  app.py:10(main)\n#\n# not slow code -- an N+1 query. Batch it."},
 r:["Premature Optimisation","Bottleneck","Time Complexity","Benchmark"]},

{t:"Hot Reload", d:"The development setup where saving a file updates the running application immediately, without a restart.", l:"core", g:["tools","crash-course"],
 b:["A watcher notices the file changed and either restarts the process or swaps the changed module in place. The value is the feedback loop: a change you see in half a second is a fundamentally different experience from one that takes twenty, and it shapes how much you experiment.",
    "It is a development tool only — never enable it in production, where the file watching and the reload machinery are both overhead and risk. Its known failure is stale state: hot module replacement can leave the app in a state that a fresh start would never produce, so *have you tried a full restart?* is the right first question when something makes no sense."],
 k:["A file watcher restarts or swaps modules as you save.",
    "The short feedback loop changes how much you are willing to try.",
    "Development only — never in production.",
    "When behaviour makes no sense, do a full restart before debugging further."],
 x:{lang:"bash", code:"uvicorn app:main --reload      # FastAPI\nflask run --debug              # Flask\nnpm run dev                    # Vite / Next.js\nnodemon server.js              # generic Node\n\n# --reload is a DEV flag. It must not reach production."},
 r:["Runtime","Build","Framework","Localhost"]},

{t:"Estimation", d:"Saying how long work will take, knowing you will be wrong, in a way that is still useful.", l:"core", g:["process","crash-course"],
 b:["Software estimates are famously poor, and mostly for a structural reason: you are estimating work you have not yet fully understood, since understanding it *is* the work. Estimates also default to the path where nothing goes wrong, which is never the path.",
    "Two habits help more than any technique. Give a range rather than a number — *three to five days* is honest where *four days* is false precision. And break the task down until each piece is under a day; the sum of small estimates is reliably better than one large guess, because the unknowns become visible as you decompose. Anything you cannot break down is a signal to timebox an investigation first."],
 k:["Give a range. A single number claims precision you do not have.",
    "Break work down until each piece is under a day, then add them up.",
    "Estimates assume nothing goes wrong; something always does.",
    "Cannot break it down? Timebox a spike to learn enough to estimate."],
 x:{lang:"text", code:"BAD   \"the import feature: about a week\"\n\nBETTER\n  parse the CSV                    0.5d\n  validate + error report          1d\n  write to the database            0.5d\n  handle partial failure           1d   <- the one nobody counts\n  tests                            1d\n  review + fixes                   0.5d\n                                   ----\n                                   4.5d, call it 4-6 days"},
 r:["Scope Creep","Technical Specification","Minimum Viable Product","Standup"]},

{t:"Technical Specification", d:"A short written plan, circulated before the work starts, describing what you intend to build and why.", l:"core", g:["process","crash-course"],
 b:["Also called a design doc or an RFC. It states the problem, the proposed approach, the alternatives considered and why they were rejected, and what could go wrong. Length is not the point — two pages that provoke the right objection are worth more than twenty nobody reads.",
    "The value arrives before anyone builds anything. Writing forces the vague parts into the open, and circulating it lets someone say *this breaks the billing service* while that costs an afternoon rather than three weeks. The alternatives section is the most useful and the most skipped: it prevents the same discussion recurring for a year, because the reasoning is on record."],
 k:["Problem, proposal, alternatives considered, risks. Two pages beats twenty.",
    "Its value is the objection you get before writing code, not the document.",
    "Record why you rejected the alternatives — it ends the debate permanently.",
    "If you cannot write down the problem clearly, you are not ready to build."],
 x:{lang:"text", code:"PROBLEM      report generation blocks the request for 40s\nPROPOSAL     queue it, email a link when ready\nALTERNATIVES\n  optimise the query    -- tried; still 12s at p99\n  cache the report      -- inputs change per user, low hit rate\n  stream partial output -- big frontend change for little gain\nRISKS        queue backlog at month-end; needs a dead-letter queue\nNOT DOING    scheduled reports (next quarter)"},
 r:["Estimation","Architecture Decision Record","Documentation","Scope Creep"]}

]);
