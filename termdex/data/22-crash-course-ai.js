/* Crash Course, part 4 — AI engineering in plain English.
   The words that come up daily once you start building with models, explained
   without assuming you have read a paper. */

TD.add("crash-course", [

{t:"Weights", a:"parameters", d:"The numbers inside a model that were adjusted during training — everything the model knows lives here.", l:"core", g:["ai","basics","crash-course"],
 b:["A model is not a program somebody wrote. It is a fixed piece of arithmetic with a very large number of adjustable numbers in it, and training is the process of nudging those numbers until the output stops being wrong. Downloading a model means downloading the weights.",
    "There is no database inside, no lookup table, no copy of the training text. A fact the model appears to know is a pattern compressed into billions of numbers, which is exactly why models produce confident, fluent, entirely wrong answers — and why retrieval exists, to put real documents in front of the model at question time."],
 k:["The learned numbers; training adjusts them, inference only reads them.",
    "No stored facts, only compressed patterns — hence hallucination.",
    "Open-weights means you can download them; open-source also means the data and code.",
    "File size roughly tracks parameter count times bytes per parameter."],
 r:["Parameter Count","Training","Checkpoint","Hallucination"]},

{t:"Parameter Count", a:"7B, 70B, 405B", d:"How many adjustable numbers a model has — the headline figure in every model name.", l:"core", g:["ai","basics","crash-course"],
 b:["The B stands for billion. A 7B model has roughly seven billion weights, and that number drives everything practical: how much memory it needs, how fast it runs, and roughly how capable it is. At full 16-bit precision the rule of thumb is two gigabytes of memory per billion parameters, so a 7B model needs about 14 GB before you have loaded a single token of context.",
    "Bigger is not automatically better any more. A well-trained recent 8B model routinely outperforms a 70B model from two years ago, because training data quality, training length and post-training matter as much as size. Parameter count tells you what it costs to run, and only loosely what it can do."],
 k:["B means billion; it drives memory, speed and price.",
    "Roughly 2 GB per billion parameters at 16-bit precision.",
    "Quantisation cuts that to about 0.5 GB per billion at 4-bit.",
    "Recent small models often beat older large ones."],
 x:{lang:"python", code:"# rough memory needed just to hold the weights\ndef vram_gb(params_b, bits=16):\n    return params_b * bits / 8\n\nprint(vram_gb(7))        # 14.0 GB at fp16\nprint(vram_gb(7, 4))     # 3.5 GB at 4-bit"},
 r:["Weights","VRAM","Quantisation","Inference Cost"]},

{t:"Training", d:"Showing a model example after example and adjusting its weights each time until its answers stop being wrong.", l:"core", g:["ai","basics","crash-course"],
 b:["The loop is the same everywhere: feed in an example, compare the model's output to the right answer, measure how wrong it was, and nudge every weight a little in the direction that would have made it less wrong. Repeat billions of times. That is gradient descent, and it is the entire mechanism.",
    "For large language models the process has stages. Pre-training on enormous amounts of text builds general capability and costs millions. Post-training — instruction tuning and preference alignment — makes the result helpful and safe, and is far cheaper. As an application engineer you will almost never do the first and quite often do a light version of the second."],
 k:["Predict, measure the error, adjust the weights, repeat.",
    "Pre-training builds capability; post-training makes it usable.",
    "Training changes the weights; inference does not.",
    "Most product work needs prompting or retrieval, not training."],
 r:["Weights","Inference","Learning Rate","Fine-Tuning","Gradient Descent"]},

{t:"Dataset", d:"The collection of examples a model learns from, or that you evaluate it against.", l:"core", g:["ai","basics","crash-course"],
 b:["Model quality is mostly dataset quality. Ten thousand carefully labelled, diverse, correctly formatted examples beat a hundred thousand noisy ones, and no amount of architecture cleverness rescues a dataset that does not represent the situation the model will actually meet.",
    "It is always split into at least two parts, and usually three: training, which the model learns from; validation, used to tune settings; and test, which is looked at once at the end. Any leakage between them produces excellent numbers and a model that fails in production — which is the single most common way a machine-learning project quietly goes wrong."],
 k:["Data quality dominates model quality.",
    "Split into training, validation and test — and keep them separate.",
    "Leakage between splits inflates scores and hides failure.",
    "Whatever is missing from the dataset is missing from the model."],
 r:["Label","Golden Dataset","Training","Data Leakage"]},

{t:"Label", a:"ground truth", d:"The correct answer attached to a training example — what the model is supposed to output.", l:"core", g:["ai","basics","crash-course"],
 b:["Supervised learning needs pairs: this email and the label spam, this image and the label cat, this question and the answer you wanted. The label is the target the model is trying to reproduce, and labelling is usually the slowest, most expensive part of any machine-learning project.",
    "Labels are also where quiet errors enter. If two human annotators disagree fifteen percent of the time, no model trained on their work will exceed roughly that accuracy — the ceiling is in the data, not the algorithm. Measuring inter-annotator agreement before training is time extremely well spent."],
 k:["The correct answer paired with an input example.",
    "Labelling is typically the biggest cost in a project.",
    "Annotator disagreement puts a hard ceiling on achievable accuracy.",
    "Ambiguous labelling guidelines produce inconsistent labels."],
 r:["Dataset","Target Variable","Supervised Learning","Golden Dataset"]},

{t:"Target Variable", d:"The thing you are trying to predict — the column in your data that everything else is used to explain.", l:"core", g:["ai","basics","crash-course"],
 b:["Features are the inputs; the target is the output. Predicting churn, the target is whether the customer left. Predicting price, the target is the price. Everything else in the table is a feature, and the modelling problem is simply learning the mapping between them.",
    "Choosing the target well is half the work and it is a product decision, not a technical one. Predicting will-this-customer-churn-in-90-days is a different problem from will-they-ever-churn, with different data and different usefulness. And it is easy to accidentally include a feature that is really a consequence of the target — which produces a spectacular score and a useless model."],
 k:["The output column; features are the inputs.",
    "Defining it precisely is a product decision.",
    "A feature that leaks the target produces fake accuracy.",
    "If it is unavailable at prediction time, it cannot be a feature."],
 r:["Label","Feature","Dataset","Data Leakage"]},

{t:"Checkpoint", d:"A saved snapshot of a model's weights partway through training, so you can resume or roll back.", l:"core", g:["ai","basics","crash-course"],
 b:["Training runs take hours or weeks and machines fail. Saving the weights every so often means a crash costs you the last few minutes rather than the whole run. That is the practical reason checkpoints exist.",
    "The other reason is that later is not always better. Models overfit — the training score keeps improving while the validation score turns and gets worse. Keeping checkpoints lets you go back and take the one from just before that turn, which is exactly what early stopping automates."],
 k:["A resumable snapshot of weights and optimiser state.",
    "Protects long runs from crashes and preemption.",
    "Keep the best-on-validation checkpoint, not the last one.",
    "The published model you download is somebody's chosen checkpoint."],
 r:["Training","Weights","Early Stopping","Overfitting"]},

{t:"VRAM", a:"GPU memory", d:"The memory on a graphics card — the hard limit on which models you can run at all.", l:"core", g:["ai","hardware","crash-course"],
 b:["A GPU can only work on data sitting in its own memory. The weights must fit, plus the activations for whatever you are processing, plus the key-value cache that grows with context length. If it does not fit, it does not run — the failure is immediate and absolute, an out-of-memory error rather than slow performance.",
    "Rough numbers help: a 7B model at 16-bit needs about 14 GB, at 4-bit about 4 GB. A 24 GB consumer card comfortably runs a quantised 30B. Long contexts are the sneaky consumer, because the key-value cache scales with sequence length and can end up rivalling the weights themselves."],
 k:["Weights plus activations plus KV cache must all fit.",
    "Out of memory is a hard failure, not a slowdown.",
    "Quantisation is the usual way to make a model fit.",
    "Long context inflates the KV cache substantially."],
 r:["Parameter Count","Quantisation","GPU","Context Window"]},

{t:"Zero-Shot Learning", d:"Asking a model to do a task it was never explicitly trained on, with no examples in the prompt.", l:"core", g:["ai","basics","crash-course"],
 b:["Classify this ticket as billing, technical or other — no examples, just the instruction. Large models handle this surprisingly well because the task resembles things they saw during pre-training. It is where every project should start, because it costs one prompt and takes five minutes.",
    "When zero-shot is not accurate enough, the ladder is well established and worth climbing in order: add a few examples to the prompt, then add retrieval so the model has the relevant facts, then fine-tune. Each step costs more and is harder to change, so stop as soon as the quality is good enough."],
 k:["No examples in the prompt — just the instruction.",
    "Always the first thing to try; it takes minutes.",
    "The ladder: zero-shot, few-shot, retrieval, fine-tuning.",
    "Clear output-format instructions matter more than clever wording."],
 r:["Few-Shot Prompting","Prompt Template","Fine-Tuning","Retrieval-Augmented Generation"]},

{t:"Prompt Template", d:"A reusable prompt with holes in it, filled with real values at request time.", l:"core", g:["ai","basics","crash-course"],
 b:["Once a prompt works, it stops being a sentence you type and becomes part of your code. A template separates the fixed instructions from the variable inputs, so you can version it, test it, and change the wording in one place rather than in fourteen string concatenations.",
    "The rule that matters is the same one that governs SQL: never paste untrusted input into the instruction section. User content belongs in a clearly delimited data section, with the instructions above it stating that the content below is data to be processed, not commands to be followed. That is the basic mitigation for prompt injection."],
 k:["Separates fixed instructions from variable input.",
    "Version prompts like code — they are code.",
    "Delimit user content clearly and label it as data.",
    "A prompt change is a deploy; test it before shipping."],
 x:{lang:"python", code:"TEMPLATE = \"\"\"Classify the support ticket into: billing, technical, other.\nReply with one word only.\n\n<ticket>\n{ticket}\n</ticket>\"\"\"\n\nprompt = TEMPLATE.format(ticket=user_text)"},
 r:["Prompt Engineering","System Message","Prompt Injection","Zero-Shot Learning"]},

{t:"System Message", a:"system prompt", d:"The standing instructions given to a model before the conversation starts — its role, rules and output format.", l:"core", g:["ai","basics","crash-course"],
 b:["Chat models take messages with roles: system, user and assistant. The system message sets the frame for everything that follows — who the model is acting as, what it must always or never do, and what shape its answers should take. Users do not see it; the model treats it as higher priority than user turns.",
    "Keep it specific and short. A system prompt that runs to a thousand words of contradictory guidance produces worse results than one paragraph of concrete rules, costs tokens on every single call, and is much harder to debug when the behaviour drifts. And it is instruction, not security: it discourages misbehaviour, it does not enforce anything."],
 k:["Standing instructions with higher priority than user messages.",
    "Set role, constraints and output format.",
    "Short and concrete beats long and vague.",
    "Not a security boundary — enforce rules in your own code."],
 x:{lang:"python", code:"messages = [\n  {\"role\": \"system\",\n   \"content\": \"You are a terse SQL assistant. Reply with one query, no prose.\"},\n  {\"role\": \"user\", \"content\": \"Top 5 customers by revenue this quarter\"},\n]"},
 r:["Prompt Template","Prompt Engineering","Guardrails","Prompt Injection"]},

{t:"Max Tokens", d:"The cap on how much the model is allowed to generate in one response.", l:"core", g:["ai","basics","crash-course"],
 b:["It limits output, not input. Set it too low and answers stop mid-sentence — the classic symptom of a response that just trails off with no punctuation. Set it very high and you cap nothing, which matters because you are billed for what is generated.",
    "Two things people confuse. The context window covers input plus output together, so a long prompt leaves less room for the answer. And the finish reason in the response tells you which happened: stop means the model finished naturally, length means it hit your cap — always check it before assuming the answer is complete."],
 k:["Caps output tokens only; the context window covers both.",
    "Truncated mid-sentence answers usually mean this is too low.",
    "Check the finish reason: `stop` versus `length`.",
    "It is your main defence against a runaway generation bill."],
 r:["Token","Context Window","Cost Per Token","Stop Sequence"]},

{t:"Stop Sequence", d:"A string that tells the model to stop generating the moment it produces it.", l:"core", g:["ai","basics","crash-course"],
 b:["Useful when you want exactly one item and the model has a habit of continuing into a second. Give it a stop sequence of a double newline, or a closing tag you asked it to emit, and generation halts there. The stop string itself is not included in the returned text.",
    "It is a cheap way to keep responses tight and predictable, and it composes well with structured output: ask for a closing tag, stop on that tag, and you get a clean parse. It is not a substitute for validating what came back — the model can still produce malformed content before it reaches the stop."],
 k:["Generation halts as soon as the string appears.",
    "The stop string is excluded from the returned text.",
    "Pairs well with a required closing tag.",
    "Still validate the output — stopping early is not correctness."],
 r:["Max Tokens","Structured Output","Streaming Response"]},

{t:"Greedy Decoding", d:"Always picking the single most likely next token — the most predictable, least creative way to generate.", l:"core", g:["ai","basics","crash-course"],
 b:["At every step the model produces a probability for every possible next token. Greedy decoding simply takes the top one. It is what temperature zero does in practice, and it is the right choice for classification, extraction, code and anything where you want the same input to give the same output.",
    "The downsides are real. Greedy text is repetitive and can get stuck in loops, and it is not globally optimal — the highest-probability token at each step does not produce the highest-probability sentence. Sampling with temperature, top-k or top-p trades that determinism for variety, which is what you want for anything creative."],
 k:["Always take the highest-probability token — no randomness.",
    "Temperature zero is effectively greedy decoding.",
    "Right for classification, extraction and code.",
    "Locally optimal, not globally — and prone to repetition."],
 r:["Temperature","Top-k Sampling","Top-p Sampling","Determinism"]},

{t:"Streaming Response", d:"Sending the answer token by token as it is produced, instead of waiting for the whole thing.", l:"core", g:["ai","basics","crash-course"],
 b:["A model generates one token at a time, so a 500-token answer takes several seconds no matter what. Streaming shows the first words in a few hundred milliseconds and the rest as they arrive. Total time is identical; perceived speed is transformed, which is why every chat interface streams.",
    "It complicates the code in specific ways worth planning for: you cannot validate JSON until the stream ends, errors can arrive halfway through an answer already on screen, and you need a way for the user to stop generation. Transport is usually server-sent events, which is simply a long-lived HTTP response."],
 k:["Same total time, dramatically better perceived latency.",
    "Cannot validate structured output until the stream completes.",
    "Handle mid-stream errors and give the user a stop button.",
    "Usually delivered over server-sent events."],
 x:{lang:"python", code:"with client.messages.stream(model=MODEL, messages=msgs) as stream:\n    for text in stream.text_stream:\n        print(text, end=\"\", flush=True)"},
 r:["Time To First Token","Tokens Per Second","Latency","Structured Output"]},

{t:"Time To First Token", a:"TTFT", d:"How long you wait before the first word appears — the number users actually feel.", l:"core", g:["ai","basics","crash-course"],
 b:["Two latency numbers matter for a language model and they behave differently. Time to first token covers queueing plus processing the whole prompt; after that, tokens arrive at a steady rate. Users judge responsiveness almost entirely on the first number.",
    "Which is useful, because the levers are different. A shorter prompt cuts time to first token directly, and prompt caching can cut it dramatically for repeated system prompts. Making the answer shorter does not help it at all — that only affects total time."],
 k:["Queue time plus prompt processing, before any output.",
    "Dominates perceived speed; total time matters far less.",
    "Shorter prompts and prompt caching reduce it.",
    "Shortening the answer does not improve it."],
 r:["Streaming Response","Tokens Per Second","Latency","Context Window"]},

{t:"Tokens Per Second", a:"throughput", d:"How fast a model produces output once it has started — the reading-speed number.", l:"core", g:["ai","basics","crash-course"],
 b:["Comfortable human reading is roughly 10 to 15 tokens per second, so anything above about 20 feels instantaneous when streaming. Hosted frontier models typically land somewhere between 30 and 100; a quantised model on a laptop might manage 10 to 30.",
    "For serving, there are two different rates and confusing them causes bad capacity planning. Per-request speed is what one user experiences; total system throughput is what the hardware delivers across all concurrent requests. Batching raises the second sharply while slightly lowering the first, which is exactly the trade a serving stack is tuned around."],
 k:["Above about 20 per second feels instant while streaming.",
    "Per-request speed and total system throughput are different numbers.",
    "Batching improves total throughput at a small per-request cost.",
    "Quantisation usually raises speed and lowers memory."],
 r:["Time To First Token","Streaming Response","Throughput","Quantisation"]},

{t:"Cost Per Token", d:"What you pay per unit of text — priced separately for input and output, with output several times dearer.", l:"core", g:["ai","basics","crash-course"],
 b:["Prices are quoted per million tokens, and output typically costs three to five times input, because generating requires a forward pass per token while the prompt is processed in parallel. Roughly 750 English words is a thousand tokens; other languages and code cost noticeably more per word.",
    "The bill in real applications is usually dominated by input, not output, because conversation history and retrieved documents get resent on every single turn. The three levers that actually move the number: trim what you resend, cache stable prefixes like a long system prompt, and route easy requests to a smaller model."],
 k:["Output tokens cost several times more than input tokens.",
    "Roughly 750 English words per 1,000 tokens; code and non-English cost more.",
    "Resent conversation history usually dominates the bill.",
    "Prompt caching and model routing are the biggest savings."],
 x:{lang:"python", code:"def cost(in_tok, out_tok, in_price, out_price):   # prices per 1M tokens\n    return (in_tok * in_price + out_tok * out_price) / 1_000_000\n\n# 100k requests, 2k in / 300 out\nprint(cost(2_000 * 100_000, 300 * 100_000, 3.0, 15.0))"},
 r:["Token","Inference Cost","Max Tokens","Context Window"]},

{t:"Tool Use", a:"function calling", d:"Letting a model call your code — it says which function and with what arguments, you run it and hand back the result.", l:"core", g:["ai","agents","crash-course"],
 b:["The model does not execute anything. You describe the tools available — name, description, argument schema — and instead of answering in prose the model can emit a structured request to call one. Your code runs it, returns the result as another message, and the model continues with real information in hand.",
    "This is what turns a text generator into something that can check today's stock level, send an email or query a database. It also means the model's output is now deciding what your code does, so every tool needs validated arguments, narrow permissions, and confirmation for anything destructive. Treat a tool call exactly like a request from an untrusted client."],
 k:["The model requests a call; your code decides whether to run it.",
    "Tool descriptions are prompt — write them carefully.",
    "Validate every argument; never pass them straight into a shell or SQL.",
    "Require confirmation for anything irreversible."],
 x:{lang:"python", code:"tools = [{\n  \"name\": \"get_stock\",\n  \"description\": \"Current stock level for a SKU.\",\n  \"input_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\"sku\": {\"type\": \"string\"}},\n    \"required\": [\"sku\"]\n  }\n}]"},
 r:["Function Calling","AI Agent","Structured Output","Model Context Protocol"]},

{t:"Golden Dataset", a:"eval set", d:"A small, hand-checked set of inputs and known-good outputs you test every prompt or model change against.", l:"core", g:["ai","evaluation","crash-course"],
 b:["Without one, prompt engineering is guesswork: you change a sentence, the two examples you tried look better, and you have no idea what you broke. Fifty to two hundred carefully chosen cases turn that into measurement — run them before and after and see the actual difference.",
    "Composition matters more than size. Include the ordinary cases, the failures you have actually seen in production, and the awkward edges. Grow it every time something goes wrong in the wild: each real failure that becomes a permanent test case is a bug that cannot silently return."],
 k:["A fixed set of inputs with verified expected outputs.",
    "Fifty to two hundred well-chosen cases is usually enough.",
    "Add every production failure to it permanently.",
    "Run it on every prompt, model or parameter change."],
 r:["LLM Evaluation","Dataset","Human-in-the-Loop","Regression"]},

{t:"Determinism", d:"Whether the same input reliably produces the same output — and with language models, it usually does not.", l:"core", g:["ai","basics","crash-course"],
 b:["Ordinary code is deterministic: same input, same output, every time. Model inference is not, unless you force it. Sampling introduces randomness by design, and even at temperature zero, floating-point non-associativity and varying batch composition on the server can produce slightly different results run to run.",
    "This changes how you test. Asserting on an exact output string produces a permanently flaky test. The workable approaches are asserting on structure and properties rather than exact text, using a model-as-judge for open-ended output, and pinning a specific model version so a silent upgrade does not shift behaviour underneath you."],
 k:["Temperature zero reduces variation but does not guarantee identical output.",
    "Never assert on exact generated text in a test.",
    "Assert on structure, schema and properties instead.",
    "Pin the model version; a silent upgrade is a silent behaviour change."],
 r:["Random Seed","Temperature","Greedy Decoding","Flaky Test"]},

{t:"Random Seed", d:"A starting number for a random number generator, so a random process can be repeated exactly.", l:"core", g:["ai","basics","crash-course"],
 b:["Computers produce pseudo-random numbers: a deterministic sequence that looks random. Give it the same seed and you get the same sequence, which is what makes a shuffled dataset split, a weight initialisation or an image generation reproducible.",
    "Set it everywhere for experiments — Python, NumPy, the framework, the data loader — or your results will not be comparable between runs. But do not read too much into a single seeded run: if changing the seed changes your conclusion, you have measured noise, and the honest report is a mean and a spread across several seeds."],
 k:["Same seed, same sequence — reproducible randomness.",
    "Seed every library involved, not just one.",
    "Never use a normal random generator for tokens or passwords.",
    "If the seed changes your conclusion, you measured noise."],
 x:{lang:"python", code:"import random, numpy as np, torch\n\ndef seed_all(s=42):\n    random.seed(s); np.random.seed(s); torch.manual_seed(s)"},
 r:["Determinism","Reproducibility","Dataset","Ablation Study"]},

{t:"Embedding Dimension", d:"How many numbers each embedding vector contains — the size of the coordinate space meaning lives in.", l:"core", g:["ai","basics","crash-course"],
 b:["An embedding turns a piece of text into a list of numbers, and the dimension is the length of that list — commonly 384, 768, 1536 or 3072. More dimensions can capture finer distinctions; they also cost proportionally more storage, more memory in the index and more time per comparison.",
    "The operational rule is that dimensions must match exactly. You cannot compare a vector from one model with a vector from another, or even from a different version of the same model. Changing the embedding model means re-embedding the entire corpus, which for a large index is a genuine migration to plan rather than a config change."],
 k:["The length of the vector — typically 384 to 3072.",
    "Vectors from different models are not comparable, ever.",
    "Changing model means re-embedding everything.",
    "Some models support truncating dimensions to trade accuracy for cost."],
 r:["Embedding","Vector Database","Cosine Similarity","Semantic Search"]},

{t:"Chunk Overlap", d:"Repeating a little of the previous chunk at the start of the next one, so meaning is not cut in half.", l:"core", g:["ai","rag","crash-course"],
 b:["Retrieval systems split documents into chunks before embedding them. Split naively and a sentence explaining a rule ends up in one chunk while the rule itself sits in the next, and neither retrieves well. Overlapping by roughly ten to twenty percent means the boundary appears in both, so the idea survives whichever chunk is retrieved.",
    "Overlap costs storage and can return near-duplicate results, so it is a dial rather than a free win. Splitting on natural boundaries — headings, paragraphs, function definitions — usually helps more than tuning the overlap number, because it stops meaning being cut arbitrarily in the first place."],
 k:["Repeat 10–20% of the previous chunk at the start of the next.",
    "Prevents ideas being severed at a boundary.",
    "Costs storage and can surface near-duplicate results.",
    "Splitting on headings and paragraphs matters more than the exact number."],
 x:{lang:"python", code:"def chunk(text, size=800, overlap=120):\n    step = size - overlap\n    return [text[i:i+size] for i in range(0, len(text), step)]"},
 r:["Chunking","Retrieval-Augmented Generation","Embedding","Reranking"]},

{t:"Ablation Study", d:"Removing one piece at a time to find out which pieces are actually doing the work.", l:"core", g:["ai","evaluation","crash-course"],
 b:["You added retrieval, reranking, a longer system prompt and few-shot examples, and quality improved. Which change caused it? Possibly one of them; possibly one helped and another hurt and they cancelled out. An ablation answers that by removing each element separately and re-measuring.",
    "It is the difference between a system you have tuned and a system you understand. It routinely reveals that an expensive component contributes nothing measurable, which is the cheapest performance and cost win available — deleting something that was not helping."],
 k:["Remove one component, re-measure, repeat.",
    "Separates what helps from what merely coexists.",
    "Frequently identifies expensive components you can delete.",
    "Needs a fixed evaluation set or the numbers mean nothing."],
 r:["Golden Dataset","LLM Evaluation","Random Seed","Baseline Model"]},

{t:"Learning Curve", d:"A graph of how error changes as training proceeds — the main way to see whether a model is learning or memorising.", l:"core", g:["ai","evaluation","crash-course"],
 b:["Plot training error and validation error together and the shape tells the story. Both falling and close together means it is learning. Training error falling while validation error rises means it is memorising the training set — that gap is overfitting, visible long before the final score.",
    "The other version plots error against dataset size, which answers a genuinely useful budget question: if the curve is still dropping when you run out of data, more data will help. If it has flattened, more data will not, and the effort belongs in features, architecture or label quality instead."],
 k:["Plot training and validation error together, always.",
    "A widening gap between them is overfitting.",
    "Both high and flat means underfitting — the model is too simple.",
    "Versus dataset size, it tells you whether more data would help."],
 r:["Overfitting","Training","Dataset","Early Stopping"]},

{t:"Inference Endpoint", d:"The hosted address you send inputs to and get model outputs back from — a model behind an API.", l:"core", g:["ai","mlops","crash-course"],
 b:["Once a model exists, something has to serve it. An inference endpoint wraps the loaded weights in an HTTP service: it accepts a request, runs the forward pass, and returns the result — with batching, queueing and health checks around it, because GPUs are expensive and must be kept busy.",
    "The operational shape differs from a normal web service in ways worth knowing. Cold starts are measured in tens of seconds because weights must be loaded into GPU memory. Concurrency is bounded by memory, not CPU. And the sensible default for most teams is a hosted provider — self-hosting only pays off at high, steady volume or under strict data-residency requirements."],
 k:["A model wrapped in an HTTP service, usually GPU-backed.",
    "Cold starts are slow — loading weights takes tens of seconds.",
    "Concurrency is limited by GPU memory, not CPU.",
    "Self-host for volume, privacy or control; otherwise use a provider."],
 r:["Model Deployment","Model Serving","VRAM","Cold Start"]}

]);
