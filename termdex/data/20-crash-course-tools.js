/* Crash Course, part 2 — when it breaks, and the tools around the code.
   Same house rule: plain English, real examples, nothing hand-waved. */

TD.add("crash-course", [

/* ------------------------------------------------------ when things break */

{t:"Bug", d:"Code that runs but does the wrong thing — a mismatch between what you meant and what you wrote.", l:"core", g:["basics","bugs","crash-course"],
 b:["A bug is not the computer misbehaving. The machine did exactly what the instructions said; the instructions were wrong. Holding that thought is the difference between debugging productively and staring at the screen feeling betrayed.",
    "The name comes from a moth found jammed in the Harvard Mark II relay in 1947 and taped into the logbook, though engineers were calling faults bugs decades earlier. The useful taxonomy is three-way: it will not run (syntax error), it crashes while running (runtime error), or it runs happily and produces the wrong answer — the third being by far the most expensive."],
 k:["The computer did what you said, not what you meant.",
    "Three flavours: will not start, crashes, or silently wrong.",
    "Silently wrong is the costliest, because nothing tells you.",
    "Reproduce it reliably before you try to fix it."],
 r:["Debugging","Syntax Error","Runtime Error","Stack Trace"]},

{t:"Syntax Error", d:"Code the language cannot even read — a missing bracket, a stray colon, a typo in a keyword.", l:"core", g:["basics","bugs","crash-course"],
 b:["This is the friendliest kind of error, because nothing runs at all and the message points at a line. The catch is that the reported line is where the parser gave up, not always where you went wrong: an unclosed bracket on line 40 is often reported on line 41 or later.",
    "So when the flagged line looks perfect, look at the line above it. Beyond that, the fix is prevention: an editor with syntax highlighting and bracket matching catches most of these as you type, long before you run anything."],
 k:["Nothing runs — the language could not parse the file.",
    "The reported line is where parsing failed, often one line late.",
    "If the flagged line looks fine, check the line above.",
    "Editors and linters catch most of these as you type."],
 x:{lang:"python", code:"def total(items:      # missing closing bracket\n    return sum(items)\n\n# SyntaxError points at the return line, not the def line"},
 r:["Bug","Runtime Error","Linter","Indentation"]},

{t:"Runtime Error", d:"An error that only appears while the program is running, when it hits data or a state it cannot handle.", l:"core", g:["basics","bugs","crash-course"],
 b:["The file parsed fine, the program started, and then it met an empty list, a missing key, a null user or a network that was down, and stopped. Dividing by zero, reading index ten of a nine-item list, calling a method on nothing — all runtime errors.",
    "They are harder than syntax errors because they depend on data. The code path that crashes may only be reached for one user in a thousand. That is why the standard defences are input validation at the edges, tests that include empty and malformed data, and error tracking in production so you learn about it before your users tell you."],
 k:["The code is valid; the situation it met was not.",
    "Depends on data, so it can hide until an unusual input arrives.",
    "Validate at the boundaries of your system, not everywhere inside.",
    "Test the empty case, the one-item case and the malformed case."],
 r:["Exception","Stack Trace","Edge Case","Bug"]},

{t:"Exception", d:"A signal a program throws when it cannot continue — an error object that travels up until something catches it.", l:"core", g:["basics","bugs","crash-course"],
 b:["When code hits something it cannot handle, it raises an exception. Execution stops there and the exception travels up through whoever called that function, and whoever called them, until some layer catches it. If nobody does, the program stops and prints a stack trace.",
    "The design idea is that the place that detects the problem is rarely the place that knows what to do about it. A file-reading function knows the file is missing; only the caller knows whether that means show a warning, use a default, or abort the whole job. Exceptions carry the bad news up to whoever can make that decision."],
 k:["Raised where a problem is detected, handled where it can be resolved.",
    "Travels up the call stack until something catches it.",
    "Uncaught exceptions end the program and print a trace.",
    "Different exception types let you catch just the case you understand."],
 x:{lang:"python", code:"def load(path):\n    with open(path) as f:      # raises FileNotFoundError\n        return f.read()\n\ntry:\n    data = load(\"config.json\")\nexcept FileNotFoundError:\n    data = \"{}\"                # caller decides the fallback"},
 r:["Exception Handling","Stack Trace","Runtime Error","Null and Undefined"]},

{t:"Exception Handling", a:"try / catch", d:"Wrapping risky code so that when it fails, you decide what happens next instead of the program simply dying.", l:"core", g:["basics","bugs","crash-course"],
 b:["The shape is the same everywhere: try this, and if a specific kind of failure happens, do that instead. A finally block runs either way, which is where you close files and release connections.",
    "The rule that separates good handling from bad is specificity. Catching every possible exception and ignoring it turns a loud, diagnosable failure into a silent wrong answer — the worst outcome available. Catch the exception you actually expect, do something meaningful about it, and let anything you did not anticipate travel up where it will be seen."],
 k:["Catch specific exceptions, not everything.",
    "An empty catch block hides bugs — never swallow silently.",
    "`finally` runs whether or not there was an error.",
    "If you cannot do anything useful about it, let it propagate."],
 x:{lang:"python", code:"try:\n    resp = requests.get(url, timeout=5)\n    resp.raise_for_status()\nexcept requests.Timeout:\n    log.warning(\"slow upstream, using cache\")\n    resp = cache.get(url)\nexcept requests.HTTPError as e:\n    log.error(\"upstream said %s\", e.response.status_code)\n    raise                     # re-raise what we cannot handle\nfinally:\n    metrics.record(url)"},
 r:["Exception","Guard Clause","Retry","Logging"]},

{t:"Stack Trace", a:"traceback", d:"The list of function calls that were in progress when an error happened — the map back to the cause.", l:"core", g:["basics","bugs","crash-course"],
 b:["It looks intimidating and it is actually a gift: a numbered path showing exactly how the program got to the line that failed. Read it from the bottom in Python, where the last line is the actual error and the frame above it is where it happened. JavaScript prints it the other way up, with the error first.",
    "The practical trick is to scan for the first frame that belongs to your own code. Everything below it is library internals you did not write and probably cannot fix. That one line — your file, your line number — is where to put the first print statement or breakpoint."],
 k:["A record of which function called which, at the moment of failure.",
    "Python reads bottom-up; JavaScript reads top-down.",
    "Find the topmost frame in your own code — start there.",
    "Never paste a trace into a bug report with the error line cut off."],
 r:["Exception","Debugging","Runtime Error","Logging"]},

{t:"Edge Case", d:"An input at the boundary of what your code expects — empty, one, maximum, negative, missing, weird.", l:"core", g:["basics","practice","crash-course"],
 b:["The code works for a list of three users. Does it work for zero users? For one? For fifty thousand? For a user with no email, an emoji in their name, or an apostrophe in it? Those are the edges, and they are where nearly all production bugs live, because the middle of the range is what everybody tests.",
    "There is a standard checklist worth running through for any function you write: empty, one, many, boundary values, negatives, zero, null, very large, and wrong type. Five minutes on that list finds more bugs than an hour of reading the code again."],
 k:["Bugs cluster at boundaries, not in the middle of the range.",
    "Standard list: empty, one, many, zero, negative, null, huge.",
    "Text edges: unicode, emoji, apostrophes, right-to-left scripts.",
    "Each edge case you find deserves a test so it stays found."],
 r:["Off-by-One Error","Happy Path","Guard Clause","Unit Test"]},

{t:"Happy Path", d:"The scenario where everything works — valid input, network up, user does exactly what you expected.", l:"core", g:["basics","practice","crash-course"],
 b:["Every feature gets built for the happy path first, and demos beautifully. Then reality arrives: the upload is 400 MB, the card is declined, the user hits back mid-payment, the third-party API returns HTML instead of JSON.",
    "Most of the real engineering in a feature is the unhappy paths, and they are where the estimate goes wrong. A useful habit before calling something done is to write down every way each step could fail, and decide for each one whether you retry, show an error, or fall back."],
 k:["The everything-works scenario — the easy 20% of the work.",
    "Unhappy paths are where most of the real code lives.",
    "List the failure modes per step before you estimate.",
    "For each failure decide: retry, fail loudly, or degrade."],
 r:["Edge Case","Error Handling","Exception Handling","Retry"]},

/* ------------------------------------------------------ the tools around code */

{t:"IDE", a:"Integrated Development Environment", d:"An editor that also understands your code — autocomplete, jump-to-definition, inline errors, a debugger.", l:"core", g:["tools","crash-course"],
 b:["A plain text editor shows characters. An IDE builds a model of your project, so it can rename a function in forty files correctly, tell you a variable does not exist before you run anything, and let you pause execution and inspect every value.",
    "The three features worth learning on day one, in order: jump to definition, so you can read any library you depend on; find all references, so you know what a change will break; and the debugger, which replaces most print statements with something far more informative."],
 k:["Understands the code, not just the characters.",
    "Learn: go-to-definition, find-references, rename symbol.",
    "The debugger beats print statements once you learn it.",
    "VS Code, JetBrains, Neovim with a language server — all fine choices."],
 r:["Debugging","Linter","Code Formatter","Stack Trace"]},

{t:"Command Line Interface", a:"CLI / terminal / shell", d:"The text window where you type commands to your computer instead of clicking.", l:"core", g:["tools","crash-course"],
 b:["Clicking is discoverable and slow; typing is opaque and fast. The terminal wins for anything repeatable, because a command can be saved, shared, scripted and run on a server that has no screen at all. Almost every development tool is a command first and a button second.",
    "The starting set is small: where am I, what is here, go there, and run this. Add the up arrow to repeat, tab to autocomplete, and Ctrl-C to stop a runaway process, and you already have most of what daily work needs."],
 k:["Commands are repeatable, scriptable and work over SSH.",
    "Core five: pwd, ls, cd, cat, and the tool you actually want.",
    "Up arrow repeats, Tab completes, Ctrl-C stops.",
    "Windows: PowerShell and Git Bash take different syntax — know which you are in."],
 x:{lang:"bash", code:"pwd                 # where am I\nls -la              # what is here, including hidden files\ncd ../project       # go up one, then into project\ncat package.json    # print a file\nnpm run dev         # run a project script"},
 r:["Shell","File Path","Exit Code","Standard Input and Output"]},

{t:"File Path", d:"The address of a file on disk — absolute from the root, or relative to where you are now.", l:"core", g:["tools","crash-course"],
 b:["An absolute path starts at the root and works anywhere: it names the whole route. A relative path starts from the current directory, where a single dot means here and two dots mean the folder above. Relative paths are shorter and survive the project being moved; absolute paths are unambiguous and break on someone else's machine.",
    "The everyday trap is that a relative path is resolved from wherever the program was started, not from where the source file lives. Running the same script from a different folder suddenly cannot find its data file. The robust fix is to build paths from the script's own location using your language's path helpers, which also handles the slash direction difference between Windows and everything else."],
 k:["Absolute starts at root; relative starts from the current directory.",
    "One dot is here, two dots is the parent folder.",
    "Relative paths resolve from where the process started, not the file.",
    "Use path helpers, not string concatenation, for cross-platform code."],
 x:{lang:"python", code:"from pathlib import Path\n\nHERE = Path(__file__).parent          # where this file lives\ndata = HERE / \"data\" / \"terms.json\"   # correct slashes everywhere\nprint(data.exists())"},
 r:["Command Line Interface","Configuration File","Module"]},

{t:"Localhost", d:"Your own machine, addressed over the network — the address 127.0.0.1, which never leaves the computer.", l:"core", g:["tools","crash-course"],
 b:["When you start a development server and it says it is running on localhost port 3000, it means a program on your machine is listening for network requests, and your browser can talk to it as if it were a real website. Nothing goes out to the internet; the traffic loops straight back inside the machine.",
    "The number after the colon is the port — a channel number, so several servers can run at once without colliding. Address-already-in-use means something is still holding that port, usually a previous run you did not stop. And localhost is only reachable from your own machine, which is why a phone on the same wi-fi needs your machine's local network address instead."],
 k:["127.0.0.1 — traffic that never leaves your computer.",
    "The number after the colon is the port; each server needs its own.",
    "Address-already-in-use means a previous process is still running.",
    "Other devices cannot reach your localhost — use the LAN address."],
 x:{lang:"bash", code:"python -m http.server 8000        # serve this folder\n# open http://localhost:8000\n\nlsof -i :8000                     # who is holding the port (mac/linux)\nnetstat -ano | findstr :8000      # same on Windows"},
 r:["Port","Server","Client","IP Address"]},

{t:"Bash and PowerShell", d:"The two shell languages you will actually meet — and why a snippet from one fails in the other.", l:"core", g:["tools","crash-course"],
 b:["The terminal is the window; the shell is the program inside it interpreting your words. Which shell you are in decides the syntax: setting an environment variable, joining commands or writing a loop all look different in bash and in PowerShell, and pasting one into the other produces baffling errors.",
    "Beyond running single commands, a shell is a small programming language with variables, conditionals and loops, which is what makes a shell script the natural home for repetitive setup and deployment steps."],
 k:["The interpreter behind the terminal window.",
    "bash and zsh on mac and Linux; PowerShell on Windows.",
    "Syntax differs — a bash snippet will not run in PowerShell.",
    "`echo $SHELL` on Unix tells you which one you are in."],
 r:["Command Line Interface","Environment Variable","Exit Code","Pipe"]},

{t:"Standard Input and Output", a:"stdin / stdout / stderr", d:"The three text channels every command-line program gets: one in, two out.", l:"core", g:["tools","crash-course"],
 b:["A program reads from standard input, writes its results to standard output, and writes complaints to standard error. Keeping errors on a separate channel is what lets you pipe a program's results into another tool while still seeing its warnings on screen.",
    "This is the whole philosophy behind command-line tools composing so well: each one reads text, writes text, and does not care whether the other end is a keyboard, a file or another program. It is also why a log line printed to stdout can end up mixed into your JSON output and break a downstream parser — logs belong on stderr."],
 k:["stdin reads, stdout is results, stderr is diagnostics.",
    "Pipes connect one program's stdout to the next one's stdin.",
    "Redirect with `>` to a file, `2>` for errors only.",
    "Print logs to stderr so stdout stays machine-readable."],
 x:{lang:"bash", code:"cat terms.json | jq '.[] | .name' > names.txt   # stdout to a file\nnpm run build 2> build-errors.log               # stderr only\nsomecommand > out.txt 2>&1                      # both together"},
 r:["Pipe","Shell","Exit Code","Command Line Interface"]},

{t:"Pipe", a:"|", d:"A vertical bar that feeds one command's output straight into the next command's input.", l:"core", g:["tools","crash-course"],
 b:["Pipes are why small command-line tools are so powerful: each does one job, and the bar chains them into an assembly line with no temporary files. List the files, filter to the ones matching a pattern, count them — three tools, one line.",
    "The data flows as text, streaming as it is produced, so a pipeline over a huge file starts producing output immediately and never loads the whole thing into memory. That streaming behaviour is the reason pipelines handle files far larger than available RAM."],
 k:["Connects stdout of the left to stdin of the right.",
    "Streams — output appears before the first command finishes.",
    "Chains as long as you like; each stage does one job.",
    "Errors are not piped; they still go to your screen."],
 x:{lang:"bash", code:"grep -r \"TODO\" src | wc -l               # count TODOs\ncat access.log | grep 500 | tail -20     # last 20 server errors\nls -la | sort -k5 -n | tail -5           # five biggest files"},
 r:["Standard Input and Output","Shell","Command Line Interface"]},

{t:"Exit Code", a:"return code / status code", d:"The number a command leaves behind when it finishes: zero means success, anything else means failure.", l:"core", g:["tools","crash-course"],
 b:["Every command sets one. Zero is success — an easy thing to get backwards, since zero is falsy in most languages but successful in a shell. Any other number signals a problem, and the specific value sometimes tells you which.",
    "This is the mechanism that makes automation possible. A CI pipeline runs your tests and looks only at the exit code to decide whether to continue. A script that ends with a chain of ampersands stops at the first failure for the same reason. If you write a script that swallows failures and always exits zero, your pipeline will happily deploy broken code."],
 k:["0 is success; non-zero is failure.",
    "CI pipelines and `&&` chains branch on this number alone.",
    "`echo $?` shows the last command's exit code.",
    "Make your own scripts exit non-zero on failure, or automation goes blind."],
 x:{lang:"bash", code:"npm test\necho $?          # 0 if the tests passed\n\nnpm test && npm run build    # build only if tests pass\nnpm test || echo \"tests failed\""},
 r:["Shell","Continuous Integration","Standard Input and Output"]},

{t:"Script", d:"A file of commands run top to bottom to do one job — automation you can read.", l:"core", g:["tools","crash-course"],
 b:["The moment you have typed the same three commands twice, they belong in a script. It is not about elegance; it is that a script is documentation that cannot go stale, because it is the thing that actually runs.",
    "Scripts start in the shell and grow up into Python or Node when the logic gets real — parsing, conditionals, error handling. A good rule is that anything a new teammate would otherwise have to be told in chat should be a script in the repository with a name that says what it does."],
 k:["If you have typed it twice, script it.",
    "Executable documentation — it cannot drift from reality.",
    "Graduate from shell to a real language once logic appears.",
    "Keep them in the repo and list them in the README."],
 r:["Shell","Entry Point","Exit Code","Runbook"]},

{t:"Entry Point", d:"The place a program starts running — the first line executed when you launch it.", l:"core", g:["tools","crash-course"],
 b:["Every runnable program has one: main in C, Java and Go; the top of the file in a Python script; whatever is named in the start script of a Node project. Finding it is the first step in understanding an unfamiliar codebase — start at the entry point and follow the calls.",
    "Python's if-name-equals-main check exists precisely so a file can be both a script and an importable module. Code inside that block runs when the file is executed directly, and stays quiet when the file is imported, which is exactly what you want when writing tests against it."],
 k:["Where execution begins — the thread you pull to read a new codebase.",
    "Named in package.json, a Dockerfile CMD, or a main function.",
    "Python's `if __name__ == \"__main__\"` separates script from module.",
    "Keep it thin: parse arguments, then call into real functions."],
 x:{lang:"python", code:"def main():\n    print(\"doing the work\")\n\nif __name__ == \"__main__\":   # only when run directly\n    main()"},
 r:["Script","Module","Build"]},

{t:"Package", d:"A bundle of code someone published so you do not have to write it — installed by name and version.", l:"core", g:["tools","crash-course"],
 b:["A package is a library plus the metadata that lets a tool fetch it: a name, a version, a list of what it itself needs. You ask for it by name, the package manager works out the whole tree of dependencies, and it lands in your project.",
    "The convenience has a cost worth being awake to. Every package is code you did not read running with your program's privileges, and each one drags in its own dependencies. Before adding one, it is fair to ask how big it is, when it was last updated, how many others it pulls in, and whether twenty lines of your own would do."],
 k:["Published code, fetched by name and version.",
    "Brings its own dependencies with it — the tree grows fast.",
    "Check maintenance, size and dependency count before adding.",
    "npm for JavaScript, PyPI for Python, crates.io for Rust."],
 r:["Package Manager","Dependency","Library","Lockfile"]},

{t:"Dependency", d:"Someone else's code that your project needs in order to run.", l:"core", g:["tools","crash-course"],
 b:["Your list of dependencies is a list of promises you are relying on. Direct ones you chose deliberately; transitive ones came along for the ride, and there are usually far more of those. A modest web project routinely ends up with several hundred packages in the tree.",
    "Two distinctions matter in practice. Runtime dependencies are needed in production; development dependencies — test runners, formatters, type checkers — are not, and keeping them separate keeps deployed images small. And a dependency you cannot easily replace is an architectural commitment, not a convenience."],
 k:["Direct dependencies are chosen; transitive ones come along.",
    "Separate runtime from development dependencies.",
    "Every dependency is code running with your privileges.",
    "Audit regularly — old packages accumulate known vulnerabilities."],
 x:{lang:"bash", code:"npm install express          # runtime dependency\nnpm install -D vitest        # development only\nnpm ls --depth=0             # what you asked for directly\nnpm audit                    # known vulnerabilities"},
 r:["Package","Package Manager","Lockfile","Virtual Environment"]},

{t:"Lockfile", d:"A generated file recording the exact version of every package installed, so every machine gets an identical tree.", l:"core", g:["tools","crash-course"],
 b:["Your manifest says you want version 4-point-something of a library. That is a range, and it resolves differently depending on what has been published since. The lockfile pins the precise version of everything, direct and transitive, along with a checksum for each.",
    "That is what makes builds reproducible: it works on my machine stops being a mystery when both machines install from the same lock. Commit it, never hand-edit it, and let the package manager regenerate it when you change something."],
 k:["Pins exact versions of the entire dependency tree.",
    "Commit it — that is the whole point.",
    "Never edit by hand; let the tool regenerate it.",
    "package-lock.json, yarn.lock, poetry.lock, Cargo.lock."],
 r:["Dependency","Package Manager","Semantic Versioning","Build"]},

{t:"Virtual Environment", a:"venv", d:"A private folder of packages for one project, so its dependencies cannot collide with another project's.", l:"core", g:["tools","crash-course"],
 b:["Install packages globally and two projects immediately fight: one needs version 1 of a library, the other needs version 3, and only one can win. A virtual environment gives each project its own isolated set, activated when you work on it.",
    "Nearly every module-not-found error in Python has the same cause: the environment is not activated, so you are looking at the system Python instead of the project's. Node solves the same problem differently, with a node_modules folder per project that requires no activation at all."],
 k:["One isolated package set per project.",
    "Must be activated in each new terminal session.",
    "Never commit the environment folder — commit the requirements file.",
    "Node's per-project node_modules does this automatically."],
 x:{lang:"bash", code:"python -m venv .venv\nsource .venv/bin/activate        # mac / linux\n.venv\\Scripts\\activate           # windows\n\npip install -r requirements.txt\ndeactivate"},
 r:["Dependency","Package Manager","Environment Variable"]},

{t:"Environment Variable", a:"env var", d:"A setting passed to a program from outside it — the standard way to hold secrets and per-environment config.", l:"core", g:["tools","crash-course"],
 b:["The same code has to run on your laptop, in staging and in production, pointing at different databases with different keys. Hard-coding those values means editing the code per environment, which is both annoying and how secrets end up in version control forever.",
    "Environment variables move that configuration outside the code. Locally they usually come from a dotenv file, which must be listed in gitignore; in production they come from the platform's secret store. The one habit to build: when a secret does leak into a commit, rotate the key — deleting the line does not remove it from git history."],
 k:["Config and secrets live outside the code, per environment.",
    "Add `.env` to .gitignore, and commit a `.env.example` instead.",
    "Read them once at startup and validate that they exist.",
    "A leaked key must be rotated — git history keeps everything."],
 x:{lang:"bash", code:"# .env  (never committed)\nDATABASE_URL=postgres://localhost/dev\nOPENAI_API_KEY=sk-...\n\n# read it\nexport $(cat .env | xargs)\necho $DATABASE_URL"},
 r:["Configuration File","API Key","Production Environment","Secrets Management"]},

{t:"Configuration File", d:"A file of settings, separate from the code, that changes how a program behaves without changing what it does.", l:"core", g:["tools","crash-course"],
 b:["Port numbers, feature toggles, log levels, model names, retry counts — anything you might reasonably want to change without a code review belongs in configuration. Common formats are JSON, YAML and TOML, each with its own irritations; YAML's significant whitespace and surprising type coercion are the most famous.",
    "The pattern that survives contact with reality is layered: sensible defaults in code, overridden by a config file, overridden by environment variables. Validate the merged result at startup and fail immediately with a clear message if something required is missing, rather than crashing an hour later."],
 k:["Behaviour settings live outside the code.",
    "Layer it: defaults, then file, then environment variables.",
    "Validate at startup and fail loudly on missing values.",
    "Secrets belong in environment variables, not in a committed config file."],
 r:["Environment Variable","YAML","Feature Flag"]},

{t:"Build", d:"Turning source code into the thing that actually runs — compiled, bundled, minified, packaged.", l:"core", g:["tools","crash-course"],
 b:["Very little source code runs as written. TypeScript becomes JavaScript, JSX becomes function calls, dozens of modules become a handful of bundles, and everything gets minified. That whole transformation is the build, and its output is what ships.",
    "Two rules make builds trustworthy. First, the output directory is generated and belongs in gitignore, never in a commit. Second, a build should be reproducible: the same source and the same lockfile should produce the same artifact anywhere, which is exactly what CI checks every time."],
 k:["Source in, runnable artifact out.",
    "Bundling, transpiling, minifying, asset hashing.",
    "Build output is generated — never commit it.",
    "Reproducible builds are what make CI meaningful."],
 x:{lang:"bash", code:"npm run build        # writes dist/ or .next/\npython -m build      # writes a wheel into dist/\ndocker build -t app:1.2.0 ."},
 r:["Compiler","Bundler","Artifact","Deployment"]},

{t:"Runtime", d:"Two meanings: the program that executes your code, and the period while your code is running.", l:"core", g:["tools","crash-course"],
 b:["As a noun, a runtime is the environment your language needs: Node is the JavaScript runtime outside the browser, the JVM runs Java, CPython runs Python. It provides the memory management, the standard library and the bridge to the operating system.",
    "As a phase, runtime is contrasted with compile time. A type error caught at compile time costs you thirty seconds; the same error at runtime costs you an incident. That trade — how much can be verified before shipping — is the whole argument between static and dynamic languages."],
 k:["The thing that executes your code: Node, JVM, CPython.",
    "Also the phase when the program is running, versus compile time.",
    "Runtime version mismatches cause works-on-my-machine bugs.",
    "The earlier an error is caught, the cheaper it is."],
 r:["Compiler","Interpreter","Runtime Error","Build"]},

{t:"Library", d:"Code you call — a toolbox of functions you reach for when you need them.", l:"core", g:["tools","crash-course"],
 b:["A library does not impose a shape on your program. You import it, call it, and stay in charge of the flow: a date library, an HTTP client, a maths package. If you removed it, you would rewrite those functions but not restructure your application.",
    "That is the whole distinction from a framework, and it is worth being deliberate about. Libraries are easy to add and easy to remove. Frameworks are cheap at the start and expensive to leave. Both are fine choices; only one of them is reversible."],
 k:["You call it; you keep control of the program's flow.",
    "Composable — several libraries coexist happily.",
    "Easy to swap out later.",
    "Prefer a library when you already know the shape of your app."],
 r:["Framework","Package","Dependency","SDK"]},

{t:"Framework", d:"Code that calls you — it owns the structure, and you fill in the blanks.", l:"core", g:["tools","crash-course"],
 b:["With a framework the control is inverted: you do not call Django or Rails or React, they call your handlers, components and hooks at moments they decide. In exchange for that control you get routing, lifecycle, conventions and a great deal of code you did not write.",
    "This is a real trade. A framework makes the first eighty percent dramatically faster and the last twenty harder, because anything the framework did not anticipate has to be fought for. Choose one for the conventions and the community, not for the feature list, and know that leaving is a rewrite."],
 k:["Inversion of control — it calls your code, not the other way round.",
    "Conventions and structure for free; less freedom.",
    "Fast to start, expensive to leave.",
    "Django, Rails, Spring, Next.js, React are frameworks; requests and lodash are libraries."],
 r:["Library","SDK","Boilerplate","Scaffolding"]},

{t:"SDK", a:"Software Development Kit", d:"An official bundle for working with a particular platform — client library, tools, docs and examples together.", l:"core", g:["tools","crash-course"],
 b:["Instead of hand-writing HTTP requests against an API, you install the vendor's SDK and call typed methods. It handles authentication, retries, pagination and error shapes, which are exactly the fiddly parts that are easy to get subtly wrong.",
    "The trade-off is coupling and lag. SDKs are usually slightly behind the raw API, and they are one more dependency to keep updated. For a well-supported platform the SDK is almost always the right call; for a small or unstable one, plain HTTP is often more predictable."],
 k:["Client library plus tooling, published by the platform itself.",
    "Handles auth, retries, pagination and typed errors for you.",
    "Sometimes lags behind the raw API.",
    "The API is the contract; the SDK is a convenience over it."],
 r:["Library","API","Package","API Key"]},

{t:"API Key", d:"A secret string that identifies your application to a service — a password for machines.", l:"core", g:["tools","security","crash-course"],
 b:["A service needs to know who is calling, both to bill you and to enforce limits. The key is the answer, usually sent in a header on every request. Anyone holding it can act as you, which is the whole reason it needs care.",
    "The rules are short and non-negotiable. Never commit a key. Never put one in frontend code, because everything the browser downloads is readable — route those calls through your own server. Give each environment its own key so one leak does not compromise everything, and rotate immediately when one escapes."],
 k:["Identifies and authorises your app; treat it as a password.",
    "Never in the frontend — proxy through your own backend.",
    "Separate keys per environment; scope them narrowly.",
    "If it leaks, rotate it. Deleting the commit is not enough."],
 x:{lang:"python", code:"import os\nkey = os.environ[\"OPENAI_API_KEY\"]   # fail fast if missing\n\n# never:\n# key = \"sk-proj-abc123...\""},
 r:["Environment Variable","Secrets Management","Rate Limiting","HTTP Header"]},

{t:"Code Formatter", d:"A tool that rewrites your code into one consistent layout automatically, so nobody has to discuss it.", l:"core", g:["tools","crash-course"],
 b:["Indentation width, quote style, line length, where a long argument list breaks — every one of these has been argued about in a pull request somewhere, and none of them matter. A formatter picks one answer and applies it on save, and the argument is over permanently.",
    "The secondary benefit is bigger than the first: consistently formatted code produces clean diffs. When formatting is stable, every changed line in a review is a real change, which makes reviewing faster and more accurate."],
 k:["Enforces one layout with no configuration debates.",
    "Run on save, and check it in CI.",
    "Consistent formatting means diffs show only real changes.",
    "Prettier, Black, gofmt, rustfmt."],
 r:["Linter","Naming Conventions","Diff","Code Review"]},

{t:"Boilerplate", d:"The repetitive setup code you have to write before the interesting part, in nearly every project.", l:"core", g:["tools","crash-course"],
 b:["Imports, configuration loading, logger setup, error middleware, the shape of a test file. It carries no ideas — it is the ceremony a language or framework demands before real work can start.",
    "Some boilerplate is honest and worth keeping explicit, because hiding it behind cleverness makes the system harder to debug. The kind worth eliminating is the kind that is copied by hand, because copies drift: three services with slightly different error handling because someone improved one and forgot the others."],
 k:["Necessary, repetitive, idea-free setup code.",
    "Hand-copied boilerplate drifts and rots.",
    "Generate it or extract it, rather than copying it.",
    "Some explicitness is worth keeping — do not hide everything."],
 r:["Scaffolding","Framework","Configuration File"]},

{t:"Scaffolding", d:"Generating a project's starting files with one command instead of assembling them by hand.", l:"core", g:["tools","crash-course"],
 b:["Create-next-app, django-admin startproject, cargo new. One command lays down the folder structure, config files, a working entry point and a sensible set of defaults, so you are running within a minute instead of an afternoon.",
    "The failure mode is accepting the output as a black box. Generated projects arrive with tooling, dependencies and conventions you did not choose. Reading through what was created once, deleting what you do not need, is a twenty-minute investment that pays for itself the first time something in that config breaks."],
 k:["One command produces a working starting point.",
    "Encodes the ecosystem's current conventions.",
    "Read what it generated — you own it now.",
    "Delete the parts you do not need before they calcify."],
 r:["Boilerplate","Framework","Build","Entry Point"]},

{t:"Jupyter Notebook", d:"A document where code, its output and written notes live together, cell by cell.", l:"core", g:["tools","ai","crash-course"],
 b:["Notebooks are the default workspace for data and machine-learning work because the loop is so tight: run one cell, look at the chart, adjust, run again, with everything you have loaded still sitting in memory. Exploration in a normal script means re-running from the top every time.",
    "The trade-off is hidden state. Cells can be run in any order, so a notebook that works on your screen may be unreproducible from top to bottom, and the outputs saved inside the file make git diffs unreadable. The professional habit is to restart-and-run-all before trusting a result, and to move anything reusable out into a proper module."],
 k:["Cells keep state in memory — ideal for exploration.",
    "Out-of-order execution creates results nobody can reproduce.",
    "Restart and run all before you trust or share a result.",
    "Move reusable code into modules; keep notebooks for exploring."],
 r:["Python","Dataset","Script","Module"]},

/* ------------------------------------------------------ working with others */

{t:"Repository", a:"repo", d:"A project folder that git is tracking — the code plus its entire history.", l:"core", g:["git","tools","crash-course"],
 b:["Initialising a repository creates a hidden folder holding every version of every file you have ever committed. That is why git can show you what a file looked like eight months ago, who changed a line and why, and let you undo almost anything.",
    "Because history is permanent, what you put in matters. Committed secrets stay in the history even after you delete the line, and committed build output or large binaries bloat the repository for everyone who clones it forever. A gitignore file written at the start of the project prevents both."],
 k:["The project plus its complete, permanent history.",
    "Every clone contains the whole history — git is distributed.",
    "Write .gitignore first: secrets, build output, dependencies.",
    "Deleting a committed secret does not remove it from history."],
 x:{lang:"bash", code:"git init\ngit add .\ngit commit -m \"first commit\"\ngit log --oneline --graph\ngit remote add origin git@github.com:me/project.git"},
 r:["Git","Commit","Fork","Diff"]},

{t:"Diff", d:"The list of exactly which lines changed between two versions — the unit reviewers actually read.", l:"core", g:["git","tools","crash-course"],
 b:["Minus lines were removed, plus lines were added, and the unchanged lines around them are context. Every code review, every commit and every pull request is ultimately a diff, so the readability of your diff is the readability of your change.",
    "This is why reformatting a file in the same commit as a logic change is unkind: the real change is now hidden among four hundred whitespace lines. Keep formatting-only changes in their own commit, and reviewers can actually see what you did."],
 k:["Minus is removed, plus is added, the rest is context.",
    "Reviewers read diffs, not files — optimise for that.",
    "Never mix reformatting with logic changes in one commit.",
    "`git diff` unstaged, `git diff --staged` for what is about to be committed."],
 r:["Commit","Code Review","Pull Request","Code Formatter"]},

{t:"Stash", d:"A shelf for work in progress, so you can switch branches without committing half-finished code.", l:"core", g:["git","tools","crash-course"],
 b:["An urgent bug arrives while you are mid-feature. Stashing takes your uncommitted changes off the working tree and stores them, leaving you clean to switch branches, fix the thing, and come back and pop them.",
    "It is a short-term tool, not storage. A stash has no branch, no message worth reading, and is invisible to everyone else, so anything left there for a week is effectively lost work. If it needs to survive to tomorrow, make a work-in-progress commit on a branch instead."],
 k:["Temporarily shelves uncommitted changes.",
    "`git stash` then `git stash pop` to bring them back.",
    "Untracked files need `-u` to be included.",
    "For anything longer than an hour, use a WIP commit on a branch."],
 x:{lang:"bash", code:"git stash -u -m \"half-done search filter\"\ngit switch main\n# ...fix the urgent thing...\ngit switch feature/search\ngit stash pop"},
 r:["Commit","Branch","Repository"]},

{t:"Fork", d:"Your own copy of someone else's repository, under your account, that you can push to freely.", l:"core", g:["git","tools","crash-course"],
 b:["You cannot push to a project you do not have write access to, so open-source contribution works by copying: fork the project, clone your fork, make a branch, push it, and open a pull request back to the original.",
    "The one thing that catches everyone is drift. Your fork does not update itself, so a week later it is behind and your pull request conflicts. Adding the original repository as a second remote, conventionally named upstream, lets you pull its changes in and rebase before you open the request."],
 k:["A server-side copy you own and can push to.",
    "The standard route for contributing to projects you cannot write to.",
    "Forks go stale — add the original as an `upstream` remote.",
    "Sync before opening a pull request to avoid conflicts."],
 x:{lang:"bash", code:"git clone git@github.com:me/project.git\ngit remote add upstream git@github.com:original/project.git\ngit fetch upstream\ngit rebase upstream/main"},
 r:["Upstream","Repository","Pull Request","Open Source"]},

{t:"Upstream", d:"The original repository or branch that yours is derived from and expects to sync with.", l:"core", g:["git","tools","crash-course"],
 b:["The word points in one direction: upstream is where changes come from, downstream is where they flow to. For a fork, upstream is the project you copied. For a local branch, its upstream is the remote branch it tracks, which is what lets a bare push or pull know where to go.",
    "The same word appears in service architecture with the same logic. An upstream service is the one you call and depend on; when it is slow, you are slow. That is where terms like upstream timeout in an error log come from."],
 k:["The source you pull from and contribute back to.",
    "A branch's upstream is the remote branch it tracks.",
    "Set it once with `push -u`, then bare push and pull work.",
    "In service architecture, upstream is the dependency you call."],
 r:["Fork","Repository","Branch","Pull Request"]},

{t:"Cherry-Pick", d:"Copying one specific commit onto another branch, without bringing along everything around it.", l:"core", g:["git","tools","crash-course"],
 b:["The classic use is a hotfix: the fix is already on the main branch, and the release branch needs exactly that one commit and nothing else. Cherry-picking replays those changes as a new commit with a new identifier.",
    "Use it sparingly. The copy is a genuinely separate commit, so the same change now exists twice in history under two identifiers, which can confuse later merges and makes tracing an issue harder. Where a proper merge is possible, merge."],
 k:["Replays one commit's changes onto the current branch.",
    "Creates a new commit — the same change now exists twice.",
    "Standard tool for backporting a fix to a release branch.",
    "Prefer merging when the whole branch is wanted."],
 x:{lang:"bash", code:"git switch release/2.1\ngit cherry-pick 4f2a9c1\ngit push"},
 r:["Commit","Branch","Hotfix","Merge"]},

{t:"Release", d:"A named, frozen version of your software that you have decided is the one people get.", l:"core", g:["process","tools","crash-course"],
 b:["A release ties together a version number, a tagged commit, built artifacts and a changelog. The tag is what makes it useful months later: when a customer reports a bug in version 2.3.1, you can check out exactly the code they are running.",
    "Two release philosophies coexist. Big periodic releases suit software people install; continuous delivery, where every merge to main can ship, suits software you host. Either way the release itself should be boring and automated — the drama belongs in the code review, not the deploy."],
 k:["A version number, a git tag, artifacts and a changelog.",
    "Tags make old versions reproducible.",
    "Semantic versioning tells users whether it is safe to upgrade.",
    "Automate it — a manual release process is a recurring outage risk."],
 r:["Semantic Versioning","Changelog","Deployment","Artifact"]},

{t:"Open Source", d:"Software whose source code is public and licensed so anyone can read, use and modify it.", l:"core", g:["process","crash-course"],
 b:["Practically all modern software sits on an enormous stack of open-source code: the language, the framework, the database, most of the libraries. It is free to use, and it is maintained by people who owe you nothing, which is worth remembering when you file an issue.",
    "Public source is not the same as no rules. The licence decides what you can do — some permit anything with attribution, others require that your changes are published too. And free does not mean unaudited: dependency vulnerabilities and abandoned packages are ordinary supply-chain risks."],
 k:["Source is public and the licence grants usage rights.",
    "Public does not mean unrestricted — read the licence.",
    "Maintainers are volunteers; issues are requests, not tickets.",
    "Contributing back is how the packages you rely on stay alive."],
 r:["Software License","Fork","Pull Request","Package"]},

{t:"Software License", d:"The legal terms that say what you are allowed to do with a piece of code.", l:"core", g:["process","crash-course"],
 b:["Permissive licences like MIT, Apache 2.0 and BSD let you use the code in anything, including closed commercial products, as long as you keep the notice. Copyleft licences like the GPL require that whatever you distribute containing that code is released under the same terms — which is why some companies forbid GPL dependencies outright.",
    "Two practical notes. Code with no licence at all is not free to use; by default all rights are reserved, and a public repository does not change that. And Apache 2.0 additionally grants patent rights, which is why legal teams often prefer it to MIT."],
 k:["MIT, Apache, BSD: permissive — use almost anywhere with attribution.",
    "GPL and AGPL: copyleft — derivatives must be open too.",
    "No licence means all rights reserved, even on a public repo.",
    "Apache 2.0 adds an explicit patent grant."],
 r:["Open Source","Dependency","Package"]},

{t:"Issue Tracker", d:"The shared list where bugs, features and questions live, so work is visible instead of scattered across chats.", l:"core", g:["process","crash-course"],
 b:["Jira, GitHub Issues, Linear — the tool matters far less than the discipline. A good issue states what happened, what was expected, how to reproduce it and how to tell when it is fixed. An issue reading login broken is an interruption, not a task.",
    "The tracker is also the project's memory. Linking commits and pull requests to issues means that in a year, when someone asks why this odd check exists, the answer is one click away instead of lost in a chat thread nobody can search."],
 k:["One shared, searchable queue instead of scattered messages.",
    "A good issue has repro steps and a definition of done.",
    "Link commits and pull requests to their issue.",
    "It becomes the project's institutional memory."],
 r:["Pull Request","Code Review","Standup","Postmortem"]},

{t:"Standup", d:"A short daily sync where a team says what they did, what they are doing, and what is blocking them.", l:"core", g:["process","crash-course"],
 b:["The point is not status reporting to a manager — that is what the tracker is for. The point is the third question: surfacing blockers early, so someone who has been stuck for a day gets unstuck in a minute by the person who already solved it.",
    "Standups go wrong when they turn into serial reporting to the lead, or when they drift into design discussions. The fix for both is the same: keep it under fifteen minutes and move anything that needs real conversation into a follow-up with only the people involved."],
 k:["Its real job is surfacing blockers, not reporting status.",
    "Fifteen minutes, standing, whole team.",
    "Take detailed discussions offline immediately.",
    "Say when you are stuck — that is the entire value."],
 r:["Issue Tracker","Sprint","Agile"]},

{t:"Runbook", d:"Step-by-step instructions for handling a specific operational situation, written before you need them.", l:"core", g:["process","crash-course"],
 b:["What to do when the queue backs up. How to roll back a bad deploy. How to rotate a leaked key. A runbook exists so the person on call at three in the morning does not have to reason from first principles under pressure, or wake the one person who knows.",
    "The best time to write one is immediately after the incident that taught you the steps, while it is fresh. The worst runbook is a stale one, so each should say when it was last verified — an untested runbook is a guess in a formal font."],
 k:["Concrete steps for a specific situation, not general documentation.",
    "Write it right after the incident that taught you.",
    "Include how to verify the problem is actually fixed.",
    "Date it and test it, or it silently rots."],
 r:["Postmortem","On-Call","Rollback","Deployment"]},

{t:"Legacy Code", d:"Code that is still in production, hard to change safely, and usually written by someone who has left.", l:"core", g:["process","crash-course"],
 b:["Michael Feathers gave the definition that stuck: legacy code is simply code without tests. Not old code — untested code, because without tests you cannot change anything with confidence, so you patch around the edges and the mess compounds.",
    "The other thing to hold on to is that legacy code is running the business. It is ugly because it accumulated real requirements nobody wrote down. Chesterton's fence applies: before removing a check that looks pointless, find out which incident put it there. The safe route is to add tests describing the current behaviour first, then refactor."],
 k:["Feathers: legacy code is code without tests.",
    "It is ugly because it encodes requirements nobody documented.",
    "Add characterisation tests first, then change.",
    "Rewrites usually cost more and lose knowledge — refactor in place."],
 r:["Technical Debt","Refactoring","Unit Test","Code Coverage"]},

{t:"Minimum Viable Product", a:"MVP", d:"The smallest version that lets real users do the core thing, so you learn whether the idea works.", l:"core", g:["process","crash-course"],
 b:["The word doing the work is viable. An MVP is not a broken product with half the buttons missing; it is a complete, narrow product that does one valuable thing properly. One flow that works beats five that almost do.",
    "It is a learning instrument. You ship it to find out whether people want the thing at all, which means deciding in advance what result would make you continue and what would make you stop. An MVP nobody measures is just an unfinished product."],
 k:["Viable means genuinely usable, not unfinished.",
    "Narrow the scope, not the quality of what remains.",
    "Decide beforehand what result changes your mind.",
    "Cut features, not correctness or basic security."],
 r:["Happy Path","Feature Flag","Technical Debt"]},

{t:"Hotfix", d:"An urgent fix applied straight to what is in production, outside the normal release cycle.", l:"core", g:["process","crash-course"],
 b:["Something is broken for users right now and the next scheduled release is Thursday. A hotfix branches from the released version, changes the smallest possible thing, and ships. Keeping it minimal is the entire discipline: a hotfix that also refactors is how one outage becomes two.",
    "The step people skip under pressure is merging the fix back into the main branch. Skip it and the next release quietly reintroduces the bug you just fixed, which is a uniquely demoralising way to spend a Thursday."],
 k:["Branch from the released version, not from main.",
    "Change the minimum; resist every temptation to tidy.",
    "Merge it back into main or it regresses next release.",
    "Write the postmortem while it is still fresh."],
 r:["Rollback","Release","Cherry-Pick","Postmortem"]},

{t:"Staging Environment", d:"A near-identical copy of production used for the last check before real users see a change.", l:"core", g:["process","devops","crash-course"],
 b:["Local development is a small, clean, forgiving world. Staging is meant to be production with different data: same infrastructure, same configuration shape, same versions. The closer it is, the more meaningful the signal when something passes there.",
    "The two things that make staging lie are stale data and drifted configuration. If staging has a thousand rows and production has ten million, performance problems will not appear. Refreshing staging with anonymised production data periodically is what keeps it honest."],
 k:["Production-shaped, so problems appear before users find them.",
    "Drift from production makes it worthless as a signal.",
    "Refresh with anonymised production data — never raw personal data.",
    "Not a substitute for feature flags and gradual rollout."],
 r:["Production Environment","Deployment","Feature Flag","Environment Variable"]},

{t:"Production Environment", a:"prod", d:"The live system real users are actually using — the one where mistakes cost something.", l:"core", g:["process","devops","crash-course"],
 b:["Production is different in kind, not just in scale: real data, real money, real people, real privacy law. That is why access is restricted, changes are logged, and the deploy process has more ceremony than anywhere else.",
    "The habits that follow are simple and worth adopting before you need them. Never test against production data casually. Never run a manual database update without a transaction and a backup. Assume anything you delete is gone. And make sure you can roll back faster than you can debug, because during an incident the first job is to stop the bleeding."],
 k:["Real users, real data, real consequences.",
    "Rolling back should be faster and safer than fixing forward.",
    "Manual database changes need a transaction and a backup, always.",
    "Watch dashboards after a deploy — not before."],
 r:["Staging Environment","Deployment","Rollback","Monitoring"]},

{t:"Deployment", d:"Getting a new version of your code running where users can reach it.", l:"core", g:["process","devops","crash-course"],
 b:["Deployment used to mean copying files onto a server. Now it usually means building an image, pushing it to a registry, and asking an orchestrator to replace the running version gradually while health checks decide whether to continue.",
    "The property that matters is not speed but reversibility. Deploys that are small, frequent and instantly reversible are safe; big rare ones are where outages come from, because each contains a hundred changes and nobody knows which one broke it. Separating deploy from release, using feature flags, makes this safer still: the code ships dark, and you turn it on when you are ready."],
 k:["Small and frequent is safer than big and rare.",
    "Rolling back must be easy, tested and fast.",
    "Feature flags separate shipping code from releasing behaviour.",
    "Health checks gate the rollout; monitoring confirms it."],
 r:["Build","Rollback","Feature Flag","CI/CD","Production Environment"]},

{t:"Pseudocode", d:"Writing out the logic in plain language before you write it in a real language.", l:"core", g:["basics","practice","crash-course"],
 b:["No syntax, no compiler, no getting stuck on whether it is append or push. Just the steps: read the file, skip blank lines, group by user, write the totals. It separates thinking about the problem from fighting the language, and those are genuinely two different jobs.",
    "It stays useful long after you are fluent. Whiteboard interviews expect it, design discussions run on it, and writing the steps as comments in an empty function before filling them in is one of the most reliable ways to notice that step three is missing."],
 k:["Logic first, syntax later — they are separate problems.",
    "Ideal for whiteboards, design discussions and interviews.",
    "Write the steps as comments, then fill each one in.",
    "Exposes missing steps before you have invested in code."],
 r:["Algorithm","Function","Comment"]},

/* ------------------------------------------------------ testing, plainly */

{t:"Assertion", d:"A statement in a test that says what should be true — and fails the test if it is not.", l:"core", g:["testing","crash-course"],
 b:["Every test comes down to assertions. Run some code, then assert that the result equals what you expected, that a list has three items, that an exception was raised. Without an assertion a test only proves the code did not crash, which is a much weaker claim than most people assume.",
    "Good assertions are specific and few. Asserting on one meaningful outcome tells you exactly what broke; asserting on fifteen incidental details produces a test that fails every time anything nearby changes, which teaches the team to ignore failures."],
 k:["The check that gives a test its meaning.",
    "No assertion means you only proved it did not crash.",
    "Assert on behaviour, not on incidental detail.",
    "Failure messages should say what was expected and what happened."],
 x:{lang:"python", code:"def test_discount_applies_once():\n    order = Order(total=100)\n    apply_discount(order, \"SAVE10\")\n    assert order.total == 90\n    assert order.discount_code == \"SAVE10\""},
 r:["Unit Test","Mock","Fixture","Code Coverage"]},

{t:"Mock", d:"A stand-in for a real dependency during a test, so the test does not hit the network, the clock or your credit card.", l:"core", g:["testing","crash-course"],
 b:["Testing checkout should not charge a real card. Testing a weather feature should not depend on an external API being up. A mock replaces that collaborator with a fake you control, which makes tests fast, deterministic and runnable offline.",
    "Over-mocking is a real trap. Mock everything and your tests verify that your mocks behave like your mocks, while the actual integration stays untested. The usual balance is: mock what is slow, costly or outside your control, and use the real thing for everything you own."],
 k:["Replaces a real dependency to keep tests fast and deterministic.",
    "Mock the network, the clock, payments, randomness.",
    "Over-mocking tests your mocks, not your code.",
    "Keep some real integration tests as a safety net."],
 r:["Stub","Fixture","Unit Test","Integration Test"]},

{t:"Stub", d:"A minimal fake that returns a fixed answer, with no logic and no expectations about how it is called.", l:"core", g:["testing","crash-course"],
 b:["The distinction from a mock is about intent. A stub exists to supply data so the test can proceed — always return this user, always return an empty list. A mock exists to verify interaction: that the email service was called exactly once with these arguments.",
    "Most people use the words interchangeably and the tooling rarely separates them, but the distinction is worth holding because it points at what a test is really checking. Asserting on stub calls usually means the test is coupled to implementation detail rather than behaviour."],
 k:["Returns canned data; carries no expectations.",
    "A mock verifies interaction; a stub only supplies input.",
    "Ideal for putting a system into a specific state.",
    "Asserting against stubs often means testing implementation, not behaviour."],
 r:["Mock","Fixture","Unit Test"]},

{t:"Fixture", d:"Reusable test setup — the known starting state a test runs against.", l:"core", g:["testing","crash-course"],
 b:["Most tests need something to exist first: a user, a database with three rows, a temporary folder, a configured client. A fixture builds that once, hands it to the test, and cleans up afterwards, which keeps the test itself focused on the behaviour being checked.",
    "The rule that keeps a suite healthy is isolation: every test gets a fresh fixture. Tests that share mutable state pass individually and fail when run together, or fail only in a different order — the most exhausting category of flaky test to diagnose."],
 k:["Reusable setup and teardown for tests.",
    "Each test gets a fresh copy — never share mutable state.",
    "Shared state produces order-dependent, flaky failures.",
    "Keep the interesting values inside the test, not buried in the fixture."],
 x:{lang:"python", code:"import pytest\n\n@pytest.fixture\ndef user():\n    u = User.create(email=\"a@b.com\")\n    yield u              # test runs here\n    u.delete()           # teardown\n\ndef test_login(user):\n    assert login(user.email, \"pw\") is True"},
 r:["Unit Test","Mock","Seed Data","Flaky Test"]},

{t:"Code Coverage", d:"The percentage of your code that runs while the tests run — useful as a warning, misleading as a target.", l:"core", g:["testing","crash-course"],
 b:["Coverage tools mark which lines and branches were executed during a test run. Low coverage in a critical module is a genuine warning: nobody has checked that code. That is the honest use of the number.",
    "The dishonest use is treating it as a goal. Code can be executed by a test with no assertions at all and still count as covered, so ninety percent coverage can coexist with tests that verify nothing. Look at what is uncovered and ask whether it matters, rather than chasing the percentage."],
 k:["Measures which lines ran, not whether they were verified.",
    "Executed is not the same as tested.",
    "Low coverage in critical paths is a real signal.",
    "Making it a target guarantees it will be gamed."],
 r:["Unit Test","Assertion","Smoke Test","Continuous Integration"]},

{t:"Smoke Test", d:"A handful of quick checks that the system basically works before anyone bothers running the full suite.", l:"core", g:["testing","crash-course"],
 b:["The name comes from hardware: power it on and see if smoke comes out. In software it is the short list — does the app start, does the home page return 200, can a user log in, does the database respond. Sixty seconds, not thirty minutes.",
    "Its two natural homes are the start of a CI pipeline, where it fails fast before the expensive suite runs, and immediately after a deploy, where it is the first evidence that the new version is alive. Neither replaces real tests; both catch catastrophes cheaply."],
 k:["A few fast checks that the system is fundamentally alive.",
    "Run before the full suite to fail fast, and after every deploy.",
    "Should finish in seconds, not minutes.",
    "Catches catastrophes, not subtle bugs."],
 r:["Unit Test","Integration Test","Deployment","Continuous Integration"]},

{t:"Seed Data", d:"A known set of starting records loaded into a database so an environment is usable and tests are predictable.", l:"core", g:["testing","crash-course"],
 b:["An empty database makes an application impossible to develop against — nothing to list, nothing to click. Seed data provides a realistic starting set: a few users, a few orders, one of each edge case you care about.",
    "Keep it in the repository as code so everyone gets the same starting point and it stays in sync with the schema. And keep it obviously fake: seeding a development database with a copy of real customer data is one of the most common ways personal information leaks out of production."],
 k:["A known starting state for development and tests.",
    "Version it with the code so it tracks the schema.",
    "Include the edge cases you actually want to look at.",
    "Never seed with real personal data — anonymise it."],
 r:["Migration","Fixture","Staging Environment"]},

{t:"Migration", d:"A versioned script that changes a database's structure, so every environment ends up with the same schema.", l:"core", g:["testing","tools","crash-course"],
 b:["Adding a column by hand in one database and forgetting another is how environments drift apart. A migration writes that change as a numbered file in the repository, so applying them in order reproduces the schema exactly anywhere.",
    "The part that bites in production is that some changes lock a table while they run, which on a large table means downtime. The safe pattern for a rename is expand-and-contract: add the new column, write to both, backfill, switch reads, then drop the old one in a later release — several small deploys instead of one dangerous one."],
 k:["Schema changes as ordered, version-controlled files.",
    "Never edit a migration that has already been applied elsewhere.",
    "Some changes lock large tables — check before running in production.",
    "Rename via expand-and-contract, across several deploys."],
 r:["Schema","Seed Data","Deployment","Production Environment"]},

{t:"Array vs Linked List", d:"Contiguous memory slots with instant random index lookup vs scattered nodes connected by pointers.", l:"core", g:["data-structures","cs","crash-course"],
 b:["An Array is a row of adjacent parking spaces in RAM: because every slot is side-by-side, finding `arr[400]` takes a single arithmetic calculation (instant O(1)). But inserting an item in the middle requires shifting every single subsequent item over by one space (slow O(N)).",
    "A Linked List is a scavenger hunt: each node lives anywhere in RAM and holds a pointer pointing to the next clue. Inserting or deleting a node takes O(1) pointer swaps without shifting anything, but finding the 400th item forces you to walk through all 399 nodes before it (O(N) lookup)."],
 k:["Array: instant O(1) index lookup; expensive O(N) middle insertions.",
    "Linked List: fast O(1) insertions/deletions; slow O(N) linear lookups.",
    "Arrays maximize CPU cache locality because elements sit side-by-side in memory.",
    "Linked lists have memory overhead because every item stores an extra pointer."],
 x:{lang:"python", code:"# Array (List in Python): contiguous memory\narr = [10, 20, 30]\nprint(arr[2])      # O(1) instant jump: 30\n\n# Linked List Node: scattered memory connected by pointers\nclass Node:\n    def __init__(self, val, next_node=None):\n        self.val = val\n        self.next = next_node\n\nhead = Node(10, Node(20, Node(30))) # 10 -> 20 -> 30"},
 dg:"array-list",
 r:["Stack and Queue","Time Complexity","Binary Search","Data Type"]},

{t:"Stack and Queue", d:"LIFO (Last-In-First-Out) plates for undo history vs FIFO (First-In-First-Out) lines for processing tasks.", l:"core", g:["data-structures","cs","crash-course"],
 b:["A Stack is a stack of cafeteria trays: the last tray placed on top is the first one someone takes off (Last-In, First-Out or LIFO). You push items onto the top and pop them off the top. Every undo button (Ctrl+Z), browser back button, and function call stack operates on this exact data structure.",
    "A Queue is a line at the supermarket: the first person to join the line is the first person served (First-In, First-Out or FIFO). You enqueue items at the back and dequeue items from the front. Job queues, printer spoolers, and message brokers like RabbitMQ manage tasks in queue order."],
 k:["Stack: LIFO — `push` and `pop` happen at the same end (undo history, call stack).",
    "Queue: FIFO — `enqueue` at back, `dequeue` at front (job queues, print buffers).",
    "Both provide instant O(1) insertion and removal operations.",
    "Breadth-First Search (BFS) uses a queue; Depth-First Search (DFS) uses a stack."],
 x:{lang:"python", code:"# Stack (LIFO)\nstack = []\nstack.append(\"Page 1\"); stack.append(\"Page 2\")\nprint(stack.pop())    # \"Page 2\" (most recent first)\n\n# Queue (FIFO)\nfrom collections import deque\nqueue = deque([\"Task A\", \"Task B\"])\nqueue.append(\"Task C\")\nprint(queue.popleft()) # \"Task A\" (oldest first)"},
 dg:"stack-queue",
 r:["Recursion","Array vs Linked List","Graph Traversal","Message Queue"]},

{t:"Graph Traversal", a:"BFS and DFS", d:"The two ways to systematically explore a network of connected nodes — layer-by-layer (BFS) vs path-by-path (DFS).", l:"core", g:["algorithms","cs","crash-course"],
 b:["A graph is a network of nodes connected by edges — friends in a social network, web pages linked by URLs, or cities connected by roads. Traversal means visiting every connected node without getting trapped in infinite loops.",
    "Breadth-First Search (BFS) explores like a ripple in a pond: it checks all immediate neighbours (1 hop away) using a Queue before moving to 2 hops away, making it ideal for finding the shortest path. Depth-First Search (DFS) explores like a maze explorer: it walks as deep down one single branch as possible using a Stack (or recursion) before backtracking."],
 k:["BFS (Breadth-First): uses a Queue, explores level-by-level, finds shortest path.",
    "DFS (Depth-First): uses a Stack/recursion, explores deep paths, detects cycles.",
    "Always maintain a `visited` set to avoid infinite loops in cyclic graphs.",
    "Underpins GPS routing, social network suggestions, and web crawlers."],
 x:{lang:"python", code:"# BFS (Shortest Path exploration using a Queue)\nfrom collections import deque\ndef bfs(graph, start):\n    visited = {start}\n    queue = deque([start])\n    while queue:\n        node = queue.popleft()\n        for neighbor in graph[node]:\n            if neighbor not in visited:\n                visited.add(neighbor)\n                queue.append(neighbor)"},
 dg:"graph-traversal",
 r:["Stack and Queue","Recursion","Binary Search Tree","Graph"]},

{t:"Process vs Thread", d:"An isolated running application with private memory vs a lightweight execution unit sharing memory inside a process.", l:"core", g:["systems","cs","crash-course"],
 b:["A Process is an independent program running in its own private sandbox: your browser, your code editor, and your music player are separate processes. The operating system strictly isolates their memory, so if one process crashes or leaks RAM, it cannot corrupt the others.",
    "A Thread is a worker inside a process. A single browser process might spin up one thread to render the UI, another to download a video, and a third to run JavaScript. Because threads share the exact same heap memory space, communication between threads is lightning fast — but an unhandled crash in one thread kills the entire process, and concurrent access creates race conditions."],
 k:["Process: heavy, isolated memory address space, high context-switch cost.",
    "Thread: lightweight, shares heap and code with sibling threads in same process.",
    "Thread communication is fast (shared RAM); process communication needs IPC/sockets.",
    "A corrupted heap in one thread crashes the entire parent process."],
 x:{lang:"python", code:"import threading\n\ndef worker(thread_id):\n    print(f\"Thread {thread_id} executing inside shared process RAM\")\n\nt1 = threading.Thread(target=worker, args=(1,))\nt2 = threading.Thread(target=worker, args=(2,))\nt1.start(); t2.start()\nt1.join(); t2.join()"},
 dg:"process-thread",
 r:["Race Condition","Deadlock","Concurrency","Operating System"]},

{t:"SOLID Principles", d:"Five fundamental object-oriented design rules that stop software from deteriorating into fragile spaghetti code.", l:"core", g:["practice","architecture","crash-course"],
 b:["As codebases grow, small changes in one module tend to unexpectedly break five other unrelated modules. In the early 2000s, Robert C. Martin packaged five design principles under the acronym SOLID to build flexible, testable, and maintainable software.",
    "The five rules are: Single Responsibility (one job per class), Open/Closed (open for extension, closed for modification), Liskov Substitution (subclasses must behave correctly as drop-in replacements), Interface Segregation (small specific interfaces rather than bloated ones), and Dependency Inversion (depend on abstractions, not concrete implementations)."],
 k:["S — Single Responsibility: a class should have only one reason to change.",
    "O — Open/Closed: add new features by writing new code, not modifying old code.",
    "L — Liskov Substitution: child classes must honour the contract of their parents.",
    "I — Interface Segregation: favour multiple small interfaces over one massive one.",
    "D — Dependency Inversion: inject abstractions rather than hardcoding concrete classes."],
 x:{lang:"typescript", code:"// Single Responsibility & Dependency Inversion:\ninterface PaymentProcessor {\n  pay(amount: number): boolean;\n}\n\nclass CheckoutService {\n  // Injects abstraction rather than hardcoding Stripe/PayPal directly\n  constructor(private processor: PaymentProcessor) {}\n  \n  completeOrder(amount: number) {\n    this.processor.pay(amount);\n  }\n}"},
 dg:"solid",
 r:["Design Patterns","Refactoring","Technical Debt","Class"]},

]);
