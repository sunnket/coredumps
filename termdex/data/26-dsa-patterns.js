/* The interview patterns and structures, as dictionary entries.

   These are all taught inside the DSA track's lessons, but a reader who meets
   the phrase "monotonic stack" in a problem cannot look it up or find it in
   search unless it also exists as a term. This file closes that gap for the
   pattern vocabulary; 27-dsa-algorithms.js does the same for named algorithms. */

TD.add("cs-fundamentals", [

{t:"Deque", a:"Double-Ended Queue", d:"A queue you can push to and pop from at either end, both in O(1).", l:"core", g:["structures","fundamentals"],
 b:["A stack gives you one end, a queue gives you one at each end but only in one direction. A deque gives you both operations at both ends, which turns out to be exactly what several classic problems need. Python's `collections.deque` is the standard implementation, built as a doubly linked list of blocks.",
    "The reason it matters in practice is that a Python list is O(n) to pop from the front — every remaining element shifts. Using a list as a BFS queue silently turns an O(V+E) traversal into O(V²+VE), which is the single most common cause of a correct graph solution timing out."],
 k:["O(1) push and pop at both ends, unlike a list which is O(n) at the front.",
    "Use it for BFS queues — `popleft`, never `pop(0)`.",
    "Sliding-window maximum keeps a decreasing deque of indices.",
    "0-1 BFS uses `appendleft` for zero-weight edges and `append` for weight-one."],
 x:{lang:"python", code:"from collections import deque\n\nq = deque([1, 2, 3])\nq.append(4)        # right: [1,2,3,4]\nq.appendleft(0)    # left:  [0,1,2,3,4]\nq.popleft()        # O(1) -- a list would be O(n)\n\nwindow = deque(maxlen=3)   # auto-evicts from the left"},
 r:["Queue","Stack","Monotonic Stack","Breadth-First Search"]},

{t:"Prefix Sum", d:"A precomputed array of running totals, so the sum of any range becomes one subtraction.", l:"core", g:["patterns","fundamentals"],
 b:["Build an array where `p[i]` holds the sum of the first i elements. Then the sum of any range `l..r` is `p[r+1] - p[l]` — a single subtraction, however long the range. You pay O(n) once and every later query is O(1).",
    "Write it with a leading zero and length n+1. That way the empty prefix has a value and the formula never needs a special case for a range starting at index 0, which is where the off-by-one bugs come from."],
 k:["O(n) to build, O(1) per range query. Worth it from about two queries upwards.",
    "Use a leading zero and length n+1 so `p[r+1] - p[l]` always works.",
    "Combined with a hash map it counts subarrays summing to k — seed it with `{0: 1}`.",
    "Works where a sliding window fails, because it tolerates negative numbers."],
 x:{lang:"python", code:"p = [0] * (len(nums) + 1)\nfor i, x in enumerate(nums):\n    p[i + 1] = p[i] + x\n\n# sum of nums[l..r] inclusive:\ntotal = p[r + 1] - p[l]"},
 r:["Difference Array","Array","Sliding Window","Hash Table"]},

{t:"Difference Array", d:"The mirror of a prefix sum: apply many range updates in O(1) each, then reconstruct in one pass.", l:"intermediate", g:["patterns","fundamentals"],
 b:["When you must add a value across many ranges and only read the array at the end, touching every element of every range is wasteful. Instead record `+val` at the start index and `-val` just past the end. A prefix sum over that difference array reconstructs the final values.",
    "The saving is dramatic. A thousand updates over a million elements costs 10⁶ + 10³ operations instead of 10⁹. The flight-booking and car-pooling interview problems are exactly this, and are close to impossible to pass without it."],
 k:["`diff[l] += val` and `diff[r+1] -= val`, then prefix-sum to rebuild.",
    "Turns u range updates from O(n·u) into O(n+u).",
    "Allocate n+1 slots so `r+1` is always writable.",
    "Only works when all updates happen before any read."],
 x:{lang:"python", code:"diff = [0] * (n + 1)\nfor l, r, val in updates:\n    diff[l] += val\n    diff[r + 1] -= val\n\nout, run = [], 0\nfor i in range(n):\n    run += diff[i]\n    out.append(run)"},
 r:["Prefix Sum","Array","Time Complexity","Sliding Window"]},

{t:"Fast and Slow Pointers", a:"Floyd's Cycle Detection", d:"Two pointers moving at different speeds through a sequence, to find cycles or the midpoint in O(1) space.", l:"core", g:["patterns","fundamentals"],
 b:["One pointer advances a step at a time, the other two. If the structure contains a cycle the fast pointer eventually laps the slow one and they meet; if there is no cycle the fast pointer simply runs off the end. When the fast pointer reaches the end, the slow one is exactly at the middle.",
    "The elegance is the space cost: it detects a cycle in O(1) memory where a visited-set needs O(n). The follow-up — finding where the cycle *starts* — has a neat proof: reset one pointer to the head, advance both one step at a time, and they meet at the cycle entrance."],
 k:["Slow moves one, fast moves two. Meeting means a cycle exists.",
    "When fast reaches the end, slow is at the midpoint.",
    "O(1) space, versus O(n) for a visited set.",
    "To find the cycle's start: reset one pointer to the head, then advance both by one."],
 x:{lang:"python", code:"def has_cycle(head):\n    slow = fast = head\n    while fast and fast.next:\n        slow = slow.next\n        fast = fast.next.next\n        if slow is fast:\n            return True      # they met -- a cycle\n    return False"},
 r:["Two Pointers","Linked List","Space Complexity","Cyclic Sort"]},

{t:"Monotonic Stack", d:"A stack kept deliberately sorted, so that popping an element is the moment you discover its answer.", l:"intermediate", g:["patterns","fundamentals"],
 b:["Keep the stack decreasing. When a larger value arrives you pop everything smaller first — and each element you pop has just found its *next greater element*. The popping is not cleanup; it is the answer-generating step.",
    "The complexity surprises people. The inner `while` loop looks like it makes this quadratic, and it does not: each element is pushed exactly once and popped at most once, so total work is at most 2n. Being able to say *\"each element enters and leaves the stack once, so it is O(n) amortised\"* is a standout interview moment, because interviewers ask about that loop deliberately."],
 k:["Decreasing stack finds the next greater; increasing finds the next smaller.",
    "O(n) amortised — each element is pushed once and popped once.",
    "Store indices rather than values, so you can write into a result array.",
    "The trigger phrases are *next greater*, *previous smaller*, *histogram*, *stock span*."],
 x:{lang:"python", code:"res, stack = [-1] * len(nums), []   # stack holds INDICES\nfor i, v in enumerate(nums):\n    while stack and nums[stack[-1]] < v:\n        res[stack.pop()] = v         # v is the next greater\n    stack.append(i)"},
 r:["Stack","Deque","Time Complexity","Amortised Analysis"]},

{t:"Sliding Window Maximum", d:"The maximum of every window of size k, in one pass, using a monotonic deque.", l:"intermediate", g:["patterns","fundamentals"],
 b:["The naive answer recomputes a maximum for each window, which is O(n·k). The trick is a deque holding indices whose values decrease from front to back: the front is always the current window's maximum, and anything smaller than an incoming value can never be the answer again, so it is discarded.",
    "Two evictions happen each step. Pop from the back while the incoming value is larger, and pop from the front if that index has fallen out of the window. Each index enters and leaves once, so the whole scan is O(n)."],
 k:["A deque of indices, values decreasing from front to back.",
    "The front is the window maximum; check it has not slid out of range.",
    "O(n) total — each index is pushed once and popped once.",
    "The same shape solves *shortest subarray with sum at least k* with a monotonic deque of prefix sums."],
 x:{lang:"python", code:"from collections import deque\n\ndq, out = deque(), []\nfor i, v in enumerate(nums):\n    while dq and nums[dq[-1]] <= v: dq.pop()      # smaller: never useful again\n    dq.append(i)\n    if dq[0] <= i - k: dq.popleft()               # slid out of the window\n    if i >= k - 1: out.append(nums[dq[0]])"},
 r:["Monotonic Stack","Deque","Sliding Window","Two Pointers"]},

{t:"Merge Intervals", d:"Sorting ranges by start, then folding each into the previous one whenever they overlap.", l:"core", g:["patterns","fundamentals"],
 b:["Sorting by start time is what makes the problem tractable: once sorted, any interval can only overlap the one immediately before it, so a single pass suffices. Without sorting you would compare every pair, which is quadratic.",
    "The detail that catches people is taking the maximum of the two end points when merging. Given `[1,10]` followed by `[2,3]`, writing the new end directly shrinks the merged interval to `[1,3]` and silently loses everything from 3 to 10."],
 k:["Sort by start; then only the previous interval can overlap.",
    "Merge with `end = max(prev_end, end)` — forgetting the max swallows nested intervals.",
    "O(n log n), dominated by the sort.",
    "If the input is already sorted, do not sort again — that throws away an O(n) solution."],
 x:{lang:"python", code:"intervals.sort(key=lambda x: x[0])\nout = []\nfor start, end in intervals:\n    if out and start <= out[-1][1]:\n        out[-1][1] = max(out[-1][1], end)    # the max matters\n    else:\n        out.append([start, end])"},
 r:["Sweep Line","Sorting Algorithm","Greedy Algorithm","Array"]},

{t:"Sweep Line", d:"Turning intervals into start and end events, sorting them, and tracking a running count.", l:"intermediate", g:["patterns","fundamentals"],
 b:["When the question is *how many things are active at the same time* — meeting rooms, concurrent calls, peak load — stop thinking about intervals and think about events. Each interval contributes a +1 at its start and a −1 at its end. Sort the events by time and the running total is the concurrency at every moment; its peak is the answer.",
    "The tie-breaking detail is deliberate: sorting `(time, delta)` puts −1 before +1 because −1 < +1, so a room freed at 10 is reusable at 10. If the problem says a resource cannot be reused at the same instant, flip the signs. Asking which convention applies is a good clarifying question."],
 k:["Each interval becomes `(start, +1)` and `(end, -1)`; sort and accumulate.",
    "The running maximum is the peak concurrency.",
    "Sorting tuples puts −1 before +1, so touching intervals share a resource.",
    "O(n log n) for the sort; the sweep itself is linear."],
 x:{lang:"python", code:"events = []\nfor s, e in intervals:\n    events.append((s, 1)); events.append((e, -1))\nevents.sort()\n\ncur = best = 0\nfor _, delta in events:\n    cur += delta\n    best = max(best, cur)"},
 r:["Merge Intervals","Sorting Algorithm","Heap","Greedy Algorithm"]},

{t:"Cyclic Sort", d:"Placing each value at its own index in one pass, for arrays holding the numbers 1..n.", l:"intermediate", g:["patterns","fundamentals"],
 b:["When an array of length n holds values drawn from 1..n, the value itself tells you where it belongs — value v goes at index v−1. Swap each element home until it is already correct, then a single scan finds whichever slot holds the wrong number. That is the missing value, the duplicate, or whatever the question asked for.",
    "The comparison must be by value (`nums[i] != nums[home]`), not by index. Comparing indices loops forever when a value is duplicated, because the swap makes no progress. This one line is where most implementations go wrong."],
 k:["For values in 1..n, value v belongs at index v−1.",
    "O(n) time and O(1) space — each swap places one element permanently.",
    "Use a `while` loop, not `for`: the index only advances on a match.",
    "Compare by value, not index, or duplicates cause an infinite loop."],
 x:{lang:"python", code:"i = 0\nwhile i < len(nums):\n    home = nums[i] - 1\n    if nums[i] != nums[home]:            # by VALUE, not index\n        nums[i], nums[home] = nums[home], nums[i]\n    else:\n        i += 1\n# first index where nums[i] != i+1 is the missing number"},
 r:["Array","Two Pointers","Space Complexity","Bit Manipulation"]},

{t:"Top-K Pattern", d:"Keeping a heap of exactly k elements so you never sort the whole input.", l:"core", g:["patterns","fundamentals"],
 b:["To find the k largest values, push each element into a **min**-heap and pop whenever it exceeds size k. The heap then holds the k largest, and its root is the k-th largest. It reads backwards until you see why: evicting the smallest is how you keep the largest.",
    "The cost is O(n log k) rather than sorting's O(n log n), which matters a lot when k is small and n is huge. It also works on a stream of unknown length, because you only ever hold k items in memory."],
 k:["k largest → min-heap of size k; k smallest → max-heap of size k.",
    "O(n log k) time and O(k) space; better than sorting when k ≪ n.",
    "Works on streams — you never need the whole input in memory.",
    "The trigger phrases are *top k*, *k closest*, *k most frequent*, *merge k sorted*."],
 x:{lang:"python", code:"import heapq\n\nh = []\nfor x in nums:\n    heapq.heappush(h, x)\n    if len(h) > k:\n        heapq.heappop(h)      # evict the smallest\n# h holds the k largest; h[0] is the k-th largest\n\nheapq.nlargest(k, nums)       # the library one-liner"},
 r:["Heap","Priority Queue","Quickselect","Two Heaps"]},

{t:"Two Heaps", d:"A max-heap for the lower half and a min-heap for the upper half, so the median sits at the boundary.", l:"advanced", g:["patterns","fundamentals"],
 b:["Split the data in two. The smaller half lives in a max-heap so its largest value is instantly available; the larger half lives in a min-heap so its smallest is. The median is either the top of the bigger heap, or the average of the two tops when the halves are equal.",
    "The insertion routine is deliberately mechanical: always push into the low heap, immediately move its top across to the high heap, then rebalance if the high heap has grown larger. Routing everything one way makes the ordering invariant automatic — trying to push straight into the correct heap needs a comparison and gets the edge cases wrong."],
 k:["Max-heap for the low half, min-heap for the high half; the median is at the boundary.",
    "Push to low, move low's top to high, then rebalance. Always in that order.",
    "Insert is O(log n); reading the median is O(1).",
    "The trigger is *median of a stream* or *balance two halves*."],
 x:{lang:"python", code:"import heapq\nlo, hi = [], []                       # lo is a max-heap (negated)\n\ndef add(x):\n    heapq.heappush(lo, -x)\n    heapq.heappush(hi, -heapq.heappop(lo))\n    if len(hi) > len(lo):\n        heapq.heappush(lo, -heapq.heappop(hi))\n\ndef median():\n    return -lo[0] if len(lo) > len(hi) else (-lo[0] + hi[0]) / 2"},
 r:["Heap","Top-K Pattern","Priority Queue","Sliding Window"]},

{t:"Binary Search on the Answer", d:"Binary searching over a range of possible answers rather than over an array.", l:"advanced", g:["patterns","fundamentals"],
 b:["Ordinary binary search looks for a value inside a sorted array. This looks for a value in an abstract space of candidate answers, and it works whenever feasibility is monotonic: if a speed of 5 works, then 6, 7 and 8 all work too. So you binary-search over speeds.",
    "Recognising it is the hard part. The tells are a question phrased as a **minimum maximum** or a **maximum minimum** — *minimise the largest sum*, *maximise the smallest distance* — or *find the smallest capacity such that…*, usually with a huge value range and a small n. The method is then mechanical: write a `check(x)` predicate that answers *is x feasible?* in O(n), and binary search over x."],
 k:["Search the answer space, not the input. Needs feasibility to be monotonic.",
    "Tells: *minimum maximum*, *maximum minimum*, *smallest X such that*, huge value range.",
    "Write `check(x)` first, then wrap binary search around it. O(n log range).",
    "Classic problems: Koko Eating Bananas, Split Array Largest Sum, Capacity To Ship Packages."],
 x:{lang:"python", code:"lo, hi = min_possible, max_possible\nwhile lo < hi:\n    mid = (lo + hi) // 2\n    if check(mid):      # feasible -> answer is mid or smaller\n        hi = mid\n    else:               # infeasible -> answer is strictly larger\n        lo = mid + 1\nreturn lo"},
 r:["Binary Search","Greedy Algorithm","Time Complexity","Algorithm"]},

{t:"Multi-Source BFS", d:"Seeding a breadth-first search with every starting point at once, so one pass labels every node with its distance to the nearest source.", l:"intermediate", g:["patterns","fundamentals"],
 b:["The standard BFS starts from one node. When something spreads from many origins simultaneously — rotting oranges, gates in a maze, every water cell in a grid — put all of them in the queue at distance zero before the loop begins. The wavefronts expand together and naturally meet where the sources are equidistant.",
    "This is the pattern people most often miss, and it collapses an apparently O(sources × V) problem into a single O(V+E) traversal. Running a separate BFS per source and taking the minimum gives the same answer far more slowly."],
 k:["Enqueue every source at distance 0 before the loop starts.",
    "One O(V+E) pass replaces one BFS per source.",
    "Every node ends up labelled with its distance to the *nearest* source.",
    "Classic problems: Rotting Oranges, 01 Matrix, Walls and Gates."],
 x:{lang:"python", code:"q = deque()\nfor r in range(rows):\n    for c in range(cols):\n        if grid[r][c] == SOURCE:\n            dist[r][c] = 0\n            q.append((r, c))      # ALL sources, before the loop\nwhile q:\n    ...                            # ordinary BFS from here"},
 r:["Breadth-First Search","Graph Traversal","Deque","Queue"]},

{t:"0-1 BFS", d:"Shortest paths when every edge costs 0 or 1, using a deque instead of a heap.", l:"advanced", g:["patterns","fundamentals"],
 b:["Dijkstra's heap exists to always process the closest unfinalised node. When edge weights are only 0 or 1, a deque achieves the same ordering for free: push zero-weight moves to the front and one-weight moves to the back. The deque then stays sorted by distance without any comparisons.",
    "The result is O(V+E) rather than Dijkstra's O(E log V). It shows up in *minimum number of obstacles to remove* and *minimum cost to make the grid have a valid path* — problems where moving normally is free and doing something special costs one."],
 k:["Weights of only 0 and 1: use a deque, not a heap.",
    "`appendleft` for a 0-weight edge, `append` for a weight-1 edge.",
    "O(V+E) instead of Dijkstra's O(E log V).",
    "Generalises to 0-k weights with a bucket queue (Dial's algorithm)."],
 x:{lang:"python", code:"dq = deque([start])\ndist[start] = 0\nwhile dq:\n    u = dq.popleft()\n    for v, w in adj[u]:\n        if dist[u] + w < dist[v]:\n            dist[v] = dist[u] + w\n            dq.appendleft(v) if w == 0 else dq.append(v)"},
 r:["Breadth-First Search","Dijkstra's Algorithm","Deque","Graph"]},

{t:"Quickselect", d:"Finding the k-th smallest element in O(n) average time by partitioning and recursing into one side only.", l:"advanced", g:["algorithms","fundamentals"],
 b:["Partition the array around a pivot exactly as quicksort does, which lands the pivot at its final sorted position. If that position is k you are finished. Otherwise recurse into whichever side contains k — and only that side, which is the entire saving.",
    "Recursing into one half gives T(n) = T(n/2) + O(n), and the geometric series n + n/2 + n/4 … sums to 2n, so the average is O(n). The worst case is O(n²) with adversarial pivots, fixed in practice by choosing the pivot at random. When an interviewer says *your heap solution is O(n log k) — can you do better?*, this is the answer they want."],
 k:["O(n) average, O(n²) worst; randomise the pivot to make the worst case negligible.",
    "Recurses into one side only — that is why it beats sorting.",
    "In-place, O(1) extra space, but it mutates the input.",
    "Beats a heap for the k-th element; a heap still wins for a stream."],
 x:{lang:"python", code:"import random\n\ndef quickselect(nums, k):            # k-th smallest, 0-indexed\n    lo, hi = 0, len(nums) - 1\n    while lo <= hi:\n        p = partition(nums, lo, hi)  # random pivot inside\n        if   p == k: return nums[p]\n        elif p < k:  lo = p + 1\n        else:        hi = p - 1"},
 r:["Quick Sort","Top-K Pattern","Heap","Time Complexity"]},

{t:"Reservoir Sampling", d:"Picking k random items from a stream of unknown length using only O(k) memory.", l:"advanced", g:["algorithms","fundamentals"],
 b:["Keep the first k items. For each later item i, keep it with probability k/i, and if kept, have it replace one of the current k at random. When the stream ends, every item that ever passed through has exactly the same probability of being in your sample.",
    "The proof is a short induction and worth being able to sketch: at any point the invariant is that each of the i items seen so far is present with probability k/i. It is the canonical answer to *sample from a stream whose length you do not know*, and it is trivial if you have seen it and effectively impossible if you have not."],
 k:["O(k) memory regardless of stream length; one pass, no rewind.",
    "Keep item i with probability k/i, replacing a random member of the reservoir.",
    "Every element ends with equal inclusion probability — provable by induction.",
    "k = 1 is the common interview version: *pick one random node from a linked list*."],
 x:{lang:"python", code:"import random\n\ndef sample(stream, k):\n    res = []\n    for i, item in enumerate(stream):\n        if i < k:\n            res.append(item)\n        else:\n            j = random.randint(0, i)     # keep with probability k/(i+1)\n            if j < k:\n                res[j] = item\n    return res"},
 r:["Algorithm","Space Complexity","Random Seed","Sampling"]},

{t:"Bitmask DP", d:"Dynamic programming where the state is a subset of items encoded as the bits of an integer.", l:"advanced", g:["patterns","fundamentals"],
 b:["With n items, a subset is an n-bit integer: bit i is set when item i is included. `dp[mask]` then stores the answer for that subset, and you iterate over all 2ⁿ masks. With n = 20 that is about a million states, which is instant.",
    "The tell is the constraint. A bound of n ≤ 20 (sometimes 22) has no other explanation — no polynomial algorithm needs a bound that small, so the intended solution is exponential in n and this is how you write it. Spotting that from the constraints alone makes you look very well prepared."],
 k:["A subset is an integer; `dp[mask]` is the answer for that subset.",
    "The giveaway is `n ≤ 20` in the constraints. Nothing else explains such a bound.",
    "`mask | (1<<i)` adds, `mask & ~(1<<i)` removes, `mask & (1<<i)` tests.",
    "`(sub - 1) & mask` enumerates every submask of a mask."],
 x:{lang:"python", code:"full = (1 << n) - 1\ndp = [INF] * (1 << n)\ndp[0] = 0\nfor mask in range(1 << n):\n    for i in range(n):\n        if not (mask & (1 << i)):\n            dp[mask | (1 << i)] = min(dp[mask | (1 << i)], dp[mask] + cost(mask, i))\nreturn dp[full]"},
 r:["Dynamic Programming","Bit Manipulation","Backtracking","Time Complexity"]},

{t:"Digit DP", d:"Counting numbers up to N with some digit property, by building the number one digit at a time.", l:"advanced", g:["patterns","fundamentals"],
 b:["When the question is *how many numbers from 1 to N satisfy property P* and N can be 10¹⁸, looping is impossible. Instead you build numbers digit by digit and count whole families at once, memoising on the position, whether the prefix is still equal to N's prefix, and whatever the property needs.",
    "The `tight` flag is the heart of it. While your prefix exactly matches N's prefix, the next digit is capped by N's digit at that position; the moment you place something smaller, every later digit is free from 0 to 9. That single boolean is what turns an impossible loop into a small table."],
 k:["State is `(position, tight, extra)`; memoise on all three.",
    "`tight` means the prefix still matches N's, so the next digit is capped.",
    "Handles bounds up to 10¹⁸ where iteration is impossible.",
    "Rare, but if it appears and you have never seen it, the interview is over."],
 x:{lang:"python", code:"from functools import cache\n\ndigits = list(map(int, str(N)))\n\n@cache\ndef go(pos, tight, state):\n    if pos == len(digits):\n        return 1 if accepts(state) else 0\n    cap = digits[pos] if tight else 9\n    return sum(go(pos + 1, tight and d == cap, update(state, d))\n               for d in range(cap + 1))"},
 r:["Dynamic Programming","Memoisation","Recursion","Bitmask DP"]},

{t:"Segment Tree", d:"A binary tree over array ranges that answers range queries and point updates, both in O(log n).", l:"advanced", g:["structures","fundamentals"],
 b:["Each node stores the aggregate — sum, min, max, gcd — of a contiguous range, with the root covering everything and leaves covering single elements. A query decomposes into O(log n) nodes; an update touches only the O(log n) nodes on one root-to-leaf path.",
    "It is the general answer where a prefix-sum array is not enough. A prefix sum gives O(1) queries but O(n) updates; a plain array gives O(1) updates but O(n) queries. A segment tree gives O(log n) for both, and with lazy propagation it also handles range updates."],
 k:["O(log n) range query and point update; O(4n) space in the array form.",
    "Handles min, max and gcd — not just sums, unlike a Fenwick tree.",
    "Lazy propagation extends it to range updates.",
    "Rarely asked at entry level. Learn the trade-off sentence before the implementation."],
 x:{lang:"python", code:"# iterative sum segment tree\nsize = 1\nwhile size < n: size *= 2\ntree = [0] * (2 * size)\n\ndef update(i, val):\n    i += size; tree[i] = val\n    while i > 1:\n        i //= 2\n        tree[i] = tree[2*i] + tree[2*i+1]"},
 r:["Fenwick Tree","Binary Tree","Prefix Sum","Time Complexity"]},

{t:"Fenwick Tree", a:"Binary Indexed Tree · BIT", d:"A compact structure giving O(log n) prefix sums and point updates in about ten lines of code.", l:"advanced", g:["structures","fundamentals"],
 b:["It exploits the binary representation of indices: each position is responsible for a range whose length is its lowest set bit, so `i & -i` walks the structure. That single trick makes it far shorter to write than a segment tree, which is why competitive programmers reach for it first.",
    "The limitation is that it only handles invertible operations — sums and counts, not minimum or maximum — because a range query is computed as the difference of two prefixes. When you need min or max, or lazy range updates, you need a segment tree instead."],
 k:["`i & -i` isolates the lowest set bit and drives both the update and query walks.",
    "O(log n) prefix sum and point update; O(n) space and ~10 lines.",
    "Sums and counts only — no min or max, because it relies on subtraction.",
    "The interview-worthy sentence is the trade-off against a prefix-sum array, not the code."],
 x:{lang:"python", code:"tree = [0] * (n + 1)              # 1-indexed\n\ndef update(i, delta):\n    while i <= n:\n        tree[i] += delta\n        i += i & -i               # next range that covers i\n\ndef prefix(i):\n    s = 0\n    while i > 0:\n        s += tree[i]\n        i -= i & -i\n    return s"},
 r:["Segment Tree","Prefix Sum","Bit Manipulation","Time Complexity"]},

{t:"Morris Traversal", d:"Traversing a binary tree in order using O(1) space, by temporarily rewiring null right-pointers as breadcrumbs.", l:"advanced", g:["structures","fundamentals"],
 b:["A recursive inorder traversal costs O(h) stack space and an iterative one costs O(h) for the explicit stack. Morris traversal uses neither: before descending left, it finds the current node's inorder predecessor and points that node's empty right pointer back at the current node. When the traversal later returns via that thread, it restores the pointer and moves right.",
    "It is rarely required, and it is exactly the answer expected when an interviewer asks *can you do this without recursion or a stack?* on a tree problem. Almost nobody has it, which is precisely why it is worth knowing."],
 k:["O(1) auxiliary space — no recursion and no stack.",
    "Threads the predecessor's null right pointer back to the current node, then undoes it.",
    "The tree is temporarily modified but fully restored by the end.",
    "The expected answer to *inorder traversal in constant space*."],
 x:{lang:"python", code:"cur = root\nwhile cur:\n    if not cur.left:\n        visit(cur); cur = cur.right\n    else:\n        pred = cur.left\n        while pred.right and pred.right is not cur:\n            pred = pred.right\n        if not pred.right:\n            pred.right = cur          # thread it\n            cur = cur.left\n        else:\n            pred.right = None         # unthread, restore\n            visit(cur); cur = cur.right"},
 r:["Binary Tree","Tree","Space Complexity","Graph Traversal"]},

{t:"LRU Cache", a:"Least Recently Used", d:"A fixed-size cache that evicts whatever was used longest ago, with O(1) reads and writes.", l:"intermediate", g:["structures","fundamentals"],
 b:["The O(1) requirement for both `get` and `put` forces a specific combination: a hash map from key to node for instant lookup, plus a doubly linked list to maintain recency order. Reading a key unlinks its node and moves it to the head; writing past capacity drops the tail.",
    "The list must be doubly linked. Removing a node in O(1) requires knowing its predecessor, and only a `prev` pointer gives you that — with a singly linked list you would have to scan. That is the question behind the question, and it is one of the most-asked design problems in the industry."],
 k:["Hash map for O(1) lookup + doubly linked list for O(1) reordering.",
    "Doubly linked because unlinking in O(1) needs a `prev` pointer.",
    "A read counts as a use — move the node to the head on `get`, not just `put`.",
    "`OrderedDict` gives it in ten lines; offer the manual version anyway."],
 x:{lang:"python", code:"from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, cap):\n        self.cap, self.d = cap, OrderedDict()\n    def get(self, key):\n        if key not in self.d: return -1\n        self.d.move_to_end(key)        # a read is a use\n        return self.d[key]\n    def put(self, key, val):\n        if key in self.d: self.d.move_to_end(key)\n        self.d[key] = val\n        if len(self.d) > self.cap:\n            self.d.popitem(last=False)  # evict least recent"},
 r:["LFU Cache","Hash Table","Linked List","Cache"]},

{t:"LFU Cache", a:"Least Frequently Used", d:"A cache that evicts the item used fewest times, breaking ties by recency.", l:"advanced", g:["structures","fundamentals"],
 b:["Harder than LRU because you must track counts as well as order. The standard construction keeps three things: a map from key to node, a map from frequency to a doubly linked list of nodes with that frequency, and a running `min_freq` pointer so eviction is O(1).",
    "Every access moves a node from its current frequency list into the next one up. `min_freq` only ever increases when the list at the minimum becomes empty, which is what keeps eviction constant time. It is a genuine step up in difficulty and usually appears as the follow-up to LRU."],
 k:["Key→node map, frequency→list map, and a `min_freq` pointer.",
    "An access moves the node from frequency list f to f+1.",
    "Eviction takes the tail of the `min_freq` list — recency breaks the tie.",
    "Usually asked as the follow-up once you have solved LRU."],
 x:{lang:"python", code:"# on get(key):\n#   f = freq[key]\n#   remove node from lists[f]; append to lists[f+1]\n#   freq[key] = f + 1\n#   if f == min_freq and not lists[f]: min_freq += 1\n#\n# on eviction: pop the TAIL of lists[min_freq]"},
 r:["LRU Cache","Hash Table","Linked List","Cache"]},

{t:"Hash Flooding", d:"A denial-of-service attack that feeds a server keys engineered to land in the same hash bucket.", l:"advanced", g:["structures","security","fundamentals"],
 b:["A hash table is O(1) on average because keys spread across buckets. An attacker who knows your hash function can craft thousands of keys that all collide, collapsing every lookup to a linear scan of one long chain — so a handful of requests consumes enormous CPU.",
    "Two real mitigations exist and both are worth naming. Randomised seeding (SipHash, which Python enables by default) means the attacker cannot predict which keys collide. Alternatively, degrade an overloaded bucket from a linked list to a balanced tree, which caps the damage at O(log n) — this is exactly what Java 8's HashMap does."],
 k:["Crafted colliding keys turn O(1) lookups into O(n) scans.",
    "Mitigated by a randomised hash seed (SipHash) so collisions are unpredictable.",
    "Or by treeifying overloaded buckets — Java 8's HashMap degrades to O(log n), not O(n).",
    "Knowing this signals real depth when asked about hash table worst cases."],
 x:{lang:"python", code:"# Python randomises str hashing per process by default:\n#   PYTHONHASHSEED=random  (the default since 3.3)\n#\n# Disabling it for reproducibility re-opens the attack:\n#   PYTHONHASHSEED=0       <- do not do this on a server"},
 r:["Hash Table","Hash Collision","Denial of Service","Time Complexity"]},

{t:"Optimal Substructure", d:"The property that an optimal solution is built from optimal solutions to its subproblems.", l:"intermediate", g:["patterns","fundamentals"],
 b:["If the shortest route from A to C passes through B, then the portion from A to B must itself be the shortest route from A to B — otherwise you could swap in a better one and improve the whole. That is optimal substructure, and it is one of the two conditions a problem must satisfy before dynamic programming or a greedy algorithm applies.",
    "It is not universal. Longest *simple* path in a graph lacks it: joining two optimal sub-paths can revisit a node, so the pieces do not compose. Checking that the property actually holds is the step that separates applying DP correctly from applying it hopefully."],
 k:["An optimal whole is composed of optimal parts — that is what makes DP valid.",
    "Paired with overlapping subproblems, it is the precondition for dynamic programming.",
    "Greedy algorithms need it too, plus a provably safe local choice.",
    "Longest simple path lacks it, which is why that problem is NP-hard."],
 x:{lang:"text", code:"shortest(A -> C) = shortest(A -> B) + shortest(B -> C)\n                   ^ must itself be optimal, or you could\n                     substitute a better one and improve the whole\n\nLongest simple path does NOT compose:\n  joining two optimal halves may revisit a node."},
 r:["Overlapping Subproblems","Dynamic Programming","Greedy Algorithm","Recursion"]},

{t:"Overlapping Subproblems", d:"The property that the same smaller question is asked repeatedly, so caching its answer pays off.", l:"intermediate", g:["patterns","fundamentals"],
 b:["Naive recursive Fibonacci computes `fib(3)` hundreds of times on the way to `fib(30)`. The recursion tree is exponential, yet it contains only n distinct questions. Storing each answer the first time collapses the tree into a table and the exponent disappears.",
    "This is what separates dynamic programming from divide and conquer. Merge sort splits into subproblems that never repeat, so memoising it buys nothing; Fibonacci's subproblems overlap heavily, so memoising it turns O(2ⁿ) into O(n)."],
 k:["The same subproblem recurs, so caching turns exponential into polynomial.",
    "Together with optimal substructure, it is what makes a problem a DP problem.",
    "Divide and conquer (merge sort) has *no* overlap — memoising it gains nothing.",
    "Count distinct states, not recursive calls: that is the real complexity."],
 x:{lang:"python", code:"from functools import cache\n\n@cache                      # the whole difference\ndef fib(n):\n    if n <= 1: return n\n    return fib(n-1) + fib(n-2)\n\n# without @cache: O(2^n).  With it: O(n) -- n distinct states."},
 r:["Optimal Substructure","Dynamic Programming","Memoisation","Recursion"]},

{t:"Tabulation", d:"Building a dynamic-programming table bottom-up with loops, rather than top-down with recursion.", l:"intermediate", g:["patterns","fundamentals"],
 b:["Memoisation is top-down: write the natural recursion and cache its results. Tabulation is bottom-up: work out the order in which states depend on each other, then fill an array with loops so every value is ready before it is needed.",
    "Both give the same complexity, and each has a genuine advantage. Memoisation is easier to write and only computes the states you actually reach; tabulation avoids recursion-depth limits, has lower constant overhead, and makes space optimisation obvious — once you can see that row i only reads row i−1, you can collapse the table to two rows."],
 k:["Bottom-up loops instead of top-down recursion; identical complexity.",
    "No stack-overflow risk, and lower constant factors than memoisation.",
    "Makes space optimisation visible: if row i reads only row i−1, keep two rows.",
    "Memoisation is easier to write; convert to tabulation when asked to optimise."],
 x:{lang:"python", code:"# memoised (top-down)          |  tabulated (bottom-up)\n@cache                        |  dp = [0] * (n + 1)\ndef f(i):                     |  dp[0], dp[1] = 0, 1\n    if i <= 1: return i       |  for i in range(2, n + 1):\n    return f(i-1) + f(i-2)    |      dp[i] = dp[i-1] + dp[i-2]"},
 r:["Memoisation","Dynamic Programming","Overlapping Subproblems","Space Complexity"]}

]);
