/* Hardcore Engineer Hacks & Power-User Systems Vocabulary.
   Covers OS internals, Windows kernel job objects, low-level networking,
   packet monitoring, performance profiling, model quantization,
   knowledge distillation, defensive security, and binary reverse engineering. */

TD.add("os-networking", [

{t:"Job Object", a:"Windows Job Object", d:"A Windows kernel object that groups one or more processes together to enforce hard limits on memory commitment, CPU rate, and process lifetime.", l:"advanced", g:["os","windows","kernel","processes"],
 b:["A Job Object is the Windows kernel equivalent of a Linux control group (cgroup). It acts as a sandbox that manages collections of processes as a single unit. Limits applied to a Job Object are inherited by all child processes created inside it, and cannot be bypassed or removed by the guest processes.",
    "Engineers use Job Objects to prevent runaway tasks, workers, and data pipelines from destabilising Windows. By setting `JOB_OBJECT_LIMIT_PROCESS_MEMORY` or `JOB_OBJECT_LIMIT_JOB_MEMORY`, Windows halts allocations that exceed the commit ceiling, raising a catchable `MemoryError` in Python rather than causing system-wide paging and freezing.",
    "Furthermore, setting `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` ensures that when the parent application exits or crashes, Windows automatically and cleanly terminates every child process assigned to the job, eliminating orphan background tasks."],
 k:["The Windows kernel counterpart to Linux cgroups — isolates and limits process groups.",
    "Enforces hard commit memory ceilings and CPU rate limits across all processes in the job.",
    "`JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` terminates all child processes when parent handle closes.",
    "Used internally by Chromium tabs, Docker for Windows, and Windows Sandbox."],
 x:{lang:"python", code:'import win32job, win32api, os\n\n# Create kernel Job Object\njob = win32job.CreateJobObject(None, "WorkerSandbox")\nlimits = win32job.QueryInformationJobObject(job, win32job.JobObjectExtendedLimitInformation)\n\n# Enforce 500 MB memory ceiling\nlimits["ProcessMemoryLimit"] = 500 * 1024 * 1024\nlimits["BasicLimitInformation"]["LimitFlags"] = (\n    win32job.JOB_OBJECT_LIMIT_PROCESS_MEMORY |\n    win32job.JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE\n)\nwin32job.SetInformationJobObject(job, win32job.JobObjectExtendedLimitInformation, limits)\nwin32job.AssignProcessToJobObject(job, win32api.GetCurrentProcess())\nprint(f"[+] Sandboxed PID {os.getpid()} capped at 500 MB RAM")'},
 r:["Process", "Thread", "Control Groups (cgroups)", "Virtual Memory"]},

{t:"Socket Exhaustion", a:"Port Exhaustion", d:"A networking failure condition where a host depletes available ephemeral TCP ports or socket buffers, causing outgoing connections to fail.", l:"advanced", g:["networking","tcp","sockets","debugging"],
 b:["Every TCP connection is uniquely defined by a 4-tuple: source IP, source port, destination IP, and destination port. When an application initiates thousands of outbound requests to the same remote host without connection pooling, it consumes a new ephemeral (dynamic) source port for every request.",
    "When a TCP connection closes, the initiating side places the socket into the `TIME_WAIT` state for up to 4 minutes (2MSL). If new connections are created faster than old ones exit `TIME_WAIT`, all dynamic ports (typically ports 49152 to 65535 on Windows) become occupied. Any subsequent `connect()` call immediately fails with `WSAENOBUFS (10055)` or `EADDRNOTAVAIL`.",
    "The architectural fix is HTTP connection pooling: using persistent `Keep-Alive` connections through tools like `requests.Session()` or `httpx.Client()` so hundreds of requests reuse the same underlying TCP socket rather than cycling through new ports."],
 k:["Occurs when all ephemeral ports are occupied, usually lingering in TIME_WAIT.",
    "Surfaces as error WSAENOBUFS (10055) on Windows or EADDRNOTAVAIL on Linux.",
    "A rapid scraper or microservice opening 100 req/sec exhausts 16,000 ports in under 3 minutes.",
    "Prevent it with connection pooling (`requests.Session()`) rather than one-off connections."],
 x:{lang:"powershell", code:'# Audit socket counts by state in PowerShell:\nGet-NetTCPConnection | Group-Object State | Select-Object Count, Name\n\n# Expand Windows dynamic ephemeral port range to 64,510 ports:\nnetsh int ipv4 set dynamicport tcp start=1025 num=64510'},
 r:["TCP", "Ephemeral Port", "TIME_WAIT", "Connection Pooling"]},

{t:"TIME_WAIT", a:"2MSL State", d:"The mandatory final TCP state where an endpoint that initiated a connection close waits for two Maximum Segment Lifetimes before recycling the port.", l:"advanced", g:["networking","tcp","protocols"],
 b:["RFC 793 mandates that whichever side of a TCP connection initiates the active close (`FIN` packet) must enter the `TIME_WAIT` state upon receiving the final `ACK`. The duration of this state is defined as 2MSL (Maximum Segment Lifetime), which is 240 seconds by default on Windows.",
    "TIME_WAIT serves two critical purposes: 1) Reliable connection teardown: if the final `ACK` is lost in transit, the remote peer will retransmit its `FIN`. If the local socket were already closed, it would reply with an error (`RST`) rather than a valid `ACK`. 2) Preventing old duplicate segments from an earlier connection from being delivered to a new connection that happens to be assigned the identical port pair.",
    "While essential for internet protocol safety, thousands of sockets sitting in TIME_WAIT can choke busy microservices. Tuning `TcpTimedWaitDelay` or reusing connections via pooling prevents this bottleneck."],
 k:["RFC 793 state entered exclusively by the party that initiates the active close.",
    "Guarantees that delayed duplicate packets from old connections die out before port reuse.",
    "Ensures the final ACK was received by the remote endpoint.",
    "Defaults to 4 minutes (240s) on Windows, 60s on modern Linux."],
 x:{lang:"text", code:"Client                 Server\n  |                      |\n  |--- FIN ------------->|  (Active close initiated)\n  |<-- ACK --------------|  (Server acknowledges)\n  |<-- FIN --------------|  (Server closes)\n  |--- ACK ------------->|  (Client acknowledges -> enters TIME_WAIT)\n  |                      |\n  [ TIME_WAIT: 240s wait ]  (Socket port cannot be reused until timer expires)\n  |                      |\n  [ CLOSED ]"},
 r:["TCP", "Socket Exhaustion", "Three-Way Handshake", "Ephemeral Port"]},

{t:"TCP Window Auto-Tuning", a:"Window Scaling", d:"An operating system feature that dynamically expands the TCP receive window to maximize throughput over high-bandwidth links.", l:"intermediate", g:["networking","tcp","performance"],
 b:["In original TCP (RFC 793), the receive window field in the header is 16 bits, capping the maximum in-flight unacknowledged data to 65,535 bytes (64 KB). On modern high-speed internet and gigabit networks with latency (the Bandwidth-Delay Product, or BDP), a 64 KB window limits transfer speed to a tiny fraction of available capacity.",
    "RFC 1323 introduced the Window Scale option, allowing window sizes up to 1 gigabyte. Windows TCP Auto-Tuning continuously samples round-trip times (RTT) and socket consumption rates to adjust the receive buffer dynamically.",
    "If TCP Auto-Tuning is disabled or restricted (common in older Windows configurations), large file downloads and high-throughput video or API streams bottleneck severely regardless of internet bandwidth."],
 k:["Solves the historic 64 KB limit using RFC 1323 Window Scale factors.",
    "Dynamically sizes receive buffers to match the Bandwidth-Delay Product (BDP = bandwidth * RTT).",
    "Essential for achieving gigabit throughput over long-distance connections.",
    "Verify and enable in Windows with: `netsh int tcp set global autotuninglevel=normal`."],
 x:{lang:"powershell", code:'# Check current TCP auto-tuning level:\nnetsh int tcp show global\n\n# Enable normal dynamic scaling:\nnetsh int tcp set global autotuninglevel=normal'},
 r:["TCP", "Latency", "Throughput", "Bandwidth"]},

{t:"Ephemeral Port", a:"Dynamic Port", d:"A short-lived transport protocol port assigned automatically by the operating system for the client end of an outbound network connection.", l:"intermediate", g:["networking","tcp","sockets"],
 b:["When you open a browser or send an HTTP request in Python, the server destination port is well-known (e.g. 80 for HTTP, 443 for HTTPS). But the client machine must also assign a source port so return packets know which application to reach. This temporary client port is an ephemeral port.",
    "Operating systems manage a designated dynamic port pool. IANA recommends 49152 to 65535 (16,384 ports). Windows 10/11 uses this range by default. Linux typically uses 32768 to 60999.",
    "When a connection terminates, the ephemeral port remains reserved during TIME_WAIT before it can be assigned again. Understanding dynamic port allocation is vital for preventing socket exhaustion in high-concurrency systems."],
 k:["Temporary port chosen automatically by the OS IP stack for outbound connections.",
    "Default range on Windows: 49152 to 65535 (16,384 total ports).",
    "Once closed, the port cannot be reused until the TIME_WAIT timer clears.",
    "Can be expanded up to 64,510 ports on Windows using `netsh int ipv4 set dynamicport`."],
 x:{lang:"powershell", code:'# View current dynamic port range in Windows PowerShell:\nnetsh int ipv4 show dynamicport tcp\n\n# Protocol tcp Dynamic Port Range\n# Start Port      : 49152\n# Number of Ports : 16384'},
 r:["Port", "Socket", "TCP", "Socket Exhaustion"]},

{t:"PktMon", a:"Windows Packet Monitor", d:"A native, in-kernel network packet monitoring and diagnostics tool built into Windows 10 and 11, requiring zero third-party drivers.", l:"intermediate", g:["networking","windows","observability","security"],
 b:["Before PktMon, capturing network traffic on Windows required installing third-party driver packages like WinPcap or Npcap (bundled with Wireshark). On locked-down corporate laptops or production servers where unsigned driver installation is strictly blocked, packet inspection was nearly impossible.",
    "Starting with Windows 10 (Build 1903), Microsoft built `pktmon.exe` directly into `C:\\Windows\\System32`. It hooks directly into the Windows NDIS (Network Driver Interface Specification) network stack at the kernel level.",
    "PktMon supports packet filtering by port, IP, and protocol, captures complete packet payloads, and features a built-in converter (`pktmon etl2pcap`) that turns Windows Event Trace Logs into standard `.pcapng` files readable by Wireshark and Python."],
 k:["Native Windows packet sniffer built into System32 — no Wireshark or Npcap required.",
    "Operates at the kernel NDIS layer across virtual switches, Wi-Fi, and physical NICs.",
    "Supports granular packet filters to prevent log buffer overflow.",
    "Converts trace logs to standard Wireshark `.pcapng` with `pktmon etl2pcap`."],
 x:{lang:"powershell", code:'# 1. Filter HTTP/HTTPS traffic:\npktmon filter add WebTraffic -p 80 443\n\n# 2. Start full packet capture:\npktmon start --capture --pkt-size 0 --file-name trace.etl\n\n# 3. Stop and convert to Wireshark format:\npktmon stop\npktmon etl2pcap trace.etl --out trace.pcapng'},
 r:["Wireshark", "Raw Socket", "tcpdump", "Promiscuous Mode"]},

{t:"Raw Socket", a:"SOCK_RAW", d:"A low-level network socket that provides direct access to underlying IP packet headers and payloads, bypassing transport-layer abstraction.", l:"hardcore", g:["networking","sockets","security","protocols"],
 b:["Standard sockets (`SOCK_STREAM` for TCP, `SOCK_DGRAM` for UDP) encapsulate data: the operating system builds and strips the IP and transport headers automatically. A raw socket (`SOCK_RAW`) bypasses this processing, allowing user-space applications to read and write complete IP datagrams.",
    "Raw sockets are used by diagnostic tools (like `ping` to send ICMP Echo Requests), packet sniffers, security auditing utilities, and custom protocol implementations.",
    "Because raw sockets can intercept other applications' traffic or forge source IP addresses (IP spoofing), modern operating systems restrict raw socket creation strictly to elevated/administrator privileges.",
    "On Windows, raw sockets bound to `IPPROTO_IP` combined with the `SIO_RCVALL` socket ioctl allow developers to implement full-featured packet sniffers in under 30 lines of pure Python."],
 k:["Bypasses transport-layer processing to provide direct access to raw IP packets.",
    "Requires Administrator/Root privileges on all modern operating systems.",
    "Used to send raw ICMP packets (ping), craft custom protocols, or sniff traffic.",
    "Windows enables interface promiscuous capture via `socket.SIO_RCVALL`."],
 x:{lang:"python", code:'import socket, struct\n\n# Create raw socket on Windows (Admin required)\ns = socket.socket(socket.AF_INET, socket.SOCK_RAW, socket.IPPROTO_IP)\ns.bind((socket.gethostbyname(socket.gethostname()), 0))\ns.setsockopt(socket.IPPROTO_IP, socket.IP_HDRINCL, 1)\ns.ioctl(socket.SIO_RCVALL, socket.RCVALL_ON)\n\nraw_packet, _ = s.recvfrom(65535)\niph = struct.unpack("!BBHHHBBH4s4s", raw_packet[:20])\nsrc = socket.inet_ntoa(iph[8])\ndst = socket.inet_ntoa(iph[9])\nprint(f"IP Header: Protocol {iph[6]} | {src} -> {dst}")'},
 r:["Promiscuous Mode", "PktMon", "Socket", "TCP"]},

{t:"Promiscuous Mode", d:"A network card configuration mode where the adapter delivers all received Ethernet frames to the CPU, regardless of destination address.", l:"advanced", g:["networking","security","hardware"],
 b:["In standard network card operation, the hardware MAC filter examines the destination address of incoming Ethernet frames. It accepts only unicast frames addressed to its own MAC address, broadcast frames (FF:FF:FF:FF:FF:FF), and joined multicast groups — dropping all others at the hardware layer to spare CPU interrupts.",
    "When promiscuous mode is enabled, the hardware filter is turned off. The NIC passes every packet transmitted across the physical or virtual segment straight up the driver stack to the operating system.",
    "Network diagnostic tools (Wireshark, tcpdump), Intrusion Detection Systems (Snort, Suricata), and packet capture frameworks require promiscuous mode to observe conversation flows between other nodes."],
 k:["Instructs NIC hardware to pass all frames to the OS, disabling MAC address filtering.",
    "Essential mode for packet sniffers, network monitors, and IDS sensors.",
    "On switched networks, only broadcast/multicast and traffic directed to the host port are seen.",
    "Requires administrative privileges to toggle on modern operating systems."],
 x:{lang:"python", code:'# Enable promiscuous mode in Python on Windows via socket IOCTL:\ns.ioctl(socket.SIO_RCVALL, socket.RCVALL_ON)\n\n# Disable when finished to prevent high CPU interrupts:\ns.ioctl(socket.SIO_RCVALL, socket.RCVALL_OFF)'},
 r:["Raw Socket", "PktMon", "Wireshark", "Network Interface Card (NIC)"]},

{t:"Portable Executable", a:"PE Format", d:"The standard binary file format used by Windows for 32-bit and 64-bit executables, dynamic link libraries, and drivers.", l:"advanced", g:["windows","security","reverse-engineering","binaries"],
 b:["Every `.exe`, `.dll`, `.sys`, and `.ocx` file in Windows adheres to the Portable Executable (PE) specification, derived from the Unix COFF format. It defines how code and data are organized on disk and how the Windows OS loader maps them into virtual memory.",
    "A PE file begins with a legacy MS-DOS 'MZ' header and stub (printing 'This program cannot be run in DOS mode'). This is followed by the PE Signature (`PE\\0\\0`), the COFF Header (target architecture like x64, section count), and the Optional Header (entry point address, image base, subsystem).",
    "Next sits the Section Table, defining isolated memory segments: `.text` (compiled CPU opcodes), `.rdata` (read-only data, imports, strings), `.data` (initialized global variables), and `.rsrc` (icons, dialogs, version info).",
    "Security analysts inspect PE headers to identify compiler provenance, compile timestamps, ASLR/DEP exploit mitigations, and imported Windows APIs."],
 k:["The standard file structure for Windows executables, DLLs, and drivers.",
    "Starts with DOS MZ header for historical compatibility, followed by PE headers.",
    "Sections divide memory permissions: .text (executable), .rdata (read-only), .data (writable).",
    "Parsed in Python using the `pefile` library without running the binary."],
 x:{lang:"python", code:'import pefile\n\npe = pefile.PE("C:\\\\Windows\\\\System32\\\\notepad.exe")\nprint(f"Architecture: {hex(pe.FILE_HEADER.Machine)} (0x8664 = x64)")\nfor section in pe.sections:\n    name = section.Name.decode().strip("\\x00")\n    print(f"Section {name:<8} Size: {section.SizeOfRawData} bytes")'},
 r:["Import Address Table (IAT)", "Shannon Entropy", "Reverse Engineering", "Dynamic-Link Library (DLL)"]},

{t:"Import Address Table", a:"IAT", d:"A lookup table inside a Windows PE executable storing memory pointers to external API functions imported from system DLLs.", l:"hardcore", g:["windows","security","reverse-engineering","binaries"],
 b:["When a Windows program calls a function from an external library — such as `CreateFileW` from `kernel32.dll` or `connect` from `ws2_32.dll` — the compiler does not know what virtual memory address that function will occupy at runtime.",
    "Instead, the compiler emits a call instruction pointing into the executable's Import Address Table. When the Windows loader launches the binary, it loads each required DLL into memory, resolves the real function addresses, and writes those pointers into the IAT.",
    "Inspecting a binary's IAT reveals its complete capability set without executing it: if an unknown `.exe` imports `VirtualAllocEx`, `WriteProcessMemory`, and `CreateRemoteThread`, it is designed for process injection; if it imports `WSAStartup`, it communicates over the network.",
    "Malware evasion techniques (like runtime dynamic API resolving via `LoadLibrary` + `GetProcAddress`) exist specifically to hide calls from static IAT inspection."],
 k:["Lookup table filled by the Windows loader with runtime addresses of imported DLL functions.",
    "Reveals what external capabilities a binary possesses (file I/O, networking, crypto).",
    "Static inspection of the IAT identifies suspicious functions without running the file.",
    "IAT hooking is a classic technique used by debuggers and rootkits to intercept API calls."],
 x:{lang:"python", code:'import pefile\n\npe = pefile.PE("suspicious.exe")\nfor entry in pe.DIRECTORY_ENTRY_IMPORT:\n    dll = entry.dll.decode()\n    apis = [f.name.decode() for f in entry.imports if f.name]\n    print(f"[+] Imports from {dll}: {apis[:3]}...")'},
 r:["Portable Executable", "Dynamic-Link Library (DLL)", "Reverse Engineering", "System Call (Syscall)"]},

{t:"strace", a:"Syscall Tracer", d:"A powerful diagnostic utility for Unix-like systems that intercepts and records all system calls made by a process and signals received.", l:"advanced", g:["debugging","linux","kernel","systems"],
 b:["Applications cannot directly touch hardware — all file operations, network sockets, memory allocations, and process forks must transition into the operating system kernel via system calls (syscalls).",
    "strace uses the `ptrace` kernel facility to pause a target process every time it enters or exits a system call, decoding the arguments, memory addresses, and return values into human-readable text.",
    "It is the ultimate tool for diagnosing 'file not found' crashes (watching `openat` return `-1 ENOENT`), identifying mystery network destinations, and uncovering frozen processes (waiting on `futex` or `select`).",
    "The Windows equivalent for inspecting file, registry, and process activity is Microsoft Sysinternals Process Monitor (ProcMon)."],
 k:["Intercepts and logs every system call (`open`, `read`, `write`, `connect`) in real time.",
    "Essential for black-box debugging when source code or debug logs are unavailable.",
    "Run `strace -c` to generate a summary histogram of where CPU time is spent across syscalls.",
    "Attach to hanging background processes on the fly with `strace -p <PID>`."],
 x:{lang:"bash", code:'# Trace only file-opening system calls and filter missing files:\nstrace -e trace=openat python3 app.py 2>&1 | grep ENOENT\n\n# Measure cumulative time spent in each system call:\nstrace -c python3 data_pipeline.py'},
 r:["System Call (Syscall)", "Kernel", "Process", "Debugging"]},

{t:"eBPF", a:"Extended BPF", d:"A revolutionary Linux kernel technology that allows running sandboxed, event-driven bytecode inside the kernel safely without modifying kernel source code.", l:"hardcore", g:["linux","kernel","observability","networking"],
 b:["Historically, adding observability, network filtering, or security monitoring to the Linux kernel required writing complex C kernel modules (which risk panicking the entire operating system on a bug) or modifying the kernel source.",
    "eBPF introduces a universal, sandboxed virtual machine inside the Linux kernel. Developers write small C or Rust programs that attach to tracepoints, kprobes, or network socket events.",
    "Before execution, the in-kernel eBPF Verifier rigorously verifies the code: proving that it cannot crash, access illegal memory, or enter infinite loops. Once verified, it is JIT-compiled to native CPU instructions, executing in microseconds with near-zero overhead.",
    "Modern cloud-native observability (Cilium, Falco, Pixie, bpftrace) is built entirely on top of eBPF."],
 k:["Runs sandboxed programs inside the OS kernel with near-zero latency and overhead.",
    "Guaranteed safe by the in-kernel verifier: cannot panic the kernel or leak memory.",
    "Attaches to system calls, socket events, kernel functions, and hardware tracepoints.",
    "Drives the modern generation of cloud security (Falco) and networking (Cilium)."],
 x:{lang:"bash", code:"# Print every program executed on the system in real time using bpftrace:\nsudo bpftrace -e 'tracepoint:syscalls:sys_enter_execve { printf(\"%s\\n\", str(args->filename)); }'"},
 r:["Kernel", "Observability", "Raw Socket", "System Call (Syscall)"]},

{t:"Control Groups", a:"cgroups", d:"A Linux kernel feature that isolates, meters, and limits the resource usage (CPU, memory, disk I/O, network) of process collections.", l:"advanced", g:["linux","kernel","containers","resources"],
 b:["Control groups (cgroups), alongside Linux namespaces, form the foundational building blocks of all modern container runtimes, including Docker, containerd, and Kubernetes.",
    "While namespaces control what a process can *see* (its own PID list, filesystem mount table, network interfaces), cgroups control what a process can *consume*.",
    "Under cgroups v2 (unified hierarchy at `/sys/fs/cgroup`), creating a directory automatically provisions a new control group. Writing numbers into pseudo-files sets hard limits: writing `500000000` into `memory.max` ensures any process tree exceeding 500 MB is throttled or terminated by the OOM killer.",
    "The Windows kernel counterpart to Linux cgroups is Windows Job Objects."],
 k:["Core kernel primitive of Docker and Kubernetes — limits CPU, RAM, and I/O usage.",
    "cgroup v2 uses a unified pseudo-filesystem mounted at `/sys/fs/cgroup`.",
    "`memory.max` sets a hard limit; exceeding it triggers the Out-Of-Memory (OOM) killer.",
    "`cpu.max` enforces CPU quota percentages over a defined time period."],
 x:{lang:"bash", code:"# Create a new cgroup and clamp memory to 500 MB:\nsudo mkdir /sys/fs/cgroup/worker_sandbox\necho 500000000 | sudo tee /sys/fs/cgroup/worker_sandbox/memory.max\n\n# Assign current shell to the sandbox:\necho $$ | sudo tee /sys/fs/cgroup/worker_sandbox/cgroup.procs"},
 r:["Namespaces", "Job Object", "Docker", "Process"]},

{t:"Namespaces", a:"Linux Namespaces", d:"A Linux kernel mechanism that isolates system resources so that different processes see completely independent views of the operating system.", l:"advanced", g:["linux","kernel","containers","security"],
 b:["Namespaces provide the illusion of a dedicated virtual operating system to a group of processes without the overhead of hardware virtual machines or hypervisors.",
    "Linux provides seven distinct namespaces:\n1. PID: Process IDs (a containerized process can see itself as PID 1)\n2. NET: Network devices, IP addresses, routing tables, port bindings\n3. MNT: Filesystem mount points\n4. UTS: Hostname and domain name\n5. IPC: Shared memory and message queues\n6. USER: Independent UID/GID mappings (root in container maps to unprivileged user on host)\n7. CGROUP: Isolated view of control group hierarchy.",
    "The `unshare` utility launches a command in new namespaces, while `nsenter` enters the existing namespaces of a running process."],
 k:["Virtualizes OS resources: creates independent views of PIDs, network, mounts, and users.",
    "The isolation primitive that makes Docker containers look and feel like virtual machines.",
    "PID namespace allows a container process to run as PID 1 without affecting host PID 1.",
    "User namespaces enable running root inside a container while mapping to non-root on the host."],
 x:{lang:"bash", code:"# Launch a shell with an independent hostname (UTS) namespace:\nsudo unshare --fork --uts /bin/bash\n\n# Inside the namespace, changing hostname does not alter the host:\nhostname container-node-01"},
 r:["Control Groups", "Docker", "Operating System", "Process"]},

{t:"sysctl", a:"Kernel Parameters", d:"A tool and kernel interface used on Unix-like systems to examine and tune runtime operating system parameters without rebooting.", l:"intermediate", g:["linux","kernel","networking","performance"],
 b:["The Linux kernel maintains thousands of runtime knobs governing networking buffers, virtual memory paging, process scheduling, and security policies. These parameters are exposed live via the `/proc/sys` virtual filesystem.",
    "`sysctl` allows administrators to view and modify these settings dynamically. For instance, high-throughput network servers tune `net.core.rmem_max` and `net.core.somaxconn` to handle traffic spikes.",
    "Modifications made via `sysctl -w` take effect immediately in RAM but are lost upon reboot. To make changes permanent across reboots, settings must be added to `/etc/sysctl.conf` or a dedicated file in `/etc/sysctl.d/`."],
 k:["Inspects and modifies kernel runtime parameters live without restarting the machine.",
    "Maps directly to the `/proc/sys` directory hierarchy (`net.ipv4.ip_forward` -> `/proc/sys/net/ipv4/ip_forward`).",
    "Essential for high-scale network tuning (buffer sizes, connection queues, TCP scaling).",
    "Persist settings across reboots by writing to `/etc/sysctl.d/*.conf` and running `sysctl -p`."],
 x:{lang:"bash", code:"# Increase maximum TCP receive buffer size immediately:\nsudo sysctl -w net.core.rmem_max=16777216\n\n# Persist in configuration and reload:\necho \"net.core.rmem_max = 16777216\" | sudo tee -a /etc/sysctl.conf\nsudo sysctl -p"},
 r:["Kernel", "Operating System", "TCP Window Auto-Tuning", "Performance Tuning"]}

]);

TD.add("software-engineering", [

{t:"cProfile", a:"Python Profiler", d:"Python's built-in deterministic profiler that tracks call counts, per-call execution times, and cumulative times for every function in a program.", l:"intermediate", g:["python","profiling","performance","tooling"],
 b:["cProfile is a C extension built into the Python standard library designed for profiling long-running programs with minimal runtime overhead. Unlike statistical profilers that sample the call stack at intervals, cProfile is deterministic: it intercepts every function entry and exit.",
    "It outputs four critical statistics for every function:\n- `ncalls`: Number of invocations\n- `tottime`: Total time spent in the function itself (excluding time in sub-functions)\n- `cumtime`: Cumulative time spent in the function and all sub-functions it called\n- `percall`: Average time per execution.",
    "If a function has high `cumtime` but near-zero `tottime`, the function is a coordinator: the true bottleneck is one of the functions it calls inside its body.",
    "cProfile dumps binary statistics (`.prof` files) that can be inspected with the standard `pstats` module or visualized as interactive flamegraphs using SnakeViz."],
 k:["Standard library deterministic profiler with low overhead, implemented in C.",
    "Profile any script without code changes: `python -m cProfile -o app.prof script.py`.",
    "`tottime` measures function self-time; `cumtime` includes all nested sub-calls.",
    "Export binary `.prof` data for browser visualization with SnakeViz."],
 x:{lang:"python", code:'import cProfile, pstats\n\ndef heavy_computation():\n    return sum(x*x for x in range(500000))\n\n# Profile programmatically:\nprofiler = cProfile.Profile()\nprofiler.enable()\nheavy_computation()\nprofiler.disable()\n\nstats = pstats.Stats(profiler).sort_stats("cumtime")\nstats.print_stats(5)'},
 r:["Flamegraph", "Benchmarking", "Python", "Profiling"]},

{t:"Flamegraph", a:"Flame Graph", d:"A visual hierarchy of software execution where the x-axis represents execution time percentage and the y-axis represents call stack depth.", l:"intermediate", g:["profiling","performance","observability","tooling"],
 b:["Invented by performance engineer Brendan Gregg, flamegraphs turn massive tabular profiler outputs into an intuitive graphical map of where CPU cycles are consumed.",
    "In a flamegraph, each rectangle represents a function. The vertical y-axis shows call stack depth (the function on top was called by the function beneath it). The horizontal x-axis spans the entire sample population: the width of a box is directly proportional to how much time was spent in that function.",
    "Unlike a timeline chart, the x-axis is not ordered chronologically by time of day; instead, identical call stacks are aggregated and sorted alphabetically to maximize merge efficiency.",
    "A wide 'plateau' at the top of the flamegraph immediately exposes the hot code path consuming CPU."],
 k:["Invented by Brendan Gregg — visualizes call stacks to find performance bottlenecks instantly.",
    "Width of a box indicates the percentage of CPU or wall-clock time consumed.",
    "Vertical y-axis shows stack depth: parent functions sit beneath child functions.",
    "Interactive: click any box to zoom into that function subtree."],
 x:{lang:"bash", code:"# Generate interactive Python flamegraph in browser using SnakeViz:\npip install snakeviz\npython -m cProfile -o trace.prof app.py\nsnakeviz trace.prof"},
 r:["cProfile", "Profiling", "Call Stack", "Performance Tuning"]},

{t:"Invoke", a:"Python Task Runner", d:"A Python library and command-line automation tool for managing project build, test, lint, and deployment tasks via clean Python code.", l:"beginner", g:["python","tooling","automation","ci"],
 b:["Makefiles are the classic Unix build automation tool, but they notoriously fail on Windows because they rely on native Unix utilities (`rm -rf`, `mkdir -p`, `cat`) and Bash shell semantics.",
    "Invoke replaces Make with clean, typed Python functions decorated with `@task`. It runs natively on Windows PowerShell, Linux, and macOS without requiring Cygwin or MinGW.",
    "Tasks are defined in a `tasks.py` file. Invoke automatically parses docstrings into CLI help documentation, provides typed flag parsing (`--verbose`, `--fix`), and handles prerequisite task chains using `pre=[clean, lint]` decorators."],
 k:["Cross-platform Python replacement for Makefiles, Rake, and shell scripts.",
    "Tasks defined as standard Python functions decorated with `@task` in `tasks.py`.",
    "Runs seamlessly in Windows PowerShell without requiring bash or MinGW.",
    "Provides automatic CLI flag parsing, help generation, and task prerequisites."],
 x:{lang:"python", code:'from invoke import task\nfrom pathlib import Path\nimport shutil\n\n@task\ndef clean(c):\n    """Remove build artifacts and bytecode caches."""\n    for p in Path(".").rglob("__pycache__"):\n        shutil.rmtree(p, ignore_errors=True)\n    print("[+] Caches cleared.")\n\n@task(pre=[clean])\ndef build(c):\n    """Run build pipeline."""\n    print("[+] Build complete.")'},
 r:["Automation", "Task Runner", "Continuous Integration (CI)", "Python"]}

]);

TD.add("deep-learning", [

{t:"Quantization", a:"Model Quantization", d:"Converting neural network weights and activations from 32-bit floating point to lower-precision formats like Int8 to slash memory and accelerate inference.", l:"advanced", g:["ai","deep-learning","optimization","inference"],
 b:["State-of-the-art AI models and LLMs are traditionally trained in 32-bit floating point (`float32`). While float32 provides the high dynamic range necessary for stable gradient backpropagation during training, it requires 4 bytes per parameter and heavy floating-point arithmetic.",
    "Quantization maps continuous float32 values into low-precision discrete integers (typically 8-bit `int8` or 4-bit `int4`). A float tensor is scaled and zero-point offset into integer ranges (-128 to 127 for int8), cutting model size on disk and RAM by 75%.",
    "Beyond memory savings, modern Intel/AMD CPUs (AVX-512 VNNI) and GPUs (Tensor Cores) feature dedicated hardware instructions for integer matrix multiplication (GEMM), accelerating inference by 2x to 4x with less than 1% loss in model accuracy.",
    "Post-Training Dynamic Quantization quantizes weights ahead of time and converts activations dynamically during inference with a single function call in PyTorch."],
 k:["Reduces weight precision from FP32 (4 bytes) to INT8 (1 byte), shrinking models by 4x.",
    "Speeds up CPU inference 2x-3x using hardware vector integer instructions (VNNI, AVX-512).",
    "Post-Training Quantization (PTQ) requires no retraining; Quantization-Aware Training (QAT) simulates int8 during training.",
    "Crucial for running local LLMs, edge devices, and cost-effective cloud serving."],
 x:{lang:"python", code:'import torch\n\n# Dynamic post-training quantization in PyTorch for Linear layers\nmodel_fp32 = MyModel().eval()\nmodel_int8 = torch.quantization.quantize_dynamic(\n    model_fp32,\n    {torch.nn.Linear},\n    dtype=torch.qint8\n)\ntorch.save(model_int8.state_dict(), "model_int8.pth")'},
 r:["Knowledge Distillation", "Inference", "Deep Learning", "PyTorch"]},

{t:"Knowledge Distillation", a:"Teacher-Student Distillation", d:"Training a lightweight student neural network to reproduce the soft probability outputs of a large, high-capacity teacher model.", l:"hardcore", g:["ai","deep-learning","model-compression","llm"],
 b:["Large neural networks (like a 70B parameter LLM or heavy ResNet) achieve state-of-the-art accuracy but are too slow, memory-intensive, and expensive for production deployment on edge devices or CPUs.",
    "Introduced by Geoffrey Hinton, Oriol Vinyals, and Jeff Dean in 2015, Knowledge Distillation transfers the capabilities of a cumbersome 'teacher' model into a compact 'student' model with 10x to 100x fewer parameters.",
    "The secret lies in 'dark knowledge': hard training labels (100% Cat, 0% Dog) discard the subtle structural relationships that the teacher has learned. The teacher's raw probability logits (e.g. 85% Cat, 14% Dog, 1% Car) teach the student that cats look somewhat like dogs, but nothing like cars.",
    "By dividing logits by a Temperature parameter T (e.g. T=4.0) before applying Softmax, the probabilities soften. The student is trained using a composite loss: Kullback-Leibler (KL) divergence against the softened teacher outputs plus standard cross-entropy against the ground truth labels."],
 k:["Transfers knowledge from a giant teacher model into a lightweight student model.",
    "Soft probability outputs reveal nuanced inter-class relationships ('dark knowledge').",
    "Temperature scaling (T > 1) flattens logit distributions during training to emphasize subtle patterns.",
    "Produces models that run 5x-10x faster (e.g. DistilBERT vs BERT) while retaining ~97% accuracy."],
 x:{lang:"python", code:'import torch.nn as nn\nimport torch.nn.functional as F\n\ndef distillation_loss(student_logits, teacher_logits, labels, T=4.0, alpha=0.7):\n    # 1. Soft loss: KL divergence between softened distributions\n    p_s = F.log_softmax(student_logits / T, dim=1)\n    p_t = F.softmax(teacher_logits / T, dim=1)\n    soft_loss = nn.KLDivLoss(reduction="batchmean")(p_s, p_t) * (T * T)\n    \n    # 2. Hard loss: standard cross entropy on ground truth\n    hard_loss = nn.CrossEntropyLoss()(student_logits, labels)\n    return alpha * soft_loss + (1 - alpha) * hard_loss'},
 r:["Quantization", "KL Divergence", "Loss Function", "Fine-Tuning"]}

]);

TD.add("math-stats", [

{t:"KL Divergence", a:"Kullback-Leibler Divergence", d:"A mathematical measure of the difference between two probability distributions P and Q over the same statistical space.", l:"advanced", g:["math","probability","information-theory","ai"],
 b:["KL Divergence (also known as relative entropy) quantifies how much information is lost when a probability distribution Q is used to approximate an underlying true distribution P.",
    "Mathematically, for discrete distributions: D_KL(P || Q) = sum(P(x) * log(P(x) / Q(x))). If P and Q are identical across all states, D_KL is exactly zero. The more the two distributions diverge, the larger the KL divergence becomes.",
    "A fundamental property of KL Divergence is that it is asymmetric: D_KL(P || Q) does not equal D_KL(Q || P). For this reason, it is referred to as a divergence rather than a true distance metric.",
    "In AI engineering, KL Divergence is the mathematical engine behind Knowledge Distillation (matching student predictions to teacher outputs), Variational Autoencoders (VAEs, forcing latent representations to match a Gaussian prior), and Reinforcement Learning from Human Feedback (RLHF, penalizing policy drift during PPO)."],
 k:["Quantifies how much distribution Q diverges from the reference distribution P.",
    "Always non-negative (D_KL >= 0); equals zero if and only if P == Q.",
    "Asymmetric: D_KL(P || Q) != D_KL(Q || P) — it is a divergence, not a distance.",
    "Core loss function in Knowledge Distillation, VAEs, and RLHF alignment."],
 x:{lang:"python", code:'import torch\nimport torch.nn.functional as F\n\n# Target distribution P and approximation Q (probabilities)\np = torch.tensor([[0.7, 0.2, 0.1]])\nq = torch.tensor([[0.5, 0.3, 0.2]])\n\n# PyTorch KLDivLoss expects input as log-probabilities:\nkl = F.kl_div(q.log(), p, reduction="batchmean")\nprint(f"KL Divergence: {kl.item():.4f}")'},
 r:["Cross-Entropy", "Entropy", "Loss Function", "Knowledge Distillation"]}

]);

TD.add("security", [

{t:"Allow-list Validation", a:"Whitelist Validation", d:"A cybersecurity practice that explicitly defines acceptable input criteria and rejects everything else by default.", l:"core", g:["security","web","defensive-coding"],
 b:["In software security, input validation determines whether data received from an untrusted source (HTTP query, user form, API payload) is safe to process.",
    "Deny-listing (blacklisting) attempts to identify and block known malicious patterns, such as `<script>` or `' OR 1=1`. Deny-lists consistently fail because attackers invent infinite evasion variations: Unicode homoglyphs, null-byte injection, URL encoding (`%27`), and nested stripping (`<scr<script>ipt>`).",
    "Allow-listing enforces a positive security model: you define strictly what IS permitted — such as 'alphanumeric characters only, between 3 and 30 characters in length'. Anything that contains any unexpected byte is rejected immediately.",
    "OWASP and modern application security standards mandate allow-list validation paired with strict regex anchoring (`^` and `$`) and schema libraries like Pydantic."],
 k:["Positive security model: define what is permitted; reject everything else by default.",
    "Deny-lists fail because attacker encoding variations (Unicode, hex, URL) are infinite.",
    "Always anchor regular expressions with `^` (start) and `$` (end).",
    "Normalize input encoding (NFKC) before validation to prevent homoglyph bypasses."],
 x:{lang:"python", code:'import re\n\n# Strict allow-list: alphanumeric and underscore only, 3 to 24 chars\nSAFE_PATTERN = re.compile(r"^[a-zA-Z0-9_]{3,24}$")\n\ndef validate_username(username: str) -> str:\n    if not SAFE_PATTERN.match(username):\n        raise ValueError("Invalid username: only alphanumeric and _ permitted.")\n    return username'},
 r:["Unicode Normalization", "Input Validation", "SQL Injection", "Cross-Site Scripting (XSS)"]},

{t:"Unicode Normalization", a:"NFKC / NFC", d:"Converting visually equivalent Unicode character sequences into a standardized, canonical binary representation to prevent security bypasses.", l:"intermediate", g:["security","encoding","unicode","web"],
 b:["Unicode allows the same visual glyph or character to be represented by multiple different byte sequences. For example, the character 'é' can be stored as a single precomposed code point (`U+00E9`) or as the letter 'e' followed by a combining acute accent (`U+0065 U+0301`).",
    "Even more dangerously, Unicode contains 'compatibility' characters: full-width letters (like '＜' `U+FF1C`) look like ASCII '<', but have distinct code points that bypass standard ASCII regex filters. When these characters later pass into a database or HTML renderer, they may be normalized back to ASCII '<', triggering SQL injection or XSS.",
    "The Unicode standard defines four normalization forms. NFKC (Compatibility Decomposition followed by Canonical Composition) collapses visual variants, full-width characters, and ligatures into their standard ASCII/Unicode equivalents.",
    "Applying `unicodedata.normalize('NFKC', text)` before running validation filters neutralizes encoding bypass attacks."],
 k:["Standardizes equivalent Unicode sequences into a consistent canonical binary form.",
    "NFKC collapses compatibility characters (e.g. full-width ＇ or ＜) to standard ASCII.",
    "Prevents authentication bypasses where visually identical usernames have different bytes.",
    "Always normalize input before regex validation, password hashing, or database queries."],
 x:{lang:"python", code:'import unicodedata\n\n# Attack payload using full-width Unicode characters:\nattack = "＜ｓｃｒｉｐｔ＞"\n\n# Without normalization, ASCII regex does not match:\nprint("Contains ASCII <: ", "<" in attack)  # False\n\n# Normalizing with NFKC collapses back to canonical ASCII:\ncleaned = unicodedata.normalize("NFKC", attack)\nprint("Cleaned text:", cleaned)            # <script>\nprint("Contains ASCII <: ", "<" in cleaned)  # True (detected!)'},
 r:["Allow-list Validation", "Encoding", "Input Validation", "Regex"]},

{t:"pip-audit", d:"A command-line security tool developed by the Python Security Authority that scans Python environments and requirements for known CVEs.", l:"beginner", g:["security","python","devops","dependencies"],
 b:["Over 80% of lines of code in modern software projects reside inside open-source third-party dependencies installed via package managers. Outdated packages with published Common Vulnerabilities and Exposures (CVEs) represent one of the most common vectors for remote code execution and data breaches.",
    "`pip-audit` is officially maintained by the Python Packaging Authority (PyPA) and OpenSSF. It inspects the packages installed in an active virtual environment, a `requirements.txt` file, or a `pyproject.toml` lockfile.",
    "It queries the Open Source Vulnerabilities (OSV) database and the PyPA Advisory Database, reporting exact CVE identifiers, CVSS severity scores, affected versions, and fixed release numbers.",
    "Using `pip-audit --fix` automatically upgrades vulnerable packages to the minimum secure version, making dependency remediation friction-free in CI/CD pipelines."],
 k:["Official PyPA security auditing tool for Python packages and virtual environments.",
    "Queries the Open Source Vulnerabilities (OSV) database and NIST NVD.",
    "Scans live virtual environments or static `requirements.txt` manifests.",
    "Use `pip-audit --fix` to automatically upgrade dependencies to safe patched versions."],
 x:{lang:"powershell", code:'# Install and audit active Python virtualenv:\npip install pip-audit\npip-audit\n\n# Audit requirements file with detailed CVE descriptions:\npip-audit -r requirements.txt --desc\n\n# Auto-remediate vulnerabilities:\npip-audit -r requirements.txt --fix'},
 r:["Virtual Environment", "Package Manager", "Dependency Injection", "Allow-list Validation"]},

{t:"Shannon Entropy", a:"Binary Entropy", d:"A mathematical metric measuring the degree of randomness or uncertainty in a sequence of bytes on a scale from 0.0 to 8.0.", l:"advanced", g:["security","reverse-engineering","cryptography","math"],
 b:["Formulated by Claude Shannon in his landmark 1948 paper on information theory, entropy measures the average information content per byte: H(X) = - sum(p(x) * log2(p(x))). For 8-bit bytes (256 possible values), maximum entropy is log2(256) = 8.0.",
    "A file containing identical repeated bytes (like all zeroes) has an entropy of 0.0. English plain text and source code have an entropy of roughly 3.5 to 5.0. Compiled uncompressed machine code typically measures between 5.8 and 6.5.",
    "Because effective compression (ZIP, GZIP) and cryptographic encryption algorithms maximize information density by eliminating repeating byte patterns, compressed or encrypted payloads have entropy approaching 8.0 (typically > 7.2).",
    "Security analysts and malware scanners calculate the Shannon entropy of executable PE sections: if a `.text` code section has an entropy > 7.2, the binary is packed or contains encrypted shellcode."],
 k:["Measures byte randomness from 0.0 (completely uniform) to 8.0 (maximum uncertainty).",
    "Plain text and source code measure ~3.5 - 5.0; compiled code measures ~6.0.",
    "Encrypted data and compressed code exhibit high entropy (> 7.2).",
    "Used in reverse engineering to detect packed malware and obfuscated payloads without execution."],
 x:{lang:"python", code:'import math\n\ndef shannon_entropy(data: bytes) -> float:\n    if not data: return 0.0\n    entropy = 0.0\n    for x in range(256):\n        p_x = float(data.count(bytes([x]))) / len(data)\n        if p_x > 0:\n            entropy += - p_x * math.log(p_x, 2)\n    return entropy\n\n# Plain text: ~3.8 | Encrypted bytes: ~7.95\nprint("Text Entropy: ", shannon_entropy(b"hello world this is plain text"))\nprint("Random Entropy:", shannon_entropy(bytes(range(256))))'},
 r:["Portable Executable", "Reverse Engineering", "Cryptography", "Entropy"]},

{t:"Wireshark", a:"Packet Analyzer", d:"The world's foremost open-source network protocol analyzer, used for real-time packet inspection, troubleshooting, and network forensics.", l:"core", g:["security","networking","troubleshooting","forensics"],
 b:["Wireshark intercepts traffic on local network interfaces and displays deep packet dissection across hundreds of protocol layers (Ethernet, IP, TCP, TLS, HTTP/2, DNS).",
    "It uses libpcap / Npcap drivers to capture frames directly from the data link layer in promiscuous mode, allowing engineers to inspect full TCP handshakes, window size negotiations, retransmissions, and latency bottlenecks.",
    "Display filters (such as `tcp.analysis.retransmission` or `http.response.code >= 400`) isolate exact anomalies among millions of transmitted frames.",
    "Engineers combine native command-line capture tools like `pktmon` (Windows) or `tcpdump` (Linux) with Wireshark to perform forensic post-mortem analysis on generated `.pcap` / `.pcapng` capture files."],
 k:["World standard packet analyzer supporting thousands of network protocols.",
    "Inspects frame headers and payload data from Layer 2 up to Layer 7.",
    "Powerful display filter syntax isolates retransmissions, latency, and TLS negotiations.",
    "Reads native `.pcapng` files exported from Windows PktMon and Linux tcpdump."],
 x:{lang:"powershell", code:'# Wireshark display filter examples:\n# Filter TCP retransmissions:\ntcp.analysis.retransmission\n\n# Filter HTTP POST requests:\nhttp.request.method == "POST"\n\n# Filter TLS handshakes with Server Hello:\ntls.handshake.type == 2'},
 r:["Raw Socket", "Promiscuous Mode", "PktMon", "TCP", "UDP"]},

{t:"Reverse Engineering", a:"Binary Analysis", d:"The discipline of disassembling, decompiling, and analyzing compiled software binaries to understand internal logic, security posture, or file formats.", l:"advanced", g:["security","reverse-engineering","binaries","malware-analysis"],
 b:["In software development, reverse engineering analyzes compiled machine code (executables, libraries, firmware) without having access to the original source code.",
    "Engineers inspect file headers (such as Windows PE or Linux ELF), Import Address Tables (IAT), exported functions, and embedded strings to reconstruct how an unknown program communicates and executes.",
    "Static analysis investigates the binary without executing it (examining Shannon entropy, imported DLLs, opcode sequences), avoiding risks of executing untrusted or potentially malicious software.",
    "Dynamic analysis executes the binary within a sandbox or under an attached debugger (like x64dbg or GDB), tracing system calls, memory allocations, and network sockets in real time."],
 k:["Extracts design architecture and algorithmic behavior from compiled binaries.",
    "Static analysis inspects headers, IAT, section entropy, and strings without execution.",
    "Dynamic analysis attaches debuggers to observe live registers, memory, and API calls.",
    "Core skill in vulnerability research, security auditing, and malware analysis."],
 x:{lang:"python", code:'# Static binary inspection with pefile:\nimport pefile\n\npe = pefile.PE("sample.exe")\nprint("Entrypoint RVA:", hex(pe.OPTIONAL_HEADER.AddressOfEntryPoint))\nfor section in pe.sections:\n    print(section.Name.decode().strip(), hex(section.SizeOfRawData))'},
 r:["Portable Executable", "Import Address Table", "Shannon Entropy", "Virtual Memory"]}

]);
