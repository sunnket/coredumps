TD.add("deep-learning", [

{t:"Deep Learning", d:"Machine learning with neural networks of many layers, which learn their own features from raw data instead of being given hand-engineered ones.", l:"core", g:["fundamentals","neural"],
 b:["The word deep refers to layer count. Each layer transforms its input into a slightly more abstract representation: in vision, edges become textures become object parts become objects. Nobody programs that hierarchy — it emerges from minimising a loss over enough data.",
    "Deep learning took over once three things arrived together: large labelled datasets, GPUs that made the matrix multiplications affordable, and architectural fixes (ReLU, batch norm, residual connections) that made deep networks trainable at all. It dominates perception and language; gradient-boosted trees still dominate tabular data."],
 k:["Learns feature hierarchies automatically from raw input.",
    "Needs far more data and compute than classical ML.",
    "Dominates vision, audio and language; loses to boosted trees on tabular data.",
    "Largely opaque — interpretability is an active and unsolved problem."],
 dg:"neural-net",
 r:["Neural Network","Machine Learning","Convolutional Neural Network","Transformer","GPU"]},

{t:"Neural Network", d:"A model built from layers of interconnected units, each computing a weighted sum of its inputs followed by a non-linear activation.", l:"core", g:["fundamentals","architecture"],
 b:["Data enters an input layer, flows through hidden layers, and exits an output layer. Each connection has a weight; each unit adds a bias and applies an activation function. Training adjusts every weight to reduce the loss, using backpropagation to work out each weight's share of the blame.",
    "The universal approximation theorem says a network with one sufficiently wide hidden layer can approximate any continuous function. In practice depth is far more efficient than width — deep narrow networks learn what shallow wide ones cannot afford to."],
 k:["Layers of weighted sums followed by non-linear activations.",
    "Without non-linearity, any stack of layers collapses to a single linear map.",
    "Depth buys representational efficiency that width cannot match.",
    "Parameter count = weights + biases across every layer."],
 dg:"neural-net",
 r:["Perceptron","Activation Function","Backpropagation","Deep Learning","Multilayer Perceptron"]},

{t:"Perceptron", d:"The simplest neural unit: multiply inputs by weights, sum, add a bias, apply an activation.", l:"core", g:["fundamentals","architecture"],
 b:["Rosenblatt's 1958 perceptron computes ŷ = f(w·x + b). A single one can only separate data with a straight line, which is why Minsky and Papert's demonstration that it cannot learn XOR stalled the field for a decade.",
    "The resolution was stacking them into layers with non-linear activations, giving the multilayer perceptron. Every unit in every modern network is still doing exactly this computation, billions of times."],
 k:["ŷ = f(w·x + b) — the atomic operation of every neural network.",
    "A single perceptron is a linear classifier and cannot learn XOR.",
    "Stacking with non-linearities removes the limitation.",
    "Weights are learned; the activation is chosen."],
 dg:"perceptron",
 r:["Neural Network","Multilayer Perceptron","Activation Function","Weight"]},

{t:"Multilayer Perceptron", a:"MLP / Dense Network", d:"A feed-forward network of fully connected layers — the baseline neural architecture.", l:"core", g:["architecture"],
 b:["Every unit in each layer connects to every unit in the next. That generality is also the weakness: an MLP knows nothing about the structure of its input, so it must learn from scratch that neighbouring pixels are related or that word order matters.",
    "It remains everywhere — as the feed-forward block inside every transformer, as the classification head on top of a CNN, and as a strong baseline for tabular data when you want a neural approach."],
 k:["Fully connected: parameters scale as inputs × outputs per layer.",
    "No built-in assumptions about spatial or sequential structure.",
    "Forms the feed-forward sublayer inside every transformer block.",
    "Needs flattened, fixed-size input."],
 dg:"neural-net",
 r:["Neural Network","Perceptron","Convolutional Neural Network","Inductive Bias"]},

{t:"Weight", d:"A learned parameter controlling how strongly one unit's output influences the next.", l:"core", g:["fundamentals","parameters"],
 b:["Weights are what a network learns. Each connection has one, initialised randomly and adjusted by gradient descent until the loss stops improving. A model's size is essentially its weight count: 7 billion parameters means 7 billion of these numbers.",
    "Storage follows directly: 7B parameters at 32-bit precision is 28 GB, at 16-bit is 14 GB, and at 4-bit quantisation is about 3.5 GB. That arithmetic is what determines whether a model fits on your GPU."],
 k:["The learned parameters — everything the model knows lives here.",
    "Initialisation matters: all-zero weights make every unit learn identically.",
    "Parameter count × bytes per parameter = memory needed to load the model.",
    "Weight decay shrinks them toward zero as a form of regularisation."],
 r:["Bias Term","Weight Initialisation","Quantisation","Gradient Descent"]},

{t:"Bias Term", d:"A learned constant added to a unit's weighted sum, letting the activation shift left or right.", l:"core", g:["fundamentals","parameters"],
 b:["Without a bias, every unit's decision boundary must pass through the origin. The bias frees it to sit anywhere, which is essential for fitting real data. It is one extra parameter per unit — negligible in count, significant in effect.",
    "Note this is a completely different concept from statistical bias in the bias-variance trade-off, and from societal bias in data. The three share a word and nothing else."],
 k:["Shifts the activation threshold; without it boundaries pass through the origin.",
    "One learned parameter per unit.",
    "Usually initialised to zero, unlike weights.",
    "Not the same as bias in bias-variance, nor as dataset bias."],
 dg:"perceptron",
 r:["Weight","Perceptron","Activation Function"]},

{t:"Activation Function", d:"The non-linear function applied to a unit's output, without which a deep network would be no more expressive than a single linear layer.", l:"core", g:["fundamentals","architecture"],
 b:["Stack two linear layers and you get another linear function — depth would buy nothing. The activation breaks that, letting each layer contribute genuinely new representational power. ReLU is the default for hidden layers; GELU and SiLU dominate in transformers.",
    "Output activations are chosen by task: sigmoid for binary probability, softmax for multi-class, and none at all for regression."],
 k:["Non-linearity is what makes depth meaningful.",
    "ReLU for hidden layers by default; GELU/SiLU in transformers.",
    "Sigmoid for binary output, softmax for multi-class, linear for regression.",
    "Saturating activations (sigmoid, tanh) cause vanishing gradients in deep stacks."],
 dg:"activations",
 r:["ReLU","Sigmoid","Softmax","Tanh","Vanishing Gradient"]},

{t:"ReLU", a:"Rectified Linear Unit", d:"The activation f(x) = max(0, x) — the default choice in hidden layers because it is cheap and does not saturate.", l:"core", g:["activation"],
 b:["ReLU passes positives through unchanged and zeroes negatives. Its gradient is exactly 1 for positive inputs, so it does not shrink gradients the way sigmoid does, which is what made training deep networks practical.",
    "Its failure mode is the dying ReLU: a unit whose input is always negative outputs zero forever, receives zero gradient, and never recovers. Leaky ReLU, PReLU and ELU allow a small negative slope to avoid it."],
 k:["f(x) = max(0, x) — one comparison, extremely cheap.",
    "Gradient is 1 for x > 0, so it does not vanish through depth.",
    "Produces sparse activations, which acts as mild regularisation.",
    "Dying ReLU: permanently negative inputs kill a unit — use Leaky ReLU if it happens."],
 dg:"activations",
 r:["Activation Function","Vanishing Gradient","GELU","Sigmoid"]},

{t:"Sigmoid", d:"An S-shaped activation squashing any real number into the range (0, 1), used to produce probabilities.", l:"intermediate", g:["activation"],
 b:["σ(x) = 1/(1 + e^-x). Its output reads naturally as a probability, which is why it sits at the end of binary classifiers and multi-label heads. It is also the gate function inside LSTMs.",
    "As a hidden activation it is largely obsolete. Its gradient peaks at 0.25 and approaches zero for large inputs, so multiplying it through ten layers shrinks the gradient to nothing — the vanishing gradient problem that ReLU solved."],
 k:["Maps ℝ to (0, 1) — interpretable as a probability.",
    "Maximum gradient is 0.25, so deep stacks vanish quickly.",
    "Still correct for binary and multi-label output layers.",
    "Not zero-centred, which slows optimisation slightly."],
 dg:"activations",
 r:["Activation Function","Softmax","Vanishing Gradient","Logistic Regression"]},

{t:"Tanh", d:"A zero-centred S-shaped activation mapping inputs to (−1, 1).", l:"intermediate", g:["activation"],
 b:["Tanh is a rescaled sigmoid with output centred at zero, which makes gradient updates better behaved than sigmoid's all-positive outputs. Its maximum gradient is 1 rather than 0.25, so it vanishes more slowly.",
    "It still saturates at both extremes, so ReLU replaced it in feed-forward networks. It survives inside LSTM and GRU cells, where the bounded output is exactly what is wanted for a cell state."],
 k:["Zero-centred output in (−1, 1) — better conditioned than sigmoid.",
    "Maximum gradient of 1 rather than 0.25.",
    "Still saturates; ReLU is preferred for deep feed-forward stacks.",
    "Standard inside LSTM and GRU cells."],
 dg:"activations",
 r:["Sigmoid","ReLU","LSTM","Activation Function"]},

{t:"Softmax", d:"A function that turns a vector of scores into a probability distribution summing to one.", l:"core", g:["activation"],
 b:["Softmax exponentiates each score and divides by the sum of all exponentials. The exponential exaggerates differences, so a modest lead in logits becomes a large lead in probability. It is the standard output layer for multi-class classification and for next-token prediction in language models.",
    "Dividing the logits by a temperature before softmax controls sharpness: low temperature approaches a hard argmax, high temperature flattens toward uniform. Implementations subtract the maximum logit first for numerical stability."],
 k:["Outputs a proper probability distribution over classes.",
    "Exponentials amplify differences between logits.",
    "Temperature scales the sharpness — the sampling knob in LLMs.",
    "Numerically stable implementations subtract the max logit before exponentiating."],
 r:["Sigmoid","Cross-Entropy","Temperature","Classification"]},

{t:"GELU", a:"Gaussian Error Linear Unit", d:"A smooth activation that weights each input by the probability it is positive — the standard in transformer architectures.", l:"advanced", g:["activation"],
 b:["GELU(x) = x · Φ(x), where Φ is the standard normal CDF. Unlike ReLU's hard cut at zero, it transitions smoothly and allows small negative values through, which gives better-behaved gradients near the origin.",
    "BERT, GPT and most modern transformers use GELU or the closely related SiLU/Swish. The empirical gain over ReLU is small but consistent at scale, and the extra compute is negligible next to the attention layers."],
 k:["Smooth, non-monotonic, allows small negative outputs.",
    "Default activation in BERT, GPT and most transformer stacks.",
    "SiLU (Swish), x·σ(x), is a very close relative used in Llama-style models.",
    "Slightly more expensive than ReLU; irrelevant at transformer scale."],
 r:["ReLU","Activation Function","Transformer"]},

{t:"Forward Pass", d:"Running input through the network layer by layer to produce a prediction.", l:"core", g:["training","fundamentals"],
 b:["Each layer takes the previous layer's output, applies its weights and activation, and passes it on. During training the intermediate activations are kept in memory because backpropagation needs them; that is why training uses far more memory than inference.",
    "Inference is a forward pass alone. Wrapping it in `torch.no_grad()` disables gradient tracking and cuts memory use substantially."],
 k:["Input → layers → prediction, with no learning involved.",
    "Training stores intermediate activations for the backward pass.",
    "Inference is a forward pass with gradient tracking disabled.",
    "Activation memory, not parameters, usually limits training batch size."],
 dg:"backprop",
 r:["Backpropagation","Inference","Computational Graph"]},

{t:"Backpropagation", d:"The algorithm that computes each weight's gradient by applying the chain rule backwards through the network.", l:"core", g:["training","fundamentals"],
 b:["After the forward pass produces a loss, backpropagation works backwards computing ∂loss/∂w for every weight. Because the chain rule reuses intermediate results, the whole thing costs roughly the same as one forward pass — which is the reason training deep networks is feasible at all.",
    "Backpropagation only computes gradients. The optimiser is what uses them to update weights. Together they form the training loop that every deep learning framework implements."],
 k:["Reverse-mode automatic differentiation, applied to the chain rule.",
    "Costs about the same as a forward pass, regardless of parameter count.",
    "Computes gradients; the optimiser applies them.",
    "Long chains multiply many small factors — the source of vanishing gradients."],
 dg:"backprop",
 r:["Gradient Descent","Automatic Differentiation","Vanishing Gradient","Forward Pass"]},

{t:"Epoch", d:"One complete pass of the training algorithm over the entire training dataset.", l:"core", g:["training","fundamentals"],
 b:["If you have 50,000 examples and a batch size of 100, one epoch is 500 iterations. Models typically train for many epochs, and validation loss is checked after each to detect the point where overfitting begins.",
    "Very large language models are often trained for a single epoch or less: with trillions of tokens available, seeing fresh data beats revisiting old data."],
 k:["One epoch = one full pass over the training set.",
    "Iterations per epoch = dataset size ÷ batch size.",
    "Track validation loss per epoch to find the early-stopping point.",
    "LLM pre-training often uses fewer than one full epoch of a huge corpus."],
 r:["Batch Size","Iteration","Early Stopping","Gradient Descent"]},

{t:"Batch Size", d:"The number of training examples processed before the model's weights are updated once.", l:"core", g:["training","hyperparameter"],
 b:["Small batches give noisy gradient estimates, update frequently, and are widely believed to find flatter minima that generalise better. Large batches give smoother gradients, use hardware more efficiently, and require a proportionally larger learning rate to compensate for fewer updates.",
    "In practice batch size is usually chosen by what fits in GPU memory. Gradient accumulation simulates a larger batch by summing gradients across several small forward/backward passes before stepping."],
 k:["Trades gradient noise against hardware efficiency.",
    "Larger batches generally need a proportionally larger learning rate.",
    "Usually capped by GPU memory, not by theory.",
    "Gradient accumulation gives large-batch behaviour on small hardware."],
 r:["Epoch","Learning Rate","Stochastic Gradient Descent","GPU"]},

{t:"Iteration", a:"Step", d:"One weight update — a single forward pass, backward pass and optimiser step on one batch.", l:"core", g:["training","fundamentals"],
 b:["Iterations are the natural unit for large-scale training, where epochs may be meaningless. Learning-rate schedules, logging intervals and checkpoint frequencies are all usually expressed in steps.",
    "Confusing iterations with epochs is a common source of misconfigured schedules: a cosine decay set to 100 steps when you intended 100 epochs will decay to zero almost immediately."],
 k:["One iteration = one batch = one weight update.",
    "Steps, not epochs, are the standard unit in large-scale training.",
    "Learning-rate schedules are usually defined over steps.",
    "steps_per_epoch = dataset size ÷ batch size."],
 r:["Epoch","Batch Size","Learning Rate Schedule"]},

{t:"Optimiser", d:"The algorithm that decides how to update weights given their gradients.", l:"core", g:["training","optimisation"],
 b:["Plain SGD steps directly along the negative gradient. Momentum accumulates a velocity so the update continues in a consistent direction and damps oscillation. RMSProp scales the step per parameter by a running average of squared gradients. Adam combines momentum and RMSProp and is the default almost everywhere.",
    "AdamW fixes Adam's incorrect coupling of weight decay with the adaptive scaling, and is the standard for transformer training. The optimiser also carries state — Adam stores two extra values per parameter, tripling optimiser memory."],
 k:["AdamW is the modern default; plain SGD with momentum still wins some vision tasks.",
    "Adam stores two extra tensors per parameter — real memory cost at scale.",
    "Adaptive methods converge faster; SGD sometimes generalises slightly better.",
    "The optimiser applies gradients; backpropagation computes them."],
 r:["Adam Optimiser","Gradient Descent","Momentum","Weight Decay"]},

{t:"Adam Optimiser", a:"Adaptive Moment Estimation", d:"The default deep learning optimiser, combining momentum with per-parameter adaptive step sizes.", l:"core", g:["training","optimisation"],
 b:["Adam tracks an exponential moving average of the gradient (first moment, giving momentum) and of the squared gradient (second moment, giving per-parameter scaling). Parameters with consistently small gradients get larger effective steps, and vice versa. Bias correction fixes the early-training underestimate from initialising both averages at zero.",
    "It converges fast and needs little tuning, which is why it is the default. AdamW decouples weight decay from the adaptive scaling — a subtle fix that materially improves generalisation and is now standard."],
 k:["First moment = momentum; second moment = per-parameter scaling.",
    "Typical defaults: lr 1e-3 (1e-4 for transformers), β₁ 0.9, β₂ 0.999.",
    "Use AdamW when you want weight decay to behave correctly.",
    "Costs two extra state tensors per parameter."],
 x:{lang:"python", code:'optimizer = torch.optim.AdamW(\n    model.parameters(),\n    lr=3e-4,\n    betas=(0.9, 0.95),\n    weight_decay=0.1)\n\nscheduler = torch.optim.lr_scheduler.OneCycleLR(\n    optimizer, max_lr=3e-4, total_steps=total_steps)'},
 r:["Optimiser","Gradient Descent","Weight Decay","Learning Rate"]},

{t:"Momentum", d:"An optimiser technique that accumulates a running direction of travel, smoothing updates and accelerating through shallow regions.", l:"intermediate", g:["training","optimisation"],
 b:["Instead of stepping purely along the current gradient, momentum keeps a velocity vector that is a decayed sum of past gradients. Consistent directions build speed; oscillating directions cancel out. The physical analogy is a ball rolling downhill rather than a hiker taking independent steps.",
    "A momentum coefficient of 0.9 means each update retains 90% of the previous velocity. Nesterov momentum evaluates the gradient at the anticipated next position, which corrects overshoot slightly earlier."],
 k:["Accumulates a velocity from past gradients, typically with β = 0.9.",
    "Damps oscillation across narrow ravines in the loss surface.",
    "Speeds progress through flat regions and small local dips.",
    "Nesterov looks ahead before computing the gradient."],
 r:["Adam Optimiser","Gradient Descent","Optimiser"]},

{t:"Learning Rate Schedule", d:"A rule that changes the learning rate over the course of training rather than holding it fixed.", l:"intermediate", g:["training","hyperparameter"],
 b:["Large early steps make fast progress; small later steps allow the model to settle into a minimum. Step decay drops the rate at fixed milestones, cosine annealing decreases smoothly, and one-cycle rises then falls.",
    "Warmup — starting near zero and ramping up over the first few thousand steps — is essential for transformers, where large early updates on randomly initialised attention weights destabilise training. Warmup followed by cosine decay is the standard recipe."],
 k:["High early for progress, low late for convergence.",
    "Warmup then cosine decay is the transformer default.",
    "Warmup prevents early divergence with adaptive optimisers.",
    "Schedules are defined over steps — check the units."],
 r:["Learning Rate","Adam Optimiser","Iteration","Gradient Descent"]},

{t:"Weight Decay", d:"A regularisation term that pulls weights toward zero on every update.", l:"intermediate", g:["training","regularisation"],
 b:["Weight decay multiplies weights by a factor slightly less than one at each step, discouraging any single weight from growing large. For plain SGD it is mathematically equivalent to L2 regularisation; for Adam it is not, because the adaptive scaling distorts the penalty — hence AdamW, which applies decay directly to the weights.",
    "Typical values are 0.01 to 0.1 for transformers. Biases and normalisation parameters are conventionally excluded from decay."],
 k:["Shrinks weights each step, favouring simpler solutions.",
    "Equivalent to L2 for SGD, but not for Adam — use AdamW.",
    "Typical range 0.01–0.1 for large language models.",
    "Exclude biases and LayerNorm parameters from decay."],
 r:["L1 and L2 Regularisation","Adam Optimiser","Regularisation","Overfitting"]},

{t:"Dropout", d:"Randomly zeroing a fraction of units during training so the network cannot depend on any single one.", l:"core", g:["training","regularisation"],
 b:["At each training step, each unit is dropped with probability p (commonly 0.1 to 0.5). The network is forced to spread its representation across many units rather than relying on a few, which reduces co-adaptation. It behaves like training an exponential ensemble of thinned networks and averaging them.",
    "Dropout is disabled at inference, with activations scaled to compensate — which is exactly what `model.eval()` does in PyTorch. Forgetting that call is one of the most common causes of inexplicably poor evaluation results."],
 k:["Randomly zeroes units during training only.",
    "Typical rates: 0.1 in transformers, 0.5 in older dense layers.",
    "Must be turned off at inference — call `model.eval()`.",
    "Largely replaced by other regularisers in very large models."],
 r:["Regularisation","Overfitting","Batch Normalisation","Early Stopping"]},

{t:"Batch Normalisation", d:"Normalising each feature across the examples in a batch, which stabilises and accelerates training.", l:"intermediate", g:["training","architecture"],
 b:["BatchNorm standardises each channel using the batch's mean and variance, then applies a learned scale and shift. It allows much higher learning rates, reduces sensitivity to initialisation, and has a mild regularising effect from the batch-to-batch noise.",
    "Its dependence on batch statistics is the catch: it behaves differently at inference (using running averages), degrades with very small batches, and is awkward for sequences of varying length. Transformers use LayerNorm instead for exactly these reasons."],
 k:["Normalises across the batch dimension per channel.",
    "Enables higher learning rates and faster convergence.",
    "Different behaviour in train and eval modes — a classic bug source.",
    "Breaks down with small batches; LayerNorm is used in transformers."],
 r:["Layer Normalisation","Dropout","Convolutional Neural Network"]},

{t:"Layer Normalisation", d:"Normalising across the features of each individual example, independent of batch size.", l:"intermediate", g:["training","architecture"],
 b:["LayerNorm computes mean and variance over the feature dimension of a single example, so it behaves identically regardless of batch size and identically in training and inference. That makes it the natural choice for sequence models with variable lengths.",
    "Every transformer block uses it. Pre-norm placement (normalising before the sublayer rather than after) made very deep transformers trainable without careful warmup. RMSNorm, a simplified variant that skips mean centring, is used in Llama-style models."],
 k:["Normalises per example across features — batch size is irrelevant.",
    "Identical behaviour in training and inference.",
    "Pre-norm placement stabilises very deep transformers.",
    "RMSNorm is a cheaper variant used in modern LLMs."],
 dg:"transformer",
 r:["Batch Normalisation","Transformer","Residual Connection"]},

{t:"Early Stopping", d:"Halting training when validation performance stops improving, rather than running a fixed number of epochs.", l:"core", g:["training","regularisation"],
 b:["Training loss decreases indefinitely; validation loss falls, bottoms out, then rises as the model begins memorising. Early stopping monitors the validation metric, waits a patience window for improvement, and stops — restoring the best checkpoint.",
    "It is the cheapest regularisation available and requires no hyperparameter tuning beyond patience. Always restore the best weights rather than keeping the final ones, or you keep the overfitted model you were trying to avoid."],
 k:["Stops when validation stops improving, not on a fixed epoch count.",
    "Patience of 5–20 evaluations is typical.",
    "Restore the best checkpoint — the last one is worse by definition.",
    "Free regularisation with essentially no downside."],
 dg:"overfitting",
 r:["Overfitting","Regularisation","Epoch","Cross-Validation"]},

{t:"Vanishing Gradient", d:"Gradients shrinking toward zero as they propagate backwards, so early layers stop learning.", l:"intermediate", g:["failure-mode","training"],
 b:["Backpropagation multiplies gradients layer by layer. If each factor is less than one — as it is with sigmoid, whose maximum derivative is 0.25 — the product decays exponentially with depth. After twenty layers the gradient reaching the first layer is effectively zero.",
    "This is why networks stayed shallow until ReLU (gradient of exactly 1 for positive inputs), residual connections (gradients bypass layers entirely), and careful initialisation and normalisation solved it. It is also why LSTMs replaced plain RNNs for long sequences."],
 k:["Gradients decay exponentially through depth when factors are below 1.",
    "Symptom: early layers barely change while later layers train fine.",
    "Fixed by ReLU, residual connections, normalisation and good initialisation.",
    "LSTM and GRU gating solves the sequential version of the problem."],
 dg:"backprop",
 r:["Exploding Gradient","ReLU","Residual Connection","LSTM","Backpropagation"]},

{t:"Exploding Gradient", d:"Gradients growing without bound during backpropagation, destroying the weights in a single step.", l:"intermediate", g:["failure-mode","training"],
 b:["The mirror image of vanishing gradients: when the multiplied factors exceed one, the product grows exponentially. The result is a huge update that pushes weights to absurd values, and the loss becomes NaN — usually within a few steps.",
    "Gradient clipping is the standard fix: rescale the gradient vector whenever its norm exceeds a threshold, typically 1.0. Lowering the learning rate and using proper initialisation prevent most cases from arising."],
 k:["Loss suddenly becoming NaN or inf is the classic symptom.",
    "Gradient clipping by global norm (threshold ~1.0) is the standard remedy.",
    "Most common in RNNs and in very deep or badly initialised networks.",
    "Often accompanied by a learning rate that is simply too high."],
 r:["Vanishing Gradient","Gradient Clipping","Learning Rate","Backpropagation"]},

{t:"Gradient Clipping", d:"Capping the magnitude of gradients before the optimiser step, to prevent a single bad batch from wrecking training.", l:"intermediate", g:["training","technique"],
 b:["Clipping by global norm computes the norm across all parameters and, if it exceeds a threshold, scales every gradient down proportionally. This preserves the direction while bounding the step size. Clipping by value caps each element independently, which distorts direction and is used less often.",
    "It is standard practice in RNN and transformer training, where a rare outlier batch can otherwise produce a catastrophic update."],
 k:["Clip by global norm to preserve gradient direction.",
    "A threshold of 1.0 is the usual default for transformers.",
    "Essential for RNNs; standard hygiene for large model training.",
    "Frequent clipping suggests the learning rate is too high."],
 x:{lang:"python", code:'loss.backward()\ntorch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\noptimizer.step()\noptimizer.zero_grad(set_to_none=True)'},
 r:["Exploding Gradient","Optimiser","Backpropagation"]},

{t:"Weight Initialisation", d:"How weights are set before training begins — a choice that determines whether the network can train at all.", l:"intermediate", g:["training","technique"],
 b:["Initialise everything to zero and every unit in a layer computes the same thing and receives the same gradient forever — symmetry never breaks. Initialise too large and activations explode; too small and they vanish.",
    "Xavier (Glorot) initialisation scales variance by fan-in and fan-out and suits tanh and sigmoid. He initialisation scales by fan-in only and suits ReLU, whose zeroing of negatives halves the effective variance. Frameworks pick sensible defaults, but the choice still matters for very deep networks."],
 k:["All-zero initialisation makes units identical — training cannot start.",
    "He initialisation for ReLU; Xavier for tanh and sigmoid.",
    "Poor initialisation causes vanishing or exploding activations immediately.",
    "Biases are conventionally initialised to zero."],
 r:["Vanishing Gradient","ReLU","Neural Network","Weight"]},

{t:"Convolutional Neural Network", a:"CNN", d:"A network that slides small learned filters across its input, exploiting the fact that useful visual patterns are local and position-independent.", l:"core", g:["architecture","vision"],
 b:["A CNN's key ideas are local connectivity (each unit sees a small patch), weight sharing (the same filter is applied everywhere) and pooling (progressively reducing spatial resolution). Weight sharing means an edge detector learned in one corner works everywhere, which cuts parameters enormously compared with a dense layer.",
    "Early layers learn edges and colour blobs; middle layers learn textures and parts; late layers learn objects. This hierarchy emerges from training alone. CNNs dominated vision from 2012 until vision transformers, and remain the efficient choice for most practical tasks."],
 k:["Local connectivity plus weight sharing gives translation invariance cheaply.",
    "Far fewer parameters than a dense layer on the same input.",
    "Feature hierarchy emerges: edges → textures → parts → objects.",
    "Still the efficient default for vision at moderate data scale."],
 dg:"cnn",
 r:["Convolution","Pooling","Kernel","Vision Transformer","ResNet"]},

{t:"Convolution", d:"Sliding a small weight matrix over an input and computing a dot product at every position.", l:"core", g:["operation","vision"],
 b:["A 3×3 kernel positioned over a 3×3 patch multiplies elementwise and sums to one output number. Sliding it across the whole image produces a feature map showing where that pattern occurs. The kernel's nine weights are learned, so the network decides what pattern to look for.",
    "Multiple kernels per layer produce multiple feature maps — a layer with 64 kernels outputs 64 channels. Stride controls step size, padding controls whether the output shrinks, and dilation spreads the kernel to see further without more parameters."],
 k:["The same kernel is applied at every position — that is weight sharing.",
    "Output size = (input − kernel + 2·padding)/stride + 1.",
    "Number of kernels = number of output channels.",
    "1×1 convolutions mix channels without touching spatial structure."],
 dg:"convolution",
 r:["Kernel","Stride","Padding","Feature Map","Convolutional Neural Network"]},

{t:"Kernel", a:"Filter", d:"The small learnable weight matrix that a convolution slides across its input.", l:"core", g:["operation","vision"],
 b:["A kernel is typically 3×3 or 5×5 spatially and spans all input channels in depth — so a 3×3 kernel on a 64-channel input has 3×3×64 weights plus a bias. Each kernel learns to detect one pattern: a vertical edge, a particular texture, a colour transition.",
    "Stacking 3×3 kernels has become standard because two stacked 3×3 layers cover the same receptive field as one 5×5 with fewer parameters and an extra non-linearity in between."],
 k:["Spans the full depth of the input: 3×3×C weights per kernel.",
    "One kernel produces one output channel.",
    "Two stacked 3×3 kernels beat one 5×5 — fewer parameters, more non-linearity.",
    "Depthwise separable convolutions split spatial and channel mixing to save compute."],
 dg:"convolution",
 r:["Convolution","Feature Map","Receptive Field","Convolutional Neural Network"]},

{t:"Stride", d:"How many pixels the kernel moves between applications.", l:"intermediate", g:["operation","vision"],
 b:["Stride 1 moves one pixel at a time and preserves resolution (with padding). Stride 2 skips every other position, halving the output's width and height and reducing computation fourfold.",
    "Strided convolution is a learnable alternative to pooling for downsampling, and many modern architectures prefer it because the reduction is learned rather than fixed."],
 k:["Stride 1 preserves size; stride 2 halves each spatial dimension.",
    "A learnable alternative to pooling for downsampling.",
    "Larger stride cuts compute quadratically.",
    "Affects the output size formula directly."],
 r:["Convolution","Padding","Pooling","Kernel"]},

{t:"Padding", d:"Adding a border of extra values around the input so the convolution can cover the edges.", l:"intermediate", g:["operation","vision"],
 b:["Without padding (valid mode) a 3×3 kernel shrinks the output by two pixels in each dimension, and edge pixels get sampled less often than centre pixels. Padding with a one-pixel border of zeros (same mode) keeps input and output the same size.",
    "Keeping spatial dimensions constant matters in deep networks, where repeated shrinkage would exhaust the resolution long before the network was deep enough."],
 k:["`same` padding preserves spatial dimensions; `valid` shrinks them.",
    "Zero padding is standard; reflect and replicate exist for image tasks.",
    "Without padding, edge pixels contribute to fewer outputs.",
    "Essential for building deep convolutional stacks."],
 r:["Convolution","Stride","Kernel"]},

{t:"Pooling", d:"Downsampling a feature map by summarising each small region with a single value.", l:"core", g:["operation","vision"],
 b:["Max pooling over 2×2 windows keeps the strongest activation in each window, halving width and height. It reduces computation, enlarges the receptive field of later layers, and adds a small amount of translation invariance — a feature detected one pixel over still survives.",
    "Global average pooling collapses each channel to a single number and is now the standard bridge from convolutional features to a classifier head, replacing the huge flatten-then-dense layers of older architectures."],
 k:["Max pooling keeps the strongest response; average pooling smooths.",
    "2×2 with stride 2 halves each spatial dimension.",
    "No learnable parameters — pure downsampling.",
    "Global average pooling replaced flatten+dense in modern CNNs."],
 dg:"cnn",
 r:["Convolution","Feature Map","Convolutional Neural Network","Stride"]},

{t:"Feature Map", a:"Activation Map", d:"The output of applying one kernel across an input — a spatial map of where that pattern was found.", l:"intermediate", g:["operation","vision"],
 b:["Each value in a feature map records how strongly the kernel's pattern matched at that location. A layer with 64 kernels produces 64 feature maps stacked as channels, which become the next layer's input.",
    "Visualising feature maps is a standard debugging technique: early layers show recognisable edges and textures, while deeper ones become abstract. All-zero feature maps indicate dead units."],
 k:["One feature map per kernel, showing where its pattern occurs.",
    "Channel depth grows while spatial size shrinks through a CNN.",
    "Visualising them is a useful debugging tool.",
    "Activation memory across feature maps often limits batch size."],
 dg:"cnn",
 r:["Convolution","Kernel","Pooling","Convolutional Neural Network"]},

{t:"Receptive Field", d:"The region of the original input that influences a single unit deep in the network.", l:"advanced", g:["theory","vision"],
 b:["A unit in the first convolutional layer sees only a 3×3 patch. After several layers of convolution and pooling its effective receptive field spans a large fraction of the image, which is what allows deep layers to reason about whole objects rather than local texture.",
    "If the receptive field is smaller than the objects you need to recognise, the network structurally cannot see them. Dilated convolutions expand it rapidly without adding parameters, which is why they are common in segmentation."],
 k:["Grows with depth, kernel size, stride and dilation.",
    "Must be large enough to cover the objects of interest.",
    "Dilated convolutions enlarge it without extra parameters.",
    "Self-attention has a global receptive field from the first layer."],
 r:["Convolution","Kernel","Pooling"]},

{t:"Residual Connection", a:"Skip Connection", d:"A shortcut that adds a layer's input to its output, letting gradients flow directly past the layer.", l:"intermediate", g:["architecture","technique"],
 b:["Instead of learning H(x), a residual block learns F(x) and outputs F(x) + x. If the optimal transformation is close to identity, the block only needs to learn a small correction — much easier than learning identity from scratch. Crucially, the addition gives the gradient an unimpeded path backwards.",
    "This single idea, introduced by ResNet in 2015, made networks of a hundred-plus layers trainable, and every transformer block uses it. It is arguably the most important architectural innovation of the deep learning era."],
 k:["output = F(x) + x — the input bypasses the transformation.",
    "Gives gradients a direct path, largely solving vanishing gradients in depth.",
    "Makes learning an identity mapping trivial rather than hard.",
    "Used in ResNet, U-Net and every transformer block."],
 dg:"transformer",
 r:["ResNet","Vanishing Gradient","Transformer","Layer Normalisation"]},

{t:"ResNet", a:"Residual Network", d:"The CNN architecture that introduced residual connections and made very deep networks trainable.", l:"intermediate", g:["architecture","vision"],
 b:["Before ResNet, adding layers past about twenty made networks worse — not from overfitting but because they became untrainable. ResNet's insight was that a deeper network should at worst be able to learn identity mappings, and residual connections make that easy.",
    "ResNet-50 remains a workhorse backbone for detection, segmentation and transfer learning, and pretrained ImageNet weights for it are among the most downloaded artefacts in machine learning."],
 k:["Residual blocks made 50, 101 and 152-layer networks trainable.",
    "Bottleneck blocks (1×1, 3×3, 1×1) keep deep variants affordable.",
    "ResNet-50 is still a default backbone and transfer-learning starting point.",
    "Its core idea now appears in essentially every deep architecture."],
 r:["Residual Connection","Convolutional Neural Network","Transfer Learning","Vanishing Gradient"]},

{t:"Recurrent Neural Network", a:"RNN", d:"A network that processes a sequence one element at a time, carrying a hidden state forward as memory.", l:"intermediate", g:["architecture","sequence"],
 b:["At each step an RNN combines the current input with the previous hidden state to produce a new hidden state. The same weights are reused at every step, so the network can handle sequences of any length and share what it learns across positions.",
    "The problems are severe for long sequences: gradients vanish across many steps, so early context is forgotten, and the sequential dependency prevents parallelisation. LSTMs addressed the first; transformers eliminated the second and displaced RNNs almost entirely."],
 k:["Same weights applied at every timestep, with a hidden state as memory.",
    "Cannot be parallelised across the sequence — inherently sequential.",
    "Vanishing gradients limit effective memory to a few dozen steps.",
    "Largely superseded by transformers, though still used where memory is tight."],
 dg:"rnn",
 r:["LSTM","GRU","Transformer","Vanishing Gradient","Sequence Model"]},

{t:"LSTM", a:"Long Short-Term Memory", d:"An RNN cell with gates that decide what to keep, what to forget and what to output, enabling much longer memory.", l:"intermediate", g:["architecture","sequence"],
 b:["An LSTM maintains a cell state that flows through time with only linear interactions, plus three learned gates. The forget gate decides what to discard, the input gate what new information to store, and the output gate what to expose as the hidden state.",
    "Because the cell state path involves addition rather than repeated multiplication, gradients survive far longer — hundreds of steps rather than tens. LSTMs powered machine translation and speech recognition for years before transformers arrived."],
 k:["Forget, input and output gates control the cell state.",
    "The additive cell-state path is what preserves gradients.",
    "Handles dependencies over hundreds of steps.",
    "Still competitive for small data and low-latency streaming."],
 dg:"rnn",
 r:["Recurrent Neural Network","GRU","Vanishing Gradient","Transformer"]},

{t:"GRU", a:"Gated Recurrent Unit", d:"A simplified LSTM with two gates instead of three and no separate cell state.", l:"intermediate", g:["architecture","sequence"],
 b:["A GRU merges the forget and input gates into a single update gate and adds a reset gate, dropping the separate cell state. That gives roughly 25% fewer parameters and faster training with performance usually comparable to an LSTM.",
    "Which of the two performs better is task-dependent and rarely dramatic. GRU is often preferred when data is limited or compute is tight."],
 k:["Update and reset gates; no separate cell state.",
    "About 25% fewer parameters than an LSTM.",
    "Comparable accuracy in most tasks; sometimes better on small data.",
    "Faster to train and to run than an LSTM."],
 r:["LSTM","Recurrent Neural Network","Sequence Model"]},

{t:"Sequence Model", d:"Any model whose input or output is an ordered sequence — text, audio, time series, DNA.", l:"intermediate", g:["architecture","sequence"],
 b:["Sequence tasks come in shapes: one-to-many (image captioning), many-to-one (sentiment classification), many-to-many aligned (part-of-speech tagging) and many-to-many unaligned (translation, which needs an encoder-decoder).",
    "The architectures evolved from RNNs to LSTMs to attention-augmented encoder-decoders to pure transformers. State-space models such as Mamba are the current attempt to regain linear-time sequence processing without losing quality."],
 k:["Order carries meaning — shuffling the input destroys it.",
    "One-to-many, many-to-one and many-to-many are the task shapes.",
    "Transformers process the whole sequence in parallel; RNNs cannot.",
    "State-space models revisit linear-time processing for very long sequences."],
 r:["Recurrent Neural Network","Transformer","Encoder-Decoder","Attention Mechanism"]},

{t:"Attention Mechanism", d:"A method that lets a model weigh how much each part of the input matters when producing each part of the output.", l:"core", g:["architecture","transformer"],
 b:["Attention computes a compatibility score between a query and every key, normalises the scores with softmax, and returns the correspondingly weighted sum of values. Rather than compressing a whole sentence into one fixed vector, the model can look directly at whichever words are relevant right now.",
    "It was introduced to fix the bottleneck in RNN translation models. The 2017 discovery that attention alone — without recurrence — was sufficient produced the transformer and reshaped the entire field."],
 k:["Query, key and value: score the query against keys, weight the values.",
    "Softmax over scores produces the attention weights.",
    "Gives a direct path between any two positions, regardless of distance.",
    "Cost is quadratic in sequence length, which bounds context size."],
 dg:"attention",
 r:["Self-Attention","Multi-Head Attention","Transformer","Softmax"]},

{t:"Self-Attention", d:"Attention where queries, keys and values all come from the same sequence, letting every token look at every other token.", l:"core", g:["architecture","transformer"],
 b:["Each token is projected into a query, a key and a value. Every query is scored against every key, giving an n×n matrix of relationships, and each token's output is the weighted sum of all values. That is how the model resolves what `it` refers to, or which adjective modifies which noun.",
    "Scaled dot-product attention divides scores by √d to keep softmax gradients healthy. Causal (masked) self-attention hides future positions so a language model cannot see the answer it is predicting."],
 k:["Q, K and V all derive from the same sequence.",
    "Produces an n×n attention matrix — hence quadratic cost.",
    "Scaling by √d keeps softmax gradients from saturating.",
    "Causal masking prevents a decoder from attending to future tokens."],
 dg:"attention",
 r:["Attention Mechanism","Multi-Head Attention","Transformer","Context Window"]},

{t:"Multi-Head Attention", d:"Running several attention operations in parallel on different learned projections, then concatenating the results.", l:"intermediate", g:["architecture","transformer"],
 b:["One attention head can only express one relationship pattern per position. Splitting the embedding into, say, 12 heads lets different heads specialise: some track syntax, some track coreference, some track position. Their outputs are concatenated and projected back to the model dimension.",
    "Total compute is roughly unchanged because each head works in a lower-dimensional space. Multi-query and grouped-query attention share keys and values across heads to shrink the KV cache during inference."],
 k:["Heads attend to different relationship patterns in parallel.",
    "Each head operates in d_model / n_heads dimensions, so cost stays similar.",
    "Interpretability studies find heads specialising in syntax and coreference.",
    "GQA and MQA share K/V across heads to reduce inference memory."],
 dg:"transformer",
 r:["Self-Attention","Transformer","Attention Mechanism","KV Cache"]},

{t:"Positional Encoding", d:"Information added to token embeddings so the model knows the order of the sequence.", l:"intermediate", g:["architecture","transformer"],
 b:["Self-attention is permutation-invariant: without positional information, `dog bites man` and `man bites dog` produce identical representations. The original transformer added fixed sinusoidal vectors of different frequencies; later models learned position embeddings instead.",
    "Rotary position embedding (RoPE) is now standard in large language models. It rotates the query and key vectors by an angle proportional to position, which encodes relative distance naturally and extrapolates better to sequences longer than those seen in training."],
 k:["Attention has no inherent notion of order — position must be injected.",
    "Sinusoidal, learned and rotary are the three main approaches.",
    "RoPE encodes relative position and extrapolates better to long contexts.",
    "Context-length extension techniques mostly work by rescaling RoPE."],
 dg:"transformer",
 r:["Transformer","Self-Attention","Embedding","Context Window"]},

{t:"Transformer", d:"The architecture built entirely from attention and feed-forward layers that underpins nearly all modern AI.", l:"core", g:["architecture","transformer"],
 b:["A transformer block does two things: multi-head self-attention mixes information across positions, then a feed-forward network processes each position independently. Residual connections and layer normalisation wrap both. Stack this block N times and you have GPT, BERT, T5, ViT, Whisper and almost everything else.",
    "Its decisive advantage over RNNs is parallelism: every position is computed simultaneously during training, so the architecture scales with available hardware. That is what made training on trillions of tokens possible, and therefore what made large language models possible."],
 k:["Attention mixes across positions; the MLP processes each position.",
    "Fully parallel during training — the reason it scales.",
    "Encoder-only (BERT), decoder-only (GPT) and encoder-decoder (T5) variants.",
    "Attention is O(n²) in sequence length, which bounds context."],
 x:{lang:"python", code:'import torch.nn as nn\n\nlayer = nn.TransformerEncoderLayer(\n    d_model=512, nhead=8,\n    dim_feedforward=2048,\n    norm_first=True)          # pre-norm: more stable when deep\n\nencoder = nn.TransformerEncoder(layer, num_layers=6)'},
 dg:"transformer",
 r:["Self-Attention","Multi-Head Attention","Large Language Model","BERT","GPT"]},

{t:"Encoder-Decoder", a:"Seq2Seq", d:"An architecture where one stack compresses the input and another generates the output, used when input and output lengths differ.", l:"intermediate", g:["architecture","sequence"],
 b:["The encoder reads the whole input and produces contextual representations; the decoder generates the output one token at a time, attending both to its own previous outputs and — via cross-attention — to the encoder's representations. Translation is the canonical use.",
    "Encoder-only models (BERT) suit understanding tasks; decoder-only models (GPT) suit generation and have proven surprisingly capable at everything; encoder-decoder models (T5, BART, Whisper) suit transformation tasks where the input and output are genuinely different sequences."],
 k:["Encoder builds a representation; decoder generates against it.",
    "Cross-attention is the link between the two stacks.",
    "Decoder-only architectures now dominate general-purpose LLMs.",
    "Encoder-decoder remains strong for translation, summarisation and speech."],
 r:["Transformer","Sequence Model","BERT","Attention Mechanism"]},

{t:"Embedding", d:"A dense vector representation in which semantically similar items sit close together.", l:"core", g:["representation","fundamentals"],
 b:["An embedding maps a discrete thing — a word, a product, a user, an image — to a vector of a few hundred to a few thousand floats. The geometry carries meaning: cosine similarity between embeddings approximates semantic similarity, and directions in the space can encode relationships.",
    "Embeddings are learned, either as a by-product of a larger task or by a dedicated model. They are what makes semantic search, retrieval-augmented generation and recommendation possible, because they turn similarity into arithmetic."],
 k:["Dense vectors where distance approximates semantic similarity.",
    "Cosine similarity is the usual metric for text embeddings.",
    "Typical dimensions: 384, 768, 1536, 3072.",
    "The foundation of vector search, RAG and modern recommendation."],
 x:{lang:"python", code:'from sentence_transformers import SentenceTransformer\n\nmodel = SentenceTransformer("all-MiniLM-L6-v2")\nvecs  = model.encode(["a cat on a mat", "a feline on a rug"])\n\nimport numpy as np\ncos = np.dot(vecs[0], vecs[1]) / (np.linalg.norm(vecs[0]) * np.linalg.norm(vecs[1]))\nprint(round(float(cos), 3))    # ~0.8 — near-identical meaning'},
 dg:"embedding",
 r:["Vector Database","Semantic Search","Cosine Similarity","Word2Vec","Tokenisation"]},

{t:"Transfer Learning", d:"Reusing a model trained on one task as the starting point for another, rather than training from scratch.", l:"core", g:["training","technique"],
 b:["A network trained on millions of images has already learned edges, textures and shapes — features useful for almost any vision task. Transfer learning keeps those learned layers, replaces the task-specific head, and trains on a much smaller dataset.",
    "Feature extraction freezes the pretrained layers and trains only the head, which suits very small datasets. Fine-tuning unfreezes some or all layers at a low learning rate, which performs better when you have more data. It is the reason useful models can be built from a few thousand examples instead of a few million."],
 k:["Reuse learned representations; replace only the task-specific head.",
    "Freeze the backbone for tiny datasets; fine-tune when you have more.",
    "Use a much lower learning rate when unfreezing pretrained layers.",
    "Works best when source and target domains are related."],
 dg:"finetune",
 r:["Fine-Tuning","Pre-training","ResNet","BERT","Foundation Model"]},

{t:"Pre-training", d:"The initial, expensive training phase on a large general corpus that produces a foundation model.", l:"core", g:["training","llm"],
 b:["Pre-training uses self-supervision — predicting masked or next tokens — so no human labels are needed and any amount of text or images can be consumed. It is where the vast majority of the compute budget goes: months of thousands of GPUs, costing millions of dollars.",
    "What results is general capability, not task-specific skill. Alignment (instruction tuning, RLHF) and adaptation (fine-tuning, prompting) come afterwards, and cost orders of magnitude less."],
 k:["Self-supervised, so it scales with raw data rather than labels.",
    "Consumes almost all of the total compute budget.",
    "Produces general capability, not task-specific behaviour.",
    "Done once by a few labs; everyone else adapts the result."],
 dg:"finetune",
 r:["Transfer Learning","Fine-Tuning","Self-Supervised Learning","Foundation Model","Large Language Model"]},

{t:"Autoencoder", d:"A network trained to reconstruct its own input through a narrow bottleneck, learning a compressed representation on the way.", l:"intermediate", g:["architecture","unsupervised"],
 b:["An encoder compresses the input to a low-dimensional latent code; a decoder reconstructs the input from it. Because the bottleneck cannot pass everything through, the network must learn what matters. Training needs no labels — the input is the target.",
    "Uses include non-linear dimensionality reduction, denoising (train on corrupted inputs, target clean ones), and anomaly detection (high reconstruction error means unfamiliar input). Variational autoencoders extend the idea to generation."],
 k:["Encoder → bottleneck → decoder, trained to reconstruct the input.",
    "Self-supervised: the input is its own label.",
    "Non-linear alternative to PCA for dimensionality reduction.",
    "Reconstruction error is a practical anomaly signal."],
 r:["Variational Autoencoder","Dimensionality Reduction","Anomaly Detection","Embedding"]},

{t:"Variational Autoencoder", a:"VAE", d:"An autoencoder that learns a probability distribution over the latent space, making it a generative model.", l:"advanced", g:["architecture","generative"],
 b:["Instead of encoding to a point, a VAE encodes to a mean and variance and samples from that distribution. A KL divergence term pushes the learned distributions toward a standard normal, which makes the latent space continuous and smooth — so sampling a random point and decoding it produces a plausible new example.",
    "VAE outputs tend to be blurrier than GAN outputs because of the reconstruction loss, but training is far more stable. VAEs also serve as the latent-space compressor inside Stable Diffusion."],
 k:["Encodes to a distribution, not a point — enabling sampling.",
    "KL term regularises the latent space toward a standard normal.",
    "Stable training, but blurrier samples than GANs.",
    "Used as the image compressor inside latent diffusion models."],
 r:["Autoencoder","Generative Adversarial Network","Diffusion Model","KL Divergence"]},

{t:"Generative Adversarial Network", a:"GAN", d:"Two networks trained against each other: a generator producing fakes and a discriminator trying to detect them.", l:"advanced", g:["architecture","generative"],
 b:["The generator maps noise to samples; the discriminator classifies samples as real or generated. Each improves by defeating the other, and at equilibrium the generator's output is indistinguishable from real data. StyleGAN produced the photorealistic faces that made this famous.",
    "Training is notoriously unstable. Mode collapse — the generator producing only a few outputs that reliably fool the discriminator — is the classic failure. Diffusion models have largely replaced GANs for image generation because they train reliably, though GANs remain faster at inference."],
 k:["Adversarial minimax game between generator and discriminator.",
    "Mode collapse and unstable training are the persistent problems.",
    "Single-pass generation makes inference much faster than diffusion.",
    "Largely displaced by diffusion models for high-quality image synthesis."],
 r:["Diffusion Model","Variational Autoencoder","Generative AI"]},

{t:"Diffusion Model", d:"A generative model that learns to reverse a gradual noising process, turning random noise into a coherent image.", l:"intermediate", g:["architecture","generative"],
 b:["Training takes real images, adds Gaussian noise in many small steps until nothing remains, and teaches a network to predict the noise added at each step. Generation runs the process backwards: start from pure noise and denoise repeatedly until an image emerges.",
    "Latent diffusion — used by Stable Diffusion — runs this in a compressed latent space rather than pixel space, cutting compute by an order of magnitude. Text conditioning via cross-attention is what makes prompts work. The trade-off versus GANs is quality and stability against the cost of many sequential denoising steps."],
 k:["Learns to predict and remove noise, step by step.",
    "Far more stable to train than GANs, with better diversity.",
    "Latent diffusion works in a compressed space to save compute.",
    "Requires 20–50 sequential steps, so inference is slower than a GAN."],
 r:["Generative Adversarial Network","Variational Autoencoder","Generative AI","Stable Diffusion"]},

{t:"Tensor", d:"A multi-dimensional array — the fundamental data structure of every deep learning framework.", l:"core", g:["fundamentals","data"],
 b:["A scalar is a 0-D tensor, a vector 1-D, a matrix 2-D, and anything beyond is simply n-D. A batch of RGB images is typically a 4-D tensor of shape (batch, channels, height, width); a batch of token sequences is (batch, sequence, embedding).",
    "Tensors carry a shape, a dtype and a device. The overwhelming majority of deep learning bugs are shape mismatches, and printing `.shape` at each step is the single most effective debugging habit in the field."],
 k:["An n-dimensional array with a shape, a dtype and a device.",
    "Images: (batch, channels, height, width). Text: (batch, seq, dim).",
    "Shape mismatches are the most common error you will hit.",
    "Broadcasting silently aligns shapes — convenient and occasionally wrong."],
 dg:"vector-matrix",
 r:["PyTorch","NumPy","Matrix"]},

{t:"Automatic Differentiation", a:"Autograd", d:"Computing exact derivatives of arbitrary code by tracking operations and applying the chain rule mechanically.", l:"intermediate", g:["fundamentals","training"],
 b:["Autodiff is neither symbolic differentiation (which produces huge expressions) nor numerical differentiation (which is imprecise and slow). It records the operations performed and applies the chain rule to that record, giving machine-precision derivatives at roughly the cost of the original computation.",
    "Reverse mode — backpropagation — computes all input gradients in one backward pass, which is exactly right when there are millions of parameters and one scalar loss. This is what PyTorch's `.backward()` does."],
 k:["Exact derivatives, not finite-difference approximations.",
    "Reverse mode is efficient for many inputs and one output.",
    "Builds a computational graph of every operation performed.",
    "`.backward()` in PyTorch; `grad()` in JAX."],
 r:["Backpropagation","Computational Graph","PyTorch","JAX"]},

{t:"Computational Graph", d:"A directed graph of the operations performed on tensors, used to compute gradients.", l:"intermediate", g:["fundamentals","training"],
 b:["Every operation adds a node recording its inputs and how to compute its local gradient. The backward pass walks this graph in reverse, multiplying local gradients together. PyTorch builds the graph dynamically as code executes (define-by-run); TensorFlow 1 built it statically before running.",
    "Dynamic graphs won because they make debugging normal — you can print intermediate values and use a debugger. Static graphs allow more optimisation, which is why `torch.compile` and XLA reintroduce a compilation step for production."],
 k:["Nodes are operations; edges are tensors flowing between them.",
    "Dynamic (PyTorch) graphs are easy to debug; static graphs optimise better.",
    "`torch.compile` traces and optimises without losing eager debugging.",
    "Detaching a tensor cuts the graph and stops gradients flowing through it."],
 r:["Automatic Differentiation","Backpropagation","PyTorch","TensorFlow"]},

{t:"Mixed Precision Training", d:"Using 16-bit floating point for most operations and 32-bit where precision matters, to train faster with less memory.", l:"advanced", g:["training","performance"],
 b:["Modern GPUs have tensor cores that run 16-bit matrix multiplies several times faster than 32-bit. Mixed precision keeps activations and gradients in fp16 or bf16 while maintaining a master copy of weights in fp32, roughly halving memory and often doubling throughput.",
    "fp16 has limited range, so gradients can underflow to zero — loss scaling multiplies the loss before backward to keep them representable. bf16 has the same exponent range as fp32 and needs no loss scaling, which is why it is preferred on hardware that supports it."],
 k:["Roughly halves memory and can nearly double throughput.",
    "bf16 avoids the loss-scaling complexity that fp16 requires.",
    "Master weights stay in fp32 to accumulate small updates correctly.",
    "`torch.autocast` plus `GradScaler` is the standard PyTorch recipe."],
 r:["Quantisation","GPU","Batch Size","Distributed Training"]},

{t:"Distributed Training", d:"Splitting training across multiple GPUs or machines to handle models and datasets that one device cannot.", l:"advanced", g:["training","infrastructure"],
 b:["Data parallelism replicates the model on every GPU, gives each a different batch slice, and averages gradients via all-reduce. It is the simple case and scales well until the model no longer fits on one device.",
    "Beyond that: tensor parallelism splits individual layers across GPUs, pipeline parallelism assigns different layers to different GPUs, and ZeRO/FSDP shards optimiser state, gradients and parameters. Large model training combines all of them, and interconnect bandwidth becomes the binding constraint."],
 k:["Data parallel is the default and scales until the model stops fitting.",
    "Tensor, pipeline and sharded (FSDP/ZeRO) parallelism handle bigger models.",
    "All-reduce communication cost often dominates at scale.",
    "Effective batch size = per-device batch × devices × accumulation steps."],
 r:["Data Parallelism","GPU","Mixed Precision Training","Large Language Model"]},

{t:"Data Parallelism", d:"Replicating the model across devices, giving each a different slice of the batch, and averaging the gradients.", l:"advanced", g:["training","infrastructure"],
 b:["Each GPU computes a forward and backward pass on its own portion of the batch. An all-reduce operation then averages gradients across devices so every replica applies the identical update and stays in sync.",
    "It is the easiest parallelism to adopt — `DistributedDataParallel` in PyTorch is a one-line wrapper — and the most efficient while the model fits in one device's memory. FSDP extends it by sharding parameters and optimiser state across devices."],
 k:["Same model everywhere, different data on each device.",
    "All-reduce keeps gradients — and therefore replicas — identical.",
    "Requires the whole model to fit on one device.",
    "FSDP/ZeRO shards state to lift that restriction."],
 r:["Distributed Training","GPU","Batch Size"]},

{t:"Knowledge Distillation", d:"Training a small student model to imitate a large teacher model's outputs.", l:"advanced", g:["technique","compression"],
 b:["The student is trained on the teacher's full probability distribution rather than hard labels. Those soft targets carry more information — that a particular image is 70% cat, 20% dog, 10% fox tells the student about similarity structure that a one-hot label does not.",
    "DistilBERT retained around 97% of BERT's performance at 60% of the size. Distillation is now routine for making large models deployable, and is increasingly used to transfer capability from frontier models to small open ones."],
 k:["Soft targets carry more signal than hard labels.",
    "Temperature softens the teacher's distribution to expose more structure.",
    "Typically 40–60% smaller with a small accuracy loss.",
    "Often combined with quantisation and pruning for deployment."],
 r:["Quantisation","Model Compression","Inference","Transfer Learning"]},

{t:"Graph Neural Network", a:"GNN", d:"A network that operates on graph-structured data by passing messages between connected nodes.", l:"advanced", g:["architecture","graph"],
 b:["Each node aggregates representations from its neighbours, combines them with its own, and updates. Stacking L such layers lets information travel L hops. Because aggregation must be permutation-invariant, it uses sum, mean or max rather than anything order-dependent.",
    "GNNs power molecular property prediction, fraud detection over transaction graphs, recommendation over interaction graphs and traffic prediction over road networks. Over-smoothing — where deep stacks make all node representations converge — limits practical depth to a few layers."],
 k:["Message passing: aggregate from neighbours, update, repeat.",
    "L layers means an L-hop receptive field.",
    "Aggregation must be permutation-invariant (sum, mean, max).",
    "Over-smoothing keeps most GNNs shallow — two to four layers."],
 r:["Graph","Neural Network","Graph Database","Embedding"]},

{t:"Contrastive Learning", d:"Learning representations by pulling similar pairs together and pushing dissimilar pairs apart in embedding space.", l:"advanced", g:["technique","representation"],
 b:["Given an anchor, a positive (an augmented view of the same item) and negatives (other items), the loss minimises distance to the positive and maximises distance to negatives. No labels are needed — the positive pair is manufactured by augmentation.",
    "SimCLR and MoCo established this for images; CLIP applied it across modalities, treating matched image-caption pairs as positives, which is what enables text-to-image search and conditioning. Sentence embedding models are trained the same way."],
 k:["Positives from augmentation or natural pairing; negatives from the batch.",
    "Requires many negatives — large batches or a memory queue.",
    "CLIP aligns image and text into one shared embedding space.",
    "The standard recipe for training sentence and image embedding models."],
 r:["Embedding","Self-Supervised Learning","CLIP","Cosine Similarity"]},

{t:"Catastrophic Forgetting", d:"When a network trained on a new task loses its ability to perform an earlier one.", l:"advanced", g:["failure-mode","training"],
 b:["Neural networks store knowledge in shared weights. Training on new data moves those weights toward the new objective with no regard for the old one, so previously learned capability can vanish entirely. Fine-tune a general model too aggressively on a narrow dataset and its general ability degrades noticeably.",
    "Mitigations include rehearsal (mixing in old data), regularisation toward the original weights (EWC), lower learning rates, and adapter methods such as LoRA that leave the base weights untouched."],
 k:["New learning overwrites the shared weights that held old knowledge.",
    "Aggressive fine-tuning visibly degrades general capability.",
    "Rehearsal, EWC and low learning rates reduce it.",
    "LoRA sidesteps it by freezing the base model entirely."],
 r:["Fine-Tuning","LoRA","Transfer Learning"]},

{t:"PyTorch", d:"The dominant deep learning framework, built around dynamic computational graphs and Python-native code.", l:"core", g:["framework","tooling"],
 b:["PyTorch's define-by-run model means the graph is built as your Python executes, so control flow is ordinary Python, `print` works, and a debugger stops where you expect. That ergonomic advantage won it the research community, and industry followed.",
    "The ecosystem is enormous: Hugging Face Transformers, Lightning, torchvision, and near-universal support for pretrained weights. `torch.compile` adds graph capture and kernel fusion for production speed without giving up eager debugging."],
 k:["Dynamic graphs make debugging feel like ordinary Python.",
    "`torch.compile` gives compiled performance while keeping eager development.",
    "Effectively the default in research, and increasingly in production.",
    "Hugging Face and Lightning sit on top of it."],
 x:{lang:"python", code:'import torch, torch.nn as nn\n\nclass Net(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.body = nn.Sequential(\n            nn.Linear(784, 256), nn.ReLU(), nn.Dropout(0.2),\n            nn.Linear(256, 10))\n\n    def forward(self, x):\n        return self.body(x)\n\nmodel = Net().to("cuda")\nmodel = torch.compile(model)'},
 r:["TensorFlow","JAX","Tensor","Automatic Differentiation"]},

{t:"TensorFlow", d:"Google's deep learning framework, historically graph-first and strong on production deployment.", l:"intermediate", g:["framework","tooling"],
 b:["TensorFlow 1 required you to define a static graph before running anything, which was efficient and painful. TensorFlow 2 adopted eager execution by default with Keras as the high-level API, closing most of the usability gap.",
    "Its enduring strengths are deployment: TensorFlow Serving, TensorFlow Lite for mobile and embedded, TensorFlow.js for browsers, and tight TPU integration. Research has largely moved to PyTorch, but plenty of production systems run TensorFlow."],
 k:["TF2 is eager by default; `tf.function` opts back into graph mode.",
    "Keras is the standard high-level API.",
    "TF Lite and TF.js cover mobile, embedded and browser deployment.",
    "First-class TPU support."],
 r:["PyTorch","Keras","JAX","Model Serving"]},

{t:"Keras", d:"A high-level neural network API that makes standard architectures a few lines of code.", l:"core", g:["framework","tooling"],
 b:["Keras hides boilerplate behind a clean declarative API: stack layers, call `compile`, call `fit`. It is the fastest way to get a working model, and its readability makes it the usual teaching choice.",
    "Keras 3 is multi-backend, running on TensorFlow, PyTorch or JAX, which decouples the API from any one framework. The trade is that unusual training loops require dropping to the underlying framework."],
 k:["Declarative layer stacking with `compile` and `fit`.",
    "Keras 3 runs on TensorFlow, PyTorch or JAX.",
    "Sequential, Functional and subclassing APIs for increasing flexibility.",
    "Custom training loops mean stepping outside the abstraction."],
 r:["TensorFlow","PyTorch","Neural Network"]},

{t:"JAX", d:"Google's numerical computing library combining NumPy syntax with automatic differentiation, JIT compilation and easy parallelism.", l:"advanced", g:["framework","tooling"],
 b:["JAX exposes composable function transformations: `grad` for derivatives, `jit` for XLA compilation, `vmap` for automatic vectorisation and `pmap` for device parallelism. They compose freely, so `jit(grad(vmap(f)))` is ordinary usage.",
    "It requires functional purity — no side effects, explicit random keys, immutable arrays — which is a genuine adjustment. In exchange it delivers excellent TPU performance and is favoured for large-scale research; Flax and Haiku provide the neural network layers on top."],
 k:["`grad`, `jit`, `vmap` and `pmap` compose arbitrarily.",
    "Requires pure functions and explicit PRNG key handling.",
    "Compiles through XLA — outstanding on TPUs.",
    "Flax and Haiku add the neural network abstractions."],
 r:["PyTorch","TensorFlow","Automatic Differentiation","NumPy"]},

{t:"NumPy", d:"The foundational Python library for numerical arrays, and the API every other numerical library imitates.", l:"core", g:["library","tooling"],
 b:["NumPy provides the n-dimensional array plus vectorised operations implemented in C. Operating on whole arrays rather than looping in Python is typically 10 to 100 times faster, and the mental shift toward vectorised thinking is a prerequisite for everything downstream.",
    "Broadcasting rules let arrays of different shapes combine without explicit copying. pandas, scikit-learn, PyTorch and JAX all follow NumPy's conventions, so the skill transfers directly."],
 k:["Vectorise: array operations in C beat Python loops by one to two orders of magnitude.",
    "Broadcasting aligns shapes automatically — powerful and occasionally surprising.",
    "Slices are views, not copies — mutating a slice mutates the original.",
    "The API convention every other numerical library follows."],
 x:{lang:"python", code:'import numpy as np\n\nX = np.random.randn(1000, 64)\nX_norm = (X - X.mean(axis=0)) / X.std(axis=0)     # broadcasting\n\nsims = X_norm @ X_norm.T                          # 1000x1000 similarity\nprint(sims.shape, X_norm.dtype)'},
 r:["Tensor","PyTorch","Linear Algebra"]},

{t:"Vision Transformer", a:"ViT", d:"A transformer applied to images by splitting them into patches and treating each patch as a token.", l:"advanced", g:["architecture","vision"],
 b:["A ViT cuts an image into fixed patches (16×16 pixels is standard), flattens and linearly projects each into an embedding, adds positional encodings, and feeds the sequence to a standard transformer encoder. No convolutions are involved.",
    "Because it lacks the CNN's built-in assumption of locality, it needs far more data to reach the same accuracy — but given enough data it surpasses CNNs. Hybrid and hierarchical variants such as Swin restore some spatial inductive bias and are more efficient at high resolutions."],
 k:["Images become sequences of patch embeddings.",
    "Weaker inductive bias than a CNN, so it needs much more data.",
    "Global receptive field from the very first layer.",
    "Swin and hybrid variants reintroduce hierarchy for efficiency."],
 r:["Transformer","Convolutional Neural Network","Inductive Bias","Self-Attention"]},

{t:"Word2Vec", d:"An early technique that learned word embeddings by predicting words from their neighbours.", l:"intermediate", g:["representation","nlp"],
 b:["Word2Vec trains a shallow network either to predict a word from its context (CBOW) or the context from the word (skip-gram). The learned weight vectors turned out to encode meaning geometrically — the famous king − man + woman ≈ queen result.",
    "Its limitation is one fixed vector per word, so `bank` has a single representation regardless of context. Contextual embeddings from BERT and modern transformers resolved that, but Word2Vec remains the clearest demonstration of why embeddings work."],
 k:["Skip-gram predicts context from a word; CBOW does the reverse.",
    "Vector arithmetic captures analogies and relationships.",
    "Static: one vector per word, regardless of sense or context.",
    "Negative sampling made training on huge corpora tractable."],
 dg:"embedding",
 r:["Embedding","GloVe","BERT","Natural Language Processing"]}

]);
