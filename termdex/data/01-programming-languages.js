TD.add("programming-languages", [

{t:"Python", d:"A high-level, dynamically typed, interpreted language designed for readability — and the default language of data science, machine learning and scripting.", l:"core", g:["language","interpreted","dynamic"],
 b:["Python trades raw execution speed for developer speed. Indentation is part of the syntax, there is no compilation step to wait for, and the standard library covers an enormous amount of ground. That combination made it the glue language of the industry: it is rarely the fastest part of a system, but it is usually the part a human wrote.",
    "In AI and ML it is effectively the only language that matters, because every serious numerical library — NumPy, pandas, PyTorch, TensorFlow, scikit-learn — is a thin Python surface over compiled C, C++ or CUDA. You write Python; the heavy loops run in machine code. The Global Interpreter Lock still limits true CPU parallelism inside one process, which is why heavy Python work is pushed into those native libraries or into multiple processes."],
 k:["Dynamically typed with optional type hints that tools check but the runtime ignores.",
    "The GIL means threads help with I/O, not with CPU-bound maths — use multiprocessing or native libraries.",
    "**pip** plus a virtual environment is the standard packaging story; **uv**, **poetry** and **conda** are common alternatives.",
    "Slow at loops, fast at everything that delegates to C."],
 x:{lang:"python", code:'from collections import Counter\n\ndef top_words(text, n=3):\n    words = [w.lower() for w in text.split() if w.isalpha()]\n    return Counter(words).most_common(n)\n\nprint(top_words("the cat the mat the end"))\n# [("the", 3), ("cat", 1), ("mat", 1)]'},
 r:["JavaScript","Interpreted Language","Dynamic Typing","NumPy","PyTorch"]},

{t:"JavaScript", d:"The only programming language browsers run natively, and — through Node.js — a general-purpose server language as well.", l:"core", g:["language","web","dynamic"],
 b:["JavaScript was written in ten days in 1995 and has been carrying the web ever since. It is dynamically typed, prototype-based rather than class-based underneath, and single-threaded with an event loop that makes asynchronous I/O feel natural. Its odd corners — type coercion, `this` binding, `null` versus `undefined` — are historical accidents that the language can never remove without breaking the internet.",
    "The modern language (ES2015 onward) is genuinely pleasant: modules, arrow functions, destructuring, classes, async/await, optional chaining. Most teams write TypeScript and compile down to it, which keeps the reach and removes most of the foot-guns."],
 k:["Single-threaded plus an event loop — concurrency without shared-memory threads.",
    "`==` coerces types, `===` does not. Use `===`.",
    "Runs in browsers, on servers (Node, Deno, Bun), in edge workers and in native app shells.",
    "Everything asynchronous ultimately resolves through Promises."],
 x:{lang:"javascript", code:'const users = [\n  { name: "Ada", active: true },\n  { name: "Linus", active: false },\n];\n\nconst active = users\n  .filter((u) => u.active)\n  .map((u) => u.name.toUpperCase());\n\nconsole.log(active); // ["ADA"]'},
 dg:"event-loop",
 r:["TypeScript","Node.js","Event Loop","DOM","Promise"]},

{t:"TypeScript", d:"JavaScript with a static type system bolted on at compile time; the types vanish before the code runs.", l:"core", g:["language","types","web"],
 b:["TypeScript is a superset of JavaScript: every valid `.js` file is valid TypeScript. You add annotations, the compiler checks them, and then it erases them and emits plain JavaScript. Nothing about the runtime changes — which is both its greatest strength (adoption is incremental) and the source of its main gotcha (types do not exist at runtime, so you cannot trust them for data coming off the network).",
    "Its type system is unusually expressive: union types, generics, conditional and mapped types, and structural rather than nominal typing. In practice it catches the entire class of bugs where a field was renamed, a return shape changed, or a value could be `undefined` — the bugs that make large JavaScript codebases miserable."],
 k:["Types are erased at compile time — validate external data at the boundary with a runtime schema library.",
    "Structural typing: if the shape matches, it fits. No explicit `implements` needed.",
    "`strict: true` is where most of the value lives; without it null checking is off.",
    "`any` opts out of the whole system; `unknown` is the safe version."],
 x:{lang:"typescript", code:'type User = {\n  id: number;\n  name: string;\n  email?: string;   // optional\n};\n\nfunction greet(u: User): string {\n  return `Hello ${u.name}`;\n}\n\ngreet({ id: 1, name: "Ada" });    // ok\n// greet({ id: 2 });              // error: name missing'},
 r:["JavaScript","Static Typing","Type Inference","Transpiler"]},

{t:"Java", d:"A statically typed, object-oriented, compiled-to-bytecode language that runs on the JVM — the backbone of a huge amount of enterprise software.", l:"core", g:["language","jvm","static"],
 b:["Java compiles to bytecode rather than machine code, and that bytecode runs on any machine with a JVM. That is the origin of write once, run anywhere. It is verbose by design: explicit types, explicit exceptions, explicit everything. The payoff is that large teams can work on large codebases without stepping on each other, and the tooling (IDEs, profilers, debuggers) is the best in the industry.",
    "Java has moved faster since version 8: lambdas, streams, records, sealed classes, pattern matching, and virtual threads in 21. It remains the default for banking, telecom, big-data infrastructure (Hadoop, Kafka, Spark, Elasticsearch are all JVM projects) and Android's original app language."],
 k:["Bytecode plus a JIT compiler means it starts slower than C but can run close to it once warm.",
    "Automatic garbage collection — you never free memory manually.",
    "Checked exceptions force callers to acknowledge failure modes; opinions differ on whether this helps.",
    "Virtual threads (Java 21) make one-thread-per-request practical again."],
 x:{lang:"java", code:'record User(int id, String name) {}\n\nList<String> names = users.stream()\n    .filter(u -> u.id() > 100)\n    .map(User::name)\n    .sorted()\n    .toList();'},
 r:["Java Virtual Machine","Bytecode","Kotlin","Static Typing","Garbage Collection"]},

{t:"C", d:"A small, fast, low-level compiled language that maps almost directly onto how the machine actually works — and the language every operating system is written in.", l:"core", g:["language","systems","compiled"],
 b:["C gives you manual memory management, raw pointers and almost no runtime. There is no garbage collector, no bounds checking, no exceptions. What you write is very close to what the CPU executes, which is why Linux, Windows kernels, database engines, embedded firmware and the interpreters of most other languages are written in it.",
    "The cost is that entire vulnerability classes — buffer overflows, use-after-free, double free — are yours to prevent. Decades of security incidents trace back to a missing bounds check in C. Modern practice leans on sanitisers, static analysis and, increasingly, rewriting the risky parts in Rust."],
 k:["Manual `malloc` / `free`: forget the free and you leak, free twice and you corrupt the heap.",
    "Pointers are just addresses — powerful, and the source of most C bugs.",
    "Undefined behaviour is a real category: the compiler may do anything if you break the rules.",
    "Still the lingua franca of ABIs — nearly every language can call a C function."],
 x:{lang:"c", code:'#include <stdio.h>\n#include <stdlib.h>\n\nint main(void) {\n    int *nums = malloc(3 * sizeof(int));\n    if (!nums) return 1;\n    for (int i = 0; i < 3; i++) nums[i] = i * i;\n    printf("%d\\n", nums[2]);   /* 4 */\n    free(nums);\n    return 0;\n}'},
 r:["C++","Rust","Pointer","Memory Management","Compiled Language"]},

{t:"C++", d:"C with objects, templates, and a very large toolbox — used where you need both control over the hardware and abstraction over the problem.", l:"core", g:["language","systems","compiled"],
 b:["C++ keeps C's performance model and adds classes, templates, operator overloading, RAII and a huge standard library. Its guiding principle is zero-cost abstraction: features you do not use cost nothing, and features you do use are about as fast as the equivalent hand-written C. That is why it runs game engines, trading systems, browsers, CAD tools and the compute kernels underneath most ML frameworks.",
    "It is also one of the largest languages in existence, and different teams use wildly different subsets. Modern C++ (11 onwards) leans on smart pointers, `auto`, move semantics and range-based loops to make manual memory management far less error-prone than the C++ of the 1990s."],
 k:["RAII: resources are tied to object lifetime, so destructors clean up automatically.",
    "`std::unique_ptr` and `std::shared_ptr` replace most raw `new` / `delete`.",
    "Templates give compile-time polymorphism with no runtime dispatch cost.",
    "Compile times and error messages are the perennial complaints."],
 x:{lang:"cpp", code:'#include <vector>\n#include <algorithm>\n#include <memory>\n\nauto scores = std::vector<int>{7, 2, 9, 4};\nstd::sort(scores.begin(), scores.end());\n\nauto buf = std::make_unique<int[]>(1024);  // freed automatically'},
 r:["C","Rust","Object-Oriented Programming","Memory Management"]},

{t:"C#", a:"C Sharp", d:"Microsoft's statically typed, object-oriented language for the .NET platform — Java-like in shape, but usually a few years ahead in language features.", l:"core", g:["language","dotnet","static"],
 b:["C# compiles to an intermediate language executed by the .NET runtime, with JIT compilation to native code. It was designed as a modern, curated language and has stayed that way: async/await originated here, along with LINQ, nullable reference types, records and pattern matching.",
    "With .NET Core and its successors the whole platform became cross-platform and open source, so C# now runs happily on Linux containers. It dominates Windows enterprise software, is the language of the Unity game engine, and is a strong general-purpose backend choice."],
 k:["`async`/`await` was introduced here and copied by nearly every other language since.",
    "LINQ gives SQL-like querying over in-memory collections and databases alike.",
    "Nullable reference types make `null` a type-system concern rather than a runtime surprise.",
    "Unity means a large share of published games contain C#."],
 x:{lang:"csharp", code:'var adults = people\n    .Where(p => p.Age >= 18)\n    .OrderBy(p => p.Name)\n    .Select(p => p.Name)\n    .ToList();\n\nawait using var conn = new SqlConnection(cs);'},
 r:[".NET","Java","Static Typing"]},

{t:"Go", a:"Golang", d:"A small, fast-compiling, statically typed language from Google built for networked services and readable concurrent code.", l:"core", g:["language","backend","concurrency"],
 b:["Go was designed by deliberately leaving things out. There is no inheritance, no exceptions, no generics until 1.18, and one canonical formatting style enforced by `gofmt`. What you get instead is a language a team can learn in a week, compilation in seconds, and a single statically linked binary that you can drop into a scratch container.",
    "Its headline feature is concurrency: goroutines are cheap green threads, and channels give you a communication primitive instead of shared mutable state. Kubernetes, Docker, Terraform, Prometheus and a large part of modern cloud infrastructure are written in Go for exactly these reasons."],
 k:["Goroutines cost a couple of kilobytes — spawning thousands is normal.",
    "Errors are values returned alongside results; `if err != nil` is the rhythm of the language.",
    "Compiles to one static binary with no runtime dependency — ideal for containers.",
    "Deliberately boring: fewer ways to do a thing means less to argue about in review."],
 x:{lang:"go", code:'func fetchAll(urls []string) []string {\n    ch := make(chan string, len(urls))\n    for _, u := range urls {\n        go func(u string) { ch <- get(u) }(u)\n    }\n    out := make([]string, 0, len(urls))\n    for range urls { out = append(out, <-ch) }\n    return out\n}'},
 r:["Rust","Concurrency","Docker","Kubernetes"]},

{t:"Rust", d:"A systems language with C-level performance and compile-time memory safety, enforced by an ownership model instead of a garbage collector.", l:"intermediate", g:["language","systems","memory-safety"],
 b:["Rust's central idea is ownership: every value has exactly one owner, and the compiler tracks how references borrow it. If a reference could outlive its data, or two writers could touch the same memory at once, the program does not compile. This eliminates use-after-free, double-free and data races at build time, without a runtime garbage collector.",
    "The tax is a steep learning curve — the borrow checker will reject code that you are certain is correct, and learning to restructure around it takes weeks. In exchange you get C++ performance with far fewer memory CVEs, which is why Linux, Windows, Android and Firefox have all started adopting it for security-sensitive components."],
 k:["Ownership and borrowing are checked at compile time — zero runtime cost.",
    "No null: absence is `Option<T>`; failure is `Result<T, E>`.",
    "`cargo` is one of the best build-and-package tools in any language.",
    "Fearless concurrency: data races are a compile error, not a production incident."],
 x:{lang:"rust", code:'fn largest(items: &[i32]) -> Option<&i32> {\n    items.iter().max()\n}\n\nfn main() {\n    let nums = vec![3, 9, 2];\n    match largest(&nums) {\n        Some(n) => println!("max {n}"),\n        None => println!("empty"),\n    }\n}'},
 r:["C++","Memory Safety","Concurrency"]},

{t:"Ruby", d:"A dynamic, object-oriented language optimised for programmer happiness, best known as the language of Rails.", l:"intermediate", g:["language","dynamic","web"],
 b:["In Ruby everything is an object, including numbers and `nil`, and blocks make it easy to write code that reads like prose. Its metaprogramming is unusually powerful — classes can be reopened and methods defined at runtime — which is what lets frameworks like Rails feel almost like a custom language for web apps.",
    "That same flexibility makes large Ruby codebases harder to reason about statically, and the runtime is not fast in benchmark terms. It remains an excellent fit for products where iteration speed matters more than microseconds: SaaS backends, internal tools, deployment scripts."],
 k:["Everything is an object; even `nil` has methods.",
    "Blocks, procs and lambdas make custom control flow easy to express.",
    "Rails popularised convention over configuration and the MVC web app.",
    "Metaprogramming is a superpower and a maintenance hazard in equal measure."],
 x:{lang:"ruby", code:'users = [\n  { name: "Ada", age: 36 },\n  { name: "Grace", age: 45 }\n]\n\nnames = users.select { |u| u[:age] > 40 }\n              .map { |u| u[:name] }\n\nputs names.join(", ")'},
 r:["Python","Dynamic Typing","MVC"]},

{t:"PHP", d:"A server-side scripting language built for the web, still running a very large share of the world's websites.", l:"intermediate", g:["language","web","backend"],
 b:["PHP was designed to be embedded directly in HTML and executed per request, which made it trivially easy to learn and deploy in the shared-hosting era. Its reputation was earned by an inconsistent standard library and a permissive type system in the 4.x and 5.x days.",
    "Modern PHP (8.x) is a different animal: JIT compilation, typed properties, enums, attributes, named arguments, and a mature ecosystem in Composer, Laravel and Symfony. WordPress alone means PHP will be maintained for decades regardless of fashion."],
 k:["Shared-nothing request model: each request starts fresh, which sidesteps a whole class of state bugs.",
    "Composer is the package manager; PSR standards define shared conventions.",
    "Laravel and Symfony are the two frameworks worth knowing.",
    "PHP 8 added a JIT, union types and enums — treat pre-7 advice as obsolete."],
 r:["JavaScript"]},

{t:"Swift", d:"Apple's modern, statically typed language for iOS, macOS and the rest of its platforms.", l:"intermediate", g:["language","mobile","apple"],
 b:["Swift replaced Objective-C as Apple's flagship language in 2014. It is compiled, statically typed, memory-safe by default through automatic reference counting, and designed so that the common case is also the safe case: optionals make absence explicit, value types are the default, and mutation is opt-in with `var`.",
    "SwiftUI made declarative UI the standard way to build Apple apps, and Swift Concurrency (async/await plus actors) gave the language a structured answer to callback soup. Server-side Swift exists but the ecosystem's centre of gravity remains Apple platforms."],
 k:["Optionals (`String?`) make nullability part of the type, checked by the compiler.",
    "ARC handles memory automatically; retain cycles are the one thing you must still think about.",
    "Structs and value semantics are preferred over classes for most model types.",
    "Actors give compiler-checked isolation for shared mutable state."],
 x:{lang:"swift", code:'struct User { let name: String; var email: String? }\n\nfunc greet(_ u: User) -> String {\n    if let email = u.email {\n        return "\\(u.name) <\\(email)>"\n    }\n    return u.name\n}'},
 r:["Objective-C","Kotlin","Static Typing","Null Safety"]},

{t:"Kotlin", d:"A concise, null-safe JVM language that is now Google's preferred language for Android.", l:"intermediate", g:["language","jvm","mobile"],
 b:["Kotlin interoperates with Java completely — you can call either from the other in the same project — while removing most of Java's ceremony. Nullability is encoded in the type system, data classes generate boilerplate for you, and extension functions let you add methods to types you do not own.",
    "Coroutines provide lightweight structured concurrency that reads like sequential code. Beyond Android, Kotlin runs on the server (Ktor, Spring), compiles to JavaScript, and targets native binaries through Kotlin Multiplatform."],
 k:["`String` versus `String?` — the compiler will not let you dereference a nullable without handling it.",
    "Coroutines make thousands of concurrent operations cheap and cancellable.",
    "Data classes give you `equals`, `hashCode`, `toString` and `copy` for free.",
    "100% interoperable with Java, so migration can be file by file."],
 x:{lang:"kotlin", code:'data class User(val id: Int, val email: String?)\n\nfun label(u: User): String =\n    u.email?.lowercase() ?: "no email on file"'},
 r:["Java","Null Safety","Java Virtual Machine","Swift"]},

{t:"Scala", d:"A JVM language that fuses object-oriented and functional programming, widely used in big-data engineering.", l:"advanced", g:["language","jvm","functional"],
 b:["Scala gives you immutable collections, pattern matching, higher-kinded types and a powerful implicit/given system, all on the JVM with full Java interop. Apache Spark is written in it, which alone guarantees its place in data engineering.",
    "The language is expressive enough that teams can diverge dramatically in style — from lightly functional Java-with-less-noise to deeply typed effect systems. Scala 3 simplified the syntax and made the type system more coherent, but the learning curve remains real."],
 k:["Pattern matching and algebraic data types are first-class and used constantly.",
    "Immutable-by-default collections encourage a functional style.",
    "Spark's native API is Scala; PySpark is a wrapper over it.",
    "Compile times and the size of the language are the usual complaints."],
 r:["Apache Spark","Functional Programming","Java Virtual Machine"]},

{t:"R", d:"A language and environment built specifically for statistics, data analysis and visualisation.", l:"intermediate", g:["language","statistics","data"],
 b:["R was written by statisticians for statisticians. Vectors are the fundamental unit, data frames are built in, and the standard library covers distributions, hypothesis tests and models that would be a third-party dependency anywhere else. CRAN hosts tens of thousands of peer-reviewed statistical packages.",
    "The tidyverse (dplyr, ggplot2, tidyr) gave R a coherent modern grammar for data manipulation and plotting. In industry, Python has taken most general ML work, but R remains dominant in biostatistics, epidemiology, econometrics and anywhere publication-quality statistical graphics matter."],
 k:["Vectorised by default — loops are usually the wrong answer.",
    "`ggplot2` implements a grammar of graphics that is still the benchmark for plotting APIs.",
    "1-indexed, unlike almost every other language you will use.",
    "R Markdown and Quarto make reproducible reports genuinely easy."],
 x:{lang:"r", code:'library(dplyr)\n\nsummary <- sales %>%\n  filter(year == 2024) %>%\n  group_by(region) %>%\n  summarise(total = sum(revenue), .groups = "drop") %>%\n  arrange(desc(total))'},
 r:["Python","Statistics","Data Visualisation"]},

{t:"Julia", d:"A high-level language with C-like speed, aimed at scientific and numerical computing.", l:"advanced", g:["language","scientific","numerical"],
 b:["Julia was created to end the two-language problem: prototype in Python or MATLAB, then rewrite the hot path in C. It uses just-in-time compilation via LLVM and an aggressive type-inference system so that idiomatic high-level code compiles to tight machine code.",
    "Its defining feature is multiple dispatch: the method chosen depends on the types of all arguments, not just the receiver. That makes it unusually good at composing independently written numerical libraries. Adoption is strongest in scientific computing, differential equations and computational biology."],
 k:["Multiple dispatch is the core organising idea, not classes.",
    "JIT compilation means a noticeable warm-up on first call — the time-to-first-plot problem.",
    "Native support for arbitrary numeric types, units and automatic differentiation.",
    "1-indexed, with first-class multidimensional arrays."],
 r:["Python","MATLAB","JIT Compilation"]},

{t:"MATLAB", d:"A commercial numerical computing environment and matrix-first language used heavily in engineering and academia.", l:"intermediate", g:["language","numerical","engineering"],
 b:["In MATLAB the matrix is the fundamental type, and operators do linear algebra by default. Combined with Simulink for modelling dynamic systems and toolboxes for control, signal processing and communications, it remains the standard environment in mechanical, electrical, aerospace and control engineering.",
    "It is proprietary and licensed per seat, which is why teaching and research often migrate to Python with NumPy or to Octave. But for hardware-in-the-loop simulation and code generation onto embedded targets, the tooling has no direct free equivalent."],
 k:["Everything is a matrix; scalars are 1×1 matrices.",
    "Simulink gives block-diagram modelling for control and signal systems.",
    "Licensed commercial software — a genuine adoption constraint.",
    "Can generate C code for embedded deployment."],
 r:["Julia","NumPy","Linear Algebra"]},

{t:"Perl", d:"A pragmatic text-processing language famous for regular expressions and for holding the early web together.", l:"advanced", g:["language","scripting","text"],
 b:["Perl's motto is that there is more than one way to do it, and its regular-expression engine was so influential that PCRE — Perl Compatible Regular Expressions — became the standard other languages implement. For decades it was the default choice for log munging, sysadmin scripting and CGI web applications.",
    "Its flexibility also earned it a reputation for write-only code. Python largely displaced it for new work, but Perl still runs quietly inside build systems, bioinformatics pipelines and a great deal of legacy infrastructure."],
 k:["Sigils (`$`, `@`, `%`) encode whether a variable is a scalar, array or hash.",
    "PCRE syntax is the regex dialect most languages copied.",
    "CPAN was the first great language package repository.",
    "Excellent at one-liners; challenging at scale."],
 r:["Regular Expression","Bash","Python"]},

{t:"Haskell", d:"A purely functional, lazily evaluated, strongly typed language — the reference implementation of academic functional programming.", l:"advanced", g:["language","functional","types"],
 b:["Haskell functions are pure: given the same input they return the same output and touch nothing else. Side effects are represented in the type system, so a function that reads a file has a different type from one that does arithmetic. Evaluation is lazy, meaning expressions are computed only when their value is needed, which makes infinite data structures ordinary.",
    "Its type system — algebraic data types, type classes, higher-kinded types — is where many ideas now in Rust, Swift, Scala and TypeScript were first worked out. Commercial use is niche but real, especially in finance and compiler work."],
 k:["Purity plus immutability makes reasoning and parallelism easier.",
    "Laziness enables elegant code and occasionally surprising memory behaviour.",
    "Monads are just a pattern for sequencing computations with context.",
    "If it compiles, it very often works — the type checker is doing a lot."],
 x:{lang:"haskell", code:'primes :: [Int]\nprimes = sieve [2..]\n  where sieve (p:xs) = p : sieve [x | x <- xs, x `mod` p /= 0]\n\n-- take 5 primes  =>  [2,3,5,7,11]'},
 r:["Functional Programming","Immutability","Pure Function","Type Inference"]},

{t:"Elixir", d:"A functional language on the Erlang VM, built for highly concurrent, fault-tolerant, always-on systems.", l:"advanced", g:["language","functional","concurrency"],
 b:["Elixir gives Erlang's battle-tested BEAM virtual machine a friendlier, Ruby-influenced syntax. Processes are extremely lightweight and isolated, communicate only by message passing, and are organised into supervision trees where a crashing process is simply restarted by its supervisor — the let it crash philosophy.",
    "Phoenix and LiveView made it a compelling choice for real-time web applications, where a single server can hold hundreds of thousands of open connections. It is the natural fit for chat, messaging, telemetry and any system where uptime matters more than raw single-thread speed."],
 k:["Millions of lightweight processes per node, each with its own heap.",
    "Supervision trees turn failure into a routine, recoverable event.",
    "Hot code reloading allows upgrades without dropping connections.",
    "Phoenix LiveView renders interactive UI over a websocket with no client framework."],
 r:["Erlang","Concurrency","Functional Programming","WebSocket"]},

{t:"Erlang", d:"A concurrency-oriented functional language from Ericsson, designed for telecom systems that must never go down.", l:"advanced", g:["language","functional","concurrency"],
 b:["Erlang's process model, supervision strategy and hot code swapping were all built to keep telephone switches running for years without interruption. It popularised the actor model in industry: isolated processes with no shared memory, communicating solely by asynchronous messages.",
    "The BEAM virtual machine underneath it also runs Elixir and Gleam. RabbitMQ, CouchDB and large parts of WhatsApp's backend are Erlang systems."],
 k:["Actor model: no shared state, so no locks and no data races.",
    "Nine nines of availability is the famous, if often over-quoted, claim.",
    "Pattern matching everywhere, including in function heads.",
    "Preemptive scheduling means one runaway process cannot starve the others."],
 r:["Elixir","Concurrency","Message Queue"]},

{t:"Clojure", d:"A modern Lisp on the JVM built around immutable data structures and a pragmatic approach to state.", l:"advanced", g:["language","functional","jvm"],
 b:["Clojure keeps Lisp's homoiconic syntax — code is data, written as lists — while sitting on the JVM with full Java interop. Its collections are persistent and immutable: updating a map returns a new map that structurally shares most of its memory with the old one.",
    "Mutable state, when you need it, is explicit and coordinated through atoms, refs and agents. ClojureScript compiles the same language to JavaScript. It rewards teams that value simplicity and data-oriented design over ceremony."],
 k:["Immutable persistent collections make concurrency straightforward.",
    "Code as data enables macros that genuinely extend the language.",
    "The REPL-driven workflow is central, not an afterthought.",
    "Data is modelled as plain maps rather than classes."],
 r:["Lisp","Functional Programming","Immutability","Java Virtual Machine"]},

{t:"Lisp", d:"A family of languages from 1958 built on symbolic expressions, where code and data share the same structure.", l:"advanced", g:["language","functional","historic"],
 b:["Lisp introduced ideas the rest of computing spent decades catching up with: garbage collection, first-class functions, recursion, dynamic typing, the REPL and macros. Programs are written as nested lists, so a program can construct and transform other programs as ordinary data.",
    "Common Lisp, Scheme, Clojure, Racket and Emacs Lisp are the dialects still in active use. It dominated early AI research, which is why so much AI vocabulary has Lisp fingerprints on it."],
 k:["Homoiconicity: source code is a data structure the language can manipulate.",
    "Macros run at compile time and can invent new syntax.",
    "Parentheses look alien for a week and then disappear.",
    "Origin of garbage collection, closures and the read-eval-print loop."],
 r:["Clojure","Functional Programming","REPL","Recursion"]},

{t:"Dart", d:"Google's client-optimised language, best known as the language of Flutter.", l:"intermediate", g:["language","mobile","ui"],
 b:["Dart is statically typed with sound null safety, and compiles two ways: just-in-time during development for sub-second hot reload, and ahead-of-time to native machine code for release builds. That dual mode is exactly what makes Flutter's developer experience feel fast while shipping fast apps.",
    "It also compiles to JavaScript and WebAssembly for the web. Outside Flutter its adoption is modest, but inside Flutter it is the only option."],
 k:["JIT in development for hot reload, AOT in production for startup speed.",
    "Sound null safety — nullable types are checked, not merely hinted.",
    "Isolates give concurrency without shared memory.",
    "One codebase targets iOS, Android, web, desktop and embedded."],
 r:["AOT Compilation","JIT Compilation","Null Safety"]},

{t:"Lua", d:"A tiny, fast, embeddable scripting language used to add scriptability to larger programs.", l:"intermediate", g:["language","scripting","embedded"],
 b:["The entire Lua interpreter is a few hundred kilobytes, which is why it is embedded in game engines, Redis, Nginx, Neovim, and countless pieces of firmware. The language itself is small: tables are the only structured data type and serve as arrays, dictionaries, objects and namespaces at once.",
    "LuaJIT delivers performance genuinely competitive with C for numeric workloads. When you need a config or plugin language inside a C or C++ program, Lua is usually the shortest path."],
 k:["Tables are the single data structure — arrays and maps are the same thing.",
    "1-indexed, which trips up newcomers from C-family languages.",
    "Coroutines are built in for cooperative multitasking.",
    "Designed from the start to be embedded rather than to stand alone."],
 r:["Python","Redis","Scripting Language"]},

{t:"Bash", a:"Bourne Again Shell", d:"The default Unix shell and its scripting language — the glue that automates everything on a Linux box.", l:"core", g:["language","shell","devops"],
 b:["Bash is both an interactive command line and a programming language. Pipes let you compose small programs into larger ones, and redirection wires their inputs and outputs together. Nearly every CI pipeline, Dockerfile and deployment script contains Bash somewhere.",
    "It is an unforgiving language for anything complex: word splitting, unquoted variables and silent failures cause real outages. The convention `set -euo pipefail` at the top of a script turns many silent failures into loud ones. Past roughly a hundred lines, reach for Python instead."],
 k:["Always quote variable expansions: `\"$var\"`, not `$var`.",
    "`set -euo pipefail` — exit on error, on unset variables, and on failure anywhere in a pipe.",
    "Exit code 0 means success; anything else is a failure.",
    "Pipes and redirection are the core composition mechanism."],
 x:{lang:"bash", code:'#!/usr/bin/env bash\nset -euo pipefail\n\nfor f in ./logs/*.log; do\n  count=$(grep -c "ERROR" "$f" || true)\n  echo "$f: $count errors"\ndone'},
 r:["PowerShell","Linux","Shell","CI/CD"]},

{t:"PowerShell", d:"Microsoft's shell and scripting language, where the pipeline carries structured objects instead of text.", l:"intermediate", g:["language","shell","windows"],
 b:["The key difference from Unix shells is that PowerShell pipes .NET objects, not strings. `Get-Process | Where-Object CPU -gt 100` filters on a real property rather than parsing columns out of text, which removes a whole category of fragile scripting.",
    "Commands follow a strict Verb-Noun naming convention, and the full .NET class library is available when the built-in cmdlets are not enough. It is cross-platform since PowerShell Core, though its centre of gravity remains Windows administration and Azure."],
 k:["The pipeline carries objects, so no text parsing is required.",
    "Verb-Noun cmdlet naming makes commands guessable: `Get-`, `Set-`, `New-`, `Remove-`.",
    "Full access to .NET types when cmdlets fall short.",
    "Execution policy blocks unsigned scripts by default on Windows."],
 x:{lang:"powershell", code:'Get-ChildItem -Path .\\logs -Filter *.log |\n  Where-Object { $_.Length -gt 1MB } |\n  Sort-Object Length -Descending |\n  Select-Object Name, Length'},
 r:["Bash",".NET","Shell"]},

{t:"Assembly Language", d:"A human-readable form of a CPU's own instruction set, one line per machine instruction.", l:"advanced", g:["language","low-level","systems"],
 b:["Assembly is as close to the hardware as text gets: registers, memory addresses, jumps and arithmetic instructions, translated one-to-one into machine code by an assembler. Each architecture — x86-64, ARM64, RISC-V — has its own dialect, so assembly is never portable.",
    "Almost nobody writes whole programs in it now. You meet it when reading compiler output, debugging at the instruction level, writing bootloaders and interrupt handlers, or hand-tuning cryptographic and SIMD routines where the compiler leaves performance on the table."],
 k:["No portability: x86 assembly does not run on ARM.",
    "Registers are the fast storage; everything else is a memory access away.",
    "Calling conventions define who saves which register across a call.",
    "Still relevant in reverse engineering, security research and hot-loop optimisation."],
 r:["C","CPU","Compiler","Machine Code"]},

{t:"COBOL", d:"A 1959 business language still processing an enormous share of the world's banking and government transactions.", l:"advanced", g:["language","legacy","mainframe"],
 b:["COBOL was written to be readable by non-programmers, which is why its syntax looks like English sentences. It excels at exactly one thing: high-volume, fixed-format record processing with exact decimal arithmetic — the sort of work banks, insurers and tax agencies do all day.",
    "Estimates of live COBOL run into the hundreds of billions of lines. It persists not from inertia alone but because the code is correct, audited, and processing real money; rewriting it is a risk with limited upside."],
 k:["Fixed-point decimal arithmetic avoids the rounding errors of binary floating point.",
    "Divided into IDENTIFICATION, ENVIRONMENT, DATA and PROCEDURE divisions.",
    "Runs mostly on IBM mainframes under z/OS with CICS and DB2.",
    "The maintenance workforce is shrinking faster than the code base."],
 r:["Fortran","Mainframe","Legacy System"]},

{t:"Fortran", d:"The first widely used high-level language, from 1957, and still the fastest option for heavy numerical array computation.", l:"advanced", g:["language","scientific","numerical"],
 b:["Fortran — formula translation — was built for scientific computing, and it never lost that niche. Its array semantics and lack of pointer aliasing let compilers vectorise and parallelise aggressively, so well-written Fortran still beats equivalent C on many numerical benchmarks.",
    "Weather models, computational fluid dynamics, nuclear simulation and the LAPACK and BLAS libraries beneath NumPy are Fortran. Modern standards (2008, 2018) added modules, coarrays and native parallelism."],
 k:["Column-major arrays, unlike C and NumPy's row-major default.",
    "BLAS and LAPACK — the linear algebra beneath most ML stacks — are Fortran.",
    "Compilers vectorise it exceptionally well.",
    "Still actively standardised, despite being older than most programmers' parents."],
 r:["C","Linear Algebra","NumPy","High-Performance Computing"]},

{t:"Objective-C", d:"A C superset with Smalltalk-style message passing; Apple's platform language before Swift.", l:"advanced", g:["language","apple","legacy"],
 b:["Objective-C adds dynamic object messaging to C using bracket syntax: `[object doThing:arg]`. Messages are resolved at runtime, so objects can respond to messages they were not compiled to handle — powerful, dynamic, and harder to check statically.",
    "It is in maintenance mode for new work, but Apple's older frameworks are written in it and Swift interoperates with it directly, so anyone maintaining a long-lived iOS or macOS codebase will still read it."],
 k:["Dynamic message dispatch resolved at runtime rather than compile time.",
    "Manual reference counting preceded ARC; retain/release bugs were common.",
    "Fully interoperable with C and with Swift.",
    "Categories allow adding methods to existing classes."],
 r:["Swift","C"]},

{t:"Groovy", d:"A dynamic JVM language that reads like scripting Java, used heavily in Gradle and Jenkins pipelines.", l:"advanced", g:["language","jvm","scripting"],
 b:["Groovy adds optional typing, closures, string interpolation and a lighter syntax on top of the JVM, with near-total Java compatibility. Most Java code is valid Groovy, which made adoption easy for JVM teams needing a scripting layer.",
    "Its largest footprint today is in build and automation tooling: Gradle build scripts and Jenkins declarative pipelines are Groovy-based domain-specific languages."],
 k:["Optional typing — annotate where it helps, omit where it does not.",
    "Powerful builder syntax underlies Gradle and Jenkinsfiles.",
    "Runs on the JVM with direct Java interop.",
    "Spock is a genuinely pleasant testing framework built on it."],
 r:["Java","Jenkins","Domain-Specific Language"]},

{t:"F#", a:"F Sharp", d:"A functional-first language on .NET, derived from OCaml, with strong type inference.", l:"advanced", g:["language","functional","dotnet"],
 b:["F# defaults to immutability, uses whitespace-significant syntax, and infers types so thoroughly that annotations are rare. Discriminated unions and exhaustive pattern matching make illegal states genuinely unrepresentable, which is its main selling point over C#.",
    "It has full .NET library access and is used where correctness matters — finance, actuarial modelling, domain modelling in DDD-heavy codebases. Type providers give strongly typed access to external data schemas at compile time."],
 k:["Immutable by default; mutation requires an explicit `mutable`.",
    "Discriminated unions plus exhaustive matching catch missing cases at compile time.",
    "Type inference is strong enough that most signatures are optional.",
    "Full interop with the .NET ecosystem."],
 r:["OCaml","C#","Functional Programming","Type Inference"]},

{t:"OCaml", d:"A practical functional language with a powerful module system and fast native compilation.", l:"advanced", g:["language","functional","types"],
 b:["OCaml blends functional, imperative and object-oriented styles under a strict, inferring type system. It compiles to fast native code and has predictable performance, which is why it is used where correctness and speed both matter: compilers, static analysers, and Jane Street's trading systems.",
    "Its module and functor system is unusually expressive, letting you parameterise whole modules over other modules. Rust's early compiler was written in OCaml before it became self-hosting."],
 k:["Hindley-Milner type inference — almost no annotations needed.",
    "Functors are modules parameterised by other modules.",
    "Pattern matching with exhaustiveness checking.",
    "Fast native compilation with predictable performance."],
 r:["F#","Haskell","Functional Programming","Type Inference"]},

{t:"Zig", d:"A modern systems language aiming to replace C, with no hidden control flow and no hidden allocations.", l:"advanced", g:["language","systems","emerging"],
 b:["Zig has no macros, no operator overloading and no garbage collector. Every allocation goes through an explicit allocator you pass in, so a library cannot allocate behind your back. `comptime` lets ordinary Zig code run at compile time, which replaces both generics and macros with one mechanism.",
    "It also ships a drop-in C compiler and excellent cross-compilation, so `zig cc` is useful even in projects that contain no Zig. Still pre-1.0, and the language changes between releases."],
 k:["Explicit allocators — no hidden heap usage anywhere.",
    "`comptime` executes normal code at compile time, replacing generics and macros.",
    "Errors are values in the type system, with `try` for propagation.",
    "Excellent C interop and cross-compilation out of the box."],
 r:["C","Rust","Memory Management"]},

{t:"Nim", d:"A statically typed compiled language with Python-like syntax that emits C, C++ or JavaScript.", l:"advanced", g:["language","systems","emerging"],
 b:["Nim looks like Python — indentation-based, low ceremony — but is statically typed, compiled ahead of time, and produces binaries with performance in C's range. It compiles through C by default, which gives it access to any platform with a C compiler, including microcontrollers.",
    "A powerful macro system operating on the syntax tree allows compile-time metaprogramming that would need code generation elsewhere. The community is small but the language is genuinely capable."],
 k:["Python-like syntax with static types and AOT compilation.",
    "Compiles via C, C++ or JavaScript backends.",
    "Choice of memory management strategies, including ARC and ORC.",
    "AST-based macros run at compile time."],
 r:["Python","C","AOT Compilation"]},

{t:"Solidity", d:"The dominant language for writing smart contracts on Ethereum and other EVM chains.", l:"advanced", g:["language","blockchain","web3"],
 b:["Solidity is statically typed, contract-oriented, and compiles to bytecode for the Ethereum Virtual Machine. Its execution model is unusual: every operation costs gas paid by the caller, state is permanent and public, and once deployed the code is effectively immutable unless you designed an upgrade path in advance.",
    "That immutability makes bugs unusually expensive — reentrancy, integer issues and access-control mistakes have caused nine-figure losses. Auditing, formal verification and well-tested libraries such as OpenZeppelin are standard practice rather than optional extras."],
 k:["Every instruction costs gas; loops over unbounded arrays can make a contract unusable.",
    "Deployed code is immutable — plan upgrade proxies before launch, not after.",
    "Follow checks-effects-interactions to avoid reentrancy.",
    "All on-chain state is publicly readable, including so-called private variables."],
 x:{lang:"solidity", code:'// SPDX-License-Identifier: MIT\npragma solidity ^0.8.20;\n\ncontract Vault {\n    mapping(address => uint256) public balance;\n\n    function withdraw(uint256 amount) external {\n        require(balance[msg.sender] >= amount, "insufficient");\n        balance[msg.sender] -= amount;            // effect first\n        payable(msg.sender).transfer(amount);     // interaction last\n    }\n}'},
 r:["Smart Contract","Blockchain","Ethereum Virtual Machine"]},

{t:"VBA", a:"Visual Basic for Applications", d:"The macro language embedded in Microsoft Office, automating Excel, Word and Access.", l:"intermediate", g:["language","office","automation"],
 b:["VBA runs inside Office applications and can drive their entire object model — cells, ranges, charts, documents, mail. For a very large number of businesses, a spreadsheet with VBA macros is a production system that nobody in IT knows about.",
    "It is slow, single-threaded, and awkward to version control. Microsoft is pushing Office Scripts and Python-in-Excel as successors, but the installed base is measured in decades of accumulated business logic."],
 k:["Drives the full Office object model from inside the application.",
    "Stored inside the workbook, which makes code review and versioning painful.",
    "Macro security settings block it by default in untrusted files.",
    "A very common vector for malware in email attachments."],
 r:["Scripting Language"]},

{t:"Pascal", d:"A teaching language from 1970 that shaped how programming was taught, and lives on as Delphi/Object Pascal.", l:"advanced", g:["language","historic","legacy"],
 b:["Pascal was designed by Niklaus Wirth to teach structured programming clearly: strong typing, explicit declarations, no clever shortcuts. It dominated computer science education through the 1980s and produced Turbo Pascal, one of the first genuinely fast IDEs.",
    "Object Pascal, via Delphi, became a serious tool for Windows desktop applications and still maintains commercial software today. Its influence shows up in the readable, keyword-based syntax of many later languages."],
 k:["Strict typing and explicit declarations, designed for teaching clarity.",
    "`begin` / `end` blocks instead of braces.",
    "Delphi and Free Pascal keep Object Pascal alive commercially.",
    "Turbo Pascal proved that compilation could feel instant."],
 r:["C","Legacy System"]},

{t:"Prolog", d:"A logic programming language where you declare facts and rules and let the engine search for answers.", l:"advanced", g:["language","logic","ai"],
 b:["Prolog inverts the usual model. Instead of describing how to compute something, you state what is true — facts and inference rules — and pose queries. The runtime performs unification and backtracking search to find every solution that satisfies them.",
    "It was central to symbolic AI and expert systems, and remains useful for constraint problems, parsing, and formal reasoning. Its declarative style is genuinely different from anything imperative and is worth encountering once."],
 k:["You declare relationships; the engine searches for satisfying bindings.",
    "Backtracking search is built into the execution model.",
    "Unification matches structures and binds variables in one step.",
    "Strong fit for constraint satisfaction and rule engines."],
 x:{lang:"prolog", code:'parent(ada, lovelace).\nparent(lovelace, byron).\n\nancestor(X, Y) :- parent(X, Y).\nancestor(X, Y) :- parent(X, Z), ancestor(Z, Y).\n\n?- ancestor(ada, byron).   % true.'},
 r:["Artificial Intelligence","Expert System","Functional Programming"]},

{t:"Ada", d:"A strongly typed language designed for safety-critical systems where failure costs lives.", l:"advanced", g:["language","safety-critical","embedded"],
 b:["Ada was commissioned by the US Department of Defense and built around the idea that the compiler should catch as much as possible. Range-constrained types, strong typing that will not silently convert, built-in concurrency (tasks), and contract-based programming with pre- and postconditions are all core language features.",
    "It runs avionics, rail signalling, spacecraft and defence systems. SPARK, a verifiable subset, allows mathematical proof that code is free of runtime errors."],
 k:["Types can carry ranges: `subtype Speed is Integer range 0 .. 300;`.",
    "Tasking and protected objects are language-level concurrency primitives.",
    "SPARK enables formal proof of absence of runtime errors.",
    "Certified for DO-178C avionics and similar safety standards."],
 r:["C","Formal Verification","Embedded Systems"]},

/* ============ language concepts ============ */

{t:"Compiled Language", d:"A language translated into machine code ahead of execution, so the program runs directly on the CPU.", l:"core", g:["concept","compilation"],
 b:["A compiler reads the whole program, type-checks it, optimises it and emits a binary. Errors surface before you ship, and the resulting program starts instantly because no translation happens at run time. C, C++, Rust, Go and Swift work this way.",
    "The trade is a build step between writing code and seeing it run, and platform-specific output — a Linux x86 binary will not run on an ARM Mac without recompilation. The line has blurred: Java and C# compile to bytecode then JIT to native code, and Python compiles to bytecode too."],
 k:["Errors are caught at build time rather than in production.",
    "No interpreter or VM needed at run time.",
    "Output is architecture and OS specific.",
    "Optimising compilers can restructure code far beyond what you wrote."],
 r:["Interpreted Language","JIT Compilation","AOT Compilation","Bytecode"]},

{t:"Interpreted Language", d:"A language executed statement by statement by a runtime, without producing a standalone machine-code binary first.", l:"core", g:["concept","compilation"],
 b:["An interpreter reads your code and executes it directly (in practice, usually via an internal bytecode step). There is no separate build, so the edit-run loop is immediate and the same source runs anywhere the interpreter exists. Python, Ruby, JavaScript and PHP are the everyday examples.",
    "You pay for that flexibility in speed — often ten to a hundred times slower for tight loops — and in the fact that syntax and type errors on an unexercised path only appear when that path runs. Which is exactly why interpreted languages depend so heavily on test suites."],
 k:["No build step: change the file, run it again.",
    "Errors on unexercised code paths hide until runtime.",
    "Portable, as long as the interpreter is installed.",
    "Hot paths are typically pushed into compiled native libraries."],
 r:["Compiled Language","Python","REPL","Bytecode"]},

{t:"Static Typing", d:"Types are checked at compile time, before the program ever runs.", l:"core", g:["concept","types"],
 b:["In a statically typed language every variable and expression has a type known at build time, and the compiler rejects code that misuses it. Passing a string where an integer is expected is a build failure, not a 3 a.m. page. Java, C#, Go, Rust, TypeScript and Swift are all static.",
    "The benefits scale with codebase size: refactoring is safe, IDE autocompletion is accurate, and whole categories of bug never reach a test. The cost is more up-front ceremony, though type inference removes most of it in modern languages."],
 k:["Errors surface at build time, not on a rare code path in production.",
    "Refactoring tools can rename and restructure with confidence.",
    "Types double as always-accurate documentation.",
    "Inference means you rarely have to write them all out."],
 r:["Dynamic Typing","Type Inference","TypeScript","Strong Typing"]},

{t:"Dynamic Typing", d:"Types are attached to values and checked while the program runs, not before.", l:"core", g:["concept","types"],
 b:["In dynamically typed languages a variable can hold anything and its type is a property of the current value. Python, Ruby, JavaScript and PHP work this way. Code is quicker to write, duck typing means anything with the right methods will do, and prototyping is fast.",
    "The trade-off is that a type error is discovered when that line executes — possibly in production, possibly months later. This is why dynamic languages lean hard on test suites, and why optional type systems (Python type hints, TypeScript) became so popular."],
 k:["The same variable can hold different types over its lifetime.",
    "Duck typing: if it responds to the method, it works.",
    "Type errors are runtime errors — tests are the safety net.",
    "Gradual typing adds optional static checks without changing the runtime."],
 r:["Static Typing","Python","JavaScript","Duck Typing"]},

{t:"Strong Typing", d:"The language refuses to silently reinterpret a value as a different type.", l:"intermediate", g:["concept","types"],
 b:["Strength is about coercion, not about when checking happens — it is a separate axis from static versus dynamic. Python is dynamically but strongly typed: `1 + \"2\"` raises an error rather than guessing. JavaScript is dynamically and weakly typed: the same expression happily produces `\"12\"`.",
    "Strong typing removes a class of bugs where a number quietly becomes a string halfway through a pipeline and only surfaces as nonsense output three functions later."],
 k:["Orthogonal to static versus dynamic — Python is dynamic and strong.",
    "Implicit conversions are rejected; explicit casts are required.",
    "Prevents silent nonsense like `\"5\" - 2` returning a number.",
    "Makes error messages point at the actual mistake."],
 r:["Weak Typing","Dynamic Typing","Static Typing"]},

{t:"Weak Typing", d:"The language will implicitly convert between types to make an operation work.", l:"intermediate", g:["concept","types"],
 b:["Weakly typed languages apply coercion rules rather than raising an error. JavaScript's `[] + {}`, `\"5\" * \"2\"` and `1 == \"1\"` are the classic examples: convenient in a one-liner, baffling in a large codebase.",
    "The pragmatic response is to avoid the loose operators entirely — use `===` in JavaScript, convert explicitly, and let a static type layer catch the rest."],
 k:["Implicit coercion makes mismatched operations succeed with surprising results.",
    "JavaScript and PHP are the canonical examples.",
    "Use strict equality and explicit conversion to opt out.",
    "Bugs from coercion tend to appear far from their cause."],
 r:["Strong Typing","JavaScript","Dynamic Typing"]},

{t:"Type Inference", d:"The compiler works out a value's type from context so you do not have to write it.", l:"intermediate", g:["concept","types"],
 b:["Inference gives you static typing without the verbosity. Writing `var count = 0` in C#, `let x = vec![1,2]` in Rust or `auto it = m.begin()` in C++ leaves the type fully known at compile time — you simply did not have to spell it out.",
    "Languages in the ML family (Haskell, OCaml, F#) infer across whole functions with Hindley-Milner, so annotations are almost never required. Most teams still annotate public function signatures deliberately, because those double as documentation and stop an accidental type change rippling outward."],
 k:["Static safety with far less typing.",
    "Hindley-Milner infers entire function signatures, not just locals.",
    "Annotate public APIs anyway — it pins the contract.",
    "Inference failures usually mean the code is genuinely ambiguous."],
 r:["Static Typing","TypeScript","Rust","Haskell"]},

{t:"Bytecode", d:"A compact instruction set produced by a compiler and executed by a virtual machine rather than by hardware.", l:"intermediate", g:["concept","compilation"],
 b:["Bytecode sits between source and machine code. Java compiles to JVM bytecode, C# to CIL, Python to `.pyc` files. The VM either interprets these instructions or JIT-compiles them to native code as the program runs.",
    "The advantage is portability: one build runs anywhere the VM exists. It also gives the runtime a chance to optimise using information only available while running, such as which branch is actually taken and which types actually appear."],
 k:["Portable across platforms — the VM absorbs the differences.",
    "Enables runtime optimisation that ahead-of-time compilers cannot do.",
    "Easier to decompile than native code, which matters for IP protection.",
    "Java `.class`, .NET CIL and Python `.pyc` are all bytecode."],
 r:["Java Virtual Machine","JIT Compilation","Compiled Language",".NET"]},

{t:"JIT Compilation", a:"Just-In-Time Compilation", d:"Compiling code to native machine instructions while the program is running, guided by what it actually does.", l:"intermediate", g:["concept","performance"],
 b:["A JIT starts by interpreting, watches which methods run hot, and then compiles those to optimised machine code — using real observations. If a loop only ever sees integers, it can compile an integer-specialised version and guard it with a cheap check. The JVM, .NET, V8 and PyPy all work this way.",
    "The costs are warm-up time (the first thousand calls are slow) and memory for the compiler and the generated code. For long-running servers that is a fine trade; for short CLI tools it is pure overhead, which is why AOT compilation is making a comeback."],
 k:["Profile-guided: it optimises for the behaviour actually observed.",
    "Warm-up means benchmarks must discard the first runs.",
    "Can deoptimise and recompile when assumptions are violated.",
    "Sometimes beats AOT, because static compilers cannot see runtime types."],
 dg:"",
 r:["AOT Compilation","Bytecode","Java Virtual Machine","Compiled Language"]},

{t:"AOT Compilation", a:"Ahead-Of-Time Compilation", d:"Compiling everything to native code before the program ships, so startup is instant.", l:"intermediate", g:["concept","performance"],
 b:["AOT is the classic model — C, Rust and Go do nothing else. It has returned to managed languages because serverless and container workloads care intensely about cold start: GraalVM Native Image, .NET Native AOT and Dart's release builds all produce native binaries with no warm-up.",
    "The trade-offs are lost runtime specialisation, larger binaries in some cases, and restrictions on reflection and dynamic class loading, which have to be declared up front."],
 k:["No warm-up: peak performance from the first instruction.",
    "Ideal for CLIs, serverless functions and short-lived containers.",
    "Reflection and dynamic loading need explicit configuration.",
    "Cannot exploit runtime type feedback the way a JIT does."],
 r:["JIT Compilation","Compiled Language","Serverless","Cold Start"]},

{t:"REPL", a:"Read-Eval-Print Loop", d:"An interactive prompt that evaluates one expression at a time and shows the result immediately.", l:"core", g:["concept","tooling"],
 b:["The REPL reads what you type, evaluates it, prints the result, and loops. It is the fastest possible feedback loop: inspect a data structure, try an API call, check a regex, without writing a file or running a build.",
    "Lisp invented it; Python, Ruby, Node, Julia and Clojure all lean on it. Jupyter notebooks are essentially a persistent, document-shaped REPL, which is why they dominate data exploration."],
 k:["Fastest feedback loop available for exploring a library or dataset.",
    "State persists between expressions within a session — which can also mislead you.",
    "Notebooks are REPLs with saved history and inline output.",
    "Clojure and Lisp treat REPL-driven development as the primary workflow."],
 r:["Interpreted Language","Python","Lisp"]},

{t:"Package Manager", d:"A tool that installs, versions and resolves the third-party libraries a project depends on.", l:"core", g:["concept","tooling"],
 b:["A package manager reads a manifest listing your direct dependencies, resolves the full transitive graph, and produces a lock file recording exact resolved versions so every machine and every build gets identical code. npm, pip, cargo, Maven, NuGet, Go modules and Composer all do this.",
    "The dependency graph is also an attack surface. Typosquatting, compromised maintainer accounts and malicious post-install scripts are all real. Lock files, integrity hashes, dependency scanning and pinned versions are basic hygiene, not paranoia."],
 k:["The lock file is what makes builds reproducible — commit it.",
    "Semantic versioning tells you what a version bump is allowed to break.",
    "Transitive dependencies mean you trust far more code than you chose.",
    "Audit and scan dependencies; supply-chain attacks target this layer."],
 r:["Semantic Versioning","Supply Chain Attack","Build Tool"]},

{t:"Standard Library", d:"The set of functionality shipped with a language itself, available without installing anything.", l:"core", g:["concept","tooling"],
 b:["What lives in the standard library shapes the whole ecosystem. Python and Go ship batteries included — HTTP servers, JSON, cryptography, testing — so many projects have almost no dependencies. JavaScript's is famously thin, which is part of why an average Node project pulls in hundreds of packages.",
    "Standard library code is stable, reviewed, versioned with the language and free of supply-chain risk. The trade is that it evolves slowly and cannot be upgraded independently of the runtime."],
 k:["Larger standard libraries mean smaller dependency trees and less supply-chain exposure.",
    "Stability guarantees are stronger than for third-party packages.",
    "Cannot be upgraded without upgrading the language runtime.",
    "Go and Python are the classic batteries-included examples."],
 r:["Package Manager","Python","Go"]},

{t:"Scripting Language", d:"A language aimed at automating tasks and gluing systems together, usually interpreted and lightly typed.", l:"core", g:["concept","language-class"],
 b:["The label describes intent more than mechanics: fast to write, no build step, strong string and file handling, easy to call other programs. Bash, Python, Ruby, Perl, Lua and JavaScript all began here.",
    "The distinction has largely dissolved. Python runs production ML systems and JavaScript runs planet-scale services. It survives mostly as a description of how a language is being used in a given moment."],
 k:["Optimised for speed of writing over speed of execution.",
    "Typically interpreted, dynamically typed, no compile step.",
    "Excellent at process control, file handling and text munging.",
    "The scripting/systems boundary is now mostly historical."],
 r:["Bash","Python","Interpreted Language"]},

{t:"Markup Language", d:"A language that annotates content with structure or presentation rather than describing computation.", l:"core", g:["concept","language-class"],
 b:["Markup languages have no logic, loops or functions — they describe what something is. HTML marks up documents for browsers, XML defines structured data exchange, Markdown handles lightweight formatting, and YAML and JSON carry configuration and data.",
    "They are not programming languages in the Turing-complete sense, which is why the phrase programming in HTML is a reliable way to identify someone who has never done either."],
 k:["Describes structure and content, not behaviour.",
    "HTML, XML, Markdown, YAML and JSON are the ones you will meet daily.",
    "Not Turing-complete — no loops, no conditionals, no computation.",
    "YAML's whitespace sensitivity is a well-known source of config bugs."],
 r:["HTML","JSON","YAML","XML"]},

{t:"Domain-Specific Language", a:"DSL", d:"A small language designed for one problem area rather than for general-purpose programming.", l:"intermediate", g:["concept","language-class"],
 b:["DSLs trade generality for expressiveness. SQL for queries, regular expressions for pattern matching, HCL for infrastructure, CSS for styling, Dockerfiles for images — each is far more direct in its niche than general-purpose code would be.",
    "External DSLs have their own parser and syntax. Internal DSLs are built inside a host language using its own constructs, which is what Gradle scripts and many testing frameworks do. The risk is always the same: a DSL that grows until it needs the features of a real language, badly reimplemented."],
 k:["Far more expressive than general-purpose code inside its narrow domain.",
    "External DSLs have their own grammar; internal ones borrow the host's.",
    "SQL, regex, CSS and HCL are DSLs you already use.",
    "Beware the DSL that slowly grows loops, variables and conditionals."],
 r:["SQL","Regular Expression","Terraform","Groovy"]},

{t:"Low-Level Language", d:"A language that exposes hardware details — memory addresses, registers, explicit allocation.", l:"intermediate", g:["concept","language-class"],
 b:["Low-level means close to the machine: assembly is the extreme, C is the practical case. You control memory layout, allocation and lifetime, and you pay for mistakes with corruption rather than exceptions.",
    "You need it when you are writing an OS kernel, a device driver, firmware for a chip with 2 KB of RAM, or a hot loop where predictable memory layout is the whole performance story."],
 k:["Direct control over memory layout and lifetime.",
    "No runtime overhead or garbage collector to schedule around.",
    "Errors corrupt state rather than raising a clean exception.",
    "Required for kernels, drivers, firmware and real-time systems."],
 r:["High-Level Language","C","Assembly Language","Memory Management"]},

{t:"High-Level Language", d:"A language that abstracts away hardware detail so you can express intent instead of mechanism.", l:"core", g:["concept","language-class"],
 b:["High-level languages hide registers, pointers and manual allocation behind variables, objects, collections and functions. Python, Java, JavaScript, C# and Go all qualify. You describe what should happen; the runtime decides how.",
    "Productivity rises sharply and whole bug classes disappear. In exchange you accept some performance overhead and less predictable memory behaviour — which matters for a real-time controller and not at all for a web API."],
 k:["Automatic memory management removes leaks and use-after-free.",
    "Portable across architectures without source changes.",
    "Abstraction has a cost, usually irrelevant outside hot paths.",
    "Garbage collection pauses can matter for latency-sensitive work."],
 r:["Low-Level Language","Python","Garbage Collection","Abstraction"]},

{t:"Java Virtual Machine", a:"JVM", d:"The runtime that executes Java bytecode, providing memory management, JIT compilation and platform independence.", l:"intermediate", g:["runtime","jvm"],
 b:["The JVM loads and verifies bytecode, manages the heap, runs garbage collection, and JIT-compiles hot methods to native code. Because it is the compilation target rather than Java specifically, Kotlin, Scala, Clojure and Groovy all run on it and interoperate freely.",
    "Modern collectors (G1, ZGC, Shenandoah) keep pause times in the single-digit milliseconds even on large heaps. JVM tuning — heap sizing, collector choice, JIT warm-up — is a genuine specialism in high-throughput systems."],
 k:["One VM, many languages: Java, Kotlin, Scala, Clojure, Groovy.",
    "Garbage collection is pluggable; ZGC and Shenandoah target sub-millisecond pauses.",
    "JIT compilation means performance improves after warm-up.",
    "`-Xmx` sets the maximum heap and is the first tuning knob anyone touches."],
 r:["Java","Bytecode","JIT Compilation","Garbage Collection","Kotlin"]},

{t:"Node.js", d:"A JavaScript runtime built on V8 that runs JavaScript outside the browser, with non-blocking I/O.", l:"core", g:["runtime","javascript","backend"],
 b:["Node put V8, an event loop and a set of I/O APIs together so JavaScript could run on servers. Its model is single-threaded with asynchronous, non-blocking I/O: one thread handles thousands of concurrent connections because it is never sitting idle waiting on a socket.",
    "That makes it excellent for I/O-bound work — APIs, proxies, real-time services — and poor for CPU-bound work, where a single long computation blocks everything. Worker threads and child processes are the escape hatch. npm, its package registry, is the largest in existence."],
 k:["Non-blocking I/O on a single thread — great for concurrency, bad for heavy computation.",
    "Never block the event loop; offload CPU work to worker threads.",
    "npm hosts the largest package ecosystem anywhere, with the supply-chain risk that implies.",
    "Deno and Bun are alternative runtimes for the same language."],
 x:{lang:"javascript", code:'import { createServer } from "node:http";\n\ncreateServer(async (req, res) => {\n  const data = await loadFromDb(req.url);   // non-blocking\n  res.writeHead(200, { "Content-Type": "application/json" });\n  res.end(JSON.stringify(data));\n}).listen(3000);'},
 dg:"event-loop",
 r:["JavaScript","Event Loop","npm","Asynchronous Programming"]},

{t:".NET", d:"Microsoft's cross-platform runtime and framework family for C#, F# and Visual Basic.", l:"intermediate", g:["runtime","dotnet"],
 b:[".NET compiles source to an intermediate language, then JIT-compiles it to native code at run time, with a garbage collector and a very large base class library underneath. Since .NET Core it is open source and runs on Linux and macOS as first-class targets.",
    "ASP.NET Core is a genuinely fast web framework, Entity Framework is its ORM, and Native AOT now produces standalone binaries with no runtime dependency for container and serverless workloads."],
 k:["One runtime for C#, F# and VB.NET.",
    "Cross-platform and open source since .NET Core.",
    "ASP.NET Core consistently ranks near the top of web framework benchmarks.",
    "Native AOT removes JIT warm-up for short-lived processes."],
 r:["C#","F#","Bytecode","JIT Compilation"]},

{t:"Runtime Environment", d:"The software layer that provides everything a program needs while it is executing.", l:"intermediate", g:["concept","runtime"],
 b:["The runtime supplies memory management, the standard library, scheduling, exception handling and the interface to the operating system. The JVM, the .NET CLR, Node.js, CPython and the Go runtime are all examples. Even C has a small one — the C runtime that sets up the stack and calls `main`.",
    "Runtime versions are a deployment concern in their own right: mismatches between the version you developed against and the one in production are a classic source of works on my machine. Containers exist largely to make the runtime part of the artefact."],
 k:["Provides memory management, scheduling and OS access.",
    "Version mismatches between environments are a leading cause of deploy failures.",
    "Containers package the runtime with the app to eliminate that drift.",
    "Even compiled languages ship a minimal runtime."],
 r:["Java Virtual Machine","Node.js","Container","Docker"]},

{t:"Null Safety", d:"A type system that distinguishes values which may be absent, so missing values cannot be dereferenced by accident.", l:"intermediate", g:["concept","types"],
 b:["Tony Hoare called null references his billion-dollar mistake, and null-pointer exceptions remain one of the most common runtime failures in software. Null safety encodes optionality in the type: `String` can never be null, `String?` might be, and the compiler forces you to handle the second case before use.",
    "Kotlin, Swift, Rust (`Option<T>`), TypeScript under `strictNullChecks` and modern C# all do this. The result is that an entire family of crashes becomes a compile error."],
 k:["Nullable and non-nullable are different types, checked at compile time.",
    "Safe-call (`?.`) and elvis (`?:`) operators handle the nullable path concisely.",
    "Rust and Haskell use `Option`/`Maybe` rather than a null value at all.",
    "The boundary with untyped external data is where nulls still sneak in."],
 r:["Kotlin","Swift","TypeScript","Static Typing"]},

{t:"Memory Safety", d:"A guarantee that a program cannot read or write memory it does not own.", l:"intermediate", g:["concept","security"],
 b:["Memory-unsafe languages let you read past the end of a buffer, use memory after freeing it, or free the same pointer twice. Microsoft and Google have both reported that roughly 70% of their severe security vulnerabilities stem from memory safety issues in C and C++.",
    "Safety can be achieved with a garbage collector (Java, Go, Python), with compile-time ownership analysis (Rust), or with runtime bounds checks. Government agencies now explicitly recommend memory-safe languages for new critical software."],
 k:["Around 70% of severe CVEs in large C/C++ codebases are memory-safety bugs.",
    "Garbage collection and Rust's borrow checker are the two mainstream solutions.",
    "Buffer overflow, use-after-free and double-free are the classic failures.",
    "Sanitisers (ASan, MSan) catch many of these during testing."],
 r:["Rust","C","Garbage Collection","Buffer Overflow"]},

{t:"Transpiler", d:"A compiler that translates source code from one high-level language into another high-level language.", l:"intermediate", g:["concept","tooling"],
 b:["Where a traditional compiler targets machine code, a transpiler targets other source. TypeScript to JavaScript, Babel from modern JavaScript to older JavaScript, Sass to CSS, and Kotlin or Dart to JavaScript are all everyday examples.",
    "The technique lets you use a nicer or safer language while still deploying to a platform that only accepts one specific language — the browser being the obvious case. Source maps preserve the link back to the original code for debugging."],
 k:["Output is human-readable source, not machine code.",
    "Source maps keep stack traces and breakpoints pointing at your real code.",
    "Enables new language features on old runtimes.",
    "Babel, tsc, Sass and SWC are the ones you will meet."],
 r:["TypeScript","Compiled Language","Bundler"]},

{t:"Syntax", d:"The grammatical rules governing how valid code must be written.", l:"core", g:["concept","fundamentals"],
 b:["Syntax is form: where the semicolons go, whether blocks use braces or indentation, how a function is declared. A syntax error means the parser could not build a tree from your text, and it is caught before anything runs.",
    "Syntax is the shallowest part of learning a language — a week's work. Semantics, idiom and ecosystem are the parts that actually take time."],
 k:["Caught by the parser before execution or type checking.",
    "Different from semantics: syntactically valid code can still be nonsense.",
    "Formatters (Prettier, gofmt, Black) end style arguments by removing the choice.",
    "The easiest part of a new language to learn."],
 r:["Semantics","Compiler","Parser"]},

{t:"Semantics", d:"What code actually means and does when it runs, as opposed to how it is written.", l:"intermediate", g:["concept","fundamentals"],
 b:["Semantics covers evaluation order, scoping rules, what equality means, whether arguments are passed by value or by reference, and when side effects occur. Two languages can share nearly identical syntax and behave completely differently.",
    "Most genuinely hard bugs are semantic, not syntactic: the code parses, compiles and runs, but means something other than what the author intended."],
 k:["Syntactically valid code can be semantically wrong.",
    "Scoping, evaluation order and equality semantics differ sharply between languages.",
    "Undefined behaviour is a semantic gap the compiler may exploit.",
    "Where the hard bugs live."],
 r:["Syntax","Compiler"]},

{t:"Duck Typing", d:"An object's suitability is decided by the methods it has, not by the class it belongs to.", l:"intermediate", g:["concept","types"],
 b:["If it walks like a duck and quacks like a duck, treat it as a duck. In Python, any object with a `read()` method can be passed to code expecting a file — no interface declaration, no inheritance, no registration.",
    "This makes code extremely flexible and testing easy, since a hand-written stub with the right methods is a valid substitute. The cost is that mismatches only appear at call time, and that the required shape is implicit rather than written down anywhere."],
 k:["Compatibility is structural, not declared.",
    "Makes test doubles trivial to write.",
    "Failures surface at the call site, not at definition.",
    "Protocols and typing.Protocol let you document the expected shape."],
 r:["Dynamic Typing","Python","Polymorphism","Interface"]}

]);
