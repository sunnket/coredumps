/* Transformer internals and the serving vocabulary. The roadmap treats these
   as the topics that carry most GenAI technical rounds, and they were taught
   in lessons without being lookup-able as terms. */

TD.add("genai-llm", [

{t:"Scaled Dot-Product Attention", d:"The core attention formula: score every pair with a dot product, divide by √d, softmax, then take a weighted average of the values.", l:"intermediate", g:["transformers"],
 b:["Each token emits a Query (*what am I looking for*), a Key (*what do I contain*) and a Value (*what I will hand over*). Comparing a Query against every Key with a dot product gives similarity scores; softmax turns those into weights that sum to one; the output is the weighted average of everyone's Values.",
    "The division by √d is the part interviewers ask about. In high dimensions dot products grow large in magnitude, which pushes softmax into a near-one-hot spike where gradients vanish. Scaling by the square root of the head dimension keeps the scores in a range where softmax stays soft and training remains stable."],
 k:["`softmax(QKᵀ / √d_k)V` — score, scale, normalise, average.",
    "√d_k stops large dot products saturating softmax and killing gradients.",
    "Q, K and V are three learned linear projections of the same input.",
    "O(n²) in sequence length — the reason long context is expensive."],
 x:{lang:"python", code:"import torch, math\n\ndef attention(Q, K, V, mask=None):\n    scores = Q @ K.transpose(-2, -1) / math.sqrt(Q.size(-1))\n    if mask is not None:\n        scores = scores.masked_fill(mask, float('-inf'))\n    return torch.softmax(scores, dim=-1) @ V"},
 r:["Self-Attention","Multi-Head Attention","Causal Masking","Softmax"]},

{t:"Causal Masking", d:"Blocking a token from attending to anything after it, so the model cannot cheat at next-token prediction.", l:"intermediate", g:["transformers"],
 b:["Training feeds a whole sentence in at once for parallelism, which means token 3 could in principle attend to token 4 — and next-token prediction becomes trivial if the answer is visible. The fix is to set every future position's attention score to negative infinity before the softmax, so those weights come out as exactly zero.",
    "It is implemented as an upper-triangular mask added to the score matrix. This is the single structural difference that makes a decoder causal rather than bidirectional, and it is why GPT-style models generate while BERT-style models only understand."],
 k:["Future positions are set to −∞ *before* softmax, so their weight becomes zero.",
    "An upper-triangular mask added to the score matrix.",
    "It is what makes a decoder causal; encoders are bidirectional and unmasked.",
    "Without it, next-token prediction is trivially solvable by copying."],
 x:{lang:"python", code:"import torch\n\nn = seq_len\nmask = torch.triu(torch.ones(n, n), diagonal=1).bool()   # strictly above the diagonal\nscores = scores.masked_fill(mask, float('-inf'))\n# after softmax those positions are exactly 0"},
 r:["Self-Attention","Scaled Dot-Product Attention","Decoder","Encoder"]},

{t:"RoPE", a:"Rotary Positional Embedding", d:"Encoding position by rotating the query and key vectors, so attention depends only on relative distance.", l:"advanced", g:["transformers"],
 b:["Instead of adding a position vector to the embedding, RoPE rotates Q and K by an angle proportional to their position. Because the dot product of two rotated vectors depends only on the difference between their angles, attention scores automatically become a function of relative distance rather than absolute index.",
    "That property is why it became the default in Llama, Mistral and Qwen. It also extends gracefully: interpolating or rescaling the rotation frequencies (NTK-aware scaling, YaRN) lets a model trained at 4k context work at 32k or beyond, which learned absolute positions simply cannot do."],
 k:["Rotates Q and K by an angle set by position; the dot product then sees relative distance.",
    "The modern default — Llama, Mistral, Qwen.",
    "No learned parameters, unlike absolute position embeddings.",
    "Extends to longer contexts via frequency interpolation (NTK scaling, YaRN)."],
 x:{lang:"text", code:"absolute:  emb = token_emb + pos_emb        (fails past trained length)\nRoPE:      rotate(Q, pos), rotate(K, pos)\n\n  Q_m . K_n  depends only on (m - n)\n  -> relative position, for free, at every layer"},
 r:["Positional Encoding","ALiBi","Self-Attention","Context Window"]},

{t:"ALiBi", a:"Attention with Linear Biases", d:"Encoding position by subtracting a penalty proportional to distance from each attention score.", l:"advanced", g:["transformers"],
 b:["No embeddings and no rotation — just a linear bias added to the scores before softmax, penalising distant tokens more heavily. Each head gets a different penalty slope, so some heads stay local while others look far away.",
    "Its selling point is extrapolation: because the bias is a simple function of distance, a model trained at 1k tokens keeps behaving sensibly at 4k with no retraining. RoPE ultimately won on quality in most model families, but ALiBi remains the cleanest illustration that positional information does not require embeddings at all."],
 k:["A distance-proportional penalty added to attention scores before softmax.",
    "Different slope per head — some heads local, some global.",
    "Extrapolates beyond the trained context length essentially for free.",
    "Simpler than RoPE; RoPE is more common in current models."],
 x:{lang:"text", code:"score(i, j) = q_i . k_j  -  m * |i - j|\n                             ^ per-head slope\n\nfarther away -> larger penalty -> lower weight\nno embeddings, no rotation, no parameters"},
 r:["RoPE","Positional Encoding","Self-Attention","Context Window"]},

{t:"Encoder", d:"The transformer half that reads a whole sequence bidirectionally to build representations.", l:"intermediate", g:["transformers"],
 b:["Every token attends to every other token in both directions, so each position's representation is informed by the full context on both sides. That makes encoders excellent at understanding tasks — classification, named-entity recognition, and above all the embeddings that retrieval systems depend on.",
    "What they cannot do is generate autoregressively, because there is no causal ordering. BERT is the canonical encoder, and nearly every embedding model you would use in a RAG pipeline is an encoder."],
 k:["Bidirectional attention — every token sees the whole sequence.",
    "Best for understanding: classification, NER, and retrieval embeddings.",
    "Cannot generate text left to right; there is no causal mask.",
    "BERT and most embedding models are encoder-only."],
 x:{lang:"text", code:"\"the cat sat on the [MASK]\"\n         every token sees every other, both directions\n         -> a rich representation per token\n\nUsed for: embeddings, classification, NER, reranking"},
 r:["Decoder","Self-Attention","Embedding","Causal Masking"]},

{t:"Decoder", d:"The transformer half that generates one token at a time, seeing only what came before.", l:"intermediate", g:["transformers"],
 b:["Causal masking restricts each position to itself and earlier tokens, which is exactly what next-token prediction requires. Generation is then autoregressive: produce a token, append it, run again — which is also why the KV cache exists.",
    "Every modern chat model is decoder-only. The encoder-decoder design survives mainly in translation and summarisation models like T5, where a separate bidirectional pass over the source text genuinely helps."],
 k:["Causal attention — a token sees only itself and earlier tokens.",
    "Generates autoregressively, one token per forward pass.",
    "Every modern chat LLM is decoder-only: GPT, Llama, Claude, Mistral.",
    "The repeated forward passes are precisely why the KV cache exists."],
 x:{lang:"text", code:"\"the cat sat\" -> \"on\"\n\"the cat sat on\" -> \"the\"\n\"the cat sat on the\" -> \"mat\"\n\neach step sees only the left context\n-> cache the K,V of everything already processed"},
 r:["Encoder","Causal Masking","KV Cache","Greedy Decoding"]},

{t:"Grouped-Query Attention", a:"GQA", d:"Letting several query heads share one key/value head, shrinking the KV cache with little quality loss.", l:"advanced", g:["transformers"],
 b:["Standard multi-head attention gives every query head its own K and V head, which makes the KV cache large. Multi-query attention goes to the other extreme with a single shared K/V head — tiny cache, noticeable quality loss. GQA sits between them: groups of query heads share a K/V head.",
    "With eight groups the cache drops to roughly a quarter of MHA's while quality stays close to baseline, which is why Llama 2/3, Mistral and Qwen all adopted it. It is the clearest example of the memory-versus-quality dial that dominates inference engineering."],
 k:["Query heads keep their count; K/V heads are shared in groups.",
    "GQA-8 gives roughly 25% of MHA's cache with near-identical quality.",
    "MHA → GQA → MQA is one dial: fewer K/V heads, smaller cache, more quality loss.",
    "The default in Llama 2/3, Mistral and Qwen."],
 x:{lang:"text", code:"MHA   32 query heads, 32 K/V heads    cache 100%\nGQA-8 32 query heads,  8 K/V heads    cache  ~25%   <- the default\nMQA   32 query heads,  1 K/V head     cache   ~3%"},
 r:["Multi-Query Attention","Multi-Head Attention","KV Cache","Multi-Head Latent Attention"]},

{t:"Multi-Query Attention", a:"MQA", d:"All query heads sharing a single key/value head — the smallest possible KV cache.", l:"advanced", g:["transformers"],
 b:["One K head and one V head for the entire layer, however many query heads there are. The KV cache shrinks to a few percent of multi-head attention's, which makes very long contexts and high concurrency far cheaper to serve.",
    "The cost is measurable quality loss, which is why GQA — a handful of groups instead of one — displaced it as the default. MQA is still used where cache size dominates everything else, as in PaLM and Falcon."],
 k:["A single shared K/V head for all query heads.",
    "Roughly 3% of MHA's cache — the smallest of the family.",
    "Measurable quality loss; GQA is the usual compromise.",
    "Used in PaLM and Falcon."],
 x:{lang:"text", code:"cache size ~ layers x kv_heads x head_dim x seq_len\n                        ^\n            MHA: 32     GQA: 8     MQA: 1\n\nfewer K/V heads -> smaller cache -> longer context, more users"},
 r:["Grouped-Query Attention","Multi-Head Attention","KV Cache","Multi-Head Latent Attention"]},

{t:"Multi-Head Latent Attention", a:"MLA", d:"Compressing keys and values into a low-rank latent vector, decompressed on the fly during attention.", l:"advanced", g:["transformers"],
 b:["Rather than reducing the *number* of K/V heads as GQA and MQA do, MLA reduces their *dimensionality*: keys and values are projected into a small shared latent space, cached in that compressed form, and reconstructed when needed.",
    "DeepSeek introduced it, and the reported result is better quality than MQA at a comparable cache size — a different point on the same trade-off curve rather than a strictly better answer. Knowing that it compresses dimensionality rather than head count is the distinction worth being able to state."],
 k:["Compresses K/V into a low-rank latent vector instead of dropping heads.",
    "Better quality than MQA at similar cache size.",
    "Introduced by DeepSeek; less widely adopted than GQA so far.",
    "Attacks the head_dim term of the cache formula, not the kv_heads term."],
 x:{lang:"text", code:"GQA / MQA  reduce the NUMBER of K/V heads\nMLA        reduces the DIMENSION of each\n\ncache stores the small latent; K and V are\nreconstructed during attention"},
 r:["Grouped-Query Attention","Multi-Query Attention","KV Cache","Quantisation"]},

{t:"Sliding Window Attention", d:"Restricting each token to attend only to the last W tokens, making attention linear rather than quadratic.", l:"advanced", g:["transformers"],
 b:["Full attention costs O(n²) in sequence length. Capping each token's view at a fixed window W makes it O(n·W), which is linear in n. Mistral uses this with a 4096-token window.",
    "The obvious objection — that the model can no longer see far back — is answered by depth. Layer one reaches W tokens back, layer two reaches through those to 2W, and so on, so the effective receptive field is W × layers even though no single layer looks that far. Information propagates rather than being attended to directly."],
 k:["Each token attends to the last W tokens only: O(n·W) instead of O(n²).",
    "Effective receptive field is W × layers, because information propagates upward.",
    "Used in Mistral with a 4096-token window.",
    "Combines with a rolling KV cache that discards entries older than W."],
 x:{lang:"text", code:"full:    every token attends to all n     O(n^2)\nwindow:  every token attends to last W   O(n*W)\n\nlayer 1: sees W back\nlayer 2: sees through those -> 2W\n...      effective reach = W x layers"},
 r:["Self-Attention","KV Cache","Context Window","FlashAttention"]},

{t:"WordPiece", d:"A subword tokenisation algorithm that merges the pair which most improves the corpus likelihood.", l:"intermediate", g:["transformers","nlp"],
 b:["Like byte-pair encoding, it starts small and repeatedly merges pairs of symbols. The difference is the criterion: BPE merges the *most frequent* adjacent pair, while WordPiece merges the pair that most increases the likelihood of the training corpus under a unigram language model.",
    "It marks continuation pieces with `##`, so *playing* becomes `play` + `##ing` and you can always reconstruct word boundaries. BERT and its descendants use it; the GPT family uses BPE."],
 k:["Merges by likelihood gain, where BPE merges by raw frequency.",
    "`##` marks a continuation piece, preserving word boundaries.",
    "Used by BERT and related encoder models.",
    "Same family as BPE and SentencePiece — subword, vocabulary-bounded."],
 x:{lang:"text", code:"\"unaffordable\"  ->  un ##afford ##able\n\n##  = continues the previous token\nBPE:       merge the most FREQUENT pair\nWordPiece: merge the pair that best raises corpus likelihood"},
 r:["Byte-Pair Encoding","SentencePiece","Tokenisation","Encoder"]},

{t:"SentencePiece", d:"A tokeniser that treats input as a raw byte stream including spaces, so it works on any language without pre-splitting.", l:"intermediate", g:["transformers","nlp"],
 b:["Most tokenisers assume you can split on whitespace first, which quietly assumes a language that uses spaces — untrue for Japanese, Chinese and Thai. SentencePiece skips that step entirely and treats the text as a stream of characters, encoding the space itself as a visible symbol (`▁`).",
    "That makes it fully reversible: decoding the token IDs reproduces the original string exactly, spaces included. Llama, T5 and most multilingual models use it, and it can implement either BPE or a unigram language model underneath."],
 k:["No pre-tokenisation, so it is language-agnostic — including spaceless scripts.",
    "Encodes the space as `▁`, making detokenisation exactly reversible.",
    "Used by Llama, T5 and most multilingual models.",
    "A framework: it can run BPE or a unigram model underneath."],
 x:{lang:"text", code:"\"Hello world\"  ->  ▁Hello  ▁world\n                   ^ the space is part of the token\n\ndecode(encode(x)) == x        exactly, always\nworks on Japanese, Thai, Chinese -- no whitespace needed"},
 r:["Byte-Pair Encoding","WordPiece","Tokenisation","Embedding"]},

{t:"GPTQ", d:"A post-training quantisation method that compresses weights layer by layer using second-order information.", l:"advanced", g:["quantisation"],
 b:["It quantises one layer at a time, using an approximation of the Hessian to decide how to round each weight so that the layer's output changes as little as possible. Errors from already-quantised weights are compensated for in those still to be processed.",
    "It needs only a small calibration set and no retraining, produces fast GPU inference at 4 bits, and was the method that made 4-bit LLMs practical. AWQ often edges it on quality at the same bit width by protecting activation-salient weights instead."],
 k:["Post-training and weight-only; no retraining, just a small calibration set.",
    "Uses second-order (Hessian) information to minimise per-layer output error.",
    "Fast 4-bit GPU inference; the method that made 4-bit LLMs mainstream.",
    "AWQ is often slightly better in quality at the same bit width."],
 x:{lang:"text", code:"for each layer:\n    quantise weights column by column\n    push the rounding error into the not-yet-quantised columns\n\nno gradient updates -- just a calibration pass"},
 r:["Quantisation","AWQ","GGUF","QLoRA"]},

{t:"AWQ", a:"Activation-aware Weight Quantisation", d:"Quantisation that identifies the small fraction of weights which matter most and protects them.", l:"advanced", g:["quantisation"],
 b:["The observation behind it: not all weights matter equally. Roughly one percent of them — those multiplying large activations — dominate the output, and if those are preserved the rest can be quantised aggressively with little damage. AWQ finds them using activation statistics rather than weight magnitudes.",
    "It is a different bet from GPTQ, which minimises reconstruction error mathematically. AWQ often wins at 4 bits and is generally faster to apply, which is why both remain in common use rather than one replacing the other."],
 k:["Protects the ~1% of weights that multiply the largest activations.",
    "Selects them from **activation** statistics, not weight magnitude.",
    "Often better quality than GPTQ at 4 bits, and faster to compute.",
    "Post-training, like GPTQ — no retraining required."],
 x:{lang:"text", code:"observation: ~1% of weights carry most of the output\n             (identified via ACTIVATION magnitude)\n\nkeep those in higher precision, quantise the rest hard\n-> most of the compression, little of the damage"},
 r:["GPTQ","Quantisation","GGUF","Pruning"]},

{t:"GGUF", d:"The file format of the llama.cpp ecosystem, packaging quantised weights plus metadata for CPU and Apple Silicon inference.", l:"intermediate", g:["quantisation","serving"],
 b:["A single self-contained file holding the weights, the tokeniser and the configuration, so a model is one download rather than a directory of tensors and JSON. It supports many quantisation levels, conventionally named `Q4_K_M`, `Q5_K_S` and so on, where the number is the bit width and the suffix the variant.",
    "It is what makes local LLMs practical: Ollama, LM Studio and llama.cpp all consume GGUF, and it runs well on CPU and Apple Silicon where GPU-oriented formats do not. The successor to the earlier GGML format."],
 k:["One self-contained file: weights, tokeniser and config together.",
    "Quant names read as `Q<bits>_<variant>` — `Q4_K_M` is the common default.",
    "CPU and Apple Silicon friendly; the format for local and edge deployment.",
    "Used by llama.cpp, Ollama and LM Studio. Replaced GGML."],
 x:{lang:"bash", code:"# Q4_K_M is the usual quality/size compromise\nollama run llama3.1:8b-instruct-q4_K_M\n\n# Q8_0  ~8 bits, near-lossless, larger\n# Q4_K_M ~4 bits, the common default\n# Q2_K   very small, noticeably degraded"},
 r:["Quantisation","GPTQ","AWQ","Ollama"]},

{t:"Pruning", d:"Removing weights that contribute little, to make a model smaller or faster.", l:"advanced", g:["quantisation"],
 b:["Unstructured pruning zeroes individual weights wherever they are small. It shrinks the model on paper but needs sparse-matrix kernels to translate into real speed, and on standard hardware it often does not.",
    "Structured pruning removes whole units — attention heads, channels, even layers — which leaves a smaller dense model that is genuinely faster everywhere with no special kernels. That is the practical difference, and it is the distinction worth stating when the topic comes up alongside quantisation and distillation."],
 k:["Unstructured: zero individual weights. Needs sparse kernels to gain speed.",
    "Structured: remove whole heads, channels or layers. Genuinely faster anywhere.",
    "Usually needs a short fine-tune afterwards to recover quality.",
    "One of three compression routes, with quantisation and distillation."],
 x:{lang:"text", code:"unstructured   scattered zeros -> smaller file, often no speedup\nstructured     whole heads/channels gone -> smaller AND faster\n\ntypical flow: prune -> brief fine-tune -> quantise"},
 r:["Quantisation","Knowledge Distillation","Mixture of Experts","Model Compression"]},

{t:"Beam Search", d:"Decoding that keeps the k most likely partial sequences at each step rather than committing to one token.", l:"intermediate", g:["decoding"],
 b:["Greedy decoding takes the best token at every step and can therefore miss a sequence that begins with a slightly worse token but ends up far more likely overall. Beam search carries k candidate sequences forward, expands each, and keeps the best k by total log-probability.",
    "It genuinely helps where there is a single correct output — translation, summarisation — and it is a poor fit for open-ended chat, where it reliably produces bland, generic text. Higher-probability sequences are duller sequences, which is why sampling won for conversation."],
 k:["Tracks the k best *sequences*, not the k best next tokens.",
    "Better for translation and summarisation, where one right answer exists.",
    "Produces bland, generic text in open-ended generation — avoid for chat.",
    "Needs length normalisation, or it systematically prefers short outputs."],
 x:{lang:"text", code:"greedy   the -> cat -> sat            one path, locally best\nbeam(3)  keeps 3 whole sequences, scored by total log-prob\n\ntranslation/summarisation: beam helps\nopen-ended chat: beam is noticeably boring"},
 r:["Greedy Decoding","Top-p Sampling","Temperature","Speculative Decoding"]},

{t:"Min-p Sampling", d:"Keeping tokens whose probability is at least a fraction p of the top token's probability.", l:"advanced", g:["decoding"],
 b:["Top-k always keeps a fixed number of tokens regardless of how confident the model is; top-p keeps a fixed cumulative mass. Min-p instead sets the threshold relative to the most likely token: with p = 0.1, anything at least a tenth as likely as the best option survives.",
    "This adapts more sensibly at high temperature. When the model is confident the cut is aggressive and only near-ties survive; when it is genuinely uncertain the pool widens naturally. It is newer than top-p and is gaining ground in creative-writing setups where high temperature is wanted without incoherence."],
 k:["Threshold is relative to the top token: keep anything ≥ p × p_max.",
    "Adapts better than top-p when temperature is high.",
    "Confident model → aggressive cut; uncertain model → wider pool.",
    "Typical values 0.05–0.1. Newer, and not yet the universal default."],
 x:{lang:"text", code:"probs      0.60  0.20  0.05  0.03  0.01 ...\nmin_p=0.1  threshold = 0.1 x 0.60 = 0.06\nkeep       0.60  0.20                       <- two tokens\n\nif the top token were 0.15, the threshold falls to 0.015\nand many more tokens survive"},
 r:["Top-p Sampling","Top-k Sampling","Temperature","Greedy Decoding"]},

{t:"Continuous Batching", d:"Admitting and evicting requests every iteration instead of waiting for a whole batch to finish together.", l:"advanced", g:["serving"],
 b:["Static batching runs a fixed group of requests until all of them finish, so a short reply sits idle waiting for a long one and the GPU is badly underused. Continuous batching operates per decoding step: any sequence that has finished is evicted immediately and a waiting request takes its slot.",
    "The published gains are large. Anyscale's widely cited benchmark reported up to 23× throughput over naive HuggingFace serving when combined with PagedAttention, with roughly 8× attributable to continuous batching alone. It is the default in vLLM and the main reason self-hosted serving became economical."],
 k:["Per-iteration scheduling: evict finished sequences, admit waiting ones.",
    "Static batching wastes the GPU while short replies wait for long ones.",
    "Roughly 8× throughput alone; up to 23× combined with PagedAttention.",
    "The default in vLLM, TGI and SGLang."],
 x:{lang:"text", code:"static      [A....][B..][C.......]  all wait for C\n            GPU idle for A and B\n\ncontinuous  A done -> D admitted immediately\n            B done -> E admitted immediately\n            the batch stays full every step"},
 r:["PagedAttention","vLLM","Prefill","Decode"]},

{t:"PagedAttention", d:"Managing the KV cache in small fixed-size pages, the way an operating system manages virtual memory.", l:"advanced", g:["serving"],
 b:["The naive approach reserves a contiguous block big enough for each request's maximum possible length, so a request that stops early wastes almost all of it. Reported utilisation in such systems was often under a third. PagedAttention instead allocates small fixed pages on demand, with a per-sequence page table mapping logical positions to physical pages.",
    "Because pages are indirected through a table, they can also be shared. Several sequences generated from one prompt can point at the same prefix pages rather than duplicating them, which is what makes parallel sampling and beam search cheap. It is the core innovation in vLLM."],
 k:["KV cache in small fixed pages with a per-sequence page table.",
    "Removes the waste of reserving max-length contiguous blocks.",
    "Pages can be shared, so a common prefix is stored once.",
    "The central idea in vLLM; combines with continuous batching."],
 x:{lang:"text", code:"naive   [==== reserved for 2048 tokens ====]\n        [used: 100] [                waste ]\n\npaged   [pg0][pg1][pg2]  allocated as needed\n        page table maps logical -> physical\n        shared prefixes point at the SAME pages"},
 r:["KV Cache","vLLM","Continuous Batching","Prefix Caching"]},

{t:"Prefix Caching", d:"Reusing the cached KV state of a shared prompt prefix instead of recomputing it on every request.", l:"advanced", g:["serving"],
 b:["A long system prompt, a few-shot block or a retrieved document is often identical across thousands of requests, and by default every request reprocesses it from scratch. Caching the KV state of that prefix means only the new suffix needs computing.",
    "The consequence is commercial as well as technical: providers expose this with steep discounts on cached input tokens, which materially changes the arithmetic when comparing RAG against stuffing everything into a long context. The engineering implication is concrete — put the stable part of your prompt first, because a cache hit requires an exact prefix match."],
 k:["Caches the KV state of a shared prompt prefix across requests.",
    "**Put the stable part of the prompt first** — matching is prefix-exact.",
    "Providers discount cached input tokens heavily; it is a major cost lever.",
    "SGLang's RadixAttention generalises it to a prefix tree across all requests."],
 x:{lang:"text", code:"GOOD   [ long stable system prompt ][ user question ]\n         ^ cached once, reused                ^ recomputed\n\nBAD    [ user question ][ long stable system prompt ]\n         ^ changes every time -> nothing is reusable"},
 r:["KV Cache","PagedAttention","Semantic Caching","Context Window"]},

{t:"Chunked Prefill", d:"Splitting a long prompt's prefill into pieces and interleaving them with other requests' decode steps.", l:"advanced", g:["serving"],
 b:["Prefill is compute-heavy and runs the whole prompt in one pass. When a very long prompt arrives, that single pass monopolises the GPU and every other user's token generation stalls — their inter-token latency spikes even though the server is not overloaded.",
    "Chunked prefill breaks the prompt into segments and schedules them between decode steps, so the long request progresses while everyone else keeps streaming. It trades a slightly slower time-to-first-token for that long request against much steadier latency for everyone else."],
 k:["Splits a long prefill into chunks interleaved with decode steps.",
    "Stops one long prompt from stalling every other user's streaming.",
    "Trades slightly worse TTFT for that request against far steadier p99 overall.",
    "Supported in vLLM and SGLang."],
 x:{lang:"text", code:"without  [------- 8k prefill -------] then decode for everyone\n         other users see a latency spike\n\nwith     [prefill 1/4] decode decode [prefill 2/4] decode ...\n         everyone keeps streaming"},
 r:["Prefill","Decode","Continuous Batching","vLLM"]},

{t:"Prefill", d:"The first phase of inference, processing the whole prompt in parallel to produce the first token.", l:"advanced", g:["serving"],
 b:["Every prompt token goes through the model together, so the GPU's matrix units are saturated and the phase is **compute-bound**. Its duration is what the user experiences as time to first token, and it grows with prompt length.",
    "The distinction from decode is one of the clearest signals of an informed candidate. Because prefill is compute-bound, batching and quantisation help it comparatively little — while both help decode a great deal. Optimising the wrong phase is a common mistake."],
 k:["Processes all prompt tokens in parallel; **compute-bound**.",
    "Determines time to first token (TTFT).",
    "Cost scales with prompt length; prefix caching is the main lever.",
    "Batching and quantisation help it far less than they help decode."],
 x:{lang:"text", code:"PREFILL   all n prompt tokens at once   compute-bound   -> TTFT\nDECODE    one token at a time           memory-bound    -> tokens/sec\n\nquantisation helps decode a lot, prefill little\nbatching     helps decode a lot, prefill little"},
 r:["Decode","Time To First Token","Prefix Caching","Chunked Prefill"]},

{t:"Decode", d:"The token-by-token generation phase, where speed is limited by memory bandwidth rather than compute.", l:"advanced", g:["serving"],
 b:["Each generated token requires reading the entire set of model weights from memory to do a comparatively tiny amount of arithmetic. The GPU spends most of its time waiting on memory, which makes decode **memory-bandwidth-bound**.",
    "Two consequences follow directly and are worth being able to state. Quantisation helps a lot, because halving the bytes read roughly halves the time. Batching helps a lot, because one weight read is amortised across many sequences. Both are much less effective on prefill, which is compute-bound instead."],
 k:["One token per forward pass; **memory-bandwidth-bound**, not compute-bound.",
    "Determines inter-token latency and tokens per second.",
    "Quantisation helps here most — fewer bytes to read per token.",
    "Batching amortises one weight read across many sequences."],
 x:{lang:"text", code:"per generated token: read ~all weights, do a little maths\n  8B model at BF16 = 16 GB read PER TOKEN\n  at 2 TB/s that is ~125 tokens/sec, memory-limited\n\n-> INT4 quartes the bytes and roughly quadruples the ceiling"},
 r:["Prefill","KV Cache","Quantisation","Time To First Token"]},

{t:"Supervised Fine-Tuning", a:"SFT", d:"Training a pretrained model on instruction-and-response pairs so it behaves as an assistant.", l:"intermediate", g:["training"],
 b:["A pretrained model only continues text; it does not answer questions, because nothing has taught it that a question should be followed by an answer rather than by more questions. SFT is the stage that teaches the format, using curated examples of a prompt and the desired reply.",
    "It is ordinary next-token training with a curated dataset — the loss is usually computed on the response tokens only. Data quality dominates: a few thousand carefully written examples routinely beat hundreds of thousands of scraped ones. SFT comes after pretraining and before preference tuning."],
 k:["Teaches instruction-following format; next-token loss on the response only.",
    "Quality beats quantity — thousands of good examples beat a scraped million.",
    "Sits between pretraining and preference tuning (RLHF/DPO) in the ladder.",
    "LoRA makes it affordable on a single GPU."],
 x:{lang:"json", code:"{\"messages\": [\n  {\"role\": \"user\", \"content\": \"Summarise this ticket.\"},\n  {\"role\": \"assistant\", \"content\": \"Customer cannot log in after...\"}\n]}\n\n// loss is computed on the assistant turn only"},
 r:["RLHF","DPO","LoRA","Instruction Tuning"]},

{t:"Reward Model", d:"A model trained on human preference comparisons to score how good a response is.", l:"advanced", g:["training"],
 b:["Humans find it much easier to say which of two responses is better than to assign an absolute quality score. A reward model is trained on exactly those pairwise comparisons, learning to output a scalar that ranks responses the way people did.",
    "In RLHF it becomes the objective the policy is optimised against, with a KL penalty keeping the policy near the original model. Its weaknesses are well documented: it is a learned proxy, so the policy can find inputs that score highly without being genuinely good — reward hacking. DPO's appeal is that it removes this separate model entirely."],
 k:["Trained on pairwise human preferences, not absolute scores.",
    "Outputs a scalar reward used as the RL objective in RLHF.",
    "Reward hacking is the failure mode: high score, poor actual quality.",
    "DPO skips it, which is much of why DPO is simpler and more stable."],
 x:{lang:"text", code:"human sees A and B, picks A\n  -> train so that  reward(A) > reward(B)\n\nRLHF: maximise reward(policy output) - beta * KL(policy || base)\n                                          ^ stops it drifting too far"},
 r:["RLHF","DPO","GRPO","KL Divergence"]},

{t:"GRPO", a:"Group Relative Policy Optimisation", d:"Preference training that scores a group of sampled responses and uses their mean as the baseline, removing the value network.", l:"advanced", g:["training"],
 b:["PPO needs a learned value model to estimate the expected reward, so it can judge whether an outcome was better than average — that is four models in memory at once. GRPO samples several responses to the same prompt and simply uses the group's mean score as the baseline instead.",
    "Removing the critic saves memory and a great deal of instability. It became prominent through DeepSeek's reasoning models, where sampling many chains of thought per prompt is natural anyway, making the group baseline nearly free."],
 k:["Uses the mean score of a sampled group as the baseline; no learned value model.",
    "Cheaper and more stable than PPO, which needs four models resident.",
    "Popularised by DeepSeek for reasoning-model training.",
    "Fits naturally where you already sample many responses per prompt."],
 x:{lang:"text", code:"prompt -> sample 8 responses -> score each\nbaseline = mean(scores)\nadvantage_i = score_i - baseline\n\nno critic network needed -- the group IS the baseline"},
 r:["RLHF","DPO","Reward Model","ORPO"]},

{t:"ORPO", a:"Odds Ratio Preference Optimisation", d:"Folding preference alignment into supervised fine-tuning so both happen in a single stage.", l:"advanced", g:["training"],
 b:["The usual pipeline runs SFT and then preference tuning as two separate stages with two datasets and two training runs. ORPO adds an odds-ratio penalty term to the SFT loss itself, so the model simultaneously learns to produce the chosen response and to disprefer the rejected one.",
    "One stage instead of two means less compute and less pipeline complexity, and it needs no reference model at all — a memory saving over DPO. The trade-off is a little less control over the two objectives, since you can no longer tune them independently."],
 k:["Combines SFT and preference alignment into one loss and one stage.",
    "No reference model required, unlike DPO — a real memory saving.",
    "Simpler pipeline; less independent control over the two objectives.",
    "In the same family as DPO and KTO: RLHF without reinforcement learning."],
 x:{lang:"text", code:"standard   SFT run  ->  DPO run          two stages, two datasets\nORPO       one run: SFT loss + odds-ratio penalty on rejected\n\nno reward model, no reference model"},
 r:["DPO","KTO","Supervised Fine-Tuning","RLHF"]},

{t:"KTO", a:"Kahneman-Tversky Optimisation", d:"Preference training that needs only a binary good-or-bad label per response, not paired comparisons.", l:"advanced", g:["training"],
 b:["DPO requires triples of prompt, chosen response and rejected response, and producing those pairs is the expensive part of preference data collection. KTO needs only a thumbs-up or thumbs-down on individual responses — which is precisely the signal a deployed product already collects for free.",
    "The name comes from prospect theory: the loss is asymmetric, treating losses as weightier than equivalent gains, mirroring how people actually judge outcomes. Slightly weaker than DPO given identical data, and far cheaper to gather data for, which usually wins in practice."],
 k:["Needs only binary good/bad labels, not chosen-versus-rejected pairs.",
    "Uses the thumbs-up/down signal a live product already produces.",
    "Asymmetric loss drawn from prospect theory — losses weigh more than gains.",
    "Slightly behind DPO on equal data, but the data is far cheaper to collect."],
 x:{lang:"text", code:"DPO needs   (prompt, chosen, rejected)     expensive to collect\nKTO needs   (prompt, response, good?)      you already have this\n\nthumbs up/down in your product = a KTO dataset"},
 r:["DPO","ORPO","RLHF","Reward Model"]}

]);
