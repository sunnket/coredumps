/* Crash Course, part 3 — how the web actually works, in plain words.
   The vocabulary that shows up in every backend conversation and every log line. */

TD.add("crash-course", [

{t:"Client", d:"Whatever is asking — a browser, a mobile app, a script, another service.", l:"core", g:["web","basics","crash-course"],
 b:["Client and server are roles, not machines. The thing that starts the conversation is the client; the thing that answers is the server. Your browser is a client to a website, and that website's backend is in turn a client to its database.",
    "The rule that follows from this is the one people learn the hard way: never trust the client. Anything running on someone else's device can be modified, inspected or replayed. Validation in the browser is for helpfulness; the real check has to happen on the server, where you control the code."],
 k:["A role, not a device — the side that initiates the request.",
    "One program can be a client and a server at the same time.",
    "Never trust client-supplied data; validate again on the server.",
    "Anything shipped to the browser is readable, including your JavaScript."],
 r:["Server", "Request", "Response", "API Key"]},

{t:"Server", d:"Whatever is listening and answering — a program waiting on a port for requests to arrive.", l:"core", g:["web","basics","crash-course"],
 b:["The word gets used for both the software and the machine it runs on, which causes needless confusion. Here it means the program: it binds to a port, waits, and for each request that arrives it does some work and sends a response back.",
    "One machine can run many servers on different ports, which is exactly what your laptop does during development. A server that handles many requests at once does so either with threads or, like Node and modern Python, with an event loop that switches between requests whenever one is waiting on the network."],
 k:["A program listening on a port, not necessarily a physical box.",
    "Many servers coexist on one machine via different ports.",
    "Concurrency comes from threads or an event loop.",
    "Statelessness is what allows you to run many identical copies."],
 r:["Client", "Port", "Localhost", "Stateless"]},

{t:"Request", d:"A message from a client asking for something — a method, a path, some headers and sometimes a body.", l:"core", g:["web","basics","crash-course"],
 b:["Every HTTP request has the same four parts. The method says what kind of action it is: GET to read, POST to create, PUT or PATCH to update, DELETE to remove. The path says which resource. The headers carry metadata like authentication and content type. The body carries the data, on the methods that have one.",
    "The distinction worth internalising early is that GET requests are supposed to be safe — they read, they never change anything. Browsers, proxies and crawlers all assume this, and a GET endpoint that deletes something will eventually be triggered by a link preview or a prefetch."],
 k:["Method, path, headers, and sometimes a body.",
    "GET reads, POST creates, PUT/PATCH update, DELETE removes.",
    "GET must never change state — everything on the internet assumes it.",
    "Open your browser's network tab: every request is visible there."],
 x:{lang:"bash", code:"curl -X POST https://api.example.com/v1/terms \\\n  -H \"Authorization: Bearer $TOKEN\" \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"name\": \"Closure\", \"level\": \"core\"}'"},
 r:["Response", "HTTP Header", "Payload", "Endpoint", "Query Parameter"]},

{t:"Response", d:"What the server sends back — a status code, headers, and usually a body.", l:"core", g:["web","basics","crash-course"],
 b:["The status code is the summary and it is worth knowing by family. Two-hundreds mean it worked. Three-hundreds mean it moved. Four-hundreds mean the client got it wrong — bad input, not logged in, not allowed, not found. Five-hundreds mean the server broke, which is your problem rather than the caller's.",
    "The most useful habit when something fails is to read the status before reading anything else. A 401 and a 500 look identical in a vague error message on screen, and they have completely different causes and completely different fixes."],
 k:["Status code, headers, body.",
    "4xx is the caller's fault; 5xx is yours.",
    "401 means not authenticated; 403 means authenticated but not allowed.",
    "Return useful error bodies — a bare 400 tells the caller nothing."],
 r:["Request", "Status Code", "Payload", "HTTP Header"]},

{t:"Endpoint", d:"One specific address on an API that does one specific thing.", l:"core", g:["web","basics","crash-course"],
 b:["A URL plus a method is an endpoint. GET on slash-users lists users; POST on the same path creates one; GET on slash-users-slash-7 returns a single user. Same path, different methods, different behaviour.",
    "Good endpoint design is mostly consistency: plural nouns for collections, the identifier in the path, filters in the query string, and the version at the front so you can change your mind later without breaking every caller."],
 k:["A path plus a method — the same path can host several endpoints.",
    "Nouns for resources; the method supplies the verb.",
    "Filters belong in the query string, identifiers in the path.",
    "Version the API from day one; breaking changes are inevitable."],
 r:["Request", "REST", "API", "Query Parameter"]},

{t:"Payload", d:"The actual data inside a request or response, as opposed to the headers wrapped around it.", l:"core", g:["web","basics","crash-course"],
 b:["When someone asks what the payload looks like, they mean the body — usually JSON. It is the content; everything else is envelope. The content-type header declares what format the payload is in, and getting it wrong is why a perfectly valid body comes back as a 400.",
    "Two practical concerns follow the payload around. Size, because large bodies are slow and most servers cap them by default, which is why file uploads use multipart or a pre-signed direct upload instead. And trust, because a payload is client-supplied data and must be validated against a schema before anything touches it."],
 k:["The body — the data, not the metadata.",
    "Content-Type declares the format; mismatches cause 400s.",
    "Servers cap body size — large files need a different route.",
    "Validate every payload against a schema before use."],
 r:["Request", "Response", "JSON", "Serialisation"]},

{t:"HTTP Header", d:"A key-value line of metadata attached to a request or response — auth, content type, caching, cookies.", l:"core", g:["web","basics","crash-course"],
 b:["Headers describe the message rather than being the message. Authorization carries the token. Content-Type says what format the body is in. Accept says what the client can handle. Cache-Control tells browsers and proxies how long they may reuse the response.",
    "Nearly every mysterious API failure is a header problem: a missing Authorization, a Content-Type of text when the server wanted JSON, or a CORS rejection because the response did not include the header permitting your origin. Reading the headers in the network tab resolves most of these in under a minute."],
 k:["Metadata about the message, not the message itself.",
    "Authorization, Content-Type, Accept, Cache-Control, Cookie.",
    "CORS is enforced entirely through response headers.",
    "Headers are visible — never put secrets in them beyond the token itself."],
 x:{lang:"bash", code:"curl -i https://api.example.com/health\n\n# HTTP/2 200\n# content-type: application/json\n# cache-control: max-age=60\n# x-request-id: 7f3c2a"},
 r:["Request", "Response", "CORS", "API Key", "Cookie"]},

{t:"Query Parameter", d:"The key-value pairs after the question mark in a URL — filters, pages, sort orders.", l:"core", g:["web","basics","crash-course"],
 b:["Everything after the question mark, separated by ampersands. They belong to GET requests and describe how you want the collection shaped: which page, how many per page, sorted by what, filtered how.",
    "Two things bite. Values must be URL-encoded, so a space becomes a plus or a percent-twenty and an ampersand inside a value must be escaped or it splits the parameter. And query strings appear in browser history, server logs and referrer headers — which is exactly why a token or a password must never travel in one."],
 k:["After the `?`, joined by `&` — filters, paging, sorting.",
    "Always URL-encode values; never build them by hand.",
    "Query strings are logged everywhere — no secrets in them.",
    "Path identifies the resource; query shapes the result."],
 x:{lang:"bash", code:"GET /v1/terms?category=genai&level=core&page=2&per_page=20\n\n# encoded properly\nGET /search?q=machine%20learning&sort=-created_at"},
 r:["Endpoint", "Request", "Encoding", "Pagination"]},

{t:"Serialization", d:"Turning an in-memory object into a string or bytes you can send or store — and back again.", l:"core", g:["web","basics","crash-course"],
 b:["A Python object cannot travel over a network. Serialising converts it into a format both sides agree on, usually JSON as text; deserialising rebuilds an object at the other end. That round trip is happening on every API call you make.",
    "Things get lost in the crossing. JSON has no date type, so dates become strings and must be parsed back deliberately — ISO 8601 in UTC is the format that causes the fewest arguments. It has no integer/float distinction the way Python does, and very large integers lose precision in JavaScript, which is why identifiers are so often sent as strings."],
 k:["Object to text or bytes and back — the cost of crossing a boundary.",
    "JSON has no date type: use ISO 8601 in UTC.",
    "Large integers lose precision in JavaScript — send IDs as strings.",
    "Never deserialise untrusted data with pickle or eval."],
 x:{lang:"python", code:"import json\nfrom datetime import datetime, timezone\n\npayload = {\"id\": \"7\", \"at\": datetime.now(timezone.utc).isoformat()}\nwire = json.dumps(payload)         # object -> text\nback = json.loads(wire)            # text -> object"},
 r:["JSON", "Payload", "Data Type", "Timestamp"]},

{t:"Cache", d:"A copy of an answer kept somewhere faster, so you do not have to work it out again.", l:"core", g:["web","basics","crash-course"],
 b:["Caches sit at every layer: the CPU, the browser, a CDN, Redis in front of the database, the query planner inside it. The trade is always the same — you exchange freshness for speed, and you must decide how stale is acceptable.",
    "The famous joke is that there are two hard problems in computer science, and cache invalidation is one of them. Knowing when a cached answer has become wrong is genuinely difficult, which is why the practical advice is to prefer short expiry times over clever invalidation logic, and to cache only what is actually slow."],
 k:["Trades freshness for speed — decide how stale is acceptable.",
    "Time-based expiry is easier to reason about than manual invalidation.",
    "Measure first: caching the wrong layer helps nothing.",
    "Never cache per-user data in a shared cache without keying by user."],
 r:["Caching", "Latency", "Throughput", "CDN"]},

{t:"Throughput", d:"How much work a system gets through per unit of time — requests per second, rows per minute.", l:"core", g:["web","basics","crash-course"],
 b:["Throughput and latency are different questions, and improving one often hurts the other. Latency asks how long one request takes; throughput asks how many you can finish per second. Batching raises throughput and raises latency at the same time, because early items wait for the batch to fill.",
    "The number people should quote is not the average. Averages hide the requests that hurt, so real systems are measured at percentiles: p50 for the typical case and p95 or p99 for the tail. A service with a 100 ms average and a 4-second p99 is a service where one user in a hundred is having a bad time."],
 k:["Work per unit of time; latency is time per unit of work.",
    "Batching raises throughput and raises latency.",
    "Quote percentiles, not averages — p50, p95, p99.",
    "The bottleneck sets the ceiling; nothing else matters until it moves."],
 r:["Latency", "Bottleneck", "Bandwidth", "Metric"]},

{t:"Bandwidth", d:"How much data can move through a connection per second — the width of the pipe.", l:"core", g:["web","basics","crash-course"],
 b:["Bandwidth is capacity; latency is delay. A satellite link can have enormous bandwidth and terrible latency: you can push a lot through it, but each round trip takes half a second. Confusing the two leads to the wrong optimisation entirely.",
    "For most web pages the fix is not more bandwidth but less data and fewer round trips: compress responses, resize images properly, and stop shipping megabytes of unused JavaScript. On mobile networks latency dominates, so cutting the number of requests usually helps more than shrinking each one."],
 k:["Capacity per second, not delay.",
    "High bandwidth plus high latency is common — satellite, mobile.",
    "Compress, resize and bundle to use less of it.",
    "On mobile, round trips usually cost more than bytes."],
 r:["Latency", "Throughput", "CDN"]},

{t:"Bottleneck", d:"The one slowest part that limits everything else — the only thing worth optimising.", l:"core", g:["web","basics","practice","crash-course"],
 b:["A pipeline runs at the speed of its slowest stage. Making a fast stage faster changes nothing at all, which is why optimisation guided by intuition so often produces no measurable improvement. Measure first: profile, look at the trace, find where the time actually goes.",
    "In web applications the bottleneck is almost never the language. It is usually the database — a missing index, or a query inside a loop firing a thousand times. Look there before rewriting anything in a faster language."],
 k:["The slowest stage sets the overall speed.",
    "Profile before optimising; intuition is usually wrong.",
    "Usual suspects: missing index, N+1 queries, an unbatched network call.",
    "Fix it and the bottleneck moves — measure again."],
 r:["Throughput", "Latency", "Metric", "Caching"]},

{t:"Uptime", d:"The share of time a system is actually working — usually quoted in nines.", l:"core", g:["web","devops","crash-course"],
 b:["Ninety-nine percent sounds excellent until you convert it: that is more than three and a half days of downtime a year. Three nines is about nine hours; four nines is under an hour. Each additional nine costs roughly an order of magnitude more engineering.",
    "Which is why the honest question is not how to reach five nines but how much downtime the business can genuinely tolerate, and what the failure looks like. Degrading gracefully — serving stale cached data when the database is down — is usually cheaper and kinder than chasing another nine."],
 k:["99% is 3.65 days a year; 99.9% is 8.8 hours; 99.99% is 53 minutes.",
    "Each nine costs roughly ten times more than the last.",
    "Partial degradation beats total failure.",
    "Measure from the user's side, not from the server's."],
 r:["SLA, SLO and SLI","Monitoring","Metric","Production Environment"]},

{t:"Metric", d:"A number tracked over time that tells you how a system is behaving.", l:"core", g:["web","devops","crash-course"],
 b:["Request rate, error rate, latency percentiles, queue depth, memory. Metrics are cheap to collect and cheap to keep, which makes them the backbone of monitoring — unlike logs, which are detailed and expensive at volume.",
    "The four golden signals are a good default set: latency, traffic, errors and saturation. A dashboard of forty graphs that nobody looks at is worth less than four that someone checks after every deploy, and an alert nobody acts on trains everyone to ignore alerts."],
 k:["A number over time; cheap to store and to graph.",
    "Golden signals: latency, traffic, errors, saturation.",
    "Percentiles over averages, always.",
    "Alert only on things a human should act on right now."],
 r:["Monitoring", "Logging", "Uptime", "Throughput"]},

{t:"Polling", d:"Asking the server again and again whether anything has changed.", l:"core", g:["web","basics","crash-course"],
 b:["The simplest way to keep a screen fresh: ask every few seconds, redraw if something is new. It is trivial to build and works through every proxy and firewall, which is why it is still everywhere.",
    "The cost is waste. Most of those requests return nothing changed, and multiplied across every user it becomes real load. Poll when updates are rare and delay is acceptable; move to server-sent events or websockets when they are frequent and the user expects immediacy."],
 k:["Repeatedly asking rather than being told.",
    "Simple, robust, and wasteful at scale.",
    "Back off when the tab is hidden or the answer keeps being no.",
    "For frequent updates prefer SSE or websockets."],
 x:{lang:"javascript", code:"const id = setInterval(async () => {\n  if (document.hidden) return;         // do not poll a hidden tab\n  const r = await fetch(\"/api/status\");\n  render(await r.json());\n}, 5000);"},
 r:["Long Polling", "WebSocket", "Throttle", "Bandwidth"]},

{t:"Long Polling", d:"A request the server deliberately holds open until it has something to say.", l:"core", g:["web","crash-course"],
 b:["A middle ground between polling and a real push channel. The client asks, and instead of answering nothing-new immediately, the server keeps the connection open until an update arrives or a timeout hits, then responds. The client immediately asks again.",
    "It gives near-instant delivery over plain HTTP, which is why it was the standard trick before websockets. The costs are that every waiting client occupies a connection, and intermediaries may close idle connections regardless — so timeouts have to be shorter than whatever the network tolerates."],
 k:["The server holds the request until there is news.",
    "Near-real-time without websockets.",
    "Each waiting client ties up a connection.",
    "Keep the hold shorter than proxy idle timeouts."],
 r:["Polling", "WebSocket", "Timeout", "Server"]},

{t:"Debounce", d:"Waiting until someone stops doing a thing before you react — one action after the flurry ends.", l:"core", g:["web","frontend","crash-course"],
 b:["A search box that fires a request on every keystroke sends ten requests for a five-letter word, and the answers arrive out of order. Debouncing waits until typing pauses for a few hundred milliseconds and then sends one request, for the final text.",
    "The same pattern applies to window resize handlers, autosave and validation. Roughly 300 milliseconds is the usual sweet spot for typing: long enough to collapse a burst, short enough that it still feels immediate."],
 k:["Fires once, after the activity stops.",
    "Ideal for search-as-you-type, autosave and resize handlers.",
    "About 300 ms feels responsive for typing.",
    "Cancel the in-flight request too, or stale answers overwrite fresh ones."],
 x:{lang:"javascript", code:"function debounce(fn, ms = 300) {\n  let t;\n  return (...args) => {\n    clearTimeout(t);\n    t = setTimeout(() => fn(...args), ms);\n  };\n}\ninput.addEventListener(\"input\", debounce(search, 300));"},
 r:["Throttle", "Rate Limiting", "Polling"]},

{t:"Throttle", d:"Allowing an action at most once per interval, no matter how often it is triggered.", l:"core", g:["web","frontend","crash-course"],
 b:["Where debouncing waits for quiet, throttling guarantees a steady drip. A scroll handler can fire hundreds of times a second; throttled to once every hundred milliseconds it still tracks the scroll smoothly while doing a fraction of the work.",
    "Choose by asking whether you need the final value or a regular sample. Search wants the final value, so debounce. Scroll position, drag tracking and progress reporting want regular samples, so throttle."],
 k:["At most once per interval; debounce waits for silence.",
    "Right for scroll, drag, mousemove and progress updates.",
    "Debounce for the final value, throttle for a running sample.",
    "The same idea at the API level is rate limiting."],
 r:["Debounce", "Rate Limiting", "Polling"]},

{t:"Blocking", d:"Code that stops and waits — nothing else on that thread happens until it finishes.", l:"core", g:["web","basics","crash-course"],
 b:["A blocking read waits for the disk. A blocking HTTP call waits for the network. During that wait the thread does nothing useful, and if it happens to be the only thread — as it is in JavaScript — the entire program freezes, including the user interface.",
    "Non-blocking code hands the waiting to the operating system and gets on with something else, resuming when the answer arrives. That is what async and await express: this line waits, but the thread does not. Calling a blocking function inside an async server is a classic way to make a fast server mysteriously slow."],
 k:["The thread stops and waits, doing nothing.",
    "In single-threaded runtimes it freezes everything.",
    "Async lets the thread work while a call is waiting.",
    "One blocking call inside an async server stalls every request on it."],
 x:{lang:"javascript", code:"// blocks the only thread -- UI freezes\nconst data = fs.readFileSync(\"big.json\");\n\n// yields while waiting -- everything else keeps running\nconst data = await fs.promises.readFile(\"big.json\");"},
 r:["Concurrency", "Event Loop", "Promise", "Latency"]},

{t:"Stateless", d:"A server that remembers nothing between requests — everything it needs arrives with the request.", l:"core", g:["web","backend","crash-course"],
 b:["If each request carries its own identity and context, any copy of the server can handle any request. That is what makes horizontal scaling possible: put ten identical servers behind a load balancer and it does not matter which one you reach.",
    "The moment a server keeps something in local memory — a session, an upload in progress, a cache of who is logged in — those copies stop being interchangeable, and users start getting logged out at random as the balancer moves them around. The fix is to push state outward: into a token, a shared cache like Redis, or the database."],
 k:["No memory of previous requests on the server itself.",
    "Any instance can serve any request — that is what enables scaling.",
    "Local in-memory session state breaks the moment you run two copies.",
    "Push state into a token, a shared cache or the database."],
 r:["Server", "Horizontal Scaling", "Session", "Load Balancer"]},

{t:"SSL Certificate", a:"TLS certificate", d:"The file that proves a domain is who it says it is, and is what puts the padlock in the address bar.", l:"core", g:["web","security","crash-course"],
 b:["A certificate binds a domain name to a public key and is signed by an authority the browser already trusts. The browser checks the signature chain, checks the name matches, checks it has not expired — and only then does it agree to encrypt the connection and show the padlock.",
    "In practice the whole thing is free and automatic now via Let's Encrypt, and the only failure that still happens regularly is expiry, because a renewal job broke months earlier and nobody noticed. Certificate expiry is one of the most reliably preventable outages in existence: monitor the expiry date and alert weeks ahead."],
 k:["Proves domain ownership and enables the encrypted connection.",
    "Browsers verify the chain, the name and the expiry date.",
    "Free and automatic with Let's Encrypt — renew every 90 days.",
    "Monitor expiry; a lapsed certificate takes the whole site down."],
 r:["TLS", "HTTPS", "Encryption", "Uptime"]},

{t:"UUID", a:"Universally Unique Identifier", d:"A 128-bit random identifier you can generate anywhere without asking a central authority, and safely assume is unique.", l:"core", g:["web","backend","crash-course"],
 b:["Auto-incrementing integer identifiers require a single source of truth and they leak information: number 1042 tells any competitor roughly how many customers you have, and makes it trivial to walk through your records one by one. A UUID has neither problem.",
    "The cost is size and ordering. UUIDs are 16 bytes rather than 4 or 8, and random ones scatter across a database index, which hurts insert performance on large tables. Version 7, which puts a timestamp at the front, keeps the uniqueness while restoring roughly sortable order — it is the sensible default for new systems."],
 k:["Generated anywhere, no coordination, effectively never collides.",
    "Does not leak record counts or allow enumeration.",
    "Random v4 fragments database indexes on large tables.",
    "v7 is time-ordered — prefer it for new primary keys."],
 x:{lang:"python", code:"import uuid\nprint(uuid.uuid4())\n# 3f2504e0-4f89-41d3-9a0c-0305e82c3301"},
 r:["Primary Key", "Relational Database", "Index"]},

{t:"Timestamp", d:"A recorded moment in time — and a surprisingly rich source of bugs.", l:"core", g:["web","basics","crash-course"],
 b:["Two representations dominate. Unix time is the number of seconds since the start of 1970 in UTC: a plain integer, easy to compare and store. ISO 8601 is the readable string form, which is what belongs in JSON because it survives being read by a human.",
    "There is one rule that prevents almost every timestamp bug: store and transmit in UTC, convert to local time only at the moment of display. The second rule is to always include the offset — a timestamp with no zone is an ambiguity waiting to happen, and it will pick the worst possible moment to matter."],
 k:["Store and send UTC; convert to local only for display.",
    "Unix seconds for comparison; ISO 8601 for interchange.",
    "A timestamp with no timezone is ambiguous — always include it.",
    "Never compute durations by subtracting local wall-clock times."],
 x:{lang:"python", code:"from datetime import datetime, timezone\n\nnow = datetime.now(timezone.utc)       # always aware\nprint(now.isoformat())                 # 2026-08-23T09:41:12+00:00\nprint(int(now.timestamp()))            # 1787...  unix seconds"},
 r:["Time Zone", "Serialisation", "Data Type"]},

{t:"Time Zone", d:"The offset and rules that convert universal time into local wall-clock time — and they change without warning.", l:"core", g:["web","basics","crash-course"],
 b:["Offsets are not fixed. Daylight saving shifts them twice a year, governments change the rules with a few weeks' notice, and some zones use half-hour or 45-minute offsets. This is why you should never hard-code an offset — use a zone name like Asia/Kolkata and let the system's database supply the current rule.",
    "The consequences are genuinely strange. During a daylight-saving spring-forward, some local times do not exist; during the autumn shift, some happen twice. A daily job scheduled at 2:30 a.m. local time can be skipped or run twice a year, which is exactly why scheduled jobs belong in UTC."],
 k:["Offsets change with daylight saving and by government decree.",
    "Use zone names, never hard-coded offsets.",
    "Some local times do not exist; some occur twice.",
    "Schedule jobs in UTC and convert only for display."],
 r:["Timestamp", "Serialisation", "Encoding", "UTF-8"]},

{t:"Base64", d:"A way of writing binary data using only ordinary text characters — encoding, not encryption.", l:"core", g:["web","basics","crash-course"],
 b:["Some channels only carry text: JSON fields, email bodies, data URLs, HTTP headers. Base64 rewrites arbitrary bytes into a 64-character alphabet so they survive the trip, at a cost of roughly 33 percent more size.",
    "It provides absolutely no security. Anyone can decode it in a second, and a JWT is a good illustration: its payload is base64 and fully readable by anyone holding the token. The signature is what makes it trustworthy, not the encoding."],
 k:["Encoding for transport, not encryption — trivially reversible.",
    "Adds about 33% to the size.",
    "Used in data URLs, email attachments, JWTs and basic auth.",
    "Never treat base64 as hiding anything."],
 x:{lang:"python", code:"import base64\nblob = base64.b64encode(b\"hello\").decode()\nprint(blob)                                # aGVsbG8=\nprint(base64.b64decode(blob))              # b'hello'"},
 r:["Encoding", "JWT", "Serialisation", "Encryption"]},

{t:"Encoding", d:"The agreement about which bytes represent which characters — get it wrong and text turns to gibberish.", l:"core", g:["web","basics","crash-course"],
 b:["Text is stored as numbers, and an encoding is the lookup table. ASCII covered 128 characters. UTF-8 covers every character in every script, uses one byte for the ASCII range and more for the rest, and is the correct default for essentially everything today.",
    "Mojibake — the familiar garbled question marks and Ã© sequences — is what you get when text written in one encoding is read as another. The cure is to be explicit everywhere: UTF-8 in the file read, in the database column, in the HTTP header, and in the HTML meta tag. One link in that chain guessing wrong corrupts the lot."],
 k:["Maps bytes to characters; UTF-8 is the modern default.",
    "Garbled text means it was written and read with different tables.",
    "Be explicit at every boundary: file, database, header, page.",
    "One character is not one byte outside the ASCII range."],
 r:["Unicode", "String", "Serialisation"]},

{t:"Filtering", d:"Narrowing a set of results down to the ones that match a condition.", l:"core", g:["web","basics","crash-course"],
 b:["Every list view eventually needs it: active users only, orders in the last week, terms in one category. In code it is a filter call or a comprehension; over an API it is query parameters; in a database it is the WHERE clause.",
    "Where the filtering happens is the decision that matters. Filtering in the database sends only what is needed; fetching ten thousand rows and filtering them in application code wastes bandwidth, memory and time. As a rule, push the filter as close to the data as you can — and make sure the column you filter on has an index."],
 k:["Filter in the database, not after fetching everything.",
    "Filtered columns need an index or the query scans the table.",
    "Over an API, filters belong in the query string.",
    "Combine with pagination — a filtered list can still be enormous."],
 x:{lang:"python", code:"# fetches everything, then throws most of it away\nusers = [u for u in db.all_users() if u.active]\n\n# lets the database do the work\nusers = db.query(User).filter(User.active.is_(True)).all()"},
 r:["Query Parameter", "Pagination", "Index", "SQL"]},

{t:"TCP Three-Way Handshake", a:"SYN-ACK", d:"The three-step greeting ritual that establishes a reliable, bidirectional internet connection before data flows.", l:"core", g:["web","networking","crash-course"],
 b:["Before your browser can send an HTTP request to a server, they must establish an agreed connection over TCP. The client sends a SYN (synchronize) packet with a starting sequence number. The server replies with SYN-ACK (acknowledging the client and sending its own number). The client replies with ACK (acknowledged).",
    "Only after this three-way exchange is complete can the first byte of real application data travel. This handshake costs one full round-trip of latency (RTT). Modern protocols like TLS 1.3 and QUIC (HTTP/3) combine the transport and cryptographic handshakes to eliminate round-trip delays."],
 k:["Three steps: Client sends SYN -> Server returns SYN-ACK -> Client sends ACK.",
    "Establishes initial sequence numbers and guarantees both sides can transmit and receive.",
    "Adds 1 Round-Trip Time (RTT) of latency before any application payload is sent.",
    "HTTP/3 (QUIC) builds on UDP to combine handshake steps into a single round-trip."],
 x:{lang:"text", code:"Client                    Server\n  │                         │\n  │────── SYN (seq=100) ────►│  (1. Can you hear me?)\n  │                         │\n  │◄─── SYN-ACK (ack=101) ──│  (2. I hear you! Can you hear me?)\n  │                         │\n  │────── ACK (ack=301) ────►│  (3. I hear you too! Connected.)\n  │                         │"},
 dg:"tcp-handshake",
 r:["Client", "Server", "DNS Resolution", "HTTP Header", "Latency"]},

{t:"DNS Resolution", d:"The distributed internet phonebook that translates human-readable domain names into computer IP addresses.", l:"core", g:["web","networking","crash-course"],
 b:["Computers on the internet route packets using numeric IP addresses (like `142.250.190.46`), but humans remember domain names (like `google.com`). DNS (Domain Name System) is the global hierarchical lookup service that bridges this gap in a few milliseconds.",
    "When you type a URL, your operating system checks its local cache. If missing, it queries a Recursive Resolver, which asks Root nameservers, then TLD (Top-Level Domain) nameservers (`.com`), and finally the Authoritative nameserver for that specific domain. Responses are cached according to a TTL (Time-To-Live) value."],
 k:["Translates domain names (`example.com`) into IP addresses (`93.184.216.34`).",
    "Hierarchical lookup: Local Cache -> Recursive Resolver -> Root -> TLD -> Authoritative.",
    "TTL (Time-To-Live) controls how long resolvers can cache the IP address.",
    "DNS lookups happen over UDP port 53 for speed."],
 x:{lang:"bash", code:"# Inspect real DNS resolution steps with dig\ndig +trace termdex.com\n\n# Lookup IP directly\nnslookup github.com"},
 dg:"dns",
 r:["Client", "Server", "TCP Three-Way Handshake", "SSL Certificate", "Port"]},

{t:"Hashing vs Encryption", d:"Encryption is a two-way secret lock reversible with a key; hashing is a one-way mathematical fingerprint that cannot be reversed.", l:"core", g:["web","security","crash-course"],
 b:["Encryption takes plaintext and transforms it into ciphertext using an encryption key. Anyone with the matching decryption key can unlock and recover the original plaintext — making it ideal for messages, files, and HTTPS web traffic.",
    "Hashing is strictly one-way: a cryptographic hash function (like SHA-256 or bcrypt) crunches data into a fixed-length string of hexadecimal characters. You cannot 'decrypt' a hash back into the password; you can only hash incoming passwords and check if the fingerprints match. This is why passwords must always be hashed with salt, never encrypted."],
 k:["Encryption: two-way, reversible with a secret key (confidentiality).",
    "Hashing: one-way, irreversible mathematical digest (integrity and passwords).",
    "Store passwords with salted hashes (bcrypt, Argon2), NEVER reversible encryption.",
    "Changing 1 bit of input radically scrambles the entire hash (avalanche effect)."],
 x:{lang:"python", code:"import hashlib\n\n# Hashing (One-Way): cannot be reversed back to \"password123\"\nhash_digest = hashlib.sha256(b\"password123\").hexdigest()\nprint(hash_digest)\n# 'ef92b778bafe771e89245b89ecbc08a44a4e166c06659911881f383d4473e94f'"},
 dg:"encryption",
 r:["Base64", "Encoding", "SSL Certificate", "API Key", "Hash Table"]},

{t:"Load Balancing", d:"Distributing incoming web traffic across a pool of backend servers so no single server gets overwhelmed.", l:"core", g:["web","architecture","crash-course"],
 b:["When your application grows from 100 users to 100,000, a single server will eventually run out of CPU and memory. Load balancing places a high-performance reverse proxy (like NGINX, HAProxy, or AWS ALB) in front of a cluster of identical backend servers.",
    "The load balancer receives all incoming traffic and distributes requests using algorithms like Round Robin, Least Connections, or IP Hash. It continuously performs health checks: if one server crashes, the load balancer quietly routes all new requests to the healthy servers with zero user downtime."],
 k:["Distributes traffic across multiple servers for high availability and scale.",
    "Algorithms: Round Robin, Least Connections, Weighted, and IP Hashing.",
    "Conducts automatic health checks to isolate and bypass failed server instances.",
    "Enables horizontal scaling: add more cheap servers instead of one massive machine."],
 x:{lang:"text", code:"                  ┌──► Backend Server 1 (healthy)\nClient Requests ──► Load Balancer ──┼──► Backend Server 2 (healthy)\n                  └──► Backend Server 3 (failed - bypassed!)"},
 dg:"load-balancer",
 r:["Server", "Client", "Throughput", "Bottleneck", "Stateless"]}

]);
