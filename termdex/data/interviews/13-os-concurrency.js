/* Interview bank — operating systems and concurrency.

   The rounds that separate candidates here are the ones about what the
   hardware and kernel are actually doing: where a page fault costs you, why a
   lock is slow, what a cache line has to do with your thread pool. */
(function (TD) {
  "use strict";

  TD.addQuestions([
    {
      id: "os-process-vs-thread",
      q: "Process versus thread. What exactly is shared, and when would you choose each?",
      topic: "OS & Concurrency",
      level: "easy",
      companies: "*",
      tags: ["processes", "threads", "memory"],
      simple: "A process is a house with its own address; a thread is a person inside it. Threads in the same house share the furniture, so they can hand things to each other instantly — and also knock things over. Separate houses cannot touch each other's furniture at all, which is safer and slower to coordinate.",
      answer: "## What is shared\n\n| Resource | Threads in one process | Separate processes |\n| --- | --- | --- |\n| Address space (heap, globals) | **Shared** | Separate |\n| Open file descriptors | Shared | Separate (unless inherited) |\n| Stack | Each thread has its own | Separate |\n| Registers, program counter | Own | Own |\n| Signal handlers | Shared | Separate |\n| Process ID | Same | Different |\n\nThat first row is the entire story. Shared memory makes thread communication free — pass a pointer — and makes every data race possible.\n\n## Cost\n\n- **Creation.** A thread is roughly 10–100× cheaper to create than a process. `fork()` must set up page tables even with copy-on-write; `pthread_create` allocates a stack and a kernel task.\n- **Context switch.** Between threads of one process: registers and stack pointer. Between processes: also the page table root register, which **flushes or invalidates TLB entries**. That TLB cost is often larger than the switch itself, and it is the reason process switches hurt.\n- **Memory.** A thread's default stack reservation is typically 8 MB of *virtual* address space, but only the touched pages are resident. This matters when someone claims 1,000 threads needs 8 GB — it does not.\n\n## Choosing\n\n**Threads** when tasks share large amounts of data, when you need low-latency communication, or when the work is I/O-bound and you want many in flight.\n\n**Processes** when you need fault isolation (one crash must not take everything down), when you need real parallelism in a runtime with a global interpreter lock, or when you need separate security contexts. Chrome uses a process per tab precisely so that one page's crash or compromise is contained.\n\n## The Python case\n\nCPython's **Global Interpreter Lock** allows only one thread to execute bytecode at a time. So:\n\n- **I/O-bound work:** threads work fine, because the GIL is released during blocking I/O.\n- **CPU-bound work:** threads give no speedup. Use `multiprocessing`, or a native extension that releases the GIL (NumPy does).\n\nThis is a CPython implementation detail, not a property of Python the language — Jython and modern free-threaded builds do not have it. Being precise about that is a small, real signal.\n\n## The middle ground\n\n**Coroutines / green threads** (`asyncio`, Go goroutines, virtual threads in Java) give you thousands of concurrent tasks scheduled in user space, with switch costs in nanoseconds rather than microseconds. They are the right answer for high-concurrency I/O, and they give no help at all with CPU-bound work.",
      takeaways: [
        "Threads share the address space; that is both the advantage and the entire source of the hazards.",
        "Process context switches invalidate TLB entries — that is the real cost, not the register save.",
        "The GIL makes Python threads useless for CPU-bound work and perfectly fine for I/O-bound work."
      ],
      followUps: [
        "What does `fork()` actually copy? (Page tables, marked copy-on-write; pages are duplicated on first write.)",
        "Why is `fork()` in a multithreaded program dangerous? (Only the calling thread survives; a lock held by another thread stays locked forever.)",
        "What is a thread pool and why not create a thread per request?"
      ],
      trap: "Saying threads are always faster. For CPU-bound Python they are strictly slower than processes, and for anything needing isolation they are the wrong tool regardless of speed."
    },
    {
      id: "os-deadlock-conditions",
      q: "Explain deadlock, livelock and starvation. How do you prevent each?",
      topic: "OS & Concurrency",
      level: "medium",
      companies: "*",
      tags: ["deadlock", "locking", "scheduling"],
      simple: "Deadlock is two people stuck in a doorway, each waiting for the other to move. Livelock is two people repeatedly stepping aside into each other's path — moving, but going nowhere. Starvation is one person who keeps getting pushed to the back of the queue while others go through.",
      answer: "## Deadlock — the four Coffman conditions\n\nAll four must hold at once:\n\n1. **Mutual exclusion** — the resource cannot be shared.\n2. **Hold and wait** — a thread holds one resource while requesting another.\n3. **No pre-emption** — resources are not forcibly reclaimed.\n4. **Circular wait** — a cycle of threads each waiting on the next.\n\nBreak any one and deadlock is impossible.\n\n### Which to break, in practice\n\n**Circular wait — the practical answer.** Impose a global ordering on locks and always acquire in that order. Assign each lock a rank; assert in debug builds that you never acquire a lower rank while holding a higher one. This catches the bug at development time rather than at 3 a.m.\n\n**Hold and wait** — acquire everything at once or nothing (`std::lock` in C++ does this with a deadlock-avoidance algorithm), then retry. Correct, and it can reduce concurrency significantly.\n\n**No pre-emption** — use timed lock attempts (`try_lock_for`), release everything on timeout, back off, retry. Simple and effective, though it can degrade into livelock if everyone retries in lockstep — jitter the backoff.\n\n**Mutual exclusion** — remove the shared resource entirely. Immutable data, per-thread copies, or message passing instead of shared state. The best fix when it is available, because it removes the problem class rather than managing it.\n\n## Livelock\n\nThreads are running and responding to each other, but no work completes. Two threads each detect a conflict, both back off, both retry at the same moment, and repeat.\n\nThe fix is **randomised backoff**. Deterministic retry keeps them synchronised; jitter breaks the symmetry. This is exactly the Ethernet CSMA/CD problem and exactly the same solution.\n\n## Starvation\n\nA thread never gets the resource because others keep taking it. Common causes:\n\n- **Unfair locks.** Most mutexes are unfair by design — an arriving thread can barge ahead of one already waiting, because barging is faster. Under contention a waiter can be skipped repeatedly.\n- **Priority scheduling** where a low-priority thread never runs.\n- **Reader-writer locks** where a stream of readers means a writer never acquires.\n\nFixes: fair (FIFO) locks, which cost throughput; ageing, which raises priority the longer a thread waits; or write-preferring reader-writer locks.\n\n## Priority inversion — the famous one\n\nA low-priority thread holds a lock. A high-priority thread needs it and blocks. A medium-priority thread, needing no lock, pre-empts the low-priority one — so the high-priority thread is now effectively blocked by a medium-priority thread.\n\nThis caused repeated system resets on **Mars Pathfinder in 1997**, diagnosed and patched remotely.\n\nThe fix is **priority inheritance**: while a high-priority thread waits on a lock, the holder temporarily inherits that priority, so it cannot be pre-empted by anything in between.\n\n## Detection versus prevention\n\nDatabases **detect** deadlocks: build a wait-for graph, find a cycle, abort a victim. That works because a transaction can be rolled back.\n\nApplications generally **prevent**, because there is no safe way to abort a thread mid-operation. Hence lock ordering rather than detection.",
      takeaways: [
        "Break circular wait with a global lock ordering; the other three conditions are usually inherent.",
        "Livelock means progress without completion — randomised backoff is the cure.",
        "Priority inversion is fixed by priority inheritance; it is the Mars Pathfinder bug."
      ],
      followUps: [
        "What is the Banker's algorithm and why is it rarely used? (Needs advance knowledge of maximum resource demands.)",
        "How do you detect deadlock in a running Java process? (Thread dump; the JVM reports detected cycles.)",
        "Why are most mutexes unfair by default?"
      ],
      trap: "Listing the four conditions without saying which one you would break and why. The list is recall; the choice is the answer."
    },
    {
      id: "os-mutex-vs-semaphore",
      q: "Mutex, semaphore, condition variable, spinlock. When does each one belong?",
      topic: "OS & Concurrency",
      level: "medium",
      companies: ["apple", "nvidia", "oracle", "microsoft", "amazon", "snowflake", "tesla", "bytedance", "tcs"],
      tags: ["synchronisation", "primitives"],
      simple: "A mutex is a key to a room, and only the person who took it can put it back. A semaphore is a bowl of tokens anyone can take or add. A condition variable is a bell you ring to wake someone waiting for news. A spinlock is standing at the door repeatedly checking rather than sitting down.",
      answer: "## Mutex\n\nMutual exclusion with **ownership**. Only the locking thread may unlock. That ownership is what enables priority inheritance and deadlock detection tooling.\n\n```c\npthread_mutex_lock(&m);\n/* critical section */\npthread_mutex_unlock(&m);\n```\n\nUse for protecting shared data. Always the default.\n\n## Semaphore\n\nA counter with atomic wait and signal. **No ownership** — any thread may signal, including one that never waited. That makes it a signalling primitive, not a mutual-exclusion primitive.\n\n- **Binary semaphore** (count 1) looks like a mutex and is not one: no ownership, so no priority inheritance and no misuse detection.\n- **Counting semaphore** limits concurrent access to N resources — a connection pool of 10, a bounded worker pool.\n\nThe canonical correct use is the producer–consumer pair, where the producer signals a semaphore the consumer waits on. That cross-thread signalling is exactly what a mutex cannot do.\n\n## Condition variable\n\nWait for a **predicate** to become true, releasing the associated mutex while asleep.\n\n```c\npthread_mutex_lock(&m);\nwhile (queue_is_empty(q))          /* while, NEVER if */\n    pthread_cond_wait(&cv, &m);    /* atomically unlocks m and sleeps */\nitem = dequeue(q);\npthread_mutex_unlock(&m);\n```\n\n**The `while` loop is mandatory**, for two reasons:\n\n1. **Spurious wakeups** are permitted by POSIX — a thread can wake with no signal at all.\n2. Even without them, another thread may have consumed the item between the signal and your reacquiring the mutex.\n\nWriting `if` here is the most common concurrency bug in C, and it fails only under load.\n\n`signal` wakes one waiter; `broadcast` wakes all. Use `broadcast` when waiters are waiting on different predicates, or a woken thread may find its condition false, go back to sleep, and nobody else gets woken.\n\n## Spinlock\n\nBusy-wait in a loop rather than sleeping.\n\n- **No context switch**, so it is far cheaper *if the wait is very short* — shorter than the ~1–5 μs a context switch costs.\n- Burns a full CPU while waiting.\n- **Catastrophic if the holder is descheduled.** You spin for a full scheduler quantum waiting for a thread that is not running. This is why spinlocks belong in kernel code with interrupts disabled, and almost never in userspace.\n\nAn **adaptive mutex** spins briefly and then sleeps, which is what most modern mutex implementations already do — so hand-rolling a spinlock is usually reinventing something worse.\n\n## Choosing\n\n| Need | Use |\n| --- | --- |\n| Protect shared data | Mutex |\n| Limit concurrency to N | Counting semaphore |\n| Wait for a state change | Condition variable + mutex |\n| Signal across threads | Semaphore or condition variable |\n| Very short critical section, kernel context | Spinlock |\n| One-time initialisation | `pthread_once` / `std::call_once` |\n| Read-mostly data | Reader-writer lock, or RCU |\n\n## The best answer is often none of them\n\nAtomics for a single counter. Immutable data needs no lock at all. Channels and message passing move the synchronisation into a library that has already been debugged. Reaching for a mutex should not be automatic.",
      takeaways: [
        "A mutex has ownership; a semaphore does not — that is why a binary semaphore is not a mutex.",
        "Always wait on a condition variable inside a `while` loop: spurious wakeups are permitted.",
        "Spinlocks are catastrophic when the holder is descheduled; leave them to kernel code."
      ],
      followUps: [
        "Implement a bounded blocking queue with a mutex and two condition variables.",
        "What is a futex and why is an uncontended lock nearly free? (No syscall unless there is contention.)",
        "When is a reader-writer lock slower than a plain mutex? (Short critical sections — the bookkeeping costs more than it saves.)"
      ],
      trap: "Using `if` instead of `while` around a condition variable wait. It passes every test you write and fails intermittently in production, which is the worst possible failure profile."
    },
    {
      id: "os-virtual-memory",
      q: "Explain virtual memory, page faults and the TLB. What does a page fault actually cost?",
      topic: "OS & Concurrency",
      level: "medium",
      companies: ["apple", "nvidia", "oracle", "microsoft", "google", "snowflake", "tesla", "amazon"],
      tags: ["memory", "paging", "tlb"],
      simple: "Every program thinks it has the whole machine's memory to itself. The hardware secretly translates those imaginary addresses to real ones, and keeps a small cache of recent translations so it does not have to look them up every time. When the page it wants is not in memory at all, everything stops while it is fetched from disk.",
      answer: "## Why it exists\n\nEach process gets its own **virtual address space**, giving three things at once:\n\n1. **Isolation.** One process cannot address another's memory. Security and stability both follow.\n2. **The illusion of contiguity.** A large array looks contiguous even when its physical pages are scattered.\n3. **Overcommit.** The sum of all virtual allocations can exceed physical RAM, since only touched pages are backed.\n\n## Translation\n\nThe MMU walks a multi-level page table. On x86-64 with 4-level paging, one translation is **four memory accesses** before you get to the actual data. That would be crippling, so the **TLB** caches recent translations.\n\n- TLB hit: translation is essentially free.\n- TLB miss: a page walk, ~100 ns, though the intermediate levels are usually cached.\n- L1 TLB typically holds 64–100 entries.\n\nWith 4 KB pages, a 64-entry TLB covers only 256 KB of memory. That is why **huge pages** (2 MB or 1 GB) matter so much for databases and JVMs: the same 64 entries then cover 128 MB, and TLB miss rates collapse.\n\n## Page faults, and their real cost\n\n**Minor fault** — the page is in physical memory but not mapped into this process's page table. Common on first touch of newly allocated memory, or on a shared library already resident. Cost: microseconds. Harmless.\n\n**Major fault** — the page must be read from disk.\n\n| Storage | Latency | Relative |\n| --- | --- | --- |\n| L1 cache | ~1 ns | 1× |\n| RAM | ~100 ns | 100× |\n| NVMe SSD | ~100 μs | 100,000× |\n| Spinning disk | ~10 ms | 10,000,000× |\n\nA major fault to spinning disk is roughly ten million times slower than an L1 hit. **This is why thrashing destroys a system:** once the working set exceeds RAM, nearly every access becomes a major fault, and throughput falls by orders of magnitude while CPU usage looks low. A machine that is thrashing appears idle, which is the confusing part.\n\n## Copy-on-write\n\n`fork()` does not copy memory. Both processes share the same physical pages, marked read-only. The first write triggers a fault, the kernel copies that single page, and both continue. Making `fork()` + `exec()` cheap is the whole reason this exists.\n\nIt is also why a Redis background save briefly increases memory: pages modified during the save get copied, so the peak can approach 2× if the write rate is high.\n\n## What an engineer does with this\n\n- **`mmap` a large file** and let the page cache manage it rather than reading it in chunks. This is how most databases read data.\n- **`mlock`** pages that must never be paged out — cryptographic keys, real-time buffers.\n- **Enable huge pages** for large-heap processes; often a double-digit percentage win.\n- **Watch major faults**, not just memory usage. `vmstat` or `/proc/PID/stat` — a rising major fault rate is the earliest signal of memory pressure.\n- **Disable swap on latency-sensitive servers.** A swapped-out page turns a 100 ns access into a 100 μs one, and the resulting tail latency is worse than an out-of-memory kill you can observe and restart from.",
      takeaways: [
        "A TLB miss costs a page walk; a major page fault costs a disk read — five orders of magnitude apart.",
        "Huge pages massively extend TLB reach; often the cheapest win for large-heap processes.",
        "A thrashing machine looks idle in CPU terms — watch major fault rate, not utilisation."
      ],
      followUps: [
        "What is the page cache and why does 'free' memory look low on a healthy Linux box?",
        "How does `madvise` help? (Tells the kernel your access pattern — sequential, random, will-need.)",
        "Why does a Redis background save spike memory usage?"
      ],
      trap: "Treating all page faults as bad. Minor faults are normal and constant; only the major fault rate indicates a problem."
    },
    {
      id: "os-false-sharing",
      q: "Your multithreaded counter gets slower as you add threads, even with no lock contention. Why?",
      topic: "OS & Concurrency",
      level: "hard",
      companies: ["nvidia", "apple", "snowflake", "databricks", "google", "meta-ai", "bytedance", "tesla"],
      tags: ["cache", "false-sharing", "memory-model"],
      simple: "The CPU moves memory around in chunks of 64 bytes, not individual variables. If two threads update different variables that happen to sit in the same chunk, the hardware treats it as a conflict and bounces that chunk between cores constantly — even though the threads never touch the same data.",
      answer: "## False sharing\n\nCaches operate on **cache lines**, typically 64 bytes. Cache coherence (MESI) works per line, not per variable.\n\n```c\nstruct counters {\n    long a;   /* thread 0 increments this */\n    long b;   /* thread 1 increments this */\n};            /* both fit in one 64-byte line */\n```\n\nThe threads share no variable — but they share a **line**. Every write by thread 0 invalidates the line in thread 1's cache and vice versa. The line ping-pongs between cores, and each access becomes a cross-core transfer of ~100 ns instead of an L1 hit of ~1 ns.\n\nThe result is the signature symptom: **performance gets worse as you add threads**, with no lock and no contention visible in any profiler that measures locks.\n\n## The fix: padding\n\n```c\nstruct counters {\n    alignas(64) long a;\n    alignas(64) long b;    /* now on separate cache lines */\n};\n```\n\nIn Java, `@Contended` (with `-XX:-RestrictContended`). In C++17, `std::hardware_destructive_interference_size` names the value portably. In Go, pad with an explicit `[64]byte` array.\n\nMemory cost: 64 bytes per counter instead of 8. Almost always worth it for a hot counter.\n\n## Where it shows up in real code\n\n- **Per-thread statistics arrays.** `long counts[NUM_THREADS]` puts eight counters in one line — the textbook case, and extremely common.\n- **Lock-free queue head and tail pointers.** Producer writes the tail, consumer writes the head; if they share a line, the queue is far slower than it looks.\n- **Adjacent fields in a hot struct** written by different threads.\n\n## Diagnosing it\n\n```bash\nperf c2c record ./app          # cache-to-cache transfer analysis\nperf c2c report\n```\n\n`perf c2c` exists specifically for this and reports the exact cache lines and source lines involved. Failing that, `perf stat -e cache-misses` showing high misses with a small working set is the signal.\n\n## True sharing\n\nIf threads genuinely update the *same* counter, padding does not help — the contention is real. Then:\n\n- **Per-thread counters, summed on read.** Each thread has its own padded counter; readers sum them. This is what Java's `LongAdder` does, and it scales nearly linearly.\n- **Sharded counters** — `counter[thread_id % N]` with padding.\n- **Approximate counting** — increment probabilistically when exactness is not required.\n\n## The related trap: atomics are not free\n\nAn atomic increment on a contended line still serialises. `LongAdder` beats `AtomicLong` under contention by roughly an order of magnitude for exactly this reason — it removes the shared line rather than making access to it cheaper.",
      takeaways: [
        "Coherence works per 64-byte cache line, not per variable — independent variables can still conflict.",
        "The signature is throughput falling as threads increase, with no visible lock contention.",
        "Pad to a cache line; for genuine sharing, use per-thread counters summed on read."
      ],
      followUps: [
        "Why does `LongAdder` beat `AtomicLong` under contention?",
        "What is the MESI protocol and which transition is expensive? (Modified in another core's cache forces a transfer.)",
        "How would padding interact with a struct you also want to fit in a page?"
      ],
      trap: "Reaching for a lock-free algorithm to fix a false-sharing problem. Lock-free code with false sharing is often slower than a plain mutex — the fix is the memory layout, not the algorithm."
    },
    {
      id: "os-epoll-vs-threads",
      q: "How do you serve a million concurrent connections on one machine?",
      topic: "OS & Concurrency",
      level: "hard",
      companies: ["bytedance", "netflix", "uber", "meta-ai", "google", "amazon", "nvidia"],
      tags: ["epoll", "io", "scalability", "c10k"],
      simple: "You cannot give each connection its own thread — a million threads would spend all their time switching. Instead one thread watches all the connections at once and only wakes for the handful that actually have data. Most connections are idle most of the time, and that is what makes it work.",
      answer: "## Why thread-per-connection fails\n\nA million threads means:\n\n- Roughly 8 KB of resident kernel and stack state each — gigabytes before any application data.\n- The scheduler managing a million runnable entities.\n- Context switches at ~1–5 μs each dominating everything.\n\nIt works to a few thousand connections. It does not reach a million.\n\n## The insight\n\n**Most connections are idle at any instant.** A million open connections might have a few thousand with data ready. So use one thread to watch all of them and process only the ready ones.\n\n## select, poll, epoll\n\n| API | Complexity per call | Limit |\n| --- | --- | --- |\n| `select` | $O(n)$ — scans every fd | 1024 fds |\n| `poll` | $O(n)$ | None, still scans |\n| `epoll` | $O(1)$ for ready fds | None |\n\n`select` and `poll` pass the entire fd set to the kernel on every call and the kernel scans all of it. At a million fds, that scan is the bottleneck — you spend all your time asking about connections that have nothing to say.\n\n**`epoll`** registers interest once. The kernel maintains a ready list, updated by the network stack as data arrives. `epoll_wait` returns only the ready descriptors, so the cost is proportional to **activity**, not to the number of connections.\n\n```c\nint ep = epoll_create1(0);\nstruct epoll_event ev = { .events = EPOLLIN | EPOLLET, .data.fd = conn_fd };\nepoll_ctl(ep, EPOLL_CTL_ADD, conn_fd, &ev);      /* register once */\n\nstruct epoll_event events[MAX];\nfor (;;) {\n    int n = epoll_wait(ep, events, MAX, -1);      /* only ready fds */\n    for (int i = 0; i < n; i++)\n        handle(events[i].data.fd);\n}\n```\n\nEquivalents: `kqueue` on BSD and macOS, IOCP on Windows, `io_uring` on modern Linux.\n\n## Level-triggered versus edge-triggered\n\n**Level-triggered** (default): reports readiness as long as data remains. Forgiving — a partial read just means you are notified again.\n\n**Edge-triggered** (`EPOLLET`): reports only on the *transition* to ready. Fewer wakeups, and it obliges you to **read until `EAGAIN`**. Miss that and the remaining data sits there with no further notification — a hung connection that is very hard to debug.\n\nEdge-triggered plus non-blocking sockets plus drain-until-`EAGAIN` is the high-performance configuration, and all three parts are required together.\n\n## Beyond the event loop\n\nA single event loop is bound by one core. Two ways out:\n\n**`SO_REUSEPORT`.** Several processes bind the same port, each with its own event loop; the kernel load-balances incoming connections across them. No shared accept queue, no thundering herd, near-linear scaling across cores. This is how NGINX scales.\n\n**`io_uring`.** Shared submission and completion ring buffers between userspace and kernel, so batches of I/O are submitted with **zero syscalls** in the common case. It also covers file I/O, which `epoll` never handled properly.\n\n## The other limits you will hit\n\nEpoll is necessary and not sufficient. At a million connections you also need:\n\n- `fs.file-max` and per-process `RLIMIT_NOFILE` raised well above one million.\n- Socket buffer memory: at a 64 KB default receive buffer, a million connections is 64 GB. Tune `net.ipv4.tcp_rmem` down.\n- Ephemeral port exhaustion on the client side — ~28,000 ports per source IP by default. Needs multiple source addresses.\n- Connection tracking table limits if a stateful firewall is in the path.\n\nNaming these is what separates someone who has run this from someone who has read about `epoll`.",
      takeaways: [
        "`epoll` costs scale with activity, not with connection count; `select` and `poll` scan everything.",
        "Edge-triggered requires non-blocking sockets and reading until `EAGAIN` — all three together or none.",
        "`SO_REUSEPORT` scales across cores without a shared accept queue."
      ],
      followUps: [
        "What is the thundering herd on accept, and how does `SO_REUSEPORT` avoid it?",
        "How does `io_uring` reduce syscall overhead further?",
        "How do goroutines and virtual threads relate to this? (Same idea, moved into the runtime.)"
      ],
      trap: "Answering only \"use epoll\". The follow-up is always about file descriptor limits, socket buffer memory, and ephemeral ports — and that is where the answer is actually decided."
    },
    {
      id: "os-memory-ordering",
      q: "What is a memory barrier, and why can code be reordered even when it is single-threaded-correct?",
      topic: "OS & Concurrency",
      level: "staff",
      companies: ["nvidia", "apple", "google", "meta-ai", "snowflake", "microsoft", "databricks"],
      tags: ["memory-model", "atomics", "barriers"],
      simple: "The compiler and the CPU both reorder your instructions to go faster, and both promise the result looks the same — to the thread doing them. Another thread watching from outside sees the real order, which may not be yours. A barrier is you telling them there is a watcher.",
      answer: "## Two reorderings, two sources\n\n**Compiler reordering.** The optimiser moves instructions as long as single-threaded semantics are preserved. It has no idea another thread is watching.\n\n**CPU reordering.** Store buffers, out-of-order execution and cache hierarchies mean writes become visible to other cores in an order different from the program order. x86 is relatively strong (it permits store–load reordering); ARM and POWER are much weaker and reorder aggressively.\n\nThis is why code that works on x86 breaks on ARM — the same bug was always there, and the stronger model was hiding it.\n\n## The classic broken example\n\n```c\n/* Thread 1 */              /* Thread 2 */\ndata = 42;                  if (ready)\nready = true;                   print(data);   /* can print garbage! */\n```\n\nWithout barriers, `ready = true` may become visible before `data = 42`. Thread 2 sees `ready`, reads `data`, and gets the old value. Nothing here is undefined behaviour in the intuitive sense — it is simply what the hardware is permitted to do.\n\n## Barrier types\n\n| Barrier | Guarantees |\n| --- | --- |\n| `acquire` | No later read or write moves before this load |\n| `release` | No earlier read or write moves after this store |\n| `acq_rel` | Both, for read-modify-write |\n| `seq_cst` | The above, plus a single total order all threads agree on |\n\n**Acquire–release is the pairing that matters.** A release store followed by an acquire load of the same variable creates a happens-before edge: everything before the release in the writing thread is visible after the acquire in the reading thread.\n\n```cpp\nstd::atomic<bool> ready{false};\nint data = 0;\n\n/* Thread 1 */\ndata = 42;\nready.store(true, std::memory_order_release);   /* nothing above moves below */\n\n/* Thread 2 */\nwhile (!ready.load(std::memory_order_acquire))  /* nothing below moves above */\n    ;\nassert(data == 42);                             /* now guaranteed */\n```\n\n## Sequential consistency\n\n`seq_cst` is the default in C++ and Java because it is the only model most people can reason about correctly. It costs a full barrier — on x86 typically an `mfence` or a locked instruction, tens of cycles.\n\nRelaxed orderings are faster and are extraordinarily easy to get wrong. The professional position is: **use `seq_cst` unless you have profiled, you can state the exact happens-before edges you rely on, and the code is small enough to verify.** Saying that in an interview is a better answer than demonstrating relaxed-atomics fluency, because it shows judgement about risk.\n\n## `volatile` is not a barrier\n\nIn C and C++, `volatile` prevents the *compiler* from optimising away or caching an access. It provides **no** ordering guarantee against other threads and no atomicity. Using it for thread synchronisation is a bug.\n\nJava's `volatile` is different and genuinely does establish happens-before — it is closer to C++'s `seq_cst` atomic. The two languages using the same keyword for different things is a genuine source of confusion, and knowing the difference is a real signal.\n\n## Why you rarely write these\n\nA mutex already contains the barriers: acquiring is an acquire operation, releasing is a release operation. Everything above works automatically. Explicit barriers belong in lock-free data structures and in library code, which is a small and specialised amount of the code anyone writes.",
      takeaways: [
        "Both the compiler and the CPU reorder; single-threaded correctness is all either one promises.",
        "Acquire–release pairing creates the happens-before edge — that is the mechanism to name.",
        "C/C++ `volatile` gives no ordering; Java's `volatile` does. Same keyword, different meanings."
      ],
      followUps: [
        "Why does the same code work on x86 and break on ARM?",
        "What is the double-checked locking bug and how does `volatile` fix it in Java?",
        "When is `memory_order_relaxed` genuinely safe? (An independent counter where only the final total matters.)"
      ],
      trap: "Using `volatile` in C or C++ for thread synchronisation. It compiles, it often appears to work on x86, and it is not a synchronisation primitive."
    },
    {
      id: "os-scheduling",
      q: "How does the OS scheduler decide what runs next, and why does your process get less CPU than you asked for?",
      topic: "OS & Concurrency",
      level: "medium",
      companies: ["apple", "google", "netflix", "amazon", "nvidia", "tesla", "oracle", "microsoft"],
      tags: ["scheduling", "cgroups", "containers"],
      simple: "The scheduler shares the CPU between everything that wants to run, giving more to whoever has had least so far. In a container there is a second layer on top that enforces a hard cap — and hitting that cap stops your process dead for the rest of the period, even if the machine is idle.",
      answer: "## Linux CFS, briefly\n\nThe Completely Fair Scheduler tracks **virtual runtime** per task and always runs the one with the smallest. Virtual runtime advances at a rate inversely proportional to weight, so a high-priority (low-nice) task's clock advances slowly and it therefore runs more.\n\nThere is no fixed time slice. The target latency (default 24 ms) is divided among runnable tasks, with a floor (`min_granularity`, ~3 ms) so that many runnable tasks do not produce pathological switching.\n\nInteractive tasks benefit naturally: a task that sleeps accumulates little virtual runtime, so when it wakes it has the smallest value and is scheduled immediately. Responsiveness falls out of the mechanism rather than needing a heuristic.\n\n## Real-time classes\n\n`SCHED_FIFO` and `SCHED_RR` sit above CFS entirely. A runnable real-time task pre-empts every normal task. `SCHED_FIFO` runs until it blocks or yields — a compute-bound FIFO task with no blocking will monopolise its CPU indefinitely, which is why `sched_rt_runtime_us` reserves a slice for normal tasks as a safety valve.\n\n## The part that bites in production: cgroup CPU limits\n\nIn Kubernetes:\n\n- **`requests`** map to CFS **shares** — a relative weight, applied only when the CPU is contended. Guarantees a proportion, not a cap.\n- **`limits`** map to CFS **quota** — a hard ceiling of `quota` microseconds per `period` (default 100 ms).\n\n**Throttling is the surprise.** If a container has a limit of 1 CPU and uses its full 100 ms quota in the first 30 ms of a period — because four threads ran in parallel — it is **frozen for the remaining 70 ms**. Even on a completely idle machine.\n\nThe symptom is latency spikes at roughly 100 ms granularity, with average CPU utilisation reported well *below* the limit. That combination is confusing and is the signature.\n\n```bash\n# Is this container being throttled?\ncat /sys/fs/cgroup/cpu.stat\n# nr_throttled and throttled_usec climbing = yes\n```\n\n### What to do about it\n\n- **Raise the limit**, or remove it and rely on requests for isolation. Many production Kubernetes users deliberately set no CPU limit for latency-sensitive services.\n- **Match your thread pool to the limit.** A JVM or Go runtime that sees 64 host cores will create 64 threads, burn the quota in a fraction of the period, and throttle constantly. Set `GOMAXPROCS` and the JVM's `ActiveProcessorCount` from the cgroup limit, not from the host.\n\nThat mismatch — runtime sees the host, cgroup enforces the limit — is one of the most common performance bugs in containerised services, and naming it is a strong practical signal.\n\n## Priority inversion, again\n\nA low-priority thread holding a lock a high-priority thread needs blocks the high-priority thread. `PTHREAD_PRIO_INHERIT` on the mutex enables priority inheritance and fixes it.",
      takeaways: [
        "CFS picks the smallest virtual runtime; interactivity emerges from the mechanism, not a heuristic.",
        "Kubernetes CPU limits are a hard quota per 100 ms period — you can be throttled on an idle machine.",
        "A runtime sizing its thread pool from host cores inside a limited cgroup throttles constantly."
      ],
      followUps: [
        "Why do requests and limits behave so differently? (Weight versus quota.)",
        "How do you size `GOMAXPROCS` correctly in a container?",
        "What is CPU pinning and when is it worth it? (NUMA locality and cache warmth for latency-critical work.)"
      ],
      trap: "Assuming low average CPU means no CPU problem. Throttling shows up as latency spikes with utilisation apparently well under the limit, and it is invisible unless you look at `nr_throttled`."
    },
    {
      id: "os-race-condition-debug",
      q: "You have an intermittent bug that only appears under load and never reproduces locally. How do you find it?",
      topic: "OS & Concurrency",
      level: "hard",
      companies: ["netflix", "uber", "amazon", "google", "apple", "nvidia", "microsoft", "databricks"],
      tags: ["debugging", "race-conditions", "tooling"],
      simple: "Stop trying to reproduce it by luck. Use tools that detect the race even when it does not go wrong on that run, and add logging that would prove or disprove your theory. Then make the conditions worse on purpose — more threads, slower machine, injected delays.",
      answer: "## Do not start by guessing\n\nIntermittent means timing-dependent, which almost always means one of: a data race, a lock ordering problem, a resource leak that only manifests when a pool exhausts, or a retry storm.\n\n## Tools that find races without needing them to fire\n\nThe key property of a **dynamic race detector** is that it flags unsynchronised access even on runs where the interleaving happened to be benign. You do not need the bug to actually manifest.\n\n| Tool | Language | Notes |\n| --- | --- | --- |\n| ThreadSanitizer (`-fsanitize=thread`) | C, C++, Go, Rust | 5–15× slowdown, very high signal |\n| Go race detector (`go test -race`) | Go | Same engine; run it in CI always |\n| Java Flight Recorder + JFR events | Java | Lock contention and blocked-thread analysis |\n| Helgrind / DRD (Valgrind) | C, C++ | Slower, catches lock ordering violations |\n\nIf the codebase supports one of these and it is not in CI, that is the first fix regardless of this bug.\n\n## Make the failure more likely\n\n- **Increase concurrency** far past production levels.\n- **Reduce the machine.** Pin to one or two cores (`taskset`); many races only appear when threads genuinely interleave rather than running on separate cores.\n- **Inject delays.** A `sleep(random)` at suspicious points widens the window enormously. Crude, effective, and it converts a one-in-a-million race into a one-in-ten.\n- **Randomise thread scheduling** if the runtime supports it.\n- **Run with a debug allocator** that poisons freed memory, turning use-after-free from silent corruption into an immediate crash.\n\n## Instrument to test a hypothesis\n\nAdd logging that **discriminates between explanations**, not logging in general. If you suspect a lock ordering problem, log every acquisition with thread ID, lock ID and timestamp, then check offline for two threads acquiring in opposite orders. If you suspect pool exhaustion, log wait time to acquire a connection — not just query duration.\n\nUse a **ring buffer in memory** flushed only on failure. Logging every operation to disk changes the timing enough to hide the bug, which is the classic heisenbug frustration.\n\n## Narrow by correlation\n\n- Does it correlate with a specific host? A specific tenant? A specific input size?\n- Does it correlate with a deploy, or with traffic crossing a threshold?\n- Does it stop if you run one instance instead of many? That distinguishes an in-process race from a distributed one.\n\n## The usual culprits\n\n1. **Check-then-act.** `if (!map.containsKey(k)) map.put(k, v)` — two threads both pass the check. Use an atomic `putIfAbsent` or `computeIfAbsent`.\n2. **Non-atomic compound operations.** `count++` is a read, an add and a write.\n3. **Unsafe lazy initialisation.** Double-checked locking without the correct memory barriers.\n4. **Shared mutable state in a supposedly stateless service.** A module-level cache, a reused buffer, a `SimpleDateFormat` field.\n5. **Connection or thread pool exhaustion** under load, appearing as timeouts rather than as an obvious resource error.\n6. **Retry amplification.** A small slowdown causes retries, which cause load, which causes more slowdown.\n\n## Prove the fix\n\nWrite a test that **fails reliably before the change** — usually by adding the injected delays that widen the window — and passes after. A fix you cannot demonstrate is a fix you do not have, and \"we could not reproduce it again\" is not evidence.",
      takeaways: [
        "A race detector flags unsynchronised access even when the run did not go wrong — do not wait for the bug to fire.",
        "Widen the window deliberately: fewer cores, more threads, injected delays.",
        "Log to an in-memory ring buffer flushed on failure, so instrumentation does not hide the timing."
      ],
      followUps: [
        "How does ThreadSanitizer work? (Shadow memory tracking a vector clock per memory location.)",
        "Why does adding a log statement sometimes make the bug disappear?",
        "How would you find the same class of bug across services rather than within one? (Distributed tracing plus idempotency checks.)"
      ],
      trap: "Adding a lock somewhere plausible and declaring victory when the symptom stops. Without a reproducing test you have probably just changed the timing, and it will return under different load."
    }
  ]);
})(window.TD = window.TD || {});
