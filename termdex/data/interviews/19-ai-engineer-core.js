/* Interview bank — AI Engineer, part one: model internals, serving economics
   and retrieval.

   Everything here carries `aiTrack: true`. The selection rule was not "what is
   interesting about transformers" but "what has an interviewer actually made a
   hiring decision on" — which is why so many of these end in arithmetic. The
   recurring failure in an AI-engineer loop is a candidate who can describe a
   mechanism qualitatively and cannot say what it costs.

   Companion file: 21-ai-engineer-applied.js — the product, coding and
   judgement half. */
(function (TD) {
  "use strict";

  TD.addQuestions([

    /* ================= serving economics ================= */
    {
      id: "ai-prefill-decode",
      q: "Why is prefill compute-bound and decode memory-bandwidth-bound, and what does that change about how you serve a model?",
      topic: "LLM & GenAI",
      level: "staff",
      companies: ["openai", "anthropic", "nvidia", "mistral", "google-deepmind", "meta-ai", "databricks", "microsoft", "perplexity"],
      tags: ["inference", "latency", "throughput"],
      aiTrack: true,
      simple: "Reading your prompt and writing the answer are two different jobs on the same hardware. Reading processes thousands of tokens at once, so the GPU is busy doing maths. Writing produces one token at a time, so the GPU spends almost all its time waiting for weights to arrive from memory and barely computes at all. Almost every serving decision follows from that split.",
      answer: "## The two phases\n\n| | Prefill | Decode |\n| --- | --- | --- |\n| Input | The whole prompt, at once | One token |\n| Work per step | $O(n^2)$ attention over $n$ prompt tokens | One token attending to the cache |\n| Bottleneck | **Compute (FLOPs)** | **Memory bandwidth** |\n| Latency metric | **TTFT** — time to first token | **TPOT** — time per output token |\n| Batching helps? | Barely — already saturated | **Enormously** |\n\n## The arithmetic that explains it\n\nThe number that decides which side you are on is **arithmetic intensity**: FLOPs performed per byte moved.\n\nAn H100 does roughly $10^{15}$ FLOP/s and moves about $3.3 \\times 10^{12}$ bytes/s from HBM. The ratio is around **300 FLOPs per byte**. Below that, you are waiting on memory; above it, you are waiting on the arithmetic units.\n\n**Prefill.** A 2,000-token prompt through a 7B model performs roughly $2 \\times 7 \\times 10^9 \\times 2000 \\approx 2.8 \\times 10^{13}$ FLOPs while reading the 14 GB of weights once. Intensity is in the thousands. Compute-bound.\n\n**Decode, batch size 1.** One token performs $2 \\times 7 \\times 10^9 = 1.4 \\times 10^{10}$ FLOPs — and still reads all 14 GB of weights. Intensity is **1 FLOP per byte**. You are using well under one percent of the GPU's arithmetic capacity, and the token rate is simply:\n\n$$\\text{tokens/s} \\approx \\frac{\\text{memory bandwidth}}{\\text{model bytes}} = \\frac{3.3 \\times 10^{12}}{1.4 \\times 10^{10}} \\approx 235$$\n\nThat formula is worth memorising. It predicts single-stream decode speed on any hardware to within a factor of two, and being able to produce it on a whiteboard is most of what this question is testing.\n\n## What follows from it\n\n**Batching is nearly free during decode.** The weights are read once for the whole batch, so 32 concurrent requests cost roughly the same memory traffic as one. Throughput scales close to linearly until the KV cache runs out of room. This is why the KV cache — not the weights — sets your maximum batch size, and therefore your cost per token.\n\n**Batching does not help prefill.** The compute units are already busy. Batching prefill just queues work.\n\n**They interfere.** A long prompt entering the batch stalls every request already decoding, because the scheduler runs prefill to completion. That is the classic \"P99 spike with no traffic increase\" — one user pasted a large document. The fix is **chunked prefill**: split the prompt into slices and interleave them with decode steps, trading a slightly worse TTFT for a far more stable TPOT.\n\n**Quantisation helps decode more than prefill.** Halving the weight bytes roughly doubles decode speed, because decode is bandwidth-bound. Its effect on prefill is much smaller.\n\n**Speculative decoding only makes sense here.** Verifying $k$ draft tokens in one forward pass costs almost the same as generating one, precisely because decode leaves the arithmetic units idle. You are spending free compute to buy back memory traffic.\n\n## The operational consequence\n\nQuote and alert on TTFT and TPOT separately, never on total latency. Total latency mixes a prompt-length effect and an output-length effect, so it moves for reasons that have nothing to do with your system, and an alert on it fires on user behaviour rather than on regressions.",
      takeaways: [
        "Decode reads every weight to produce one token — single-stream speed is bandwidth ÷ model bytes.",
        "Batching is nearly free in decode and useless in prefill, which is why the KV cache caps your throughput.",
        "A long prompt stalls everyone mid-decode; chunked prefill trades TTFT for stable TPOT."
      ],
      followUps: [
        "How would you set a fair scheduling policy between a 50-token and a 50,000-token prompt?",
        "Why does quantisation speed up decode more than prefill?",
        "What happens to the roofline when you add a second GPU with tensor parallelism? (Interconnect becomes a third bottleneck.)"
      ],
      trap: "Saying decode is slow \"because it is sequential\". Sequentiality is why you cannot parallelise across time; the reason each step is slow is that it re-reads the entire model from memory to compute one token."
    },

    {
      id: "ai-continuous-batching",
      q: "Static batching versus continuous batching. Why does the second one roughly triple throughput?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["nvidia", "mistral", "anthropic", "openai", "databricks", "microsoft", "amazon", "perplexity", "huggingface"],
      tags: ["inference", "throughput", "serving"],
      aiTrack: true,
      simple: "In old-style batching you group eight requests, run them together, and wait for all eight to finish before starting anything new. Because one request might write 800 tokens while another writes 20, seven GPU slots sit idle for most of the batch. Continuous batching lets a finished request leave immediately and a queued one take its place on the very next step.",
      answer: "## The waste in static batching\n\nA batch runs until the **longest** sequence completes. With generation lengths of 20, 35, 40, 60, 90, 200, 400 and 800 tokens, the batch runs for 800 steps. Total useful token-steps are 1,645 out of a possible $8 \\times 800 = 6,400$.\n\n**Utilisation: 26%.** And it gets worse as generation length variance grows, which is exactly what real chat traffic looks like.\n\n## Continuous batching\n\nThe scheduler operates per **iteration**, not per batch:\n\n```\nfor each decoding step:\n    for each active sequence: run one token\n    evict sequences that emitted EOS or hit max_tokens\n    admit queued sequences into the freed slots\n```\n\nA sequence that finishes at step 20 frees its slot at step 21. Reported gains are typically **2–4× throughput** at the same latency, and the improvement is larger the more variable your output lengths are.\n\nThis is only possible because of two things:\n\n- Decode is memory-bandwidth-bound, so adding a sequence to the batch is nearly free in time.\n- **PagedAttention** makes the KV cache non-contiguous, so a slot can be freed and reused without defragmenting.\n\n## What actually limits the batch\n\nNot compute. **KV-cache memory.** The scheduler admits a new sequence only if there is room for its cache. So the real capacity question is:\n\n$$\\text{max concurrent} = \\frac{\\text{GPU memory} - \\text{weights} - \\text{activations}}{\\text{KV bytes per token} \\times \\text{average sequence length}}$$\n\nOn an 80 GB H100 running a 7B model in FP16: 14 GB weights, ~4 GB overhead, leaving 62 GB. With GQA the cache is roughly 128 KB per token, so 62 GB holds about 480,000 tokens — about 240 concurrent requests at 2,000 tokens each.\n\nThat is the number you should be able to produce when someone asks how many users one GPU serves.\n\n## Preemption, which is where it gets subtle\n\nIf memory runs out mid-generation the scheduler must evict somebody. Two options:\n\n- **Recompute** — drop the cache, re-run prefill when readmitted. Costs compute, frees memory instantly.\n- **Swap** — move the cache to CPU RAM and back. Costs PCIe bandwidth, which is slow.\n\nMost servers prefer recomputation for short sequences and swapping for long ones. Either way, aggressive admission plus long generations produces **thrashing**: throughput collapses while the GPU looks busy. Watching preemption count as a first-class metric is the operational tell of someone who has run one of these in production.\n\n## Chunked prefill\n\nContinuous batching alone still lets a long prompt monopolise a step. Chunked prefill splits a 32,000-token prompt into 2,000-token slices and interleaves them with decode steps, so ongoing generations keep progressing. It is on by default in current vLLM and it is the single best fix for \"P99 spikes when a user pastes a document\".\n\n## What to say about the trade\n\nThroughput and latency trade against each other through the admission policy. Admitting more sequences raises tokens per second per GPU — which is your cost — and raises per-user TPOT, because each step now serves more sequences. There is no setting that optimises both; there is a policy that matches your product. A batch assistant wants throughput. An interactive chat wants a cap on concurrency and a queue in front of it.",
      takeaways: [
        "Static batching wastes ~70% of slots because a batch runs as long as its longest sequence.",
        "Continuous batching evicts and admits per iteration; PagedAttention is what makes the slot reusable.",
        "The batch ceiling is KV-cache memory, not compute — and preemption thrashing is the failure to watch for."
      ],
      followUps: [
        "How would you prevent one tenant from occupying the whole batch? (Per-tenant concurrency limits, fair-share scheduling.)",
        "Recomputation or swapping on preemption — which for a 32k-token sequence?",
        "How does continuous batching interact with structured-output constraints?"
      ],
      trap: "Describing continuous batching as \"dynamic batch size\". Batch size varying is a consequence; the mechanism is that scheduling happens per decoding iteration rather than per batch, which is what allows a finished sequence to leave without waiting."
    },

    {
      id: "ai-speculative-decoding",
      q: "Explain speculative decoding. Why is the output distribution unchanged, and when does it lose?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["nvidia", "openai", "anthropic", "google-deepmind", "mistral", "meta-ai", "apple", "perplexity"],
      tags: ["inference", "latency", "sampling"],
      aiTrack: true,
      simple: "A small fast model guesses the next few tokens, and the big model checks all of them in a single pass. Where the guesses were right you got several tokens for the price of one; where they were wrong you throw away the rest and carry on. Because the checking step is done with a specific acceptance rule, the text you end up with is statistically identical to what the big model would have produced alone.",
      answer: "## The mechanism\n\n1. A **draft model** — perhaps 100× smaller, or the target model's own early layers — generates $k$ candidate tokens autoregressively. Cheap.\n2. The **target model** runs **one** forward pass over the prompt plus all $k$ drafts, producing the true distribution at each of those positions.\n3. Accept or reject each draft token in order using the rejection-sampling rule below. On the first rejection, resample that position from a corrected distribution and discard the rest.\n\nStep 2 is the whole trick. Verifying $k$ tokens costs almost exactly what generating one costs, because decode is memory-bandwidth-bound — the weights are read once either way. You are spending idle compute to buy back memory traffic.\n\n## Why the distribution is preserved\n\nFor draft distribution $q$ and target distribution $p$ at a position, accept the drafted token $x$ with probability\n\n$$\\min\\left(1, \\frac{p(x)}{q(x)}\\right)$$\n\nOn rejection, sample from the residual\n\n$$p'(x) = \\frac{\\max(0,\\; p(x) - q(x))}{\\sum_{x'} \\max(0,\\; p(x') - q(x'))}$$\n\nThis is standard rejection sampling, and the composition of the accept step and the residual step is provably exactly $p$. So speculative decoding is **lossless** — not \"almost as good\", *identical in distribution*. Saying that plainly is what separates a real answer from a paraphrase of a blog post.\n\nGreedy decoding is the degenerate case: accept while the draft matches the target's argmax.\n\n## The speedup\n\nWith acceptance rate $\\alpha$ and draft length $k$, the expected accepted tokens per verification step are\n\n$$E[\\text{tokens}] = \\frac{1 - \\alpha^{k+1}}{1 - \\alpha}$$\n\nAt $\\alpha = 0.8$, $k = 4$: about 3.4 tokens per target pass. Net of the draft model's own cost, real systems report **1.5–3×** wall-clock improvement.\n\nNote what the formula says about $k$: returns saturate. Beyond $k \\approx 5$ you are mostly generating drafts that will be discarded, and at low $\\alpha$ a large $k$ actively hurts.\n\n## The variants\n\n| Approach | Draft source | Note |\n| --- | --- | --- |\n| Classic | A separate small model | Needs a matching tokeniser and similar training data |\n| **Medusa** | Extra prediction heads on the target | No second model to serve; heads are trained on the target |\n| **EAGLE** | Predicts features, not tokens | Higher acceptance than Medusa at similar cost |\n| **Lookahead / n-gram** | Copies from the prompt | Free, and excellent for summarisation, code editing and RAG where output repeats input |\n| **Self-speculative** | The target's own early layers | No extra weights |\n\nThe prompt-copying variant deserves attention in an applied interview: for a RAG answer that quotes its sources, acceptance rates can exceed 90% with no model to train at all.\n\n## When it loses\n\n- **The batch is already full.** Speculative decoding spends spare compute. At high batch sizes there is none spare, and the extra verification work reduces throughput. It is a **latency** optimisation for lightly-loaded serving, not a throughput optimisation. This is the answer most candidates miss.\n- **Low acceptance.** A draft model trained on different data, or a high sampling temperature, drops $\\alpha$ until the draft cost exceeds the saving.\n- **Divergent domains.** Code, other languages, unusual formats — wherever the small model was never good.\n- **Memory pressure.** The draft model and its own KV cache take space that would otherwise hold more concurrent requests.\n\n## How to present it\n\nSay what it costs, not only what it saves: extra memory, a second model to version and evaluate, and a benefit that evaporates under load. Then say you would measure acceptance rate in production as a live metric, because it drifts with the traffic mix.",
      takeaways: [
        "Verification of k drafts costs one forward pass because decode is bandwidth-bound, not compute-bound.",
        "The accept/residual rule makes the output distribution exactly the target's — lossless, not approximate.",
        "It is a latency win at low load and a throughput loss at high load; that trade is the point of the question."
      ],
      followUps: [
        "How would you pick k adaptively at runtime? (Track acceptance rate and adjust.)",
        "Why does prompt-copying speculation work so well for RAG and code editing?",
        "How does speculative decoding interact with continuous batching?"
      ],
      trap: "Claiming it is an approximation that trades quality for speed. It is exact. Say so, and be able to sketch why the residual distribution makes it exact."
    },

    {
      id: "ai-quantization",
      q: "You need to fit a 70B model on one GPU. Walk me through quantisation and what it costs you.",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["nvidia", "mistral", "meta-ai", "apple", "huggingface", "amazon", "microsoft", "sarvam", "databricks"],
      tags: ["quantisation", "inference", "memory"],
      aiTrack: true,
      simple: "Model weights are stored as 16-bit numbers. Most of that precision is not needed at inference time, so you store them as 8-bit or 4-bit numbers instead and convert back on the fly. The model shrinks by two to four times, runs faster because there is less to read from memory, and loses a small amount of quality — unless you do it naively, in which case it falls apart.",
      answer: "## The memory arithmetic first\n\n70B parameters:\n\n| Precision | Weights | Fits on |\n| --- | --- | --- |\n| FP32 | 280 GB | 4× A100-80 |\n| FP16 / BF16 | 140 GB | 2× A100-80 |\n| INT8 | 70 GB | 1× H100-80, barely |\n| **INT4** | **35 GB** | **1× A100-40, comfortably on one 80 GB card with room for a real KV cache** |\n\nAnd because decode is memory-bandwidth-bound, halving the bytes roughly doubles single-stream token rate. Quantisation buys memory *and* speed, which is unusual — most optimisations trade one for the other.\n\n## How it works\n\nMap a float range onto integers:\n\n$$q = \\text{round}\\left(\\frac{w}{s}\\right) + z, \\qquad w \\approx s\\,(q - z)$$\n\nThe scale $s$ is the whole game. One scale for the entire tensor is destroyed by a single outlier. So real schemes use **per-channel** or **per-group** scales — typically one scale per 64 or 128 weights, which costs a little metadata and preserves far more of the distribution.\n\n## Weight-only versus weight-and-activation\n\n**Weight-only (INT4/INT8 weights, BF16 compute).** Dequantise on the fly inside the kernel. This is what GPTQ and AWQ do. It fixes memory and bandwidth, not arithmetic — which is exactly right, because decode is bandwidth-bound. **This is the default answer for LLM inference.**\n\n**Weight and activation (W8A8).** Quantises activations too, so the matrix multiply itself runs in INT8. Faster on compute-bound work — prefill, large batches — and much harder, because activations contain enormous outliers.\n\n## The outlier problem\n\nAt scale, a small number of hidden dimensions carry activations 100× larger than the rest. They are not noise; they are functionally important, and clipping them wrecks the model. Three responses:\n\n- **LLM.int8()** — keep the outlier channels in FP16, quantise the rest. Correct, and slower.\n- **SmoothQuant** — migrate the difficulty from activations into weights by rescaling: $Y = (X \\,\\text{diag}(s)^{-1})(\\text{diag}(s)\\, W)$. Mathematically identical, and both sides become quantisable.\n- **AWQ** — observe that roughly 1% of weight channels matter disproportionately, identify them from activation statistics, and protect them by scaling.\n\n## The methods you should be able to name\n\n| Method | Type | Calibration | Note |\n| --- | --- | --- | --- |\n| **GPTQ** | Weight-only, 3–4 bit | A few hundred samples | Layer-by-layer second-order error compensation |\n| **AWQ** | Weight-only, 4 bit | Activation statistics | Usually better than GPTQ at 4-bit, faster to produce |\n| **GGUF / k-quants** | Weight-only, 2–8 bit | Mostly none | The llama.cpp family — CPU and Apple silicon |\n| **SmoothQuant** | W8A8 | Yes | Enables INT8 matmuls |\n| **FP8** | W8A8 | Minimal | Hardware-native on H100/Ada; the current default for serving |\n| **QLoRA (NF4)** | Weight-only, 4 bit | None | For **training** with frozen quantised weights |\n\n## What it costs\n\n- **INT8 / FP8**: quality loss essentially unmeasurable on standard benchmarks. Take it.\n- **INT4** with a good method: typically 1–3% relative on reasoning benchmarks. Usually worth it — a 4-bit 70B beats a 16-bit 13B decisively on the same memory.\n- **Below 4 bits**: degradation becomes visible and uneven. It hits long-context behaviour, multi-step reasoning and rare languages hardest, which aggregate benchmarks under-report.\n\n## Two things people forget\n\n**The KV cache is not quantised by weight quantisation.** At long context the cache can exceed the weights, so quantising weights to 4-bit and leaving the cache at FP16 solves half your problem. KV cache quantisation to INT8 or FP8 is a separate switch and usually worth turning on.\n\n**Evaluate on your task, not on a leaderboard.** Perplexity is a poor proxy. Run your own golden set before and after, and specifically test long-context and non-English behaviour, because that is where 4-bit quantisation degrades first and where MMLU will not show it.",
      takeaways: [
        "Weight-only 4-bit is the default for decode because decode is memory-bandwidth-bound.",
        "Activation outliers are the real difficulty; SmoothQuant and AWQ are the two answers to know.",
        "Quantising weights does not touch the KV cache — that is a separate and often larger win."
      ],
      followUps: [
        "Why does per-group scaling beat per-tensor so decisively?",
        "When would you quantise the KV cache and what breaks first?",
        "How does QLoRA differ from GPTQ, given both produce 4-bit weights?"
      ],
      trap: "Reporting only aggregate benchmark scores after quantising. Damage concentrates in long context, multi-step reasoning and low-resource languages; a flat MMLU delta can hide a system that has become useless for your actual users."
    },

    {
      id: "ai-moe",
      q: "How does a Mixture-of-Experts model work, and why is a 200B MoE cheaper to run than a 70B dense model but harder to deploy?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["google-deepmind", "mistral", "meta-ai", "openai", "nvidia", "microsoft", "databricks", "bytedance"],
      tags: ["architecture", "moe", "serving"],
      aiTrack: true,
      simple: "Instead of one big feed-forward block that every token passes through, the model has many smaller ones and a router that sends each token to just two of them. The model holds far more knowledge in total, but any single token only pays for the two experts it visited. The catch is that you still have to keep all of the experts in memory.",
      answer: "## The substitution\n\nReplace the feed-forward block in some or all transformer layers with $N$ parallel experts plus a router:\n\n```\nh = attention(x)\nscores  = softmax(h @ W_router)        # one score per expert\ntop_k   = argtopk(scores, k)           # usually k = 2, sometimes 1 or 8\ny = sum( scores[i] * expert_i(h) for i in top_k )\n```\n\nAttention is untouched and shared. Only the feed-forward part is sparse.\n\n## Active versus total parameters\n\nThis distinction is the question.\n\n| Model shape | Total params | Active per token | Compute cost | Memory cost |\n| --- | --- | --- | --- | --- |\n| Dense 70B | 70B | 70B | High | 140 GB @ FP16 |\n| MoE, 8×22B, top-2 | ~176B | ~39B | **Lower than the 70B** | **~350 GB @ FP16** |\n\nSo: **compute scales with active parameters; memory scales with total parameters.** An MoE gives you the knowledge capacity of a very large model at the inference FLOPs of a medium one — and the VRAM bill of the very large one. That is the entire trade, and it is why MoEs are wonderful for a lab with many GPUs and awkward for a startup with one.\n\n## Training difficulties\n\n**Load balancing.** Left alone, the router collapses onto a few popular experts; the rest never receive gradient and never improve. The fix is an **auxiliary load-balancing loss** penalising the correlation between how often an expert is chosen and its mean routing probability. Newer models (DeepSeek-V3 among them) instead use a per-expert bias adjusted without gradients, avoiding the auxiliary loss's interference with the language objective.\n\n**Capacity factor and dropping.** Each expert has a fixed token capacity per batch. Overflow tokens are dropped — they skip the feed-forward entirely and pass through the residual. A capacity factor of 1.25 is typical; too low silently degrades quality, too high wastes compute and memory.\n\n**Instability.** Router logits drift large, softmax saturates, routing becomes near-deterministic and the model stops exploring. Router z-loss is the standard remedy.\n\n**Fine-tuning is harder.** MoEs overfit more readily on small datasets — each expert sees only a fraction of the data, so the effective data-per-parameter is much lower. Freezing the router and tuning only attention is a common workaround.\n\n## Serving difficulties\n\n**Expert parallelism.** Experts are sharded across GPUs, so every layer requires an all-to-all: send each token to whichever device holds its experts, then send the results back. Two all-to-alls per MoE layer. On a poor interconnect this dominates, and it is why MoEs are far more sensitive to network topology than dense models.\n\n**Unpredictable memory.** A batch whose tokens all route to expert 3 has a very different memory and compute profile from a balanced one. Capacity planning is harder.\n\n**Batch-size effects on quality.** With token-choice routing, tokens compete for capacity, so *what else is in your batch* can affect whether your token gets dropped. This surprises people the first time they see non-deterministic outputs at fixed temperature.\n\n## The honest summary for an interview\n\nAn MoE is the right answer when you have GPUs to spare and want more capability per FLOP — a frontier lab, or a serving fleet at scale. It is the wrong answer when memory is the binding constraint, which is the usual situation for anyone deploying a model themselves. A dense model of the same *active* size is easier to fine-tune, easier to serve and easier to reason about.",
      takeaways: [
        "Compute follows active parameters; memory follows total parameters. That single line answers most MoE questions.",
        "Routing collapses without a load-balancing mechanism, and overflow tokens are silently dropped.",
        "Expert parallelism adds two all-to-alls per layer, making interconnect quality a first-order concern."
      ],
      followUps: [
        "Why is expert-choice routing free of dropped tokens, and what does it break? (Causality — it needs the whole batch.)",
        "How would you fine-tune an MoE on 5,000 examples without destroying it?",
        "What does shared-expert isolation buy? (A common expert for general knowledge, freeing the routed ones to specialise.)"
      ],
      trap: "Saying an MoE is \"cheaper\". Cheaper in FLOPs, more expensive in VRAM and interconnect. If the interviewer is deploying on limited hardware, the honest answer is that an MoE may be the wrong choice entirely."
    },

    {
      id: "ai-rope-long-context",
      q: "How does RoPE encode position, and why does a model trained at 8k break at 32k when you just extend it?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["meta-ai", "mistral", "google-deepmind", "anthropic", "openai", "nvidia", "cohere", "huggingface"],
      tags: ["attention", "position", "long-context"],
      aiTrack: true,
      simple: "Attention has no built-in sense of order, so position has to be injected. RoPE does it by rotating each query and key vector by an angle proportional to its position, which makes the dot product between two tokens depend only on the distance between them. If you then feed the model positions far beyond anything it saw in training, those angles land in a region it has never encountered and the attention pattern falls apart.",
      answer: "## Why position must be injected at all\n\nSelf-attention is permutation-equivariant: shuffle the tokens and the outputs shuffle with them, unchanged. \"Dog bites man\" and \"man bites dog\" would be identical. Position information has to be added explicitly.\n\n## The progression\n\n| Scheme | Mechanism | Extrapolates? |\n| --- | --- | --- |\n| Learned absolute | A trainable vector per index, added to the embedding | **No** — index 5000 has no trained vector at all |\n| Sinusoidal | Fixed sin/cos of position, added | Poorly in practice |\n| **RoPE** | **Rotate** q and k by an angle proportional to position | Somewhat, and extendable |\n| **ALiBi** | Add a distance-proportional linear penalty to attention scores | Well, by construction |\n\n## RoPE\n\nSplit each head's vector into 2-D pairs. Rotate pair $i$ at position $m$ by angle $m\\theta_i$, where\n\n$$\\theta_i = 10000^{-2i/d}$$\n\nEarly pairs rotate fast (high frequency, local detail); late pairs rotate slowly (low frequency, long-range structure). The essential property is\n\n$$\\langle R_m q,\\; R_n k \\rangle = f(q, k, m - n)$$\n\nThe score depends only on **relative** distance, even though the rotation was applied absolutely. That is elegant, and it is why RoPE won: it is applied to q and k directly, needs no extra parameters, and costs almost nothing.\n\n## Why naive extension fails\n\nThe slow, low-frequency pairs are the problem. At $d_{head} = 128$ the slowest pair completes barely a fraction of a full rotation across 8,000 positions. The model has therefore only ever seen a small arc of that pair's rotation. Ask for position 32,000 and you demand an angle from an arc it has never seen — out-of-distribution input to a function that was never asked to be smooth there.\n\nThe symptom is characteristic: perplexity is fine for the first few thousand tokens and then rises sharply, and retrieval from the middle of the context collapses.\n\n## The extension methods\n\n**Position Interpolation (PI).** Scale positions down: $m \\to m \\cdot L_{train}/L_{target}$. Now everything falls inside the trained arc. It works, needs a little fine-tuning, and it compresses high-frequency detail — the model becomes slightly worse at fine local distinctions.\n\n**NTK-aware scaling.** Do not scale all frequencies equally. Leave the high-frequency pairs alone (they already saw full rotations, local detail is preserved) and interpolate only the low-frequency ones. Change the base from 10,000 to a larger value. Often works with **no fine-tuning at all**, which is why it spread so fast.\n\n**YaRN.** NTK-by-parts plus a temperature correction on the attention softmax to compensate for the entropy change at longer context. Currently the strongest of the three; used by several open long-context models.\n\n**ALiBi.** Sidestep the problem: add $-m \\cdot |i - j|$ to the attention scores, with a different slope $m$ per head. Extrapolates naturally, costs nothing, and gives up some ability to attend precisely to a distant specific token — heads with steep slopes effectively cannot see far.\n\n**Sliding-window attention.** Each token attends only to the previous $w$ tokens. Makes the KV cache constant rather than linear in context, and information travels further only through stacked layers.\n\n## The part candidates forget\n\nExtending the context window does not make the model *use* it. \"Lost in the middle\" is a separate and stubborn failure: retrieval accuracy is high at the start and end of a long context and sags in the middle. So an extended-context model still benefits from putting the most relevant retrieved chunk first, and a 128k window is not a substitute for retrieval. Saying that unprompted is what marks out someone who has actually measured it.",
      takeaways: [
        "RoPE rotates q and k so the dot product depends only on relative distance, with no added parameters.",
        "Naive extension fails because low-frequency pairs are asked for angles never seen in training.",
        "NTK-aware scaling and YaRN interpolate selectively; ALiBi avoids the problem at a cost in precise long-range attention."
      ],
      followUps: [
        "Why does ALiBi extrapolate for free where RoPE does not?",
        "What does the attention-temperature term in YaRN correct for?",
        "Does a 1M-token context remove the need for RAG? (No — cost, latency and lost-in-the-middle all remain.)"
      ],
      trap: "Describing RoPE as \"adding\" position information. It multiplies — a rotation applied to q and k — and that is precisely why the resulting score is a function of relative distance."
    },

    {
      id: "ai-normalization-stability",
      q: "Pre-norm or post-norm? Why did RMSNorm replace LayerNorm, and what does a loss spike tell you?",
      topic: "LLM & GenAI",
      level: "staff",
      companies: ["google-deepmind", "meta-ai", "openai", "anthropic", "nvidia", "mistral", "huggingface"],
      tags: ["architecture", "training", "stability"],
      aiTrack: true,
      simple: "Normalisation keeps the numbers flowing through a deep network from exploding or vanishing. Where you put it relative to the residual connection decides whether a hundred-layer model trains at all, and the modern choice — before the block, not after — is why large models can be trained without a delicate warm-up schedule.",
      answer: "## Post-norm, the original\n\n$$x_{l+1} = \\text{LN}(x_l + F(x_l))$$\n\nThe residual stream is normalised at every layer. Gradients flowing backwards are repeatedly rescaled, and in a deep stack they vanish. The original transformer needed a careful learning-rate warm-up to train at all, and beyond about 12 layers it became genuinely unstable.\n\n## Pre-norm, the modern default\n\n$$x_{l+1} = x_l + F(\\text{LN}(x_l))$$\n\nNow there is an **unnormalised identity path from input to output**. A gradient can travel from the loss to layer 1 without passing through a single normalisation. Deep models train without warm-up and tolerate much larger learning rates.\n\nThe cost is real but smaller: the residual stream's magnitude grows with depth (each layer adds to it), so later layers contribute proportionally less — an effective-depth loss. Papers report pre-norm models being very slightly worse at equal depth. Everybody accepts that in exchange for being able to train at all.\n\nA final normalisation before the output head is standard in both.\n\n## RMSNorm\n\nLayerNorm centres and scales:\n\n$$\\text{LN}(x) = \\gamma \\cdot \\frac{x - \\mu}{\\sqrt{\\sigma^2 + \\epsilon}} + \\beta$$\n\nRMSNorm drops the mean subtraction and the bias:\n\n$$\\text{RMS}(x) = \\gamma \\cdot \\frac{x}{\\sqrt{\\frac{1}{d}\\sum x_i^2 + \\epsilon}}$$\n\nTwo reasons it won. **It is measurably faster** — 7–15% of total training step time in large models, because normalisation is memory-bound and re-centring costs an extra pass over the tensor. And **the re-centring turns out not to matter**: the rescaling is what stabilises training, and dropping the mean costs nothing in quality. Llama, Mistral, Gemma and most current models use it.\n\n## The residual stream\n\nWorth naming, because it reframes the architecture usefully. In a pre-norm transformer the residual stream is a **shared communication channel**: every block reads from it (through a normalisation), computes, and writes its result back by addition. Nothing is overwritten. Interpretability work leans on this heavily, and it explains why layers can be pruned or reordered with less damage than you would expect.\n\n## What a loss spike means\n\nA sudden vertical jump in training loss usually comes from one of:\n\n- **Attention logit growth.** The q·k products drift large, softmax saturates to one-hot, gradients vanish through it. The fix is QK-norm — normalise q and k before the dot product — which several recent large models adopted specifically for this.\n- **Bad data.** A batch of repeated tokens, mojibake, or a scraped page of garbage. Check the batch before blaming the optimiser.\n- **Optimiser state.** Adam's second moment collapsing after a long run of tiny gradients, then a normal gradient producing an enormous step. Raising $\\epsilon$ helps.\n- **Numerical range.** FP16 overflow. BF16 has the same exponent range as FP32 and is why it is now standard for training despite worse mantissa precision.\n\nThe recovery procedure everyone actually uses: roll back to the last checkpoint, skip the offending data batches, resume. Say that — it is what practitioners do, and it signals you have watched a real training run rather than only read about one.\n\n## The current default stack\n\nPre-norm, RMSNorm, SwiGLU feed-forward, RoPE, grouped-query attention, BF16 with FP32 master weights. Being able to state that list and give one sentence of justification per item is a strong, compact answer.",
      takeaways: [
        "Pre-norm leaves an unnormalised identity path, which is what makes deep models trainable without warm-up.",
        "RMSNorm drops mean-centring: 7–15% faster, no measurable quality cost.",
        "Loss spikes are usually attention-logit growth, bad data, or optimiser state — and the real-world fix is rollback-and-skip."
      ],
      followUps: [
        "What does QK-norm fix that pre-norm does not?",
        "Why BF16 over FP16 for training, given BF16 has fewer mantissa bits?",
        "What is the effective-depth cost of pre-norm, and does anything recover it? (DeepNorm, sandwich norms.)"
      ],
      trap: "Answering \"pre-norm is more stable\" and stopping. The mechanism — an identity path that gradients traverse without rescaling — is the answer; the stability is the observation."
    },

    {
      id: "ai-parallelism",
      q: "You have 64 GPUs and a 70B model to train. Which parallelism strategy, and why?",
      topic: "System Design",
      level: "staff",
      companies: ["google-deepmind", "meta-ai", "nvidia", "openai", "anthropic", "microsoft", "databricks", "bytedance"],
      tags: ["training", "distributed", "scaling"],
      aiTrack: true,
      simple: "One GPU cannot hold a 70B model in training, so you split the work. You can give each GPU a different slice of data, a different slice of each layer's weights, or a different set of layers. Each split costs a different kind of communication, and the right combination depends almost entirely on how fast the wires between your GPUs are.",
      answer: "## Start with the memory bill\n\nFor a 70B model trained in mixed precision with Adam, per parameter:\n\n| Item | Bytes/param | 70B total |\n| --- | --- | --- |\n| BF16 weights | 2 | 140 GB |\n| BF16 gradients | 2 | 140 GB |\n| FP32 master weights | 4 | 280 GB |\n| Adam momentum (FP32) | 4 | 280 GB |\n| Adam variance (FP32) | 4 | 280 GB |\n| **Subtotal** | **16** | **~1.1 TB** |\n\nPlus activations, which depend on batch size and sequence length and are frequently larger than all of the above.\n\nAn 80 GB GPU holds none of this. **Sharding is not an optimisation here; it is the only way the job runs at all** — and opening with that arithmetic is how you show you understand the problem rather than the vocabulary.\n\n## The four axes\n\n**Data parallelism (DP).** Every GPU holds a full copy, processes a different micro-batch, and gradients are all-reduced. Communication: one all-reduce of the full gradient per step. Simple, and useless alone here because the model does not fit.\n\n**ZeRO / FSDP.** Data parallelism with the redundancy removed. Shard optimiser state (stage 1), then gradients (2), then parameters (3) across the data-parallel group; gather each layer's weights just in time for its forward pass and release them afterwards. Stage 3 cuts per-GPU memory by the DP degree at the cost of extra all-gathers. **This is what most teams should use, and it is the correct default answer.**\n\n**Tensor parallelism (TP).** Split individual matrices across GPUs — attention heads across devices, feed-forward matrices column- then row-wise. Two all-reduces per transformer layer, on the critical path. Extremely bandwidth-hungry, so **keep TP inside one node**, over NVLink. Across a slower network it is a disaster.\n\n**Pipeline parallelism (PP).** Different GPUs hold different layers; micro-batches flow through. Communication is small — just activations at the boundaries — so it tolerates slow links. The cost is the **bubble**: with $p$ stages and $m$ micro-batches, idle fraction is roughly $(p-1)/(m+p-1)$. Interleaved schedules (1F1B) shrink it. Use PP to cross slow network boundaries.\n\n**Sequence / context parallelism.** Split the sequence dimension for very long context, where activations dominate. Ring attention passes KV blocks around the ring.\n\n## The answer for 64 GPUs\n\nAssume 8 nodes × 8 H100s, NVLink inside a node, InfiniBand between them.\n\n```\nTP = 8    within a node, over NVLink        (model fits per node)\nDP = 8    across nodes, with ZeRO-2/3       (throughput + memory)\nPP = 1    not needed at this scale\n```\n\nThe rule to state out loud: **order your parallelism by communication cost against link speed.** TP is chattiest, so it goes on the fastest link and never crosses a node boundary. DP is a single all-reduce per step and tolerates the slower inter-node fabric. PP is cheapest per step, so it is what you reach for when you must span slow links or when the model will not fit even with TP across a node.\n\nAdd activation checkpointing — recompute activations in the backward pass instead of storing them. Roughly 30% more compute for a large memory saving, and essentially always worth it at this scale.\n\n## What goes wrong\n\n- **TP across nodes.** The most common expensive mistake. Watch utilisation collapse to 20%.\n- **Too few micro-batches with PP.** The bubble eats the gain.\n- **Stragglers.** One slow GPU sets the pace of every all-reduce. A single throttling card can halve cluster throughput and it will not appear in any average.\n- **Checkpointing cost.** Saving 1.1 TB takes real time; asynchronous and sharded checkpointing exists for a reason.\n- **Fault rate.** At 64 GPUs for a week, something will fail. Checkpoint frequently enough that a failure costs less than an hour.\n\n## The metric to quote\n\n**Model FLOPs Utilisation (MFU)** — achieved FLOPs divided by hardware peak. Well-tuned large training runs sit at 40–55%. If you are at 15%, you have a communication or pipeline problem, not a hardware problem. Naming MFU and giving a target range is the single most credible thing you can say in this round.",
      takeaways: [
        "Start from the memory arithmetic: ~16 bytes per parameter with Adam, before activations.",
        "Order parallelism by communication intensity against link speed — TP inside a node, DP across, PP to span slow links.",
        "MFU is the metric; 40–55% is healthy and 15% means communication, not hardware."
      ],
      followUps: [
        "What changes if you only have Ethernet between nodes? (More PP, less DP; gradient compression.)",
        "How does ZeRO-3 differ from tensor parallelism, given both shard parameters?",
        "How would you detect a straggler GPU? (Per-rank step-time histograms, not the mean.)"
      ],
      trap: "Naming the techniques without connecting each to its communication pattern. The interviewer is testing whether you can reason from interconnect bandwidth to a configuration, not whether you have memorised four acronyms."
    },

    {
      id: "ai-scaling-laws",
      q: "What did Chinchilla actually change, and why are today's models trained far past compute-optimal?",
      topic: "ML & Statistics",
      level: "staff",
      companies: ["google-deepmind", "openai", "anthropic", "meta-ai", "mistral", "microsoft", "scale-ai"],
      tags: ["scaling", "training", "economics"],
      aiTrack: true,
      simple: "There is a predictable relationship between how big a model is, how much text it is trained on, and how good it gets. For years people made models bigger without giving them proportionally more text. Chinchilla showed that was the wrong split — and then the industry deliberately went past even that answer, because a model you train once and serve a billion times should be optimised for serving, not for training.",
      answer: "## The empirical law\n\nLoss follows a power law in parameters $N$ and training tokens $D$:\n\n$$L(N, D) = E + \\frac{A}{N^{\\alpha}} + \\frac{B}{D^{\\beta}}$$\n\n$E$ is the irreducible entropy of language. The other two terms shrink predictably. Because these curves hold over many orders of magnitude, you can fit them on small runs and **predict the loss of a model you have not trained yet** — which is what makes a nine-figure training run a defensible business decision rather than a gamble. That practical consequence is the real content of the question.\n\n## What Chinchilla found\n\nGiven a fixed compute budget $C \\approx 6ND$, how should you split it?\n\nThe earlier Kaplan work suggested favouring model size. Chinchilla (2022) redid the analysis with a learning-rate schedule matched to each run's length and found something different: **$N$ and $D$ should scale in equal proportion**, at roughly **20 tokens per parameter**.\n\nBy that rule, GPT-3 (175B, 300B tokens — about 1.7 tokens per parameter) was badly under-trained. Chinchilla itself, at 70B parameters and 1.4T tokens, beat it on essentially everything while being 2.5× smaller and consequently much cheaper to serve.\n\n## Why nobody follows the rule any more\n\nChinchilla optimises **training** compute. It ignores inference entirely.\n\nIf a model is served billions of times, total cost is dominated by inference, and inference cost scales with $N$ — not with $D$. So the right objective is different:\n\n> Train a **smaller** model on **far more** data than compute-optimal. You spend more to train it once; you save on every request forever.\n\nLlama 3 8B was trained on roughly 15T tokens — about **1,875 tokens per parameter**, nearly 100× the Chinchilla ratio. It is deliberately, massively \"over-trained\", and that is the correct engineering decision for a model that will be downloaded and run by millions of people.\n\nThe modern framing is **inference-optimal scaling**: pick $N$ from your serving budget, then push $D$ until the loss curve flattens or you run out of data.\n\n## What this means for an AI engineer\n\nYou will not train a frontier model. You will make the decision this analysis implies, constantly:\n\n- A smaller model trained on more data usually beats a larger under-trained one at equal serving cost. This is why 7–8B models became genuinely useful.\n- Data quality shifts the curve, not just the position on it. Deduplication and filtering are worth more than a proportional increase in raw tokens — this is the Phi models' entire thesis.\n- High-quality text is finite. Estimates put usable public text in the low tens of trillions of tokens, which is why synthetic data, multi-epoch training and multimodal data became urgent research directions rather than curiosities.\n\n## The newest wrinkle\n\nTest-time compute breaks the two-variable picture. A model that thinks for longer before answering trades inference FLOPs for quality without changing $N$ or $D$ — a third axis. For a serving team this reframes the trade completely: you can now buy accuracy per request rather than per training run, and the cost lands on the inference line rather than the capital budget. Mentioning that shows you are current rather than reciting 2022.",
      takeaways: [
        "Loss is a predictable power law in parameters and tokens — which is what makes a huge training run a planned investment.",
        "Chinchilla's ~20 tokens/parameter optimises training compute only; inference cost scales with N alone.",
        "Modern models are deliberately over-trained because serving cost dominates total cost."
      ],
      followUps: [
        "How would you decide model size for a product served 10 million times a day?",
        "Why does deduplication improve the loss curve rather than just saving compute?",
        "How does test-time compute change the cost model? (Accuracy becomes a per-request purchase.)"
      ],
      trap: "Quoting \"20 tokens per parameter\" as current best practice. It answers a question — how to spend a fixed training budget — that almost nobody actually has. Say what it optimises and why the industry deliberately violates it."
    },

    {
      id: "ai-gpu-memory-budget",
      q: "Will a 13B model fine-tune on a single 24 GB GPU? Show me the arithmetic.",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["huggingface", "nvidia", "mistral", "sarvam", "scale-ai", "amazon", "databricks", "microsoft"],
      tags: ["memory", "fine-tuning", "hardware"],
      aiTrack: true,
      simple: "You add up four things: the weights, the gradients, the optimiser's bookkeeping, and the intermediate values kept for the backward pass. Full fine-tuning of a 13B model needs about ten times a 24 GB card. With 4-bit weights and LoRA it fits comfortably — and knowing which of the four terms each trick removes is the whole answer.",
      answer: "## The four terms\n\nFor $P$ parameters, mixed-precision training with Adam:\n\n| Term | Bytes per parameter | 13B |\n| --- | --- | --- |\n| BF16 weights | 2 | 26 GB |\n| BF16 gradients | 2 | 26 GB |\n| FP32 master copy | 4 | 52 GB |\n| Adam $m$ | 4 | 52 GB |\n| Adam $v$ | 4 | 52 GB |\n| **Static total** | **16** | **~208 GB** |\n\nPlus activations, roughly\n\n$$\\text{activations} \\approx b \\cdot s \\cdot h \\cdot L \\cdot k$$\n\nfor batch $b$, sequence $s$, hidden size $h$, layers $L$, and $k \\approx 10\\text{–}20$ bytes depending on what the implementation keeps. For 13B ($h = 5120$, $L = 40$) at batch 4 and 2,048 tokens that is another 15–30 GB.\n\n**Full fine-tuning: roughly 230 GB. On 24 GB: no, by an order of magnitude.**\n\n## Removing the terms, one at a time\n\n**LoRA.** Freeze the base weights; train small rank-$r$ adapters. Gradients, master weights and optimiser state now exist only for the adapters — perhaps 0.1% of parameters.\n\n```\nweights (BF16, frozen)    26 GB\nadapters + their Adam     ~0.3 GB\nactivations               ~12 GB\n                          --------\n                          ~38 GB   -> still over 24\n```\n\nBetter, not enough.\n\n**QLoRA.** Quantise the frozen base to 4-bit NF4 and keep LoRA in BF16.\n\n```\nweights (NF4)             ~7 GB\nadapters + Adam           ~0.3 GB\nactivations (checkpointed) ~4 GB\ndequant + fragmentation   ~2 GB\n                          --------\n                          ~13 GB   -> fits, with headroom\n```\n\n**Yes**, with room for a batch size that makes the run finish this week.\n\n## The levers, in the order to reach for them\n\n| Lever | Removes | Cost |\n| --- | --- | --- |\n| **Gradient checkpointing** | Most activation memory | ~30% more compute |\n| **LoRA** | Gradients, master weights, optimiser state | Slightly lower ceiling than full tuning |\n| **4-bit base (QLoRA)** | Three-quarters of the weight memory | Small quality cost, slower steps |\n| **8-bit Adam** | Half the optimiser state | Negligible |\n| **Gradient accumulation** | Nothing — simulates a large batch at small memory | More steps |\n| **Shorter sequences** | Activations, linearly | Task-dependent |\n| **Flash Attention** | The $O(s^2)$ attention matrix | None. Always on |\n\n## Inference is a different sum\n\nNo gradients, no optimiser. Weights plus KV cache:\n\n$$\\text{KV bytes} = 2 \\times L \\times H_{kv} \\times d_{head} \\times \\text{precision} \\times \\text{tokens}$$\n\nA 13B model in 4-bit is about 7 GB, leaving 17 GB of a 24 GB card for cache — tens of thousands of tokens, so dozens of concurrent users. The card that could not fine-tune the model serves it comfortably. Stating that contrast is a good way to close the answer.\n\n## The two things people get wrong\n\n**Forgetting the FP32 master copy.** Mixed precision does not mean everything is 16-bit; the optimiser keeps a full-precision copy or the small updates round to zero. That is 4 bytes per parameter people routinely omit.\n\n**Assuming quantised weights shrink the KV cache.** They are independent. At long context the cache can exceed 4-bit weights, and quantising it is a separate switch.\n\n## The number worth memorising\n\n> Full fine-tune ≈ **16 bytes per parameter** plus activations.\n> QLoRA ≈ **0.6 bytes per parameter** plus activations.\n> Inference at 4-bit ≈ **0.55 bytes per parameter** plus KV cache.\n\nThose three lines answer most hardware questions you will be asked in an hour.",
      takeaways: [
        "Full fine-tuning costs ~16 bytes per parameter before activations; the FP32 master copy is the term people forget.",
        "LoRA removes gradients and optimiser state; QLoRA additionally removes three-quarters of the weight memory.",
        "Weight quantisation does not shrink the KV cache — that is a separate lever and often the larger one."
      ],
      followUps: [
        "Where does gradient checkpointing put the time back, and how much?",
        "Why does mixed precision still need FP32 master weights?",
        "How many concurrent users would that 24 GB card serve at 4k context?"
      ],
      trap: "Answering \"use LoRA\" without the arithmetic. LoRA alone does not fit 13B on 24 GB — the frozen BF16 base is still 26 GB. The 4-bit quantisation is what makes it fit, and that distinction is the question."
    },

    /* ================= retrieval ================= */
    {
      id: "ai-ann-index",
      q: "HNSW, IVF-PQ, or brute force? Choose an index for 50 million embeddings and justify it.",
      topic: "System Design",
      level: "hard",
      companies: ["perplexity", "glean", "cohere", "databricks", "snowflake", "microsoft", "amazon", "google", "palantir"],
      tags: ["retrieval", "vector-search", "indexing"],
      aiTrack: true,
      simple: "Comparing a query against fifty million vectors one by one is exact and far too slow. Approximate indexes trade a little accuracy for enormous speed, in two main families: one builds a navigable graph you walk downhill through, the other clusters the vectors and compresses them. The graph is fast and memory-hungry; the clustered one is compact and needs tuning.",
      answer: "## Size the problem first\n\n50M vectors at 768 dimensions, FP32: $50{,}000{,}000 \\times 768 \\times 4 = 153$ GB of raw vectors. That number decides most of the answer, and producing it immediately is the right opening move.\n\n## The families\n\n| Index | Recall | Latency | Memory | Build | Updates |\n| --- | --- | --- | --- | --- | --- |\n| **Flat** | 100% | Slow, linear | 153 GB | None | Trivial |\n| **HNSW** | 95–99% | **Fastest** | 153 GB + ~40% graph | Slow | Insert cheap, delete awkward |\n| **IVF-Flat** | 90–97% | Fast | 153 GB + centroids | Needs training | Easy |\n| **IVF-PQ** | 80–95% | Fast | **~5 GB** | Needs training | Easy |\n| **ScaNN / DiskANN** | 95–98% | Fast | Small RAM, SSD-resident | Slow | Batch |\n\n## HNSW, briefly\n\nA multi-layer proximity graph. The top layer is sparse and long-range; each layer down is denser. Search enters at the top, greedily walks towards the query, drops a layer, repeats. Logarithmic-ish hops.\n\nThe knobs: $M$ (neighbours per node, 16–64) sets graph quality and memory; `ef_construction` sets build quality; **`ef_search` is the runtime recall/latency dial** and is the one you actually tune in production. The right answer to \"what recall do you need\" is a measurement, not a default.\n\nIts weaknesses: memory (vectors must be resident), slow builds, and deletions that only tombstone — a high-churn collection degrades until rebuilt.\n\n## IVF-PQ, briefly\n\nTwo ideas stacked. **IVF**: cluster into $\\sqrt{N}$-ish Voronoi cells with k-means; search only the `nprobe` nearest cells. **PQ**: split each vector into $m$ sub-vectors, quantise each against a 256-entry codebook, store one byte per sub-vector.\n\n768 dimensions with $m = 96$: 96 bytes instead of 3,072. **32× compression**, so 153 GB becomes about 5 GB and fits in RAM on one machine. Distances are computed against precomputed lookup tables, which is fast.\n\nThe costs: it must be trained on a representative sample; recall drops because compressed distances are approximate; and the **cell-boundary problem** — a true neighbour in an unprobed cell is invisible, which is why `nprobe` matters and why recall is capped by it.\n\nThe standard mitigation is **re-ranking**: retrieve 200 candidates using compressed distances, then recompute exact distances for those 200 from the full vectors. Cheap, and it recovers most of the lost recall.\n\n## The answer\n\nFor 50M vectors, one honest recommendation with a stated assumption:\n\n> **IVF-PQ with re-ranking, or DiskANN, unless recall above 98% is a hard requirement.** 153 GB of RAM for HNSW costs roughly ten times what 5 GB does, and the recall difference after re-ranking is a couple of points. If the workload is latency-critical and the budget is there, HNSW sharded across machines is the alternative.\n\nThen give the decision rule, which is the part that generalises:\n\n- **Under ~1M vectors: use Postgres with pgvector.** No separate system, transactional consistency with your metadata, joins against your real tables, and one fewer thing to operate. Most people reach for a dedicated vector database years before they need one, and interviewers notice when you say this.\n- **1M–100M: IVF-PQ, or HNSW if memory is cheap and recall is critical.**\n- **Beyond 100M: shard, and consider a disk-based index.**\n\n## Filtering, which is where systems actually break\n\nReal queries are \"documents this user can see, from this year, matching this meaning\". Three strategies:\n\n- **Post-filter** — search, then discard. Simple, and catastrophic when the filter is selective: retrieve 100, keep 2.\n- **Pre-filter** — restrict the candidate set, then search. Exact, and it breaks the graph or cell structure the index depends on.\n- **Filtered search** — evaluate the predicate during traversal, as modern implementations do.\n\nSay how you would measure it: **recall@k against a brute-force ground truth on a held-out query set**, computed per filter-selectivity bucket. Aggregate recall hides a total collapse on the narrow filters, and the narrow filters are usually the important users.",
      takeaways: [
        "Compute the raw vector bytes first — 50M × 768 × 4 = 153 GB — because that number picks the index.",
        "IVF-PQ plus re-ranking gets most of HNSW's recall at a thirtieth of the memory.",
        "Under a million vectors, pgvector in your existing Postgres beats adding a vector database."
      ],
      followUps: [
        "How do you handle deletions in HNSW without a full rebuild?",
        "Why does post-filtering destroy recall, and what is the alternative?",
        "How would you measure recall with no labelled data? (Brute force on a sample is the ground truth.)"
      ],
      trap: "Recommending a dedicated vector database by default. For most production corpora — well under a million chunks — pgvector inside the database you already run is faster to build, cheaper, and removes an entire consistency problem."
    },

    {
      id: "ai-embedding-migration",
      q: "A better embedding model is released. Your live RAG system has 20 million chunks indexed. What do you do?",
      topic: "System Design",
      level: "hard",
      companies: ["cohere", "glean", "perplexity", "microsoft", "amazon", "databricks", "salesforce", "snowflake"],
      tags: ["retrieval", "migration", "evaluation"],
      aiTrack: true,
      simple: "Embeddings from two different models are not comparable, so you cannot mix old and new vectors in one index. You must re-embed everything, and you must do it without taking the system down or discovering afterwards that the new model is worse for your data. The answer is to build the second index alongside the first, compare them on real traffic, and switch.",
      answer: "## Why this is not a config change\n\nA vector from model A and a vector from model B live in unrelated spaces. Their cosine similarity is meaningless. There is no partial migration: the index is either wholly old or wholly new. Dimensions usually differ too, so the schema changes as well.\n\n## The plan\n\n**1. Prove it is better on your data, before touching anything.**\n\nPublic benchmarks are not your corpus. MTEB rankings routinely invert on domain-specific text. Take 300–500 real queries with known-good chunks, embed the relevant subset with both models, and compare **recall@k, MRR and NDCG**. If you have no labels, mine them: take answers your users rated positively and record which chunk was cited.\n\nIf the new model is not better on your evaluation, the migration ends here, and that is a successful outcome — you spent a day instead of a month.\n\n**2. Cost the backfill.**\n\n20M chunks, ~500 tokens each = 10B tokens. At roughly \\$0.02 per million tokens for a hosted embedding model, about **\\$200** and several hours to a couple of days depending on rate limits. Self-hosted on a GPU is cheaper and slower to set up. Either way, size it before promising a date — and store the raw chunk text, because if you did not, re-embedding means re-parsing every source document and the cost is an order of magnitude higher.\n\n**3. Dual index, shadow read.**\n\n```\nnew index built offline from the same chunk table\n        |\nquery --+--> old index --> results --> served to the user\n        |\n        +--> new index --> results --> logged only\n```\n\nCompare on live traffic for a week: overlap@10 between the two result sets, latency, and — most importantly — the queries where they disagree most. Read fifty of those by hand. Aggregate metrics will not tell you the new model has quietly become worse at product codes.\n\n**4. Roll out behind a flag.** 5% → 25% → 100%, watching answer quality and user feedback signals at each step. Keep the old index warm until you are confident; the rollback is then a flag flip rather than a two-day re-index.\n\n**5. Handle the writes during the gap.** New documents arriving mid-backfill must be embedded into **both** indexes, or the new one starts life stale. Dual-write from the ingestion path for the whole migration window, then remove it.\n\n## The details that catch people\n\n**Dimension changes** alter memory and index parameters. Going 768 → 1536 doubles storage and changes the HNSW memory profile; re-tune, do not assume.\n\n**Matryoshka embeddings** are worth knowing here: models trained so that a truncated prefix of the vector is still a valid embedding. You can store 1536 dimensions and search on the first 256 for a cheap first pass, then re-rank on the full vector. It makes the storage decision reversible.\n\n**Normalisation and prefixes.** Several models require an instruction prefix (`query: ` / `passage: `) and normalised output. Getting this wrong silently costs 10–20% recall, and it produces no error — the system just gets worse. It is the single most common bug in an embedding migration.\n\n**Chunking may need to change too.** A model with an 8k input window can take larger chunks than one with 512. If you change both at once you cannot attribute the difference — change one variable per evaluation.\n\n## What a strong answer sounds like\n\nA weak answer says \"re-embed everything\". A strong one says: evaluate on our own labelled set first, cost the backfill, dual-write and shadow-read, roll out behind a flag with a rollback, and check the prefix convention. The interviewer is looking for someone who has migrated a live index and remembers what hurt.",
      takeaways: [
        "Two embedding spaces are incomparable, so there is no partial migration — build a second index.",
        "Validate on your own labelled queries first; public benchmark rank frequently inverts on domain text.",
        "Dual-write during the backfill and shadow-read before switching, with a flag you can flip back."
      ],
      followUps: [
        "How would you build the labelled set if you have none? (Mine production logs and citations.)",
        "What do Matryoshka embeddings let you defer?",
        "How would you detect that the new model is worse only for one document type? (Segment the metrics.)"
      ],
      trap: "Forgetting the query/passage prefix convention some models require. It causes no error, costs a large slice of recall, and is invisible unless you are measuring — which is exactly why interviewers like the question."
    },

    {
      id: "ai-rag-permissions",
      q: "Design RAG for a company where every document has different access rules. A user must never see a chunk they are not entitled to.",
      topic: "System Design",
      level: "staff",
      companies: ["glean", "palantir", "microsoft", "salesforce", "cohere", "snowflake", "amazon", "oracle", "google"],
      tags: ["retrieval", "security", "multi-tenancy"],
      aiTrack: true,
      simple: "Search has to respect permissions, and doing it after the search is both slow and dangerous. The correct design carries each document's access list into the index itself, filters during the search rather than after it, and treats a leaked chunk as a security incident rather than a quality bug.",
      answer: "## Why this is the hard version of RAG\n\nA public-corpus RAG system has one failure mode: a wrong answer. A permissioned one has two, and the second is a breach. Once a chunk enters the model's context, it can be summarised, paraphrased or quoted into the answer — **the context window is the trust boundary**, and everything before it is the control.\n\n## Three placements, one right answer\n\n**Post-filter** — retrieve top 50, drop what the user cannot see. Simple, and wrong twice over. It is a correctness disaster (a user with access to 1% of the corpus gets almost nothing back, because the top 50 were dominated by documents they cannot see) and a latency disaster (you over-fetch to compensate). It is also fragile: the day someone forgets the filter, it leaks.\n\n**Pre-filter** — compute the allowed document set, then search within it. Exact, and it fights the index: a graph index cannot be traversed usefully over a 0.1% subset, and cell-based indexes lose their pruning.\n\n**Filtered search** — evaluate the predicate during traversal. This is what modern engines support and what you should propose.\n\n## The design\n\n**Denormalise the ACL into the chunk.** Each chunk row carries the identifiers permitted to read it:\n\n```\nchunk_id | doc_id | text | embedding | acl_groups[] | acl_users[] | tenant_id\n```\n\nAt query time, resolve the user to their full expanded group set (including nested groups) and push a set-overlap predicate into the search:\n\n```sql\nSELECT chunk_id, text, embedding <=> :q AS dist\nFROM chunks\nWHERE tenant_id = :tenant\n  AND (acl_groups && :user_groups OR acl_users @> ARRAY[:user_id])\nORDER BY embedding <=> :q\nLIMIT 50;\n```\n\n**Tenant isolation is a separate, stronger control.** Cross-customer leakage is existential in a way intra-customer leakage is not. Use a separate index or collection per tenant where volume allows, and make `tenant_id` a mandatory partition key so that omitting it is a query error rather than a full scan.\n\n**Permissions change; embeddings do not.** Someone leaves a group and the index must reflect it in seconds, not on the next nightly re-index. Two workable patterns: keep the ACL in a fast side store consulted at query time (accepting a lookup on the path), or subscribe to permission-change events and patch the metadata in place. Never require re-embedding for a permission change — the vector has not changed.\n\n**Re-check at generation time.** Belt and braces: before assembling the context, re-verify each chunk against the live permission service. It costs a few milliseconds and it catches the stale-index window. In a regulated environment this is what an auditor asks about.\n\n## The failure modes to raise unprompted\n\n**Inference through aggregation.** Fifty chunks a user is individually entitled to can together reveal something they are not — headcount, a salary band, an unannounced project. No filter catches this. Say plainly that it is a residual risk mitigated by policy and audit rather than by architecture.\n\n**Citations leaking titles.** \"I cannot answer that\" is safe; \"I cannot answer that, see *Project Everest Q3 Layoff Plan*\" is a breach. Filter the citation metadata by the same rule as the content.\n\n**Cache poisoning.** A semantic or answer cache keyed only on the query text will serve one user's answer to another. **Every cache key must include the permission scope**, not the user id (which would make the cache useless) but a hash of their expanded group set.\n\n**Evaluation must be permission-aware.** Your golden set needs a user attached to each query, and a test that asserts a restricted chunk is *not* retrieved. A retrieval test suite that only checks recall will never catch a leak.\n\n## Closing line worth using\n\nMeasure recall per permission-scope bucket. A user who can see 0.5% of the corpus is the one whose retrieval quality is worst and whose leak would be most serious, and an aggregate number is dominated by the users who can see everything.",
      takeaways: [
        "The context window is the trust boundary; filtering must happen during retrieval, never after.",
        "Denormalise ACLs onto chunks and push a set-overlap predicate into the query — permissions change, embeddings do not.",
        "Every cache key must include the permission scope, or the cache becomes the leak."
      ],
      followUps: [
        "How do you propagate a group membership change in seconds without re-embedding?",
        "How would you test for leaks in CI? (Adversarial queries with a user who should see nothing.)",
        "What do you do about aggregation-based inference? (Name it as residual; policy and audit, not architecture.)"
      ],
      trap: "Post-filtering. It is the intuitive design, it collapses recall for exactly the users with the narrowest access, and it turns one forgotten code path into a data breach."
    },

    {
      id: "ai-chunking-strategy",
      q: "How do you chunk documents for retrieval? Talk me through a 200-page PDF with tables.",
      topic: "LLM & GenAI",
      level: "medium",
      companies: ["perplexity", "glean", "cohere", "anthropic", "openai", "microsoft", "amazon", "salesforce", "adobe", "sarvam"],
      tags: ["rag", "chunking", "retrieval"],
      aiTrack: true,
      simple: "You cannot embed a whole book, so you cut it into pieces. Where you cut decides everything: cut in the middle of a table and neither half means anything; cut too small and the piece loses the context that made it findable. The good strategies cut along the document's own structure and attach enough surrounding context that a piece can stand alone.",
      answer: "## The tension\n\n- **Small chunks** embed precisely — one idea, one vector, high retrieval accuracy.\n- **Large chunks** carry the context the model needs to answer.\n\nThese pull opposite ways, and the good techniques resolve the tension rather than compromising on a chunk size.\n\n## The ladder, worst to best\n\n**1. Fixed size.** 512 tokens, hard cut. Splits sentences, tables and code. It is the default in every tutorial and it is the reason so many RAG demos fail on real documents.\n\n**2. Recursive character splitting.** Try to split on `\\n\\n`, then `\\n`, then `. `, then space. Strictly better than fixed, and it is a reasonable floor.\n\n**3. Structure-aware.** Parse the document first. Markdown headings, HTML sections, PDF outline, code functions. A document already tells you where its seams are; using them is nearly free and is the largest single improvement available.\n\n**4. Contextual retrieval — the technique to name.** Prefix each chunk with a short, generated summary of where it sits:\n\n```\n[Annual Report 2025 > Risk Factors > Supply Chain]\nThis section discusses semiconductor sourcing risk.\n\n---\nThis increased 40% year over year, driven primarily by...\n```\n\nA chunk reading \"this increased 40%\" is unretrievable and useless in isolation; the same chunk with its heading path and a one-line context is both findable and answerable. Published results show large recall improvements, and the cost is one cheap model call per chunk at ingestion — paid once, recovered on every query. This is the answer that marks out someone who has read the current literature.\n\n**5. Small-to-big.** Embed small precise chunks for matching, but return the **parent section** for generation. Decouples \"what matches\" from \"what the model reads\", which is the cleanest resolution of the tension.\n\n**6. Late chunking.** Embed the whole document with a long-context embedding model, then pool token embeddings into chunk vectors. Each chunk's vector has seen the entire document. Elegant; requires a model that supports it.\n\n## The 200-page PDF specifically\n\nPDFs are a layout format, not a document format. There is no reliable notion of a paragraph in the file — only glyphs at coordinates. So:\n\n1. **Extract with a layout-aware tool**, not naive text extraction. Two-column pages otherwise interleave into nonsense, and this alone breaks more pipelines than any other single cause.\n2. **Tables become Markdown, never flowed text.** A table read as prose is noise in both retrieval and generation. If tables matter, extract them separately and store each with its caption and surrounding paragraph as one unit.\n3. **Rebuild the heading hierarchy** from font sizes or the PDF outline, and carry the path into every chunk.\n4. **Split on sections, then subdivide anything over ~800 tokens** at paragraph boundaries with 10–15% overlap.\n5. **Keep page numbers** in the metadata. Users will not trust a citation they cannot open at the right page, and trust is what the citation is for.\n6. **Handle scanned pages separately.** If the text layer is empty, it is an image; route it to OCR or a vision model and mark it lower-confidence.\n7. **Drop the furniture.** Headers, footers, page numbers repeated on every page pollute embeddings and dominate keyword search.\n\n## How to decide, rather than guess\n\nChunking is an empirical question with a cheap experiment. Build 100 questions whose answers you know, then measure **recall@10** across four or five strategies. The whole thing takes an afternoon and it is the highest-information experiment in a RAG project. Reported optima cluster around 300–800 tokens for prose, but yours will differ and the point is that you measured rather than adopted a number from a blog post.\n\nSaying \"I would run that experiment and here is how I would set it up\" is a better answer than any specific chunk size.",
      takeaways: [
        "Split on the document's own structure; a chunk that crosses a table or a section boundary is dead weight.",
        "Contextual retrieval — a generated heading path and summary prefixed to each chunk — is the biggest single recall win available.",
        "Small-to-big separates what matches from what the model reads, which resolves the size tension rather than compromising."
      ],
      followUps: [
        "How would you chunk source code? (By function or class, with the file path and imports as context.)",
        "How do you keep a chunk's context header from dominating its embedding? (Keep it short; weight the body.)",
        "What breaks when documents are updated? (Content-hash each chunk and re-embed only what changed.)"
      ],
      trap: "Naming a chunk size as though it were a fact. The interviewer is testing whether you know chunking is measured, not chosen — and whether you would design the measurement."
    },

    {
      id: "ai-goldenset-bootstrap",
      q: "You inherit a RAG system with no evaluation and no labelled data. Build the evaluation.",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["anthropic", "openai", "scale-ai", "glean", "cohere", "perplexity", "databricks", "microsoft", "salesforce"],
      tags: ["evaluation", "rag", "process"],
      aiTrack: true,
      simple: "You cannot improve what you cannot measure, and nobody hands you a labelled test set. So you make one: generate candidate questions from your own documents, have a person check a couple of hundred of them, and separate the measurement into retrieval and generation so that when the number moves you know which half moved.",
      answer: "## Day one: the retrieval set, which you can build without humans\n\nRetrieval has a machine-checkable ground truth, and this is what makes it the right place to start.\n\nFor each of 300 randomly sampled chunks, ask a model: *write a question this passage answers, which would not be answerable from a generic document*. You now have (question, known-correct-chunk) pairs — for free, at ingestion-time cost.\n\nMeasure:\n\n| Metric | Question it answers |\n| --- | --- |\n| **Recall@k** | Did the right chunk make it into the context at all? |\n| **MRR** | How high did it rank? |\n| **NDCG@k** | Ranking quality when several chunks are relevant |\n\n**Recall@k is the ceiling on your entire system.** If it is 60%, no prompt change can take end-to-end accuracy above 60%. Establishing that number first prevents weeks spent tuning prompts against a retrieval problem, and saying so is the strongest single sentence you can offer in this round.\n\nFilter the generated questions: drop any the source chunk itself does not answer (checked by a model), and drop questions containing rare exact strings that make retrieval trivial.\n\n## Week one: the human-checked golden set\n\nSynthetic questions are cleaner than real ones and therefore optimistic. Take **150–250 real user queries** from logs — stratified across query types, not the head — and have a human record the correct answer and the supporting chunks. This is a day or two of work for one person and it is the most valuable artefact in the project.\n\nStratify deliberately: simple lookups, multi-hop questions, aggregations, questions with no answer in the corpus, and adversarial ones. That last two categories are the ones nobody builds and the ones that catch over-confidence.\n\n## Then: the generation metrics\n\nWith retrieval measured separately, generation can be judged on its own terms:\n\n- **Faithfulness** — decompose the answer into atomic claims; check each is entailed by a retrieved chunk. This is the one that matters, because it is hallucination made countable.\n- **Answer relevance** — does it address the question asked?\n- **Context precision** — how much of what you retrieved was actually used? Directly a cost metric.\n- **Correct refusal rate** — on the unanswerable subset, how often does it decline? Most teams never measure this and it is where over-confidence shows.\n\nRagas implements most of these; the concepts matter more than the library.\n\n## Make the judge trustworthy\n\nAn LLM judge is unavoidable at scale and is itself a model that can be wrong. Calibrate it: have a human label 100 outputs, then measure the judge's agreement with the human (Cohen's kappa). Below about 0.6 the judge is not usable and needs a better rubric or a stronger model. Report the agreement number alongside the scores — an uncalibrated judge is a number generator.\n\nAlso control for the known biases: **position bias** (randomise order in pairwise comparisons), **verbosity bias** (longer answers score higher — cap length or normalise), and **self-preference** (a model rates its own family's output higher, so avoid judging with the same model that generated).\n\n## Wire it into the pipeline\n\nAn evaluation that is not automated stops being run after week three.\n\n```\non pull request:\n    run 50-question smoke set        -> block if recall@5 drops > 3 points\nnightly:\n    run full golden set               -> dashboard, alert on regression\nweekly:\n    sample 20 production answers      -> human review, feed failures back in\n```\n\nEvery production failure a user reports becomes a permanent test case. That loop — bug becomes test case — is what turns an evaluation harness into a ratchet, and it is exactly what a hiring manager wants to hear you say.\n\n## Online, because offline is not enough\n\nThumbs up/down (sparse, biased), copy and regenerate rates (excellent implicit signals), conversation abandonment, escalation to a human, and citation click-through. Offline metrics tell you whether you changed the system; online metrics tell you whether the change helped anyone.\n\n## The honest closing note\n\nSay what the evaluation cannot see. A golden set built from today's corpus will not catch tomorrow's document types, and a judge calibrated on English will not transfer to Hindi. Naming the blind spots is what makes the rest of the numbers believable.",
      takeaways: [
        "Generate a synthetic retrieval set first — recall@k is a hard ceiling on everything downstream.",
        "150–250 human-checked real queries, stratified to include unanswerable ones, is the artefact worth a day of work.",
        "Calibrate the LLM judge against human labels and report the agreement; otherwise it is a number generator."
      ],
      followUps: [
        "How would you detect that your golden set has gone stale?",
        "Which online signal correlates best with quality in your experience? (Regenerate and copy rates.)",
        "How do you stop the judge from preferring its own model family's output?"
      ],
      trap: "Reporting one end-to-end accuracy number. It tells you the system is at 71% and nothing about whether to fix retrieval, ranking or the prompt — which is the only thing the number was needed for."
    },

    {
      id: "ai-reranker-training",
      q: "Your retrieval recall is stuck at 70%. How would you train a reranker or an embedding model to fix it?",
      topic: "ML & Statistics",
      level: "hard",
      companies: ["cohere", "perplexity", "glean", "google", "meta-ai", "bytedance", "huggingface", "microsoft"],
      tags: ["retrieval", "training", "embeddings"],
      aiTrack: true,
      simple: "Off-the-shelf embedding models are trained on general web text and know nothing about your domain's vocabulary. Training your own means showing a model many pairs of question and correct passage, alongside wrong passages that look deceptively similar. Those deliberately tricky wrong answers are what actually teach it.",
      answer: "## First, check it is a model problem\n\nBefore training anything, rule out the cheaper fixes, because three of them beat a custom model most of the time:\n\n- **Add BM25 and fuse with RRF.** Dense retrieval fails on exact tokens — part numbers, error codes, rare acronyms. This is usually the single largest improvement available and it costs an afternoon.\n- **Add a cross-encoder reranker** from the shelf. Retrieve 100, rerank to 8.\n- **Fix chunking.** Contextual headers on chunks routinely move recall more than a model swap.\n\nIf recall is still short after those, training is justified. Saying this before proposing training is most of the answer.\n\n## Bi-encoder versus cross-encoder\n\n| | Bi-encoder | Cross-encoder |\n| --- | --- | --- |\n| Input | Query and document **separately** | Query and document **together** |\n| Output | Two vectors, compared by cosine | One relevance score |\n| Can pre-compute? | **Yes** — the corpus is indexed offline | No — every pair at query time |\n| Cost | Millions of documents, milliseconds | ~100 documents, tens of milliseconds |\n| Accuracy | Good | **Much better** |\n\nHence the two stages: a bi-encoder for recall over everything, a cross-encoder for precision over the survivors. Train whichever is your bottleneck. If recall@100 is high but recall@5 is low, the reranker is where the gain is — and that is a far cheaper training job.\n\n## The training data\n\n**Positives.** Mine them rather than write them. Production logs where a user accepted the answer; support tickets paired with the article that resolved them; question-answer pairs from documentation; click logs. Ten thousand mined pairs beat a thousand hand-written ones.\n\nWhere nothing exists, generate: ask a model to write questions for each chunk, then filter with a second pass that checks the chunk actually answers the question. Noisy, and workable.\n\n**Negatives — the part that decides everything.**\n\n- **In-batch negatives.** Every other document in the batch is treated as a negative. Free, and this is why contrastive training uses large batches: more negatives per step. But random documents are trivially distinguishable, so the signal saturates.\n- **Hard negatives.** Documents your *current* retriever ranks highly and that are nonetheless wrong. These are what teach the model the distinctions that matter. Mine them by running your existing retriever over the training queries and taking ranks 10–50.\n- **The trap: false negatives.** A mined \"hard negative\" is often a second correct passage. Training on it teaches the model that a correct answer is wrong. Filter with a cross-encoder or a model check before use — this single step separates a reranker that helps from one that quietly makes things worse.\n\n## The objective\n\nInfoNCE with temperature, for a bi-encoder:\n\n$$\\mathcal{L} = -\\log \\frac{\\exp(\\text{sim}(q, d^+)/\\tau)}{\\exp(\\text{sim}(q, d^+)/\\tau) + \\sum_j \\exp(\\text{sim}(q, d_j^-)/\\tau)}$$\n\n$\\tau$ around 0.02–0.05. Lower $\\tau$ sharpens the distribution and emphasises the hardest negatives — too low and training destabilises.\n\nFor a cross-encoder, a simple binary cross-entropy on (query, document, relevant) triples works, or a listwise loss if you have graded relevance.\n\n## Practicalities\n\n- **Start from a strong base** (a current open embedding or reranker model), never from scratch.\n- **LoRA is enough.** Full fine-tuning of a 300M-parameter encoder is possible but rarely necessary.\n- **Watch for catastrophic forgetting.** A model tuned hard on your domain gets worse everywhere else. Mix in some general data, and evaluate on a general benchmark as a guard.\n- **Re-embed the whole corpus afterwards.** A new embedding model means a full re-index — see the migration question. A **reranker**, by contrast, requires no re-indexing at all, which is a strong practical argument for training that one first.\n\n## Report it honestly\n\nRecall@10 before and after, on a held-out set of real queries, with latency and cost alongside. A reranker that adds 40 ms and six points of recall is a clear win; one that adds 400 ms for two points is not, and saying which is which is the judgement being tested.",
      takeaways: [
        "Exhaust hybrid search, an off-the-shelf reranker and better chunking before training anything.",
        "Hard negatives are the entire signal — and false negatives among them are the most common way the training silently fails.",
        "Train the reranker first: it needs no re-indexing, unlike a new embedding model."
      ],
      followUps: [
        "How do you mine hard negatives without poisoning the set with false ones?",
        "Why do contrastive methods want large batches?",
        "When is a late-interaction model like ColBERT the right middle ground?"
      ],
      trap: "Jumping to training. Interviewers are largely testing whether you reach for the cheap fixes first — BM25 fusion alone frequently closes the gap the training was proposed to close."
    }

  ]);
})(window.TD = window.TD || {});
