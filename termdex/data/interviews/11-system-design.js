/* Interview bank — system design.

   A design round is graded on whether you can size a system, name the
   bottleneck, and say what you are giving up. Every answer below therefore
   leads with numbers and ends with a trade-off, because that is the shape of
   a passing answer. */
(function (TD) {
  "use strict";

  TD.addQuestions([
    {
      id: "sd-url-shortener",
      q: "Design a URL shortener handling 100 million new links per month at a 100:1 read-to-write ratio.",
      topic: "System Design",
      level: "medium",
      companies: ["google", "microsoft", "amazon", "adobe", "flipkart", "salesforce", "uber", "bytedance"],
      tags: ["sizing", "caching", "encoding", "nosql"],
      simple: "You are building a lookup table with a very short key. The interesting parts are not the table — they are how you generate short keys without collisions across many servers, and how you serve reads fast enough that the database never sees most of them.",
      answer: "## Start with the arithmetic\n\nDo this out loud. It is half the grade.\n\n- Writes: 100M/month ≈ **40 writes/sec** average, say **400/sec** at peak.\n- Reads: 100× that ≈ **4,000/sec** average, **40,000/sec** peak.\n- Storage: 100M rows × ~500 bytes ≈ **50 GB/year**. Trivial.\n- Key space: 7 characters of base-62 gives $62^7 \\approx 3.5 \\times 10^{12}$ — about 3,000 years at this rate.\n\nThe conclusion falls straight out of those numbers: **this is a read-heavy caching problem, not a storage problem.**\n\n## Generating the short code\n\nThree options, and the reason to reject two of them:\n\n1. **Random 7 characters, retry on collision.** By the birthday bound, collisions become common once you have used a meaningful fraction of the space. Worse, every write needs a read to check — doubling your write path.\n2. **Hash the URL, take 7 characters.** Deterministic, so the same URL always maps to the same code, which may or may not be wanted. Collisions still need handling.\n3. **Monotonic counter, base-62 encoded.** No collisions by construction, no read before write.\n\nOption 3 wins, and the only real problem is generating the counter across many servers. Do not use a single database sequence — it is a synchronous global bottleneck. Instead:\n\n- **Ranges.** Each app server claims a block of 10,000 IDs from a coordination service and hands them out locally. One round trip per 10,000 writes. Gaps appear when a server dies holding a partial block; nobody cares.\n- **Snowflake IDs.** 41 bits of timestamp, 10 of machine ID, 12 of per-millisecond sequence. No coordination at all, and the IDs sort by time.\n\n```python\nALPHABET = \"0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ\"\n\ndef encode(n):\n    if n == 0:\n        return ALPHABET[0]\n    out = []\n    while n:\n        n, r = divmod(n, 62)\n        out.append(ALPHABET[r])\n    return \"\".join(reversed(out))\n```\n\n## The read path\n\n```\nGET /aB3xK9p\n      |\n      v\n  CDN / edge         <- 302s can be edge-cached briefly\n      |  miss\n      v\n  App server\n      |\n      +--> Redis (cache-aside, 24h TTL)   <- ~95% of reads stop here\n      |        miss\n      v\n  Key-value store (partitioned on code)\n      |\n      +--> async event -> Kafka -> analytics\n```\n\nAt a 95% cache hit rate, the database sees 2,000 reads/sec at peak — comfortable for a single primary with read replicas.\n\n## 301 versus 302 — this is the question behind the question\n\n**HTTP 301 (Moved Permanently) is cached by the browser indefinitely.** The second time a user clicks the link, the request never reaches your server, so your click count is wrong forever and you can never change where the link points.\n\n**HTTP 302 (Found)** is not cached by default, so every click is counted and the destination stays editable.\n\nIf analytics matter, use 302 and accept the load. If they do not, 301 is faster and cheaper. State the trade-off explicitly — this is the detail interviewers use to separate people who have built this from people who have read about it.\n\n## Analytics without slowing the redirect\n\nNever write to the analytics store on the request path. Fire an event to Kafka and return the redirect immediately; a consumer aggregates counts. If a few events are lost during an outage, a click count is off by a rounding error — an acceptable loss for keeping the P99 redirect under 10 ms.\n\n## Custom aliases\n\nA user-chosen alias needs a uniqueness check, so this path *does* read before writing. Keep custom aliases in the same table with a unique constraint and let the database reject duplicates rather than checking first — a check-then-insert races.",
      takeaways: [
        "Do the arithmetic out loud; it determines the whole design.",
        "Counter plus base-62 avoids collisions entirely; ID ranges or Snowflake avoid the coordination bottleneck.",
        "302 keeps analytics working, 301 is faster — name the trade-off rather than picking silently."
      ],
      followUps: [
        "Add link expiry. (TTL in the store plus a lazy check on read.)",
        "Prevent abuse — malware and phishing links. (Safe Browsing lookup on write, plus a takedown path.)",
        "Make it multi-region. (Codes are immutable, so replicate freely; only the counter needs care.)"
      ],
      trap: "Reaching for a relational database with an auto-increment primary key and stopping there. It works at this scale, but if you cannot say what breaks when writes go 100× you have not answered the question."
    },
    {
      id: "sd-news-feed",
      q: "Design a news feed for 500 million daily users. How do you handle a celebrity with 100 million followers?",
      topic: "System Design",
      level: "hard",
      companies: ["meta-ai", "bytedance", "google", "amazon", "netflix", "uber"],
      tags: ["fanout", "caching", "ranking", "hot-key"],
      simple: "You can either write each post into every follower's mailbox as it happens, or leave posts where they are and gather them when someone opens the app. The first is fast to read and terrible for celebrities. The second is cheap to write and slow to read. Real systems do both, choosing per author.",
      answer: "## Sizing first\n\n- 500M daily users, each opening the feed ~10 times: **~58,000 feed reads/sec** average, call it 150,000 at peak.\n- Posts: say 100M/day ≈ **1,200 writes/sec**.\n- Average follower count ~200, so naive fan-out on write is **240,000 mailbox writes/sec**. Large but survivable.\n\n## Fan-out on write (push)\n\nWhen someone posts, append the post ID into every follower's precomputed feed list — typically a capped Redis list of a few hundred entries per user.\n\n- Read: one Redis range query. Sub-millisecond.\n- Write: $O(\\text{followers})$.\n\nThis is right for the overwhelming majority of users.\n\n## Fan-out on read (pull)\n\nStore posts once. On read, fetch the posts of everyone the user follows and merge them.\n\n- Write: $O(1)$.\n- Read: expensive, and gets worse the more people you follow.\n\n## The celebrity problem\n\nAt 100 million followers, fan-out on write means one post generates 100 million Redis writes. Even at 100,000 writes/sec per shard that is minutes of delay, and it monopolises the cluster while a hundred other celebrities post too.\n\n**The hybrid is the answer.** Classify authors by follower count:\n\n- **Below a threshold (~10,000 followers):** push. Their posts are already in every follower's feed.\n- **Above it:** do not push at all. Store their posts in a small per-celebrity list.\n\nOn read, take the user's precomputed feed and **merge in the recent posts of the handful of celebrities they follow**. A user follows at most a few dozen celebrities, so this is a few dozen cheap lookups against a tiny, extremely hot, heavily cached set.\n\n```\nfeed(user):\n    base      = redis.lrange(\"feed:\" + user, 0, 200)        # pushed posts\n    celebs    = follows_celebrities(user)                    # usually < 50\n    extra     = [recent_posts(c) for c in celebs]            # hot cache\n    merged    = merge_by_time(base, flatten(extra))\n    return rank(merged)[:50]\n```\n\nThis is genuinely how Twitter and Instagram solved it. Naming the threshold and the merge step is what a strong answer looks like.\n\n## Ranking\n\nChronological is a starting point, not an answer. A ranked feed is two stages:\n\n1. **Candidate generation.** Reduce thousands of possible posts to a few hundred, cheaply — recency, affinity, and an embedding-based retrieval.\n2. **Ranking.** Score those few hundred with a heavier model predicting engagement, then apply diversity and integrity rules on top.\n\nThe split exists because you cannot afford to run the expensive model over thousands of candidates within a 200 ms budget.\n\n## Consistency\n\nFeeds are eventually consistent and users accept it — a post appearing two seconds late is invisible. But **your own post must appear in your own feed immediately**, or the app feels broken. Handle that with a read-your-writes path: after posting, inject the post locally on the client and into the author's own cached feed synchronously.\n\n## Failure behaviour\n\nIf the ranking service is down, fall back to reverse-chronological. A slightly worse feed beats no feed — and being able to name that degradation is exactly what a Netflix-style interviewer is listening for.",
      takeaways: [
        "Push for normal users, pull for celebrities, merge at read time — the hybrid is the real answer.",
        "Ranking is two stages because you cannot score thousands of candidates in the latency budget.",
        "Read-your-writes for the author is the one place eventual consistency is not acceptable."
      ],
      followUps: [
        "How does a user unfollow someone? (Do not rewrite feeds — filter at read time.)",
        "How do you avoid showing the same post twice? (Seen-set per user, bloom-filtered.)",
        "How would you A/B test a new ranking model safely?"
      ],
      trap: "Committing to pure fan-out on write and only discovering the celebrity problem when the interviewer raises it. Bring it up yourself — the question exists to see whether you spot it."
    },
    {
      id: "sd-chat-system",
      q: "Design a real-time chat system supporting one-to-one and group messages, with delivery receipts and offline delivery.",
      topic: "System Design",
      level: "hard",
      companies: ["meta-ai", "bytedance", "microsoft", "amazon", "uber", "salesforce", "netflix"],
      tags: ["websockets", "queueing", "ordering", "presence"],
      simple: "Every online user holds an open connection to some server. To send a message you have to find which server is holding the recipient's connection — that is what the routing layer is for. If they are offline, the message waits in storage until they come back and ask for everything they missed.",
      answer: "## Transport\n\n**WebSockets**, not polling. A persistent bidirectional connection avoids per-message HTTP overhead and lets the server push. Long polling is the fallback for restrictive networks; server-sent events are one-directional and so are not sufficient alone.\n\nBudget roughly 10 KB of kernel and userspace memory per idle connection. One tuned server handles ~100,000 connections, so 10 million concurrent users needs ~100 chat servers plus headroom.\n\n## The architecture\n\n```\n  Client A                                         Client B\n     |  WebSocket                                     |  WebSocket\n     v                                                v\n +-----------+                                  +-----------+\n | Chat srv 1|                                  | Chat srv 7|\n +-----+-----+                                  +-----+-----+\n       |                                              ^\n       |  1. persist                                  | 4. push\n       v                                              |\n  +---------+     2. look up B's server        +------+------+\n  | Message | ------------------------------>  |  Routing /  |\n  |  store  |     3. forward via pub/sub       |  presence   |\n  +---------+                                  +-------------+\n```\n\n1. A sends to its own chat server.\n2. The server assigns an ID, persists the message, and acknowledges to A (**sent**).\n3. It asks the routing layer which server holds B. If B is connected, publish to that server; it pushes to B (**delivered**).\n4. If B is offline, the message simply stays in the store. On reconnect B asks for everything after its last-seen message ID.\n\nPersisting before acknowledging is what makes the system durable. Acknowledge first and a server crash loses messages that the sender believes were sent.\n\n## Message IDs and ordering\n\nDo not use timestamps as IDs — clocks skew across servers, and two messages in the same millisecond tie. Use **Snowflake-style IDs**: time-ordered, unique, and sortable, so `WHERE id > last_seen ORDER BY id` is the entire sync query.\n\nOrdering only needs to be consistent **within a conversation**, not globally. That is a much weaker and cheaper guarantee. Partition by conversation ID and a single partition gives you per-conversation order for free.\n\n## Group messages\n\nFor small groups (up to a few hundred), fan out to each member — reuse the one-to-one path. For large groups, do not fan out on write: store once per group and let members pull, exactly as with the celebrity feed problem.\n\n## Delivery receipts\n\nThree states, three different acknowledgements:\n\n| State | Triggered by |\n| --- | --- |\n| Sent | Server persisted it |\n| Delivered | Recipient's client acknowledged receipt |\n| Read | Recipient's client reported the chat was open |\n\nEach is a separate small message travelling the reverse path. They are high-volume and low-value, so batch them — send one \"read up to message X\" rather than one per message.\n\n## Exactly-once, honestly\n\nYou cannot have exactly-once delivery over an unreliable network. What you can have is **at-least-once delivery plus idempotent handling**: the client generates a UUID per message, the server deduplicates on it, and a retry after a lost acknowledgement is harmless. Say it this way — claiming exactly-once is a red flag.\n\n## Presence\n\nOnline status is high-churn and low-value. Store it in Redis with a TTL refreshed by heartbeat, and never persist it. Do not broadcast every status change to every contact of a popular user — that is a fan-out storm for information nobody is watching. Push presence only for conversations the user currently has open.",
      takeaways: [
        "Persist before acknowledging, or a crash silently loses messages the sender saw as sent.",
        "Ordering per conversation is enough, and is far cheaper than global ordering.",
        "At-least-once plus client-generated idempotency keys — never claim exactly-once."
      ],
      followUps: [
        "Add end-to-end encryption. (Signal protocol; the server can no longer help with search or group fan-out.)",
        "Support message editing and deletion across devices.",
        "How do you sync across a user's five devices? (Per-device cursors against one message log.)"
      ],
      trap: "Storing presence in the main database. It changes constantly, is worthless if lost, and will become the hottest write path in your system."
    },
    {
      id: "sd-rate-limiter-distributed",
      q: "Design a distributed rate limiter for an API gateway running on 50 nodes.",
      topic: "System Design",
      level: "hard",
      companies: ["amazon", "google", "netflix", "uber", "salesforce", "microsoft", "flipkart"],
      tags: ["redis", "atomicity", "distributed", "latency"],
      simple: "Each gateway node sees only part of the traffic, so no single node can tell whether a user is over their limit. Either they share one counter — accurate but a network hop on every request — or each keeps a local budget and they reconcile occasionally, which is fast but approximate.",
      answer: "## The central problem\n\nA user's requests land on any of 50 nodes. Node-local counters mean the real limit is 50× what you configured. So the counter must be shared.\n\n## Centralised counter in Redis\n\nThe read-modify-write **must be atomic**. Separate `GET` then `SET` races: two nodes read 99, both allow, both write 100, and the user got 101 requests through a limit of 100.\n\nA Lua script runs atomically inside Redis, in one round trip:\n\n```lua\n-- KEYS[1] = bucket key, ARGV[1] = limit, ARGV[2] = window seconds\nlocal current = redis.call('INCR', KEYS[1])\nif current == 1 then\n  redis.call('EXPIRE', KEYS[1], ARGV[2])   -- only the first request sets TTL\nend\nif current > tonumber(ARGV[1]) then\n  return 0\nend\nreturn 1\n```\n\nSetting the TTL only when the counter is created is deliberate: refreshing it on every request would keep extending the window and the limit would never reset.\n\nCost: one round trip, roughly 0.5–1 ms in-datacentre. At 100,000 requests/sec that is 100,000 Redis operations/sec — a single Redis instance handles this, but it is now a hard dependency on your request path.\n\n## The latency and availability problem\n\nAdding a millisecond to every request is real, and if Redis is down, everything stops. Two mitigations:\n\n**Fail open.** If Redis is unreachable, allow the request. You would rather serve unlimited traffic briefly than serve none. Make this an explicit, logged decision — for a limiter protecting against billing abuse rather than overload, you might choose to fail closed instead.\n\n**Local token buckets with periodic reconciliation.** Each node claims a slice of the global budget (limit / 50, plus a share of unused capacity) and enforces locally with zero network cost, syncing usage every second. Slightly over-permissive at the boundaries; effectively free per request. This is how large-scale limiters actually work.\n\n## Choosing the algorithm\n\nUse the **sliding window counter**: two integers per key, error under 1%, and no boundary burst. The fixed window counter permits 2× the limit across the seam; the sliding log is exact but stores every timestamp.\n\n## Where to enforce\n\n| Layer | Good for | Limitation |\n| --- | --- | --- |\n| CDN / edge | Volumetric floods | No application context |\n| API gateway | Per-key and per-plan limits | The usual home |\n| Service | Expensive-endpoint limits | Too late to save the gateway |\n\nRun cheap coarse limits at the edge and precise per-user limits at the gateway.\n\n## Responding correctly\n\nReturn **429 Too Many Requests** with:\n\n- `Retry-After: 14` — seconds until capacity frees up. Without it, well-behaved clients retry immediately and make things worse.\n- `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset` so clients can self-pace.\n\nDo not return 503; it tells the client your service is broken rather than that they are over quota, and their retry logic will differ.",
      takeaways: [
        "The counter update must be atomic — a Lua script in Redis, not GET followed by SET.",
        "Set the TTL only when creating the key, or the window never resets.",
        "Fail open or closed is a deliberate product decision; state which and why."
      ],
      followUps: [
        "Different limits per plan tier, changeable at runtime.",
        "Limit by cost rather than request count. (Weighted tokens per endpoint.)",
        "Stop a single abusive IP from exhausting a shared pool. (Layered limits: per-IP, per-key, global.)"
      ],
      trap: "Non-atomic increments. The code looks obviously correct, passes every single-node test, and breaks precisely when it matters — under concurrent load."
    },
    {
      id: "sd-notification-service",
      q: "Design a notification service delivering push, SMS and email, with retries and deduplication.",
      topic: "System Design",
      level: "medium",
      companies: ["amazon", "uber", "microsoft", "salesforce", "flipkart", "netflix", "adobe"],
      tags: ["queueing", "retries", "idempotency", "third-party"],
      simple: "The tricky part is not sending — it is that the outside providers you depend on are slow and unreliable. So you accept the request, put it on a queue, return immediately, and let workers deal with the flaky parts. Retries need care, because sending the same alert five times is worse than not sending it.",
      answer: "## Shape of the system\n\n```\n  Service --> Notification API --> Kafka topic --> Worker pool --> APNs / FCM\n                    |                                        \\-> Twilio\n                    |                                        \\-> SES\n                    v\n              validate + dedupe\n                    |\n                    v\n            template + preference store\n```\n\nThe API validates, resolves the template, checks user preferences, and enqueues. It **does not call the provider** — that would tie your latency and availability to Twilio's.\n\n## Partitioning\n\nPartition the topic by **user ID**, not round-robin. That gives you per-user ordering (a \"your order shipped\" cannot overtake \"your order was placed\") and lets you apply per-user rate limits inside one consumer without coordination.\n\n## Retries\n\nExponential backoff with jitter:\n\n$$\\text{delay} = \\min(\\text{cap},\\ \\text{base} \\times 2^{n}) \\times (0.5 + \\text{rand}(0, 0.5))\n$$\n\nThe jitter is not optional. Without it, every notification that failed during a provider outage retries at exactly the same moment, and you re-create the outage yourself the instant the provider recovers.\n\nRetry only what is retryable:\n\n| Provider response | Action |\n| --- | --- |\n| 5xx, timeout | Retry with backoff |\n| 429 | Retry, honouring `Retry-After` |\n| 400 (bad payload) | Do not retry — fix or drop |\n| 410 (token gone) | Do not retry, and delete the device token |\n\nRetrying a 400 forever is a classic dead-letter-queue filler.\n\n## Deduplication\n\nThe caller supplies an idempotency key. Store it with a TTL longer than your maximum retry window:\n\n```python\ndef enqueue(notification, idem_key):\n    if not redis.set(\"notif:\" + idem_key, \"1\", nx=True, ex=86400):\n        return {\"status\": \"duplicate\"}          # already accepted\n    kafka.produce(topic, key=notification.user_id, value=notification)\n```\n\n`SET NX EX` is a single atomic operation. Checking existence and then setting is a race that lets duplicates through under concurrent retries.\n\nThis gives at-least-once delivery with duplicate *suppression* — not exactly-once, which is unachievable across a network boundary you do not control.\n\n## Dead-letter queue\n\nAfter N attempts, move the message to a DLQ with the full failure history. A DLQ nobody looks at is just a slower deletion, so it needs an alert on depth and a documented replay path.\n\n## Preferences and quiet hours\n\nCheck at **send** time, not enqueue time — a user may unsubscribe while the message sits in the queue. Store per-user, per-channel, per-category preferences plus a timezone-aware quiet-hours window, and hold non-urgent notifications until morning rather than dropping them.\n\n## Priority\n\nA one-time password and a marketing digest must not share a queue: a marketing burst would delay logins. Use separate topics with separate worker pools, so a flood of low-priority traffic cannot starve the high-priority path.",
      takeaways: [
        "Never call a third-party provider on the request path — enqueue and return.",
        "Jitter on retries prevents your own recovery from re-creating the outage.",
        "`SET NX EX` is the atomic dedupe primitive; check-then-set races."
      ],
      followUps: [
        "Guarantee ordering for one user across channels.",
        "Handle a provider outage lasting hours. (Queue depth, backpressure, and a secondary provider.)",
        "Add delivery and open tracking without slowing the send path."
      ],
      trap: "Retrying every failure identically. A permanently invalid device token retried with backoff for a day wastes the worker pool and hides the real failures in the noise."
    },
    {
      id: "sd-video-streaming",
      q: "Design a video streaming platform. Focus on upload, transcoding and delivery.",
      topic: "System Design",
      level: "hard",
      companies: ["netflix", "bytedance", "google", "amazon", "adobe", "meta-ai"],
      tags: ["cdn", "transcoding", "storage", "adaptive-bitrate"],
      simple: "One uploaded video has to become a dozen versions — different resolutions and bitrates — so any device on any connection can play it. Then those pieces get pushed close to viewers geographically, because the speed of light is the real constraint on video.",
      answer: "## Upload\n\nA 4 GB upload over a mobile connection will fail. Use **resumable chunked upload**: split into 5–10 MB parts, upload each with its own retry, and let the client resume from the last successful part. S3 multipart upload does this; so does the `tus` protocol.\n\nUpload directly to object storage using a **pre-signed URL** rather than through your API servers. Otherwise every byte of every video crosses your application tier for no reason.\n\n## Transcoding\n\nOne source file becomes a ladder of renditions:\n\n| Rendition | Resolution | Bitrate |\n| --- | --- | --- |\n| 240p | 426×240 | 400 kbps |\n| 480p | 854×480 | 1 Mbps |\n| 720p | 1280×720 | 2.5 Mbps |\n| 1080p | 1920×1080 | 5 Mbps |\n| 4K | 3840×2160 | 15 Mbps |\n\nThe pipeline is a DAG: validate → extract metadata → **split into segments** → transcode segments in parallel → package (HLS/DASH) → generate thumbnails → publish.\n\nSegment-level parallelism is the key throughput decision. A two-hour film split into 6-second segments is 1,200 independent transcode jobs; on 100 workers that is minutes rather than hours. Segments are independent because each starts on a keyframe.\n\nUse spot or preemptible instances — the work is idempotent and restartable, so a lost worker just means re-running one segment.\n\n## Delivery and adaptive bitrate\n\nSegments plus a manifest. The client reads the manifest, measures its own throughput and buffer level, and requests the next segment at whatever rendition it can sustain. The **client** adapts, not the server — which is why a CDN can serve video with no application logic at all.\n\n```\nmaster.m3u8\n  ├── 240p/index.m3u8  -> seg0.ts, seg1.ts, ...\n  ├── 720p/index.m3u8  -> seg0.ts, seg1.ts, ...\n  └── 1080p/index.m3u8 -> seg0.ts, seg1.ts, ...\n```\n\nEvery rendition must use **aligned keyframes** at identical timestamps, or switching mid-stream produces a visible glitch.\n\n## Caching strategy\n\nVideo popularity follows a power law: a small fraction of titles is the overwhelming majority of traffic.\n\n- **Push** the predicted-popular catalogue to edge caches ahead of demand — Netflix ships physical appliances into ISP networks for exactly this.\n- **Pull** the long tail on first request.\n\nA 95%+ edge hit rate is achievable and is the difference between a viable business and a bandwidth bill that eats the company.\n\n## Storage tiering\n\nKeep the original master forever in cold storage (you will need it when a new codec arrives). Keep hot renditions on fast storage. Move renditions of unwatched content to infrequent-access tiers, and consider deleting rarely-requested renditions entirely — regenerating from the master costs less than storing them.\n\n## The metric that matters\n\nNot bitrate — **rebuffer ratio** and **time to first frame**. A viewer will accept 480p; they will not accept a spinner. Every design choice above serves those two numbers, and saying so is what distinguishes a senior answer.",
      takeaways: [
        "Chunked resumable upload straight to object storage via pre-signed URLs.",
        "Segment-level parallel transcoding turns hours into minutes and tolerates spot instances.",
        "The client picks the bitrate; keyframes must align across renditions or switching glitches."
      ],
      followUps: [
        "Add live streaming. (Low-latency HLS or WebRTC; the transcode budget collapses to seconds.)",
        "Add DRM. (Widevine/FairPlay, per-segment keys, and a licence server.)",
        "Detect and take down copyrighted uploads. (Perceptual hashing against a reference database.)"
      ],
      trap: "Transcoding the whole file as one job. It is simple, it is 10× slower, and a single failure late in a two-hour encode throws away all the work."
    },
    {
      id: "sd-search-autocomplete",
      q: "Design search autocomplete for a site with a billion queries a day.",
      topic: "System Design",
      level: "hard",
      companies: ["google", "amazon", "bytedance", "meta-ai", "adobe", "uber"],
      tags: ["trie", "caching", "latency", "streaming"],
      simple: "Suggestions have to appear before the user finishes typing, which means under about 100 milliseconds including the network. That budget is too small to compute anything, so everything is precomputed and the request is just a lookup.",
      answer: "## The latency budget\n\nSuggestions must feel instantaneous, which means **under ~100 ms end to end**, of which 40–60 ms is network. That leaves roughly 40 ms of server time — and that budget is what forces every other decision.\n\n## Two systems, not one\n\n**Data collection (offline, minutes to hours behind).**\nQuery logs → aggregate counts in a streaming job → build a trie with top-k cached at each node → publish a new snapshot.\n\n**Serving (online, microseconds).**\nA read-only, in-memory trie. No database call on the request path.\n\nThe separation matters because you cannot afford to update a shared trie concurrently while serving reads. Build a fresh snapshot, then atomically swap the pointer. Readers never lock.\n\n## The serving structure\n\nEach trie node caches its own top-k completions, so answering is: walk $p$ characters, read a cached list. $O(p)$ with a tiny constant.\n\nA full trie over a billion distinct queries is far too large for memory, so:\n\n- **Prune aggressively.** Queries seen fewer than ~50 times in a week are noise. This typically removes 90%+ of distinct queries.\n- **Compress to a radix tree.** Collapse single-child chains, which are the bulk of the nodes.\n- **Shard by prefix.** The first two characters give ~1,300 well-balanced shards.\n\n## Caching\n\nPrefix popularity is extremely skewed. A small cache of the most common prefixes serves a large share of traffic:\n\n- Browser-side cache for the user's own recent prefixes.\n- CDN cache with a short TTL — suggestions are the same for everyone, so they are highly cacheable.\n- Server-side LRU in front of the trie.\n\nCache hit rates above 90% are normal, and each layer strips traffic before it reaches the trie.\n\n## Freshness\n\nA daily rebuild misses breaking news, which is exactly when autocomplete matters most. Run a **real-time layer** alongside: a streaming job over the last few minutes of queries, surfacing sharp risers. Merge the real-time candidates with the batch trie at query time. This is a lambda architecture, and autocomplete is one of the cases where it is genuinely justified.\n\n## Personalisation\n\nBlend a small per-user trie of their own history with the global one. Keep it client-side where possible — it is faster, it avoids a per-user server structure, and it is better for privacy.\n\n## Safety\n\nAutocomplete puts words in your product's mouth. You need a blocklist applied at build time, suppression of suggestions involving named private individuals, and a manual override path. This is not optional polish; it is a recurring source of real-world incidents.",
      takeaways: [
        "Split offline trie building from online serving; swap snapshots atomically so reads never lock.",
        "Prune low-frequency queries — it is what makes the structure fit in memory.",
        "A real-time layer merged over the batch trie is what handles breaking news."
      ],
      followUps: [
        "Handle typos. (Levenshtein automaton, or index common misspellings.)",
        "Support multiple languages and scripts.",
        "Measure quality. (Suggestion acceptance rate and reduction in query abandonment.)"
      ],
      trap: "Querying a database per keystroke. Even a 5 ms query is fatal — a user typing eight characters generates eight requests, and the tail latency compounds into visible stutter."
    },
    {
      id: "sd-payment-idempotency",
      q: "Design a payment system. How do you guarantee a customer is never charged twice?",
      topic: "System Design",
      level: "hard",
      companies: ["amazon", "flipkart", "uber", "salesforce", "netflix", "microsoft", "adobe"],
      tags: ["idempotency", "saga", "consistency", "double-entry"],
      simple: "The network can fail at any point, including after the money moved but before you heard back. So the client attaches a unique ticket number to each attempt, and the server promises that the same ticket number never causes a second charge — retries just return the first result.",
      answer: "## The core problem\n\nThe client sends a charge request. The network drops the response. The client does not know whether the charge happened. If it retries and you process it again, you have double-charged a customer — which is a regulatory and trust failure, not just a bug.\n\n## Idempotency keys\n\nThe **client** generates a unique key per logical payment attempt (not per retry) and sends it as a header. The server stores it before doing anything else:\n\n```sql\nCREATE TABLE payment_attempts (\n    idempotency_key  TEXT PRIMARY KEY,\n    request_hash     TEXT NOT NULL,\n    status           TEXT NOT NULL,     -- in_progress | succeeded | failed\n    response_body    JSONB,\n    created_at       TIMESTAMPTZ NOT NULL DEFAULT now()\n);\n```\n\n```python\ndef charge(key, request):\n    try:\n        db.execute(\n            \"INSERT INTO payment_attempts (idempotency_key, request_hash, status)\"\n            \" VALUES (%s, %s, 'in_progress')\", (key, hash_of(request)))\n    except UniqueViolation:\n        prior = db.fetch_one(\n            \"SELECT * FROM payment_attempts WHERE idempotency_key = %s\", (key,))\n        if prior[\"request_hash\"] != hash_of(request):\n            raise Conflict(\"key reused with a different request\")   # 422\n        if prior[\"status\"] == \"in_progress\":\n            raise Conflict(\"original request still running\")        # 409, retry later\n        return prior[\"response_body\"]                               # replay the result\n\n    result = call_payment_processor(request)\n    db.execute(\"UPDATE payment_attempts SET status = %s, response_body = %s\"\n               \" WHERE idempotency_key = %s\", (result.status, result.body, key))\n    return result.body\n```\n\nThree details do the real work:\n\n1. **The unique constraint is the lock.** `INSERT` and catch the violation — do not `SELECT` then `INSERT`, which races.\n2. **Hash the request body.** If the same key arrives with different parameters, that is a client bug and must be rejected loudly, not silently treated as a duplicate.\n3. **`in_progress` is a real state.** A retry arriving while the first is still running gets a 409, not a second charge.\n\n## Money is never a single UPDATE\n\nUse **double-entry bookkeeping**. Every movement is two rows that sum to zero, and balances are derived from the ledger rather than stored:\n\n```\nledger_entries\n  txn_id  | account          | amount | currency\n  ------- | ---------------- | ------ | --------\n  t_9f2   | user:1234        | -49.99 | USD\n  t_9f2   | merchant:acme    | +49.99 | USD\n```\n\nA constraint that entries per transaction sum to zero makes an entire class of bug impossible. An audit is then a sum, and every balance can be reconstructed from history.\n\n## Distributed transactions\n\nCharging a card, decrementing inventory, and creating an order span several services. Two-phase commit blocks and does not survive a coordinator failure. Use a **saga**: a sequence of local transactions, each with a compensating action.\n\n```\nreserve inventory  --> charge card  --> confirm order\n      |                    |\n  release stock      refund charge      (compensations, run in reverse)\n```\n\nCompensation is not rollback. A refund is a *new* transaction that appears in the ledger; the original charge still happened and is still visible. That is the correct behaviour for money, and saying so is the point.\n\n## Reconciliation\n\nWhatever you build, run a daily job comparing your ledger against the processor's settlement file. Discrepancies happen — a webhook was lost, a timeout resolved differently on each side. The reconciliation job is how you find them, and every real payment system has one.\n\n## Never trust the client for amounts\n\nThe amount comes from your own priced cart, server-side. A client-supplied amount is a free-money vulnerability and it still ships regularly.",
      takeaways: [
        "The unique constraint on the idempotency key *is* the concurrency control — insert and catch, never check-then-insert.",
        "Hash the request so a reused key with different parameters is rejected rather than silently replayed.",
        "Double-entry ledger plus a daily reconciliation job; compensating transactions, not rollbacks."
      ],
      followUps: [
        "Handle a webhook that arrives before your own database commit. (Retry with backoff; the webhook is authoritative but may be early.)",
        "Support partial refunds and chargebacks.",
        "Multi-currency. (Store the rate used at transaction time — never re-derive it later.)"
      ],
      trap: "Making the idempotency key server-generated. It has to come from the client, because only the client knows that this retry is the same logical attempt as the last one."
    },
    {
      id: "sd-metrics-monitoring",
      q: "Design a metrics and monitoring system ingesting 10 million data points per second.",
      topic: "System Design",
      level: "hard",
      companies: ["netflix", "amazon", "google", "databricks", "uber", "microsoft", "snowflake"],
      tags: ["time-series", "aggregation", "storage", "cardinality"],
      simple: "Metrics arrive constantly and are mostly read as summaries over time ranges. So you store them in a shape optimised for that: sorted by time, heavily compressed, and pre-aggregated into coarser buckets as they age. What kills these systems is not volume — it is too many distinct label combinations.",
      answer: "## Sizing\n\n10M points/sec × 16 bytes raw ≈ 160 MB/sec ≈ **13 TB/day** uncompressed. That number rules out storing raw data indefinitely and drives everything that follows.\n\n## Push versus pull\n\n**Pull (Prometheus).** The server scrapes targets. Service discovery tells you what should exist, so a missing target is itself an alert. Simpler to reason about; struggles with short-lived jobs.\n\n**Push (StatsD, OpenTelemetry).** Clients send. Handles batch jobs and serverless naturally; you need a separate mechanism to notice something stopped reporting.\n\nLarge deployments run both — pull for long-lived services, push through a gateway for ephemeral work.\n\n## Ingestion path\n\n```\nAgents --> Load balancer --> Ingest tier --> Kafka --> Writers --> TSDB\n                                              |\n                                              +--> Stream aggregator --> alerts\n```\n\nKafka in the middle absorbs bursts and lets you replay after a storage outage. Without it, a slow write tier applies backpressure all the way to the agents, which then drop data — precisely during the incident you most need to observe.\n\n## Storage\n\nA time-series database, not a general-purpose one. Three properties make it work:\n\n**Delta-of-delta encoding for timestamps.** Points arrive at regular intervals, so the *difference between successive differences* is almost always zero and compresses to a bit or two.\n\n**XOR encoding for values.** Consecutive float values are usually similar; XOR them and most bits are zero. Gorilla — Facebook's paper on this — reports roughly **1.37 bytes per point**, down from 16.\n\n**Time-partitioned immutable blocks.** Write to a two-hour in-memory head block, then flush it as an immutable compressed block. Retention becomes deleting whole files rather than row-by-row deletion.\n\n## Downsampling\n\nNobody queries last year at one-second resolution. Roll up as data ages:\n\n| Age | Resolution | Retention |\n| --- | --- | --- |\n| 0–2 days | 10 s | raw |\n| 2–30 days | 1 min | rolled up |\n| 1–13 months | 5 min | rolled up |\n| > 13 months | 1 hour | rolled up |\n\nStore min, max, sum, count and a quantile sketch per bucket — not just the average, because an average hides exactly the spikes you are looking for.\n\n## Cardinality is what actually kills you\n\nA time series is identified by its full label set. Adding one label with many values multiplies the number of series:\n\n```\nhttp_requests{service=\"api\", endpoint=\"/users\", status=\"200\"}          -> fine\nhttp_requests{service=\"api\", endpoint=\"/users\", user_id=\"8a3f...\"}     -> catastrophe\n```\n\nA user ID label on a million users creates a million series from one metric. This is the number one cause of monitoring outages, and it is always self-inflicted.\n\nDefences: enforce a per-metric series limit at ingest, reject unknown labels, and alert on cardinality growth itself. Put high-cardinality identifiers in **traces and logs**, where they belong, and keep metrics low-cardinality.\n\n## Percentiles do not average\n\nYou cannot average the P99 of ten hosts to get the fleet P99 — that is not how quantiles compose. Store a mergeable sketch (t-digest, DDSketch) or a histogram of bucket counts, and compute the quantile at query time from the merged distribution.",
      takeaways: [
        "Delta-of-delta plus XOR encoding gets ~1.4 bytes per point; it is what makes the volume affordable.",
        "Downsample as data ages, keeping min/max/sum/count and a quantile sketch — never just the mean.",
        "Cardinality, not throughput, is what takes these systems down. Never label with a user ID."
      ],
      followUps: [
        "Design the alerting layer. (Rule evaluation, `for` duration to suppress flapping, and grouping.)",
        "Query across a year of data efficiently. (Query the downsampled tier and say so in the UI.)",
        "Handle a metrics agent that has been buffering for six hours and floods on reconnect."
      ],
      trap: "Averaging percentiles across hosts or over time. It is wrong, it is extremely common, and it consistently under-reports the tail latency you are monitoring for."
    },
    {
      id: "sd-caching-strategies",
      q: "Compare cache-aside, write-through, write-behind and refresh-ahead. When does each fail?",
      topic: "System Design",
      level: "medium",
      companies: ["amazon", "google", "microsoft", "netflix", "uber", "flipkart", "salesforce", "oracle"],
      tags: ["caching", "consistency", "patterns"],
      simple: "The question is who writes to the cache and when. Either the application manages it, or the cache sits in front of the database and manages itself. Every option trades freshness against latency against complexity — there is no free choice.",
      answer: "## Cache-aside (lazy loading)\n\nThe application owns the cache:\n\n```python\ndef get_user(uid):\n    user = cache.get(uid)\n    if user is None:\n        user = db.query(uid)          # miss: fetch\n        cache.set(uid, user, ttl=300) # and populate\n    return user\n\ndef update_user(uid, data):\n    db.update(uid, data)\n    cache.delete(uid)                 # invalidate, do NOT update\n```\n\nThe default, and usually correct. Only requested data is cached, and a cache failure degrades to slow rather than broken.\n\n**Delete, do not update.** Two concurrent writers can interleave their cache updates so the cache ends up holding the older value permanently. Deleting is idempotent and forces the next read to fetch the truth.\n\n**Failure mode: the thundering herd.** A popular key expires and a thousand concurrent requests all miss and all hit the database at once. Fix with a per-key lock (only one request refills; the rest wait briefly), or by refreshing slightly *before* expiry.\n\n## Write-through\n\nWrites go to the cache, which synchronously writes to the database.\n\n- Cache is never stale.\n- Every write pays both latencies.\n- Cold data is cached whether or not anyone reads it, wasting memory.\n\nGood when reads immediately follow writes. Combine with a TTL, or data that is never read again lingers forever.\n\n## Write-behind (write-back)\n\nWrites go to the cache and return immediately; the database is updated asynchronously.\n\n- Fastest writes, and it absorbs bursts by coalescing repeated updates to the same key.\n- **A cache node crash loses acknowledged writes.**\n\nOnly acceptable where losing recent writes is survivable — view counters, session activity, analytics. Never for orders or payments.\n\n## Refresh-ahead\n\nThe cache proactively refreshes entries that are about to expire and are being actively read.\n\n- Hides refresh latency entirely for hot keys.\n- Wastes work on keys that would not have been requested again.\n\nWorth it when access is predictable and the backing query is expensive.\n\n## Comparison\n\n| Pattern | Write latency | Staleness | Data loss risk | Use when |\n| --- | --- | --- | --- | --- |\n| Cache-aside | DB only | Until invalidated | None | Default |\n| Write-through | DB + cache | None | None | Read-after-write |\n| Write-behind | Cache only | None | **Yes** | Tolerant counters |\n| Refresh-ahead | n/a | Very low | None | Predictable hot keys |\n\n## The two hard problems\n\n**Cache stampede** — covered above. Locking, jittered TTLs, or probabilistic early expiry.\n\n**Cache penetration** — repeated requests for a key that does not exist in the database either. Every one is a miss and hits the database. Cache the negative result with a short TTL, or put a Bloom filter in front to reject known-absent keys immediately.\n\n## Always jitter the TTL\n\nSetting every key to exactly 300 seconds means everything cached during a deploy expires simultaneously. Use `300 + random(0, 60)` and the expiries spread out.",
      takeaways: [
        "On write, delete the cache entry rather than updating it — concurrent updates can leave stale data permanently.",
        "Write-behind is the only pattern that can lose acknowledged writes; restrict it to tolerant data.",
        "Jitter every TTL, or a deploy creates a synchronised expiry storm."
      ],
      followUps: [
        "How do you invalidate across a multi-region cache? (Pub/sub invalidation, or short TTLs and accept staleness.)",
        "Cache an expensive query whose result changes on any of a hundred rows. (Version key, or tag-based invalidation.)",
        "What does a 60% hit rate tell you? (Usually the TTL is too short or the key space is too fine-grained.)"
      ],
      trap: "Updating the cache on write instead of deleting. It seems more efficient and it introduces a permanent-staleness race that is nearly impossible to reproduce in testing."
    },
    {
      id: "sd-cap-pacelc",
      q: "Explain CAP, and then explain why PACELC is the more useful framework in practice.",
      topic: "System Design",
      level: "medium",
      companies: ["amazon", "google", "databricks", "snowflake", "oracle", "netflix", "uber", "palantir"],
      tags: ["consistency", "distributed", "theory"],
      simple: "CAP says that when the network splits, you must choose between every node agreeing and every node still answering. But networks are usually fine — and even then you are still choosing between fast and consistent. PACELC is the version that covers both situations.",
      answer: "## CAP, stated correctly\n\nIn the presence of a network **partition (P)**, a distributed system must choose between:\n\n- **Consistency (C)** — every read sees the most recent write (this is linearizability, and it is stricter than the C in ACID).\n- **Availability (A)** — every request to a non-failing node gets a non-error response.\n\nThe common misstatement is \"pick two of three\". You do not get to pick P. Partitions happen to you; the theorem is about what you do when one occurs.\n\n## Why CAP is not enough\n\nPartitions are rare. CAP says nothing about the other 99.9% of the time — and the interesting trade-off exists then too.\n\n## PACELC\n\n> **If** there is a **P**artition, choose **A**vailability or **C**onsistency; **E**lse, choose **L**atency or **C**onsistency.\n\nThe second clause is the one you live with daily. Even with a healthy network, a linearizable read must confirm with a quorum, and that confirmation costs a round trip.\n\n| System | Partition | Normal operation |\n| --- | --- | --- |\n| DynamoDB (default) | PA | EL — eventually consistent reads are faster and cheaper |\n| DynamoDB (strong) | PC | EC — strongly consistent reads cost 2× and are slower |\n| Cassandra | PA | EL — tunable per query |\n| Spanner | PC | EC — TrueTime commit-wait buys linearizability, at a latency cost |\n| MongoDB | PC | EC by default |\n| PostgreSQL (single node) | n/a | Not distributed; no partition to survive |\n\n## What this looks like in an interview\n\nDo not say \"it is AP\" and stop. Say what the user experiences:\n\n- **Shopping cart:** AP. A cart that will not open loses a sale; a cart that briefly shows a stale item does not. Merge divergent replicas on read.\n- **Inventory decrement for the last unit:** CP. Overselling costs real money and customer trust.\n- **Follower count:** AP, obviously. Nobody notices being off by three for a second.\n- **Bank balance transfer:** CP, non-negotiable.\n\nThe strongest answers are per-operation, not per-system. The same product will want AP for browsing and CP for checkout, and that is a design decision you should make explicitly.\n\n## Tunable consistency\n\nDynamo-style systems let you choose per request with $R$ (read replicas consulted), $W$ (write replicas acknowledged) and $N$ (total replicas):\n\n$$R + W > N \\implies \\text{strong consistency}$$\n\nBecause the read set and the write set must overlap by at least one node, guaranteeing the read sees the latest write. With $N = 3$: $W = 3, R = 1$ makes reads fast and writes fragile; $W = 1, R = 3$ is the reverse; $W = 2, R = 2$ is the balanced default.\n\n## Eventual consistency is a spectrum\n\n\"Eventually consistent\" alone is not a useful description. What users actually need is usually one of:\n\n- **Read-your-writes** — you see your own comment immediately.\n- **Monotonic reads** — you never see time go backwards.\n- **Causal consistency** — a reply never appears before the message it replies to.\n\nThese are much cheaper than linearizability and are usually what \"consistent enough\" really means.",
      takeaways: [
        "You do not choose P — partitions happen. CAP is about what you do during one.",
        "PACELC adds the everyday trade-off: even with a healthy network, consistency costs latency.",
        "Decide per operation, not per system; and name the session guarantee you actually need."
      ],
      followUps: [
        "Why is Spanner not a counterexample to CAP? (It chooses C and becomes unavailable during a partition — it just makes partitions very rare.)",
        "Design a shopping cart that is AP. (CRDTs, or last-write-wins with a merge on read.)",
        "How do you give read-your-writes on an eventually consistent store? (Sticky routing, or a client-held version token.)"
      ],
      trap: "\"Pick two of three.\" It is the folk version, it is wrong, and every senior interviewer is listening for it."
    },
    {
      id: "sd-microservices-boundaries",
      q: "When should a monolith be split into microservices, and where do the boundaries go?",
      topic: "System Design",
      level: "medium",
      companies: ["amazon", "netflix", "microsoft", "salesforce", "uber", "flipkart", "adobe"],
      tags: ["architecture", "organisation", "coupling"],
      simple: "Microservices buy you the ability for teams to deploy independently. They cost you a network hop, a distributed transaction problem, and much harder debugging. If you do not have the first problem, you are paying for nothing.",
      answer: "## The honest starting position\n\nStart with a well-structured monolith. Most teams that adopt microservices early get the costs immediately and the benefits never — because the benefits are organisational and only appear at a certain team size.\n\n## What microservices actually buy\n\n1. **Independent deployment.** Team A ships without coordinating with Team B. This is the real reason, and it is worth a lot at 200 engineers and nothing at 12.\n2. **Independent scaling.** The image processor needs GPUs; the login service does not.\n3. **Technology heterogeneity.** Rarely a good enough reason on its own.\n4. **Fault isolation** — but only if you actually add bulkheads and circuit breakers. Without those, a synchronous call chain propagates failure faster than a monolith would.\n\n## What they cost\n\n- Every in-process call becomes a network call that can fail, retry, time out, and arrive twice.\n- Transactions across services need sagas, and eventual consistency becomes a product decision.\n- Debugging needs distributed tracing to be tolerable.\n- Local development needs a way to run or mock a dozen services.\n- Operational surface multiplies: deploys, dashboards, on-call rotations, schema versions.\n\n## Where to draw the boundary\n\n**By business capability, not by technical layer.** A \"database service\" and a \"business logic service\" are not microservices — they are a distributed monolith with a network hop in the middle, and every change touches both.\n\nUseful tests:\n\n- **Conway's law.** Boundaries that do not match team boundaries create constant cross-team coordination — exactly what you were buying your way out of.\n- **The transaction test.** If two things must change atomically, they belong in one service. Needing a saga to do something routine is a sign the boundary is wrong.\n- **The change-coupling test.** Look at your git history. Files that always change together belong together.\n- **Data ownership.** Each service owns its data exclusively. Two services writing one table are one service pretending to be two.\n\n## Communication\n\n| Style | Use for | Cost |\n| --- | --- | --- |\n| Synchronous (REST/gRPC) | The caller needs the answer now | Coupling, cascading failure |\n| Asynchronous (events) | Notifying that something happened | Eventual consistency, ordering |\n\nPrefer asynchronous where the caller does not need an answer. Every synchronous hop multiplies into your availability: five services at 99.9% each, called in series, is 99.5% — about four hours of downtime a month from nothing being individually broken.\n\n## The strangler fig migration\n\nDo not rewrite. Put a facade in front of the monolith, route one capability at a time to a new service, and let the monolith shrink. Each step is independently reversible, which is the entire point.\n\n## When to say no\n\nIf the team is small, the domain is not yet understood, or the pain is performance rather than deployment coordination — a modular monolith with clean internal boundaries is the better engineering decision. Being willing to say that in an interview is a strength, not a weakness.",
      takeaways: [
        "The benefit is organisational — independent deployment. Without that problem you are paying for nothing.",
        "Boundaries follow business capabilities and data ownership, never technical layers.",
        "Chained synchronous calls multiply availability downward; prefer events where an answer is not needed."
      ],
      followUps: [
        "How do you handle a query needing data from four services? (API composition, or CQRS with a read model.)",
        "How do you version an API without breaking consumers? (Additive changes; expand then contract.)",
        "How do you test across service boundaries? (Consumer-driven contract tests.)"
      ],
      trap: "Proposing microservices as an automatic best practice. The strongest answer names the team size and deployment-coupling pain that justifies the split — and says when a monolith is correct."
    },
    {
      id: "sd-ecommerce-inventory",
      q: "Design a flash-sale system. 10,000 units, one million people hitting buy at the same instant. Nobody may oversell.",
      topic: "System Design",
      level: "hard",
      companies: ["flipkart", "amazon", "bytedance", "uber", "salesforce", "adobe"],
      tags: ["concurrency", "redis", "queueing", "consistency"],
      simple: "A hundred people can read 'one left' at the same moment and all decide to buy it. The fix is to make 'check and decrement' a single indivisible operation, so only one of them can win. Then the slow parts — payment, order creation — happen afterwards, off the critical path.",
      answer: "## Why the obvious approach fails\n\n```sql\nSELECT stock FROM items WHERE id = 1;      -- 100 requests all read 1\n-- ... application decides it is fine ...\nUPDATE items SET stock = stock - 1 WHERE id = 1;   -- 100 decrements\n```\n\nClassic read-modify-write race. Stock goes negative and you have sold inventory you do not have.\n\n## Layer 1: shed load before it arrives\n\nA million requests must not reach your application tier.\n\n- Static page and assets on the CDN.\n- **Admission control**: admit a random fraction, or issue queue tokens ahead of the sale. If there are 10,000 units, letting 100,000 requests through is generous; letting a million through is self-harm.\n- Reject early: users who already bought, users failing bot checks, requests without a valid sale token.\n\n## Layer 2: atomic decrement in Redis\n\nThis is the core. A Lua script executes atomically inside Redis — no other command interleaves:\n\n```lua\n-- KEYS[1] = stock key, KEYS[2] = buyers set\n-- ARGV[1] = user id\nif redis.call('SISMEMBER', KEYS[2], ARGV[1]) == 1 then\n  return -2                                  -- already bought\nend\nlocal stock = tonumber(redis.call('GET', KEYS[1]))\nif not stock or stock <= 0 then\n  return -1                                  -- sold out\nend\nredis.call('DECR', KEYS[1])\nredis.call('SADD', KEYS[2], ARGV[1])\nreturn stock - 1                             -- remaining\n```\n\nOne round trip, one atomic decision, ~100,000 operations/sec on a single instance. The duplicate-purchase check lives in the same script deliberately — split it out and it races.\n\n## Layer 3: asynchronous fulfilment\n\nA successful decrement returns immediately with \"reserved\". It does **not** wait for payment.\n\n```\nreserve (Redis, atomic)  -->  Kafka  -->  order service  -->  payment\n        ~1 ms                              seconds\n```\n\nThe user sees \"you got one — complete payment within 10 minutes\". Payment gateways take seconds and fail often; holding a million users on a synchronous payment call is not survivable.\n\n## Releasing abandoned reservations\n\nA reservation that is not paid within the window must return to stock. A delayed message or a sweeper job re-increments Redis and marks the order expired. Without this, abandoned carts permanently consume inventory.\n\n**The re-increment must not exceed the original stock.** Use a compare-and-set bounded by the initial count, or a sold-count that only increases and a derived remaining value.\n\n## Layer 4: durability\n\nRedis is the source of truth during the sale, which means a Redis failure is a correctness problem, not just an availability one. Mitigate with:\n\n- AOF persistence with `appendfsync everysec` — bounded loss.\n- Replica with automatic failover.\n- The database as the reconciling authority afterwards: a unique constraint on `(sale_id, user_id)` and a check that the order count never exceeds the stock. The database is the backstop even though it is not on the hot path.\n\n## Alternative: pre-generate tokens\n\nBefore the sale, push exactly 10,000 tokens into a Redis list. Buying is `LPOP`. When the list is empty, it is sold out. `LPOP` is atomic, the count can never go negative by construction, and it is even simpler than the Lua script. Worth offering as the cleaner design.\n\n## The database-only alternative\n\nIf you must do it in the database:\n\n```sql\nUPDATE items SET stock = stock - 1\n WHERE id = 1 AND stock > 0;      -- the guard is the whole trick\n-- then check affected row count: 0 means sold out\n```\n\nA single conditional `UPDATE` is atomic and cannot oversell. It is correct — it simply will not do 100,000 per second on one row, because every transaction serialises on the same row lock.",
      takeaways: [
        "Check-and-decrement must be one atomic operation; a Redis Lua script or a guarded conditional UPDATE.",
        "Reserve fast, fulfil asynchronously — never hold users on a synchronous payment call.",
        "Pre-generated tokens popped from a list make overselling structurally impossible."
      ],
      followUps: [
        "Prevent bots and scalpers. (Pre-registration, device fingerprinting, per-account limits.)",
        "The sale spans regions. (Partition stock per region, or accept one global authoritative shard.)",
        "How do you test this? (Load test at 10× expected, and verify the invariant that sold never exceeds stock.)"
      ],
      trap: "Using a distributed lock around read-then-write. It is correct and it serialises every buyer through one lock, giving you a queue that moves at one purchase per network round trip. The atomic operation avoids the lock entirely."
    },
    {
      id: "sd-observability-three-pillars",
      q: "You are paged: P99 latency has tripled. Walk me through the investigation.",
      topic: "System Design",
      level: "hard",
      companies: ["netflix", "amazon", "google", "uber", "databricks", "microsoft", "snowflake"],
      tags: ["debugging", "observability", "incident-response"],
      simple: "Start by asking what changed and who is affected, before asking why. Narrow from 'the system is slow' to 'this endpoint, for these users, since this time' — and only then go looking for a cause. Most people invert this and start guessing.",
      answer: "## First: stop the bleeding, then diagnose\n\nIf there is a recent deploy, roll it back before you finish the investigation. Restoring service and finding the cause are separate activities, and doing them in the wrong order costs users.\n\n## Narrow the blast radius\n\nBefore asking *why*, establish *what*:\n\n| Question | What it rules out |\n| --- | --- |\n| Every endpoint or one? | One endpoint points at a specific code path or query |\n| Every region or one? | One region points at infrastructure, not code |\n| All users or a segment? | A segment points at data shape — a big tenant, a bad cache key |\n| Started when? | Correlate with deploys, config changes, traffic shifts |\n| P50 too, or only P99? | **This is the most informative question** |\n\n**If P50 is flat and P99 tripled**, most requests are fine and a subset is pathological — a hot shard, a slow tenant, garbage collection pauses, a cache miss path, lock contention. **If P50 moved too**, something systemic changed: a dependency, a resource ceiling, a bad query plan.\n\n## Use the three pillars in the right order\n\n1. **Metrics — where and when.** Dashboards narrow to the service and the time.\n2. **Traces — which hop.** Pull traces from the slow tail specifically, not average traces. A trace shows exactly which downstream call ate the time. This is the step people skip, and it is usually the one that answers the question.\n3. **Logs — why.** Now that you know the service and the moment, read the logs for that specific slice.\n\nGoing metrics → logs directly means reading a haystack. Traces are what tell you which haystack.\n\n## The usual causes, roughly in order of frequency\n\n- **A deploy.** Always check first. Correlate the change in the graph with the deploy marker.\n- **A dependency degraded.** Your P99 is somebody else's P50.\n- **A query plan flipped.** A table crossed a size threshold and the planner switched from index scan to sequential scan. Latency triples with no code change at all.\n- **Cache hit rate dropped.** A deploy changed a key format, a cache node was lost, a TTL was shortened.\n- **Connection pool exhaustion.** Requests queue for a connection. The symptom is latency; the cause is saturation. Check pool wait time, not just query time.\n- **Garbage collection or CPU throttling.** In Kubernetes, CPU limits cause throttling that looks exactly like slow code.\n- **A hot partition.** One shard receives disproportionate traffic; its latency dominates the tail.\n- **Retry amplification.** Something slowed slightly, clients retried, load rose, everything slowed more. The system is now sustaining its own outage.\n\n## Confirm before you fix\n\nState a hypothesis that predicts something checkable. \"If it is the query plan, the database's slow query log will show this statement appearing at that timestamp.\" Then check. Changing things until the symptom disappears leaves you unsure what happened, and it will happen again.\n\n## Afterwards\n\nA blameless postmortem with a timeline, contributing factors, and one honest question: **what would have made this visible sooner?** The best outcome of an incident is usually a better alert, not a code change.",
      takeaways: [
        "Mitigate first, diagnose second — rollback is not defeat.",
        "P50 versus P99 divergence is the single most informative first question.",
        "Metrics find where, traces find which hop, logs find why — in that order."
      ],
      followUps: [
        "How would you tell retry amplification from genuine load growth?",
        "What would you add so the next occurrence is diagnosed in one minute?",
        "How do you set an alert threshold that catches this without paging on noise?"
      ],
      trap: "Jumping straight to a cause. Interviewers are grading the narrowing process, not whether you guess right — and a confident wrong guess is worse than a slow correct method."
    },
    {
      id: "sd-multi-region",
      q: "Design a multi-region deployment. What actually gets hard?",
      topic: "System Design",
      level: "staff",
      companies: ["amazon", "netflix", "google", "microsoft", "uber", "snowflake", "salesforce"],
      tags: ["replication", "latency", "failover", "consistency"],
      simple: "Serving reads from a nearby region is easy. Writes are the problem, because two regions can accept conflicting writes at the same moment and physics puts 80 milliseconds between them. Every multi-region design is really an answer to 'where do writes go'.",
      answer: "## Why go multi-region\n\nThree different reasons, needing three different designs — pick one before designing:\n\n1. **Latency.** Users far from your region see slow pages. Solved largely by a CDN and read replicas.\n2. **Availability.** Survive a region-wide outage. Needs real failover, tested.\n3. **Data residency.** EU data must stay in the EU. Needs partitioning by jurisdiction, which is a different shape entirely.\n\n## The three patterns\n\n**Active–passive.** One region takes all writes; the other is warm standby. Simple, no conflicts, and failover means promoting the standby — with data loss equal to the replication lag. RPO of seconds, RTO of minutes.\n\n**Active–active with a single write region.** All regions serve reads locally; writes route to one home region. Reads are fast everywhere, writes pay the cross-region trip. This handles most real needs and avoids conflicts entirely — the most under-rated option.\n\n**Active–active with writes everywhere.** Every region accepts writes. Fast writes everywhere, and you have signed up for **conflict resolution**, which is the hard part.\n\n## The conflict problem\n\nTwo regions accept a write to the same record within the same 80 ms round trip. Neither is wrong. Options:\n\n- **Last-write-wins** by timestamp. Simple and it silently loses data — and clock skew means \"last\" may not be last.\n- **CRDTs.** Data types whose merge is mathematically guaranteed to converge: counters, sets, registers. Excellent where your data fits one; not everything does.\n- **Partition ownership.** Each record has a home region that owns its writes. No conflicts by construction. This is the pragmatic answer, and it works well when data partitions naturally by user.\n- **Application-level merge.** Keep both versions and let the user or business logic decide. Correct, and expensive to build.\n\n## The physics\n\n| Route | Round trip |\n| --- | --- |\n| Within a datacentre | < 1 ms |\n| Cross-AZ, same region | 1–2 ms |\n| US East ↔ US West | ~60 ms |\n| US ↔ Europe | ~80 ms |\n| US ↔ Asia | ~150 ms |\n\nA synchronous cross-region write costs at least one of these. A checkout doing three sequential cross-region writes has spent half a second on network alone. Design so writes are local, or so there is only one of them.\n\n## Failover is the part people get wrong\n\n- **DNS failover** is throttled by TTLs and resolvers that ignore them. Expect minutes, not seconds.\n- **Anycast plus health checks** is faster but needs network-level control.\n- **Split brain** is the real danger: if the regions cannot see each other but both can see users, both may promote themselves. You need a quorum-based decision (an odd number of regions, or an external arbiter) about who is primary.\n- **An untested failover does not work.** Run it on a schedule, in production, during business hours. A failover procedure that has never been executed is a document, not a capability.\n\n## What to keep single-region\n\nNot everything needs replicating. Keep the complexity where it earns its cost:\n\n- Analytics and reporting — one region, replicate asynchronously.\n- Admin tooling — accept downtime during a regional failure.\n- The write path for money — often deliberately single-region for correctness, with the cost paid in latency.",
      takeaways: [
        "Decide why first: latency, availability, and residency need different architectures.",
        "Active–active with a single write region avoids conflicts entirely and covers most real needs.",
        "An untested failover is a document, not a capability — and split brain is the failure mode to design against."
      ],
      followUps: [
        "How do you handle schema migrations across regions? (Expand–migrate–contract, never a breaking change.)",
        "What is your RPO and RTO, and how do you verify them?",
        "How do you keep a cache coherent across regions? (Usually: do not — use short TTLs per region.)"
      ],
      trap: "Proposing synchronous replication across continents. It is consistent and it puts 80 ms of unavoidable latency into every write, plus it makes each region's availability depend on the other's."
    }
  ]);
})(window.TD = window.TD || {});
