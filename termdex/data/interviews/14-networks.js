/* Interview bank — networks.

   Network questions are usually a proxy for "do you know where the latency
   goes". Answers here are built around round trips, because that is the unit
   that actually shows up in a latency budget. */
(function (TD) {
  "use strict";

  TD.addQuestions([
    {
      id: "net-url-to-page",
      q: "What happens between typing a URL and the page rendering? Go as deep as you can.",
      topic: "Networks",
      level: "medium",
      companies: "*",
      tags: ["http", "dns", "tls", "rendering"],
      simple: "Your browser looks up the name to get an address, opens a connection, negotiates encryption, asks for the page, then downloads everything the page references and draws it. Almost all the waiting is round trips, not bandwidth.",
      answer: "## The sequence\n\n**1. URL parsing and preprocessing.** The browser checks HSTS — if the domain is on the preload list, an `http://` request is upgraded to `https://` locally, before any network traffic. It also checks the service worker, which may answer from cache without touching the network at all.\n\n**2. DNS resolution.** Browser cache → OS cache → router → recursive resolver → root → TLD → authoritative. A cold lookup is 20–120 ms; a warm one is free. This is why `dns-prefetch` exists.\n\n**3. TCP handshake.** SYN, SYN-ACK, ACK. **One round trip** before any data can be sent.\n\n**4. TLS handshake.** TLS 1.2 costs two round trips; **TLS 1.3 costs one**, and zero with session resumption (0-RTT, which carries a replay risk and so is limited to idempotent requests).\n\n**5. The HTTP request.** One more round trip for the first byte.\n\nSo on a fresh connection over a link with 50 ms RTT: DNS 50 + TCP 50 + TLS 50 (1.3) + request 50 ≈ **200 ms before a single byte of HTML arrives**. On mobile with 100 ms RTT it is 400 ms. This arithmetic is the whole reason connection reuse, HTTP/2 and QUIC exist.\n\n**6. Server processing** — routing, application logic, database queries, template rendering.\n\n**7. Response and parsing.** The browser builds the DOM incrementally as bytes arrive.\n\n**8. The critical rendering path.** DOM + CSSOM → render tree → layout → paint → composite.\n\n- **CSS is render-blocking.** Nothing paints until the CSSOM is complete, because an element's computed style could change everything.\n- **A synchronous `<script>` blocks parsing**, because `document.write` could alter the document. `async` and `defer` avoid this; `defer` preserves execution order and runs after parsing.\n\n**9. Subresources.** Images, fonts, more scripts — each potentially needing its own connection and handshake unless multiplexed.\n\n## Where to optimise, in order of effect\n\n| Problem | Fix |\n| --- | --- |\n| Handshake cost | Connection reuse, HTTP/2 or HTTP/3, `preconnect` |\n| Distance | CDN — nothing beats being physically closer |\n| Render-blocking CSS | Inline critical CSS, load the rest asynchronously |\n| Blocking JS | `defer` or `async`, and ship less of it |\n| Large images | Modern formats, correct sizing, lazy loading |\n| Repeat visits | Cache headers, service worker |\n\n## What a strong answer sounds like\n\nGive the sequence, then **pick one step and go deep** — the recursive DNS chain, the TLS 1.3 handshake, or the critical rendering path. Depth in one place demonstrates more than a uniformly shallow list, and the interviewer will usually pick the step they care about anyway.",
      takeaways: [
        "Round trips dominate: DNS, TCP, TLS and the request itself are four before the first byte.",
        "TLS 1.3 halves the handshake to one round trip; 0-RTT resumption removes it with a replay caveat.",
        "CSS blocks rendering and synchronous JS blocks parsing — different problems, different fixes."
      ],
      followUps: [
        "How does HTTP/3 remove a round trip? (QUIC merges the transport and TLS handshakes.)",
        "What is the difference between `async` and `defer`?",
        "Why is a CDN more effective than any protocol optimisation? (It reduces the RTT that everything else multiplies.)"
      ],
      trap: "Stopping at \"then the server responds\". The rendering half is usually where the interviewer wants depth, and skipping it reads as a backend-only answer to a full-stack question."
    },
    {
      id: "net-tcp-vs-udp",
      q: "TCP versus UDP. When is UDP the right choice, and what do you have to rebuild?",
      topic: "Networks",
      level: "easy",
      companies: "*",
      tags: ["tcp", "udp", "protocols"],
      simple: "TCP is a phone call: connection established, everything arrives in order, and it slows down if the line is bad. UDP is shouting across a room: fast, no setup, and some of it may not land. You pick UDP when late data is worse than lost data.",
      answer: "## The differences that matter\n\n| | TCP | UDP |\n| --- | --- | --- |\n| Connection | Handshake required | None |\n| Delivery | Guaranteed, retransmitted | Best effort |\n| Ordering | Guaranteed | None |\n| Congestion control | Yes | You build it |\n| Header | 20+ bytes | 8 bytes |\n| Boundaries | Byte stream — none | Message-preserving |\n\nThat last row is under-appreciated. TCP is a **byte stream**: two `send()` calls may arrive as one `recv()`, or one may arrive split across two. You must frame your own messages — a length prefix or a delimiter. Assuming one send equals one receive is a real and common bug.\n\n## When UDP wins\n\n**Late data is worthless.** In a voice call, a packet from 200 ms ago is useless even if it arrives perfectly. Retransmitting it wastes bandwidth and adds jitter. Better to conceal the loss and move on.\n\n**Head-of-line blocking is intolerable.** TCP delivers strictly in order, so one lost packet stalls everything behind it even though that data has already arrived. For a game sending position updates, the newest position supersedes the lost one entirely — waiting for it is exactly wrong.\n\n**The exchange is a single small request and response.** DNS over UDP is one packet each way with no handshake. TCP would triple the cost for a 100-byte answer.\n\n**Multicast or broadcast.** TCP is strictly point-to-point.\n\n## What you rebuild\n\nAnything real over UDP re-implements a subset of TCP:\n\n- **Sequence numbers** for ordering and loss detection.\n- **Selective acknowledgement and retransmission** for the data that genuinely matters.\n- **Congestion control** — this one is not optional. Without it you contribute to congestion collapse, and it is the hardest part to get right.\n- **Path MTU discovery**, or fragmentation destroys your reliability.\n\nQUIC does all of this, over UDP, in userspace.\n\n## QUIC — the modern answer\n\nQUIC runs on UDP and provides reliability, ordering *per stream*, and encryption:\n\n- **Independent streams.** A loss on stream 1 does not block stream 2 — this is the head-of-line blocking that HTTP/2 over TCP could not solve, because TCP orders the whole connection.\n- **A merged transport and crypto handshake**, so connection setup is one round trip rather than TCP's one plus TLS's one.\n- **Connection migration.** The connection is identified by a connection ID, not by the 4-tuple, so moving from Wi-Fi to cellular keeps it alive. TCP would have to reconnect.\n- **Userspace implementation**, so congestion control can be improved without a kernel update.\n\nWhen an interviewer asks \"when would you use UDP\", the strongest answer today usually ends at QUIC: you use UDP as a substrate and get reliability where you want it, per stream.",
      takeaways: [
        "TCP is a byte stream — you must frame your own messages regardless of protocol choice.",
        "UDP wins when late data is worthless, or when in-order delivery causes head-of-line blocking.",
        "QUIC is UDP with per-stream reliability, a one-round-trip handshake, and connection migration."
      ],
      followUps: [
        "Why can HTTP/2 still suffer head-of-line blocking despite multiplexing? (TCP orders the whole connection.)",
        "How does QUIC keep a connection alive across a network change?",
        "What is congestion collapse, and why is congestion control non-negotiable?"
      ],
      trap: "Saying UDP is faster. It has less overhead; over a lossy link a naive UDP protocol without congestion control performs far worse than TCP, and can harm every other flow sharing the path."
    },
    {
      id: "net-tls-handshake",
      q: "Walk me through a TLS 1.3 handshake. How does the client know it is talking to the real server?",
      topic: "Networks",
      level: "medium",
      companies: ["google", "amazon", "apple", "microsoft", "netflix", "palantir", "salesforce", "oracle"],
      tags: ["tls", "pki", "security"],
      simple: "The two sides agree on a shared secret without ever sending it, then the server proves who it is by signing something with a private key that a certificate authority has vouched for. Your browser already trusts that authority, so the chain holds.",
      answer: "## TLS 1.3, one round trip\n\n**ClientHello** — supported cipher suites, a random value, and a **key share**: the client's public value for Diffie-Hellman, sent optimistically for its best-guess group.\n\n**ServerHello** — the chosen cipher suite, the server's key share, and its certificate chain, plus a signature over the handshake transcript. **All of this is in one flight.**\n\nBoth sides now derive the same shared secret from their own private value and the other's public value. Application data flows.\n\nSending the key share in the first message is what removes a round trip compared with TLS 1.2, where the client had to wait to learn which group the server wanted.\n\n## How the key exchange works\n\n**Ephemeral Diffie-Hellman (ECDHE).** Each side picks a random private value, sends the corresponding public value, and both compute the same shared secret. An eavesdropper who records everything cannot derive it.\n\n**Ephemeral** is the crucial word. The private values are discarded after the handshake, so **compromising the server's long-term private key later does not decrypt past sessions**. That is forward secrecy, and TLS 1.3 mandates it — every non-forward-secret key exchange (static RSA) was removed from the protocol.\n\n## How identity is proven\n\nDiffie-Hellman alone gives you a secure channel to *someone* — possibly a man in the middle. Authentication is what binds it to a specific identity.\n\n1. The server sends a **certificate**: its public key plus its domain, signed by a certificate authority.\n2. The client verifies the CA's signature, then that CA's certificate against its issuer, up to a **root** in the client's trust store.\n3. The client checks the domain matches, the certificate has not expired, and it has not been revoked.\n4. The server **signs the handshake transcript** with the private key corresponding to the certificate. Only the real key holder can do this — and it proves possession right now, not merely that it once had a certificate.\n\nStep 4 is the one people miss. The certificate proves the CA vouches for the key; the signature proves this server actually holds it.\n\n## Revocation, honestly\n\n- **CRLs** are large and stale.\n- **OCSP** leaks browsing to the CA and fails open (a soft-fail check an attacker can simply block).\n- **OCSP stapling** has the server fetch and attach its own fresh proof, fixing both privacy and latency.\n- In practice browsers ship curated revocation lists (CRLite, CRLSets), because the online protocols did not work well enough.\n\nShort-lived certificates — 90 days, or increasingly much less — sidestep the problem by limiting the exposure window instead of trying to revoke reliably.\n\n## 0-RTT and its caveat\n\nOn a resumed connection, the client can send application data in the very first packet using a pre-shared key. Zero round trips.\n\nThe cost: 0-RTT data is **replayable**. An attacker who captures it can resend it, and the server cannot distinguish. So only idempotent requests may go in 0-RTT — a GET, never a POST that transfers money.\n\n## Certificate pinning\n\nHardcoding an expected certificate or public key defeats an attacker with a fraudulent-but-valid certificate from a compromised CA. It also **bricks your app** if you rotate the key without shipping an update first. HTTP Public Key Pinning was deprecated for exactly this reason; mobile apps still pin, carefully, with backup pins.",
      takeaways: [
        "TLS 1.3 is one round trip because the client sends its key share in the first message.",
        "Ephemeral Diffie-Hellman gives forward secrecy — past sessions stay safe if the long-term key leaks.",
        "The certificate proves the CA vouches; the transcript signature proves the server holds the key now."
      ],
      followUps: [
        "What is Certificate Transparency and what problem does it solve? (Public logs make mis-issuance detectable.)",
        "How does mutual TLS differ? (The client presents a certificate too — the basis of zero-trust service meshes.)",
        "Why is 0-RTT limited to idempotent requests?"
      ],
      trap: "Describing the TLS 1.2 handshake and calling it 1.3. The round-trip count and the mandatory forward secrecy are the two differences an interviewer is checking for."
    },
    {
      id: "net-http-versions",
      q: "HTTP/1.1, HTTP/2 and HTTP/3. What problem does each solve?",
      topic: "Networks",
      level: "medium",
      companies: ["google", "netflix", "amazon", "meta-ai", "microsoft", "adobe", "bytedance"],
      tags: ["http", "multiplexing", "quic"],
      simple: "HTTP/1.1 can only handle one request at a time per connection, so browsers open several. HTTP/2 lets many share one connection, but a lost packet still stalls all of them. HTTP/3 fixes that by moving off TCP entirely.",
      answer: "## HTTP/1.1 — one at a time\n\nA connection handles one request-response at a time. Pipelining was specified and never worked in practice, because responses still had to return in order and any slow one blocked the rest.\n\nBrowsers worked around it by opening **6 connections per origin**, and developers worked around *that* with domain sharding, sprite sheets and file concatenation. Every one of those techniques exists to compensate for a protocol limitation, and every one becomes counterproductive under HTTP/2.\n\n## HTTP/2 — multiplexing\n\n- **Binary framing.** Requests and responses are split into frames tagged with a stream ID, interleaved on one connection, and reassembled. Many requests genuinely concurrent on one connection.\n- **Header compression (HPACK).** Headers repeat almost identically across requests; a shared dynamic table cuts them to a few bytes. Meaningful when a request carries 800 bytes of cookies.\n- **Server push.** Send resources before they are requested. It sounded good, proved hard to use without wasting bandwidth on already-cached resources, and Chrome removed it. `103 Early Hints` replaced it and works better.\n- **Stream priorities.** Under-implemented and largely replaced.\n\n**The remaining problem: TCP head-of-line blocking.** HTTP/2 multiplexes at the HTTP layer, but TCP delivers bytes strictly in order. One lost packet stalls *every* stream on that connection until it is retransmitted — so on a lossy link, HTTP/2 can be **worse** than HTTP/1.1 with six connections, because six connections mean a loss stalls only one of them.\n\n## HTTP/3 — QUIC\n\nHTTP/3 runs over QUIC, over UDP, and streams are independent at the transport layer.\n\n- **No head-of-line blocking.** A loss on stream 1 delays only stream 1.\n- **Faster setup.** Transport and TLS handshakes are merged: one round trip, or zero on resumption.\n- **Connection migration.** A connection ID rather than a 4-tuple, so switching from Wi-Fi to cellular does not reset it.\n- **Encrypted transport headers**, which prevents middlebox ossification — the reason TCP itself became so hard to evolve.\n\n## What to do differently\n\n| Practice | HTTP/1.1 | HTTP/2 and 3 |\n| --- | --- | --- |\n| Domain sharding | Helps | **Hurts** — defeats multiplexing and multiplies handshakes |\n| Concatenating files | Helps | Hurts caching granularity |\n| Sprite sheets | Helps | Unnecessary |\n| Inlining small assets | Helps | Usually unnecessary |\n\nThese reversals are a good interview follow-up, because a lot of production advice on the web still assumes HTTP/1.1.\n\n## Adoption reality\n\nHTTP/2 is essentially universal. HTTP/3 is widely deployed by major CDNs and browsers, with the caveat that some corporate networks block or throttle UDP — so a TCP fallback is still required, and clients race both.",
      takeaways: [
        "HTTP/2 multiplexes at the HTTP layer; TCP still blocks all streams on a single loss.",
        "HTTP/3 moves streams into the transport (QUIC/UDP), which is the actual fix.",
        "Domain sharding and file concatenation actively hurt under HTTP/2 and later."
      ],
      followUps: [
        "Why did server push fail in practice?",
        "What is protocol ossification and how does QUIC's encryption prevent it?",
        "How does a client discover HTTP/3 support? (`Alt-Svc` header, or the DNS HTTPS record.)"
      ],
      trap: "Claiming HTTP/2 eliminated head-of-line blocking. It removed it at the HTTP layer and left it at the TCP layer — and that distinction is the entire reason HTTP/3 exists."
    },
    {
      id: "net-load-balancing",
      q: "Compare L4 and L7 load balancing, and the algorithms each can use.",
      topic: "Networks",
      level: "medium",
      companies: ["amazon", "netflix", "google", "uber", "microsoft", "salesforce", "bytedance", "flipkart"],
      tags: ["load-balancing", "proxies", "health-checks"],
      simple: "A layer 4 balancer only sees addresses and ports, so it just forwards packets — fast and blind. A layer 7 balancer reads the actual HTTP request, so it can route by URL and retry failures — slower and much smarter.",
      answer: "## Layer 4\n\nOperates on the TCP/UDP 4-tuple. It never parses the payload.\n\n- **Very fast.** Can be pure packet forwarding with no connection termination at all.\n- **Protocol-agnostic.** Works for anything over TCP.\n- **Blind.** Cannot route on URL, cannot retry a failed request, cannot see HTTP status codes.\n\n**Direct Server Return** is the extreme form: the balancer rewrites only the destination MAC address, and the server replies **directly to the client**, bypassing the balancer entirely on the return path. Since responses are typically far larger than requests, this removes the balancer as a bandwidth bottleneck. It requires the servers to share the virtual IP and to be on the same L2 segment.\n\n## Layer 7\n\nTerminates the connection, parses HTTP, and makes a new request to the backend.\n\n- **Content-based routing** — `/api` to one pool, `/static` to another.\n- **Retries and circuit breaking.** It knows a 503 happened, so it can retry elsewhere. L4 cannot.\n- **TLS termination**, so backends need no certificates.\n- **Header manipulation**, compression, rate limiting, authentication.\n- Costs more CPU, adds a millisecond or so, and is a full proxy in the path.\n\n## Algorithms\n\n| Algorithm | Behaviour | Weakness |\n| --- | --- | --- |\n| Round robin | Next in sequence | Ignores actual load and request cost |\n| Weighted round robin | Proportional to capacity | Still ignores current state |\n| **Least connections** | Fewest active connections | Good default for variable request cost |\n| Least response time | Lowest latency × connections | Can oscillate |\n| IP hash | Same client to same backend | Breaks on client IP changes; poor distribution behind NAT |\n| **Power of two choices** | Pick 2 at random, choose the less loaded | Near-optimal, and stateless |\n\n**Power of two random choices** deserves the attention. Picking two backends at random and choosing the less loaded one gives load variance dramatically better than pure random — exponentially better in the maximum load — and almost as good as full least-connections, without needing global state. That makes it ideal for distributed balancing where no single node knows the whole picture.\n\nStraight least-connections has a failure mode at scale: every balancer sends to the same apparently-idle backend simultaneously, and it is instantly overwhelmed. Two random choices avoids that herding entirely.\n\n## Health checks\n\n- **Passive** — observe real traffic for errors. Free, and slow to react.\n- **Active** — periodic probe. Faster detection, and it adds load.\n\nProbe a **deep** endpoint that touches the dependencies the service needs, not a static `/ping` that returns 200 while the database is unreachable. But beware the opposite failure: if the health check fails when a shared database is down, **every** backend is marked unhealthy at once and you have converted a degraded service into a total outage.\n\nThe usual resolution is two endpoints: a shallow liveness check (is this process alive — restart it if not) and a deeper readiness check (should it receive traffic), plus a rule that the balancer never removes more than a fraction of the pool at once.\n\n## Sticky sessions\n\nRouting a user consistently to one backend. Sometimes necessary for in-memory session state — and it is a design smell, because it prevents even load distribution and makes deployments disruptive. Prefer stateless backends with session state in Redis.",
      takeaways: [
        "L4 forwards packets and is fast and blind; L7 parses HTTP and can route, retry and terminate TLS.",
        "Power of two random choices gives near-optimal balance with no global state and avoids herding.",
        "A health check that fails on a shared dependency can mark the entire fleet unhealthy at once."
      ],
      followUps: [
        "What is Direct Server Return and when is it worth the constraints?",
        "How do you deploy without dropping requests? (Connection draining, plus readiness gates.)",
        "How does a service mesh change this? (Balancing moves into a per-pod sidecar with full service discovery.)"
      ],
      trap: "Recommending sticky sessions as the way to handle state. It is a workaround; the interviewer is usually checking whether you reach for externalised session state instead."
    },
    {
      id: "net-dns-deep",
      q: "Explain DNS resolution end to end. How would you use DNS for failover, and why is it a poor tool for it?",
      topic: "Networks",
      level: "medium",
      companies: ["amazon", "google", "netflix", "microsoft", "uber", "palantir", "oracle"],
      tags: ["dns", "caching", "failover"],
      simple: "DNS turns a name into an address by asking a chain of servers, and every answer is cached at several layers along the way. That caching is what makes it fast and exactly what makes it useless for quick failover — you cannot un-cache an answer someone already has.",
      answer: "## The chain\n\n1. **Browser cache** (~60 seconds in Chrome).\n2. **OS cache** — `systemd-resolved`, `nscd`, the Windows DNS Client.\n3. **Recursive resolver** — your ISP's, or 8.8.8.8 / 1.1.1.1. Does the real work.\n4. **Root servers** (13 logical, hundreds of physical via anycast) — return the TLD nameservers.\n5. **TLD servers** (`.com`) — return the domain's authoritative nameservers.\n6. **Authoritative servers** — return the actual record.\n\nOnly the recursive resolver walks the chain; your machine asks once and waits. A fully cold resolution is 4 round trips; a warm one at the resolver is one; a local cache hit is zero.\n\n## Record types worth knowing\n\n| Type | Purpose |\n| --- | --- |\n| A / AAAA | IPv4 / IPv6 address |\n| CNAME | Alias to another name — **cannot coexist with other records at the same name** |\n| MX | Mail servers, with priorities |\n| TXT | Arbitrary text: SPF, DKIM, domain verification |\n| NS | Delegation to authoritative servers |\n| SRV | Service location with port — used by service discovery |\n| CAA | Which CAs may issue certificates for this domain |\n\nThe CNAME restriction is the practical one: you cannot put a CNAME at the zone apex (`example.com`) because the apex must have NS and SOA records. Providers solve it with non-standard ALIAS or ANAME records that resolve server-side.\n\n## DNS-based failover, and why it disappoints\n\nThe idea: health-check your endpoints and remove the failed one from the DNS response. Route 53 and equivalents do this.\n\nWhy it is slow and unreliable:\n\n**TTLs are not obeyed.** You set 60 seconds; some resolvers enforce their own minimum, some clients cache far longer, and some ignore TTL entirely.\n\n**Application-level caching is worse.** A **JVM historically cached DNS forever** by default (`networkaddress.cache.ttl = -1` under a security manager). Many HTTP clients and connection pools resolve once at startup and never again. Your DNS change is irrelevant to a process that is not going to ask.\n\n**Existing connections do not move.** DNS affects new lookups only. A client with an open connection to the failed host stays there until it errors.\n\nRealistic expectation: **minutes**, with a long tail of clients that take much longer. That is fine for a planned migration and inadequate for an outage.\n\n## What to use instead\n\n- **Anycast** — the same IP announced from many locations; BGP routes to the nearest healthy one. Failover in seconds, no client involvement. This is how the root servers and modern CDNs work.\n- **A load balancer with health checks** — the address never changes; the balancer stops sending traffic to the failed backend. Sub-second.\n- **Client-side load balancing with a service registry** — the client holds a live list of endpoints. Used by service meshes and gRPC.\n\n**Use DNS for coarse routing (geographic, or a planned migration) and a load balancer for actual failover.** That sentence is the answer to the question.\n\n## TTL as a trade-off\n\nLow TTL means faster propagation and more DNS queries and higher latency on cold lookups. High TTL is the reverse. The standard tactic is to lower the TTL a day or two *before* a planned change and raise it afterwards — which only works when the change is planned.\n\n## DNS as a dependency\n\nDNS failures cause outages that look like everything being broken at once. The 2016 Dyn attack took down a large part of the web without touching any of those sites. Use two independent DNS providers for anything critical.",
      takeaways: [
        "Caching at browser, OS, resolver and application layers is what makes DNS fast and failover unreliable.",
        "Expect minutes with a long tail — some clients (historically the JVM) cache forever.",
        "Anycast or a load balancer for failover; DNS for coarse routing and planned migrations."
      ],
      followUps: [
        "What is DNS-over-HTTPS and what does it change? (Privacy from the network; centralisation onto a few resolvers.)",
        "Why can you not put a CNAME at the zone apex?",
        "How does anycast achieve failover without any client change?"
      ],
      trap: "Proposing DNS failover with a 60-second TTL as an availability strategy. Stating the TTL as though it were a guarantee is the specific thing that reads as inexperience."
    },
    {
      id: "net-idempotency-retries",
      q: "A request times out. Do you retry? How do you retry safely?",
      topic: "Networks",
      level: "hard",
      companies: ["amazon", "netflix", "uber", "google", "microsoft", "salesforce", "flipkart", "palantir"],
      tags: ["retries", "idempotency", "resilience"],
      simple: "A timeout tells you nothing about whether the work happened — the response may just have been lost. So you only retry when doing it twice is harmless, and you make it harmless by attaching an ID the server uses to recognise a repeat.",
      answer: "## The ambiguity\n\nA timeout has three possible causes and you cannot distinguish them:\n\n1. The request never arrived.\n2. It arrived, was processed, and the response was lost.\n3. It arrived and is still being processed.\n\nCase 2 is why blind retries duplicate work. Case 3 is why aggressive retries make an overloaded server worse.\n\n## Which methods are safe\n\n| Method | Idempotent by spec | Retry safely? |\n| --- | --- | --- |\n| GET, HEAD | Yes | Yes |\n| PUT | Yes | Yes |\n| DELETE | Yes | Yes (a second delete is a no-op) |\n| POST | **No** | Only with an idempotency key |\n| PATCH | Depends | Only if the patch is absolute, not relative |\n\nPATCH is the subtle one. `{\"balance\": 100}` is idempotent; `{\"balance\": \"+10\"}` is not. Same method, opposite answer, decided by your payload design.\n\n## Making POST retryable\n\nThe client generates a key per logical operation — **not per attempt** — and the server deduplicates on it:\n\n```http\nPOST /payments\nIdempotency-Key: 8f14e45f-ea6b-4c2f-9a91-2d0f7c1e4b33\n```\n\nThe key must be generated **before** the first attempt and reused for every retry of that same operation. Generating a new one per retry defeats the entire mechanism, and it is a genuinely common bug.\n\n## Backoff with jitter\n\n$$\\text{delay} = \\min(\\text{cap},\\ \\text{base} \\times 2^{n}) \\times \\text{random}(0, 1)$$\n\nThe jitter is not a refinement. Without it, every client that failed during an outage retries at the same instant, and the recovering service is immediately knocked over again. This is a real, repeated cause of extended outages.\n\nFull jitter (multiplying by a uniform random in [0, 1]) outperforms both no jitter and partial jitter in AWS's published analysis.\n\n## Retry budgets — the important refinement\n\nCapping retries per request is not enough. Under a partial outage, every request retries three times and you have **tripled the load on an already-failing service**. That is retry amplification, and it converts a degradation into an outage.\n\nA **retry budget** caps retries as a fraction of total traffic — allow retries only while they are under, say, 10% of requests. When failures are widespread the budget is exhausted and retries stop automatically, which is exactly when they should.\n\nAlso: retry at **one layer only**. Retries at the client, the gateway, the service mesh and the SDK compose multiplicatively — three layers of three retries is 27 requests from one call.\n\n## Circuit breakers\n\nWhen failures exceed a threshold, stop calling entirely and fail fast.\n\n```\nCLOSED  --failures exceed threshold-->  OPEN\nOPEN    --after a cooldown-->           HALF-OPEN\nHALF-OPEN --a probe succeeds-->         CLOSED\nHALF-OPEN --a probe fails-->            OPEN\n```\n\nThis protects **you** (fail in 1 ms instead of waiting for a 30 s timeout, so your threads are not all blocked) and it protects **them** (a struggling service gets a chance to recover instead of being held down).\n\n## Timeouts\n\nA retry policy is meaningless without a timeout. Set it from the **observed P99**, not from a guess — typically P99 plus headroom. A default 30-second timeout on a service that normally responds in 50 ms means every failure occupies a thread for 30 seconds, and thread exhaustion follows.\n\nAnd propagate a **deadline**, not a per-hop timeout. If the caller has 500 ms left, each downstream hop should know that, so nobody spends time on work whose result is already too late to use.",
      takeaways: [
        "A timeout does not tell you whether the work happened — only idempotency makes a retry safe.",
        "The idempotency key is generated once per logical operation, not per attempt.",
        "Retry budgets and single-layer retries prevent amplification turning a degradation into an outage."
      ],
      followUps: [
        "How does a server implement idempotency-key storage correctly? (Unique constraint, plus an in-progress state.)",
        "Why does full jitter beat partial jitter?",
        "How do you propagate a deadline across services? (gRPC deadlines, or an explicit header.)"
      ],
      trap: "Retrying at several layers. Each layer looks reasonable in isolation; together they multiply, and the total request amplification is what actually takes the service down."
    },
    {
      id: "net-cors-cookies",
      q: "Explain CORS and the same-origin policy. Why does a preflight request exist?",
      topic: "Networks",
      level: "medium",
      companies: ["adobe", "salesforce", "microsoft", "flipkart", "amazon", "google", "uber", "tcs"],
      tags: ["browser-security", "cors", "cookies"],
      simple: "By default a page cannot read a response from a different site, or every site you visit could read your email. CORS is the server saying which other sites are allowed to read it. The extra preflight request exists so the browser can ask permission before doing anything that might change data.",
      answer: "## Same-origin policy\n\nAn origin is **scheme + host + port**. All three must match. Without this policy, any page could issue a request to your bank with your cookies attached and read the response.\n\nCrucially, the browser usually **sends** the cross-origin request and then blocks JavaScript from **reading** the response. The server may already have acted on it. That distinction is the entire reason CSRF exists as a separate problem from CORS.\n\n## CORS\n\nThe server opts in by returning headers:\n\n```http\nAccess-Control-Allow-Origin: https://app.example.com\nAccess-Control-Allow-Credentials: true\nAccess-Control-Allow-Methods: GET, POST, PUT, DELETE\nAccess-Control-Allow-Headers: Content-Type, Authorization\nAccess-Control-Max-Age: 86400\n```\n\n## Simple requests versus preflight\n\nA **simple request** is sent directly, no preflight. It qualifies only if:\n\n- The method is GET, HEAD or POST, **and**\n- The only custom headers are on a small safelist, **and**\n- `Content-Type` is `application/x-www-form-urlencoded`, `multipart/form-data`, or `text/plain`.\n\nThat set is deliberately exactly what an HTML form could already produce before CORS existed. Those requests were always possible, so allowing them changes nothing.\n\nAnything else triggers a **preflight**: an `OPTIONS` request asking permission *before* the real one is sent.\n\n```http\nOPTIONS /api/users HTTP/1.1\nOrigin: https://app.example.com\nAccess-Control-Request-Method: DELETE\nAccess-Control-Request-Headers: Authorization\n```\n\n**Why it exists:** a `DELETE` with an `Authorization` header could not be produced by an old HTML form, so no pre-CORS server was ever designed to defend against one arriving cross-origin. Preflight ensures such a request only reaches the server if the server has explicitly said it is prepared for it.\n\nNote that `Content-Type: application/json` is **not** on the safelist, which is why almost every real API call preflights.\n\n## Credentials\n\nWith `credentials: 'include'`, two rules tighten:\n\n1. `Access-Control-Allow-Origin` **may not be `*`.** It must name the exact origin. Echo the request's `Origin` after checking it against an allowlist — never reflect it unchecked, which is equivalent to allowing everyone.\n2. `Access-Control-Allow-Credentials: true` is required.\n\n## CORS is not a security control for your server\n\nThis is the most important point. CORS is enforced **by the browser**, for the browser's own users. `curl`, a server, or a mobile app ignores it entirely.\n\nCORS protects *your users from other websites*. It does not protect *your API from attackers*. Authentication and authorisation are what protect your API, and they are still required no matter what your CORS policy says.\n\n## SameSite cookies\n\nThe modern CSRF defence:\n\n| Value | Behaviour |\n| --- | --- |\n| `Strict` | Never sent cross-site — even when following a link, which logs users out of a link they clicked |\n| `Lax` | Sent on top-level GET navigation only. The browser default. |\n| `None` | Always sent; **requires `Secure`** |\n\n`Lax` by default stops the classic CSRF POST outright, which is why explicit CSRF tokens are less load-bearing than they were. They are still worth having for defence in depth.\n\n## Debugging\n\nA CORS error in the console almost never means the client is wrong — it means the **server** did not send the right headers, or the preflight `OPTIONS` was not handled. Check whether `OPTIONS` returns 2xx; many frameworks route it to a 404 or an auth middleware that rejects it, and the resulting error message points nowhere useful.",
      takeaways: [
        "The browser usually sends the request and blocks the read — which is why CSRF is a separate problem.",
        "Preflight exists for requests an old HTML form could not have made, so no legacy server is exposed.",
        "CORS is enforced by browsers only; it is not an access control for your API."
      ],
      followUps: [
        "Why can `Access-Control-Allow-Origin: *` not be used with credentials?",
        "How does `SameSite=Lax` prevent CSRF, and what does it not cover?",
        "Why does a failing preflight produce such an unhelpful error?"
      ],
      trap: "Treating CORS as protecting the API. Any non-browser client ignores it entirely, and relying on it as an authorisation mechanism is a genuine vulnerability."
    }
  ]);
})(window.TD = window.TD || {});
