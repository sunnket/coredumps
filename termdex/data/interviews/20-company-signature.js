/* Interview bank — company signature questions.

   Questions tied to one company's actual domain rather than to a general
   topic: CUDA for NVIDIA, Spark internals for Databricks, H3 for Uber. These
   are what makes a company page feel like that company's loop rather than a
   generic bank with a logo on it. */
(function (TD) {
  "use strict";

  TD.addQuestions([
    /* ---------------- NVIDIA ---------------- */
    {
      id: "co-nvidia-warp-divergence",
      q: "Explain warp divergence and shared-memory bank conflicts. How do you fix each?",
      topic: "OS & Concurrency",
      level: "hard",
      companies: ["nvidia", "tesla", "meta-ai", "openai"],
      tags: ["cuda", "gpu", "performance"],
      simple: "A GPU runs 32 threads in lockstep on the same instruction. If half of them take an if-branch and half take the else, it has to run both halves one after the other with the other half idle. Separately, shared memory is split into 32 lanes, and if two threads in a group want different addresses in the same lane they queue up.",
      answer: "## Warp divergence\n\nCUDA executes in **SIMT** groups of 32 threads called a warp — one instruction, all 32 threads. When threads in one warp take different branches, the hardware executes **both paths serially**, masking off the non-participating threads in each.\n\n```cuda\nif (threadIdx.x % 2 == 0) {   // half the warp\n    expensive_a();\n} else {                       // the other half\n    expensive_b();\n}\n// cost = cost(a) + cost(b), not max(a, b)\n```\n\nThat is a 2× loss here, and worse with more branches. Note that divergence **within** a warp is what costs; different warps taking different branches is free, because they are scheduled independently.\n\n**Fixes:**\n\n- **Align branches to warp boundaries.** `if (threadIdx.x / 32 == 0)` means whole warps take the same path — no divergence at all.\n- **Reorder or sort data** so similar work lands in the same warp.\n- **Use predication for short branches.** The compiler already converts small if-bodies to predicated instructions, which execute both sides but avoid the branch overhead. Fine for a couple of instructions; bad for a large body.\n- **Move the branch out of the kernel.** Two kernels launched over sorted data beat one divergent kernel.\n\n## Shared memory bank conflicts\n\nShared memory has **32 banks**, each 4 bytes wide, striped so that consecutive 32-bit words fall in consecutive banks. Bank $b$ holds word indices where $\\text{index} \\bmod 32 = b$.\n\nIf threads in a warp access **different addresses in the same bank**, those accesses serialise. An $n$-way conflict costs $n$ times as long.\n\nThe classic case is a column-major access into a square tile:\n\n```cuda\n__shared__ float tile[32][32];\nfloat x = tile[threadIdx.x][0];   // thread i reads word i*32\n                                  // i*32 mod 32 == 0 for every i\n                                  // ALL 32 threads hit bank 0: 32-way conflict\n```\n\n**The fix is padding:**\n\n```cuda\n__shared__ float tile[32][33];    // row stride 33, not 32\nfloat x = tile[threadIdx.x][0];   // thread i reads word i*33\n                                  // i*33 mod 32 == i  ->  every bank once\n```\n\nOne extra column costs 128 bytes and removes a 32× slowdown. This is the single most-quoted CUDA optimisation, and it appears in every tiled matrix-multiply kernel.\n\n**Note:** if all threads read the *same* address, that is a **broadcast**, not a conflict — it is served in one transaction. Only *different* addresses in the same bank serialise.\n\n## Global memory coalescing\n\nRelated and often more important. When threads in a warp access **consecutive** global addresses, the hardware merges them into a few wide transactions. Strided or scattered access issues one transaction per thread — up to 32× the memory traffic.\n\n```cuda\nfloat v = data[blockIdx.x * blockDim.x + threadIdx.x];  // coalesced\nfloat v = data[threadIdx.x * stride];                    // not coalesced\n```\n\nSince most kernels are memory-bound, coalescing is usually where the time goes. Fix it by changing the data layout — often from array-of-structs to struct-of-arrays.\n\n## Diagnose before optimising\n\n`ncu` (Nsight Compute) reports warp execution efficiency, shared-memory bank conflicts, and global load efficiency directly. Guessing which of the three you have is the mistake; the profiler tells you in one run.",
      takeaways: [
        "Divergence within a warp serialises both paths; divergence across warps is free.",
        "Padding a shared array to stride 33 makes each thread hit a distinct bank — removes a 32× conflict.",
        "Coalescing usually matters more than either, because most kernels are memory-bound."
      ],
      followUps: [
        "What is occupancy, and why is higher not always better? (It hides latency; past a point, registers per thread matter more.)",
        "How does a tiled matrix multiply use shared memory to raise arithmetic intensity?",
        "What are tensor cores and what shapes do they require?"
      ],
      trap: "Optimising for occupancy alone. A kernel at 100% occupancy with uncoalesced access is far slower than one at 50% with coalesced access — bytes moved is the thing to reason about."
    },
    {
      id: "co-nvidia-flashattention",
      q: "How does FlashAttention compute exact attention without materialising the $N \\times N$ matrix?",
      topic: "LLM & GenAI",
      level: "staff",
      companies: ["nvidia", "openai", "anthropic", "google-deepmind", "meta-ai"],
      tags: ["cuda", "attention", "memory-hierarchy"],
      aiTrack: true,
      simple: "Standard attention writes a huge score matrix to slow memory and reads it back. FlashAttention processes the input in tiles small enough to stay in the chip's fast scratchpad, and keeps a running correction so the softmax comes out exactly right even though it never sees all the scores at once.",
      answer: "## The problem is memory traffic, not arithmetic\n\nStandard attention:\n\n1. $S = QK^\\top$ — write an $N \\times N$ matrix to HBM.\n2. $P = \\text{softmax}(S)$ — read it, write it back.\n3. $O = PV$ — read it again.\n\nFor $N = 8192$ in FP16, that matrix is 128 MB **per head per layer**, moved three times. HBM bandwidth is roughly 3 TB/s while on-chip SRAM is around 20 TB/s, so the kernel spends its time waiting on memory. The arithmetic is not the bottleneck.\n\nFlashAttention is **IO-aware**: same result, far fewer bytes moved.\n\n## The online softmax trick\n\nSoftmax normally needs a global maximum for numerical stability:\n\n$$\\text{softmax}(x_i) = \\frac{e^{x_i - \\max(x)}}{\\sum_j e^{x_j - \\max(x)}}$$\n\nwhich seems to require seeing every score first. The insight is that you can maintain a **running maximum** and **running sum**, and correct previous partial results when the maximum increases.\n\nProcessing block $j$ with block max $\\tilde{m}$:\n\n$$m_{new} = \\max(m_{old}, \\tilde{m})$$\n$$\\ell_{new} = e^{m_{old} - m_{new}} \\ell_{old} + \\sum_i e^{s_i - m_{new}}$$\n$$O_{new} = e^{m_{old} - m_{new}} O_{old} + \\sum_i e^{s_i - m_{new}} v_i$$\n\nThe factor $e^{m_{old} - m_{new}}$ is the correction: it rescales everything accumulated so far to the new maximum. Because it is applied consistently to both the accumulator and the normaliser, dividing $O$ by $\\ell$ at the end gives **exactly** the same result as computing softmax over the whole row.\n\n**This is not an approximation.** That is the point worth stating clearly — sparse and linear attention approximate; FlashAttention is bit-comparable to within floating-point reassociation.\n\n## The loop structure\n\n```\nfor each block of Q (outer, kept in SRAM):\n    initialise O_block = 0, m = -inf, l = 0\n    for each block of K, V (inner, streamed into SRAM):\n        S_block = Q_block @ K_block^T          # in SRAM\n        update m, l, O_block with the rescaling above\n    write O_block to HBM once\n```\n\nQ, K and V blocks are sized to fit in SRAM (~100 KB per streaming multiprocessor). The $N \\times N$ scores exist only as small tiles inside SRAM and are **never written to HBM**.\n\nHBM traffic drops from $O(N^2)$ to $O(N^2 d / M)$ where $M$ is SRAM size — in practice a 10–20× reduction in memory traffic and 2–4× wall-clock speedup, with memory usage becoming **linear** in sequence length instead of quadratic.\n\n## The backward pass\n\nBackprop normally needs the attention matrix. FlashAttention **recomputes** the tiles during the backward pass from the stored $m$ and $\\ell$ statistics — trading extra FLOPs for far less memory traffic. Since the kernel is memory-bound, recomputation is *faster* than storing and reloading. That inversion of the usual intuition is the elegant part.\n\n## Why it matters\n\nLong-context training is memory-bound on the attention matrix. FlashAttention is what made 32k+ context training practical, and it is now standard in essentially every serious training and inference stack. FlashAttention-2 improved the work partitioning across warps; FlashAttention-3 exploits Hopper's asynchrony and FP8.",
      takeaways: [
        "The bottleneck is HBM traffic, not FLOPs — FlashAttention is an IO optimisation.",
        "The running-max rescaling factor makes tiled softmax exact, not approximate.",
        "The backward pass recomputes tiles because recomputation is cheaper than memory traffic here."
      ],
      followUps: [
        "Why does recomputation beat storing in the backward pass?",
        "How does this interact with GQA and the KV cache at inference time?",
        "What is arithmetic intensity, and where does attention sit on a roofline plot?"
      ],
      trap: "Calling it an approximation. It is mathematically exact, and confusing it with sparse or linear attention is the specific error this question detects."
    },

    /* ---------------- Databricks / Snowflake ---------------- */
    {
      id: "co-databricks-spark-skew",
      q: "A Spark job runs for hours because one task never finishes. What is happening and how do you fix it?",
      topic: "System Design",
      level: "hard",
      companies: ["databricks", "snowflake", "uber", "netflix", "amazon", "bytedance"],
      tags: ["spark", "skew", "shuffle"],
      simple: "One key has far more rows than the others, so one worker gets all of them while the rest sit idle. The job finishes when that one task finishes. The fixes all amount to splitting that key up or avoiding the shuffle entirely.",
      answer: "## Recognising skew\n\nIn the Spark UI, look at the task duration distribution for the slow stage. Skew looks like: median task 3 seconds, max task 45 minutes. Also check shuffle read size per task — the slow task will have read far more bytes.\n\nThe cause is that a shuffle partitions by `hash(key) % numPartitions`, so every row with the same key goes to one partition. If one key holds 40% of the rows, one task does 40% of the work while the rest finish and idle.\n\n## Common sources\n\n- **Null keys.** All nulls hash together. `LEFT JOIN` on a mostly-null column is the classic.\n- **A default or sentinel value.** `user_id = 0`, `country = 'UNKNOWN'`.\n- **Genuine power-law data.** One enormous customer, one viral video.\n\n## The fixes, in order of preference\n\n**1. Broadcast join — best when it applies.**\n\nIf one side fits in memory (default threshold 10 MB, routinely raised to a few hundred), broadcast it to every executor and **eliminate the shuffle entirely**. No shuffle, no skew.\n\n```python\nfrom pyspark.sql.functions import broadcast\nresult = large_df.join(broadcast(small_df), \"key\")\n```\n\nAlways check this first — many skew problems are actually a broadcast that did not happen because statistics were stale.\n\n**2. Adaptive Query Execution — free if you enable it.**\n\nSpark 3+ detects skewed partitions at runtime from actual shuffle statistics and splits them automatically:\n\n```python\nspark.conf.set(\"spark.sql.adaptive.enabled\", \"true\")\nspark.conf.set(\"spark.sql.adaptive.skewJoin.enabled\", \"true\")\n```\n\nAQE also coalesces small partitions and can switch a sort-merge join to a broadcast join once it sees the real sizes. This handles a large share of real-world skew with no code change, and it is genuinely the first thing to check.\n\n**3. Salting — the manual fix when AQE cannot help.**\n\nSpread one hot key across $n$ partitions by adding a random suffix, and replicate the other side $n$ times so every salted variant finds its match:\n\n```python\nfrom pyspark.sql import functions as F\n\nN = 50\nleft_salted = large_df.withColumn(\n    \"salted_key\",\n    F.concat_ws(\"_\", F.col(\"key\"),\n                (F.rand() * N).cast(\"int\"))\n)\n\n# explode the small side across all salt values\nright_salted = (small_df\n    .withColumn(\"salt\", F.explode(F.array([F.lit(i) for i in range(N)])))\n    .withColumn(\"salted_key\", F.concat_ws(\"_\", F.col(\"key\"), F.col(\"salt\")))\n)\n\nresult = left_salted.join(right_salted, \"salted_key\")\n```\n\nThe cost is $n$× replication of the small side. Salt only the keys that are actually hot — a two-pass approach that finds the top keys first and salts only those keeps the replication cost down.\n\n**4. Isolate and union.** Process the hot keys separately with a different strategy and union the results. More code, and sometimes the cleanest option.\n\n**5. Filter nulls before joining.** If nulls cannot match anything, `WHERE key IS NOT NULL` removes the largest skew source for free. Astonishingly effective and frequently overlooked.\n\n## What not to do\n\n**Do not just raise `spark.sql.shuffle.partitions`.** More partitions does not help when the problem is one key — that key still lands in exactly one partition regardless of how many there are. It is the most common wrong answer to this question.\n\n**Do not just add executors.** The extra ones idle while the skewed task runs. You pay more for the same wall-clock time.",
      takeaways: [
        "Skew means one key dominates a partition — more partitions cannot help, since one key still lands in one.",
        "Try broadcast join and AQE first; both may remove the problem with no code change.",
        "Salting works but replicates the other side — salt only the genuinely hot keys."
      ],
      followUps: [
        "Why does raising shuffle partitions not fix skew?",
        "How does AQE decide a partition is skewed? (Median-relative size threshold from runtime statistics.)",
        "How would you find the hot keys? (Approximate frequency count on a sample.)"
      ],
      trap: "Reaching for more partitions or more executors. Both cost money and change nothing, and it is the answer that most clearly signals no production Spark experience."
    },
    {
      id: "co-snowflake-storage-compute",
      q: "What does separating storage from compute actually buy, and what does it cost?",
      topic: "DBMS",
      level: "hard",
      companies: ["snowflake", "databricks", "amazon", "google", "microsoft", "oracle"],
      tags: ["architecture", "warehouse", "elasticity"],
      simple: "Data lives in cheap object storage; compute clusters are spun up to read it and shut down when idle. Several teams can query the same data with completely separate machines, so nobody slows anyone else down. The price is that reading data now goes over the network.",
      answer: "## The three-layer architecture\n\n1. **Storage.** Immutable compressed columnar files in object storage (S3, ADLS, GCS). Cheap, effectively infinite, durable.\n2. **Compute.** Independent clusters that read from storage. Started, resized and stopped on demand.\n3. **Cloud services.** Metadata, transaction coordination, query planning, and security. The genuinely hard part.\n\nThe classic shared-nothing warehouse couples storage to compute: each node owns a slice of the data, so resizing means physically redistributing it — hours of work, and you cannot do it while queries are running.\n\n## What it buys\n\n**Independent scaling.** Storage grows without buying compute; compute scales for a heavy query without moving a byte.\n\n**Workload isolation.** ETL, BI dashboards and data science each get their own cluster over the **same** data. A runaway analytics query cannot slow the dashboards. This is often the biggest practical win and the one users notice.\n\n**Elasticity and pay-per-use.** Auto-suspend an idle warehouse, and a nightly batch runs on a large cluster for 20 minutes rather than an always-on one.\n\n**Zero-copy cloning.** Because storage is immutable files plus metadata, cloning a database copies only metadata pointers. A full-size test environment in seconds, with no storage cost until it diverges. Time travel and undrop come from the same property — old file versions still exist and only the metadata moved on.\n\n## What it costs\n\n**Network latency on cache miss.** Reading from object storage is milliseconds, not microseconds. This is why every such system has a local SSD cache on the compute nodes, and why the **first** query after resuming a warehouse is markedly slower — the cache is cold.\n\n**The metadata service is a hard problem and a single point of failure.** It coordinates transactions across independent compute clusters, so it must be highly available and strongly consistent. Most of the engineering difficulty lives here rather than in the query engine.\n\n**Not suited to OLTP.** Object storage latency and immutable files make single-row updates expensive. These are analytical systems; row-level transactional workloads still want a coupled architecture.\n\n**Cost can surprise you.** Per-second billing across many warehouses that nobody remembers to suspend is a well-known way to receive a large invoice.\n\n## Micro-partitions and pruning\n\nSnowflake stores data in immutable 50–500 MB compressed micro-partitions and keeps **min/max statistics per column per partition** in the metadata service. A query with `WHERE date = '2026-01-15'` consults metadata and skips every partition whose range excludes that date — often reading 1% of the table.\n\nThe key property: **no index to build or maintain**, and no index tuning. Pruning is a metadata operation. Clustering keys let you influence how data is grouped so pruning is more effective, which is the closest analogue to index design.\n\nThe limit: pruning only works on columns correlated with physical layout. A filter on an uncorrelated column reads everything, and that is the failure case worth naming.",
      takeaways: [
        "Workload isolation over shared data is usually the biggest practical win.",
        "Zero-copy cloning and time travel both fall out of immutable storage plus metadata pointers.",
        "Pruning is metadata-driven and needs no index — but only helps on columns correlated with layout."
      ],
      followUps: [
        "Why is the first query after resume slow? (Cold local SSD cache.)",
        "How do concurrent compute clusters see a consistent snapshot? (Metadata-service transactions over immutable files.)",
        "How does Delta Lake or Iceberg achieve something similar on open formats?"
      ],
      trap: "Describing it as \"just putting data in S3\". The metadata and transaction layer is what makes it work, and skipping it means missing the actual engineering."
    },

    /* ---------------- Uber ---------------- */
    {
      id: "co-uber-h3-geospatial",
      q: "How would you index the world for real-time driver–rider matching?",
      topic: "System Design",
      level: "hard",
      companies: ["uber", "google", "amazon", "bytedance", "meta-ai"],
      tags: ["geospatial", "indexing", "matching"],
      simple: "You cannot compute the distance to every driver in the city on every request. So you divide the world into cells with fixed IDs, keep a list of drivers per cell, and only look at the rider's cell and its neighbours. Hexagons are used because every neighbour is the same distance away.",
      answer: "## Why not a plain database query\n\n```sql\nSELECT * FROM drivers\n WHERE ST_DWithin(location, ST_Point(:lng, :lat), 2000);\n```\n\nWith a spatial index this is fine at moderate scale. At Uber's — millions of drivers, positions updating every few seconds, matching latency in the low hundreds of milliseconds — the update rate is the problem: a spatial index that must be rebalanced on every position change becomes the bottleneck.\n\n## Grid indexing\n\nMap each location to a cell ID, keep a set of drivers per cell in memory, and query only the relevant cells. A position update is a set removal plus a set insertion — $O(1)$, no tree rebalancing.\n\n## Why hexagons\n\n| Shape | Neighbours | Distance to neighbours | Tiles the plane |\n| --- | --- | --- | --- |\n| Square | 8 | Edge 1, diagonal $\\sqrt{2}$ | Yes |\n| Triangle | 3 (or 12) | Awkward | Yes |\n| **Hexagon** | **6** | **All equal** | Yes |\n\nA hexagon has exactly six neighbours, all at the same centre-to-centre distance. With squares, a diagonal neighbour is 41% further than an edge neighbour, so \"the ring of neighbours\" is not a consistent distance — which matters when you are aggregating supply and demand over a neighbourhood, or smoothing a surge-pricing surface.\n\nHexagons also approximate circles better, so a k-ring is a better approximation of a radius query.\n\n**The catch:** hexagons cannot tile a sphere perfectly. H3 uses an icosahedron projection and accepts exactly **12 pentagons** globally, placed in the ocean where possible. Any implementation must handle those as a special case — and knowing that detail is a good signal.\n\n## H3 in practice\n\nA 64-bit integer encodes the cell and its resolution, from resolution 0 (continent-scale) to 15 (under a square metre). Resolution 8 is roughly 0.7 km², a reasonable neighbourhood cell for dispatch.\n\n```python\nimport h3\n\ncell = h3.latlng_to_cell(37.7749, -122.4194, 9)   # driver's cell\nnearby = h3.grid_disk(cell, 2)                     # cell + 2 rings out\n```\n\nThe hierarchical property matters: a resolution-9 cell's parent at resolution 8 is derivable from the ID itself, so you can aggregate up cheaply for heat maps or roll down for precision.\n\n## The matching flow\n\n```\ndriver location update ---> compute H3 cell ---> Redis: SREM old, SADD new\n\nride request ---> rider cell ---> k-ring(cell, 1..3)\n             ---> union of driver sets from those cells\n             ---> filter (available, vehicle class, rating)\n             ---> rank by ETA from the routing engine, not straight-line distance\n             ---> offer to the best few\n```\n\nExpand the ring outward until you have enough candidates, so a dense city centre searches one ring and a rural area searches five.\n\n## The details that separate answers\n\n**Rank by ETA, not distance.** A driver 200 m away across a river with no bridge is useless. Straight-line distance is only a candidate filter; the routing engine decides the order.\n\n**Match in batches, not greedily.** Assigning each request to its nearest driver as it arrives is locally optimal and globally poor. Batching over a short window (a few seconds) and solving as an assignment problem produces materially better overall wait times. The trade-off is that batching adds latency, so the window is tuned.\n\n**Handle the boundary case.** A rider at the edge of a cell may have the nearest driver in the adjacent cell. Always search at least one ring out, never only the containing cell.\n\n**Partition by cell for horizontal scale.** Geography is a natural shard key with genuine locality — a match rarely crosses a city.",
      takeaways: [
        "Grid indexing makes a position update $O(1)$ set operations rather than a tree rebalance.",
        "Hexagons have six equidistant neighbours; H3 accepts exactly 12 pentagons as the price of tiling a sphere.",
        "Rank by ETA and match in batches — greedy nearest-driver assignment is globally worse."
      ],
      followUps: [
        "How do you handle the pentagon cells?",
        "How would you compute surge pricing on this structure? (Supply/demand ratio per cell, smoothed over the k-ring.)",
        "Why is batched assignment better than greedy, and what does the window cost?"
      ],
      trap: "Describing only the grid lookup. The interview turns on ETA-versus-distance and batch-versus-greedy matching, which is where the real engineering is."
    },

    /* ---------------- Google ---------------- */
    {
      id: "co-google-spanner-truetime",
      q: "How does Spanner provide external consistency across datacentres?",
      topic: "System Design",
      level: "staff",
      companies: ["google", "google-deepmind", "amazon", "snowflake", "oracle", "databricks"],
      tags: ["distributed", "consistency", "clocks"],
      simple: "Ordinary clocks disagree by unknown amounts, so you cannot use timestamps to order events across machines. Google gave every datacentre atomic clocks and GPS, so each machine knows not just the time but how wrong it might be — then simply waits out that uncertainty before committing.",
      answer: "## The problem\n\nExternal consistency (linearizability) means: if transaction $T_1$ commits before $T_2$ begins **in real time**, then $T_1$'s timestamp is less than $T_2$'s — even if they ran on different continents and touched different data.\n\nOrdinary NTP-synchronised clocks drift by an *unknown* amount, typically tens to hundreds of milliseconds. So a timestamp comparison tells you nothing reliable about ordering. Traditional systems use logical clocks (Lamport, vector) which capture causality but cannot relate to real time — so they cannot give external consistency to an outside observer.\n\n## TrueTime\n\nGoogle deployed **GPS receivers and atomic clocks** in every datacentre and exposed an API that returns an *interval*, not a point:\n\n```\nTT.now() -> [earliest, latest]\n```\n\nThe guarantee: the true absolute time is somewhere in that interval. The width, $\\epsilon$, is typically under 7 ms and averages around 4 ms.\n\nThe crucial shift is that the uncertainty is **explicit and bounded** rather than unknown. Two clock sources with different failure modes — GPS can fail from antenna or satellite issues, atomic clocks drift slowly and predictably — let the system detect and eject a bad clock.\n\n## Commit wait\n\nHere is the mechanism, and it is remarkably simple once you have TrueTime.\n\nWhen a transaction commits, assign timestamp $s = TT.now().latest$. Then **wait until $TT.now().earliest > s$** before releasing locks and acknowledging.\n\nThat wait — roughly $2\\epsilon$, so under 10 ms — guarantees the timestamp is definitely in the past everywhere. Any transaction that starts after this one is acknowledged must receive a strictly larger timestamp, because its `latest` is drawn after this one's absolute time has certainly passed.\n\n> **The system deliberately slows down to make a distributed guarantee true.**\n\nThat inversion — spending latency to buy consistency, rather than approximating consistency to save latency — is the idea the question is really testing.\n\n## What it buys\n\n**Lock-free consistent reads at a timestamp.** A read at time $t$ reads the version as of $t$ from any replica, with no locks and no coordination. This is enormous: read-only transactions across the globe cost one local read, because MVCC plus a meaningful global timestamp makes snapshot reads trivially correct anywhere.\n\n**Consistent backups and global snapshots** — take everything as of one timestamp, meaningfully, across continents.\n\n## The costs\n\n- **Specialised hardware** in every datacentre. This is why the design did not spread immediately, though AWS and others now offer comparable time services.\n- **Write latency floor** of $2\\epsilon$ plus Paxos consensus across regions. Writes are slower than an eventually consistent store, by design.\n- **Availability follows CP.** During a partition, the minority side becomes unavailable. Spanner does not evade CAP; it chooses C and invests enormously in making partitions rare.\n\n## The honest framing\n\nSpanner is sometimes described as beating CAP. It does not. It picks consistency, and then spends money on redundant networking and clocks so that the unavailability that choice implies almost never materialises. That is an engineering and economic achievement, not a theoretical one — and saying so precisely is what a staff-level answer sounds like.",
      takeaways: [
        "TrueTime returns a bounded interval, making clock uncertainty explicit rather than unknown.",
        "Commit wait deliberately spends ~2ε of latency to make the timestamp definitely past everywhere.",
        "It does not defeat CAP — it chooses C and makes partitions rare through hardware investment."
      ],
      followUps: [
        "What happens if ε grows because a GPS receiver fails? (Writes slow; the node is ejected if it stays bad.)",
        "How do lock-free snapshot reads work on top of this?",
        "How does CockroachDB approximate this without atomic clocks? (HLC plus uncertainty restarts — correct, with more retries.)"
      ],
      trap: "Saying Spanner is CA. It is CP; a partition makes the minority side unavailable, and the achievement is making that rare rather than impossible."
    },

    /* ---------------- Meta ---------------- */
    {
      id: "co-meta-recommender",
      q: "Design the ranking system for a recommendation feed. Why two stages?",
      topic: "ML & Statistics",
      level: "hard",
      companies: ["meta-ai", "bytedance", "netflix", "google", "amazon", "uber", "adobe"],
      tags: ["recommender", "ranking", "retrieval"],
      simple: "You have millions of candidates and about 200 milliseconds. So you use something very cheap to get from millions down to a few hundred, then something expensive and accurate to order those few hundred. Running the expensive model on everything is not possible.",
      answer: "## The latency budget forces the architecture\n\nMillions of candidate items, a ~200 ms budget, and a ranking model that costs perhaps 1 ms per item. Scoring everything is off by four orders of magnitude. So:\n\n```\nmillions --> RETRIEVAL (cheap) --> ~1,000 --> RANKING (expensive) --> ~100\n         --> RE-RANKING (rules, diversity) --> ~20 shown\n```\n\nEach stage is 10–100× more expensive per item and operates on 10–100× fewer items. Total cost stays roughly constant per stage, which is the design principle.\n\n## Stage 1: retrieval\n\nSeveral cheap sources in parallel, unioned:\n\n- **Two-tower embedding model.** Encode user and item independently into the same space; retrieve by approximate nearest neighbour. **The towers must not interact** — item embeddings are precomputed offline and indexed, so serving is one user-tower forward pass plus an ANN lookup. Adding a cross-feature between towers would force recomputing every item embedding per request, which destroys the whole design.\n- **Collaborative filtering** — users like you engaged with this.\n- **Recency and trending** — fresh content, popular now.\n- **Followed sources** — direct connections.\n\nUnion and deduplicate to a few thousand. Optimise for **recall**: a good item missed here can never be recovered downstream, so the retrieval stage sets a hard ceiling on the whole system.\n\n## Stage 2: ranking\n\nNow you can afford a large model with full cross-features. Predict several engagement probabilities per item — click, like, share, watch time, hide — then combine:\n\n$$\\text{score} = \\sum_k w_k \\cdot p_k$$\n\n**Multi-task** matters because a single objective is gameable. Optimising click alone produces clickbait; adding a negative weight on \"hide\" and a positive one on long dwell time is how you counteract it. The weights encode a product decision, and they are typically the most argued-over numbers in the company.\n\n## Stage 3: re-ranking\n\nBusiness and quality rules the model should not learn:\n\n- **Diversity.** Do not show five posts from one author. Maximal marginal relevance, or a hard cap.\n- **Integrity.** Demote or remove policy-violating content.\n- **Freshness and exploration.** Reserve slots for content with little data, or the system converges on a narrow set and never learns anything new.\n\n## The problems specific to recommenders\n\n**Position bias.** Items shown at the top get clicked more *because* they are at the top. Training on click logs teaches the model to predict position, not quality. Fix with an explicit position feature at training time set to a constant at serving, or with inverse-propensity weighting.\n\n**Feedback loops.** The model only sees engagement on what it chose to show. It cannot learn that something it never surfaced would have done well. Reserve a small fraction of traffic for random or exploratory recommendations — this is genuinely expensive and genuinely necessary.\n\n**Cold start.** New items have no engagement history. Fall back on content features (text, image embeddings) and give them exploration slots.\n\n**Delayed feedback.** A conversion may arrive days after the impression. Training on immediate labels biases toward fast outcomes.\n\n## Measuring it\n\nOffline: NDCG, recall@k, AUC per task. These are necessary and never sufficient — offline gains routinely fail to reproduce online, because offline evaluation cannot capture the feedback loop.\n\nOnline A/B is the decision, with guardrails: session length, retention, reported-content rate, and diversity of sources. A model that increases engagement while decreasing next-week retention is a loss, and that is why long-horizon guardrails exist.",
      takeaways: [
        "Two stages exist because a full ranking model over millions of candidates cannot fit the latency budget.",
        "The two towers must not interact, or item embeddings cannot be precomputed.",
        "Position bias and feedback loops are the two problems unique to this domain — name both."
      ],
      followUps: [
        "How do you correct position bias in training data?",
        "How do you handle a brand new user with no history?",
        "How do you set the multi-task weights? (A/B testing against long-horizon retention, not offline metrics.)"
      ],
      trap: "Describing one big model. The interviewer is specifically checking whether you understand why retrieval and ranking are separate, and a single-stage answer misses the entire question."
    },

    /* ---------------- Netflix ---------------- */
    {
      id: "co-netflix-chaos",
      q: "What is chaos engineering, and how would you introduce it without causing an outage?",
      topic: "System Design",
      level: "hard",
      companies: ["netflix", "amazon", "uber", "google", "microsoft", "databricks"],
      tags: ["resilience", "testing", "operations"],
      simple: "Deliberately break things in production, in a controlled way, so you find out how the system fails before it happens on its own at three in the morning. You start small, with a way to stop it instantly, and only in areas you already believe will hold.",
      answer: "## The premise\n\nDistributed systems fail in ways nobody designed for. Testing in staging does not find them because staging has different traffic, different data volumes and different timing. The only way to know how a system behaves under failure is to fail it — deliberately, in a controlled way, while you are watching.\n\nThe goal is not to break things. It is **to build confidence that the system withstands turbulent conditions**, by testing hypotheses about its behaviour.\n\n## The method\n\n1. **Define steady state** with a business metric, not a technical one. Netflix uses stream starts per second — it captures the user experience directly, and CPU usage does not.\n2. **Form a hypothesis.** \"If one availability zone fails, stream starts per second stays within 2% of baseline.\"\n3. **Introduce a real-world failure.** Instance termination, latency injection, dependency failure, packet loss, resource exhaustion.\n4. **Try to disprove the hypothesis.** A difference between control and experiment means you have found real weakness.\n5. **Fix it, then automate the experiment** so it stays fixed.\n\n## Introducing it safely\n\nThis is what the question is really asking.\n\n**Do not start in production.** Start in staging to shake out the tooling and build trust. But be clear that staging results are weak evidence — you have to graduate to production for it to mean anything.\n\n**Start with a hypothesis you are confident about.** The first experiment should confirm something you already believe. If your first act is to break something nobody understands, you cause an outage and the practice is cancelled.\n\n**Minimise the blast radius.**\n\n- One instance before one zone.\n- 1% of traffic before 100%.\n- One non-critical service before a payment path.\n\n**Have an abort button, and test it first.** An automatic halt when the steady-state metric moves beyond a threshold, and a manual stop anyone can hit. Verify the stop works *before* running the experiment.\n\n**Run during business hours** with people watching. Running chaos experiments at 2 a.m. combines the failure with nobody being awake — the opposite of the point.\n\n**Announce it.** Everyone affected should know it is happening. An unannounced experiment that causes an incident destroys the political capital the practice needs.\n\n## What to inject\n\n| Failure | Reveals |\n| --- | --- |\n| Instance termination | Whether the service is genuinely stateless and replaceable |\n| Latency injection | Missing timeouts, thread-pool exhaustion |\n| Dependency failure | Missing fallbacks, cascading failure |\n| Zone failure | Whether multi-AZ actually works |\n| Clock skew | Time-dependent bugs |\n| Resource exhaustion | Behaviour under memory or disk pressure |\n\n**Latency injection finds more real bugs than termination.** Services usually handle a dead dependency (it fails fast); a *slow* dependency exhausts the caller's thread pool and takes it down too. That is the most common cascading-failure pattern in production, and it is invisible until you inject latency.\n\n## What must exist first\n\nChaos engineering on an unobservable system just produces outages. Prerequisites:\n\n- Good monitoring, so you can see the effect within seconds.\n- Meaningful alerting.\n- A tested rollback path.\n- An on-call rotation that knows the experiment is running.\n\nIf a team cannot tell whether the system is healthy right now, chaos engineering is premature, and saying so is the mature answer.\n\n## The organisational half\n\nIt requires a blameless culture. If breaking production is punished, nobody will run an experiment — and you will find the same failures anyway, at 3 a.m., with a customer discovering them first.",
      takeaways: [
        "Define steady state with a business metric; stream starts per second, not CPU.",
        "Latency injection finds more real bugs than termination — slow dependencies exhaust thread pools.",
        "Test the abort button before the experiment, and run during business hours with people watching."
      ],
      followUps: [
        "Why does a slow dependency cause more damage than a dead one?",
        "How would you run this in a system handling payments?",
        "What would you need in place before you could start?"
      ],
      trap: "Describing it as randomly killing servers in production. The controlled, hypothesis-driven, blast-radius-limited framing is the whole discipline, and the random-killing version is how teams get it banned."
    },

    /* ---------------- Apple ---------------- */
    {
      id: "co-apple-on-device",
      q: "How would you run a neural network on a phone with a strict battery and latency budget?",
      topic: "ML & Statistics",
      level: "hard",
      companies: ["apple", "tesla", "nvidia", "google", "meta-ai", "adobe"],
      tags: ["on-device", "quantisation", "efficiency"],
      simple: "You shrink the model until it fits in memory and runs fast enough, using fewer bits per number and fewer operations. The unusual constraint is heat and battery: a model that runs fast for ten seconds and then gets throttled is not actually fast.",
      answer: "## The constraints are different\n\n| Constraint | Server | Phone |\n| --- | --- | --- |\n| Memory | 80 GB HBM | ~1–2 GB available to an app |\n| Power | Effectively unlimited | Battery, and heat |\n| Latency | Batched throughput | Single-stream, must feel instant |\n| Thermals | Datacentre cooling | **Sustained load throttles the chip** |\n\nThe thermal constraint is the one people miss. A model that hits target latency in a 10-second benchmark and then throttles is not shippable, because real usage is sustained. **Benchmark sustained performance, not peak.**\n\n## Why on-device at all\n\n- **Privacy.** Data never leaves the device — the decisive argument for anything personal, and often a legal requirement.\n- **Latency.** No network round trip. 20 ms locally beats 200 ms over a good connection and infinitely beats a bad one.\n- **Offline.** Works on a plane, in a lift, on a train.\n- **Cost.** Zero marginal inference cost.\n\n## Making it fit\n\n**Quantisation — the biggest single lever.**\n\n| Precision | Size | Notes |\n| --- | --- | --- |\n| FP32 | 1× | Rarely needed |\n| FP16 | 0.5× | Near-lossless |\n| INT8 | 0.25× | Small loss, hardware-accelerated |\n| INT4 | 0.125× | Noticeable; fine for many tasks |\n\n**Post-training quantisation** needs only a small calibration set and takes minutes. **Quantisation-aware training** simulates the rounding during training and recovers most of the loss, at the cost of a full training run. Start with post-training; escalate only if the quality loss is unacceptable.\n\n**Architecture, not just compression.** A model designed for mobile beats a compressed large one at the same size. Depthwise separable convolutions (MobileNet) factor a convolution into a depthwise and a pointwise step, cutting compute by roughly $1/k^2$ for a $k \\times k$ kernel with a small accuracy cost.\n\n**Distillation.** Train a small student on the large teacher's soft output distribution. The soft targets carry more information than hard labels — the relative probabilities of the wrong classes encode similarity structure the student can learn from.\n\n**Pruning.** Structured pruning (removing whole channels or heads) gives real speedups because the resulting dense tensors map onto the hardware. Unstructured pruning gives better compression ratios and usually **no speedup at all** without sparse-matrix hardware support — a distinction worth making, because reported sparsity numbers are frequently meaningless in practice.\n\n## Use the right silicon\n\nModern phones have a CPU, a GPU and a dedicated neural accelerator (Apple's Neural Engine, Qualcomm's Hexagon). The accelerator is far more power-efficient per operation and supports a **restricted set of operations**. An unsupported layer causes a fallback to CPU — and the transfer overhead can make the whole model slower than if it had never touched the accelerator.\n\nSo: check the operator support list before designing the architecture, not after. This is the practical detail that distinguishes someone who has shipped an on-device model.\n\n## Measure the right things\n\n- **Sustained latency** over minutes, not a single cold run.\n- **Energy per inference** (mJ), not just time. A GPU path can be faster and drain the battery twice as fast.\n- **Peak memory**, including activations — the OS kills apps that exceed it, and that is a crash, not a slowdown.\n- **Model download size**, which affects install conversion rate directly.\n- **Thermal state** during the run.\n\n## The hybrid answer\n\nOften the right design is neither: run a small model on-device for the common case, and escalate to a server model when the on-device model is uncertain. You get local latency and privacy for most requests and full quality when it matters — provided you can define \"uncertain\" reliably, which is its own problem.",
      takeaways: [
        "Benchmark sustained performance — thermal throttling makes peak numbers misleading.",
        "Unstructured pruning compresses but usually does not speed anything up without sparse hardware.",
        "Check the neural accelerator's operator support before choosing an architecture; a fallback can be net-negative."
      ],
      followUps: [
        "Why does distillation work better than training the small model directly?",
        "How do you decide the on-device/server escalation threshold?",
        "How would you update a model without a full app update? (Downloadable model assets with versioning.)"
      ],
      trap: "Optimising only for latency. Energy and thermals are what actually constrain a phone, and a model that wins the benchmark and drains the battery does not ship."
    },

    /* ---------------- Palantir ---------------- */
    {
      id: "co-palantir-abac",
      q: "Design fine-grained access control over a knowledge graph, where permissions vary per property.",
      topic: "System Design",
      level: "staff",
      companies: ["palantir", "salesforce", "microsoft", "google", "amazon", "snowflake"],
      tags: ["authorisation", "abac", "security"],
      simple: "Rather than giving people roles, you attach labels to every piece of data and evaluate a rule at query time against who is asking and what they are asking for. The hard part is doing that filtering inside the query, so results come back fast and nothing leaks through counts or error messages.",
      answer: "## Why roles are not enough\n\nRole-based access control assigns permissions to roles and roles to users. It works until requirements look like:\n\n> An analyst may see a person's name and city, but their address only if the case is assigned to them, and only if the source classification is at or below their clearance, and only during their assignment window.\n\nEncoding that as roles produces a combinatorial explosion. **Attribute-based access control** evaluates a policy over attributes of the subject, the resource, the action and the environment.\n\n$$\\text{permit} = f(\\text{subject attrs}, \\text{resource attrs}, \\text{action}, \\text{context})$$\n\n## The model\n\n**Subject attributes:** clearance level, department, assigned cases, training completed, employment status.\n\n**Resource attributes at the property level:** classification, source system, data subject's jurisdiction, retention state. This is the key move — the label sits on the property, not just the object, so `person.name` and `person.address` can carry different classifications.\n\n**Environment:** time, network location, whether the session was re-authenticated.\n\n```\npolicy view_property:\n  permit if\n    subject.clearance >= resource.classification\n    and resource.case_id in subject.assigned_cases\n    and subject.training_current\n    and now() within subject.assignment_window\n```\n\n## Making it fast\n\nThe naive approach — fetch everything, then filter — is both slow and dangerous, because the data has already left the storage layer and any bug leaks it.\n\n**Push the predicate into the query.** Translate the policy into a filter the storage engine applies, so unauthorised rows are never read. In a relational store this is row-level security; in a graph store it is a traversal-time predicate.\n\n**Precompute what you can.** Materialise the subject's effective clearance and assignment set at session start rather than re-deriving it per property.\n\n**Cache decisions carefully.** A policy decision cache keyed on (subject, resource-class, action) is effective, and the TTL must be short enough that a revocation takes effect quickly. Revocation latency is a security property and should be stated explicitly, not left as an accident of the cache configuration.\n\n## The leaks that are easy to miss\n\nThis is what separates a real answer from a textbook one.\n\n**Existence leaks through counts.** \"Showing 10 of 47 results\" tells an unauthorised user that 37 hidden records exist. Either filter before counting, or do not show a total.\n\n**Error messages.** \"Access denied\" confirms the record exists; \"not found\" does not. For sensitive resources, return not-found for both cases.\n\n**Timing.** A permission check that is slow when the record exists and fast when it does not is an oracle.\n\n**Aggregates.** A user may not see individual salaries but may see a departmental average. With a department of one, the average *is* the salary. Enforce minimum group sizes for aggregate queries — this is the classic statistical disclosure problem and it recurs constantly.\n\n**Joins and inference.** A user permitted to see two datasets separately may be able to join them into something neither permits alone.\n\n## Auditing\n\nIn this domain the audit log is a first-class requirement, not an add-on. Record who accessed what, when, under which policy version, and why the decision was permit or deny. **Log denials too** — a pattern of denials is often the first sign of an insider threat.\n\nThe log must be append-only and tamper-evident, and it must itself be access-controlled, since it contains sensitive metadata about who is investigating what.\n\n## Policy as versioned code\n\nPolicies belong in version control with review, tests, and a staged rollout. A policy change is a security change. Test both directions: that the permitted case is permitted, and that the denied case is denied — the second half is the one that gets skipped.",
      takeaways: [
        "Attach classification to the property, not just the object, so one record can be partially visible.",
        "Push the policy predicate into the query so unauthorised data is never read.",
        "Counts, error messages, timing and small-group aggregates all leak existence — name them."
      ],
      followUps: [
        "How do you handle a policy change that must apply retroactively?",
        "How do you prevent inference attacks across separately permitted datasets?",
        "How do you test a policy engine? (Property-based tests over generated subject/resource combinations.)"
      ],
      trap: "Filtering results after fetching them. It is the obvious implementation, it is slow, and any bug in the filter leaks data that should never have left storage."
    },

    /* ---------------- Tesla ---------------- */
    {
      id: "co-tesla-perception-latency",
      q: "Design a perception pipeline with a hard 100 ms end-to-end latency budget.",
      topic: "System Design",
      level: "hard",
      companies: ["tesla", "nvidia", "apple", "google-deepmind", "uber"],
      tags: ["real-time", "vision", "embedded"],
      simple: "Everything has a deadline, and being late is the same as being wrong. So you budget the milliseconds stage by stage, you care about the worst case rather than the average, and you make sure nothing in the pipeline can pause unpredictably.",
      answer: "## Hard real time is a different discipline\n\nA web service optimises average latency and tolerates a slow tail. A vehicle at 30 m/s covers 3 metres in 100 ms — **a missed deadline is a wrong answer**, not a slow one.\n\nSo the design target is **worst-case execution time**, not the mean. A pipeline averaging 40 ms with a 150 ms P99.9 fails, while one that always takes 80 ms passes.\n\n## Budget every stage\n\n```\nsensor capture and transfer        12 ms\nundistort / rectify                 5 ms\npreprocessing (resize, normalise)   4 ms\nbackbone inference                 35 ms\ndetection heads + NMS               8 ms\nmulti-camera fusion                10 ms\ntracking and association            8 ms\nplanning interface                  6 ms\n---------------------------------------\ntotal                              88 ms   (12 ms margin)\n```\n\nWrite it down and measure each stage independently. Without per-stage budgets, an optimisation goes into whichever stage someone found interesting rather than the one that is over.\n\n## What kills determinism\n\n- **Dynamic memory allocation.** `malloc` can take an unbounded time under fragmentation. Preallocate every buffer at startup and reuse. This is the single most important rule in a real-time path.\n- **Garbage collection.** Rules out most managed runtimes for the hot path.\n- **Page faults.** `mlock` the working set so nothing is paged out.\n- **Unbounded queues.** A queue that grows adds unbounded latency. Use fixed-size ring buffers that **drop the oldest frame** when full — a stale frame is worse than no frame.\n- **Priority inversion.** A low-priority thread holding a lock the perception thread needs. Use priority inheritance.\n- **CPU frequency scaling and thermal throttling.** Pin frequencies, or budget for the throttled clock.\n\n## Where the milliseconds go\n\n**Inference.** INT8 quantisation with TensorRT typically gives 2–4× over FP32 with negligible accuracy loss for detection. Layer fusion (conv + batchnorm + ReLU into one kernel) removes intermediate memory traffic. Run a shared backbone with several task heads rather than separate networks per task — the backbone is the expensive part and the heads are cheap.\n\n**Overlap, do not serialise.** While the GPU runs inference on frame $n$, the CPU should be preprocessing frame $n+1$ and post-processing frame $n-1$. Use CUDA streams and pinned memory for asynchronous transfers. A properly pipelined system has throughput equal to its slowest stage rather than the sum of all stages — though **latency** is still the sum, and it is latency that has the deadline.\n\n**Avoid host round trips.** Keep data on the GPU across stages. Preprocessing on the GPU is usually worth it purely to avoid the transfer.\n\n## Multi-camera timing\n\nCameras must be **hardware-synchronised** — a common trigger signal, not software timestamps. Fusing frames captured 30 ms apart while moving at 30 m/s means fusing views of positions a metre apart, and the resulting geometry is silently wrong.\n\nEvery message carries a **capture timestamp** from the trigger, and downstream stages reason in capture time, never arrival time.\n\n## Degrade, do not fail\n\nThe system must have a defined behaviour when a stage misses its deadline:\n\n- Reuse the previous frame's tracks, extrapolated forward.\n- Drop to a lower-resolution model.\n- Reduce the operational envelope and hand back to the driver.\n\nAn undefined behaviour on deadline miss is the actual safety failure. Define it, implement it, and test it by deliberately injecting delays — which is chaos engineering applied to a real-time system.\n\n## Measure correctly\n\nMeasure **end to end from photon to actuation**, not per-component. Component benchmarks miss queueing between stages, which is usually where the surprise is. And report the maximum over a long run, not the mean — the mean is not the quantity with a deadline.",
      takeaways: [
        "Budget worst-case execution time per stage; the mean is not the quantity with a deadline.",
        "Preallocate everything — dynamic allocation is the main source of non-determinism.",
        "Hardware-sync the cameras and reason in capture time; software timestamps fuse the wrong geometry."
      ],
      followUps: [
        "How would you test that the deadline is met under worst case? (Inject load and delays; run for hours; report the maximum.)",
        "Why is a bounded drop-oldest queue better than a growing one?",
        "How do you handle a sensor that fails entirely mid-drive?"
      ],
      trap: "Optimising average latency. In a hard real-time system the tail is the requirement, and an answer built around throughput or mean latency misses what the question is about."
    },

    /* ---------------- Flipkart / high-scale commerce ---------------- */
    {
      id: "co-flipkart-saga",
      q: "An order spans inventory, payment and shipping services. How do you keep it consistent without a distributed transaction?",
      topic: "System Design",
      level: "hard",
      companies: ["flipkart", "amazon", "uber", "salesforce", "bytedance", "microsoft"],
      tags: ["saga", "distributed-transactions", "consistency"],
      simple: "You cannot hold a lock across three services, so instead you do the steps one at a time and, if a later one fails, you run an undo action for each completed step. The undo is a new action, not a rewind — a refund is a real event, not the charge disappearing.",
      answer: "## Why two-phase commit is not the answer\n\n2PC gives atomicity across services, and:\n\n- **It blocks.** Participants hold locks through the prepare phase. If the coordinator dies after prepare, participants hold locks indefinitely, unable to commit or abort.\n- **Availability multiplies downward.** Every participant must be up for any transaction to complete.\n- **Most modern datastores do not support it** across heterogeneous systems.\n\nHolding a database lock across a payment gateway call — seconds, sometimes failing — is not viable at any volume.\n\n## The saga\n\nA sequence of local transactions, each with a **compensating action** that semantically undoes it.\n\n```\nforward:      reserve stock --> charge card --> create shipment --> confirm\ncompensate:   release stock <-- refund      <-- cancel shipment\n```\n\nIf step 3 fails, run the compensations for steps 2 and 1, in reverse.\n\n## Compensation is not rollback\n\nThis is the conceptual point of the question.\n\nA rollback makes it as if nothing happened. A **compensation is a new transaction with its own record**. A refund appears in the ledger alongside the original charge; the charge still happened and is still visible on the customer's statement.\n\nThat is the correct behaviour for money and it has a consequence: **intermediate states are visible**. A customer may see a charge and then a refund. Design for that — an email explaining the failed order is part of the compensation, not an afterthought.\n\n## Orchestration or choreography\n\n**Orchestration.** A central saga coordinator calls each service and tracks state.\n\n- Explicit, visible flow. Easy to see where a saga is stuck.\n- The coordinator is a component to build, scale and make fault-tolerant.\n- Better for complex flows, and better for debugging.\n\n**Choreography.** Each service listens for events and emits its own.\n\n- No central component; services stay decoupled.\n- The flow exists only implicitly across the codebase. Understanding it means reading every service.\n- Cyclic dependencies are easy to create by accident.\n\n**Prefer orchestration for anything with more than three steps.** The debuggability is worth the coordinator, and \"where is order 4471 stuck\" should be answerable with one query.\n\n## The requirements that make it work\n\n**Idempotency everywhere.** Every step and every compensation must be safe to run twice — the coordinator will retry after a timeout, not knowing whether the first attempt landed. Idempotency keys on every call.\n\n**Compensations must not fail.** Or rather: they must be retried until they succeed, because there is no compensation for a failed compensation. Design them to be simple, and alert loudly when one exhausts its retries — that is a case for a human.\n\n**Durable saga state.** The coordinator's state must survive a crash. Persist after every step so recovery can resume from where it stopped.\n\n**Timeouts on every step.** A step that never returns must eventually trigger compensation, or the saga hangs and the stock stays reserved forever.\n\n## The semantic lock\n\nSome steps cannot be compensated at all — an email cannot be unsent. Two techniques:\n\n- **Reorder** so irreversible steps come last, after everything that can fail.\n- **Use a pending state** as a semantic lock. Reserve stock rather than decrementing it; create the shipment in `pending` and only dispatch after confirmation. The reservation is a compensatable proxy for the irreversible act.\n\n## The transactional outbox\n\nA subtle failure: the service commits its local transaction and then crashes before publishing its event. The saga stalls.\n\nFix by writing the event into an `outbox` table **in the same local transaction** as the state change. A separate relay reads the outbox and publishes. The database transaction makes the state change and the intent-to-publish atomic, and the relay guarantees at-least-once delivery.\n\nThis pattern is what makes choreographed sagas reliable at all, and it is the detail that shows real experience.",
      takeaways: [
        "Compensation is a new visible transaction, not a rollback — customers see the charge and the refund.",
        "Prefer orchestration past three steps; \"where is this order stuck\" must be one query.",
        "The transactional outbox makes the state change and the event publication atomic."
      ],
      followUps: [
        "What happens if a compensation fails permanently? (Retry, then alert — it is a human case.)",
        "How do you avoid holding stock reserved forever? (A reservation TTL plus a sweeper.)",
        "How do you test a saga's failure paths? (Fault injection at each step, verifying the invariant afterwards.)"
      ],
      trap: "Proposing two-phase commit. It is the textbook answer, it blocks on coordinator failure, and at e-commerce volumes across a payment gateway it is simply not viable."
    },

    /* ---------------- TCS / foundations ---------------- */
    {
      id: "co-tcs-sdlc-fundamentals",
      q: "Explain the SDLC and compare Agile with Waterfall. When would you genuinely choose Waterfall?",
      topic: "OOP & Code Quality",
      level: "easy",
      companies: ["tcs", "oracle", "salesforce", "microsoft", "adobe"],
      tags: ["sdlc", "process", "fundamentals"],
      simple: "The development lifecycle is the path from understanding a need to running software that is maintained. Waterfall does each stage once in order; Agile does all of them repeatedly in short cycles. Waterfall still wins where the requirements genuinely cannot change.",
      answer: "## The phases\n\n1. **Requirements** — what is needed, and why. Functional and non-functional.\n2. **Design** — architecture, data model, interfaces.\n3. **Implementation** — writing it.\n4. **Testing** — unit, integration, system, acceptance.\n5. **Deployment** — release to users.\n6. **Maintenance** — fixes and enhancements, which is where most of the total cost lives.\n\nEvery methodology contains these. The difference is how many times you go through them and in what size.\n\n## Waterfall\n\nEach phase completes before the next begins, with a signed-off document at each gate.\n\n**Strengths:** clear milestones, thorough documentation, predictable in a fixed-scope contract, and it produces the audit trail regulators want.\n\n**Weaknesses:** requirements are assumed knowable up front, which for most software they are not. Feedback comes only at the end, so a misunderstanding in phase 1 is discovered in phase 4 when it is most expensive. The cost of change grows through the project.\n\n## Agile\n\nShort iterations (typically two weeks), each producing something working. Requirements are expected to evolve.\n\n**Strengths:** early and continuous feedback, change is cheap and expected, working software early, and a much shorter path from a wrong assumption to discovering it.\n\n**Weaknesses:** harder to fix scope and cost in advance, which fits badly with fixed-price contracts. Documentation is often thinner. Without discipline, \"we're Agile\" becomes an excuse for no planning at all — which is the most common failure in practice, and worth naming.\n\n## When Waterfall is genuinely right\n\nA good answer names real cases rather than dismissing it:\n\n- **Regulated and safety-critical work** — avionics, medical devices, nuclear. The regulator requires documented traceability from requirement to test. This is not bureaucracy; it is how the certification works.\n- **Fixed-price fixed-scope contracts**, where the deliverable is legally defined in advance.\n- **Hardware-coupled projects**, where a physical manufacturing deadline cannot iterate.\n- **A genuinely well-understood problem** you have built several times before.\n\nThe honest position: most organisations run a **hybrid**. Iterative delivery inside a phase-gated programme, or Agile development against a fixed regulatory documentation set. Presenting Agile as universally correct is a weaker answer than knowing why the alternative exists.\n\n## The related models\n\n- **V-model** — Waterfall with each development phase paired to a matching test phase. Standard in automotive and medical device work.\n- **Spiral** — iterative with explicit risk analysis each cycle. Used where risk is the dominant concern.\n- **Incremental** — deliver in slices, each slice done fully.\n\n## What matters more than the label\n\nWhichever process is named, the questions that predict outcomes are the same: how quickly does a bad assumption get found, how fast can a change ship safely, and does the team have a way to say something is not working? A team with continuous integration, tests and short feedback loops does well under either label.",
      takeaways: [
        "Every methodology contains the same phases; the difference is iteration size and count.",
        "Waterfall is genuinely correct for regulated, safety-critical and fixed-contract work.",
        "Most real organisations run a hybrid — saying so is stronger than dismissing Waterfall."
      ],
      followUps: [
        "What is the V-model, and why does automotive use it?",
        "How does DevOps relate to these? (It extends iteration through deployment and operation.)",
        "What is technical debt, and how do you argue for paying it down?"
      ],
      trap: "Dismissing Waterfall as outdated. It is the required process in several industries, and knowing which ones is a better answer than a blanket preference."
    },
    {
      id: "co-tcs-sql-basics",
      q: "Write SQL to find employees earning more than their department's average, then explain how you would make it fast.",
      topic: "DBMS",
      level: "easy",
      companies: ["tcs", "oracle", "salesforce", "adobe", "flipkart", "microsoft", "amazon"],
      tags: ["sql", "subquery", "fundamentals"],
      simple: "You need each employee's salary compared against a number computed from their own department. A correlated subquery does it directly; a window function does the same thing in one pass and is usually faster.",
      answer: "## The correlated subquery\n\n```sql\nSELECT e.employee_id, e.name, e.department_id, e.salary\n  FROM employees e\n WHERE e.salary > (\n        SELECT AVG(e2.salary)\n          FROM employees e2\n         WHERE e2.department_id = e.department_id\n       );\n```\n\nCorrect and the most direct expression of the requirement. The subquery is **correlated**: it references `e.department_id` from the outer query, so conceptually it runs once per row.\n\nGood optimisers rewrite this into a join against a grouped aggregate, but do not rely on it — check the plan.\n\n## The window function version\n\n```sql\nSELECT employee_id, name, department_id, salary\n  FROM (\n    SELECT employee_id, name, department_id, salary,\n           AVG(salary) OVER (PARTITION BY department_id) AS dept_avg\n      FROM employees\n  ) t\n WHERE salary > dept_avg;\n```\n\nOne pass over the table. The window function computes the departmental average and attaches it to every row without collapsing them — which is exactly what `GROUP BY` cannot do.\n\nThe subquery wrapper is required because **you cannot reference a window function in `WHERE`**; window functions are evaluated after `WHERE`. Engines with `QUALIFY` (Snowflake, BigQuery, Databricks) let you skip the wrapper.\n\n## The explicit join version\n\n```sql\nSELECT e.employee_id, e.name, e.department_id, e.salary\n  FROM employees e\n  JOIN (SELECT department_id, AVG(salary) AS dept_avg\n          FROM employees\n         GROUP BY department_id) d\n    ON d.department_id = e.department_id\n WHERE e.salary > d.dept_avg;\n```\n\nComputes each average once. Often what the optimiser produces from the correlated version anyway, and it makes the intent explicit.\n\n## Making it fast\n\n**Index** on `(department_id, salary)`. It groups rows by department physically, so the aggregate reads contiguous data, and it covers both columns the query needs.\n\n**Check the plan.** In PostgreSQL, `EXPLAIN ANALYZE` will show either a `SubPlan` executed repeatedly (bad) or a `HashAggregate` joined once (good). If it is the first, rewrite as the explicit join.\n\n**On a large table**, the window-function version usually wins: one scan plus one sort or hash per partition, versus a potential re-scan per row.\n\n## The NULL trap\n\n`AVG` ignores NULLs, so a department where every salary is NULL yields NULL, and `salary > NULL` is NULL — which is **not true**, so those rows are excluded. That may or may not be what you want. Make it explicit:\n\n```sql\nWHERE e.salary > COALESCE((SELECT AVG(...) ...), 0)\n```\n\nInterviewers ask about NULL behaviour constantly, because three-valued logic is where most SQL bugs live. Volunteering it is a good signal.\n\n## The follow-up to expect\n\n\"Now show the department name too.\" You need a join to `departments` — and it should be a `LEFT JOIN` if an employee might have no department, or you silently drop those rows.",
      takeaways: [
        "The window-function version computes the average in one pass without collapsing rows.",
        "Window functions cannot be filtered in `WHERE` — wrap in a subquery or use `QUALIFY`.",
        "`AVG` ignores NULLs and comparison to NULL is not true; state the behaviour explicitly."
      ],
      followUps: [
        "Include the department name — and say why it should be a LEFT JOIN.",
        "Now find the top earner per department. (`ROW_NUMBER` or a correlated `MAX`.)",
        "What is the difference between `WHERE` and `HAVING`? (Before and after aggregation.)"
      ],
      trap: "Writing `GROUP BY department_id` and then selecting individual employee columns. Most engines reject it, and MySQL historically accepted it and returned an arbitrary row per group — which is worse than an error."
    },

    /* ---------------- Adobe ---------------- */
    {
      id: "co-adobe-diffusion",
      q: "How do diffusion models generate images, and what does classifier-free guidance do?",
      topic: "ML & Statistics",
      level: "hard",
      companies: ["adobe", "openai", "google-deepmind", "meta-ai", "nvidia", "bytedance"],
      tags: ["diffusion", "generative", "guidance"],
      aiTrack: true,
      simple: "Training teaches a network to remove a little noise from a noisy image. Generation starts from pure static and applies that network repeatedly until an image appears. Guidance works by comparing what the model would draw with your prompt against what it would draw with no prompt, and exaggerating the difference.",
      answer: "## The forward process\n\nGradually add Gaussian noise to a real image over $T$ steps until nothing but noise remains. This has a closed form — you can jump to any noise level directly:\n\n$$x_t = \\sqrt{\\bar{\\alpha}_t}\\, x_0 + \\sqrt{1 - \\bar{\\alpha}_t}\\, \\epsilon, \\qquad \\epsilon \\sim \\mathcal{N}(0, I)$$\n\nThat closed form is what makes training efficient: you can sample a random timestep and construct its noisy image in one step, without simulating the chain.\n\n## Training\n\nThe network predicts the **noise** that was added, given the noisy image and the timestep:\n\n$$\\mathcal{L} = E_{t, x_0, \\epsilon}\\left[\\|\\epsilon - \\epsilon_\\theta(x_t, t)\\|^2\\right]\\!$$\n\nPredicting the noise rather than the clean image is a reparameterisation that works substantially better empirically. Note how simple the objective is — a plain mean squared error, no adversarial game, which is a large part of why diffusion models train more stably than GANs.\n\n## Sampling\n\nStart from pure noise and iteratively denoise. Each step predicts the noise, removes a portion of it, and adds a smaller amount back. DDPM uses many steps; DDIM makes the process deterministic and allows far fewer (20–50) with little quality loss.\n\n## Classifier-free guidance\n\nDuring training, drop the conditioning (the text prompt) some fraction of the time — typically 10%. The same network therefore learns both the conditional and unconditional distributions.\n\nAt sampling, run both and extrapolate away from the unconditional:\n\n$$\\hat{\\epsilon} = \\epsilon_\\theta(x_t, \\varnothing) + s \\cdot \\big(\\epsilon_\\theta(x_t, c) - \\epsilon_\\theta(x_t, \\varnothing)\\big)$$\n\nThe difference term is the direction the prompt pulls in. Scaling it by $s > 1$ **exaggerates** that direction, pushing the sample further into prompt-consistent territory than the model's own conditional prediction would.\n\n| $s$ | Effect |\n| --- | --- |\n| 1 | Plain conditional generation |\n| 3–8 | The usual range: strong prompt adherence, good diversity |\n| > 15 | Over-saturated, high-contrast, artefact-prone, low diversity |\n\nThe trade-off is **fidelity to the prompt versus diversity**. High guidance narrows the distribution toward the prompt's most typical interpretation, which is why heavily guided images look samey.\n\nThe cost is two forward passes per step rather than one — guided sampling is roughly 2× the compute, which is a substantial part of image-generation inference cost.\n\n\"Classifier-free\" refers to the predecessor: classifier guidance required training a separate noise-aware classifier and backpropagating through it. This achieves the same steering with no extra model.\n\n## Latent diffusion\n\nRunning diffusion on 512×512 pixels is expensive. **Latent diffusion** (Stable Diffusion) trains an autoencoder to compress to a much smaller latent space — typically 8× smaller per side, so 64× fewer elements — and runs the diffusion there, decoding once at the end.\n\nRoughly 64× less compute per step for a small quality cost, and it is what made high-resolution generation practical on consumer hardware.\n\n## Diffusion versus GANs\n\n| | Diffusion | GAN |\n| --- | --- | --- |\n| Training stability | Stable (plain MSE) | Adversarial, fragile |\n| Mode coverage | Good | Mode collapse is common |\n| Sampling speed | Slow (many steps) | One forward pass |\n| Controllability | Strong (guidance, inpainting) | Harder |\n\nDiffusion won on stability and coverage; its weakness is sampling speed, which is what consistency models and distillation are attacking.",
      takeaways: [
        "The closed-form forward process is what makes the training objective a simple one-step MSE.",
        "Guidance extrapolates along the conditional-minus-unconditional direction, trading diversity for prompt fidelity.",
        "Latent diffusion runs the process in a 64×-smaller space, which is what made it practical."
      ],
      followUps: [
        "Why does predicting noise work better than predicting the clean image?",
        "How does inpainting work? (Mask, and replace the known region with its correctly-noised version at each step.)",
        "How do consistency models cut sampling to one or two steps? (Distil the trajectory into a direct map.)"
      ],
      trap: "Describing guidance as \"turning up the prompt strength\". The mechanism is extrapolation along the difference between conditional and unconditional predictions, and that is what the question is checking."
    }
  ]);
})(window.TD = window.TD || {});
