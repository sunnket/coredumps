/* Interview bank — databases.

   Database rounds reward precision. "It locks" and "it uses an index" are not
   answers; which lock, on what, held for how long, and what the planner did
   instead — those are answers. */
(function (TD) {
  "use strict";

  TD.addQuestions([
    {
      id: "db-acid",
      q: "Explain ACID. Which of the four is the one people actually misunderstand?",
      topic: "DBMS",
      level: "easy",
      companies: "*",
      tags: ["transactions", "fundamentals"],
      simple: "Four promises a database makes about a transaction: it happens completely or not at all, it leaves the data valid, other transactions do not see it half-done, and once it says yes the data survives a crash. The one everybody gets wrong is the second — Consistency does not mean what it means in CAP.",
      answer: "## The four properties\n\n**Atomicity.** All statements in a transaction succeed or none do. Implemented with an undo log: uncommitted changes can be rolled back.\n\n**Consistency.** The transaction moves the database from one valid state to another, where \"valid\" means *your declared constraints hold* — foreign keys, unique constraints, check constraints, triggers. It is the database enforcing rules you wrote.\n\n**Isolation.** Concurrent transactions do not observe each other's partial work. How strictly is set by the isolation level, and this is where all the interesting behaviour lives.\n\n**Durability.** Once committed, the data survives a crash. Implemented with a write-ahead log flushed to stable storage before the commit is acknowledged.\n\n## The one people misunderstand\n\n**Consistency.** Two distinct meanings share the word:\n\n- **ACID consistency** — your constraints are satisfied. It is really a property of your schema, not of the database engine.\n- **CAP consistency** — every read sees the most recent write, across all nodes. This is linearizability, and it is a distributed-systems property.\n\nThey are unrelated. A single-node PostgreSQL is fully ACID-consistent and the CAP question does not even apply to it. A distributed database can be strongly CAP-consistent while your schema has no constraints at all.\n\nSaying this unprompted is a reliable signal that you have read past the acronym.\n\n## How durability is actually achieved\n\nWrite-ahead logging. Before modifying a data page, write the change to a sequential log and `fsync` it. Commit is acknowledged once the **log** is durable — not once the data pages are.\n\nThis is faster than it sounds: the log is sequential (fast on any storage), while data pages are scattered (slow). Pages are written back lazily, and recovery replays the log to reconstruct anything not yet written.\n\nThe honest caveat: durability depends on `fsync` actually reaching stable storage. Consumer SSDs with volatile write caches, and `fsync` implementations that lie, have caused real data loss. Enterprise drives use power-loss-protected caches for exactly this reason.",
      takeaways: [
        "ACID consistency means your constraints hold; CAP consistency means replicas agree. Different things, same word.",
        "Durability comes from flushing the write-ahead log, not the data pages.",
        "Sequential log writes are what make durability affordable."
      ],
      followUps: [
        "What does a database do on restart after a crash? (Redo committed transactions, undo uncommitted ones.)",
        "Can you have atomicity without durability? (Yes — an in-memory database.)",
        "How does `synchronous_commit = off` change the guarantee? (Trades a small durability window for much faster commits.)"
      ],
      trap: "Defining Consistency as \"all nodes see the same data\". That is CAP, not ACID, and it is the most common error in the whole topic."
    },
    {
      id: "db-isolation-levels",
      q: "Walk through the SQL isolation levels and the anomaly each one prevents.",
      topic: "DBMS",
      level: "medium",
      companies: ["oracle", "snowflake", "amazon", "microsoft", "google", "databricks", "salesforce", "flipkart", "tcs", "uber"],
      tags: ["transactions", "isolation", "mvcc"],
      simple: "The levels are a ladder. Each rung stops one more way that two transactions running at the same time can confuse each other — and each rung costs more performance. Most databases default to the second or third rung because the top one is genuinely expensive.",
      answer: "## The anomalies\n\n**Dirty read.** You read a row another transaction has written but not committed. If it rolls back, you acted on data that never existed.\n\n**Non-repeatable read.** You read a row, someone commits an update, you read it again and it changed within your own transaction.\n\n**Phantom read.** You run a range query, someone commits an insert into that range, you run it again and there is a new row.\n\n**Write skew.** Two transactions each read an overlapping set, each decides its write is fine based on what it read, and together they violate a constraint neither could see. This one is not in the original standard and is the most interesting.\n\n## The ladder\n\n| Level | Dirty read | Non-repeatable | Phantom | Write skew |\n| --- | --- | --- | --- | --- |\n| Read Uncommitted | possible | possible | possible | possible |\n| Read Committed | prevented | possible | possible | possible |\n| Repeatable Read | prevented | prevented | possible* | possible |\n| Serializable | prevented | prevented | prevented | prevented |\n\n\\* The standard permits phantoms at Repeatable Read. PostgreSQL's implementation (snapshot isolation) prevents them anyway, which is why the standard's table and PostgreSQL's behaviour disagree.\n\n## Defaults differ, and it matters\n\n- **PostgreSQL, Oracle, SQL Server:** Read Committed.\n- **MySQL/InnoDB:** Repeatable Read.\n\nSo the same application code behaves differently across engines. Under Read Committed each statement sees a fresh snapshot; under Repeatable Read the whole transaction sees one snapshot taken at its start. Code that reads, computes, and writes can be correct on one and wrong on the other.\n\n## Write skew — the one worth explaining\n\nThe classic: a hospital requires at least one doctor on call. Two are on call; both want to go off.\n\n```sql\n-- Transaction A                     -- Transaction B (concurrent)\nSELECT count(*) FROM doctors         SELECT count(*) FROM doctors\n  WHERE on_call = true;   -- 2         WHERE on_call = true;   -- 2\n-- 2 >= 2, safe to leave              -- 2 >= 2, safe to leave\nUPDATE doctors SET on_call = false   UPDATE doctors SET on_call = false\n  WHERE name = 'Alice';                WHERE name = 'Bob';\nCOMMIT;                              COMMIT;\n```\n\nBoth commit. Nobody is on call. Each transaction's write was individually valid given what it read — but they read overlapping data and wrote to disjoint rows, so no write-write conflict was detected.\n\nSnapshot isolation permits this. Only Serializable prevents it. The alternatives are an explicit lock (`SELECT ... FOR UPDATE`) or materialising the conflict into a row both transactions must touch.\n\n## How Serializable is implemented\n\n- **Two-phase locking** (SQL Server, MySQL): acquire locks, hold to commit. Correct, and blocking.\n- **Serializable Snapshot Isolation** (PostgreSQL): run optimistically on snapshots, track read/write dependencies, and abort a transaction if a cycle forms. No blocking, but **your application must handle serialisation failures and retry**. Code that does not retry will simply fail under load.",
      takeaways: [
        "Each level prevents one more anomaly at increasing cost; know the table.",
        "MySQL defaults to Repeatable Read and PostgreSQL to Read Committed — the same code can behave differently.",
        "Write skew is invisible to snapshot isolation; Serializable or an explicit lock is the fix."
      ],
      followUps: [
        "What does `SELECT ... FOR UPDATE` do, and when is it the right tool?",
        "Why does PostgreSQL's Serializable need application retry logic?",
        "Give a write-skew example from a real product. (Double-booking, overdraft across two accounts.)"
      ],
      trap: "Saying Repeatable Read prevents phantoms because PostgreSQL does. The standard permits them at that level; PostgreSQL exceeds the standard. Naming both is the precise answer."
    },
    {
      id: "db-btree-vs-lsm",
      q: "Compare B-trees and LSM-trees. When would you choose each?",
      topic: "DBMS",
      level: "hard",
      companies: ["oracle", "snowflake", "databricks", "amazon", "google", "uber", "bytedance", "netflix"],
      tags: ["storage-engine", "indexing", "write-amplification"],
      simple: "A B-tree edits pages where they sit, so reads are direct but every small write touches a whole page. An LSM-tree just appends everything and sorts it out later in the background, so writes are cheap but a read may have to look in several places.",
      answer: "## B-tree\n\nA balanced tree of fixed-size pages (typically 4–16 KB), updated **in place**. A lookup walks from root to leaf.\n\n- Depth is $O(\\log_B n)$ with a high branching factor — a billion rows is about 4 levels, and the upper levels stay cached.\n- Reads are predictable: one page read per level, and only the leaf is usually uncached.\n- Range scans follow sibling pointers between leaves — sequential and fast.\n\n**Write amplification.** Changing one 100-byte row rewrites an entire page, plus a write-ahead log entry. On some engines a full-page image is logged after a checkpoint, so a 100-byte update can cost 20 KB of I/O.\n\n## LSM-tree\n\nWrites land in an in-memory sorted structure (the memtable). When it fills, it is flushed as an immutable sorted file (an SSTable). Background compaction merges those files.\n\n- Writes are sequential appends — excellent on both spinning disks and SSDs, and gentler on flash wear.\n- A read may need to check the memtable and several SSTables. **Bloom filters** per SSTable make most of those checks a cheap negative.\n- Deletes write a tombstone; the space is reclaimed only at compaction.\n\n**Read amplification** from checking multiple files, and **space amplification** from obsolete versions awaiting compaction.\n\n## Compaction strategy is the real trade-off\n\n| Strategy | Write amp | Read amp | Space amp | Used by |\n| --- | --- | --- | --- | --- |\n| Size-tiered | Low | High | **High (~2×)** | Cassandra default |\n| Levelled | High | Low | Low (~1.1×) | RocksDB, LevelDB |\n\nSize-tiered merges similarly-sized files: cheap writes, many files to search, and at worst you need double the disk during a merge. Levelled keeps one sorted run per level: at most one file per level to check, at the cost of rewriting data repeatedly as it moves down.\n\nThat choice is more consequential than B-tree versus LSM in isolation.\n\n## Choosing\n\n| Workload | Choose | Why |\n| --- | --- | --- |\n| Read-heavy OLTP with point lookups | B-tree | Predictable single-lookup latency |\n| Write-heavy ingest, time-series, logs | LSM | Sequential writes absorb the volume |\n| Range scans dominate | B-tree | Clustered leaves are contiguous |\n| Data much larger than RAM | LSM | Better compression, smaller footprint |\n| Latency must be predictable | B-tree | Compaction causes latency spikes |\n\nThat last row matters. Compaction is background I/O that competes with foreground queries, so LSM systems show periodic P99 spikes. If your SLA is on the tail, that is a real cost.\n\n## In the wild\n\n- B-tree: PostgreSQL, MySQL/InnoDB, Oracle, SQL Server, SQLite.\n- LSM: RocksDB, LevelDB, Cassandra, HBase, ScyllaDB, and most of the internals of modern time-series databases.\n\nMySQL with MyRocks swaps InnoDB's B-tree for an LSM specifically to cut write amplification on flash — a good concrete example that the choice is about the workload, not the product.",
      takeaways: [
        "B-tree: in-place updates, predictable reads, high write amplification.",
        "LSM: append-only writes, background compaction, read and space amplification instead.",
        "Levelled versus size-tiered compaction is the bigger practical decision within LSM."
      ],
      followUps: [
        "How do Bloom filters reduce LSM read amplification, and what is the false-positive cost?",
        "Why do tombstones cause problems in Cassandra? (They accumulate and slow range scans until compaction.)",
        "What is a fractal tree or a $B^\\epsilon$-tree? (A middle ground buffering writes inside internal nodes.)"
      ],
      trap: "Saying LSM is simply faster. It is faster at writes and slower at reads, and compaction introduces tail-latency spikes that a B-tree does not have."
    },
    {
      id: "db-index-design",
      q: "You have a slow query. Walk me through deciding which index to add — and when not to add one.",
      topic: "DBMS",
      level: "medium",
      companies: ["amazon", "oracle", "microsoft", "salesforce", "flipkart", "uber", "snowflake", "adobe", "tcs"],
      tags: ["indexing", "query-plan", "performance"],
      simple: "Look at what the query filters on, what it sorts by, and what it returns. Put the equality filters first in the index, then the sort column, then a range filter. If the index also contains every column the query asks for, the database never touches the table at all.",
      answer: "## Read the plan first\n\nNever guess. `EXPLAIN ANALYZE` (PostgreSQL) or `EXPLAIN` (MySQL) shows what actually happened.\n\nLook for:\n\n- **Seq Scan / full table scan** on a large table with a selective filter.\n- **A large gap between estimated and actual rows** — the planner's statistics are stale, and no index will fix that. Run `ANALYZE`.\n- **Sort or Hash spilling to disk** — often fixable with an index that provides the order.\n- **Nested loop over many rows** — usually a missing index on the join column.\n\n## Column order in a composite index\n\nThis is the part people get wrong. For a B-tree index on `(a, b, c)`:\n\n**Equality first, then sort, then range.**\n\n```sql\nSELECT * FROM orders\n WHERE tenant_id = 42          -- equality\n   AND created_at > '2026-01-01'  -- range\n ORDER BY created_at DESC\n LIMIT 20;\n```\n\nThe right index is `(tenant_id, created_at)`. `tenant_id` narrows to one tenant, and within that the entries are already ordered by `created_at` — so the range and the sort are both satisfied by walking the index backwards, and `LIMIT 20` stops after 20 entries.\n\nReversing to `(created_at, tenant_id)` forces a scan of every row in the date range across all tenants.\n\n**The rule: once you use a range predicate on a column, no column after it in the index can be used for further filtering.** A range leaves the index positioned across many entries, so subsequent columns are no longer in sorted order within the matched set.\n\n## The leftmost prefix rule\n\nAn index on `(a, b, c)` can serve queries filtering on `a`, on `(a, b)`, or on `(a, b, c)` — but **not** on `b` alone or on `(b, c)`. The index is sorted by `a` first, so without a constraint on `a` there is no contiguous range to scan.\n\nThis is why `(a, b)` and `(b, a)` are genuinely different indexes and you sometimes need both.\n\n## Covering indexes\n\nIf the index contains every column the query needs, the database answers entirely from the index and never reads the table:\n\n```sql\nCREATE INDEX idx_orders_cover\n    ON orders (tenant_id, created_at) INCLUDE (status, total);\n```\n\nPostgreSQL calls it `INCLUDE`, SQL Server calls it `INCLUDE`, MySQL gets it implicitly since secondary indexes carry the primary key. The saving is real — a table lookup per row is often the dominant cost.\n\n## When not to add an index\n\n- **Low selectivity.** An index on a boolean that is 50/50 will not be used; a scan is cheaper than an index lookup per row plus a table fetch.\n- **Write-heavy tables.** Every index is maintained on every insert, update and delete. Five indexes make writes several times more expensive.\n- **You already have a usable prefix.** If `(a, b)` exists, adding `(a)` is redundant.\n- **The table is small.** Under a few thousand rows a scan wins outright.\n\n## Find the unused ones\n\n```sql\n-- PostgreSQL: indexes never scanned since statistics were reset\nSELECT schemaname, relname, indexrelname, idx_scan,\n       pg_size_pretty(pg_relation_size(indexrelid)) AS size\n  FROM pg_stat_user_indexes\n WHERE idx_scan = 0\n ORDER BY pg_relation_size(indexrelid) DESC;\n```\n\nUnused indexes cost write throughput and disk for nothing. Dropping them is often a bigger win than adding one.",
      takeaways: [
        "Composite index order: equality columns, then the sort column, then the range column.",
        "A range predicate ends the usable portion of the index — nothing after it filters.",
        "The leftmost prefix rule means `(a, b)` cannot serve a query filtering only on `b`."
      ],
      followUps: [
        "What is a partial index and when is it better? (`WHERE status = 'active'` — much smaller, ideal for skewed data.)",
        "Why might the planner ignore your index? (Stale statistics, low selectivity, or a type mismatch preventing its use.)",
        "How do you add an index to a large production table safely? (`CREATE INDEX CONCURRENTLY`, or an online DDL tool.)"
      ],
      trap: "Adding an index per WHERE clause. Five single-column indexes usually serve you worse than one well-ordered composite, and they cost five times the write overhead."
    },
    {
      id: "db-normalization",
      q: "Explain 1NF through BCNF, and then tell me when you would deliberately denormalise.",
      topic: "DBMS",
      level: "easy",
      companies: ["tcs", "oracle", "salesforce", "flipkart", "microsoft", "amazon", "adobe"],
      tags: ["schema-design", "normalisation"],
      simple: "Normalising means storing each fact exactly once. If a fact lives in two places, the two copies will eventually disagree — that is the whole motivation. Denormalising means deliberately copying a fact to make reads faster, and accepting responsibility for keeping the copies in step.",
      answer: "## The normal forms\n\n**1NF — atomic values.** No repeating groups, no comma-separated lists in a column. A column holding `\"red,green,blue\"` cannot be indexed, filtered or joined sensibly.\n\n**2NF — 1NF, plus no partial dependency on a composite key.** If the key is `(order_id, product_id)` and you store `product_name`, that depends on `product_id` alone — half the key. It belongs in a products table.\n\n**3NF — 2NF, plus no transitive dependency.** If `employee` stores `dept_id` and `dept_name`, then `dept_name` depends on `dept_id` which depends on the key. Renaming a department would require updating every employee row.\n\n**BCNF — every determinant is a candidate key.** Stricter than 3NF, and the difference only shows up with overlapping candidate keys. Classic case: `(student, subject, teacher)` where each teacher teaches exactly one subject. `teacher -> subject` is a dependency whose left side is not a candidate key, so it is 3NF but not BCNF.\n\nIn practice, **3NF is the working target** and BCNF is the thing you should be able to define.\n\n## Why it matters — the three anomalies\n\n- **Update anomaly.** A department name stored on every employee row must be updated in a thousand places; miss one and the data contradicts itself.\n- **Insert anomaly.** You cannot record a new department until it has an employee.\n- **Delete anomaly.** Removing the last employee in a department erases the department's existence.\n\nThese three are the actual argument for normalisation. Reciting the forms without them is reciting.\n\n## When to denormalise\n\nDeliberately, with a reason, and never as a first move:\n\n**Read-heavy aggregates.** Storing `comment_count` on a post avoids a `COUNT(*)` on every page view. The cost is keeping it accurate — a trigger, or a periodic reconciliation job, and an acceptance that it may drift.\n\n**Historical accuracy.** An order line must store the price **at the time of purchase**. This looks like denormalisation and is not — the historical price is genuinely a different fact from the current price. Joining to the products table for the price is an actual bug.\n\n**Cross-service boundaries.** In a microservice architecture you cannot join across services, so a local read model duplicates the data you need.\n\n**Analytical schemas.** Star schemas denormalise dimensions on purpose. OLAP queries scan and aggregate; joins are the expensive part.\n\n## The rule\n\nNormalise until it hurts, denormalise until it works — and write down which copy is authoritative. Every denormalised field needs an owner and a reconciliation story, or it will silently drift and nobody will notice until a customer does.",
      takeaways: [
        "The forms exist to prevent update, insert and delete anomalies — lead with those, not the definitions.",
        "3NF is the practical target; know BCNF and the overlapping-candidate-key case.",
        "Storing an order's price at purchase time is not denormalisation — it is a different fact."
      ],
      followUps: [
        "Show a table in 3NF but not BCNF.",
        "How do you keep a denormalised counter accurate? (Triggers, event-driven updates, or a nightly reconciliation.)",
        "What is a star schema and why does it denormalise deliberately?"
      ],
      trap: "Treating normalisation as always correct. A senior answer names the read pattern that would justify breaking it, and says how the copies stay in step."
    },
    {
      id: "db-sql-window-functions",
      q: "Write a query returning the top 3 highest-paid employees per department.",
      topic: "DBMS",
      level: "medium",
      companies: ["amazon", "databricks", "snowflake", "microsoft", "oracle", "uber", "adobe", "flipkart", "tcs"],
      tags: ["sql", "window-functions", "ranking"],
      simple: "You need a per-group ranking, which is exactly what a window function does: it numbers rows within each group without collapsing them the way GROUP BY would. Rank inside a subquery, then filter on the rank.",
      answer: "## The window function answer\n\n```sql\nSELECT department_id, employee_id, name, salary\n  FROM (\n    SELECT department_id, employee_id, name, salary,\n           DENSE_RANK() OVER (\n             PARTITION BY department_id\n             ORDER BY salary DESC\n           ) AS rnk\n      FROM employees\n  ) ranked\n WHERE rnk <= 3\n ORDER BY department_id, salary DESC;\n```\n\nThe subquery is required: **you cannot filter on a window function in `WHERE`**, because `WHERE` is evaluated before window functions are computed. Use a subquery, a CTE, or `QUALIFY` in engines that support it (Snowflake, BigQuery, Databricks):\n\n```sql\n-- Snowflake / BigQuery / Databricks\nSELECT department_id, employee_id, name, salary\n  FROM employees\n QUALIFY DENSE_RANK() OVER (PARTITION BY department_id ORDER BY salary DESC) <= 3;\n```\n\n## Choosing the ranking function\n\nThis is the real question. Given salaries 100, 90, 90, 80:\n\n| Function | Result | Behaviour |\n| --- | --- | --- |\n| `ROW_NUMBER()` | 1, 2, 3, 4 | Always distinct; ties broken arbitrarily |\n| `RANK()` | 1, 2, 2, 4 | Ties share a rank, then it **skips** |\n| `DENSE_RANK()` | 1, 2, 2, 3 | Ties share a rank, no gap |\n\nFor \"top 3 salaries\":\n\n- `DENSE_RANK() <= 3` returns everyone earning one of the three highest salaries — which may be more than three people.\n- `RANK() <= 3` returns the top three *positions*; a three-way tie for first returns three people and no second place.\n- `ROW_NUMBER() <= 3` returns exactly three rows, choosing arbitrarily among ties — **non-deterministic** unless you add a tiebreaker to the `ORDER BY`.\n\nAsk which the business wants. \"Exactly three\" and \"the top three salary levels\" are different requirements, and choosing silently is the mistake.\n\n## The pre-window-function version\n\nWorth knowing, because interviewers ask for it and because it shows what the window function is doing:\n\n```sql\nSELECT e.*\n  FROM employees e\n WHERE (\n   SELECT COUNT(DISTINCT e2.salary)\n     FROM employees e2\n    WHERE e2.department_id = e.department_id\n      AND e2.salary > e.salary\n ) < 3;\n```\n\nCorrelated subquery, roughly $O(n^2)$, and much slower. The window-function version sorts once per partition.\n\n## Performance\n\nAn index on `(department_id, salary DESC)` lets the engine read each partition in order without a sort — often the difference between a scan-and-sort and a straight index read.",
      takeaways: [
        "Window functions cannot be filtered in `WHERE`; wrap in a subquery, a CTE, or use `QUALIFY`.",
        "`ROW_NUMBER` / `RANK` / `DENSE_RANK` answer three different questions — pick deliberately.",
        "`ROW_NUMBER` without a tiebreaker in `ORDER BY` is non-deterministic across runs."
      ],
      followUps: [
        "Compute a running total per department. (`SUM(...) OVER (PARTITION BY ... ORDER BY ...)`.)",
        "Find the salary difference between each employee and the next-highest. (`LAG` / `LEAD`.)",
        "A 7-day moving average. (`ROWS BETWEEN 6 PRECEDING AND CURRENT ROW`.)"
      ],
      trap: "Using `GROUP BY department_id` with `LIMIT 3`. `LIMIT` applies to the whole result set, not per group — you get three rows total, not three per department."
    },
    {
      id: "db-sharding",
      q: "Your single database cannot keep up. Walk me through sharding, and what gets hard.",
      topic: "DBMS",
      level: "hard",
      companies: ["amazon", "uber", "bytedance", "flipkart", "google", "salesforce", "snowflake", "databricks"],
      tags: ["sharding", "partitioning", "scaling"],
      simple: "Sharding splits your rows across several databases. The hard part is not the splitting — it is choosing what to split on, because every query that does not include that key now has to ask every shard.",
      answer: "## Exhaust the cheaper options first\n\nSharding is a large, hard-to-reverse commitment. Before it:\n\n1. **Query and index tuning.** Often a single missing index is the whole problem.\n2. **Caching.** A 90% hit rate cuts database load tenfold.\n3. **Read replicas.** Solves read scaling entirely; does nothing for writes.\n4. **Vertical scaling.** A modern machine handles far more than people assume. Buying a bigger box is usually cheaper than an engineering quarter.\n5. **Moving cold data out.** Archiving 90% of rows to cheap storage can restore performance outright.\n\nSharding is for **write throughput** or **dataset size beyond one machine**. If your problem is read throughput, replicas are the correct answer.\n\n## Choosing the shard key\n\nThe most consequential decision in the whole design, and it is very hard to change later.\n\n**Good shard keys:**\n- `tenant_id` for B2B SaaS — queries are naturally scoped to one tenant.\n- `user_id` for consumer apps — a user's data lives together.\n\n**Bad shard keys:**\n- **Timestamp.** All writes go to the newest shard while the rest sit idle. The one guaranteed hotspot.\n- **Auto-increment ID.** Same problem.\n- **Anything with heavy skew.** One enormous tenant becomes one overloaded shard.\n\nThe question to ask is: **what does my most frequent query filter on?** That column should be the shard key, so the query touches exactly one shard.\n\n## Strategies\n\n| Strategy | Pros | Cons |\n| --- | --- | --- |\n| Range | Efficient range scans; easy to reason about | Hotspots on sequential keys |\n| Hash | Even distribution | Range queries hit every shard |\n| Directory | Flexible, easy rebalancing | The lookup service is a dependency and a failure point |\n| Consistent hashing | Even, and minimal movement on resize | More complex; needs virtual nodes |\n\n## What gets hard\n\n**Cross-shard joins.** They effectively do not exist. Either denormalise so the join is unnecessary, replicate small reference tables to every shard, or join in the application — which means N queries and a merge.\n\n**Cross-shard transactions.** No single-node ACID. You need a saga with compensations, or two-phase commit and its blocking behaviour.\n\n**Fan-out queries.** Anything not filtered by the shard key hits every shard, and the latency is the **slowest** shard's, not the average. With 100 shards you are exposed to the tail of all 100 on every such query.\n\n**Unique constraints.** A database-enforced unique index on `email` cannot span shards. You need a separate lookup table or a central service that owns uniqueness.\n\n**Rebalancing.** Adding a shard means moving data while serving traffic. Plan for this before you need it — consistent hashing or a directory layer makes it possible; naive modulo makes it a full migration.\n\n**Schema migrations.** Every DDL runs N times, and you must handle partial failure. Migrations become a orchestrated operation rather than a command.\n\n## Practical advice\n\nStart with **more logical shards than physical machines** — say 1,024 logical shards across 4 servers. Growing then means moving whole logical shards to new machines with no re-hashing and no application change. This one decision saves an enormous amount of future pain.",
      takeaways: [
        "Exhaust indexing, caching, replicas and vertical scaling first — sharding is for write volume or size.",
        "The shard key should match your most frequent query's filter; never shard on a timestamp.",
        "Over-provision logical shards so growth means moving shards, not re-hashing data."
      ],
      followUps: [
        "How do you enforce a globally unique email across shards?",
        "How do you migrate from unsharded to sharded with no downtime? (Dual-write, backfill, verify, cut over.)",
        "What is a fan-out query and why is its latency the maximum rather than the mean?"
      ],
      trap: "Choosing a timestamp or auto-increment ID as the shard key. It distributes storage evenly and concentrates every write on one shard — the exact opposite of the goal."
    },
    {
      id: "db-mvcc",
      q: "How does MVCC let readers avoid blocking writers?",
      topic: "DBMS",
      level: "hard",
      companies: ["oracle", "snowflake", "databricks", "amazon", "google", "microsoft"],
      tags: ["mvcc", "concurrency", "isolation"],
      simple: "Instead of overwriting a row, the database writes a new version and leaves the old one. A reader that started earlier keeps seeing the old version, so it never has to wait for a writer to finish. The cost is that all those old versions have to be cleaned up eventually.",
      answer: "## The mechanism\n\nEvery row version carries two transaction IDs:\n\n- `xmin` — the transaction that created this version.\n- `xmax` — the transaction that deleted or superseded it (empty if still current).\n\nAn `UPDATE` is really an insert plus a logical delete: write a new version with `xmin = current_txn`, and set `xmax = current_txn` on the old one. The old version stays on disk.\n\nEach transaction takes a **snapshot** at its start: the set of transaction IDs committed at that moment. A row version is visible to it if `xmin` is in the snapshot as committed, and `xmax` is either empty or not committed as of the snapshot.\n\nSo a reader looking at a row a writer is currently changing simply reads the older version. **No lock, no wait.**\n\n## What this buys\n\n- Readers never block writers.\n- Writers never block readers.\n- Writers still block writers on the same row — two transactions cannot both update one row concurrently. That conflict is real and cannot be avoided.\n\n## What it costs: garbage\n\nDead versions accumulate. Two families of solution:\n\n**PostgreSQL** keeps versions in the main table (heap) and runs `VACUUM` to reclaim space.\n\n- **Bloat.** A table with heavy updates grows far beyond its live data.\n- **Transaction ID wraparound.** The ID counter is 32 bits. If vacuum falls too far behind, PostgreSQL refuses writes to protect itself — a genuine production outage, and one that has hit large deployments.\n- **A long-running transaction blocks all cleanup.** An idle-in-transaction session holding a snapshot open prevents vacuuming any version newer than its snapshot, across the entire database. One forgotten `BEGIN` in a psql window can bloat a database for days. This is the single most common MVCC-related production incident.\n\n**Oracle and MySQL/InnoDB** keep old versions in a separate undo/rollback segment.\n\n- The main table stays compact.\n- Reading old versions requires reconstructing them from undo, which is slower.\n- Oracle's `ORA-01555: snapshot too old` is what happens when the undo needed by a long-running query has already been recycled.\n\nDifferent designs, and each has a characteristic failure mode worth naming.\n\n## Where the index fits\n\nIn PostgreSQL, an index entry points at a **physical** row location, so every new version needs new index entries in every index — which is why updates are expensive on heavily indexed tables. **HOT (heap-only tuple) updates** avoid this when no indexed column changed and the new version fits on the same page. That is why \"do not update indexed columns unnecessarily\" is real advice, not folklore.\n\n## Practical monitoring\n\n```sql\n-- PostgreSQL: transactions open too long, the usual culprit for bloat\nSELECT pid, now() - xact_start AS duration, state, left(query, 80)\n  FROM pg_stat_activity\n WHERE xact_start IS NOT NULL\n   AND now() - xact_start > interval '5 minutes'\n ORDER BY duration DESC;\n```",
      takeaways: [
        "Row versions plus per-transaction snapshots let readers and writers proceed without blocking each other.",
        "A long-running transaction blocks cleanup database-wide — the classic bloat incident.",
        "PostgreSQL bloats the heap and needs VACUUM; Oracle/InnoDB use undo segments and can hit 'snapshot too old'."
      ],
      followUps: [
        "What is transaction ID wraparound and why does it stop writes?",
        "Why can an UPDATE be slower on a table with many indexes? (New index entries per version, unless HOT applies.)",
        "How does MVCC interact with `SELECT ... FOR UPDATE`? (It takes a real lock — MVCC does not help there.)"
      ],
      trap: "Saying MVCC removes all locking. Writer–writer conflicts on the same row still block, and explicit locking still exists — MVCC only removes the reader–writer conflict."
    },
    {
      id: "db-nplus-one",
      q: "What is the N+1 query problem, and what are the ways to fix it?",
      topic: "DBMS",
      level: "easy",
      companies: ["salesforce", "microsoft", "amazon", "flipkart", "adobe", "uber", "tcs", "oracle"],
      tags: ["orm", "performance", "joins"],
      simple: "You fetch a list of things, then loop over them fetching a detail for each. One query becomes a hundred and one. Each is fast; the round trips are what kill you.",
      answer: "## The shape of the bug\n\n```python\nposts = Post.objects.all()          # 1 query\nfor post in posts:\n    print(post.author.name)         # 1 query per post — N more\n```\n\n100 posts means 101 queries. Each may take 0.5 ms, but 101 network round trips at 1 ms each is 100 ms of pure latency, for data that one query could have returned.\n\nIt is insidious because it works perfectly in development with 10 rows and falls over in production with 10,000.\n\n## Fix 1: eager loading with a join\n\n```python\nposts = Post.objects.select_related(\"author\")   # Django: SQL JOIN\n```\n```sql\nSELECT p.*, a.* FROM posts p JOIN authors a ON p.author_id = a.id;\n```\n\nOne query. Right for **to-one** relationships.\n\n## Fix 2: eager loading with a second query\n\n```python\nposts = Post.objects.prefetch_related(\"comments\")   # Django: 2 queries\n```\n```sql\nSELECT * FROM posts;\nSELECT * FROM comments WHERE post_id IN (1, 2, 3, ...);\n```\n\nTwo queries total, stitched in application memory. Right for **to-many** relationships, because a join would multiply rows: 100 posts × 50 comments returns 5,000 rows repeating each post 50 times. The join is often *slower* than two queries once the duplication is large.\n\nKnowing which of these two to use, and why, is the actual point of the question.\n\n| Relationship | Use | Reason |\n| --- | --- | --- |\n| Many-to-one, one-to-one | Join (`select_related`) | No row multiplication |\n| One-to-many, many-to-many | Second query (`prefetch_related`) | Avoids the cartesian blow-up |\n\nORM equivalents: SQLAlchemy `joinedload` / `selectinload`; Rails `includes`; Hibernate `JOIN FETCH` / `@BatchSize`.\n\n## Fix 3: DataLoader batching\n\nIn GraphQL the query shape is decided by the client, so you cannot pre-plan the eager loading. The **DataLoader** pattern collects all the IDs requested during one tick of the event loop and issues a single batched query:\n\n```javascript\nconst authorLoader = new DataLoader(async (ids) => {\n  const rows = await db.query(\n    \"SELECT * FROM authors WHERE id = ANY($1)\", [ids]);\n  const byId = new Map(rows.map(r => [r.id, r]));\n  return ids.map(id => byId.get(id));   // must match input order exactly\n});\n```\n\nReturning results in the same order as the input IDs is required, and returning `undefined` for a missing ID rather than skipping it — otherwise the results misalign with the requests.\n\n## Catching it before production\n\n- **Assert query counts in tests.** Django's `assertNumQueries`, Rails' `bullet` gem. A test that fails when a query count changes is the only reliable defence, because the bug is invisible in code review.\n- **Log slow endpoints with their query count**, not just their duration.\n- **APM traces** make the sawtooth of 101 identical queries immediately visible.\n\n## The opposite mistake\n\nEagerly loading everything on every request fetches data nobody uses, and a join across five tables can be slower than the N+1 it replaced. Load what the endpoint actually needs — which is an argument for explicit loading over ORM defaults.",
      takeaways: [
        "Join for to-one relationships, a second batched query for to-many — the join multiplies rows.",
        "DataLoader batches per event-loop tick and must preserve input order.",
        "Assert query counts in tests; this bug is invisible at development data sizes."
      ],
      followUps: [
        "Why can a join be slower than two queries here? (Row multiplication and repeated parent columns.)",
        "How do you solve this in GraphQL where the query shape is dynamic?",
        "What is the SELECT * problem, and how does it interact with covering indexes?"
      ],
      trap: "Fixing it by joining everything everywhere. A five-table join fetching columns nobody reads trades one performance bug for another."
    },
    {
      id: "db-deadlock",
      q: "Two transactions deadlock in production. Explain how, and how you prevent it.",
      topic: "DBMS",
      level: "medium",
      companies: ["oracle", "amazon", "microsoft", "flipkart", "salesforce", "uber", "tcs", "snowflake"],
      tags: ["locking", "deadlock", "concurrency"],
      simple: "Two transactions each hold something the other needs, and both wait forever. The reliable cure is to make everybody acquire locks in the same order — then a cycle cannot form, because someone always gets there first.",
      answer: "## How it happens\n\n```sql\n-- Transaction A                    -- Transaction B\nBEGIN;                             BEGIN;\nUPDATE accounts SET ... WHERE id=1; UPDATE accounts SET ... WHERE id=2;\n-- holds lock on row 1              -- holds lock on row 2\nUPDATE accounts SET ... WHERE id=2; UPDATE accounts SET ... WHERE id=1;\n-- waits for B                      -- waits for A\n```\n\nA cycle in the wait-for graph. The database detects it, picks a victim, and aborts it with a deadlock error.\n\n## The four Coffman conditions\n\nAll four must hold simultaneously:\n\n1. **Mutual exclusion** — a resource is held exclusively.\n2. **Hold and wait** — a transaction holds one lock while requesting another.\n3. **No pre-emption** — locks are not forcibly taken away.\n4. **Circular wait** — a cycle of transactions each waiting on the next.\n\nBreaking any one prevents deadlock. In practice you break **circular wait**, because the other three are inherent to how transactions work.\n\n## Prevention, in order of usefulness\n\n**1. Consistent lock ordering.** Always acquire in the same deterministic order — ascending primary key, for instance. If both transactions above locked row 1 before row 2, one would simply wait and then proceed. This is the fix that actually works.\n\n```sql\n-- Instead of updating in arbitrary order:\nUPDATE accounts SET balance = balance + delta\n WHERE id IN (:a, :b)\n ORDER BY id;         -- deterministic order (MySQL); in PostgreSQL sort in the app\n```\n\n**2. Keep transactions short.** The lock is held from acquisition to commit. Never do network I/O, call an external API, or wait on user input inside a transaction — that turns a 2 ms lock hold into a 2 second one, and deadlock probability rises with the square of the hold time.\n\n**3. Lower the isolation level where it is safe.** Serializable takes more locks over more rows. Read Committed takes fewer.\n\n**4. Use `SELECT ... FOR UPDATE` deliberately.** Taking the lock you will need up front, in order, converts a potential deadlock into a simple wait.\n\n**5. Add the right index.** Without an index, an `UPDATE ... WHERE` may lock far more rows than it changes — in MySQL, a full scan takes locks on every row examined. Adding an index can eliminate deadlocks by reducing the lock footprint, which surprises people.\n\n## Always retry\n\nDeadlocks cannot be eliminated entirely. A victim transaction gets an error, and the application must retry it:\n\n```python\nfor attempt in range(3):\n    try:\n        with transaction.atomic():\n            do_the_work()\n        break\n    except DeadlockDetected:\n        if attempt == 2:\n            raise\n        time.sleep(0.05 * (2 ** attempt) * random.random())   # backoff + jitter\n```\n\nThe retry is not a workaround; it is part of a correct design under any optimistic concurrency scheme.\n\n## Diagnosing\n\n```sql\n-- MySQL: details of the most recent deadlock\nSHOW ENGINE INNODB STATUS;\n\n-- PostgreSQL: turn on logging, then read the log\nSET log_lock_waits = on;\n```\n\nThe log names both transactions and the exact statements involved, which almost always makes the ordering problem obvious.",
      takeaways: [
        "Break circular wait with a consistent lock ordering — the other Coffman conditions are inherent.",
        "Never hold a transaction open across network I/O; hold time drives deadlock probability.",
        "A missing index widens the lock footprint and can cause deadlocks by itself."
      ],
      followUps: [
        "How does the database choose the victim? (Usually the transaction with the least work done.)",
        "What is lock escalation and why can it cause deadlocks? (Row locks promoted to a table lock under pressure.)",
        "How would you detect deadlock in your own distributed lock service?"
      ],
      trap: "Claiming deadlocks can be designed away entirely. They can be made rare; the retry loop is mandatory regardless, and an interviewer will ask for it."
    },
    {
      id: "db-sql-vs-nosql",
      q: "SQL or NoSQL for a new service? Give me a decision procedure, not a preference.",
      topic: "DBMS",
      level: "medium",
      companies: ["amazon", "google", "microsoft", "uber", "netflix", "salesforce", "flipkart", "adobe"],
      tags: ["architecture", "data-modelling", "trade-offs"],
      simple: "Relational databases are good when your data has relationships and your queries are unpredictable. Document and key-value stores are good when access is by a known key and you need to scale writes hard. Most teams should start relational and move only when they can name what forced them.",
      answer: "## Start from the questions, not the products\n\n**1. Do you know your access patterns in advance?**\n\nIf yes, a NoSQL store can be modelled around them and will be very fast. If not — and for most new products it is not — relational is safer, because a new query is a new `SELECT` rather than a data migration. This is the single most important question.\n\n**2. Does your data have relationships you will need to traverse?**\n\nOrders belong to customers, contain line items, referencing products. In a document store you either duplicate (and manage consistency yourself) or do several round trips. Relational does this natively.\n\n**3. Do you need multi-record transactions?**\n\nMoving money, decrementing inventory while creating an order. Relational gives ACID across rows and tables. Document stores increasingly support transactions but usually with tighter scope and higher cost.\n\n**4. What is your actual write volume?**\n\nBe honest. A well-configured PostgreSQL on modern hardware handles tens of thousands of writes per second. Most services never approach that, and \"we might need to scale\" is not a reason to give up joins today.\n\n**5. Is your schema genuinely variable?**\n\nIf every record has genuinely different fields — user-defined forms, heterogeneous event payloads — a document store fits. But note that PostgreSQL's `JSONB` gives you document storage *with* indexing, joins and transactions, which often removes the need to choose at all.\n\n## The families\n\n| Type | Good at | Examples | Weak at |\n| --- | --- | --- | --- |\n| Relational | Joins, transactions, ad-hoc queries | PostgreSQL, MySQL | Horizontal write scaling |\n| Document | Flexible schema, nested data | MongoDB, DynamoDB | Cross-document joins |\n| Key-value | Extremely fast lookup by key | Redis, DynamoDB | Any query not by key |\n| Wide-column | Huge write volume, time-series | Cassandra, HBase | Ad-hoc queries |\n| Graph | Deep relationship traversal | Neo4j | Aggregate analytics |\n| Search | Full text, relevance ranking | Elasticsearch | Being a source of truth |\n\n## The default recommendation\n\n**Start with PostgreSQL.** It handles relational, JSON documents, full-text search, arrays, and geospatial data competently. One system to operate, one backup strategy, one set of transactional guarantees. Move a specific workload out when you can name the specific limit you hit.\n\nThe failure mode people regret is adopting a distributed store early, discovering their access patterns were wrong, and finding the data cannot be re-queried without a migration.\n\n## Polyglot persistence\n\nMature systems mix: PostgreSQL as the source of truth, Redis for caching and sessions, Elasticsearch for search, a time-series store for metrics. That is right — but every additional store is another thing to back up, monitor, upgrade and be paged for. Add one when a workload demands it, not in anticipation.",
      takeaways: [
        "The decisive question is whether you know your access patterns in advance.",
        "PostgreSQL with JSONB covers most 'we need flexible schema' cases without giving up joins or transactions.",
        "Every extra datastore is operational cost; adopt one when a workload forces it."
      ],
      followUps: [
        "Model a shopping cart in DynamoDB. (Single-table design, composite sort keys.)",
        "When is Cassandra genuinely the right answer? (Very high write volume, known queries, tolerance for eventual consistency.)",
        "How do you keep Elasticsearch in sync with your source of truth? (Change data capture, not dual writes.)"
      ],
      trap: "Answering \"NoSQL scales better\". Scaling is a property of the data model and the access pattern, not the product category — and a badly modelled DynamoDB table with a hot partition scales worse than a well-indexed PostgreSQL."
    },
    {
      id: "db-query-optimization",
      q: "Explain how a query optimiser decides on a join strategy.",
      topic: "DBMS",
      level: "hard",
      companies: ["snowflake", "databricks", "oracle", "google", "amazon", "microsoft"],
      tags: ["query-planning", "joins", "statistics"],
      simple: "The optimiser guesses how many rows each step will produce, using statistics it collected earlier, and then picks the cheapest plan among the ones it considers. When the guess is badly wrong, the plan is badly wrong — and that is nearly always the cause of a mysteriously slow query.",
      answer: "## The pipeline\n\n1. **Parse** into a syntax tree.\n2. **Bind** — resolve names against the catalogue.\n3. **Logical optimisation** — rule-based rewrites that are always wins: push predicates down toward the scans, prune unused columns, fold constants, unnest subqueries.\n4. **Physical planning** — cost-based. Enumerate join orders and algorithms, estimate cost for each, pick the cheapest.\n5. **Execute**, often with compiled or vectorised operators.\n\n## The three join algorithms\n\n**Nested loop.** For each row of the outer relation, look up matches in the inner.\n\n- Cost roughly $O(N \\times \\log M)$ with an index on the inner join column; $O(N \\times M)$ without.\n- Best when the outer side is small and the inner side is indexed.\n- Pathological when the outer side turns out to be large — and that is the classic bad-estimate failure.\n\n**Hash join.** Build a hash table on the smaller relation, then probe it with the larger.\n\n- $O(N + M)$, and the usual winner for large equi-joins.\n- Needs memory for the build side. If it does not fit, it spills to disk in partitions and gets much slower.\n- Equality joins only.\n\n**Sort-merge join.** Sort both sides, then walk them in step.\n\n- $O(N \\log N + M \\log M)$, or $O(N + M)$ if both inputs are already sorted — which they often are when reading in index order.\n- Handles inequality joins and produces sorted output, which may satisfy a later `ORDER BY` for free.\n\n| | Nested loop | Hash | Sort-merge |\n| --- | --- | --- | --- |\n| Best when | Small outer + indexed inner | Large equi-join | Inputs pre-sorted |\n| Memory | Minimal | Build side | Sort buffers |\n| Inequality joins | Yes | No | Yes |\n| Output sorted | No | No | Yes |\n\n## Statistics are what make or break it\n\nThe optimiser estimates cardinality from histograms, distinct-value counts and null fractions collected by `ANALYZE`. Errors compound: get the first join's row count wrong by 10× and every estimate above it is wrong too.\n\nCommon causes of bad estimates:\n\n- **Stale statistics** after a bulk load. Run `ANALYZE`.\n- **Correlated columns.** The optimiser assumes independence, so `WHERE city = 'Paris' AND country = 'France'` is estimated as the product of two selectivities — badly under-estimating, since the columns are almost perfectly correlated. PostgreSQL's `CREATE STATISTICS` declares the correlation explicitly.\n- **Skew.** A histogram summarises; one value holding 40% of the rows may be smoothed over.\n- **Opaque expressions.** `WHERE lower(email) = ?` cannot use column statistics on `email`. An expression index fixes both the estimate and the access path.\n\n## Reading a plan\n\nIn `EXPLAIN ANALYZE`, compare **estimated versus actual rows** at each node. A node showing `rows=10` and `actual rows=100000` is the root cause — everything above it was planned for the wrong data size.\n\n```\nNested Loop  (cost=0.29..8.32 rows=1 width=64)\n             (actual time=0.02..4210.11 rows=98432 loops=1)\n```\n\nEstimated 1 row, got 98,432. The optimiser chose a nested loop because it expected one iteration and got a hundred thousand. Fix the estimate, not the plan.\n\n## Hints, and why they are a last resort\n\nOracle and MySQL support hints; PostgreSQL deliberately does not. A hint freezes today's plan against tomorrow's data. Prefer fixing statistics, adding an index, or rewriting the query so the estimate is achievable.",
      takeaways: [
        "Hash join for large equi-joins, nested loop for small outer with an indexed inner, sort-merge when inputs are already ordered.",
        "Compare estimated to actual rows in the plan — the first large divergence is the root cause.",
        "Column correlation breaks the independence assumption; declare it or add an expression index."
      ],
      followUps: [
        "Why does PostgreSQL refuse to support join hints?",
        "What does a hash join spilling to disk look like in a plan, and how do you fix it?",
        "How do columnar engines change these trade-offs? (Vectorised execution and late materialisation change the constants substantially.)"
      ],
      trap: "Blaming the optimiser. It is almost always doing the right thing with wrong information — find the bad cardinality estimate before you reach for a hint."
    }
  ]);
})(window.TD = window.TD || {});
