/* Interview bank — object-oriented design and code quality.

   This is the round where machine-coding companies (Flipkart, Salesforce) do
   most of their filtering, and where service companies test whether you can
   define a term rather than gesture at it. Both are covered. */
(function (TD) {
  "use strict";

  TD.addQuestions([
    {
      id: "oop-four-pillars",
      q: "Explain the four pillars of OOP with an example of each that is not a shape or an animal.",
      topic: "OOP & Code Quality",
      level: "easy",
      companies: "*",
      tags: ["oop", "fundamentals"],
      simple: "Encapsulation is hiding the messy internals behind a clean control panel. Abstraction is deciding what belongs on the panel. Inheritance is a specialised version of an existing thing. Polymorphism is many different things responding to the same instruction in their own way.",
      answer: "## Encapsulation\n\nBundle data with the code that operates on it, and hide the internal state so it can only change through methods that keep it valid.\n\n```python\nclass BankAccount:\n    def __init__(self, balance=0):\n        self._balance = balance          # not part of the public surface\n\n    @property\n    def balance(self):\n        return self._balance\n\n    def withdraw(self, amount):\n        if amount <= 0:\n            raise ValueError(\"amount must be positive\")\n        if amount > self._balance:\n            raise InsufficientFunds(self._balance, amount)\n        self._balance -= amount\n```\n\nThe point is the **invariant**: the balance can never go negative, because the only path to changing it enforces that. A public field cannot make that promise.\n\n## Abstraction\n\nExpose *what* something does and hide *how*. A `PaymentProcessor` interface with `charge(amount, method)` lets callers work without knowing whether it is Stripe, PayPal, or a test double.\n\nEncapsulation hides *state*; abstraction hides *implementation*. They travel together and are not the same thing, and being able to separate them is the small precision this question is testing.\n\n## Inheritance\n\nA subclass reuses and specialises a base class. Its real justification is being a **subtype** — usable anywhere the base is expected — not code reuse. For reuse, composition is safer.\n\n```python\nclass HttpError(Exception):\n    def __init__(self, status, message):\n        self.status = status\n        super().__init__(message)\n\nclass NotFound(HttpError):\n    def __init__(self, resource):\n        super().__init__(404, f\"{resource} not found\")\n```\n\nException hierarchies are the case where inheritance is unambiguously right: `except HttpError` genuinely should catch `NotFound`.\n\n## Polymorphism\n\nOne interface, many implementations, resolved at runtime.\n\n```python\nclass Notifier:\n    def send(self, user, message): raise NotImplementedError\n\nclass EmailNotifier(Notifier):\n    def send(self, user, message): smtp.send(user.email, message)\n\nclass SmsNotifier(Notifier):\n    def send(self, user, message): twilio.send(user.phone, message)\n\ndef notify_all(notifiers, user, message):\n    for n in notifiers:               # does not know or care which is which\n        n.send(user, message)\n```\n\nThe payoff is that `notify_all` never changes when you add a channel. That is the test of whether polymorphism is earning its keep.\n\n**Overriding is runtime polymorphism** (which method runs depends on the actual object). **Overloading is compile-time** (which method runs depends on the declared argument types) — and Python does not have it, which is itself worth knowing.\n\n## What a good answer adds\n\nSay when *not* to use inheritance. A deep hierarchy is one of the most common sources of unmaintainable code, and \"favour composition over inheritance\" — with a reason — turns a recall answer into a judgement answer.",
      takeaways: [
        "Encapsulation exists to protect an invariant, not merely to make fields private.",
        "Inheritance is justified by subtyping, not by code reuse — use composition for reuse.",
        "Overriding is runtime dispatch; overloading is compile-time and absent from Python."
      ],
      followUps: [
        "Why favour composition over inheritance? (Fragile base class, and inability to change behaviour at runtime.)",
        "What is duck typing, and how does it change the picture in Python?",
        "What are abstract base classes for if you already have duck typing?"
      ],
      trap: "Reciting definitions with shapes and animals. Every candidate does; a concrete example from real code is what makes the answer land."
    },
    {
      id: "oop-solid",
      q: "Explain SOLID. Which principle do you see violated most often, and what does it cost?",
      topic: "OOP & Code Quality",
      level: "medium",
      companies: ["salesforce", "microsoft", "amazon", "flipkart", "oracle", "adobe", "tcs", "palantir"],
      tags: ["solid", "design", "architecture"],
      simple: "Five rules for keeping code changeable: each class does one job, you extend rather than edit, a subclass must be usable wherever its parent is, interfaces should be small, and code should depend on abstractions rather than concrete details.",
      answer: "## S — Single Responsibility\n\nA class should have **one reason to change**. The emphasis is on *reason*, not on doing one thing: a class touching the database, formatting HTML and sending email changes when the schema changes, when the design changes, and when the email provider changes. Three separate teams can force you to edit it.\n\n## O — Open/Closed\n\nOpen for extension, closed for modification. Adding a payment method should mean adding a class, not editing a growing `if/elif` chain that every other payment path also runs through.\n\n```python\n# Closed to modification: the registry is extended, not edited\nPROCESSORS = {}\n\ndef register(name):\n    def wrap(cls):\n        PROCESSORS[name] = cls\n        return cls\n    return wrap\n\n@register(\"stripe\")\nclass StripeProcessor(PaymentProcessor): ...\n```\n\n## L — Liskov Substitution\n\nA subtype must be usable anywhere its base type is, without the caller knowing. The classic violation:\n\n```python\nclass Rectangle:\n    def set_width(self, w): self.w = w\n    def set_height(self, h): self.h = h\n\nclass Square(Rectangle):          # a square IS a rectangle, mathematically\n    def set_width(self, w): self.w = self.h = w    # but not behaviourally\n    def set_height(self, h): self.w = self.h = h\n\ndef stretch(r: Rectangle):\n    r.set_width(5)\n    r.set_height(4)\n    assert r.w * r.h == 20        # holds for Rectangle, fails for Square\n```\n\nThe lesson is that \"is-a\" in English is not \"is-a\" in a type system. Behaviour is what must be substitutable, not vocabulary.\n\n## I — Interface Segregation\n\nMany small interfaces beat one large one. A `Worker` interface requiring `work()`, `eat()` and `sleep()` forces a robot implementation to throw on two of them. Split it.\n\n## D — Dependency Inversion\n\nDepend on abstractions, not concretions. High-level policy should not import low-level detail.\n\n```python\n# Depends on a concrete class: untestable without a real database\nclass OrderService:\n    def __init__(self):\n        self.db = PostgresClient(\"host=prod\")\n\n# Depends on an abstraction: a fake is trivial\nclass OrderService:\n    def __init__(self, repository: OrderRepository):\n        self.repo = repository\n```\n\n## The one violated most often\n\n**Single Responsibility**, and it is not close. It happens by accretion rather than by decision — a class starts focused, and each small addition is individually reasonable. Nobody ever chooses to write a 3,000-line class.\n\nThe costs are concrete:\n\n- **Merge conflicts.** Everyone edits the same file.\n- **Untestable.** Testing one behaviour means constructing every dependency of all the others.\n- **Fear of change.** Nobody knows what else touches it, so changes get bolted on rather than made properly, which makes it worse.\n\nThe reliable early symptom is the test setup: when a unit test needs six mocks, the class has six responsibilities.\n\n## The honest caveat\n\nSOLID is a set of heuristics, not laws. Splitting a 40-line class into six interfaces because a principle says so produces indirection nobody can navigate. Apply them when the pain they prevent is real — and being willing to say that is a stronger answer than reciting all five perfectly.",
      takeaways: [
        "Single Responsibility means one reason to change, and it is the one violated most.",
        "Liskov is about behavioural substitutability — square/rectangle is the canonical failure of English \"is-a\".",
        "A test needing six mocks is telling you the class has six responsibilities."
      ],
      followUps: [
        "How does dependency injection support the D in SOLID, and when is a framework overkill?",
        "Give a case where following SOLID strictly makes the code worse.",
        "How do these translate to a language without classes, like Go or Rust?"
      ],
      trap: "Listing all five and stopping. The question asks which is violated most and what it costs — the second half is where the judgement shows."
    },
    {
      id: "oop-machine-coding",
      q: "Machine coding round: design a parking lot. 90 minutes, working code, no framework.",
      topic: "OOP & Code Quality",
      level: "hard",
      companies: ["flipkart", "salesforce", "uber", "amazon", "microsoft", "adobe", "bytedance"],
      tags: ["machine-coding", "design", "lld"],
      simple: "The grade is: does it run, is it extensible, and can you explain your class boundaries. Spend ten minutes clarifying requirements, write the core flow first, and never let time run out with half-finished cleverness.",
      answer: "## How the round is actually graded\n\nIn priority order:\n\n1. **It runs and demonstrates the flows.** An elegant unfinished design scores below a working plain one.\n2. **Clear separation of concerns.** Can they see where a new feature would go?\n3. **Extensibility at the points you were told matter.** New vehicle type, new pricing rule.\n4. **No hardcoding.** Rates in a strategy or config, not scattered through the code.\n5. **A few meaningful tests** — the happy path and one edge case.\n\nUnimportant: persistence, HTTP, a UI, or a real database. In-memory is expected. Say so explicitly at the start so nobody assumes you forgot.\n\n## First ten minutes: clarify\n\nAsk, and write down the answers:\n\n- Multiple floors? Multiple entry and exit gates?\n- Vehicle types, and can a large vehicle take a small spot or vice versa?\n- Pricing — hourly, slab-based, different per vehicle type?\n- Payment — cash and card, or just record it?\n- Do we need to find the *nearest* free spot, or any free spot?\n\nAsking these is scored. Assuming silently is scored too, in the wrong direction.\n\n## The model\n\n```python\nfrom abc import ABC, abstractmethod\nfrom dataclasses import dataclass, field\nfrom datetime import datetime\nfrom enum import Enum\nimport itertools\n\nclass VehicleType(Enum):\n    MOTORCYCLE = 1\n    CAR = 2\n    TRUCK = 3\n\n@dataclass(frozen=True)\nclass Vehicle:\n    plate: str\n    type: VehicleType\n\n@dataclass\nclass Spot:\n    id: str\n    floor: int\n    type: VehicleType\n    occupied_by: str | None = None      # plate, or None\n\n    @property\n    def is_free(self):\n        return self.occupied_by is None\n\n@dataclass\nclass Ticket:\n    id: str\n    vehicle: Vehicle\n    spot: Spot\n    entry_at: datetime\n    exit_at: datetime | None = None\n```\n\n## Strategies — where extensibility lives\n\nThe two things that change are **which spot you pick** and **how much you charge**. Make both pluggable and you have satisfied the extensibility criterion visibly.\n\n```python\nclass SpotAllocationStrategy(ABC):\n    @abstractmethod\n    def allocate(self, spots, vehicle): ...\n\nclass NearestFirst(SpotAllocationStrategy):\n    def allocate(self, spots, vehicle):\n        candidates = [s for s in spots\n                      if s.is_free and s.type == vehicle.type]\n        return min(candidates, key=lambda s: (s.floor, s.id), default=None)\n\nclass PricingStrategy(ABC):\n    @abstractmethod\n    def price(self, ticket, exit_at): ...\n\nclass HourlyPricing(PricingStrategy):\n    RATES = {VehicleType.MOTORCYCLE: 10,\n             VehicleType.CAR: 20,\n             VehicleType.TRUCK: 40}\n\n    def price(self, ticket, exit_at):\n        hours = (exit_at - ticket.entry_at).total_seconds() / 3600\n        started_hours = max(1, -(-hours // 1))       # ceiling, minimum 1\n        return started_hours * self.RATES[ticket.vehicle.type]\n```\n\n## The service\n\n```python\nclass ParkingLot:\n    def __init__(self, spots, allocator, pricing):\n        self.spots = spots\n        self.allocator = allocator\n        self.pricing = pricing\n        self.active: dict[str, Ticket] = {}          # plate -> ticket\n        self._ids = itertools.count(1)\n\n    def park(self, vehicle):\n        if vehicle.plate in self.active:\n            raise AlreadyParked(vehicle.plate)\n        spot = self.allocator.allocate(self.spots, vehicle)\n        if spot is None:\n            raise LotFull(vehicle.type)\n        spot.occupied_by = vehicle.plate\n        ticket = Ticket(f\"T{next(self._ids)}\", vehicle, spot, datetime.now())\n        self.active[vehicle.plate] = ticket\n        return ticket\n\n    def unpark(self, plate, now=None):\n        ticket = self.active.pop(plate, None)\n        if ticket is None:\n            raise NotParked(plate)\n        now = now or datetime.now()\n        ticket.exit_at = now\n        ticket.spot.occupied_by = None               # free it before returning\n        return self.pricing.price(ticket, now)\n```\n\n## Edge cases to handle out loud\n\n- Lot full for this vehicle type but not others.\n- Exit with an unknown or already-exited ticket.\n- Zero-duration stay — charge a minimum, do not charge nothing.\n- Two entry gates allocating the same spot concurrently. Say that a lock or an atomic claim is needed, and whether you implemented it.\n\n## Time management\n\nA workable split of 90 minutes: 10 clarifying, 15 modelling, 40 implementing the core flows, 15 tests, 10 buffer. **Never** spend 60 minutes on a beautiful abstraction and demo nothing. If time is short, cut features, not correctness.",
      takeaways: [
        "Working code beats elegant unfinished code — that is the grading order.",
        "Put the variation points behind strategies: spot allocation and pricing.",
        "Ask clarifying questions out loud; it is scored, and assuming is scored against you."
      ],
      followUps: [
        "How would you add monthly pass holders with reserved spots?",
        "How do two entry gates avoid allocating the same spot? (An atomic claim, or a per-floor lock.)",
        "How would you persist this without changing the domain model? (A repository interface.)"
      ],
      trap: "Building a database layer or an HTTP API. Neither is graded, both consume the time you needed for the flows, and finishing with nothing runnable is the most common way this round is failed."
    },
    {
      id: "oop-design-patterns",
      q: "Which design patterns do you actually use, and which are overused?",
      topic: "OOP & Code Quality",
      level: "medium",
      companies: ["salesforce", "microsoft", "adobe", "oracle", "flipkart", "amazon", "tcs"],
      tags: ["patterns", "design"],
      simple: "Patterns are names for solutions people kept re-deriving. Knowing the names helps you communicate. Reaching for them before you have the problem produces code with five layers of indirection and nothing behind them.",
      answer: "## The ones that earn their keep\n\n**Strategy.** Swap an algorithm at runtime. Pricing rules, retry policies, compression choices. The most consistently useful pattern in the catalogue, and the one that most often removes a growing `if/elif` chain.\n\n**Factory.** Centralise construction when the concrete type depends on input or configuration. It keeps `if type == \"a\"` in one place instead of scattered across the codebase.\n\n**Observer.** Publish–subscribe. UI events, domain events, cache invalidation. Note the failure mode: with many observers, tracing why something happened becomes hard, and an exception in one observer can silently break the rest unless you handle it.\n\n**Decorator.** Wrap to add behaviour without changing the wrapped thing. Middleware chains, caching wrappers, instrumentation. Composes cleanly, which is why it survives.\n\n**Adapter.** Make an incompatible interface fit. Unglamorous and constantly needed at the boundary of any third-party library.\n\n**Builder.** Construct an object with many optional parameters. Especially valuable in languages without keyword arguments — in Python, keyword arguments already are the builder, which is worth saying.\n\n**Repository.** Abstract data access behind a collection-like interface. Makes the domain testable without a database.\n\n## The overused ones\n\n**Singleton.** Global mutable state wearing a costume. It makes testing hard (you cannot get a clean instance per test), hides dependencies (nothing in a signature says the function reaches for it), and creates thread-safety problems. What you usually want is **one instance managed by your dependency injection container** — the same object everywhere, but passed in explicitly and replaceable in tests.\n\n**Abstract Factory.** A factory that makes factories. Occasionally right — cross-platform UI toolkits — and usually a sign that a simple function would do.\n\n**Visitor.** Solves the expression problem for stable type hierarchies, at the cost of code nobody can follow. Pattern matching in modern languages replaces it more legibly.\n\n**Deep inheritance hierarchies.** Not a pattern, but the most common structural mistake. Four levels of inheritance and the behaviour lives in none of the classes you are reading.\n\n## What good judgement sounds like\n\nA senior answer is not a longer list. It is:\n\n> \"I use Strategy and Decorator constantly because they remove conditionals and compose. I avoid Singleton — I want one instance, but injected rather than globally reachable. And I try not to introduce a pattern until the second or third time the same shape appears, because a premature abstraction is harder to remove than a duplication.\"\n\nThat last clause is the point. The rule of three exists because you cannot see the right abstraction from one example, and a wrong abstraction is far more expensive than duplicated code.\n\n## Patterns that faded\n\nSome exist to work around language limitations that no longer apply:\n\n- **Command** is often just a closure.\n- **Iterator** is built into every modern language.\n- **Template Method** is frequently better as a higher-order function.\n\nBeing able to say \"that pattern is a language feature now\" is a good signal.",
      takeaways: [
        "Strategy and Decorator earn their keep constantly; Singleton is global state in disguise.",
        "Wait for the third occurrence before abstracting — a wrong abstraction costs more than duplication.",
        "Several classic patterns are now language features; naming which shows real familiarity."
      ],
      followUps: [
        "Why is Singleton hard to test, and what do you use instead?",
        "When is duplication better than abstraction?",
        "Which patterns does a functional language make unnecessary?"
      ],
      trap: "Listing patterns you have never used. The follow-up is always \"where did you use that, and what went wrong\", and the answer is very obvious when there is none."
    },
    {
      id: "oop-testing-strategy",
      q: "What does your test suite look like, and what do you not test?",
      topic: "OOP & Code Quality",
      level: "medium",
      companies: ["microsoft", "salesforce", "amazon", "anthropic", "netflix", "adobe", "palantir", "flipkart"],
      tags: ["testing", "quality"],
      simple: "Lots of fast tests on your own logic, some tests that check the pieces fit together, and a few that drive the whole system the way a user would. Do not test the framework, and do not test that your mocks return what you told them to return.",
      answer: "## The shape\n\nThe pyramid is a heuristic about **cost and speed**, not a rule about counts:\n\n- **Unit tests** — many, milliseconds each, no I/O. Test your logic.\n- **Integration tests** — fewer, seconds each, real database or real HTTP against a container. Test that the pieces fit.\n- **End-to-end tests** — few, minutes, and flaky. Test the handful of flows that must never break.\n\nThe honest version for a service-heavy system is closer to a **trophy**: fewer pure unit tests, more integration tests, because most bugs live at the boundaries — serialisation, SQL, HTTP contracts — and a mocked unit test cannot see any of them.\n\n## What to test\n\n- **Business logic and its edge cases.** Boundaries, empty inputs, and the maximum.\n- **Every bug you fix.** A regression test is the cheapest test to justify, because you already know it can fail.\n- **Contracts between services**, with consumer-driven contract tests.\n- **Anything where being wrong is expensive**: money, permissions, data deletion.\n\n## What not to test\n\n- **The framework.** Do not test that your ORM saves a row. It has its own tests.\n- **Getters and setters.** Coverage theatre.\n- **That a mock returns what you configured it to return.** This is the most common worthless test — it asserts your own test setup.\n- **Implementation details.** A test asserting that a private method was called breaks on every refactor while catching nothing. Test behaviour through the public surface.\n\nThat last one is the difference between a suite that enables refactoring and one that prevents it.\n\n## Mocking\n\nMock at the **boundary of your system** — the payment gateway, the email provider, the clock. Do not mock your own internal classes; that couples the test to the structure and it will break the moment you rearrange anything.\n\nA test needing six mocks is not telling you about testing. It is telling you the class under test has six responsibilities.\n\n## The clock is a dependency\n\n```python\n# Untestable: depends on when it runs\ndef is_expired(token):\n    return token.expires_at < datetime.now()\n\n# Testable: time is an argument\ndef is_expired(token, now):\n    return token.expires_at < now\n```\n\nThis one change removes an entire class of flaky, time-dependent tests. The same reasoning applies to randomness — inject the seed or the generator.\n\n## Coverage\n\nUseful as a **signal**, dangerous as a **target**. 100% coverage with no assertions proves nothing. A file at 20% coverage is worth a look; a team chasing 95% will write tests for getters and stop there.\n\nBranch coverage is more informative than line coverage. Mutation testing (deliberately breaking the code and checking whether tests notice) measures what coverage claims to.\n\n## Flaky tests\n\nA flaky test is worse than no test, because it trains the team to ignore failures. Quarantine it immediately, then fix or delete it. The usual causes are shared state between tests, real time, real network, and test-order dependence — running the suite in random order surfaces the last one.",
      takeaways: [
        "Test behaviour through the public surface; tests asserting on internals block refactoring.",
        "Mock at your system's boundary only — six mocks means six responsibilities.",
        "Inject the clock and the random source; it removes an entire family of flaky tests."
      ],
      followUps: [
        "What is a consumer-driven contract test and what does it replace?",
        "How do you test something that hits a third-party API? (Record and replay, or a contract test against a stub.)",
        "What is mutation testing and why is it a better metric than coverage?"
      ],
      trap: "Claiming a coverage percentage as a quality measure. The immediate follow-up is what that number excludes, and \"we have 90% coverage\" alone is not an answer to it."
    },
    {
      id: "oop-code-review",
      q: "What do you look for in a code review, and how do you give feedback someone will act on?",
      topic: "OOP & Code Quality",
      level: "medium",
      companies: ["microsoft", "salesforce", "amazon", "google", "netflix", "adobe", "anthropic"],
      tags: ["review", "collaboration"],
      simple: "Read for correctness first, then for whether the next person will understand it. Comment on things that matter, say why, and be clear about which comments are blocking and which are just opinions.",
      answer: "## What to look for, in order\n\n**1. Correctness.** Does it do what the description says? Are the edge cases handled — empty, null, maximum, concurrent?\n\n**2. Security.** Injection, authorisation checks (is the *object-level* check there, not just \"is logged in\"), secrets in code, unvalidated input crossing a trust boundary.\n\n**3. Failure behaviour.** What happens when the dependency times out? Is the error swallowed? Does a retry duplicate work?\n\n**4. Tests.** Do they exist, and do they test behaviour rather than implementation? Would they fail if the code were wrong?\n\n**5. Readability.** Will someone understand this in six months with no context? Are the names accurate?\n\n**6. Design.** Does it fit the existing structure, or fight it?\n\n**7. Performance.** Only where it matters — an N+1 query, an unbounded fetch, a lock held across I/O. Not micro-optimisation.\n\nStyle is last and should mostly be automated. If people are commenting on formatting, install a formatter and stop having the conversation.\n\n## How to give feedback\n\n**Say why.** \"Rename this\" is an instruction. \"This is called `data` but it holds validated orders — `validated_orders` would save the next reader a trip through the function\" is a reason someone can agree or disagree with.\n\n**Label the severity.** Adopt a prefix convention:\n\n- `blocking:` must change before merge.\n- `suggestion:` I would do it differently; your call.\n- `nit:` trivial, feel free to ignore.\n- `question:` I genuinely do not understand this.\n\nWithout labels, every comment reads as a demand, and reviews turn adversarial over things the reviewer did not actually care about.\n\n**Ask rather than assert when you are unsure.** \"What happens if `items` is empty here?\" is better than \"this breaks on empty input\" — because sometimes it does not, and you have not spent credibility being wrong.\n\n**Praise specifically.** \"This error handling is much clearer than what it replaced\" costs nothing and makes the next critical comment land better.\n\n## Reviewing well as a habit\n\n- **Respond fast.** A review sitting for two days blocks someone. Within a few hours, ideally.\n- **Small PRs get real reviews.** Beyond about 400 lines, review quality falls sharply — reviewers start skimming. Ask for a split rather than rubber-stamping.\n- **Pull the branch** for anything non-trivial. Reading a diff misses things that running it does not.\n- **Do not review while irritated.** It shows, and it costs more to repair than to wait an hour.\n\n## Receiving feedback\n\nAssume good intent, ask for clarification rather than defending, and if a reviewer misread the code that is usually a signal the code is unclear — even when their comment is technically wrong. Fixing the clarity is often the right response to a wrong comment.\n\n## What an interviewer is really asking\n\nWhether you can improve a colleague's work without making them defensive. Bring a concrete example: a review where you disagreed, how it resolved, and — ideally — one where you turned out to be wrong.",
      takeaways: [
        "Correctness, security and failure behaviour before style; automate formatting entirely.",
        "Label comments as blocking, suggestion or nit — unlabelled comments all read as demands.",
        "Above ~400 lines review quality collapses; ask for a split rather than skimming."
      ],
      followUps: [
        "How do you handle a reviewer blocking on a stylistic preference?",
        "How do you review code in a language or area you do not know well?",
        "What do you do when a review reveals the whole approach is wrong?"
      ],
      trap: "Describing only what you look for. The question is half about communication, and an answer with no view on how to deliver feedback misses the part they are actually assessing."
    },
    {
      id: "oop-refactor-legacy",
      q: "You inherit a 4,000-line class with no tests. A bug needs fixing. What do you do?",
      topic: "OOP & Code Quality",
      level: "hard",
      companies: ["microsoft", "salesforce", "oracle", "amazon", "adobe", "palantir", "tcs"],
      tags: ["legacy", "refactoring", "testing"],
      simple: "Do not start by cleaning it up. Get some kind of safety net first — even a crude one that just records what the code currently does — then make the smallest change that fixes the bug, and improve only the part you had to touch.",
      answer: "## Do not rewrite\n\nThe instinct is to rewrite. Resist it. That code contains years of accumulated fixes for cases nobody documented, and a rewrite loses all of them. Rewrites of systems in production have a poor track record for exactly this reason.\n\n## Step 1: reproduce the bug\n\nWrite a failing test that demonstrates it. If you cannot get the class under test at all, reproduce it manually and write down the exact steps. **You cannot claim a fix you cannot demonstrate.**\n\n## Step 2: characterisation tests\n\nThese pin down what the code *currently does*, correct or not. You are not asserting correctness — you are creating a tripwire.\n\n```python\ndef test_characterise_calculate_discount():\n    # Not a specification. A record of current behaviour, so a refactor\n    # that changes it fails loudly instead of silently.\n    assert legacy.calculate_discount(100, \"GOLD\", 5) == 87.5\n    assert legacy.calculate_discount(0, \"GOLD\", 5) == 0\n    assert legacy.calculate_discount(100, \"UNKNOWN\", 0) == 100\n```\n\nWhere behaviour is genuinely obscure, run the code and record what it returns. Michael Feathers' phrase for a legacy codebase is \"code without tests\", and this is his technique for getting out of it.\n\n## Step 3: break the dependencies\n\nThe usual reason you cannot test it is that the constructor opens a database connection or reads a config file. Two low-risk techniques:\n\n**Extract and override.** Pull the awkward call into its own method, then subclass in the test and override it.\n\n```python\nclass OrderProcessor:\n    def process(self, order):\n        rate = self._fetch_tax_rate(order.region)     # extracted\n        return order.total * (1 + rate)\n\n    def _fetch_tax_rate(self, region):                # the hard bit, isolated\n        return requests.get(f\"{TAX_API}/{region}\").json()[\"rate\"]\n\nclass TestableOrderProcessor(OrderProcessor):\n    def _fetch_tax_rate(self, region):\n        return 0.2\n```\n\n**Parameterise the constructor.** Add an optional dependency argument defaulting to the current behaviour, so no caller changes.\n\nBoth are deliberately conservative: they change structure without changing behaviour, which is what you want before you have tests.\n\n## Step 4: fix the bug, minimally\n\nSmallest possible change. Do not bundle the fix with refactoring — if something breaks, you will not know which caused it. Ship the fix on its own.\n\n## Step 5: improve what you touched\n\nOnly now, and only the region you were already in. Extract the method you had to understand. Give the confusing variable an accurate name. Commit separately from the fix.\n\nThis is the **boy scout rule**: leave it slightly better. Over a year of touching it, a class genuinely improves — without anyone ever scheduling a risky rewrite.\n\n## When a rewrite is justified\n\nRarely, and only when: the requirements have genuinely changed, the technology is unsupported, you have the tests to prove equivalence, and you can migrate incrementally with a **strangler fig** — routing one capability at a time to the new code with the old still running as a fallback.\n\nA big-bang rewrite with a cutover date is the pattern that fails.\n\n## What the interviewer is checking\n\nWhether you are safe around production code. The rewrite answer is exciting and reads as a risk. The characterisation-test answer reads as someone who has been on call for their own changes.",
      takeaways: [
        "Characterisation tests record current behaviour so a refactor fails loudly rather than silently.",
        "Fix and refactor in separate commits, or you cannot tell which broke something.",
        "Rewrites lose the undocumented fixes that are the reason the code is 4,000 lines."
      ],
      followUps: [
        "What is a seam, and how do you find one in untestable code?",
        "How would you split this class safely over several months?",
        "When would you argue for a rewrite, and how would you de-risk it?"
      ],
      trap: "Leading with \"I'd rewrite it properly\". It is the answer that sounds most confident and reads as the least experienced."
    },
    {
      id: "oop-api-design",
      q: "Design a REST API for a resource. What makes an API good, and how do you version it?",
      topic: "OOP & Code Quality",
      level: "medium",
      companies: ["salesforce", "microsoft", "amazon", "adobe", "flipkart", "uber", "google"],
      tags: ["api", "rest", "versioning"],
      simple: "Nouns for resources, verbs from HTTP, status codes that mean what they say. The hard parts are pagination, errors, and changing the thing later without breaking everyone using it.",
      answer: "## The basics done right\n\n```\nGET    /orders              list, filtered and paginated\nPOST   /orders              create\nGET    /orders/{id}         read one\nPATCH  /orders/{id}         partial update\nDELETE /orders/{id}         delete\nGET    /orders/{id}/items   sub-resource\n```\n\nPlural nouns, no verbs in paths. `POST /orders/{id}/cancel` is a reasonable exception for an action that is not a CRUD operation — dogmatic REST purity here helps nobody.\n\n## Status codes that mean something\n\n| Code | Use |\n| --- | --- |\n| 200 | Success with a body |\n| 201 | Created — include a `Location` header |\n| 204 | Success with no body |\n| 400 | Malformed request |\n| 401 | Not authenticated |\n| 403 | Authenticated but not permitted |\n| 404 | Not found — or hiding a 403 deliberately |\n| 409 | Conflict — a version clash, or a duplicate |\n| 422 | Well-formed but semantically invalid |\n| 429 | Rate limited — always with `Retry-After` |\n| 503 | Temporarily unavailable — with `Retry-After` |\n\nThe 401/403 distinction gets confused constantly: 401 means \"I do not know who you are\", 403 means \"I know, and no\".\n\n## Pagination\n\n**Offset pagination** (`?page=3&size=20`) is simple and breaks in two ways: it gets slower the deeper you go (`OFFSET 100000` still scans those rows), and inserting a row shifts everything, so a user paging through sees duplicates or skips.\n\n**Cursor pagination** (`?after=eyJpZCI6MTIzfQ&limit=20`) encodes the position of the last item. Constant time regardless of depth, and stable under insertion.\n\n```json\n{\n  \"data\": [ ... ],\n  \"next_cursor\": \"eyJpZCI6MTQzfQ\",\n  \"has_more\": true\n}\n```\n\nUse cursors for anything that grows. Offset only where users genuinely need to jump to page 47.\n\n## Errors\n\nBe consistent and machine-readable. RFC 9457 (`application/problem+json`) exists so you do not have to invent one:\n\n```json\n{\n  \"type\": \"https://api.example.com/errors/insufficient-funds\",\n  \"title\": \"Insufficient funds\",\n  \"status\": 402,\n  \"detail\": \"Account balance 45.00 is below the required 99.99\",\n  \"instance\": \"/payments/8f14e45f\",\n  \"trace_id\": \"c8d4a2b1\"\n}\n```\n\nA stable `type` so clients can branch on it, human-readable `detail` for the developer, and a `trace_id` so a support ticket can be traced to a log line. That last field saves more time than everything else on this page.\n\n## Versioning\n\n| Approach | Pro | Con |\n| --- | --- | --- |\n| URL path (`/v1/orders`) | Obvious, cacheable, easy to route | Version leaks into every URL |\n| Header (`Accept: application/vnd.api.v2+json`) | Clean URLs | Harder to test, easy to cache wrong |\n| Query (`?version=2`) | Simple | Easy to omit accidentally |\n\n**URL path versioning is the pragmatic default.** It is visible, debuggable in a browser, and simple to route.\n\n**The better goal is not to version at all.** Most changes can be additive:\n\n- Adding a field is safe if clients ignore unknown fields — say so in your documentation, from day one.\n- Adding an optional parameter is safe.\n- Removing or renaming a field, or changing a type, is breaking.\n\nUse **expand and contract**: add the new field, support both, migrate clients, then remove the old one after a deprecation window. A new major version should be a last resort, because every version is one you maintain indefinitely.\n\n## What people forget\n\n- **Idempotency keys** on POST, so retries are safe.\n- **Rate limit headers** on every response, not just on 429.\n- **`Retry-After`** on 429 and 503.\n- **ETags and conditional requests** for both caching and optimistic concurrency.\n- **Consistent field naming and timestamp format.** ISO 8601 with a timezone, everywhere, no exceptions."
      ,
      takeaways: [
        "Cursor pagination is stable and constant-time; offset degrades and skips rows under insertion.",
        "Return a machine-readable error `type` and a `trace_id` — it is the highest-value field in the response.",
        "Prefer additive change and expand–contract over cutting a new version."
      ],
      followUps: [
        "How do you deprecate a field without breaking clients? (Announce, `Deprecation` header, monitor usage, then remove.)",
        "REST or GraphQL for this? (GraphQL for varied client needs; REST for cacheable, simple resources.)",
        "How do you do optimistic concurrency over HTTP? (ETag plus `If-Match`, returning 412 on a mismatch.)"
      ],
      trap: "Returning 200 with an error in the body. It breaks every client's error handling, defeats retry logic and monitoring, and it is disappointingly common."
    }
  ]);
})(window.TD = window.TD || {});
