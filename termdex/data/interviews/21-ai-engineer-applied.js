/* Interview bank — AI Engineer, part two: the applied half.

   Product engineering, the coding round, the design round and the two
   behavioural questions that are specific to this job. If part one is what a
   frontier lab probes, this is what an applied-AI team actually hires on —
   and it is where most offers are won or lost, because almost every candidate
   over-prepares transformer internals and under-prepares cost, evaluation and
   the ability to say no.

   Companion file: 19-ai-engineer-core.js */
(function (TD) {
  "use strict";

  TD.addQuestions([

    /* ================= product engineering ================= */
    {
      id: "ai-llm-judge-design",
      q: "You want to use an LLM to grade your LLM's outputs. Design it so the scores mean something.",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["anthropic", "openai", "scale-ai", "google", "microsoft", "databricks", "salesforce", "cohere", "perplexity"],
      tags: ["evaluation", "llm-judge", "measurement"],
      aiTrack: true,
      simple: "Human grading is accurate and far too slow, so you have a model do it. But the grader is itself a model with its own quirks — it prefers longer answers, it prefers whichever option it saw first, and it prefers its own writing. A judge that has not been checked against humans is producing numbers, not measurements.",
      answer: "## Absolute or pairwise\n\n**Pairwise** — *which of these two answers is better?* — is substantially more reliable. Models are poor at absolute calibration (\"is this a 7 or an 8?\") and good at comparison. Use pairwise for model or prompt comparisons, which is most of what you actually need.\n\n**Absolute scoring** is required when you need a number to track over time or to gate a release. Make it survivable by using a small ordinal scale (1–5, never 1–100) with **a written description of what each point means**, and by asking for the reasoning before the score.\n\n## The four biases, and what to do about each\n\n| Bias | What happens | Mitigation |\n| --- | --- | --- |\n| **Position** | The first option wins more often, sometimes by 10–20 points | Run both orders, average. If they disagree, call it a tie |\n| **Verbosity** | Longer answers score higher regardless of content | Report length alongside score; penalise explicitly in the rubric |\n| **Self-preference** | A model rates its own family's output higher | Judge with a different model family from the one generating |\n| **Sycophancy** | Agrees with an assertion embedded in the prompt | Never reveal which answer is the new one, or which system produced it |\n\nPosition bias alone invalidates more evaluations than any other single cause, and swapping the order is one line of code.\n\n## The rubric is the actual work\n\nA prompt saying *rate the quality from 1 to 5* produces noise. A usable rubric names the dimensions and decides them separately:\n\n```\nGrade the answer on each dimension independently.\n\nFAITHFULNESS: every factual claim is supported by the provided sources.\n  5 = every claim supported, nothing added\n  3 = mostly supported, one unsupported detail\n  1 = contains a claim contradicted by the sources\n\nCOMPLETENESS: addresses every part of the question.\nFORMAT: obeys the requested structure.\n\nFor each: give one sentence of reasoning, then the score.\nOutput JSON: {\"faithfulness\": {\"reason\": ..., \"score\": ...}, ...}\n```\n\nThree things matter here. **Reasoning before the score** — reversed, the model rationalises a number it has already committed to. **Separate dimensions** — a single score collapses into \"did I like it\", and a system that is faithful but badly formatted needs a different fix from one that is fluent and wrong. **Structured output** so results are aggregable without parsing prose.\n\n## Calibrate it, or it is not a measurement\n\nThis is the step that separates a real evaluation from theatre.\n\n1. Have a human grade 100 outputs with the same rubric.\n2. Compute agreement between judge and human — **Cohen's kappa** for categorical, Spearman for ranked.\n3. Below ~0.6, the judge is unusable: improve the rubric, add few-shot examples of correct grading, or use a stronger model.\n4. Re-calibrate whenever the rubric, the judge model, or the task changes.\n\nReport the agreement number next to the scores, permanently. \"Faithfulness 4.2, judge–human kappa 0.71\" is a measurement. \"Faithfulness 4.2\" is a number.\n\n## Cheaper variants worth knowing\n\n- **Reference-based grading.** If you have a gold answer, ask the judge to compare against it rather than to judge in the abstract. Far more reliable and it lets a smaller judge model do the work.\n- **Deterministic checks first.** JSON validity, schema conformance, citation existence, forbidden strings, length. Run these before the judge — they are free, exact, and catch a surprising share of regressions.\n- **Ensembling.** Three judges, majority vote. Triples cost for a modest reliability gain; reserve it for release gates.\n\n## What to say about the limits\n\nAn LLM judge cannot evaluate anything the judge model itself cannot do. It will not catch a subtle domain error a specialist would spot, and it inherits the generator's blind spots when they share a family. Use it for regression detection at scale and keep a small human-reviewed set for absolute truth. Volunteering that limit is what makes the rest credible.",
      takeaways: [
        "Pairwise beats absolute scoring; if you must score absolutely, use a small ordinal scale with a written rubric.",
        "Swap the order and average — position bias alone can move results by ten points or more.",
        "Report judge–human agreement beside every score, or you are publishing numbers rather than measurements."
      ],
      followUps: [
        "How would you detect that your judge has drifted after a model upgrade?",
        "When is a small fine-tuned classifier better than an LLM judge? (High volume, narrow criterion, latency budget.)",
        "How do you evaluate the judge's own faithfulness?"
      ],
      trap: "Using the same model family to generate and to judge. Self-preference is measurable and large, and it makes every comparison against another provider quietly favour your own."
    },

    {
      id: "ai-semantic-cache",
      q: "Would you put a semantic cache in front of your LLM? Talk me through the risk.",
      topic: "System Design",
      level: "medium",
      companies: ["perplexity", "microsoft", "amazon", "salesforce", "adobe", "flipkart", "glean", "sarvam"],
      tags: ["caching", "cost", "latency"],
      aiTrack: true,
      simple: "An exact cache only helps when two users type identical text, which is rare. A semantic cache embeds the question and reuses the answer for anything similar enough. That is powerful and dangerous — because 'similar enough' will eventually match two questions whose answers are opposite.",
      answer: "## The three caches, in the order to build them\n\n**1. Exact-match cache.** Hash the full prompt including system prompt, model and parameters. Zero risk of a wrong hit. Low hit rate on free-text chat, high on templated calls — classification, extraction, summarisation of the same document. **Build this first; it is free correctness.**\n\n**2. Prefix cache.** Not your cache — the provider's. A long shared system prompt or a repeated document is processed once and its KV cache reused, cutting prefill cost by 50–90% on the cached portion. Ordering your prompt **static-first, variable-last** is a one-line change that unlocks it, and most candidates have never heard of it.\n\n**3. Semantic cache.** Embed the query, search cached queries, return the stored answer if similarity exceeds a threshold. Hit rates of 20–40% are realistic on support-style traffic. This is the one with the risk.\n\n## Why the risk is real\n\nCosine similarity does not respect negation, quantity or entity.\n\n| Query A | Query B | Similarity | Same answer? |\n| --- | --- | --- | --- |\n| \"Can I cancel my order?\" | \"Can I cancel my subscription?\" | ~0.93 | **No** |\n| \"Is the API rate limited?\" | \"Is the API not rate limited?\" | ~0.95 | **No** |\n| \"Refund policy for India\" | \"Refund policy for the EU\" | ~0.94 | **No** |\n| \"How do I reset my password\" | \"password reset steps\" | ~0.95 | Yes |\n\nA single threshold cannot separate rows two and four. That is the honest core of the answer, and candidates who say \"set the threshold to 0.95\" have not thought about it.\n\n## Making it safe\n\n- **Threshold high, and tuned on data.** Take 500 real query pairs, label same-answer or not, and plot the false-hit rate against threshold. Choose from the curve. Typically 0.95–0.97, far higher than intuition suggests.\n- **Namespace the key by everything that changes the answer.** User's permission scope, tenant, locale, model version, prompt version, and any retrieved document set. A cache key that omits the permission scope is a data-leak bug, not a performance bug.\n- **Never cache personalised or stateful answers.** Anything containing the user's own data, order status, or account balance.\n- **Short TTL** where the underlying corpus changes, and explicit invalidation on document update.\n- **Log every hit** with both queries, and sample them for review. You cannot detect a bad hit any other way.\n- **Bypass on follow-ups.** Multi-turn context makes a short query like \"and for the EU?\" meaningless in isolation.\n\n## What it actually buys\n\nOn 100,000 requests/day at ₹0.4 each, a 30% hit rate saves roughly ₹12,000/day — about ₹4.4 lakh a year — plus a latency drop from ~2 s to ~50 ms on hits, which users feel more than the cost saving. Those are the two numbers to state.\n\n## The recommendation\n\nExact cache always. Prefix caching always — it is a prompt-ordering change. Semantic cache **only for a bounded, high-volume, non-personalised query class**: FAQ, documentation lookup, product questions. Not on an open-ended assistant, where the first wrong hit is a support ticket and the second is a lost customer.\n\nOffering that scoped recommendation, rather than a yes or a no, is the answer.",
      takeaways: [
        "Exact-match and provider prefix caching are free wins; order prompts static-first to unlock the second.",
        "Embeddings do not respect negation, locale or entity — a single similarity threshold cannot be safe across all traffic.",
        "The cache key must include permission scope, tenant, locale and prompt version, or the cache becomes a leak."
      ],
      followUps: [
        "How would you tune the threshold empirically?",
        "How do you invalidate a semantic cache when a source document changes?",
        "What is the cost of a false hit in your product, in support tickets?"
      ],
      trap: "Treating a semantic cache as a pure optimisation. It changes answers. Any candidate who does not raise negation and entity mismatches has not run one."
    },

    {
      id: "ai-model-routing",
      q: "Design a router that sends easy requests to a cheap model and hard ones to an expensive one.",
      topic: "System Design",
      level: "hard",
      companies: ["openai", "anthropic", "microsoft", "amazon", "databricks", "salesforce", "perplexity", "sarvam", "flipkart"],
      tags: ["cost", "routing", "architecture"],
      aiTrack: true,
      simple: "Most requests are easy and do not need your best model. If you can tell in advance which ones are easy, you send those to a model that costs a twentieth as much and keep the expensive one for the rest. The whole difficulty is deciding, cheaply and reliably, which is which.",
      answer: "## Why it is worth doing\n\nFrontier and small-model prices differ by roughly 20–30×. If 70% of traffic is routable to the cheap model, blended cost falls by around 65% — a number that appears in somebody's quarterly review, which is why this question is asked so often in applied loops.\n\n## Three architectures\n\n**Cascade.** Try the cheap model; escalate if the result is unsatisfactory.\n\n```\nresult = cheap(request)\nif not confident(result):\n    result = expensive(request)\n```\n\nSimple, needs no training, and self-correcting: mistakes cost latency, not quality. The cost is a double call on the escalated fraction, so the economics only work if escalation stays below roughly 40%.\n\n**Predictive router.** Classify the request *before* calling anything, using a small trained model over the query text. One cheap call total. Requires labelled data — which you generate by running both models on historical traffic and recording where the cheap one sufficed.\n\n**Hybrid.** Predictive routing for the obvious cases, cascade for the uncertain middle. What most mature systems converge on.\n\n## The confidence signal, which is the real question\n\n\"Ask the model how confident it is\" does not work — models are systematically over-confident, and RLHF made that worse. Usable signals, roughly in order of reliability:\n\n- **Self-consistency.** Sample $n$ answers at temperature > 0; disagreement means difficulty. Correlates well with correctness and costs $n$× — reserve for high stakes.\n- **A verifier.** For code, run the tests. For extraction, validate against the schema. For retrieval, check the answer is entailed by the cited chunk. **Where a verifier exists, it beats every heuristic**, and pointing that out is a strong move.\n- **Retrieval quality.** In RAG, a low top-1 similarity score means the corpus probably does not answer the question — escalate or refuse.\n- **Token log-probabilities.** Low mean log-prob correlates weakly with error. Cheap, and only a tiebreaker.\n- **Query features.** Length, question type, presence of code, language, requested output format. Crude and surprisingly effective as a first cut.\n\n## Guardrails the design needs\n\n- **Never route on price alone for regulated or high-stakes paths.** Medical, legal, financial advice go to the strong model unconditionally, and that carve-out should be explicit configuration, not a threshold.\n- **Cap the escalation rate.** If it exceeds your model, you are paying for two calls on most traffic and the router is losing money.\n- **Route sticky within a conversation.** Switching models mid-conversation produces a visible personality and quality change that users notice and dislike.\n- **Keep a shadow sample.** Send 1–2% of routed-cheap traffic to the expensive model as well, and compare. This is how you detect that your router has drifted, and without it the router degrades invisibly as traffic shifts.\n\n## Report it as economics\n\n```\nbaseline: 100% frontier      ₹0.42/req   quality 4.31\nrouted:   68% small          ₹0.15/req   quality 4.27\n                             -64% cost   -0.9% quality\nescalation rate 31%, shadow-sampled disagreement 3.4%\n```\n\nThat table is the answer. A candidate who produces cost, quality and escalation rate together is describing a system they have operated; one who describes the routing logic alone is describing one they have read about.\n\n## The unglamorous alternative\n\nBefore building a router, check whether prompt compression, a shorter output, caching, or simply using the small model for *everything* on that endpoint gets you most of the way. Routing adds a component that can fail; sometimes the answer is that the cheap model was always good enough for this feature and nobody tested it.",
      takeaways: [
        "A cascade needs no training and self-corrects; a predictive router needs labels but avoids the double call.",
        "Self-reported confidence is useless — use verifiers, self-consistency, or retrieval scores.",
        "Shadow-sample the cheap path against the expensive one, or the router silently degrades as traffic shifts."
      ],
      followUps: [
        "How would you build the training set for a predictive router?",
        "What breaks if you switch models mid-conversation?",
        "How would you handle a request whose difficulty is only apparent after retrieval?"
      ],
      trap: "Routing on the model's self-reported confidence. It is poorly calibrated and biased upward, so the router will confidently send hard requests to the weak model — the exact failure it exists to prevent."
    },

    {
      id: "ai-streaming-transport",
      q: "How do you stream LLM output to a browser, and what goes wrong at scale?",
      topic: "System Design",
      level: "medium",
      companies: ["openai", "anthropic", "perplexity", "microsoft", "amazon", "adobe", "salesforce", "glean", "flipkart"],
      tags: ["streaming", "latency", "backend"],
      aiTrack: true,
      simple: "Waiting eight seconds for a full answer feels broken; seeing words appear after 400 milliseconds feels fast, even though the total time is the same. Streaming is mostly a perception fix, and it complicates everything downstream — error handling, cancellation, caching, cost accounting and connection limits.",
      answer: "## The transport\n\n**Server-Sent Events is the right default.** It is plain HTTP, one direction, auto-reconnects, works through most proxies, and needs no special client. WebSockets are for genuinely bidirectional traffic — live voice, collaborative editing — and cost you a stateful connection and more infrastructure.\n\n```\nGET /v1/chat  Accept: text/event-stream\n\ndata: {\"delta\":\"The \"}\ndata: {\"delta\":\"answer \"}\ndata: {\"usage\":{\"in\":812,\"out\":140},\"finish\":\"stop\"}\ndata: [DONE]\n```\n\nSend `Cache-Control: no-cache` and `X-Accel-Buffering: no`, or nginx will buffer the whole response and you will ship a streaming endpoint that does not stream — an extremely common and very confusing bug.\n\n## The metrics that matter\n\n- **TTFT** — time to first token. The number users experience as speed. Under 500 ms feels instant; over 2 s feels broken.\n- **TPOT** — time per output token. Above roughly 30 tokens/second, faster stops being noticeable, so buying more than that is wasted money.\n- **Total** — matters for cost and for timeouts, not for perceived speed.\n\nAlert on TTFT and TPOT separately. Total latency moves whenever prompt or output lengths shift, so an alert on it fires on user behaviour rather than on your system.\n\n## What breaks\n\n**Errors after a 200.** The status code is sent before generation starts. A provider failure at token 300 cannot become a 500 — you must define an in-band error event and have the client render partial output plus a retry, rather than a blank screen.\n\n**Cancellation.** A user navigating away leaves the upstream generation running and billing. Propagate the disconnect: in FastAPI, check `await request.is_disconnected()` in the loop and abort the upstream call. On a busy product this is a real fraction of spend, and forgetting it is one of the most common cost leaks in a first production AI feature.\n\n**Connection limits.** Each stream holds a connection for the whole generation — often 10–60 s. A thousand concurrent users means a thousand held connections. Async workers handle this comfortably; a synchronous WSGI server with 16 workers does not. This is why async matters more for AI backends than for ordinary CRUD.\n\n**Proxy and load-balancer timeouts.** Default idle timeouts (60 s on many load balancers) kill long generations. Raise them, and send a heartbeat comment line so intermediaries do not consider the connection idle.\n\n**Cost accounting.** Token usage arrives in the final event. If the stream is abandoned you never see it — count output tokens yourself as they pass, or you will systematically under-report spend on exactly the requests users hated most.\n\n**Guardrails become awkward.** You cannot moderate an answer you have not finished generating. Options: buffer the first N tokens before flushing, run a moderation pass on a rolling window and cut the stream mid-answer, or accept the risk and moderate the input only. Each is a real product decision — say which you would pick and why.\n\n**Structured output.** Streaming JSON is unparseable until complete. Either stream a text field only, or use a partial-JSON parser, or stream discrete events per completed field rather than raw characters.\n\n## The perception trick worth naming\n\nSend something before the model does. A retrieval status line — *searching 4 sources…* — displayed at 200 ms while the model is still on prefill converts a two-second wait into a system that is visibly working. It costs nothing and it is the single highest-leverage latency change in most AI products, because the fastest fix for a slow system is often to stop it looking idle.",
      takeaways: [
        "SSE is the default; WebSockets only for genuinely bidirectional traffic.",
        "Errors after the 200 need an in-band error event, and abandoned streams must be cancelled upstream or you keep paying.",
        "Alert on TTFT and TPOT separately — total latency moves with user behaviour, not with your system."
      ],
      followUps: [
        "How do you moderate a streamed answer without buffering it entirely?",
        "How would you resume a stream after a dropped connection? (Sequence ids and a short server-side buffer.)",
        "Why does async matter more for an AI backend than a CRUD one?"
      ],
      trap: "Forgetting to cancel upstream generation when the client disconnects. It is invisible in testing, it costs real money in production, and it is the first thing an experienced interviewer probes."
    },

    {
      id: "ai-tool-design",
      q: "Your agent has fifteen tools and keeps calling the wrong one. How do you design tools properly?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["anthropic", "openai", "microsoft", "salesforce", "glean", "amazon", "google", "adobe", "cohere"],
      tags: ["agents", "tool-use", "design"],
      aiTrack: true,
      simple: "A tool definition is a prompt. The model chooses a tool by reading its name, its description and its parameters — so a vague description is a bug in exactly the way an ambiguous function name is not, and fifteen overlapping tools guarantee the model will pick badly.",
      answer: "## The framing that fixes most of it\n\nTool schemas are not an API contract; they are **the prompt the model reads to decide**. Write them for a competent new colleague who has your documentation but no context, and most of the problem disappears.\n\n## The rules\n\n**Fewer tools.** Accuracy degrades noticeably past roughly 10–15 tools, and collapses when several overlap. If you have `search_docs`, `search_wiki` and `search_tickets`, replace them with one `search(source: enum)`. Group by *what the user is trying to do*, not by which backing service you happen to own.\n\n**Names are instructions.** `get_data` tells the model nothing. `search_customer_orders` tells it when to reach for it.\n\n**The description should say when NOT to use it.** This is the highest-value line in most tool definitions and almost nobody writes it:\n\n```json\n{\n  \"name\": \"search_orders\",\n  \"description\": \"Search a customer's past orders by date range or status. Use for questions about what a customer bought or when. Do NOT use for refunds (use process_refund), for stock levels (use check_inventory), or for orders not yet placed.\",\n  \"input_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"customer_id\": {\"type\": \"string\", \"description\": \"Internal customer id, format CUS-XXXXX. Obtain from find_customer if you only have an email.\"},\n      \"status\": {\"type\": \"string\", \"enum\": [\"placed\", \"shipped\", \"delivered\", \"cancelled\"]}\n    },\n    \"required\": [\"customer_id\"]\n  }\n}\n```\n\n**Constrain the types.** An enum is a guarantee; a free-text string is a hope. Every enum you add removes a class of retry.\n\n**Say where a parameter comes from.** `\"Obtain from find_customer if you only have an email\"` prevents the model inventing an id — the single most common agent failure and the cheapest to prevent.\n\n## Error messages are prompts too\n\nThe tool's error text goes straight back into the model's context, so it should tell the model what to do next:\n\n| Bad | Good |\n| --- | --- |\n| `Error: 400` | `customer_id must match CUS-XXXXX; you passed an email. Call find_customer first.` |\n| `Not found` | `No orders in that range. The customer's earliest order is 2024-03-11 — try a wider range.` |\n| `Unauthorized` | `This customer's records are restricted. Tell the user to contact their account manager; do not retry.` |\n\nThat last one matters: an error the model should not retry must **say so**, or it will loop until it hits your step cap.\n\n## Safety and correctness\n\n- **Idempotency keys on anything that writes.** Agents retry. Without a key, one retry becomes two refunds.\n- **Separate read from write.** Read tools run freely; write tools require confirmation, a spend cap, or a human. This is the containment boundary, and it should be enforced by the tool layer, not by the prompt.\n- **Return small results.** A tool returning 50 KB of JSON burns context, costs money and buries the useful fields. Return the fields the model needs plus a handle for more.\n- **Make results stable.** Non-deterministic ordering makes agent behaviour irreproducible and evaluation impossible.\n\n## How to debug it\n\nBuild a labelled set of user requests with the correct tool call for each, then measure **tool-selection accuracy** and **parameter accuracy** separately. They fail for different reasons: selection failures mean overlapping descriptions, parameter failures mean under-specified schemas. Fixing the wrong one wastes a week.\n\nMost teams never do this and debug by staring at traces. Saying that you would build the labelled set is the answer that gets remembered.\n\n## The escape hatch\n\nIf tool count is genuinely large, do not put them all in context. Retrieve the relevant subset first — embed the tool descriptions, search them with the user's request, and present the top eight. It is RAG over your own tool catalogue, and it is how systems with hundreds of tools stay accurate.",
      takeaways: [
        "A tool description is a prompt; the most valuable line in it says when NOT to use the tool.",
        "Error strings go back into context — write them as instructions for the next step, and mark the non-retryable ones.",
        "Measure tool-selection and parameter accuracy separately; they have different causes and different fixes."
      ],
      followUps: [
        "How would you handle 200 tools? (Retrieve the relevant subset per request.)",
        "How do you stop an agent retrying a permanently failing tool?",
        "Why do idempotency keys matter more for agents than for ordinary clients?"
      ],
      trap: "Treating tool definitions as API documentation. They are model-facing instructions, and the phrasing changes behaviour as much as any system-prompt edit."
    },

    {
      id: "ai-agent-evaluation",
      q: "How do you evaluate an agent, given the same task can be done correctly in several different ways?",
      topic: "LLM & GenAI",
      level: "staff",
      companies: ["anthropic", "openai", "microsoft", "google-deepmind", "scale-ai", "salesforce", "glean", "databricks"],
      tags: ["agents", "evaluation", "reliability"],
      aiTrack: true,
      simple: "For a single answer you can check whether it is right. For an agent that takes twelve steps, the final state might be correct while the route there was wasteful, unsafe, or lucky. So you measure the outcome, the path, and the cost — and you run each task many times, because agents are not deterministic.",
      answer: "## Three layers, measured separately\n\n**Outcome.** Did the world end up in the right state? This is the primary metric and it should be checked programmatically wherever possible — the row exists in the database, the file compiles, the tests pass, the ticket is closed with the right tag. Where a checkable end state exists, use it and do not ask a model.\n\n**Trajectory.** Was the route sane? Number of steps, tools chosen, redundant calls, loops, recoveries after an error. An agent that reaches the right answer in 19 steps when 4 would do is a cost problem now and a reliability problem soon.\n\n**Cost and latency.** Tokens, tool calls, wall-clock. Agents have unbounded cost by construction, so this is a first-class metric, not an afterthought.\n\n## The pass@k problem\n\nAgents are stochastic. A task that succeeds once may fail four times out of five. Reporting a single run is meaningless.\n\nRun every task $n \\geq 5$ times and report:\n\n- **pass@1** — succeeded on the first attempt. What a user actually experiences.\n- **pass^k** (all $k$ attempts succeeded) — the honest reliability measure, and always far lower than teams expect.\n- **Variance.** High variance on a task means it is on a knife edge and will regress on any change.\n\nA demo shows pass@1 on a lucky run. Production cares about pass^5. Making that distinction out loud is the strongest single thing you can say in this round.\n\n## Why step count compounds\n\nWith per-step reliability $p$ and $n$ steps, end-to-end success is roughly $p^n$:\n\n| Steps | $p = 0.95$ | $p = 0.99$ |\n| --- | --- | --- |\n| 5 | 77% | 95% |\n| 10 | 60% | 90% |\n| 20 | 36% | 82% |\n| 50 | 8% | 61% |\n\nThis single table explains why long-horizon agents disappoint, and why the two effective responses are **fewer steps** (better tools, more done per call) and **recovery** (checkpoints, verification, retries that reset rather than compound). It is also the argument for narrow agents over general ones, which is the judgement the question is really probing.\n\n## Building the harness\n\n**A sandbox with a resettable state.** A container, a seeded database, a mock of every external service. Real APIs make evaluation slow, expensive, non-reproducible and occasionally destructive.\n\n**Deterministic tool mocks.** Fix the responses so a change in agent behaviour is attributable to the agent.\n\n**Programmatic assertions per task**, not a model judging the transcript, wherever the end state is checkable.\n\n**Hard caps** on steps, wall-clock and spend — with a cap breach recorded as a distinct failure category, because \"failed\" and \"never terminated\" need different fixes.\n\n## Categorise the failures\n\nAn aggregate pass rate tells you nothing actionable. Bucket every failure:\n\n| Category | Typical fix |\n| --- | --- |\n| Wrong tool selected | Tool descriptions overlap |\n| Bad parameters | Schema under-specified |\n| Did not recover from a tool error | Error message was not actionable |\n| Looped | No progress check or stop condition |\n| Stopped early | Success criteria unclear in the prompt |\n| Hallucinated a result | Never called the tool at all |\n| Ran out of context | Needs compaction |\n\nThe distribution across these buckets is what tells you what to work on this week, and producing that table is the difference between an evaluation and a scoreboard.\n\n## Online\n\nOffline suites go stale. In production, track task completion, human-intervention rate, cost per completed task, and — the most honest signal available — **how often a user takes over and finishes it themselves**.",
      takeaways: [
        "Measure outcome, trajectory and cost separately; check the end state programmatically wherever you can.",
        "Report pass^k, not a single run — reliability compounds as p^n and long agents therefore disappoint.",
        "Bucket failures by cause; an aggregate pass rate is not actionable."
      ],
      followUps: [
        "How do you make an agent evaluation reproducible? (Sandbox, deterministic mocks, fixed seeds where possible.)",
        "How would you decide between one 20-step agent and four 5-step ones?",
        "What is the right response to a task with high variance across runs?"
      ],
      trap: "Reporting a single successful run. Agents are stochastic; a number without a repetition count and a variance figure is a demo, not an evaluation."
    },

    {
      id: "ai-context-engineering",
      q: "Your agent runs for 60 turns and degrades badly after turn 20. What is happening and how do you fix it?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["anthropic", "openai", "microsoft", "cohere", "glean", "google", "salesforce", "perplexity"],
      tags: ["agents", "context", "reliability"],
      aiTrack: true,
      simple: "Everything the agent has seen — every tool call and every result — piles up in the context. It gets slower, more expensive, and worse at paying attention to what matters, because the important instruction from turn 2 is now buried under forty screens of JSON. Managing what stays in the window is a design problem in its own right.",
      answer: "## The four failure modes\n\n**Context rot.** As the window fills, attention spreads thin. The instruction from turn 2 competes with hundreds of irrelevant tool results, and models attend most reliably to the beginning and end of a long context — the middle sags. This is measurable and it is why a model with a 200k window is not equally good across all 200k tokens.\n\n**Cost and latency growth.** Every turn resends the whole history. Cost per turn grows linearly, so total cost over a 60-turn session grows **quadratically**. A session that starts at ₹0.3 per turn ends at ₹4.\n\n**Poisoning.** One wrong fact or one bad tool result stays in the context forever and is re-read on every subsequent turn, so a single early error contaminates the rest of the session.\n\n**Distraction.** Long histories of past tool calls make the model repeat what it already did instead of choosing a new action.\n\n## The techniques, in the order to apply them\n\n**1. Compaction.** At a threshold — say 70% of the window — summarise the history into a structured brief and start a fresh context from it.\n\n```\nSummarise for a fresh agent continuing this task:\n  GOAL:           the original objective, verbatim\n  DONE:           steps completed and their outcomes\n  LEARNED:        facts discovered, with sources\n  FAILED:         approaches tried that did not work, and why\n  STATE:          file paths, ids, open handles still in use\n  NEXT:           the immediate next action\n```\n\nThe FAILED section is the one people omit and the one that matters most — without it the agent cheerfully retries the approach that already failed twice.\n\nKeep the original user request **verbatim**, never summarised. Compaction drift is a real failure: after three compactions the agent is working on a paraphrase of a paraphrase.\n\n**2. Externalise state.** The context is working memory, not storage. Write findings to files, a scratchpad, or a database, and keep only handles in context. `notes/api-findings.md (2.1 KB)` costs eight tokens and the agent can re-read it when needed. This is the single largest lever, and it is what makes long-running agents feasible at all.\n\n**3. Prune tool results.** A search returning 50 results needs the top 5 in context. Full outputs go to disk with a reference. Truncate aggressively, and let the agent request more explicitly.\n\n**4. Sub-agents.** Delegate a self-contained investigation to a fresh agent with its own clean context; it returns a paragraph. The parent's context grows by a paragraph rather than by forty tool calls. Costs more tokens overall, keeps the main thread coherent, and is the standard answer for research-shaped tasks.\n\n**5. Just-in-time retrieval.** Do not preload everything the agent might need. Give it the ability to fetch, and let it fetch what it actually uses.\n\n## Structure what remains\n\n- **System prompt and goal at the top**, stable so prefix caching applies.\n- **The most relevant retrieved material adjacent to the question**, not buried in the middle.\n- **A running structured state block** the agent rewrites each turn — a to-do list, effectively. It survives compaction and it keeps the objective salient.\n\n## Diagnosing it in production\n\nLog tokens-in per turn, and plot quality against context length. If quality falls as the context grows, you have context rot rather than a model problem — and the fix is architectural, not a better prompt. Most teams reach for a bigger model first and waste a month.\n\n## The honest limit\n\nNo amount of context engineering makes a 200-step agent as reliable as a 10-step one. If the task genuinely needs 200 steps, decompose it into checkpointed stages with verification between them, and accept human review at the boundaries. Saying that — rather than promising the technique scales indefinitely — is the judgement being tested.",
      takeaways: [
        "Cost grows quadratically over a session because every turn resends the whole history.",
        "Externalise state to files and keep handles in context; that is the largest single lever.",
        "Compaction must preserve the verbatim goal and the list of failed approaches, or the agent repeats itself."
      ],
      followUps: [
        "How do you stop compaction drift over several compactions?",
        "When is a sub-agent worth the extra tokens?",
        "How would you tell context rot apart from a model regression? (Plot quality against context length.)"
      ],
      trap: "Reaching for a larger context window. It postpones the problem by a few turns, multiplies cost immediately, and does nothing about poisoning or distraction."
    },

    {
      id: "ai-unit-economics",
      q: "Your AI feature costs ₹4 per request and the plan sells for ₹499 a month. Fix it.",
      topic: "System Design",
      level: "hard",
      companies: ["flipkart", "sarvam", "salesforce", "adobe", "microsoft", "amazon", "perplexity", "databricks", "uber"],
      tags: ["cost", "economics", "product"],
      aiTrack: true,
      simple: "Software usually costs nothing extra per user; AI features cost money every single time somebody presses the button. That changes the business, so an AI engineer has to be able to compute the cost of a request, find where it is going, and cut it without ruining the product.",
      answer: "## Do the arithmetic out loud first\n\n₹499/month, and say a 30% gross-margin floor after other costs leaves roughly ₹350 for inference. At ₹4 per request that is **87 requests a month** before the customer is unprofitable. A daily user does 300. **The feature loses money on its best customers**, which is the worst possible shape for a business.\n\nSay this number immediately. The interviewer is testing whether you think in unit economics at all.\n\n## Find where the ₹4 goes\n\nBreak the request down before optimising anything:\n\n| Component | Tokens | Share |\n| --- | --- | --- |\n| System prompt | 1,200 | 8% |\n| Retrieved chunks (12 × 800) | 9,600 | **62%** |\n| Conversation history | 3,000 | 19% |\n| User message | 200 | 1% |\n| Output | 600 | 10% |\n\nInput dominates, and retrieval dominates input. That is typical, and it points the work.\n\n## The levers, ordered by ratio of saving to risk\n\n**1. Retrieve less, rank better.** 12 chunks to 5 with a reranker: −38% cost, and answer quality usually *improves* — more context dilutes attention. Best lever in the list by a distance.\n\n**2. Prefix caching.** Put the system prompt and any stable content first and unchanged. Providers charge a fraction for cached input. −15–40% of input cost for a prompt-ordering change.\n\n**3. Route by difficulty.** Most requests do not need the frontier model. 70% routed to a small model at a twentieth of the price: −60% blended.\n\n**4. Cap and trim history.** Last 6 turns plus a running summary, not 20 turns verbatim.\n\n**5. Cap output.** `max_tokens`, and a prompt that asks for brevity. Output tokens cost 3–5× input.\n\n**6. Exact-match cache** on templated calls.\n\n**7. Batch the asynchronous work.** Anything not user-facing — nightly summaries, enrichment, backfills — goes through a batch endpoint at roughly half price.\n\n**8. Self-host, last.** Worth it only above roughly ₹8–10 lakh a month of steady traffic; below that a GPU sitting idle at night costs more than the API, and you have added an operations burden.\n\nStacked, this typically lands around **₹0.55–0.90 per request**, which makes the plan work.\n\n## The product levers, which engineers forget to mention\n\n- **Fair-use limits.** Generous, published, enforced. Most users will never see them, and they cap the tail that would otherwise define your cost.\n- **Per-user budgets** enforced in code. One user with a script can otherwise consume a month's margin in a night.\n- **Tiering.** Heavy usage belongs on a higher plan. This is a pricing fix for a pricing problem, and proposing it shows commercial literacy.\n- **Cheaper defaults.** The fast model by default, the strong one on an explicit \"think harder\" action the user takes deliberately.\n\n## Instrument it permanently\n\nLog per request: prompt tokens split by source (system, retrieved, history), output tokens, cached tokens, model, cost, user and feature. Then a dashboard of cost per user per day and cost per feature.\n\nWithout this you cannot answer *which feature is unprofitable*, and that is the question your CFO will eventually ask. Teams that add it after the first alarming invoice spend a month reconstructing history they could have logged from day one.\n\n## Close on the trade\n\nName what you would give up. A cheaper model on the routine path costs a fraction of a quality point; fewer retrieved chunks costs nothing measurable; a shorter history costs conversational memory on long threads. State which of those you would accept and which you would not, and why. An answer that claims a 90% cost cut with no downside is not credible.",
      takeaways: [
        "Compute requests-to-breakeven immediately — it reframes the problem as a business one.",
        "Retrieved context is usually the dominant token cost; retrieving fewer, better-ranked chunks often improves quality too.",
        "Log tokens by source per request from day one, or you cannot say which feature is losing money."
      ],
      followUps: [
        "At what monthly volume does self-hosting become cheaper?",
        "How would you enforce a per-user budget without breaking the experience?",
        "Which of these levers would you not take, and why?"
      ],
      trap: "Jumping to self-hosting. It is the largest change, the slowest to deliver, and it only pays above substantial steady volume — while a prompt reordering and a reranker land the same week."
    },

    {
      id: "ai-provider-failure",
      q: "Your model provider has a two-hour outage, and last month they deprecated the model you built on. Design for both.",
      topic: "System Design",
      level: "medium",
      companies: ["microsoft", "amazon", "salesforce", "adobe", "flipkart", "uber", "databricks", "sarvam", "oracle"],
      tags: ["reliability", "architecture", "operations"],
      aiTrack: true,
      simple: "Your most important dependency is a company you cannot call, whose model can change under you without warning. That is unusual in software, and it means abstraction, fallbacks and a regression suite are not nice-to-haves — they are what stops a provider's bad Tuesday from becoming yours.",
      answer: "## Abstract the provider, not the prompt\n\nOne internal interface, several implementations:\n\n```python\nclass LLM(Protocol):\n    async def complete(self, messages, *, tools=None,\n                       max_tokens=None, schema=None) -> Completion: ...\n```\n\nThis is worth doing and it is worth being honest about its limits. Providers differ in tool-calling format, system-prompt handling, token counting, stop sequences and refusal behaviour, and **prompts do not transfer cleanly**. The abstraction gives you a swap path in hours instead of weeks; it does not give you a free swap. Saying that distinguishes you from someone quoting a design pattern.\n\n## Outage handling\n\n```\nprimary (provider A)\n   |-- timeout / 5xx / 429 after retries\n   v\nsecondary (provider B, different company, equivalent model)\n   |-- also failing\n   v\ndegraded mode: cached answer, retrieval-only results, or an honest\n               \"AI answers are temporarily unavailable\" with the\n               non-AI path still working\n```\n\nThe rules:\n\n- **A different company, not a different region of the same one.** Correlated failure is the point.\n- **Circuit breaker.** After N consecutive failures, stop trying for 30 seconds. Hammering a failing provider adds latency to every request and delays your own recovery.\n- **Retry with exponential backoff and full jitter.** Without jitter, every client retries in lockstep and you re-create the thundering herd against a provider that is already struggling.\n- **Distinguish the errors.** A 429 means back off and it may be *your* key's limit rather than an outage. A 400 must never be retried. A 500 is retryable. A timeout may have already been billed.\n- **Design the degraded path deliberately.** For a search product, returning the retrieved documents without a generated summary is a genuinely useful degraded mode. Decide it in advance and test it.\n\n## Deprecation and silent drift\n\nDeprecation is the slower and more damaging failure.\n\n- **Pin exact model versions.** Never a floating alias in production. `model-2025-11-04`, not `model-latest`. A floating alias means your behaviour changes on a date somebody else picked.\n- **Subscribe to the provider's deprecation notices**, and put the end-of-life date in your own calendar the day you adopt a model.\n- **Keep the regression suite runnable on demand.** When a migration is forced, the suite is what turns a fortnight of anxiety into a day of work. This is the concrete payoff for having built the evaluation harness, and it is worth stating as such.\n- **Watch for silent drift** even on a pinned version: run a small canary set daily and alert on a change. Providers do adjust serving stacks, and quantisation or routing changes have moved behaviour without a version bump.\n\n## The migration procedure\n\n1. Run the golden set against the candidate model. Compare per category, not in aggregate — a model can be better overall and much worse at your one important format.\n2. Fix prompts where the new model needs different phrasing. Budget for this; it is not zero.\n3. Shadow-run on live traffic, comparing outputs without serving them.\n4. Roll out 5% → 50% → 100% behind a flag, watching quality and cost.\n5. Keep the old model reachable until the deprecation date genuinely passes.\n\n## Costs and rate limits\n\nSecondary providers need a warm account, a tested key and a non-zero quota — a fallback you have never exercised is not a fallback. Run a synthetic request through the secondary path daily so you find out it is broken on an ordinary Tuesday rather than during an incident.\n\n## The one-line summary\n\nTreat the model as a **versioned, replaceable dependency with an SLA you do not control**. The abstraction, the pinned version, the second provider and the regression suite are four cheap things that together convert an existential dependency into an ordinary one.",
      takeaways: [
        "Abstract the provider for a fast swap, but be honest that prompts do not transfer for free.",
        "Fail over to a different company, with a circuit breaker and jittered backoff — and design the degraded mode deliberately.",
        "Pin exact model versions and run a daily canary; silent drift happens even without a version change."
      ],
      followUps: [
        "How would you detect silent quality drift on a pinned model?",
        "What is a genuinely useful degraded mode for your product?",
        "Which errors must never be retried, and why?"
      ],
      trap: "Failing over to another region of the same provider. Outages are usually correlated across regions, so the fallback fails at exactly the moment it is needed."
    },

    {
      id: "ai-pii-residency",
      q: "Your AI feature processes customer support tickets containing personal data. What has to be true before it ships?",
      topic: "System Design",
      level: "hard",
      companies: ["salesforce", "microsoft", "palantir", "oracle", "amazon", "glean", "cohere", "flipkart", "tcs"],
      tags: ["privacy", "compliance", "guardrails"],
      aiTrack: true,
      simple: "Sending customer data to a third party is a legal act, not just a technical one. You need to know what data leaves, where it goes, how long it is kept, whether it trains anyone's model, and whether you could delete one person's data on request. Most of that has to be designed in, because it cannot be added later.",
      answer: "## The questions that must have answers before launch\n\n1. **What leaves the building?** Enumerate the fields. \"The ticket text\" is not an answer if the ticket text contains an Aadhaar number.\n2. **Where is it processed?** Which country, which region. India's DPDP Act, the GDPR and sectoral rules (RBI, HIPAA) all care.\n3. **Is it retained, and for how long?** Most providers offer zero-retention or short-retention endpoints. Get it in the contract, not the marketing page.\n4. **Is it used for training?** Default terms differ between consumer and enterprise tiers, and the difference is the whole compliance argument.\n5. **What is the lawful basis, and did the user consent?** Consent given for \"support\" may not cover \"AI processing by a third party\".\n6. **Can you honour deletion?** If a user exercises erasure, what happens to the copy in the provider's logs, in your prompt cache, in your traces?\n\nA candidate who asks these before designing anything has already answered the question.\n\n## The technical controls\n\n**Redact before the boundary.** A detection layer between your system and the provider:\n\n```\nticket text\n   -> regex pass    (Aadhaar, PAN, card numbers, phone, email, IFSC)\n   -> NER pass      (names, addresses, organisations)\n   -> tokenise      \"Ravi Kumar\" -> [PERSON_1], stored in a local map\n   -> send to model\n   -> re-hydrate    [PERSON_1] -> \"Ravi Kumar\" in the response\n```\n\nRegex catches structured identifiers with high precision; NER catches unstructured ones with lower precision. Use both. And be honest about the residual: redaction is not perfect, so it reduces exposure rather than eliminating it, and free-text fields will always leak something.\n\nReversible tokenisation matters — the model needs to refer to \"the customer\" coherently, and a blanket `[REDACTED]` destroys the answer.\n\n**Minimise, do not just redact.** The strongest control is not sending the field at all. Does the summariser need the account number? Usually not.\n\n**Regional endpoints and data residency.** If data must stay in India, use a provider region in India, or self-host. This is increasingly a procurement requirement rather than a preference, and it is a genuine argument for open-weight models.\n\n**Encrypt and scope your own storage.** Traces, prompt logs and evaluation datasets are copies of personal data and inherit every obligation the original had. Teams routinely forget that their observability tool now holds every customer's ticket text.\n\n**Retention and deletion in your own systems.** A 30-day trace retention, a deletion job keyed by subject id, and a documented path that covers logs, caches, vector index and evaluation sets. Deleting the row and leaving the embedding is not deletion.\n\n**Audit log.** Who accessed what, when, and why. In a regulated setting this is what an auditor asks for first.\n\n## The output side\n\n- **Do not let the model echo PII into places it should not go** — a summary posted to a shared channel, an analytics event, a log line.\n- **Filter logging.** The single most common leak is `log.info(prompt)` in a debug branch that reached production.\n- **Watch cross-tenant leakage** through caches and few-shot examples. Never build a dynamic few-shot pool from other customers' data.\n\n## What to say about vendor terms\n\nRead the actual data-processing agreement. Consumer-tier terms and enterprise terms differ on training, retention and sub-processors, and the enterprise agreement is what makes the deployment defensible. If your company has a DPA with the provider, name it; if it does not, that is a blocker to raise now rather than a detail to resolve later.\n\n## The closing judgement\n\nThe strongest answer ends with a scope decision: which categories of ticket go through the AI path at all. Excluding tickets flagged as containing health or financial data is a one-line rule that removes most of the risk, and it is the kind of proposal that gets a feature approved rather than delayed for a quarter.",
      takeaways: [
        "Enumerate exactly which fields cross the boundary, and prefer not sending a field over redacting it.",
        "Traces, prompt logs, caches and evaluation sets are copies of personal data with the same obligations.",
        "Reversible tokenisation preserves coherence where blanket redaction destroys the answer."
      ],
      followUps: [
        "How would you honour a deletion request across logs, cache and vector index?",
        "What is the residual risk after redaction, and how would you quantify it?",
        "When does data residency force you to self-host?"
      ],
      trap: "Treating redaction as sufficient. It is one control among several, it is imperfect on free text, and it does nothing about retention, training use, residency or deletion."
    },

    {
      id: "ai-document-ai",
      q: "Extract structured data from 100,000 scanned invoices. Vision model or OCR pipeline?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["adobe", "microsoft", "amazon", "google", "salesforce", "palantir", "oracle", "tcs", "sarvam"],
      tags: ["multimodal", "extraction", "pipeline"],
      aiTrack: true,
      simple: "You can either read the text out of the page first and then ask a model to interpret it, or hand the page image straight to a model that reads and interprets at once. The second is far easier to build and more expensive to run; the first is cheaper and breaks on messy layouts. Real systems use both, chosen per document.",
      answer: "## The two pipelines\n\n**Classic: OCR then extract.**\n\n```\nPDF -> is there a text layer?\n         yes -> extract text with layout\n         no  -> OCR (Tesseract, PaddleOCR, a cloud OCR)\n     -> layout reconstruction (columns, tables, key-value blocks)\n     -> LLM or rules -> structured JSON -> validation\n```\n\nCheap, fast, debuggable at every stage, and it degrades badly on unusual layouts, handwriting, stamps, rotated scans and multi-column tables.\n\n**Vision-language model: image straight in.**\n\nOne call, image plus a schema, JSON out. Dramatically simpler and it handles layout, tables and handwriting far better because it never discards the spatial information. It costs more per page, latency is higher, and it is harder to explain a wrong answer.\n\n## The pragmatic architecture\n\nRoute by document, do not choose globally:\n\n```\nclassify page\n  |- has a good text layer, standard template   -> OCR path        (~85%)\n  |- scanned, unusual layout, or handwriting    -> VLM path        (~13%)\n  |- low confidence from either                 -> human review    (~2%)\n```\n\nThat routing is the answer. It gets the cost of the cheap path on most volume and the accuracy of the expensive path where it is needed, and it makes the human-review budget explicit.\n\n## Cost at 100,000 documents\n\n| Path | Per page | 100k pages |\n| --- | --- | --- |\n| OCR + small model | ~₹0.4 | ~₹40,000 |\n| VLM, frontier | ~₹4 | ~₹4,00,000 |\n| Routed 85/13/2 with review | ~₹1.1 | ~₹1,10,000 |\n\nProduce a table like this. The interviewer wants to see that you cost a batch job before proposing it.\n\n## Extraction, done properly\n\n- **Constrained output.** A JSON schema with typed fields and enums, enforced at decoding. An invoice total that arrives as `\"₹ 1,23,456/-\"` should be rejected by the schema, not fixed downstream.\n- **Field-level confidence.** Ask for a confidence per field, and — more reliably — cross-check: does `sum(line_items) == subtotal`? Does `subtotal + tax == total`? **Arithmetic self-consistency is the best confidence signal available for invoices**, and it costs nothing.\n- **Bounding boxes or quoted source text per field.** A reviewer must be able to see where a number came from. This turns a two-minute manual re-read into a five-second confirmation and it is what makes the human-review tier affordable.\n- **Never let the model do the arithmetic.** Extract the numbers, compute the totals in code.\n\n## Evaluate per field\n\nA single \"accuracy\" figure is useless. Measure precision and recall **per field**, because `invoice_number` at 99% and `line_items` at 62% need entirely different work, and the aggregate hides it. Add a small hand-labelled set of 200 documents covering every vendor template you know about.\n\n## Operational reality\n\n- **Vendor templates cluster.** Twenty vendors are usually most of the volume. Template-specific handling for the top twenty plus a general path for the tail is far cheaper than a single clever system.\n- **Multi-page documents.** Invoice line items continuing across pages is the classic silent failure — the extractor reports only page one's items and the totals do not reconcile. Your arithmetic check catches this, which is another reason to have it.\n- **Human review is a product, not an afterthought.** Build the queue, the diff view and the correction path. Corrections become training data and evaluation cases.\n- **Idempotency.** Reprocessing must not create duplicate records. Key on a document hash.\n\n## The closing line\n\nSay what the acceptable error rate is and who pays for a mistake. For invoices, a wrong total is a payment error with a financial cost, so the threshold for automatic acceptance should be set from that cost — not from what the model happens to achieve.",
      takeaways: [
        "Route per document: cheap OCR path for the majority, a vision model for the hard tail, human review for the rest.",
        "Arithmetic self-consistency is the strongest confidence signal for financial documents, and it is free.",
        "Measure precision and recall per field; an aggregate accuracy number hides the field that is failing."
      ],
      followUps: [
        "How would you handle line items continuing across a page break?",
        "How do corrections from human review feed back into the system?",
        "When would you fine-tune a small vision model instead of calling a frontier one?"
      ],
      trap: "Asking the model to compute the totals. Extract the numbers and do the arithmetic in code — a model that adds is a model that will occasionally add wrong, silently and plausibly."
    },

    {
      id: "ai-online-experiment",
      q: "You improved the prompt and your offline scores went up 6%. How do you prove it helped real users?",
      topic: "ML & Statistics",
      level: "hard",
      companies: ["meta-ai", "uber", "netflix", "flipkart", "microsoft", "amazon", "adobe", "perplexity", "bytedance"],
      tags: ["experimentation", "metrics", "evaluation"],
      aiTrack: true,
      simple: "Offline scores measure whether the output changed in a way your metric likes. Only a live experiment tells you whether people were better off. You split traffic, decide the metric and the sample size before you look, and run it long enough that a weekday-weekend difference does not decide the result for you.",
      answer: "## Why offline is not enough\n\nOffline evaluation measures agreement with your own judgement, on a set you built. It cannot see whether users understood the answer, trusted it, acted on it, or came back. Offline-to-online gaps of the sign-flipping kind are routine — a change that scores better on faithfulness can be worse for users if it hedges more.\n\n## Pick the metric before you run it\n\nOne **primary** metric, decided in advance, that reflects user value:\n\n| Product | Primary |\n| --- | --- |\n| Support assistant | Deflection rate — resolved without a human |\n| Search / answers | Session success, or query reformulation rate (lower is better) |\n| Coding assistant | Suggestion acceptance and retention of accepted code |\n| Writing tool | Output kept or edited rather than discarded |\n\nAnd **guardrail** metrics that must not regress: latency, cost per session, error rate, escalation rate, and safety-filter trigger rate. A change that improves quality and doubles cost is not automatically a win — the guardrails are what force that conversation.\n\n## Size it before you start\n\nFor a proportion metric with baseline $p$ and minimum detectable effect $\\delta$, per arm:\n\n$$n \\approx \\frac{16\\,p(1-p)}{\\delta^2}$$\n\nBaseline 40% deflection, wanting to detect a 2-point absolute change: $n \\approx 16 \\times 0.24 / 0.0004 = 9{,}600$ sessions per arm. At 2,000 sessions a day that is about ten days.\n\nDoing this arithmetic before launching is the difference between an experiment and a hunch. It also frequently reveals that you cannot detect the effect you care about at your traffic — which is worth knowing on day zero rather than day fourteen.\n\n## Randomise on the right unit\n\n**By user, not by request.** A user who sees both variants within a session gives you a contaminated comparison and an inconsistent experience. Hash the user id, assign stably, and keep the assignment for the whole experiment.\n\nWatch for interference: in a shared-cache system one arm's cache entries can serve the other. Namespace caches by variant.\n\n## Run it honestly\n\n- **A full week minimum.** Weekday and weekend traffic differ in composition, not just volume.\n- **No peeking.** Checking daily and stopping at the first significant result inflates the false-positive rate substantially. Fix the duration in advance, or use a sequential test designed for continuous monitoring.\n- **Run an A/A test first** if you have never validated the framework. If two identical arms show a significant difference, your assignment or your metric pipeline is broken and every result you have ever shipped is suspect.\n- **Segment afterwards, hypothesise before.** Slicing twenty ways until something is significant is how teams ship changes that do nothing. Pre-register the two or three segments you actually expect to differ.\n\n## Interpreting it\n\nReport the effect size with a confidence interval, not a p-value alone. \"+1.8 points deflection, 95% CI [0.4, 3.2]\" is decision-ready; \"p = 0.03\" is not. If the interval includes effects too small to be worth the cost, say so.\n\nAnd if the result is flat: that is a real finding. A 6% offline gain that produces nothing online means your offline metric is not measuring what users care about — which is more valuable to learn than the prompt change was.\n\n## When you cannot A/B test\n\nLow traffic, a B2B product with twelve customers, or a change that cannot be split. Then: interrupted time series with a control metric, a staged rollout watched closely, or a blind side-by-side human review of 200 real outputs. Say which you would use and why, rather than insisting on an experiment the traffic cannot support.",
      takeaways: [
        "Pick one primary metric and explicit guardrails before launching; a quality win that doubles cost is a decision, not a win.",
        "Size the experiment first — it often reveals you cannot detect the effect you care about at your traffic.",
        "Randomise by user, run a full week, and do not stop at the first significant reading."
      ],
      followUps: [
        "What would an A/A test failure tell you?",
        "How do you experiment with only twelve enterprise customers?",
        "Your offline metric improved and online was flat — what do you change?"
      ],
      trap: "Randomising per request. The user sees both variants, the experience is inconsistent, and the comparison is contaminated in a way no amount of statistics repairs."
    },

    {
      id: "ai-guardrail-layer",
      q: "Design the safety layer for a consumer-facing assistant. What is the latency and false-positive budget?",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["openai", "anthropic", "microsoft", "google", "meta-ai", "adobe", "bytedance", "salesforce", "sarvam"],
      tags: ["safety", "guardrails", "moderation"],
      aiTrack: true,
      simple: "You need checks on what goes in and what comes out, and those checks cost time and sometimes block things they should not. Designing it well means deciding, per category, what you must never allow, what you accept a bit of over-blocking on, and what you monitor rather than block.",
      answer: "## Layers, and what each is for\n\n```\ninput  -> rate limit / abuse           (cheap, before anything)\n       -> input classifier             (~20-50 ms)\n       -> prompt-injection check       (only for untrusted content)\n MODEL\noutput -> deterministic checks         (schema, PII regex, forbidden strings)\n       -> output classifier            (~30-80 ms, or streamed)\n       -> citation / grounding check   (for RAG)\n       -> log + sample for review\n```\n\n**Deterministic checks come first and are free.** Schema validity, a card-number regex, a blocklist of internal hostnames, a check that every citation id exists. These are exact, they run in microseconds, and they catch a surprising share of real incidents. Reaching straight for a classifier without them is a common mistake.\n\n## Set the budget explicitly\n\nA safety layer that adds 400 ms to every request will be turned off by the first person who owns latency. Realistic budget for a consumer assistant:\n\n| Stage | Budget |\n| --- | --- |\n| Input classifier | 50 ms, run in parallel with retrieval |\n| Output deterministic | < 5 ms |\n| Output classifier | 80 ms, or on a rolling window during streaming |\n| **Total added** | **< 150 ms** |\n\nTwo tricks buy most of that back. **Run the input classifier concurrently with retrieval**, since neither depends on the other. **Classify the output on a rolling window while streaming**, cutting the stream if it trips, rather than buffering the whole answer.\n\n## The false-positive question is the real one\n\nEvery category needs a decided threshold, and the right threshold differs by category because the cost of each error differs:\n\n| Category | Miss costs | False positive costs | Setting |\n| --- | --- | --- | --- |\n| CSAM, credible violence | Catastrophic, legal | An annoyed user | **Aggressive. Over-block.** |\n| Self-harm | Severe | A user in distress refused help | Aggressive, and **route to resources, do not just refuse** |\n| Medical / legal advice | Liability | A blocked legitimate question | Moderate, with a disclaimer path |\n| Profanity | Minor | Blocks normal speech and whole dialects | **Loose. Monitor, do not block** |\n| Competitor mentions | Nothing | Looks absurd | Do not build it |\n\nThat table is the answer. A candidate who proposes one threshold for everything has not thought about who pays for each mistake.\n\n**Measure the false-positive rate on real traffic**, not on a red-team set. A 2% false-positive rate on a million requests is twenty thousand wrongly blocked users a day, and support will hear about it before your dashboard does.\n\n## Refusal quality\n\nA refusal is a product surface. \"I can't help with that\" is a bad experience and drives users to jailbreak. Better: say what you cannot do, why, and what you can do instead — and for sensitive categories, route to real resources rather than a wall.\n\n## Prompt injection is a different problem\n\nContent moderation asks *is this harmful*. Injection asks *is this content trying to issue instructions*. Any system that reads untrusted text — web pages, uploaded documents, emails — needs the second, and the controls are architectural rather than classifier-based:\n\n- **Untrusted content is data, never instruction.** Mark its boundaries explicitly and instruct the model accordingly. Helps; is not sufficient.\n- **The real control is the tool layer.** The agent's permissions must be scoped so that even a fully successful injection cannot do serious damage: read-only by default, no exfiltration path, confirmation on writes, allow-listed outbound domains.\n- **Assume the classifier will be bypassed.** Design so the blast radius is small rather than betting on perfect detection.\n\n## Operating it\n\n- **Sample and human-review** both blocked and allowed traffic weekly. You cannot see your false negatives any other way.\n- **Version and evaluate the guardrails** like any model: a labelled set, precision and recall per category, and a regression gate.\n- **An appeal path.** Users must be able to report a wrong block, and those reports become test cases.\n- **Fail open or closed, deliberately.** If the classifier is down, does the product stop or does it serve unchecked? Decide it per category, write it down, and test it.",
      takeaways: [
        "Deterministic checks first — exact, free, and they catch real incidents before any classifier runs.",
        "Thresholds are per category, set by who pays for each kind of error, not by one global number.",
        "Against prompt injection the real control is scoped tool permissions, not better detection."
      ],
      followUps: [
        "How do you moderate a streamed response without buffering it?",
        "What is your fail-open or fail-closed policy if the classifier is unavailable?",
        "How would you measure the false-positive rate on live traffic?"
      ],
      trap: "Setting one threshold for every category. The cost of a miss and the cost of a false positive differ by orders of magnitude between self-harm and profanity, and one number cannot serve both."
    },

    /* ================= the design rounds ================= */
    {
      id: "ai-design-enterprise-assistant",
      q: "Design an AI assistant over ten million internal company documents.",
      topic: "System Design",
      level: "staff",
      companies: ["glean", "microsoft", "google", "amazon", "salesforce", "palantir", "databricks", "snowflake", "oracle", "cohere"],
      tags: ["rag", "system-design", "scale"],
      aiTrack: true,
      simple: "This is the standard AI system design round. The interviewer is not looking for a diagram of a RAG pipeline — everyone draws that. They are looking for whether you talk about permissions, freshness, evaluation and cost without being asked.",
      answer: "## Clarify first, for two minutes\n\nAsk, and state the assumptions you proceed with:\n\n- **Users and traffic.** 20,000 employees, ~5 queries/day each ≈ 100k queries/day, peaking around 5 per second.\n- **Documents.** 10M documents. What kinds? Confluence, Google Docs, PDFs, Slack, tickets, code. Each needs a different connector and a different chunking strategy.\n- **Freshness.** Minutes or days? This single answer decides the entire ingestion architecture.\n- **Permissions.** Almost certainly per-document. Assume yes.\n- **Latency target.** 3 seconds to first token is acceptable for internal search.\n- **Budget.** Ask. It changes the model choice and the index choice.\n\nCandidates who design before asking about permissions and freshness are marked down here more than for any technical gap.\n\n## Size it\n\n10M documents × ~8 chunks = **80M chunks**. At 768 dimensions FP32 that is 245 GB of raw vectors, so quantised or PQ-compressed indexing and sharding are required, not optional. Embedding the corpus once: ~40B tokens, roughly ₹70,000–1,50,000 depending on the model. Full re-embedding is therefore a decision, not a routine operation — which is why the embedding-model choice deserves real evaluation up front.\n\n## Ingestion\n\n```\nconnectors (Confluence, Drive, Slack, Jira, GitHub)\n   -> change-data-capture where available, polling where not\n   -> parse per type (layout-aware for PDF, structure for Markdown/code)\n   -> chunk with heading path + contextual prefix\n   -> content-hash each chunk\n   -> embed only chunks whose hash changed\n   -> upsert into vector index + keyword index + metadata store\n```\n\n**Content hashing is the key economic decision.** A document edited daily should cost one or two chunk re-embeddings, not eighty. Without it your ingestion bill is unbounded.\n\nPermissions are synced separately and more frequently than content, because a revoked access must take effect in seconds while a document edit can take minutes.\n\n## Retrieval\n\n```\nquery\n -> resolve user's expanded group set (cached, short TTL)\n -> query understanding: acronym expansion, spelling, intent\n -> parallel:  dense (ANN, filtered by ACL)  +  BM25 (filtered by ACL)\n -> reciprocal rank fusion -> ~80 candidates\n -> cross-encoder rerank -> top 8\n -> assemble context (best chunk first) -> generate -> cite\n```\n\nThings to raise unprompted:\n\n- **Hybrid, always.** Internal corpora are full of product codenames, ticket ids and acronyms that dense retrieval fails on.\n- **ACL filtering during traversal**, never after — see the permissions question.\n- **Recency and authority as ranking signals.** An official policy page should outrank a two-year-old Slack thread saying the opposite. Pure semantic similarity cannot tell them apart, and this is the failure users complain about most in internal search.\n- **Personalisation.** The same query from someone in finance and someone in engineering wants different documents. Team and past-behaviour signals in the ranker.\n\n## Serving\n\n- Stateless API, autoscaled; streaming responses.\n- **Cache in three places**: user group expansion (seconds), retrieval results keyed by query + permission-scope hash (minutes), provider prefix cache for the system prompt (free).\n- Vector index sharded by tenant or department, replicated for read throughput.\n- Model routing: a small model for lookups, the larger one for synthesis.\n\n## Evaluation, which most candidates omit\n\n- A golden set of 200 real employee queries with correct documents, **each attached to a user** so permissions are exercised.\n- Retrieval recall@10 and generation faithfulness tracked nightly, **segmented by source system and by department**, because quality is never uniform and the aggregate hides the failing connector.\n- A leak test: an adversarial user who should see nothing must retrieve nothing.\n- Online: click-through on citations, reformulation rate, escalation to a human, thumbs.\n\n## Cost, stated concretely\n\n100k queries/day at ~5k input and 400 output tokens with a mid-tier model is roughly ₹1.5–3 lakh a month, plus index hosting and ingestion. Then name the levers: rerank to fewer chunks, route by difficulty, cache aggressively, and use a small model for the 60% of queries that are lookups.\n\n## Close on failure modes\n\nStale permissions, a connector silently failing (monitor document counts per source, not just job status), the corpus containing contradictory documents, and the assistant confidently answering from an obsolete policy page. For the last one, surface the document date next to the answer — a small product decision that removes a large class of complaint.",
      takeaways: [
        "Ask about permissions and freshness before designing; they determine the architecture more than scale does.",
        "Content-hash chunks so an edited document costs two re-embeddings rather than eighty.",
        "Recency and authority signals matter as much as similarity — an old Slack thread must not outrank the policy page."
      ],
      followUps: [
        "How do you handle a connector that silently stops syncing?",
        "What changes if freshness must be under 30 seconds?",
        "How would you segment your quality metrics to find a failing source?"
      ],
      trap: "Drawing the standard RAG pipeline and stopping. Everyone draws it. The differentiators are permissions, freshness, per-segment evaluation and cost — raise all four unprompted."
    },

    {
      id: "ai-design-copilot",
      q: "Design an inline code-completion assistant. You have 200 ms.",
      topic: "System Design",
      level: "staff",
      companies: ["microsoft", "google", "meta-ai", "anthropic", "openai", "nvidia", "databricks", "adobe", "huggingface"],
      tags: ["latency", "system-design", "code"],
      aiTrack: true,
      simple: "A suggestion that appears after a second is worse than no suggestion, because the developer has already typed the next line. Everything about this design follows from the latency budget: a small model, a short prompt, aggressive cancellation, and a decision about when not to ask at all.",
      answer: "## The budget decides the architecture\n\nA developer types roughly 4–8 characters a second. A suggestion is useful only if it arrives before they have moved on — so the target is **under 200 ms end to end at p50**, and above ~500 ms the feature is worse than nothing because it flickers in after the decision was made.\n\n```\nkeystroke -> debounce           30 ms\n          -> build context      10 ms  (local, in the editor)\n          -> network            20 ms  (regional endpoint, kept-alive connection)\n          -> prefill + decode  120 ms  (small model, short output)\n          -> render             10 ms\n                               ------\n                               ~190 ms\n```\n\nEvery design choice below is a consequence of that table.\n\n## Model choice\n\nA **1–7B model, fine-tuned for fill-in-the-middle**, not a frontier chat model. FIM training is what lets the model use the code *after* the cursor:\n\n```\n<PRE> code before cursor <SUF> code after cursor <MID> -> completion\n```\n\nThis matters more than model size. A small FIM model beats a large chat model at this task because it was trained on the actual shape of the problem.\n\nQuantised, served with continuous batching, warm, in the user's region. Cold starts are unacceptable here.\n\n## Context assembly, in the editor\n\nThe budget allows roughly 2–4k tokens of prompt. Spend it deliberately:\n\n1. The current file around the cursor — the most valuable tokens by a distance.\n2. Imports and type signatures from the current file.\n3. Neighbouring open tabs, ranked by recency and by symbol overlap with the current file.\n4. Definitions of symbols visible near the cursor, pulled from a local index (LSP or a lightweight symbol table).\n\nA local repository index makes the difference between generic completions and completions that use your own helper functions. Build it incrementally in the background; never on the request path.\n\n## Deciding not to ask\n\nThe cheapest millisecond is the request you never make, and this is where most of the win is:\n\n- **Debounce** 20–40 ms after the last keystroke.\n- **Cancel in flight** on the next keystroke. Without this you queue stale requests behind fresh ones and everything gets slower.\n- **Suppress in dead positions** — inside a string or comment where a suggestion is unwanted, immediately after a completed statement, mid-word.\n- **Client-side cache** keyed by the context hash. Backspace then retype is very common and should never hit the network.\n- **Speculative prefetch** while the developer reads.\n\nA good gating layer removes 40–60% of requests, which is both a latency and a cost win.\n\n## Streaming and stopping\n\nStream tokens and render progressively. Stop early and deliberately: at a line end for single-line mode, at a matching brace for block mode, and on any line that duplicates what already exists after the cursor. An over-long suggestion is worse than a short one — it is more likely to be wrong and more work to reject.\n\n## Measuring it\n\nThe metric is **acceptance rate** — suggestions accepted divided by suggestions shown — plus **retention**, the share of accepted characters still present in the file after 10 minutes. Retention is the honest one: accepted-then-deleted is a failure that acceptance rate scores as a success.\n\nAlso: p50 and p99 latency, suggestion rate (how often you offer at all), and cost per accepted completion. That last number is the one that decides whether the feature is viable.\n\nGood systems sit in the 20–35% acceptance range. Anything much higher usually means you are only suggesting the trivially predictable.\n\n## Cost control\n\nOnly a fraction of suggestions are accepted, so most of the compute is discarded by construction. Levers: the gating layer above, a smaller model for single-line completions with a larger one reserved for explicit multi-line requests, and per-user rate limits.\n\n## Safety and trust\n\n- **Never send code from a repository the user has not opted in for**, and honour a `.copilotignore`-style exclusion. Getting this wrong is an enterprise-blocking incident.\n- **Filter suggestions that reproduce long verbatim spans** from training data — a licensing hazard, checked with a suffix-array or hash lookup rather than a model.\n- **Never suggest a secret.** Filter anything resembling a key or token in the output.\n\n## The judgement to state\n\nThis is a latency product with a model in it, not a model product. The engineering that matters is caching, gating, cancellation and context assembly — and being explicit about that is what separates a staff-level answer from a description of an API call.",
      takeaways: [
        "Under 200 ms p50 or the feature is counterproductive — every choice follows from that budget.",
        "A small fill-in-the-middle model beats a large chat model here, because FIM matches the task shape.",
        "Retention of accepted code, not acceptance rate, is the honest quality metric."
      ],
      followUps: [
        "How would you rank which open tabs to include in the context?",
        "Why is cancelling in-flight requests essential rather than an optimisation?",
        "How do you detect verbatim reproduction of training data cheaply?"
      ],
      trap: "Proposing a frontier model. It cannot meet the latency budget, it costs many times more per suggestion on a feature where most output is discarded, and it is not trained for fill-in-the-middle."
    },

    /* ================= the coding round ================= */
    {
      id: "ai-code-attention",
      q: "Implement multi-head self-attention with a causal mask, from scratch. No `nn.MultiheadAttention`.",
      topic: "LLM & GenAI",
      level: "medium",
      companies: ["openai", "anthropic", "google-deepmind", "meta-ai", "nvidia", "mistral", "huggingface", "microsoft", "scale-ai"],
      tags: ["coding", "attention", "pytorch"],
      aiTrack: true,
      simple: "This is the standard AI-engineer coding round. It is not hard once you have written it twice, and interviewers use it because almost nobody has. Write it out three times before your interviews and this round becomes free marks.",
      answer: "## The reference implementation\n\n```python\nimport torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass MultiHeadSelfAttention(nn.Module):\n    def __init__(self, d_model: int, n_heads: int, dropout: float = 0.0):\n        super().__init__()\n        assert d_model % n_heads == 0, \"d_model must divide by n_heads\"\n        self.n_heads = n_heads\n        self.d_head  = d_model // n_heads\n\n        # One fused projection for Q, K, V is faster than three separate ones.\n        self.qkv  = nn.Linear(d_model, 3 * d_model, bias=False)\n        self.proj = nn.Linear(d_model, d_model, bias=False)\n        self.drop = nn.Dropout(dropout)\n\n    def forward(self, x, mask=None):\n        B, T, C = x.shape                      # batch, time, channels\n\n        qkv = self.qkv(x)                      # (B, T, 3C)\n        q, k, v = qkv.split(C, dim=-1)\n\n        # (B, T, C) -> (B, n_heads, T, d_head)\n        def heads(t):\n            return t.view(B, T, self.n_heads, self.d_head).transpose(1, 2)\n        q, k, v = heads(q), heads(k), heads(v)\n\n        att = (q @ k.transpose(-2, -1)) / (self.d_head ** 0.5)   # (B, h, T, T)\n\n        causal = torch.ones(T, T, dtype=torch.bool, device=x.device).tril()\n        att = att.masked_fill(~causal, float(\"-inf\"))\n        if mask is not None:                                     # padding mask\n            att = att.masked_fill(~mask[:, None, None, :], float(\"-inf\"))\n\n        att = self.drop(F.softmax(att, dim=-1))\n\n        out = att @ v                                            # (B, h, T, d_head)\n        out = out.transpose(1, 2).contiguous().view(B, T, C)\n        return self.proj(out)\n```\n\n## The five things they are checking\n\n**1. The $\\sqrt{d_k}$ division.** With $q, k$ of unit-variance components, the dot product over $d_k$ dimensions has variance $d_k$. At $d_k = 64$ the logits reach ±8 and softmax saturates towards one-hot, so gradients vanish. Dividing by $\\sqrt{d_k}$ restores unit variance. Be ready to say this; it is the most common follow-up in the entire round.\n\n**2. Masking with `-inf` before the softmax**, not zeroing after. Zeroing after softmax leaves the denominator wrong, so the remaining weights no longer sum to one — a bug that trains to a worse model rather than crashing, which is why it is a favourite.\n\n**3. `.transpose(1, 2)` then `.contiguous()` before `.view()`.** Transposing produces a non-contiguous tensor and `.view()` will throw. Knowing this is a small but real signal of having written PyTorch rather than read it.\n\n**4. Heads are a reshape, not a loop.** Splitting the channel dimension into heads keeps everything as one batched matmul.\n\n**5. The output projection.** Concatenating heads and skipping `self.proj` is a common omission — the projection is what lets heads combine rather than merely be concatenated.\n\n## The follow-ups to expect\n\n**\"Make it faster.\"** `F.scaled_dot_product_attention(q, k, v, is_causal=True)` dispatches to FlashAttention where available: same result, never materialises the $T \\times T$ matrix, memory drops from $O(T^2)$ to $O(T)$.\n\n**\"Add a KV cache.\"** Keep `k` and `v` across steps; on each new token compute only its q, k, v, concatenate to the cache, and attend. No causal mask needed during single-token decode — the cache only contains the past.\n\n**\"Make it grouped-query attention.\"** Project fewer K/V heads and `repeat_interleave` them across query-head groups. Cache shrinks by the group factor.\n\n**\"Complexity?\"** Time $O(T^2 d)$, memory $O(T^2)$ for the naive version — which is the whole reason FlashAttention exists.\n\n## Practise it as a drill\n\nWrite it from memory, run it on a `(2, 5, 32)` tensor with 4 heads, and assert the output shape and that the attention rows sum to one. Twenty minutes, three times, and it stops being a risk.",
      takeaways: [
        "Divide by √d_head or the softmax saturates and gradients vanish — know why, not just that.",
        "Mask with -inf before softmax; zeroing after leaves an incorrect denominator.",
        "Transpose then .contiguous() before .view(), and do not forget the output projection."
      ],
      followUps: [
        "Add a KV cache for single-token decoding.",
        "Convert it to grouped-query attention.",
        "What does F.scaled_dot_product_attention change about the memory profile?"
      ],
      trap: "Applying the mask after the softmax. It looks right, produces a wrong distribution, and trains to a quietly worse model instead of raising an error."
    },

    {
      id: "ai-code-sampling",
      q: "Implement temperature, top-k and top-p sampling. Where is the off-by-one?",
      topic: "LLM & GenAI",
      level: "medium",
      companies: ["openai", "anthropic", "mistral", "huggingface", "nvidia", "meta-ai", "perplexity", "microsoft"],
      tags: ["coding", "sampling", "decoding"],
      aiTrack: true,
      simple: "The model gives you a score for every possible next token. Sampling turns those scores into a choice. Temperature flattens or sharpens the distribution, top-k keeps the best k options, and top-p keeps the smallest set whose probabilities add up to p. The classic bug is in the boundary of that last one.",
      answer: "## The implementation\n\n```python\nimport torch\nimport torch.nn.functional as F\n\ndef sample(logits, temperature=1.0, top_k=None, top_p=None):\n    \"\"\"logits: (vocab,) for one position. Returns a token id.\"\"\"\n\n    # Greedy: temperature 0 means argmax, and dividing by zero is not it.\n    if temperature == 0:\n        return int(torch.argmax(logits))\n\n    logits = logits / temperature\n\n    if top_k is not None:\n        k = min(top_k, logits.size(-1))\n        kth = torch.topk(logits, k).values[-1]\n        logits = logits.masked_fill(logits < kth, float(\"-inf\"))\n\n    if top_p is not None:\n        sorted_logits, sorted_idx = torch.sort(logits, descending=True)\n        probs = F.softmax(sorted_logits, dim=-1)\n        cumulative = torch.cumsum(probs, dim=-1)\n\n        # Remove tokens once the cumulative mass has ALREADY exceeded p.\n        remove = cumulative > top_p\n        # Shift right so the token that crosses the threshold is kept,\n        # and force-keep the top token so we can never remove everything.\n        remove[1:] = remove[:-1].clone()\n        remove[0]  = False\n\n        logits = logits.masked_fill(\n            torch.zeros_like(remove).scatter(0, sorted_idx, remove),\n            float(\"-inf\"),\n        )\n\n    probs = F.softmax(logits, dim=-1)\n    return int(torch.multinomial(probs, num_samples=1))\n```\n\n## The off-by-one\n\nIt is the two lines shifting `remove`.\n\nTop-p means *the smallest set whose cumulative probability is at least p*. Without the shift you remove every token whose cumulative mass exceeds p — including **the token that crossed the threshold**, which must be kept, because without it the retained mass is strictly less than p.\n\nWorse, in the degenerate case where the top token alone already exceeds p (very common — a confident model at p=0.9 with a 0.95 top token), the unshifted version removes *everything* and `multinomial` raises on an all-`-inf` distribution. Forcing `remove[0] = False` is the guard.\n\nThat is the whole question, and the interviewer will usually just watch to see whether you write it correctly rather than asking.\n\n## The parameters, honestly\n\n| Parameter | Effect | Sensible range |\n| --- | --- | --- |\n| **temperature** | Divides the logits. <1 sharpens, >1 flattens | 0 for extraction and code; 0.7–1.0 for prose |\n| **top_k** | Fixed cutoff at k tokens | 40–100, or unset |\n| **top_p** | Adaptive cutoff by mass | 0.9–0.95 |\n\n**Order matters**: temperature, then top-k, then top-p. Applying temperature after truncation changes which tokens survive.\n\n**Why top-p usually beats top-k**: it adapts. When the model is confident, the nucleus is 2 tokens; when uncertain, 200. A fixed k either admits rubbish in confident positions or truncates legitimate variety in uncertain ones.\n\n## Follow-ups worth having ready\n\n**Repetition penalty.** Divide the logits of already-generated tokens by a factor (1.1–1.2). Be ready to say it is a blunt tool that damages legitimate repetition — code indentation, list formatting, a name that must recur.\n\n**Min-p.** Keep tokens with probability at least `min_p × max_prob`. Scales with confidence, more robust than top-p at high temperature, increasingly the recommended default.\n\n**Batched version.** Everything above generalises to `(B, vocab)` with `dim=-1` and per-row masking; `torch.multinomial` handles batches natively.\n\n**Why temperature 0 for structured output.** Determinism, reproducibility, and the fact that creativity is exactly the wrong property when the output must match a schema. Note that temperature 0 is still not bit-identical across batch sizes on GPU — floating-point reduction order changes. Knowing that surprises most interviewers pleasantly.",
      takeaways: [
        "Top-p must keep the token that crosses the threshold, and must never remove every token.",
        "Apply temperature before truncation — the order changes which tokens survive.",
        "Temperature 0 is right for extraction and code, and still not bit-reproducible across batch sizes on GPU."
      ],
      followUps: [
        "Implement min-p and explain why it is more robust at high temperature.",
        "How would you batch this across a whole decoding step?",
        "Why is a repetition penalty dangerous for code generation?"
      ],
      trap: "Filtering with `cumulative > top_p` and no shift. It drops the boundary token, and it throws on a confident distribution where the top token alone exceeds p."
    },

    {
      id: "ai-code-bpe",
      q: "Implement byte-pair encoding: train the merges on a corpus, then encode a string.",
      topic: "LLM & GenAI",
      level: "hard",
      companies: ["openai", "anthropic", "huggingface", "mistral", "cohere", "google-deepmind", "sarvam", "meta-ai"],
      tags: ["coding", "tokenisation", "algorithms"],
      aiTrack: true,
      simple: "Tokenisation splits text into the pieces a model actually sees. BPE builds those pieces by starting from single characters and repeatedly merging whichever adjacent pair is most frequent, until it has the vocabulary size you asked for. Writing it is a satisfying hour and it explains a long list of strange model behaviours.",
      answer: "## Training\n\n```python\nfrom collections import Counter\n\ndef train_bpe(corpus: list[str], vocab_size: int):\n    \"\"\"Returns an ordered list of merges. Byte-level: no unknown tokens ever.\"\"\"\n    # Start from raw bytes so any input is representable.\n    words = Counter()\n    for line in corpus:\n        for word in line.split(\" \"):\n            if word:\n                # A leading space is part of the token, which is why GPT-style\n                # tokenisers distinguish \"hello\" from \" hello\".\n                words[tuple(bytes(\" \" + word, \"utf-8\"))] += 1\n\n    merges: list[tuple[int, int]] = []\n    next_id = 256                       # 0-255 are the byte tokens\n\n    while next_id < vocab_size:\n        pairs = Counter()\n        for symbols, freq in words.items():\n            for a, b in zip(symbols, symbols[1:]):\n                pairs[(a, b)] += freq\n\n        if not pairs:\n            break\n        best = max(pairs, key=pairs.get)\n        if pairs[best] < 2:             # nothing left worth merging\n            break\n\n        merges.append(best)\n        words = Counter({_merge(sym, best, next_id): f for sym, f in words.items()})\n        next_id += 1\n\n    return merges\n\n\ndef _merge(symbols, pair, new_id):\n    out, i = [], 0\n    while i < len(symbols):\n        if i < len(symbols) - 1 and (symbols[i], symbols[i + 1]) == pair:\n            out.append(new_id)\n            i += 2\n        else:\n            out.append(symbols[i])\n            i += 1\n    return tuple(out)\n```\n\n## Encoding\n\nThe critical detail: apply merges **in training order**, not greedily by length.\n\n```python\ndef encode(text: str, merges: list[tuple[int, int]]) -> list[int]:\n    ids = list(text.encode(\"utf-8\"))\n    rank = {pair: i for i, pair in enumerate(merges)}\n\n    while len(ids) >= 2:\n        # The lowest-rank (earliest-learned) adjacent pair merges first.\n        pairs = {(a, b) for a, b in zip(ids, ids[1:])}\n        best = min(pairs, key=lambda p: rank.get(p, float(\"inf\")))\n        if best not in rank:\n            break\n        ids = list(_merge(tuple(ids), best, 256 + rank[best]))\n    return ids\n\n\ndef decode(ids: list[int], merges) -> str:\n    table = {i: bytes([i]) for i in range(256)}\n    for i, (a, b) in enumerate(merges):\n        table[256 + i] = table[a] + table[b]\n    return b\"\".join(table[i] for i in ids).decode(\"utf-8\", errors=\"replace\")\n```\n\nApplying merges out of order produces a *different* tokenisation of the same string — which would not match training and would silently degrade the model. Getting the ordering right is the main thing being tested.\n\n## Why byte-level\n\nStarting from the 256 byte values means **there is no unknown token, ever**. Any Unicode string — emoji, Devanagari, a corrupted file — is representable. Character-level BPE needs an `<UNK>` and a vocabulary of every character it might meet.\n\n## What this explains\n\n- **Counting letters.** The model never sees letters. \"strawberry\" may be two or three tokens, so counting the `r`s is a question about something it cannot observe.\n- **Arithmetic.** \"1234\" might be `123|4` or `12|34` depending on frequency; digit grouping is inconsistent, so column-wise arithmetic is genuinely hard. Some recent tokenisers deliberately split digits individually for this reason.\n- **The leading space.** `\"hello\"` and `\" hello\"` are different tokens. A prompt ending in a space frequently produces worse output, and this is why.\n- **Cost by language.** English is roughly 4 characters per token; Hindi in Devanagari can be 1–2, so the same sentence costs several times more tokens. For an Indian product this is a real budget line, not a curiosity.\n- **Glitch tokens.** Strings present in the tokeniser's training corpus but absent from the model's produce untrained embeddings and bizarre behaviour — the `SolidGoldMagikarp` family.\n\n## Complexity and the real implementation\n\nThe naive trainer is $O(V \\times N)$ — recounting every pair each merge. Real implementations keep an index from pair to the positions it occurs at and update incrementally. Mention this if asked to scale it; a naive trainer on a large corpus takes hours.\n\nAt inference, production tokenisers pre-split with a regex (contractions, letters, digits, punctuation, whitespace runs) before applying merges, so a merge can never span a word boundary in a way that hurts.",
      takeaways: [
        "Merges must be applied in training order; greedy longest-match gives a different, wrong tokenisation.",
        "Byte-level start means no unknown token is possible for any Unicode input.",
        "Tokenisation explains letter-counting failures, arithmetic weakness, the leading-space effect, and why Indic text costs several times more."
      ],
      followUps: [
        "How would you make the trainer efficient on a large corpus? (Incremental pair index.)",
        "Why do production tokenisers pre-split with a regex?",
        "What is a glitch token and how would you find one?"
      ],
      trap: "Encoding by greedily matching the longest known token. BPE is defined by merge order, and a longest-match encoder produces token sequences the model was never trained on."
    },

    {
      id: "ai-code-llm-client",
      q: "Write the LLM client you would actually ship: retries, rate limiting, timeouts, streaming, cost tracking.",
      topic: "OOP & Code Quality",
      level: "medium",
      companies: ["anthropic", "openai", "microsoft", "amazon", "salesforce", "flipkart", "adobe", "perplexity", "sarvam", "glean"],
      tags: ["coding", "reliability", "backend"],
      aiTrack: true,
      simple: "Calling a model API in a tutorial is one line. Calling it in production means handling rate limits, transient failures, slow responses, cancelled requests and a bill you need to attribute. This is the code you will actually write in the job, and it is what a practical coding round asks for.",
      answer: "## The implementation\n\n```python\nimport asyncio, random, time\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Usage:\n    input_tokens: int = 0\n    output_tokens: int = 0\n    cached_tokens: int = 0\n\n\nclass TokenBucket:\n    \"\"\"Client-side rate limit. Being polite is cheaper than being throttled.\"\"\"\n    def __init__(self, rate_per_sec: float, burst: float):\n        self.rate, self.capacity = rate_per_sec, burst\n        self.tokens, self.updated = burst, time.monotonic()\n        self._lock = asyncio.Lock()\n\n    async def take(self, n: float = 1.0):\n        async with self._lock:\n            while True:\n                now = time.monotonic()\n                self.tokens = min(self.capacity,\n                                  self.tokens + (now - self.updated) * self.rate)\n                self.updated = now\n                if self.tokens >= n:\n                    self.tokens -= n\n                    return\n                await asyncio.sleep((n - self.tokens) / self.rate)\n\n\nclass RetryableError(Exception): ...\nclass FatalError(Exception): ...\n\n\nclass LLMClient:\n    def __init__(self, transport, *, rpm=600, max_concurrency=32,\n                 timeout_s=60.0, max_retries=4, on_usage=None):\n        self.transport   = transport\n        self.bucket      = TokenBucket(rpm / 60, burst=rpm / 6)\n        self.sem         = asyncio.Semaphore(max_concurrency)\n        self.timeout_s   = timeout_s\n        self.max_retries = max_retries\n        self.on_usage    = on_usage          # (model, Usage, latency_ms) -> None\n\n    async def complete(self, messages, *, model, max_tokens=1024, **kw):\n        attempt, started = 0, time.monotonic()\n        while True:\n            try:\n                await self.bucket.take()\n                async with self.sem:                       # cap in-flight work\n                    async with asyncio.timeout(self.timeout_s):\n                        resp = await self.transport.send(\n                            messages, model=model, max_tokens=max_tokens, **kw\n                        )\n                if self.on_usage:\n                    self.on_usage(model, resp.usage,\n                                  (time.monotonic() - started) * 1000)\n                return resp\n\n            except FatalError:\n                raise                                       # 400s: never retry\n            except (RetryableError, asyncio.TimeoutError) as e:\n                attempt += 1\n                if attempt > self.max_retries:\n                    raise\n                # Exponential backoff with FULL jitter. Without the jitter,\n                # every client retries in lockstep and re-creates the spike.\n                delay = random.uniform(0, min(30.0, 2 ** attempt))\n                retry_after = getattr(e, \"retry_after\", None)\n                await asyncio.sleep(max(delay, retry_after or 0))\n\n    async def stream(self, messages, *, model, **kw):\n        \"\"\"Yields deltas. Counts output tokens locally so an abandoned\n        stream is still accounted for.\"\"\"\n        await self.bucket.take()\n        usage = Usage()\n        started = time.monotonic()\n        try:\n            async with self.sem:\n                async for event in self.transport.stream(messages, model=model, **kw):\n                    if event.delta:\n                        usage.output_tokens += 1\n                        yield event.delta\n                    if event.usage:\n                        usage = event.usage\n        finally:\n            # Runs on cancellation too, which is the whole point.\n            if self.on_usage:\n                self.on_usage(model, usage, (time.monotonic() - started) * 1000)\n```\n\n## What each part is defending against\n\n| Concern | Mechanism | Why it matters |\n| --- | --- | --- |\n| Provider rate limits | Client-side token bucket | Being throttled costs a round trip; self-limiting costs nothing |\n| Overload | Semaphore on concurrency | Unbounded async work exhausts memory and connections |\n| Transient failure | Retry on 429/5xx/timeout | Providers have bad minutes |\n| Thundering herd | **Full jitter** on backoff | Lockstep retries recreate the spike that caused the failure |\n| Permanent failure | `FatalError` never retried | Retrying a 400 wastes four round trips and hides the bug |\n| Hung request | `asyncio.timeout` | The default is often no timeout at all |\n| Cost attribution | `on_usage` callback | You cannot answer \"which feature costs what\" retroactively |\n| Abandoned streams | `finally` block | Cancelled generations are billed and would otherwise vanish |\n\n## The details interviewers look for\n\n- **Full jitter, not `2**n` exactly.** `sleep(random.uniform(0, backoff))` is the AWS-recommended form and it measurably outperforms equal jitter under contention.\n- **Honour `Retry-After`.** The provider knows better than your formula.\n- **Classify errors before retrying.** 400 is your bug; 429 is pacing; 500 is theirs; a timeout may already have been billed and may not be safe to retry if the call had side effects.\n- **Accounting in `finally`.** A cancelled stream still cost money.\n- **Idempotency keys** on anything with side effects, so a retry cannot duplicate.\n- **Never log the full prompt by default.** It is the most common PII leak in an AI codebase.\n\n## What to say if there is time\n\nAdd a circuit breaker (stop calling a provider that has failed N times in a row), a per-tenant bucket rather than a global one, and a hedged request for latency-critical paths — fire a second call at p95 and take the first response. Naming those without being asked reads as production experience.",
      takeaways: [
        "Full jitter on exponential backoff; lockstep retries recreate the outage you are retrying into.",
        "Classify errors — a 400 must never be retried, and a timeout may already have been billed.",
        "Account for usage in a finally block, or abandoned streams disappear from your cost data."
      ],
      followUps: [
        "Add a circuit breaker and explain when it should half-open.",
        "How would you make retries safe for a call with side effects?",
        "What would a per-tenant rate limit change about this design?"
      ],
      trap: "Retrying every exception. A 400 retried four times is four wasted round trips and a bug you have hidden from yourself; the classification is the point of the exercise."
    },

    {
      id: "ai-code-chunker",
      q: "Write a token-aware chunker that respects Markdown structure and adds overlap.",
      topic: "OOP & Code Quality",
      level: "medium",
      companies: ["cohere", "glean", "perplexity", "microsoft", "amazon", "salesforce", "databricks", "sarvam", "adobe"],
      tags: ["coding", "rag", "chunking"],
      aiTrack: true,
      simple: "Cutting a document every 500 characters is easy and wrong. A useful chunker splits on the document's own headings, keeps each piece under a token budget, overlaps neighbours slightly so a fact spanning a boundary survives, and stamps each piece with where it came from.",
      answer: "## The implementation\n\n```python\nimport re\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass Chunk:\n    text: str\n    heading_path: list[str] = field(default_factory=list)\n    start_line: int = 0\n    n_tokens: int = 0\n\n    def with_context(self) -> str:\n        \"\"\"What actually gets embedded: the path, then the body. A chunk\n        reading 'this rose 40%' is unretrievable without its heading.\"\"\"\n        header = \" > \".join(self.heading_path)\n        return f\"[{header}]\\n{self.text}\" if header else self.text\n\n\nHEADING = re.compile(r\"^(#{1,6})\\s+(.*)$\")\n\n\ndef chunk_markdown(md: str, count_tokens, *, max_tokens=600,\n                   min_tokens=80, overlap_tokens=60) -> list[Chunk]:\n    sections, path, buf, start = [], [], [], 0\n\n    def flush(line_no):\n        if buf:\n            sections.append((list(path), \"\\n\".join(buf).strip(), start))\n        buf.clear()\n\n    # 1. Split on headings, carrying the heading path down.\n    for i, line in enumerate(md.splitlines()):\n        m = HEADING.match(line)\n        if m:\n            flush(i)\n            start = i\n            depth = len(m.group(1))\n            path[:] = path[: depth - 1] + [m.group(2).strip()]\n        else:\n            buf.append(line)\n    flush(len(md.splitlines()))\n\n    # 2. Subdivide anything over budget, and merge anything trivially small.\n    out: list[Chunk] = []\n    for hpath, body, line_no in sections:\n        if not body:\n            continue\n        n = count_tokens(body)\n        if n <= max_tokens:\n            out.append(Chunk(body, hpath, line_no, n))\n            continue\n        for piece in _split_paragraphs(body, count_tokens,\n                                       max_tokens, overlap_tokens):\n            out.append(Chunk(piece, hpath, line_no, count_tokens(piece)))\n\n    # 3. Merge runt chunks forward — a 12-token chunk is noise in the index.\n    merged: list[Chunk] = []\n    for c in out:\n        if merged and c.n_tokens < min_tokens and \\\n           merged[-1].heading_path == c.heading_path and \\\n           merged[-1].n_tokens + c.n_tokens <= max_tokens:\n            merged[-1].text += \"\\n\\n\" + c.text\n            merged[-1].n_tokens += c.n_tokens\n        else:\n            merged.append(c)\n    return merged\n\n\ndef _split_paragraphs(body, count_tokens, max_tokens, overlap_tokens):\n    \"\"\"Split on blank lines, never inside a fenced code block or a table.\"\"\"\n    atoms, cur, in_fence = [], [], False\n    for para in body.split(\"\\n\\n\"):\n        if para.lstrip().startswith(\"```\"):\n            in_fence = not in_fence\n        cur.append(para)\n        if not in_fence:\n            atoms.append(\"\\n\\n\".join(cur))\n            cur = []\n    if cur:\n        atoms.append(\"\\n\\n\".join(cur))\n\n    pieces, window = [], []\n    for atom in atoms:\n        window.append(atom)\n        if count_tokens(\"\\n\\n\".join(window)) > max_tokens and len(window) > 1:\n            window.pop()\n            pieces.append(\"\\n\\n\".join(window))\n            # Overlap: carry the tail of the piece into the next window.\n            tail, total = [], 0\n            for a in reversed(window):\n                t = count_tokens(a)\n                if total + t > overlap_tokens:\n                    break\n                tail.insert(0, a); total += t\n            window = tail + [atom]\n    if window:\n        pieces.append(\"\\n\\n\".join(window))\n    return pieces\n```\n\n## What the code is deliberately doing\n\n**Tokens, not characters.** Pass the real tokeniser in. Characters are a proxy that is wrong by 3× between English and Devanagari, and a chunk that overflows the embedding model's window is silently truncated — losing the end of every long chunk without any error.\n\n**Heading path carried onto every chunk.** This is contextual retrieval in its cheapest form and it is the single biggest recall improvement available.\n\n**Never split inside a code fence.** Half a code block is useless in both retrieval and generation. The same argument applies to tables, and extending `_split_paragraphs` to treat a table as one atom is the natural follow-up.\n\n**Overlap by whole paragraphs**, not by characters. Character overlap cuts mid-sentence and produces chunks starting with half a word.\n\n**Runt merging.** A section containing one line becomes a chunk of twelve tokens whose embedding is dominated by noise. Merging it forward is a small step that measurably improves retrieval.\n\n**Stable identity.** In production add a content hash per chunk so re-ingesting an edited document re-embeds only what changed. That is the difference between an ingestion bill that scales with edits and one that scales with corpus size.\n\n## Tests to write in the round\n\n```python\nassert all(c.n_tokens <= 600 for c in chunks)\nassert \"```\" not in \"\".join(c.text for c in chunks) or fences_balanced(chunks)\nassert chunks[0].heading_path == [\"Guide\", \"Setup\"]\nassert overlap_between(chunks[0], chunks[1]) > 0\n```\n\nWriting two or three assertions unprompted is worth more in a practical round than another feature.",
      takeaways: [
        "Count real tokens, not characters — an overflowing chunk is truncated silently.",
        "Carry the heading path onto every chunk; it is the cheapest large recall win available.",
        "Never split inside a code fence or a table, and merge runt chunks forward."
      ],
      followUps: [
        "Extend it to keep a Markdown table as a single atom.",
        "How would you add a content hash so edits re-embed only changed chunks?",
        "How would you adapt this for source code? (Split on function and class boundaries.)"
      ],
      trap: "Splitting by character count. It cuts mid-sentence, mis-sizes chunks across languages by a factor of three, and overflows the embedding window without raising anything."
    },

    {
      id: "ai-code-vector-search",
      q: "Implement top-k cosine similarity search over a million vectors. Then tell me why you would not ship it.",
      topic: "DSA",
      level: "easy",
      companies: ["perplexity", "cohere", "glean", "amazon", "microsoft", "flipkart", "adobe", "tcs", "databricks"],
      tags: ["coding", "retrieval", "numpy"],
      aiTrack: true,
      simple: "Finding the closest vectors is a dot product against everything, then taking the best few. It is ten lines with NumPy. The interesting half of the question is knowing when that stops being acceptable and what replaces it.",
      answer: "## The implementation\n\n```python\nimport numpy as np\n\n\nclass BruteForceIndex:\n    def __init__(self, dim: int):\n        self.dim = dim\n        self.vectors = np.zeros((0, dim), dtype=np.float32)\n        self.ids: list[str] = []\n\n    def add(self, ids: list[str], vecs: np.ndarray):\n        vecs = np.asarray(vecs, dtype=np.float32)\n        assert vecs.shape[1] == self.dim\n        # Normalise ONCE at insert. Then cosine similarity is a plain dot\n        # product and every query avoids recomputing a million norms.\n        vecs = vecs / (np.linalg.norm(vecs, axis=1, keepdims=True) + 1e-12)\n        self.vectors = np.vstack([self.vectors, vecs])\n        self.ids.extend(ids)\n\n    def search(self, q: np.ndarray, k: int = 10, mask: np.ndarray | None = None):\n        q = np.asarray(q, dtype=np.float32).ravel()\n        q = q / (np.linalg.norm(q) + 1e-12)\n\n        scores = self.vectors @ q                    # (N,) one BLAS call\n        if mask is not None:\n            scores = np.where(mask, scores, -np.inf)  # pre-filter, not post\n\n        k = min(k, scores.shape[0])\n        # argpartition is O(N); a full sort would be O(N log N) for nothing.\n        top = np.argpartition(-scores, k - 1)[:k]\n        top = top[np.argsort(-scores[top])]\n        return [(self.ids[i], float(scores[i])) for i in top]\n```\n\n## The three details being checked\n\n**Normalise once at insert.** Cosine similarity is $\\frac{a \\cdot b}{\\|a\\|\\|b\\|}$. Pre-normalising both sides reduces it to a dot product, which turns the query into a single matrix-vector multiply that BLAS executes at memory bandwidth.\n\n**`argpartition`, not `sort`.** You need the top $k$, not a total order. $O(N)$ against $O(N \\log N)$ — on a million vectors that is roughly 20× on the selection step.\n\n**One matrix multiply, not a Python loop.** A loop over a million rows is 100× slower and is the most common way this is written badly.\n\n**The epsilon.** A zero vector — an empty document — produces `nan` without it, and one `nan` poisons the entire ranking silently.\n\n## Performance, honestly\n\n1M vectors × 768 dims FP32 = 3 GB. One query is a 3 GB read: roughly **30–80 ms** on a modern CPU. Fine for a hundred queries a second on a batch job; too slow at ten thousand, and impossible at 50M vectors.\n\n## Why you would not ship it\n\n| Limit | What happens | Replacement |\n| --- | --- | --- |\n| Linear scan | 80 ms → 4 s at 50M vectors | HNSW or IVF-PQ |\n| Memory-resident | 3 GB at 1M, 150 GB at 50M | Product quantisation, or a disk index |\n| No persistence | Lost on restart | A real store |\n| No concurrent writes | Rebuilds the array every insert | An index with incremental insert |\n| No metadata filtering | The mask is a full-array scan | Filtered search inside the index |\n| No hybrid search | Fails on exact tokens and rare codes | BM25 fused with RRF |\n\n## What you would actually do\n\n- **Under ~100k vectors: this is genuinely fine**, and simpler than adding a dependency. Say so — interviewers are pleased by the candidate who does not over-engineer.\n- **100k–1M: pgvector.** It lives in the database you already run, filters against your real columns in the same query, and is transactional.\n- **Above that: a purpose-built ANN index**, and then the real conversation is recall against latency against memory.\n\n## The follow-up to be ready for\n\n\"Make it approximate.\" Sketch IVF: k-means the corpus into $\\sqrt{N}$ cells, store the assignment, and at query time score only the `nprobe` nearest centroids. Twenty lines, an order of magnitude faster, and a recall knob you can measure. Being able to write that on the spot is what turns an easy question into a strong signal.",
      takeaways: [
        "Pre-normalise at insert so every query is a single dot product.",
        "argpartition for top-k; a full sort does asymptotically more work for no benefit.",
        "Brute force is genuinely correct under ~100k vectors — knowing when not to add an ANN index is part of the answer."
      ],
      followUps: [
        "Turn it into an IVF index with a measurable recall knob.",
        "How would you support metadata filtering without scanning everything?",
        "What changes if vectors arrive continuously rather than in one batch?"
      ],
      trap: "Sorting the full score array. It is the reflex, it is asymptotically worse, and argpartition is the one-line improvement the interviewer is waiting for."
    },

    /* ================= behavioural, AI-specific ================= */
    {
      id: "ai-behav-model-wrong",
      q: "Tell me about a time your model was wrong in production. What did you do?",
      topic: "Behavioural",
      level: "medium",
      companies: "*",
      tags: ["behavioural", "reliability", "ownership"],
      aiTrack: true,
      simple: "Every AI system is wrong sometimes. The interviewer is not checking whether you have shipped a perfect system — they are checking whether you noticed, whether you found out why, and whether the same class of failure can happen again.",
      answer: "## What is actually being assessed\n\nFour things, and a good answer covers all four in order:\n\n1. **Detection.** Did you find it, or did a customer? Both are acceptable answers; only one of them is acceptable *twice*.\n2. **Diagnosis.** Did you find the mechanism, or did you add a prompt line and hope?\n3. **Response.** What did you do in the first hour, and what did you do in the following week?\n4. **Prevention.** What is different now, structurally, so that this class of failure is caught?\n\n## The shape of a strong answer\n\n> **Situation.** Our support assistant answered refund questions from a retrieved policy corpus. A customer received an answer citing a policy that had been superseded four months earlier, and we had already told them the wrong thing twice before anyone escalated it.\n>\n> **Detection.** It came in through support, not through our monitoring — which was the first finding. We had faithfulness metrics but nothing measuring whether the *retrieved document was current*.\n>\n> **Diagnosis.** I pulled the traces for the affected conversations. Retrieval was working exactly as designed: the superseded policy was semantically the best match, because the replacement used different wording. Both documents were in the index and nothing marked one as obsolete. The model was faithful to a source that should not have been there.\n>\n> **Immediate response.** Same afternoon: filtered the index by an `effective_to` metadata field so superseded documents could not be retrieved, and had the model surface the document date next to every policy answer so a user could see what it was reading from.\n>\n> **The week after.** Added a document-freshness signal to the ranker, added 30 questions to the golden set specifically covering superseded-policy cases, and worked with the content team so that publishing a replacement marks the previous version rather than leaving both live. That last one was the actual root cause and it was not an engineering problem.\n>\n> **What I would do differently.** I would have asked what happens to old documents during ingestion design. I assumed the corpus was current because nobody said otherwise, and I never tested it.\n\n## What makes it land\n\n- **A specific mechanism**, not \"the model hallucinated\". Naming the retrieval-of-a-stale-document cause shows you diagnosed rather than guessed.\n- **The detection gap admitted plainly.** Saying a customer found it first is not a weakness; pretending your monitoring caught everything is not believable.\n- **A structural fix, not a prompt patch.** Metadata filtering and ranking changes outlast a prompt line.\n- **A non-engineering root cause acknowledged.** Real failures often are, and noticing that is a seniority signal.\n- **A genuine \"what I would do differently\"** that is a decision, not a platitude.\n\n## The version that fails\n\n\"The model hallucinated sometimes so we improved the prompt and added a disclaimer.\" No mechanism, no measurement, no prevention. It reads as someone who has watched failures without ever investigating one.\n\n## If you have not shipped an AI system yet\n\nUse a project, honestly framed. *\"In my RAG project over my university's regulations, I found it answering from the 2019 version because both PDFs were in the index. Here is how I found it and here is what I changed.\"* A real, small, investigated failure beats an invented large one every time — and the interviewer will probe, so it has to be real.",
      takeaways: [
        "Cover detection, diagnosis, response and prevention — most candidates only cover response.",
        "Name the mechanism; \"it hallucinated\" is a description of the symptom, not a diagnosis.",
        "The best answers end with a structural change and an honest thing you would do differently."
      ],
      followUps: [
        "How would you have detected it earlier?",
        "What did you add to the evaluation set as a result?",
        "Has that class of failure recurred?"
      ],
      trap: "Choosing a failure that was somebody else's fault. The interviewer wants your decisions, and a story where you were blameless throughout gives them nothing to assess."
    },

    {
      id: "ai-behav-say-no",
      q: "A senior stakeholder wants an AI feature that should not use AI. What do you do?",
      topic: "Behavioural",
      level: "medium",
      companies: "*",
      tags: ["behavioural", "judgement", "communication"],
      aiTrack: true,
      simple: "A large part of this job is deciding what not to build with a model. Doing that well means arriving with evidence rather than an opinion, offering something rather than only refusing, and accepting the decision gracefully if you are overruled.",
      answer: "## Why the question exists\n\nAI engineers are the only people in the room who know what the model cannot do. A team that cannot say no ships expensive, unreliable features that a rule or a query would have handled — and then spends a year maintaining them. Interviewers ask this because the failure mode is common and costly.\n\nEqually, an engineer who says no reflexively is useless. The question is about *how* you disagree.\n\n## The method\n\n**1. Understand the goal, not the request.** \"Add AI to the invoice screen\" is a solution someone has already chosen. The goal underneath might be *reduce manual data entry*, and that goal may have three solutions, one of which is a regex.\n\n**2. Do not argue — measure.** An opinion loses to a director's opinion. A number does not. Spend a day building the smallest possible evidence:\n\n> *\"I took 200 real invoices. A rules-based extractor gets 94% of the fields right for the 12 vendors that are 80% of our volume, runs in 40 ms, costs nothing and can be debugged. A model gets 96%, takes 2 seconds, costs ₹3 per document, and when it is wrong we cannot say why. Here is the file.\"*\n\nA day of work converts a disagreement into a decision.\n\n**3. Bring an alternative.** \"No\" is not a proposal. \"Rules for the twelve known templates, a model for the long tail, and a review queue for anything either is unsure about\" is — and it often gets you a better version of what they wanted.\n\n**4. Name the real cost, not just the technical one.** Per-request cost, latency, an evaluation harness someone has to maintain, an on-call burden, and a failure mode you cannot fully explain to a customer. Decision-makers respond to ongoing cost more than to accuracy percentages.\n\n**5. Separate the reasonable case for AI.** Sometimes the stakeholder is right and you are pattern-matching. If the input is genuinely unstructured, if the rules would be hundreds and change monthly, or if the value is in handling the long tail — the model is correct. Saying that explicitly makes your objection credible when you do raise one.\n\n## The shape of the story to tell\n\n> A product lead wanted an LLM to categorise support tickets into 14 categories. I asked what accuracy would be needed and what happened when it was wrong — it routed to the wrong team, costing a few hours. So I built both in a day: a TF-IDF and logistic regression baseline on 3,000 historical tickets, and the LLM version. The classical model got 91% at effectively zero cost and 8 ms; the LLM got 93% at ₹0.40 a ticket and 1.2 seconds. On 4,000 tickets a day that was ₹1,600 daily for two points.\n>\n> I showed both, and recommended the classical model with a rule to escalate low-confidence tickets to a human. The product lead agreed immediately — the number made it obvious. We revisited it a year later when a new product line broke the classifier's assumptions, and that time the model was the right answer, which I think was the correct outcome for both decisions.\n\n## If you are overruled\n\nSay this part, because it is what a hiring manager is actually listening for: you state your concern once, clearly, in writing; you ask what evidence would change the decision; and then you build the thing properly and instrument it so the data arrives. Sulking, or building it badly to prove a point, is the behaviour that ends careers. Being right later, with data you collected because you set it up, is the behaviour that gets you trusted.\n\n## The version that fails\n\nA story where you were right, everyone else was foolish, and you won by force of argument. It reads as someone difficult to work with, and it is the most common way this question is failed.",
      takeaways: [
        "Convert the disagreement into a measurement — a day of evidence beats any amount of argument.",
        "Always bring an alternative and name the ongoing cost, not just the accuracy delta.",
        "Say what you do when overruled: state it once in writing, ask what would change the decision, then build it well and instrument it."
      ],
      followUps: [
        "When has a stakeholder been right and you wrong about this?",
        "How do you explain a probabilistic failure mode to a non-technical decision-maker?",
        "What would you put in writing, and what would you leave to the conversation?"
      ],
      trap: "Telling a story where you were purely right and prevailed by arguing. Interviewers are assessing collaboration under disagreement, and a story with no concession in it answers a different question than the one asked."
    }

  ]);
})(window.TD = window.TD || {});
